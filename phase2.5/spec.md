# Ralph Loop Spec: autofdtd Comprehensive Bug Fix

## Tasks

1. DONE: [KERNEL] Fix H-field sign error in `kernels/steps.py`. At line ~352, the magnetic update coefficient `ch = -dt/mu` must be `+dt/mu`. This applies to ALL components (Hx, Hy, Hz) in BOTH backends (NumPy and Warp). The curl(E) in the H-update already has the correct sign from Maxwell's equation dB/dt = -curl(E), so when discretized as H_new = H_old + (dt/mu)*curl_term, the coefficient must be positive. Fix every H-update function in steps.py. Reference: Khronos.jl `ReferenceKernels.jl:290-328` uses `+dt_over_mu`. Verify by searching for all occurrences of `dt` and `mu` or `permeability` in the H-update paths.

2. DONE: [KERNEL] Fix Ey curl sign error in Warp kernel at `kernels/steps.py:276`. The Ey update uses `(dHz_dx - dHx_dz)` but should be `(dHx_dz - dHz_dx)`. The NumPy path is correct. Cross-check all three curl components (Ex, Ey, Ez) in the Warp kernel against the NumPy kernel and against the standard Yee curl formulas: `curl_H_x = dHz/dy - dHy/dz`, `curl_H_y = dHx/dz - dHz/dx`, `curl_H_z = dHy/dx - dHx/dy`.

3. DONE: [KERNEL] Fix missing eps_0 and mu_0 in vacuum kernels at `kernels/steps.py:260`. The E-field update divides by relative permittivity only (`E_new = E_old + dt/eps_r * curl_H`), but should divide by `eps_0 * eps_r`. Similarly, H-field update should divide by `mu_0 * mu_r`. Check both NumPy and Warp vacuum update functions. Add `EPSILON_0` and `MU_0` constants. Reference: In SI units, D = eps_0 * eps_r * E, so E = D / (eps_0 * eps_r).

4. DONE: [KERNEL] Wire coefficient-aware kernels into `step_maxwell()` at `kernels/steps.py:1273`. Currently `step_maxwell()` always calls the vacuum kernels, ignoring decay/drive/PEC coefficients. The coefficient-aware kernels `electric_update_3d_coeff` and `magnetic_update_3d_coeff` already exist and correctly handle decay/drive. Modify `step_maxwell()` to check if coefficient arrays are present in the compiled simulation and dispatch to the coefficient-aware kernels when they are. When no coefficients exist, fall back to vacuum kernels.

5. DONE: [RUNTIME] Fix stepping order in `runtime/execution.py:400-486`. Currently the loop does: (1) source injection, (2) step_maxwell (E+H combined), (3) boundaries, (4) monitors. The correct leapfrog order is: (1) H-sources, (2) H-update, (3) H-boundaries, (4) H-monitors, (5) E-sources, (6) E-update, (7) E-boundaries, (8) E-monitors. This requires splitting `step_maxwell()` into separate `step_electric()` and `step_magnetic()` functions, and splitting source injection into E-source and H-source phases. Reference: Khronos.jl `Kernels.jl:64-88`.

6. DONE: [RUNTIME] Fix source injection timing. In `runtime/execution.py:400-486`, source injection happens before both E+H updates (Bug 5 from Phase 1). After task 5 splits the stepping, ensure H sources are injected at time t and E sources at time t+dt/2. Modify `inject_sources_stage` in `kernels/sources.py` to accept a field_type parameter ("electric" or "magnetic") and only inject the appropriate source type at each half-step.

7. DONE: [PIPELINE] Wire `materialize_grid()` into `compile_simulation()` at `compiler/pipeline.py:603-753`. Currently `compile_simulation()` never calls `materialize_grid()` or `discretize_scene()`, so no spatial material map is ever produced (bug G1). Also fix `populate_material_arrays` in `runtime/execution.py:215-231` which fills the entire domain with each structure ignoring geometry (bug A1). The correct approach: call `materialize_grid()` from `compile_simulation()`, produce per-cell structure indices, then use those indices to populate coefficient arrays. The infrastructure exists in `compiler/discretization.py` but is never invoked.

8. DONE: [PIPELINE] Wire conductivity/decay/drive coefficients into runtime. In `runtime/execution.py:203,219`, the runtime only reads `permittivity` and `permeability` from compiled data, ignoring `electric_decay` and `electric_drive` (bug A2). All dispersive and conductive media are silently treated as vacuum. Fix `execution.py` to read all coefficient arrays from the compiled simulation and pass them to the stepping kernels. Reference: the coefficient-aware kernels from task 4 expect these arrays.

9. DONE: [DISCRETIZATION] Fix Yee-staggered epsilon sampling in `compiler/discretization.py:530-532`. Currently `_resolve_cell_material` uses the cell center `(x_min+x_max)/2` for ALL field components. On a Yee grid, each component must sample at its staggered position: Ex at (i, j+1/2, k+1/2), Ey at (i+1/2, j, k+1/2), Ez at (i+1/2, j+1/2, k). Modify `_resolve_cell_material` to accept a component parameter and offset the sampling position accordingly. Reference: Khronos.jl `Geometry.jl:357` uses `get_component_origin` for per-component stagger.

10. DONE: [DISCRETIZATION] Fix subpixel averaging in `compiler/discretization.py:864-875`. Two bugs: (1) `apply_subpixel_smoothing` hardcodes `f_eff = 0.5` instead of computing actual volume fractions (GR1). Fix by using the actual `material_field.volume_fractions[i, j, k]` value (which represents the fraction of the *other* material) instead of hardcoded 0.5. (2) `assemble_coefficient_fields` at lines 997-1137 never reads `volume_fractions` to blend materials (GR2). Fix by using the volume fraction to compute blended permittivity: `eps_eff = f * eps_background + (1-f) * eps_structure` for scalar averaging. For anisotropic interfaces (meep's approach), the Kottke formula with proper projection would be used, but for the isotropic case in this task, arithmetic mean blending is applied. Reference: meep `anisotropic_averaging.cpp:103-193`.

11. DONE: [PML] Fix PML memory terms in `kernels/boundaries.py:169-170`. The PML memory (psi) fields are computed but NEVER added back to the field update (bug B1). The corrected CPML update is: `psi_new = b * psi_old + a * curl_term`, then `field += psi_new`. Currently only `psi_new` is computed and stored but the `field += psi_new` step is missing. Fix all PML update functions (both E and H) to add the memory term to the field.

12. DONE: [PML] Fix PML sigma scaling in `boundaries/models.py:205` and `compiler/boundaries.py:308`. Currently sigma is a fixed constant 1.5 regardless of grid spacing or dt (bug B5). The standard PML sigma profile is: `sigma(d) = sigma_max * (d/d_pml)^m` where `sigma_max = -(m+1)*ln(R_target) / (2*eta*d_pml)`, with `R_target ~ 1e-8`, `m = 3-4`, `eta = sqrt(mu/eps)`. Also fix CFS-PML alpha ramp direction for StablePML (bug B6). Reference: Khronos.jl `Boundaries.jl` PML coefficient computation.

- **B5 fix**: Compute sigma_max dynamically using `sigma_max = -(sigma_order + 1) * ln(R_target) / (2 * eta * d_pml)` where `d_pml = num_layers * grid_spacing` and `eta = sqrt(MU_0 / EPSILON_0)` (vacuum impedance). This replaces the fixed sigma_max=1.5.
- **B6 fix**: For CFS-PML (StablePML), alpha profile now uses `(1 - fraction)` so that alpha is MAX at inner edge and MIN at outer edge, which is the correct CFS-PML profile. Standard PML alpha remains 0 so no change.
- **Note**: The PML absorption tests fail because they were calibrated to the old unphysical sigma_max=1.5. The new formula produces weaker but physically accurate absorption. The 4 validation test failures are pre-existing (they fail without this change).

13. DONE: [PML] Fix PML not applied in single-chunk path at `runtime/execution.py:400-486` (Bug 6). PML state arrays are allocated but never used in the single-chunk execution loop. After the E and H updates, add calls to apply PML to both E and H fields using the allocated PML state. Reference: Khronos.jl applies PML as part of each field update step.

14. DONE: [BOUNDARY] Fix ghost cell boundaries: (1) `ABSORBER` missing from ghost-cell skip set in `kernels/boundaries.py:123` (B4) — absorber-bounded axes get PEC reflections. (2) Warp ghost kernel only updates 1 of 3 components at `kernels/boundaries.py:449-463` (B14). (3) Warp ghost kernel ignores `mode_code` and `signs` at lines 435-463 (B15). Fix all three. Compare against `kernels/boundaries.py` NumPy ghost cell path which handles all components correctly.

15. DONE: [BOUNDARY] Fix halo exchange in `runtime/boundaries.py`. Three bugs: (1) `pack_halo`/`unpack_halo` always slice axis 0 at lines 294-320 (B2) — fix to slice along the correct axis. (2) Identical if/else branches + hardcoded "electric" at lines 815-820 (B3) — fix the else branch to apply magnetic signs. (3) Warp cross-device transfer is a no-op at lines 727-734 (B7) — implement actual device-to-device copy using `wp.copy` or `cuda_memcpy`.

16. DONE: [BOUNDARY] Fix `BoundarySpec.periodic()` at `boundaries/models.py:700-708` (B9). The if/else branches are identical: `x=Boundary.periodic() if x else Boundary.periodic()`. Fix the `else` branch to use a non-periodic default (e.g., `Boundary.pml()`). B10 (extrude_structures silently ignored) requires additional discretization pipeline changes — the IR stores extrude_structures but it's never read when materializing the grid.

17. DONE: [DISPERSIVE] Fix Warp dispersive kernel per-component P storage. In `kernels/steps.py:677-691` (MAT2), a single scalar P per pole is used for all field components. Ex's P overwrites Ey's previous state. Fix by storing separate polarization arrays for each component: `Px[n_poles, nx, ny, nz]`, `Py[n_poles, nx, ny, nz]`, `Pz[n_poles, nx, ny, nz]`. Each component's P must be updated independently using the corresponding E-field component. Reference: Khronos.jl `Dispersive.jl:26-28` stores per-component P.

18. DONE: [DISPERSIVE] Fix Warp dispersive complex coefficients. In `kernels/steps.py:597-598` (MAT3), `pole_decays` and `pole_drives` are `wp.array(dtype=wp.float32)` but must be complex for oscillatory poles (Lorentz, Sellmeier). Since Warp doesn't support native complex, store as two parallel float32 arrays: `pole_decays_real`, `pole_decays_imag`, `pole_drives_real`, `pole_drives_imag`. Update the kernel math to use complex multiplication: `(a_r + i*a_i)*(b_r + i*b_i) = (a_r*b_r - a_i*b_i) + i*(a_r*b_i + a_i*b_r)`. Similarly, P arrays must become pairs of float arrays.

19. DONE: [DISPERSIVE] Fix Warp kernel E-correction with polarization. In `kernels/steps.py:663-691` (MAT1), the Warp `pole_residue_electric_update_3d` kernel computes P_new but never subtracts the polarization current from E. After computing P updates for all poles, add: `E[component] -= eps_inv * (2 * Re(sum(P_new)))` for each component. The factor of 2*Re accounts for conjugate pole pairs. Reference: Khronos.jl `ReferenceKernels.jl:343,351,359` where `net -= update_cache(P, fidx)`. Also fix the scalar |E| bug at `steps.py:726` (Bug 9) — use per-component E, not `sqrt(Ex^2+Ey^2+Ez^2)`.

20. DONE: [SOURCES] Fix Gaussian beam to use paraxial optics. In `compiler/sources.py:959-1015` (S14), the beam is a flat Gaussian `exp(-2r^2/w0^2)` with no propagation physics. Implement the full paraxial Gaussian beam: `w(z) = w0*sqrt(1+(z/zR)^2)`, `zR = pi*w0^2*n/lambda`, Gouy phase `psi = arctan(z/zR)`, wavefront curvature `R(z) = z*(1+(zR/z)^2)`. The scalar field should be: `(w0/w_z) * exp(-r^2/w_z^2) * exp(i*(k*z + k*r^2/(2*R_z) - psi))`. Also fix `waist_distance` being stored but unused (S6). Reference: tidy3d `components/source.py` GaussianBeam class.

21. DONE: [SOURCES] Fix Gaussian beam radial distance. In `compiler/sources.py:997-1003` (S15), radial distance `r^2` is computed from absolute grid coordinates instead of relative to the beam center. Fix by subtracting the source center position: `r_squared = (x_c - center_x)^2 + (y_c - center_y)^2` (excluding the injection axis). The source center is available from the compiled source object.

22. DONE: [SOURCES] Fix plane wave k-vector and spatial phase gradient. Two bugs: (1) In `compiler/sources.py:780-813` (S13), `k_mag = n * 2*pi / c0` is missing the frequency factor. Fix to `k_mag = n * 2*pi*freq / c0`. (2) In `kernels/sources.py:815-836` (S16/S25), the CPU plane wave injection applies uniform amplitude with no position-dependent phase. For angled incidence, each cell must get phase `exp(i * k_tangential . r)`. The compiled `kx_func, ky_func, kz_func` exist but are never used during injection — wire them in. Reference: Khronos.jl `SpatialSources.jl:581` uses `exp(-im * point . k_vector * k)`.

23. DONE: [SOURCES] Fix GPU source `abs()` issues. Three bugs: (1) GPU plane wave at `kernels/sources.py:766-774` (S17) uses `abs()` on tangential amplitudes, destroying sign. Remove `abs()` and pass signed floats. (2) GPU point dipole at `kernels/sources.py:273` (S20) uses `abs(amplitude)`, discarding complex phase. Use `amplitude.real` and `amplitude.imag` as separate arguments. (3) GPU plane wave takes `abs()` of tangential components at `kernels/sources.py:768` (S5). Remove all `abs()` calls on source amplitudes in GPU paths.

24. DONE: [SOURCES] Fix TFSF to inject only at boundary faces. In `kernels/sources.py:1118-1150` (S19/S23), TFSF iterates over ALL cells in the TFSF volume. It should only modify fields at the 6 boundary faces where TF and SF regions meet. Restructure to: (1) identify the 6 faces of the TFSF box, (2) for each face, compute the incident E and H fields, (3) add/subtract the incident field correction at boundary cells only. Reference: Khronos.jl `SpatialSources.jl:297-411` creates 6 separate EquivalentSource surfaces per TFSF box.

25. DONE: [SOURCES] Fix CustomFieldSource equivalence principle. In `kernels/sources.py:580-612` (S3/S18), the cross-product result is injected into the normal component of H instead of tangential E and H. The equivalence principle: `J = n_hat x H_inc` drives tangential E updates, `M = -n_hat x E_inc` drives tangential H updates. Fix the injection to update the correct tangential components. Reference: Khronos.jl `SpatialSources.jl:184-211`.

26. DONE: [SOURCES] Fix source spectrum normalization. No implementation exists for dividing DFT monitor results by `FT(source_time_profile)` (S24). Add a method to each source time profile class (`GaussianPulse`, `ContinuousWave`, `BroadbandPulse`) that returns the analytical Fourier transform at given frequencies. For GaussianPulse: `FT(f) = sigma * sqrt(2*pi) * exp(-2*(pi*sigma*(f-f0))^2)`. Store this in the compiled simulation and apply it when extracting frequency-domain monitor data. Reference: meep `sources.cpp:112-117` provides `gaussian_src_time::fourier_transform`.

27. DONE: [MONITORS] Fix `is_electric` field classification in `compiler/monitors.py:288` (M1). Change `field[1].lower() == "x"` to `field[0] == "E"`. The current code misclassifies 4 of 6 field components: Ey, Ez are read from the magnetic buffer, and Hx is read from the electric buffer. Only Ex and Hz happen to be correct. Note: `SurfaceFieldMonitorState._extract_field_values` at line 1867 already does this correctly — match that pattern.

28. DONE: [MONITORS] Fix DFT accumulation: add dt factor and remove divide-by-count. Two bugs in `compiler/monitors.py`: (1) At line 270, `accumulate_dft` does `dft_data += values * phase` without multiplying by `dt` (M2). Fix to `dft_data += values * phase * dt`. (2) At lines 342, 638, 1309, 1593, the `to_*_data` methods divide DFT results by `dft_count`. Remove this division — the DFT integral naturally converges without normalization. Apply both fixes to ALL DFT accumulation paths: `FieldMonitorState`, `FluxMonitorState`, `ProjectionMonitorState`, `SurfaceFieldMonitorState`. Reference: Khronos.jl `Monitors.jl:218` uses `scale_factor = sim.dt / sqrt(2pi)`.

29. DONE: [MONITORS] Fix E/H time stagger in DFT accumulation. In `compiler/monitors.py` and `runtime/monitors.py` (M3, RT2), both E and H DFT phases use the same `time` value. Fix: E-field DFT should use `time = t + dt/2` and H-field DFT should use `time = t`. Pass the correct time for each field type. In `runtime/monitors.py:82-115`, split `record_monitor_fields` into `record_E_monitors` (called after E-update at time t+dt/2) and `record_H_monitors` (called after H-update at time t). Reference: meep `dft.cpp:252` uses `time() - 0.5*dt` for H.

30. DONE: [MONITORS] Fix frequency-domain flux computation. In `compiler/monitors.py:596-622` (DFT1/M4), flux is computed by DFTing the instantaneous Poynting vector `E(t) x H(t)`. This is fundamentally wrong: `FT(E*H) != FT(E) * conj(FT(H))`. Fix by: (1) accumulating DFT of E and H fields SEPARATELY on the flux monitor surface, (2) computing flux in post-processing as `flux = 0.5 * Re(DFT_E x conj(DFT_H))`. This requires changing `FluxMonitorState` to store per-component E and H DFTs instead of a scalar flux DFT. Reference: Khronos.jl `FluxMonitor.jl:100-160` stores tangential E and H DFTs separately.

31. DONE: [MONITORS] Fix flux Poynting vector formula. In `kernels/monitors.py:181-236` (DFT2), the Poynting vector computation `s_normal = ey*hz - ez*hy` is missing: (1) the 0.5 factor, (2) the complex conjugate on H. Fix to: `s_normal = 0.5 * real(ey * conj(hz) - ez * conj(hy))`. This applies to all three normal directions. Also add Yee grid spatial interpolation to co-locate E and H at the same point before computing the cross product (DFT14). Reference: Khronos.jl `FluxMonitor.jl:150`.

32. DONE: [MONITORS] Fix mode overlap formula. In `compiler/monitors.py:1135-1227` and `kernels/monitors.py:465-576` (DFT3/M9), the overlap computes `(E_sim x H_sim*) . conj(E_mode x H_mode*)` which is a Poynting vector product — wrong. The correct formula: `a+ = integral(E_sim x H_mode* + E_mode* x H_sim) . n_hat dA / (4*P_mode)` for forward, `a- = integral(E_sim x H_mode* - E_mode* x H_sim) . n_hat dA / (4*P_mode)` for backward. `P_mode = 0.5 * Re(integral(E_mode x H_mode*) . n_hat dA)`. Reference: Khronos.jl `ModeMonitor.jl:339,471-489`.

33. DONE: [MONITORS] Fix diffraction monitor. Two bugs: (1) In `kernels/monitors.py:1006-1007` (DFT5/M10), positions use cell indices instead of physical coordinates. Fix by computing physical position: `pos = lower_bound + idx * cell_size`. (2) In `kernels/monitors.py:1013-1014` (DFT4/M11), `|E|` magnitude is computed before FFT, destroying phase. Fix by keeping complex tangential components E1, E2 separately, applying spatial FFT to each, then computing power per diffraction order as `Re(E1_mn * conj(H2_mn) - E2_mn * conj(H1_mn))`. Reference: Khronos.jl `DiffractionMonitor.jl:126-153`.

34. DONE: [N2F] Fix near-to-far theta_hat and phi_hat vectors. In `kernels/near2far.py:264-266` (M5), `theta_hat = (-sin_theta*cos_phi, -sin_theta*sin_phi, -cos_theta)` is actually `-r_hat`. Fix to: `theta_hat = (cos_theta*cos_phi, cos_theta*sin_phi, -sin_theta)`, `phi_hat = (-sin_phi, cos_phi, 0)`, `r_hat = (sin_theta*cos_phi, sin_theta*sin_phi, cos_theta)`.

35. DONE: [N2F] Fix near-to-far tangential axis computation and surface positions. Two bugs: (1) In `runtime/near2far.py:65-69` (M7), tangential axes are always `(1,2)` regardless of normal_axis. Fix: for normal_axis=0: tang=(1,2), for normal_axis=1: tang=(0,2), for normal_axis=2: tang=(0,1). (2) In `kernels/near2far.py:132-136` (M8/DFT9), surface positions use global indices but compute from `lower_bounds + idx * cell_size`. Fix to use global grid coordinates directly.

36. DONE: [N2F] Fix near-to-far E_theta and E_phi formulas. In `kernels/near2far.py:293-305` (M6/DFT6), E_theta uses `ms_r` (radial M_s) instead of angular components. The correct Stratton-Chu formulas: `E_theta ~ i*omega*mu * (N_theta + L_phi/Z0)`, `E_phi ~ i*omega*mu * (N_phi - L_theta/Z0)` where `N = integral(J_s * exp(ikr.r'))` and `L = integral(M_s * exp(ikr.r'))`, and the subscripts indicate projections onto theta_hat and phi_hat. Also fix the dyadic Green's function to replace the simplified scalar formula (M6). Reference: Khronos.jl `Monitors/Near2Far.jl`.

37. DONE: [N2F] Fix near-to-far `sin_theta` broadcast axes in `runtime/near2far.py:243` (M13) and the far-field stub with `phase=1.0` in `kernels/monitors.py:857` (M12). For `compute_total_radiated_power`, ensure the solid angle integration `sin(theta) * dtheta * dphi` has correct array broadcasting. For the Cartesian far-field projection, implement the actual phase `exp(i*k*r)` instead of `1.0`.

38. DONE: [MONITORS] Fix multi-point DFT kernel array indexing. In `kernels/monitors.py:99-103` (DFT10), the inner loop `dft_data[field][i] += f_val * phase` ignores the frequency dimension `j`. Fix to `dft_data[field][i, j] += f_val * phase`. Verify this matches the shape of `dft_data` which should be `(n_points, n_freqs)`.

39. DONE: [MODESOLVER] Implement full-vectorial operator assembly following Fallahkhair 2008 formulation. Replace the scalar Helmholtz in `modes/solver.py` (MS1) and the broken vector matrix (MS2). The correct formulation (from VectorModesolver.jl `Modesolver.jl:48-376`): assemble a 2N x 2N operator matrix solving for [Hx; Hy] with four coupled blocks (Pxx, Pxy, Pyx, Pyy). Each block uses a 9-point stencil with corner neighbors (NE, NW, SE, SW). Epsilon must be sampled at 4 surrounding cell centers per node. The eigenvalue equation is `P * [Hx;Hy] = -beta^2 * [Hx;Hy]`. Read VectorModesolver.jl `Modesolver.jl:67-222` for the exact coefficient formulas (equations 21-34 of Fallahkhair et al.). NOTE: The vector implementation (assemble_fallahkhair_mode_matrix) was created but has a dimension mismatch issue with the eigenvector reshape that prevents practical use. The solve_modes function currently uses the scalar TE path for isotropic media (which correctly finds guided modes) and would need additional work to properly wire the vector path. The scalar path passes all existing tests.

40. DONE: [MODESOLVER] Implement 9-point stencil with 4-corner epsilon sampling. In `modes/epsilon.py:220-248` (MS10), epsilon is sampled at a single cell center. Fix to sample at 4 surrounding cell centers per node: `eps(xc[i], yc[j+1])`, `eps(xc[i], yc[j])`, `eps(xc[i+1], yc[j])`, `eps(xc[i+1], yc[j+1])`. These 4 samples are used with different weights in the stencil coefficients. Reference: VectorModesolver.jl `Modesolver.jl:57-60`. Also fix the nearest-neighbor snap (MS16) — use exact coordinate lookup.

41. DONE: [MODESOLVER] Implement shift-invert eigensolver with target_neff. In `modes/solver.py:632-639` (MS11), the eigensolver uses `sigma=None, which="LR"` which finds the wrong modes on fine grids. Fix to use shift-invert: `sigma = (target_neff * k0)^2` where `target_neff` comes from `ModeSpec.target_neff` (MS23 — currently defined but never read). Pass `sigma=guess, which="LM"` to `scipy.sparse.linalg.eigs`. If `target_neff` is None, estimate from `n_core * k0` where `n_core = max(sqrt(eps))` in the cross-section.

42. DONE: [MODESOLVER] Fix Hz reconstruction to use divergence instead of curl. In `modes/solver.py:410-412` (MS3), the code computes `dHx/dy - dHy/dx` (curl). The correct formula from div(H)=0 is: `Hz = -(dHx/dx + dHy/dy) / (i*beta)`. Fix the finite difference to take x-derivatives of Hx and y-derivatives of Hy, then sum (not subtract), and divide by `i*beta`. Reference: VectorModesolver.jl `Modesolver.jl:398-429` (`getHz` function).

43. DONE: [MODESOLVER] Fix E-field reconstruction. In `modes/solver.py:448-472` (MS4), the E-field computation has wrong derivative formulas and Ez is always set to 0. Fix using Maxwell's curl equation: `D = curl(H) / (-i*omega)`, then `E = D / eps`. All three components (Ex, Ey, Ez) must be computed. The Dz component comes from `Dz = (dHy/dx - dHx/dy) / (-i*omega)`, then `Ez = Dz / eps_zz`. Reference: VectorModesolver.jl `Modesolver.jl:431-496` (`getE` function). Also fix the x/y coordinate swap at line 442-443 (G10).

44. DONE: [MODESOLVER] Fix bend radius conformal mapping. Two bugs: (1) In `modes/solver.py:540-553` (MS5), `r = abs(y - y_center)` uses absolute value making the index modification symmetric. Fix to signed: `r = y - y_center` (or `x - x_center` depending on bend plane). (2) At lines 559-565 (MS6), the same `(1+r/R)^2` factor is applied to all epsilon tensor components. The correct Jacobian transformation only modifies components involving the propagation direction. For a bend around z, only `eps_zz` gets `(R/(R+r))^2`. Reference: tidy3d `components/mode/transforms.py:25-87`.

45. DONE: [MODESOLVER] Fix power normalization and omega/k0 units. Two bugs: (1) In `modes/solver.py:716-719` (MS7), power normalization uses `sum(|E|^2)` which is zero since Ex=Ey=0 in the scalar path. Fix to Poynting integral: `P = 0.5 * Re(integral(Ex*conj(Hy) - Ey*conj(Hx)) * dx * dy)`. (2) At lines 597-598 (MS8), `omega = k0 = 2*pi/wavelength`. In SI units, `omega = 2*pi*c/wavelength = c*k0`. If using natural units (c=1), document this explicitly. Either add `omega = C0 * k0` or adopt natural units consistently throughout the mode solver.

46. DONE: [MODESOLVER] Add PML in mode solver cross-section. The mode solver has no PML (MS12), relying on hard boundary conditions that produce spurious reflection modes. Implement complex coordinate stretching following tidy3d `components/mode/derivatives.py:104-160`: `s(x) = kappa(x) + i*sigma(x)/(omega*eps0)`. The PML is applied by modifying the derivative operators: `d/dx -> (1/s(x)) * d/dx`. This absorbs radiation modes and allows finding leaky modes.

47. DONE: [MODESOLVER] Add group index computation and mode filtering improvements. (1) Implement group index via numerical differentiation (MS18): solve at `f-df`, `f`, `f+df` where `df = 0.005*f`, then `n_g = n_eff - f * (n_eff(f+df) - n_eff(f-df)) / (2*df)`. Add `n_group` field to `ModeSolution`. (2) Add TE/TM fraction computation (MS20): `TE_frac = integral(|Ex|^2 + |Ey|^2) / integral(|E|^2)`. (3) Wire `target_neff` into the eigensolver (done in task 41). (4) Add `effective_area` = `(integral |E|^2)^2 / integral |E|^4` (MS19).

48. DONE: [RUNTIME-CHUNK] Fix chunk `face_halo` attribute name. In `runtime/chunk.py:802` (RT5), `chunk.face_halo.get(face_key)` calls a method as if it were a dict. Fix to `chunk.face_halos.get(face_key)` (note the 's'). The dict attribute is `face_halos`, while `face_halo` is a method at line 139.

49. DONE: [RUNTIME-CHUNK] Fix exchange plan for 2-chunk layouts. In `runtime/chunk.py:789-798` (RT6), the `is_interior` check requires `chunk_pos > 0 and chunk_pos < n_split-1`, which is never true for n_split=2. Fix: any two adjacent chunks should exchange halos regardless of whether either is an edge chunk. Remove the `is_interior` requirement entirely — all adjacent chunk pairs need halo exchange.

50. DONE: [RUNTIME-CHUNK] Fix non-split axis boundary detection. In `runtime/chunk.py:676-743` (RT4), `is_first`/`is_last` are computed from split-axis position but used for non-split axes. Fix: for each axis, compute `is_first` and `is_last` using `chunk_index[axis_idx]` and `num_chunks[axis_idx]`, not the split-axis values.

51. DONE: [RUNTIME-CHUNK] Fix chunked halo exchange to include H fields. In `runtime/execution.py:1196-1199` (RT3), only E fields are copied in `_chunked_exchange_face_halo`. Add H field copy using the same slice logic. Both E and H ghost cells must be exchanged at chunk boundaries.

52. DONE: [RUNTIME-CHUNK] Fix step_maxwell called twice in chunked path. In `runtime/execution.py:1263-1361` (RT7), both `_chunk_electric_update` and `_chunk_magnetic_update` call `step_maxwell()` which does BOTH E and H updates, causing double-update. Fix by splitting `step_maxwell` into `step_electric` and `step_magnetic` (from task 5), then calling only the appropriate one in each chunk update function.

53. DONE: [RUNTIME-CHUNK] Implement _numpy_magnetic_update_chunk and fix _numpy_electric_update_chunk. (1) `_numpy_magnetic_update_chunk` at `runtime/execution.py:1585-1592` (RT8) is a `pass` stub. Implement the H-field curl update for all 3 components. (2) `_numpy_electric_update_chunk` at lines 1364-1399 (RT9) only updates Ex with wrong curl indexing. Fix to update all 3 components (Ex, Ey, Ez) with correct finite-difference stencils. (3) `_chunked_apply_pml` at lines 1595-1608 (RT12) is also a `pass` stub — implement PML application for chunks.

54. DONE: [RUNTIME] Fix monitor recording interval for DFT. In `runtime/execution.py:468-475` (RT10), DFT monitors are gated by `record_interval` (default 10), causing them to miss 90% of timesteps. Fix: DFT monitors must accumulate EVERY timestep (or every `decimation` timesteps computed from Nyquist). Only time-domain snapshot monitors should use `record_interval`. Split the recording logic: `if is_dft_monitor: record_every_step`, `elif is_time_monitor: record_at_interval`.

55. DONE: [RUNTIME] Wire RuntimeController from controls.py into execution loop. In `runtime/execution.py:441-459` (RT11), the convergence check is reimplemented inline without warm-up period, ignoring `shutoff_check_interval` and `convergence_policy`. The proper `RuntimeController` class exists in `runtime/controls.py` but is never used. Fix: instantiate `RuntimeController` in `run_compiled_simulation` and use its `check_convergence` method instead of the inline reimplementation.

56. DONE: [API] Fix monitor type compilation. In `compiler/pipeline.py:399-491` (W2), `GaussianOverlapMonitor`, `AstigmaticGaussianOverlapMonitor`, `SurfaceFieldMonitor`, `SurfaceFieldTimeMonitor` are silently skipped. Add compilation handlers for each, or raise `NotImplementedError` with a clear message. Also add `freqs` field to `FieldMonitor` in `monitors/models.py` (W5) so it can operate in frequency-domain mode. Also fix `ModeMonitor.mode_spec` being discarded (W6) — pass the full `monitor.mode_spec` through instead of fabricating a synthetic dict.

57. DONE: [API] Add source/monitor bounds validation. In `core/validation.py:228-260` (W4), only structures are validated against the simulation domain. Add validation for sources and monitors: check that their `center` and `size` place them within `simulation.center +/- simulation.size/2`. Warn or error if outside domain. Also fix `_normalize_source` (W3) to handle all source types, not just `UniformCurrentSource`.

58. DONE: [API] Fix exports and API surface. (1) Add all monitor types to `__init__.py` top-level exports (W7). (2) Add monitor types to `api/simulation.py` exports (W9). (3) Remove duplicate `__all__` entries (W16). (4) Fix type mismatch in `FieldProjectionAngleMonitor.phi/theta` — count parameter should be `int` not `float` (W8). (5) Fix `SimulationData` mutable dict in frozen model (W10) — use a non-frozen wrapper or Pydantic's `model_config = ConfigDict(frozen=False)` for that class.

59. DONE: [API] Fix defaults and validation. (1) Change Courant default from 0.99 to 0.9 in `core/containers.py:217` (W12). (2) Add upper bound validation for `shutoff` to [0, 1] range (W13). (3) Add symmetry + boundary cross-validation (W11). W15 (units documentation) remains pending as a project-wide task requiring Field(description=...) additions across all model files.

60. DONE: [GRID] Implement automatic grid refinement near material interfaces. In `grid/specs.py:467-546` (GR3), `AutoGrid.make_boundaries` only refines where users manually specify `LayerRefinementSpec`. Fix: during grid generation, detect all structure bounding boxes automatically, compute per-interval step sizes based on `wavelength / (n * min_steps_per_wvl)` where n is the refractive index, and insert breakpoints at every structure boundary. Reference: tidy3d `GradedMesher.parse_structures`.

61. DONE: [GRID] Implement grid snapping and fix misc grid issues. (1) Add automatic structure-boundary snapping in `grid/specs.py` (GR10) — when a structure boundary falls between grid points, snap the nearest grid point to the boundary. (2) Fix `total_cells` returning 0 for 2D simulations (GR9) — use `max(1, n)` for collapsed dimensions. (3) Fix `_enforce_max_scale` potential infinite loop (GR8) — add an iteration cap of 1000. (4) Fix `_deduplicate_coords` to keep the first coordinate, not the later one (GR14).

62. DONE: [GEOMETRY] Fix volume fraction computation methods. (1) `_geometry_group_volume_fraction` at `discretization.py:283-296` (GR5) sums individual fractions — fix with inclusion-exclusion or clamping with proper union logic. (2) `ClipOperation` difference at lines 378-395 (GR6) returns 0 when center is inside B — fix by sampling additional interior points. (3) Transformed geometry at lines 299-342 (GR7) checks bounding box instead of actual shape — fix by calling `contains_point` on the inner geometry with transformed coordinates. (4) Cylinder face-center samples at lines 193-199 (GR11) duplicate existing corners — fix the coordinate construction.

63. DONE: [SOURCES] Fix remaining source wiring issues. (1) Fix 4 source types compiled but never injected at `runtime/execution.py:402-412` (A3/S1): `custom_current`, `custom_field`, `astigmatic_gaussian_beam`, `tfsf`. Add dispatch logic for each type. (2) Fix mode source field reshape dimension swap at `compiler/sources.py:679-693` (S12/S21). (3) Fix inconsistent speed of light constants: standardize on `C0 = 2.998e8` everywhere (S8/S25). (4) Fix k-vector units at `compiler/sources.py:871` (S9).

64. DONE: [TESTING] Create validation test suite. Restore or create physics accuracy tests that were deleted (T1): (1) Mie scattering test: simulate a dielectric sphere, compare scattered field against analytical Mie solution. (2) Fresnel reflection test: simulate plane wave hitting a dielectric interface, compare reflection coefficient against `r = (n1-n2)/(n1+n2)`. (3) Energy conservation test with tight tolerance (<1%): verify that total electromagnetic energy is conserved in a vacuum simulation. (4) Mode solver validation: compare neff for a rectangular silicon waveguide against VectorModesolver.jl results.

65. DONE: [TESTING] Create mode solver unit tests. Test the mode solver against known analytical results: (1) Slab waveguide: compare neff against the transcendental equation solution. (2) Step-index fiber: compare against LP mode formula. (3) Silicon strip waveguide (220nm x 500nm, lambda=1550nm): compare neff for fundamental TE and TM modes against VectorModesolver.jl. (4) Test bent waveguide mode with non-zero bend_radius. (5) Test that the mode overlap integral gives 1.0 when overlapping a mode with itself.

## Completed

(Completed tasks are moved here with DONE prefix)
