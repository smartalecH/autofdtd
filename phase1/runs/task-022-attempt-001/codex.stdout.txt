OpenAI Codex v0.118.0 (research preview)
--------
workdir: /workspace/autofdtd
model: gpt-5.4
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: none
reasoning summaries: none
session id: 019d6452-3ccb-71b3-97bb-43b6164c9b55
--------
user
# Phase 1 Maxwell Solver Loop

## Mission

Drive Phase 1 of autofdtd from an almost-empty repository to a credible, pip-installable, GPU-first FDTD package whose feature plan is anchored explicitly to the local Tidy3D EM API surface. The loop must produce a Tidy3D-inspired public API, a tagged execution IR, a scene-compilation pipeline, a chunk-based multi-node-ready runtime, Warp kernel conventions, concrete implementations for the highest-priority Tidy3D feature families, validation examples, and documentation without copying protected code or docs from any reference project.

## Operator Policies

- Failure policy: `retry`
- Permission mode: `danger-full-access`
- End-of-list behavior: `stop cleanly`
- Codex working directory: `..` (repo root relative to `phase1/`)

## Global Execution Rules

- Read relevant files before editing.
- Keep progress notes concise and factual.
- Treat `loop.spec.md` as the queue source of truth.
- Report output that matches `output.schema.json`.
- Use `completed` only when the task's success criteria were actually satisfied.
- Use `needs_retry` when meaningful work was done but the success criteria are not yet fully met.
- Use `blocked` only when an external dependency or missing input prevents further progress.
- Use `failed` when the attempt is unusable or the task direction was not successfully carried out.
- Prefer producing concrete artifacts and findings over generic summaries.
- Keep `working-memory.md` concise and deduplicated.
- Do not repeat reference material in `working-memory.md`; point to local files when possible.
- Prefer recording file paths, decisions, and distilled facts instead of copying long passages.
- Do substantial reference-backed work before implementation and keep updating working-memory.md with distilled decisions that later tasks should inherit.
- Treat Tidy3D compatibility as a typed tagged object-model and lowering problem, not as a loose dict compatibility exercise.
- Treat the local Tidy3D EM feature surface as the gold standard for feature planning and error-policy decisions in Phase 1.
- Keep the mathematical stage graph separate from execution scheduling so bulk-synchronous stepping, overlap, and future systolic variants can coexist.
- Separate scene modeling, scene compilation, runtime stepping, and IO or packaging into distinct subsystems.
- Use NVIDIA Warp for GPU kernel implementation work and design kernels around stable module contents, persistent arrays, capture-safe stepping, and intentional instrumentation.
- Do not build a monolithic everything-kernel. Prefer staged update kernels and capability-driven specialization by region or chunk.
- Every substantive implementation task should include or update tests, examples, benchmarks, or instrumentation that exercise the new behavior.
- When a Tidy3D feature is not yet supported, implement or document explicit validation or error behavior rather than leaving silent gaps.
- Record initialization time, JIT time, warm steady-state timestep metrics, and cells-updated-per-second style metrics whenever solver execution paths become runnable.
- Use local reference repos and papers aggressively for research, but do not copy protected code or documentation into this repository.
- Assume the current machine has at least two NVIDIA GPUs available and use that for meaningful smoke tests where feasible.

## Output Contract

- `summary` should be a concise outcome statement.
- `key_findings` should capture the most important concrete discoveries or decisions.
- `artifacts` should list meaningful outputs with both `path` and `description`.
- `error_summary` should be filled for `blocked` or `failed` runs and empty otherwise.
- `next_action` should be `retry`, `human_review`, or `none`.
## Reference Material

### Reference 1

Repository baseline at bundle generation time: the `autofdtd` repo has a minimal README, phase directories, and an effectively empty src tree. Phase 1 therefore starts from architecture, package skeleton, and subsystem contracts rather than refining an existing solver. The prep-generated feature ledger lives at `phase1/feature-checklist.md` and should be treated as input context for the loop, not as a loop deliverable.

### Reference 2

Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.

### Reference 3

Tidy3D compatibility guidance for Phase 1: keep the public and IR models tagged, immutable, schema-driven, and validation-heavy; preserve named monitor access patterns and ordered structure semantics; and front-load an explicit feature matrix that distinguishes must-implement features, defer buckets, and reject-with-clear-error buckets rather than leaving gaps ambiguous.

### Reference 4

Meep architectural findings: the stable decomposition unit is the chunk rather than the rank; a rank may own multiple chunks; chunk ownership, symmetry reduction, and periodic or Bloch remapping behave like logical address transforms over a full-domain view; communication plans are stage-specific and component-family-specific; geometry and material initialization are separate from stepping; and near-to-far is a DFT-backed monitor family rather than a lightweight add-on. The Meep paper sharpens this further: the solver is designed to preserve an illusion of continuous space and time on top of chunked discrete execution, uses pervasive interpolation and restriction for sources, monitors, and field queries, relies on anisotropic subpixel smoothing to remove first-order interface error, and favors bulk region operations over pointwise APIs. High-value references: `../meep/doc/docs/Parallel_Meep.md`, `../meep/doc/docs/Chunks_and_Symmetry.md`, `../meep/src/structure.cpp`, `../meep/src/boundaries.cpp`, `../meep/src/fields.cpp`, `../meep/src/loop_in_chunks.cpp`, `../meep/src/near2far.cpp`, and `papers/meep_paper.pdf`.

### Reference 5

Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.

### Reference 6

Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.

### Reference 7

Geometry and mode-solver findings: GeometryPrimitives.jl is centered on analytic shape queries such as level, nearest surface point, normals, bounds, and translation rather than a full scene graph. Orthogonal prisms and convex polygons are natural early scope; subpixel support exists mainly through local plane-cut overlap ideas rather than a full arbitrary-shape averaging engine; KD-tree and periodic replication patterns matter early; and libctl provides richer object-model and overlap-integral precedents for material-carrying ordered geometry lists and periodic duplication. VectorModesolver.jl is best treated as a formulation and interface reference rather than a polished dependency surface. It expects a sampled tensor-valued permittivity callback over a rectilinear cross-section, assembles a sparse operator, solves for magnetic fields, and reconstructs electric fields.

### Reference 8

Working-memory seed facts that later tasks should preserve: chunk is the architectural unit while rank or device is placement; stored topology and logical topology diverge because of symmetry and periodic or Bloch remapping; scene compilation must remain distinct from stepping; continuous problem specifications must lower through explicit interpolation and restriction into discrete execution artifacts; geometry lowering and materialization must precede serious kernels; subpixel smoothing can induce effective anisotropy even for physically isotropic materials; stage-specific halos and monitor metadata should be explicit; Tidy3D-style compatibility should be represented as a tagged immutable IR with normalization rules and unsupported-feature policy; runtime metrics should include Gcells/s plus separate end-to-end and steady-state timing; and near-to-far should be built on top of DFT monitor infrastructure rather than as an isolated feature.


## Runtime Context

- The runner injects the current task's title, success criteria, and notes from `loop.spec.md` on every iteration. Do not duplicate the full task catalog here.
- Use `phase1/loop.spec.md` as the authoritative task board.
- Use `phase1/feature-checklist.md` as the compact feature ledger for what Phase 1 should implement, defer, or reject clearly.
- Use `phase1/kernel-formula-sources.md` as the compact source map for kernel formulas, update equations, and implementation references.
- Keep the key architectural dependencies in mind while executing any task:
  - mode solver work should precede `ModeSource` and mode-monitor workflows
  - second-order subpixel smoothing depends on the anisotropic material representation and update path already working
  - logging and convergence behavior are first-class runtime deliverables, not incidental byproducts
  - kernel/runtime support must be explicit for materials, boundaries, sources, monitors, and postprocessing rather than implied by API support

## Working Memory

# Phase 1 Maxwell Solver Loop Working Memory

Use this file for cumulative findings, reusable facts, decisions, and cross-task notes.

Keep it concise. Do not repeat information already captured in `loop.prompt.md` or other local files.
Prefer referencing local file paths and distilled takeaways instead of copying long excerpts.

Suggested sections:

- Facts
- Decisions
- Open Questions
- Reusable References
- Task Handoffs

## Facts

- `task-001` starts from an effectively empty repo with no `src/` package or tooling.
- `phase1/feature-checklist.md` is now mirrored in code via `src/autofdtd/planning.py` so later tasks can reuse a local feature/status source instead of reparsing markdown.
- `src/autofdtd/core/containers.py` now holds the first frozen tagged container models for `Structure`, `Scene`, and `Simulation`; `src/autofdtd/api/simulation.py` re-exports them as the public API surface.
- `src/autofdtd/ir/models.py` now defines the first versioned execution IR, with `ComponentIR`, `StructureIR`, `SceneIR`, `SimulationIR`, and `ExecutionPackageIR` as the transport contract.
- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
- `src/autofdtd/core/models.py` now provides the shared frozen model contract with `model_tag()`, `copy_update()`, `json_ready()`, and stable JSON serialization helpers reused by both public containers and IR models.
- `src/autofdtd/geometry/primitives.py` now provides typed `Box`, `Sphere`, and `Cylinder` public models plus `GeometryTransform`, bounds, point-containment, and translation helpers.
- `src/autofdtd/ir/models.py` now lowers primitive geometries into typed `BoxIR`, `SphereIR`, and `CylinderIR` payloads with explicit bounds and scene-compilation transform metadata.
- `src/autofdtd/geometry/polyslab.py` now provides a typed `PolySlab` model for simple-polygon extrusions with bounds, containment, translation, and simple-polygon validation; `src/autofdtd/ir/models.py` lowers it into `PolySlabIR`.
- `src/autofdtd/geometry/composite.py` now provides `GeometryGroup`, `Transformed`, `GeometryArray`, and a narrow `ClipOperation` subset (`union`, `intersection`, `difference`) for Phase 1 scene construction.
- `GeometryTransform` now supports explicit rigid-transform composition plus translation, rotation, reflection, and conservative transformed-bounds evaluation for wrapper lowering and scene queries.
- `src/autofdtd/ir/models.py` now carries typed wrapper IR via `GeometryGroupIR`, `TransformedIR`, and `ClipOperationIR`.
- `GeometryArray` uses Tidy3D-style `geometry + offsets + transforms` public semantics, but Phase 1 keeps transforms rigid and linear-only and lowers arrays into explicit grouped instances for IR and overlap evaluation.
- `Scene.structure_precedence()` and `Scene.resolve_structure_stack_at_point()` now expose concrete overlap order with `source_index`, `resolved_priority`, and `precedence_rank`, and `StructureIR` carries the same metadata for later compiler stages.
- `src/autofdtd/grid/specs.py` now provides `AutoGrid`, `GridRefinement`, and `LayerRefinementSpec` in addition to the manual grid models; the current AutoGrid path is wavelength-driven, axis-local, and only applies axis-aligned layer refinement metadata.
- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
- `src/autofdtd/grid/subpixel.py` now provides the first explicit subpixel policy layer with `SubpixelSpec`, `Staircasing`, and `PolarizedAveraging`, plus Tidy3D-style `bool` coercion for `Simulation.subpixel`.
- `src/autofdtd/ir/models.py` now lowers subpixel settings into `SubpixelSpecIR` with explicit averaging-vs-staircasing targets and an anisotropic-materialization flag for later scene compilation.
- `src/autofdtd/materials/isotropic.py` now provides typed `Medium`, `PECMedium`, and `PMCMedium` public models with validation and normalization helpers; `src/autofdtd/ir/models.py` lowers them into typed `MediumIR`, `PECMediumIR`, and `PMCMediumIR`.
- `src/autofdtd/compiler/materials.py` now compiles isotropic constitutive coefficients, exposes scene-point medium sampling, and keeps explicit clamp-zero modes for `PECMedium` and `PMCMedium`.
- `src/autofdtd/kernels/materials.py` now provides the first staged constitutive-update helpers with a NumPy path and optional Warp-availability reporting.
- `src/autofdtd/materials/dispersive.py` now provides a Phase 1 `PoleResidue` public model with JSON-safe complex-pair storage, causal validation (`Re(a) <= 0`), `eps_model()`, and limited `Medium` conversion helpers.
- `src/autofdtd/materials/dispersive.py` now also provides a passive homogeneous `Sellmeier` model with `(B_i, C_i)` coefficient storage, direct `eps_model()` evaluation, single-pole `from_dispersion()` fitting, and explicit lowering to an equivalent `PoleResidue` representation.
- `src/autofdtd/materials/dispersive.py` now also provides passive homogeneous `Lorentz`, damped `Drude`, and passive homogeneous `Debye` models with family-specific analytical coefficients and explicit lowering to equivalent `PoleResidue` payloads.
- `src/autofdtd/ir/models.py` now lowers supported dispersive media into typed `PoleResidueIR` payloads instead of a generic medium envelope.
- `src/autofdtd/ir/models.py` now lowers `Sellmeier` into a typed `SellmeierIR` while keeping runtime compilation on the shared pole-residue auxiliary-state contract.
- `src/autofdtd/ir/models.py` now lowers `Lorentz`, `Drude`, and `Debye` into typed IR families while runtime compilation still targets the shared pole-residue auxiliary-state contract.
- `src/autofdtd/compiler/materials.py` now compiles `PoleResidue` media into explicit epsilon-infinity electric coefficients plus per-pole exact-hold recurrence terms for complex auxiliary polarization state.
- `src/autofdtd/compiler/materials.py` now compiles `Sellmeier` by first reducing it to an equivalent `PoleResidue`, then re-tagging the compiled coefficients as `medium_type="Sellmeier"` so scene diagnostics preserve the public family name.
- `src/autofdtd/compiler/materials.py` now compiles `Lorentz`, `Drude`, and `Debye` by first reducing them to equivalent `PoleResidue` terms, re-tagging the compiled coefficients with the public family name, and failing explicitly when converted poles are too stiff for the current exact-hold timestep path.
- `src/autofdtd/kernels/materials.py` now allocates explicit PoleResidue auxiliary state and applies a staged `pole_residue_electric_update()` that subtracts a compiled polarization-current estimate from the baseline electric update.
- `src/autofdtd/materials/anisotropic.py` now provides `AnisotropicMedium` as the implemented diagonal tensor family plus explicit `FullyAnisotropicMedium` and `Medium2D` defer-policy surfaces; `Medium2D` is parseable and convertible to volumetric `AnisotropicMedium`, but direct simulation use stays rejected in Phase 1.
- `src/autofdtd/compiler/materials.py` now compiles `AnisotropicMedium` into `AnisotropicMaterialCoefficients` with one scalar constitutive branch per principal axis, allowing nondispersive, dispersive, and clamp-mode components to coexist without introducing a dense-tensor runtime yet.
- `src/autofdtd/kernels/materials.py` now provides `allocate_anisotropic_state()`, `anisotropic_electric_update()`, and `anisotropic_magnetic_update()` for diagonal tensor updates with per-axis dispersive auxiliary state.
- `src/autofdtd/materials/advanced.py` now maps `LossyMetalMedium`, `PerturbationMedium`, `PerturbationPoleResidue`, `CustomMedium`, and `CustomAnisotropicMedium` into explicit Phase 1 policy surfaces, and `src/autofdtd/core/validation.py` buckets unknown `Custom*` medium tags into the clear custom-media reject path instead of treating them as unmapped types.
- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile the supported subset into explicit halo metadata and apply periodic-copy or conductor-reflection ghost-cell updates for `(nx, ny, nz, 3)` vector fields.
- `src/autofdtd/boundaries/models.py` now implements `BlochBoundary` plus `Boundary.bloch(...)`; runtime compilation lowers Bloch axes into explicit conjugate phase factors on `minus` faces and forward phase factors on `plus` faces, which forces complex field storage even when `bloch_vec == 0`.
- `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
- `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.

## Decisions

- Reserve subsystem namespaces now: `api`, `core`, `geometry`, `materials`, `boundaries`, `sources`, `monitors`, `grid`, `modes`, `ir`, `compiler`, `runtime`, `kernels`, `diagnostics`, `examples`.
- Use `hatchling` with a `src/` layout and ship a small CLI (`autofdtd-feature-matrix`) so the scaffold has a concrete install/runtime smoke target before solver code exists.
- Keep this task focused on packaging, docs, and planning metadata; full tagged models remain for `task-002` and `task-003`.
- Model ordered overlap explicitly through `Scene.structures_in_resolution_order()`, sorting by resolved priority and then insertion order so later structures still win ties during sequential materialization.
- Keep the initial container layer serialization-light: `to_payload()`, `to_json_text()`, and `write_json()` are enough for snapshots and handoff, while `task-005` can generalize the shared model base.
- Use a generic family-tagged component envelope in the IR so Phase 1 can package realistic simulations before every feature family has concrete typed IR classes; later tasks can specialize payloads without changing bundle shape.
- Bundle packaging is directory-based: `manifest.json` points at a hashed `simulation.json` entrypoint, avoiding self-referential manifest hashing while staying suitable for remote upload or local caching.
- Core validation is checklist-driven: implemented feature tags pass, deferred or rejected tags fail early with explicit errors, and known primitive geometry payloads validate only when enough fields are present so placeholder tagged dicts can survive until their concrete family tasks land.
- Shared model behavior should live in `autofdtd.core.models`; public API and IR classes should subclass it rather than duplicating serialization or copy helpers in subsystem-local base classes.
- Primitive geometry stays axis-aligned in Phase 1 public models; scene-compilation transforms are still explicit so later `Transformed` and grouped-geometry tasks can lower onto the same contract instead of rewriting primitive handling.
- Structure precedence is now inspectable with `Scene.resolve_structure_at_point()`, which mirrors materialization order by applying primitive containment checks in resolved-priority order.
- Keep `GeometryArray` as a public wrapper only; repeated placements lower into ordinary grouped and transformed instances so runtime/compiler code can treat arrays as pre-expanded ordered geometry.
- Phase 1 `PolySlab` scope is intentionally limited to constant-cross-section extrusions of simple polygons on principal axes; non-zero `dilation` or `sidewall_angle`, self-intersections, and curved or boolean polygon features must fail explicitly until later geometry tasks land.
- Keep the feature ledger conservative for clipping: `ClipOperation` remains a deferred family in `planning.py`, but geometry validation explicitly allows the implemented subset and rejects unsupported boolean ops with clear errors.
- Phase 1 transform wrappers are rigid only; free-form affine matrices and exact boolean meshing remain out of scope even though the public model now supports grouped and transformed composition.
- Keep the top-level simulation grid surface strict: `grid_spec` must be a `GridSpec`, `AutoGrid` requires `GridSpec.wavelength`, and `QuasiUniformGrid` remains an explicit deferred error.
- Phase 1 AutoGrid is deliberately narrow: derive a vacuum baseline from wavelength and domain size, discretize piecewise with a simple `max_scale` grading pass, and limit refinement inputs to `LayerRefinementSpec` body and bound hints rather than full geometry-driven meshing.
- Keep Phase 1 subpixel support narrow and explicit: dielectric interfaces may use `PolarizedAveraging` or `Staircasing`, while metal, PEC, PMC, and lossy-metal interfaces stay staircasing-only until anisotropic media and conformal policies land.
- Treat PEC and PMC as explicit constitutive clamp modes in coefficient compilation and runtime helpers instead of pretending they are extremely lossy ordinary media.
- Phase 1 `PoleResidue` keeps complex numbers JSON-safe as `(real, imag)` tuples in the public API and IR so serialization stays stable without inventing ad hoc string encoding.
- Phase 1 PoleResidue runtime support is electric-only and uses one complex polarization accumulator per pole; magnetic dispersion remains out of scope until later material families require it.
- Phase 1 `Sellmeier` support is intentionally passive-only and homogeneous-only: `B_i >= 0`, `C_i >= 0`, zero-`C_i` terms fold into `eps_inf`, and all timestep execution reuses the existing electric-only `PoleResidueMaterialCoefficients` path instead of creating a new auxiliary-state layout.
- Phase 1 `Lorentz` is passive-only (`delta_eps >= 0`, `damping >= 0`, `frequency^2 != damping^2`), `Drude` is limited to damped poles (`damping > 0`) so it can lower cleanly onto the current pole-residue path, and `Debye` is passive-only (`delta_eps >= 0`, `tau > 0`).
- The current exact-hold pole-residue recurrence path can overflow for sufficiently stiff poles relative to `dt`; material compilation now raises an explicit error instead of emitting unusable coefficients, which is especially relevant for very small-`tau` Debye fits.
- Phase 1 anisotropy stops at diagonal tensor media: `AnisotropicMedium` is implemented and IR-lowered, `FullyAnisotropicMedium` remains an explicit deferred error for runtime compilation, and `Medium2D` remains a deferred direct-simulation feature with offline conversion helpers only.
- Advanced material policy is explicit rather than silent: `LossyMetalMedium` and perturbation media are parseable defer buckets with manual fallback helpers, while sampled `Custom*` media are parsed or bucketed only to produce clear reject errors.
- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
- Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.

## Task Handoffs

- `task-003` should add versioned IR models under `autofdtd.ir` without coupling them to the public API module layout; it can treat `Simulation.to_payload()` as an interim handoff format, not the final IR contract.
- Later geometry, material, source, and monitor tasks can plug concrete tagged models into the existing container fields without changing the ordered-container semantics.
- `task-004` and `task-005` can reuse `ComponentIR.payload` plus the shared IR schema version constants instead of inventing another transport shape.
- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
- Later compiler work can rely on wrapper IR bounds as conservative planning envelopes; exact voxelization or boolean rasterization still belongs in scene compilation rather than the public geometry layer.
- Later compiler work can consume `StructureIR.source_index` plus `StructureIR.precedence_rank` directly when building ordered region materialization plans or conflict diagnostics.
- Later grid/compiler tasks should treat `LayerRefinementSpecIR` as planning metadata only; corner finding, snapping-point localization, and structure-derived meshing still need a separate scene-aware meshing pass.
- Material and compiler tasks should consume `SubpixelSpecIR.averaging_targets` and `requires_anisotropic_materialization` directly instead of re-deriving which interface families need tensor-valued effective media.
- Later runtime and chunk-planning tasks can treat `IsotropicMaterialCoefficients` plus `ConstitutiveMode` as the stable baseline constitutive contract before dispersive and anisotropic state is added.
- Later Lorentz, Drude, Debye, and Sellmeier tasks should lower onto the `PoleResidueMaterialCoefficients` auxiliary-state contract whenever the family can be reduced to pole-residue terms instead of creating parallel runtime state layouts.
- Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.

## Current Task

Task ID: task-022
Title: Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.

warning: Codex could not find system bubblewrap on PATH. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
exec
/bin/bash -lc "sed -n '1,240p' phase1/loop.spec.md" in /workspace/autofdtd
 succeeded in 0ms:
# Phase 1 Maxwell Solver Loop

Drive Phase 1 of autofdtd from an almost-empty repository to a credible, pip-installable, GPU-first FDTD package whose feature plan is anchored explicitly to the local Tidy3D EM API surface. The loop must produce a Tidy3D-inspired public API, a tagged execution IR, a scene-compilation pipeline, a chunk-based multi-node-ready runtime, Warp kernel conventions, concrete implementations for the highest-priority Tidy3D feature families, validation examples, and documentation without copying protected code or docs from any reference project.

Statuses: `QUEUED`, `RUNNING`, `COMPLETED`, `FAILED`, `BLOCKED`

## [COMPLETED] task-001 - Create the package, tooling, and docs scaffold around the planned feature surface
Success: Create a pip-installable src-layout package with module namespaces, development tooling, test entrypoints, and docs scaffolding that are aligned with the feature checklist and subsystem boundaries.
Attempts: 1
Last Run: task-001-attempt-001
Last Duration: 335.1s
Completed At: 2026-04-06T02:09:22Z
Notes: The Tidy3D feature checklist is already part of prep and should be consumed here rather than regenerated.

## [COMPLETED] task-002 - Implement the tagged core Simulation, Scene, and Structure data model
Success: Provide the foundational public and IR-facing container models for Simulation, Scene, and Structure, including ordered structure semantics, names, priority behavior, and serialization scaffolding.
Attempts: 1
Last Run: task-002-attempt-001
Last Duration: 318.3s
Completed At: 2026-04-06T02:14:41Z
Notes: This is the container backbone that many later feature tasks depend on.

## [COMPLETED] task-003 - Define the tagged execution IR and serialization contract
Success: Land an inspectable, versioned IR schema and serialization plan that can represent realistic Phase 1 simulations independently of the public API and is suitable for local or remote execution packaging.
Attempts: 1
Last Run: task-003-attempt-001
Last Duration: 249.2s
Completed At: 2026-04-06T02:18:50Z
Notes: The IR should be specific enough to support later feature tasks without redesign.

## [COMPLETED] task-004 - Implement core normalization and validation behavior
Success: Provide validation and normalization passes for names, bounds, basic shape constraints, unsupported-feature errors, and selected warn-and-coerce behaviors that align with the feature checklist.
Attempts: 1
Last Run: task-004-attempt-001
Last Duration: 499.7s
Completed At: 2026-04-06T02:27:10Z
Notes: This task should make later feature work safer instead of treating validation as cleanup at the end.

## [COMPLETED] task-005 - Implement shared model-base and serialization helpers
Success: Provide the shared base-model, tagging, copy-or-update, and serialization helpers needed by the public API and IR layers so later feature tasks build on a common contract.
Attempts: 1
Last Run: task-005-attempt-001
Last Duration: 164.9s
Completed At: 2026-04-06T02:29:54Z
Notes: This keeps tagging and serialization from being reimplemented ad hoc in every feature family.

## [COMPLETED] task-006 - Implement primitive geometry support for Box, Sphere, and Cylinder
Success: Provide public and IR representations plus tests for Box, Sphere, and Cylinder geometries, including bounds, transforms needed for scene compilation, and precedence interaction inside structures.
Attempts: 1
Last Run: task-006-attempt-001
Last Duration: 406.1s
Completed At: 2026-04-06T02:36:41Z
Notes: Start with the simplest Tidy3D geometry primitives first.

## [COMPLETED] task-007 - Implement PolySlab and polygon-driven geometry support
Success: Support PolySlab-style polygon extrusion geometry with explicit scope and limitation documentation, plus tests that cover representative planar photonics cases.
Attempts: 1
Last Run: task-007-attempt-001
Last Duration: 460.5s
Completed At: 2026-04-06T02:44:21Z
Notes: This is one of the most important geometry families for Tidy3D-style compatibility.

## [COMPLETED] task-008 - Implement geometry grouping, transforms, and clip-style wrappers
Success: Provide GeometryGroup-style composition plus the minimum transform and clip operation support needed for credible Phase 1 scene construction.
Attempts: 1
Last Run: task-008-attempt-001
Last Duration: 531.5s
Completed At: 2026-04-06T02:53:13Z
Notes: This task should turn individual primitives into useful scene-building blocks.

## [COMPLETED] task-009 - Implement geometry arrays, precedence, and repeated placement semantics
Success: Support repeated or array-style geometry placement and explicit precedence semantics for ordered structures with tests that verify overlap behavior.
Attempts: 1
Last Run: task-009-attempt-001
Last Duration: 378.3s
Completed At: 2026-04-06T02:59:31Z
Notes: This is where structure ordering becomes concrete.

## [COMPLETED] task-010 - Implement grid controls for UniformGrid, CustomGrid, and GridSpec core paths
Success: Provide the core grid specification surface needed for Phase 1 simulations, including uniform grids, custom boundaries or custom grids, and GridSpec-style composition.
Attempts: 1
Last Run: task-010-attempt-001
Last Duration: 678.8s
Completed At: 2026-04-06T03:10:50Z
Notes: Grid support should be concrete enough to drive later discretization and mode-solver work.

## [COMPLETED] task-011 - Implement AutoGrid and refinement-spec planning surfaces
Success: Support an initial AutoGrid-style and refinement-spec surface with clear limitations and lowering behavior, even if the first meshing policy is simpler than Tidy3D's full behavior.
Attempts: 1
Last Run: task-011-attempt-001
Last Duration: 414.5s
Completed At: 2026-04-06T03:17:44Z
Notes: This task can begin with planning or simplified refinement behavior, but it must not be hand-wavy.

## [COMPLETED] task-012 - Implement SubpixelSpec and core averaging policy models
Success: Provide SubpixelSpec-style controls and the core averaging or staircasing policy representations needed to drive geometry materialization behavior.
Attempts: 1
Last Run: task-012-attempt-001
Last Duration: 629.8s
Completed At: 2026-04-06T03:28:14Z
Notes: This task is about policy modeling before the heavier compiler behavior lands.

## [COMPLETED] task-013 - Implement isotropic Medium, PECMedium, and PMCMedium with coefficient compilation and constitutive kernels
Success: Support the foundational isotropic material family, including plain Medium and perfect-conductor medium variants, with tests for serialization, validation, structure application, coefficient preparation, and the corresponding constitutive-update kernel paths.
Attempts: 1
Last Run: task-013-attempt-001
Last Duration: 589.0s
Completed At: 2026-04-06T03:38:03Z
Notes: These are the baseline material types many later tasks assume exist.

## [COMPLETED] task-014 - Implement PoleResidue medium support with coefficient prep and update kernels
Success: Support PoleResidue-style dispersive media with explicit API and IR models, coefficient preparation, auxiliary-state handling, update-kernel support, and representative tests.
Attempts: 1
Last Run: task-014-attempt-001
Last Duration: 522.2s
Completed At: 2026-04-06T03:46:45Z
Notes: Keep PoleResidue separate because it is the most general dispersive family and often subsumes the others conceptually.

## [COMPLETED] task-015 - Implement Sellmeier medium support with coefficient prep and update kernels
Success: Support Sellmeier media or a clearly documented supported subset, including API and IR models, coefficient preparation, update-kernel support, and representative validation tests.
Attempts: 1
Last Run: task-015-attempt-001
Last Duration: 299.6s
Completed At: 2026-04-06T03:51:45Z
Notes: This stays separate so optical-material support is not hidden inside a large dispersive bucket.

## [COMPLETED] task-016 - Implement Lorentz, Drude, and Debye medium support with update kernels
Success: Support Lorentz, Drude, and Debye medium families or a clearly delimited subset, including API and IR models, coefficient preparation, update-kernel support, and tests that show each family exercises the intended constitutive path.
Attempts: 1
Last Run: task-016-attempt-001
Last Duration: 485.0s
Completed At: 2026-04-06T03:59:50Z
Notes: These families are close enough to share a task without becoming ambiguous if the tests name each family explicitly.

## [COMPLETED] task-017 - Implement anisotropic medium families, Medium2D policy, and anisotropic kernel support
Success: Support the initial anisotropic medium surface, including a clear policy for AnisotropicMedium, FullyAnisotropicMedium, and Medium2D-related behavior, with explicit tests, limitations, coefficient preparation, and anisotropic kernel support.
Attempts: 19
Last Run: task-017-attempt-019
Last Duration: 502.8s
Completed At: 2026-04-06T19:06:42Z
Notes: This task should respect the fact that subpixel smoothing can create effective anisotropy and must land before second-order subpixel materialization is treated as implemented.

## [COMPLETED] task-018 - Implement perturbation, lossy-metal, and advanced-material defer buckets
Success: Provide an explicit Phase 1 handling policy for PerturbationMedium, LossyMetalMedium, custom media, and other advanced material families so the API surface is mapped rather than ignored.
Attempts: 1
Last Run: task-018-attempt-001
Last Duration: 399.5s
Completed At: 2026-04-06T19:13:22Z
Notes: This is intentionally a policy-heavy task if full implementation is not yet realistic.

## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
Attempts: 1
Last Run: task-019-attempt-001
Last Duration: 708.3s
Completed At: 2026-04-06T19:25:10Z
Notes: These are the first boundary primitives the compiler and runtime should rely on.

## [COMPLETED] task-020 - Implement BlochBoundary, symmetry-aware metadata, and phase-aware runtime transforms
Success: Provide BlochBoundary support and the related symmetry-aware metadata and runtime transform support needed for chunk planning and execution lowering.
Attempts: 1
Last Run: task-020-attempt-001
Last Duration: 460.9s
Completed At: 2026-04-06T19:32:51Z
Notes: This task is important because periodic-phase behavior leaks into runtime metadata and not just public API models.

## [COMPLETED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
Attempts: 1
Last Run: task-021-attempt-001
Last Duration: 632.6s
Completed At: 2026-04-06T19:43:24Z
Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.

## [RUNNING] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
Attempts: 1
Last Run: task-022-attempt-001
Last Duration: -
Completed At: -
Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.

## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.

## [QUEUED] task-024 - Implement Simulation runtime controls for timestep count, shutoff, and convergence policy
Success: Support the core Simulation runtime-control surface needed for practical runs, including run-time or step-count control, shutoff or early-stop criteria, and clearly defined convergence-related behavior.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: This task makes convergence and stop behavior explicit instead of leaving it buried inside the simulation loop.

## [QUEUED] task-025 - Implement detailed simulation logging, progress reporting, and convergence evidence capture
Success: Execution paths emit structured progress logs, stop-reason records, convergence or shutoff evidence, and enough runtime detail to diagnose practical simulation behavior during end-to-end runs.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: Keep this distinct from benchmark metrics so debugging and validation remain first-class.

## [QUEUED] task-026 - Implement GaussianPulse and ContinuousWave source-time profiles
Success: Support GaussianPulse and ContinuousWave source-time profiles with explicit parameter validation, serialization, IR lowering, and representative waveform tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: These are the two most common source-time profiles and should be available early.

## [QUEUED] task-027 - Implement BroadbandPulse and CustomSourceTime profiles
Success: Support BroadbandPulse and CustomSourceTime or a clearly delimited subset, including API and IR models, validation behavior, and representative tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: Keep the more specialized source-time families separate so missing behavior cannot hide behind the common waveforms.

## [QUEUED] task-028 - Implement UniformCurrentSource with placement semantics and injection kernels
Success: Support UniformCurrentSource with explicit placement semantics, source-time integration, IR lowering, injection-kernel support, and representative end-to-end tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: This is the cleanest current-source family and should come before the more specialized dipole and custom cases.

## [QUEUED] task-029 - Implement PointDipole and CustomCurrentSource with injection kernels
Success: Support PointDipole and CustomCurrentSource or a clearly supported subset, including validation, IR lowering, injection-kernel support, and representative tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: PointDipole is important because it stresses interpolation and restriction behavior directly.

## [QUEUED] task-030 - Implement CustomFieldSource with field-injection runtime support
Success: Support CustomFieldSource or a clearly delimited subset, including validation, IR lowering, field-injection runtime support, and representative tests.

exec
/bin/bash -lc 'pwd && rg -n "task-022|StablePML|Absorber|class PML|PMLIR|BoundarySpecIR|CompiledBoundarySpec|apply_boundary_stages|allocate_pml_boundary_state" phase1 src tests -S' in /workspace/autofdtd
 succeeded in 1ms:
/workspace/autofdtd
tests/test_boundaries.py:19:from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
tests/test_boundaries.py:22:    apply_boundary_stages,
tests/test_boundaries.py:27:from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
tests/test_boundaries.py:101:        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
tests/test_boundaries.py:151:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_boundaries.py:176:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_boundaries.py:177:    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
tests/test_boundaries.py:222:    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
tests/test_boundaries.py:236:    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
tests/test_boundaries.py:245:        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
tests/test_ir.py:38:    BoundarySpecIR,
tests/test_ir.py:48:    PMLIR,
tests/test_ir.py:185:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_ir.py:186:    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/kernel-formula-sources.md:80:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/kernel-formula-sources.md:88:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/feature-checklist.md:84:| `StablePML` | Implement | May land as a supported subset first |
phase1/feature-checklist.md:85:| `Absorber` | Implement | Important practical fallback |
phase1/feature-checklist.md:88:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/feature-checklist.md:167:- Absorber kernels
phase1/working-memory.md:54:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/working-memory.md:58:- `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
phase1/working-memory.md:89:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/working-memory.md:106:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/working-memory.md:107:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
src/autofdtd/planning.py:216:        "StablePML",
src/autofdtd/planning.py:220:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/logs/events.jsonl:75:{"artifacts": [{"description": "Typed non-absorbing boundary public models and normalization helpers.", "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py"}, {"description": "Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling.", "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py"}, {"description": "Concrete ghost-cell update helpers and boundary kernel metadata.", "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py"}, {"description": "Runtime wrapper that applies compiled boundary ghost updates.", "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py"}, {"description": "Typed boundary IR classes and lowering helpers.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors.", "path": "/workspace/autofdtd/tests/test_boundaries.py"}, {"description": "Boundary scope documentation for the implemented Phase 1 subset.", "path": "/workspace/autofdtd/docs/boundaries.md"}, {"description": "Structured task result matching the Phase 1 output schema.", "path": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json"}], "attempt": 1, "duration_s": 708.3, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.", "key_findings": ["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.", "Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.", "`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.", "Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.", "Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."], "message": "Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-019-attempt-001"}, "result_status": "completed", "run_id": "task-019-attempt-001", "status": "COMPLETED", "task_id": "task-019", "task_title": "Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers", "tasks_remaining": 40, "ts": "2026-04-06T19:25:10Z"}
phase1/logs/events.jsonl:77:{"artifacts": [{"description": "Added `BlochBoundary`, public constructors, and boundary-pair validation rules.", "path": "src/autofdtd/boundaries/models.py"}, {"description": "Extended compiled boundary metadata with Bloch phase factors and symmetry-axis transforms.", "path": "src/autofdtd/compiler/boundaries.py"}, {"description": "Added typed Bloch and symmetry IR payloads plus boundary-spec lowering with complex-field requirements.", "path": "src/autofdtd/ir/models.py"}, {"description": "Implemented phase-aware Bloch ghost updates and reusable boundary/symmetry transform helpers.", "path": "src/autofdtd/kernels/boundaries.py"}, {"description": "Extended runtime boundary wrapper to compile symmetry metadata and expose transform helpers.", "path": "src/autofdtd/runtime/boundaries.py"}, {"description": "Added Bloch boundary, symmetry metadata, phase-transform, and runtime/IR coverage.", "path": "tests/test_boundaries.py"}, {"description": "Recorded Bloch runtime behavior and symmetry-metadata decisions for later boundary tasks.", "path": "phase1/working-memory.md"}], "attempt": 1, "duration_s": 460.9, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Absorbing-boundary tasks can now extend the existing `CompiledBoundarySpec` contract without redesigning periodic/Bloch/symmetry lowering. No multi-GPU smoke was attempted because this task only touched boundary metadata and local ghost-transform behavior.", "key_findings": ["`BlochBoundary` is now a first-class public boundary edge with `Boundary.bloch(...)`, strict both-sides validation, and explicit complex phase metadata derived from `bloch_vec`.", "Boundary IR now distinguishes periodic, Bloch, and reflective axes, marks `requires_complex_fields`, and carries per-axis symmetry sign metadata for electric and magnetic field families.", "Compiled boundary metadata now includes Bloch exchange kinds, per-face phase factors, active symmetry axes, and reusable symmetry transforms for future chunk-planning and reduced-domain exchange paths.", "Boundary ghost updates now apply conjugate phase wrapping on minus Bloch faces and forward phase wrapping on plus faces, while separate helper transforms expose boundary-edge and symmetry remaps directly to runtime callers.", "Focused validation passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py` and `python -m compileall src/autofdtd`."], "message": "Implemented BlochBoundary support plus symmetry-aware boundary metadata and phase-aware runtime transforms across the public API, IR, compiler, kernels, runtime wrapper, and tests.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-020-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-020-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-020-attempt-001"}, "result_status": "completed", "run_id": "task-020-attempt-001", "status": "COMPLETED", "task_id": "task-020", "task_title": "Implement BlochBoundary, symmetry-aware metadata, and phase-aware runtime transforms", "tasks_remaining": 39, "ts": "2026-04-06T19:32:51Z"}
phase1/logs/events.jsonl:79:{"artifacts": [{"description": "Baseline PML public models and boundary constructors.", "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py"}, {"description": "Typed PML IR models and boundary-spec PML metadata lowering.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Compiled PML coefficient preparation and boundary-stage metadata.", "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py"}, {"description": "Boundary kernel stages including PML layer damping with persistent face memory.", "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py"}, {"description": "Runtime state allocation and staged boundary application wrapper.", "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py"}, {"description": "Representative PML validation and absorption tests.", "path": "/workspace/autofdtd/tests/test_boundaries.py"}, {"description": "Boundary runtime snapshots including a compact PML absorption example.", "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py"}, {"description": "Updated Phase 1 handoff notes for baseline PML support and extension seams.", "path": "/workspace/autofdtd/phase1/working-memory.md"}], "attempt": 1, "duration_s": 632.6, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Current PML support is intentionally narrow: it uses a staged damping-layer backend with persistent face memory rather than a full split-field CPML. `StablePML` and `Absorber` should extend the compiled-face/state contract in task-022.", "key_findings": ["Added Tidy3D-shaped baseline `PML` and `PMLParams` models plus `Boundary.pml(...)` / `BoundarySpec.pml(...)` constructors and zero-dimension coercion behavior.", "Extended boundary IR with typed `PMLIR` and `PMLParamsIR`; `BoundarySpecIR` now records `pml_axes` and `pml_faces` so scene lowering preserves absorbing-face metadata explicitly.", "Boundary compilation now prepares per-face polynomial PML coefficient profiles (`sigma`, `kappa`, `alpha`, attenuation, memory terms) and advertises explicit `electric_pml` / `magnetic_pml` stage ordering.", "Runtime now allocates persistent per-face PML memory via `allocate_pml_boundary_state()` and applies PML stages through `apply_boundary_stages()` / `BoundaryRuntime.apply()`.", "Representative tests now cover typed PML lowering, coefficient prep, face-state allocation, and staged absorption behavior; full suite passes with `PYTHONPATH=src python -m pytest -q` (110 passed)."], "message": "Implemented baseline PML boundary support across the public API, typed IR, boundary compilation, runtime state allocation, staged kernel updates, examples, and absorption-focused tests.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-021-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-021-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-021-attempt-001"}, "result_status": "completed", "run_id": "task-021-attempt-001", "status": "COMPLETED", "task_id": "task-021", "task_title": "Implement PML boundary support with coefficient prep and kernel stages", "tasks_remaining": 38, "ts": "2026-04-06T19:43:24Z"}
phase1/logs/events.jsonl:80:{"attempt": 1, "duration_s": null, "event": "task_selected", "message": "Task selected and marked RUNNING.", "paths": {"run_dir": "/workspace/autofdtd/phase1/runs/task-022-attempt-001"}, "run_id": "task-022-attempt-001", "status": "RUNNING", "task_id": "task-022", "task_title": "Implement StablePML and Absorber boundary support with coefficient prep and kernel stages", "tasks_remaining": 37, "ts": "2026-04-06T19:43:24Z"}
src/autofdtd/runtime/boundaries.py:12:    CompiledBoundarySpec,
src/autofdtd/runtime/boundaries.py:19:    apply_boundary_stages,
src/autofdtd/runtime/boundaries.py:25:def allocate_pml_boundary_state(
src/autofdtd/runtime/boundaries.py:26:    compiled_boundary_spec: CompiledBoundarySpec,
src/autofdtd/runtime/boundaries.py:83:            allocate_pml_boundary_state(self.compiled, field_shape, dtype=field_dtype)
src/autofdtd/runtime/boundaries.py:99:        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
src/autofdtd/runtime/boundaries.py:111:        return apply_boundary_stages(
src/autofdtd/runtime/boundaries.py:165:    compiled_boundary_spec: CompiledBoundarySpec,
src/autofdtd/runtime/boundaries.py:171:    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
src/autofdtd/runtime/__init__.py:5:    allocate_pml_boundary_state,
src/autofdtd/runtime/__init__.py:12:    "allocate_pml_boundary_state",
phase1/runs/task-017-attempt-002/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-002/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-022-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-022-attempt-001/codex.stdout.txt:162:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-022-attempt-001/codex.stdout.txt:166:- `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
phase1/runs/task-022-attempt-001/codex.stdout.txt:197:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/runs/task-022-attempt-001/codex.stdout.txt:214:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/runs/task-022-attempt-001/codex.stdout.txt:215:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/runs/task-022-attempt-001/codex.stdout.txt:219:Task ID: task-022
phase1/runs/task-022-attempt-001/codex.stdout.txt:220:Title: Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-022-attempt-001/codex.stdout.txt:221:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-022-attempt-001/codex.stdout.txt:222:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-017-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-022-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-022-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-022-attempt-001/prompt.txt:154:- `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
phase1/runs/task-022-attempt-001/prompt.txt:185:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/runs/task-022-attempt-001/prompt.txt:202:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/runs/task-022-attempt-001/prompt.txt:203:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/runs/task-022-attempt-001/prompt.txt:207:Task ID: task-022
phase1/runs/task-022-attempt-001/prompt.txt:208:Title: Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-022-attempt-001/prompt.txt:209:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-022-attempt-001/prompt.txt:210:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
src/autofdtd/kernels/boundaries.py:13:    CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:27:class PMLFaceState(AutoFDTDModel):
src/autofdtd/kernels/boundaries.py:39:class PMLBoundaryState(AutoFDTDModel):
src/autofdtd/kernels/boundaries.py:212:    boundary_spec: CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:226:    boundary_spec: CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:254:def apply_boundary_stages(
src/autofdtd/kernels/boundaries.py:257:    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-013-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-014-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
src/autofdtd/kernels/__init__.py:9:    apply_boundary_stages,
src/autofdtd/kernels/__init__.py:41:    "apply_boundary_stages",
phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-021-attempt-001/result.json:1:{"task_id":"task-021","status":"completed","summary":"Implemented baseline PML boundary support across the public API, typed IR, boundary compilation, runtime state allocation, staged kernel updates, examples, and absorption-focused tests.","key_findings":["Added Tidy3D-shaped baseline `PML` and `PMLParams` models plus `Boundary.pml(...)` / `BoundarySpec.pml(...)` constructors and zero-dimension coercion behavior.","Extended boundary IR with typed `PMLIR` and `PMLParamsIR`; `BoundarySpecIR` now records `pml_axes` and `pml_faces` so scene lowering preserves absorbing-face metadata explicitly.","Boundary compilation now prepares per-face polynomial PML coefficient profiles (`sigma`, `kappa`, `alpha`, attenuation, memory terms) and advertises explicit `electric_pml` / `magnetic_pml` stage ordering.","Runtime now allocates persistent per-face PML memory via `allocate_pml_boundary_state()` and applies PML stages through `apply_boundary_stages()` / `BoundaryRuntime.apply()`.","Representative tests now cover typed PML lowering, coefficient prep, face-state allocation, and staged absorption behavior; full suite passes with `PYTHONPATH=src python -m pytest -q` (110 passed)."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Baseline PML public models and boundary constructors."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed PML IR models and boundary-spec PML metadata lowering."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Compiled PML coefficient preparation and boundary-stage metadata."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Boundary kernel stages including PML layer damping with persistent face memory."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime state allocation and staged boundary application wrapper."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Representative PML validation and absorption tests."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Boundary runtime snapshots including a compact PML absorption example."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Updated Phase 1 handoff notes for baseline PML support and extension seams."}],"error_summary":"","follow_up_notes":"Current PML support is intentionally narrow: it uses a staged damping-layer backend with persistent face memory rather than a full split-field CPML. `StablePML` and `Absorber` should extend the compiled-face/state contract in task-022.","next_action":"none"}
phase1/runs/task-016-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
src/autofdtd/core/validation.py:74:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
src/autofdtd/ir/models.py:411:class PMLParamsIR(IRModel):
src/autofdtd/ir/models.py:426:class PMLIR(IRModel):
src/autofdtd/ir/models.py:429:    type: Literal["PMLIR"] = "PMLIR"
src/autofdtd/ir/models.py:449:    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
src/autofdtd/ir/models.py:463:class BoundarySpecIR(IRModel):
src/autofdtd/ir/models.py:466:    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
src/autofdtd/ir/models.py:660:    boundary_spec: BoundarySpecIR | ComponentIR | None = None
src/autofdtd/ir/models.py:992:        return PMLIR(
src/autofdtd/ir/models.py:1069:) -> BoundarySpecIR | ComponentIR:
src/autofdtd/ir/models.py:1120:    return BoundarySpecIR(
phase1/runs/task-015-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
src/autofdtd/ir/__init__.py:12:    BoundarySpecIR,
src/autofdtd/ir/__init__.py:34:    PMLIR,
src/autofdtd/ir/__init__.py:79:    "BoundarySpecIR",
src/autofdtd/ir/__init__.py:103:    "PMLIR",
phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-013-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-013-attempt-001/codex.stdout.txt:365:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-013-attempt-001/codex.stdout.txt:366:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-013-attempt-001/codex.stdout.txt:371:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-013-attempt-001/codex.stdout.txt:498:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-013-attempt-001/codex.stdout.txt:499:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-013-attempt-001/codex.stdout.txt:502:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-013-attempt-001/codex.stdout.txt:581:- Absorber kernels
phase1/runs/task-013-attempt-001/codex.stdout.txt:1085:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-013-attempt-001/codex.stdout.txt:2950:        "StablePML",
phase1/runs/task-013-attempt-001/codex.stdout.txt:2954:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-013-attempt-001/codex.stdout.txt:3185:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-013-attempt-001/codex.stdout.txt:3193:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
src/autofdtd/compiler/boundaries.py:87:class CompiledBoundarySpec(AutoFDTDModel):
src/autofdtd/compiler/boundaries.py:90:    type: str = "CompiledBoundarySpec"
src/autofdtd/compiler/boundaries.py:321:) -> CompiledBoundarySpec:
src/autofdtd/compiler/boundaries.py:372:    return CompiledBoundarySpec(
phase1/runs/task-017-attempt-013/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-013/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-015-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-015-attempt-001/codex.stdout.txt:2203:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
src/autofdtd/compiler/__init__.py:8:    CompiledBoundarySpec,
src/autofdtd/compiler/__init__.py:41:    "CompiledBoundarySpec",
phase1/runs/task-017-attempt-012/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-012/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-018/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-018/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-007-attempt-001/codex.stdout.txt:345:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-007-attempt-001/codex.stdout.txt:346:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-007-attempt-001/codex.stdout.txt:351:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-007-attempt-001/codex.stdout.txt:1011:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:7217: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:7685: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:8156: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:8626: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:9099: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:9641: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:10186: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:10748: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:11313: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:11940: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:12570: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:13208: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:13898: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:14585: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:15287: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:15986: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:16703: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:17417: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:18152: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:18884: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:19619: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:20448: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:21280: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:22188: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:23098: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:24077: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:24985: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:25895: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:26809: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:27911: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:28876: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:29806: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:30733: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:31676: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:32616: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:33559: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:34505: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:35454: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:36402: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:37353: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:38313: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:39276: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:40236: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:41199: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:42167: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:43192: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:44163: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:45129: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:46098: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:47069: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:48068: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:49042: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:50013: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:51012: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:51988: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:53003: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:53978: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:54983: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:55981: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:56994: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-007-attempt-001/codex.stdout.txt:57994: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-017-attempt-011/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-011/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-017/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
src/autofdtd/boundaries/models.py:66:class PMLParams(TaggedModel):
src/autofdtd/boundaries/models.py:118:class PML(BoundaryEdge):
phase1/runs/task-012-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:363:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-012-attempt-001/codex.stdout.txt:364:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-012-attempt-001/codex.stdout.txt:369:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-012-attempt-001/codex.stdout.txt:459:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-012-attempt-001/codex.stdout.txt:1607:phase1/runs/task-012-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1629:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1639:phase1/runs/task-012-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1646:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1654:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1659:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1664:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1812:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1830:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:1903:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2005:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2010:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2015:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2034:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2039:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2066:phase1/runs/task-010-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2108:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2113:phase1/runs/task-010-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2118:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2123:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2128:phase1/runs/task-011-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2133:phase1/runs/task-011-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2453:phase1/runs/task-008-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2463:phase1/runs/task-008-attempt-001/codex.stdout.txt:1523:phase1/runs/task-008-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2465:phase1/runs/task-008-attempt-001/codex.stdout.txt:1549:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2467:phase1/runs/task-008-attempt-001/codex.stdout.txt:1560:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2470:phase1/runs/task-008-attempt-001/codex.stdout.txt:1573:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2472:phase1/runs/task-008-attempt-001/codex.stdout.txt:1576:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2474:phase1/runs/task-008-attempt-001/codex.stdout.txt:1603:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2476:phase1/runs/task-008-attempt-001/codex.stdout.txt:1613:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2478:phase1/runs/task-008-attempt-001/codex.stdout.txt:1631:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2480:phase1/runs/task-008-attempt-001/codex.stdout.txt:1633:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2482:phase1/runs/task-008-attempt-001/codex.stdout.txt:1647:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2484:phase1/runs/task-008-attempt-001/codex.stdout.txt:1662:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2486:phase1/runs/task-008-attempt-001/codex.stdout.txt:1664:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2488:phase1/runs/task-008-attempt-001/codex.stdout.txt:1667:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2490:phase1/runs/task-008-attempt-001/codex.stdout.txt:1669:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2492:phase1/runs/task-008-attempt-001/codex.stdout.txt:1671:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2494:phase1/runs/task-008-attempt-001/codex.stdout.txt:1683:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2496:phase1/runs/task-008-attempt-001/codex.stdout.txt:1708:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2498:phase1/runs/task-008-attempt-001/codex.stdout.txt:1711:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2500:phase1/runs/task-008-attempt-001/codex.stdout.txt:1826:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2504:phase1/runs/task-008-attempt-001/codex.stdout.txt:3389:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2506:phase1/runs/task-008-attempt-001/codex.stdout.txt:3402:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2520:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2538:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2543:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2827:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2835:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2845:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2853:phase1/runs/task-009-attempt-001/codex.stdout.txt:422:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2854:phase1/runs/task-009-attempt-001/codex.stdout.txt:464:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2856:phase1/runs/task-009-attempt-001/codex.stdout.txt:506:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2857:phase1/runs/task-009-attempt-001/codex.stdout.txt:520:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2859:phase1/runs/task-009-attempt-001/codex.stdout.txt:572:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2860:phase1/runs/task-009-attempt-001/codex.stdout.txt:613:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2861:phase1/runs/task-009-attempt-001/codex.stdout.txt:616:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2862:phase1/runs/task-009-attempt-001/codex.stdout.txt:879:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2863:phase1/runs/task-009-attempt-001/codex.stdout.txt:881:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2864:phase1/runs/task-009-attempt-001/codex.stdout.txt:899:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2865:phase1/runs/task-009-attempt-001/codex.stdout.txt:902:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2866:phase1/runs/task-009-attempt-001/codex.stdout.txt:908:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2867:phase1/runs/task-009-attempt-001/codex.stdout.txt:912:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2869:phase1/runs/task-009-attempt-001/codex.stdout.txt:916:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2870:phase1/runs/task-009-attempt-001/codex.stdout.txt:927:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2871:phase1/runs/task-009-attempt-001/codex.stdout.txt:1059:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2872:phase1/runs/task-009-attempt-001/codex.stdout.txt:1157:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2873:phase1/runs/task-009-attempt-001/codex.stdout.txt:1359:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2877:phase1/runs/task-009-attempt-001/codex.stdout.txt:2556:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2878:phase1/runs/task-009-attempt-001/codex.stdout.txt:2568:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2879:phase1/runs/task-009-attempt-001/codex.stdout.txt:2938:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:2880:phase1/runs/task-009-attempt-001/codex.stdout.txt:2987:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/codex.stdout.txt:3219:        "StablePML",
phase1/runs/task-012-attempt-001/codex.stdout.txt:3223:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-012-attempt-001/codex.stdout.txt:6149:    internal_absorbers: tuple[InternalAbsorber, ...] = Field(
phase1/runs/task-012-attempt-001/codex.stdout.txt:6151:        title="Internal Absorbers",
phase1/runs/task-012-attempt-001/codex.stdout.txt:6256:    def _shifted_internal_absorbers(self) -> list[InternalAbsorber]:
phase1/runs/task-012-attempt-001/codex.stdout.txt:9677:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-012-attempt-001/codex.stdout.txt:12020:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/loop.spec.md:175:## [RUNNING] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/loop.spec.md:176:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/loop.spec.md:178:Last Run: task-022-attempt-001
phase1/loop.spec.md:181:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-017-attempt-017/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-016/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-010/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-010/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-016/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-003-attempt-001/codex.stdout.txt:395:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-003-attempt-001/codex.stdout.txt:396:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-003-attempt-001/codex.stdout.txt:401:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-002-attempt-001/codex.stdout.txt:330:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-002-attempt-001/codex.stdout.txt:331:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-002-attempt-001/codex.stdout.txt:336:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-002-attempt-001/codex.stdout.txt:768:        "StablePML",
phase1/runs/task-002-attempt-001/codex.stdout.txt:772:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:1042:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1043:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1046:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1125:- Absorber kernels
phase1/runs/task-002-attempt-001/codex.stdout.txt:1179:    Absorber,
phase1/runs/task-002-attempt-001/codex.stdout.txt:1180:    AbsorberSpec,
phase1/runs/task-002-attempt-001/codex.stdout.txt:1184:    InternalAbsorber,
phase1/runs/task-002-attempt-001/codex.stdout.txt:1189:    StablePML,
phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-015/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-009/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-011-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-015/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-009/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-014/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-008/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-014/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-008/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/result.json:1:{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.","key_findings":["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.","Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.","`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.","Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.","Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed boundary IR classes and lowering helpers."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary scope documentation for the implemented Phase 1 subset."},{"path":"/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.","next_action":"none"}
phase1/runs/task-017-attempt-007/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-007/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-006/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-006/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-005/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-005/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-014-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-014-attempt-001/codex.stdout.txt:490:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-014-attempt-001/codex.stdout.txt:491:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-014-attempt-001/codex.stdout.txt:496:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-014-attempt-001/codex.stdout.txt:623:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-014-attempt-001/codex.stdout.txt:624:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-014-attempt-001/codex.stdout.txt:627:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-014-attempt-001/codex.stdout.txt:706:- Absorber kernels
phase1/runs/task-014-attempt-001/codex.stdout.txt:2299:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-014-attempt-001/codex.stdout.txt:2307:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/runs/task-014-attempt-001/codex.stdout.txt:3760:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-017-attempt-004/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-005-attempt-001/codex.stdout.txt:186:phase1/loop.spec.md:175:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-005-attempt-001/codex.stdout.txt:187:phase1/loop.spec.md:176:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-017-attempt-004/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-004-attempt-001/codex.stdout.txt:337:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-004-attempt-001/codex.stdout.txt:338:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-004-attempt-001/codex.stdout.txt:343:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-004-attempt-001/codex.stdout.txt:537:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-004-attempt-001/codex.stdout.txt:538:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-004-attempt-001/codex.stdout.txt:541:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-004-attempt-001/codex.stdout.txt:620:- Absorber kernels
phase1/runs/task-004-attempt-001/codex.stdout.txt:1022:        "StablePML",
phase1/runs/task-004-attempt-001/codex.stdout.txt:1026:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-004-attempt-001/codex.stdout.txt:1953:    Absorber,
phase1/runs/task-004-attempt-001/codex.stdout.txt:1954:    AbsorberSpec,
phase1/runs/task-004-attempt-001/codex.stdout.txt:1958:    InternalAbsorber,
phase1/runs/task-004-attempt-001/codex.stdout.txt:1963:    StablePML,
phase1/runs/task-004-attempt-001/codex.stdout.txt:2152:                    isinstance(bnd, (AbsorberSpec, ABCBoundary, ModeABCBoundary))
phase1/runs/task-004-attempt-001/codex.stdout.txt:2192:../tidy3d/tidy3d/components/boundary.py:61:    def _warn_num_layers(cls: type[AbsorberSpec], val: int) -> int:
phase1/runs/task-004-attempt-001/codex.stdout.txt:3223:) -> Callable[[type[AbsorberSpec], int], int]:
phase1/runs/task-004-attempt-001/codex.stdout.txt:3230:    def _warn_num_layers(cls: type[AbsorberSpec], val: int) -> int:
phase1/runs/task-004-attempt-001/codex.stdout.txt:3847:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:4583:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:5041:    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:5042:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:5323:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:5891:        "StablePML",
phase1/runs/task-004-attempt-001/codex.stdout.txt:5895:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:5953:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:6837:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:7639:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:8377:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:8706:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:9195:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:9524:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:10236:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:10565:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:11067:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:11396:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:11887:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:12216:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:12732:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:13061:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:13579:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:13908:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:14423:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:14752:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:15269:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:15598:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:16318:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:16664:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:17179:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:17525:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:18042:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:18388:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:19052:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:19398:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:19917:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:20263:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:20783:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:21129:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:21651:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:21997:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:22526:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:22872:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:23438:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:23784:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:24346:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:24692:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:25273:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:25619:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:26183:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:26529:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:27104:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:27462:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:28025:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:28383:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:28948:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:29306:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:29879:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:30237:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:30810:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:31168:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:31738:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:32096:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-004-attempt-001/codex.stdout.txt:32668:+_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-004-attempt-001/codex.stdout.txt:33026:     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-017-attempt-003/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-010-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-001-attempt-001/codex.stdout.txt:223:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-001-attempt-001/codex.stdout.txt:224:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-001-attempt-001/codex.stdout.txt:227:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-001-attempt-001/codex.stdout.txt:306:- Absorber kernels
phase1/runs/task-001-attempt-001/codex.stdout.txt:489:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-001-attempt-001/codex.stdout.txt:490:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-001-attempt-001/codex.stdout.txt:495:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-001-attempt-001/codex.stdout.txt:740:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-001-attempt-001/codex.stdout.txt:748:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/runs/task-001-attempt-001/codex.stdout.txt:869:    Absorber,
phase1/runs/task-001-attempt-001/codex.stdout.txt:870:    AbsorberSpec,
phase1/runs/task-001-attempt-001/codex.stdout.txt:874:    InternalAbsorber,
phase1/runs/task-001-attempt-001/codex.stdout.txt:879:    StablePML,
phase1/runs/task-001-attempt-001/codex.stdout.txt:2131:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2132:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2857:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2858:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3161:    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3162:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3797:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3798:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4525:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4526:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5251:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5252:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5977:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5978:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6857:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6858:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7587:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7588:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7931:76 |     FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7939:76 |     FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7941:77 |     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8781:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8782:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9587:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9588:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10367:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10368:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11234:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11235:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11518:126 |     FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11520:127 |     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12231:+    FeatureEntry("boundaries", "StablePML", FeatureStatus.IMPLEMENT, "Supported subset acceptable."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12232:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13109:+        "StablePML",
phase1/runs/task-001-attempt-001/codex.stdout.txt:13113:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14016:+        "StablePML",
phase1/runs/task-001-attempt-001/codex.stdout.txt:14020:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14329:  - StablePML [Implement]
phase1/runs/task-001-attempt-001/codex.stdout.txt:14330:  - Absorber [Implement]
phase1/runs/task-001-attempt-001/codex.stdout.txt:14957:+        "StablePML",
phase1/runs/task-001-attempt-001/codex.stdout.txt:14961:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15878:+        "StablePML",
phase1/runs/task-001-attempt-001/codex.stdout.txt:15882:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16787:+        "StablePML",
phase1/runs/task-001-attempt-001/codex.stdout.txt:16791:+    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-017-attempt-003/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-019/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-010-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-010-attempt-001/codex.stdout.txt:356:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-010-attempt-001/codex.stdout.txt:357:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-010-attempt-001/codex.stdout.txt:362:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-010-attempt-001/codex.stdout.txt:519:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-010-attempt-001/codex.stdout.txt:1565:        "StablePML",
phase1/runs/task-010-attempt-001/codex.stdout.txt:1569:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:2595:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2596:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2599:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2678:- Absorber kernels
phase1/runs/task-010-attempt-001/codex.stdout.txt: WARNING: stopped searching binary file after match (found "\0" byte around offset 160926)
phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-006-attempt-001/codex.stdout.txt:340:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-006-attempt-001/codex.stdout.txt:341:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-006-attempt-001/codex.stdout.txt:346:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-006-attempt-001/codex.stdout.txt:473:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-006-attempt-001/codex.stdout.txt:474:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-006-attempt-001/codex.stdout.txt:477:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-006-attempt-001/codex.stdout.txt:556:- Absorber kernels
phase1/runs/task-006-attempt-001/codex.stdout.txt:889:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-006-attempt-001/codex.stdout.txt:2769:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2777:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/runs/task-018-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-021-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-021-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-021-attempt-001/prompt.txt:199:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch and symmetry metadata now already live on that runtime contract.
phase1/runs/task-019-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-020-attempt-001/result.json:1:{"task_id":"task-020","status":"completed","summary":"Implemented BlochBoundary support plus symmetry-aware boundary metadata and phase-aware runtime transforms across the public API, IR, compiler, kernels, runtime wrapper, and tests.","key_findings":["`BlochBoundary` is now a first-class public boundary edge with `Boundary.bloch(...)`, strict both-sides validation, and explicit complex phase metadata derived from `bloch_vec`.","Boundary IR now distinguishes periodic, Bloch, and reflective axes, marks `requires_complex_fields`, and carries per-axis symmetry sign metadata for electric and magnetic field families.","Compiled boundary metadata now includes Bloch exchange kinds, per-face phase factors, active symmetry axes, and reusable symmetry transforms for future chunk-planning and reduced-domain exchange paths.","Boundary ghost updates now apply conjugate phase wrapping on minus Bloch faces and forward phase wrapping on plus faces, while separate helper transforms expose boundary-edge and symmetry remaps directly to runtime callers.","Focused validation passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py` and `python -m compileall src/autofdtd`."],"artifacts":[{"path":"src/autofdtd/boundaries/models.py","description":"Added `BlochBoundary`, public constructors, and boundary-pair validation rules."},{"path":"src/autofdtd/compiler/boundaries.py","description":"Extended compiled boundary metadata with Bloch phase factors and symmetry-axis transforms."},{"path":"src/autofdtd/ir/models.py","description":"Added typed Bloch and symmetry IR payloads plus boundary-spec lowering with complex-field requirements."},{"path":"src/autofdtd/kernels/boundaries.py","description":"Implemented phase-aware Bloch ghost updates and reusable boundary/symmetry transform helpers."},{"path":"src/autofdtd/runtime/boundaries.py","description":"Extended runtime boundary wrapper to compile symmetry metadata and expose transform helpers."},{"path":"tests/test_boundaries.py","description":"Added Bloch boundary, symmetry metadata, phase-transform, and runtime/IR coverage."},{"path":"phase1/working-memory.md","description":"Recorded Bloch runtime behavior and symmetry-metadata decisions for later boundary tasks."}],"error_summary":"","follow_up_notes":"Absorbing-boundary tasks can now extend the existing `CompiledBoundarySpec` contract without redesigning periodic/Bloch/symmetry lowering. No multi-GPU smoke was attempted because this task only touched boundary metadata and local ghost-transform behavior.","next_action":"none"}
phase1/runs/task-020-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-020-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-020-attempt-001/prompt.txt:197:- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:353:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-009-attempt-001/codex.stdout.txt:354:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-009-attempt-001/codex.stdout.txt:359:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-009-attempt-001/codex.stdout.txt:422:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:464:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:506:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:520:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:572:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:613:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:616:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:879:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:881:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:899:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:902:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:908:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:912:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:916:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:927:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:1059:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:1157:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:1359:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:2556:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:2568:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:2938:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:2987:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/codex.stdout.txt:10307:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:14160: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:14487: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:14899: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:15308: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:15741: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:16171: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:16620: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:17066: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:17540: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:18009: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:18475: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:18944: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:19518: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:20095: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:20713: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:21334: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:21970: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:22664: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:23355: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:24050: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:24787: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:25497: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:26204: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:26914: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:27743: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:28575: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:29483: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:30393: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:31320: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:32232: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:33143: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:34055: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:34969: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:35890: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:36861: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:37808: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:38752: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:39718: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:40681: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:41647: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:42628: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-009-attempt-001/codex.stdout.txt:43593: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-018-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-018-attempt-001/codex.stdout.txt:388:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-018-attempt-001/codex.stdout.txt:389:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-018-attempt-001/codex.stdout.txt:394:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-018-attempt-001/codex.stdout.txt:1289:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-018-attempt-001/codex.stdout.txt:1867:        "StablePML",
phase1/runs/task-018-attempt-001/codex.stdout.txt:1871:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:3102:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-018-attempt-001/codex.stdout.txt:3103:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-018-attempt-001/codex.stdout.txt:3106:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-011-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-011-attempt-001/codex.stdout.txt:623:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-011-attempt-001/codex.stdout.txt:624:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-011-attempt-001/codex.stdout.txt:629:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-011-attempt-001/codex.stdout.txt:758:| `StablePML` | Implement | May land as a supported subset first |
phase1/runs/task-011-attempt-001/codex.stdout.txt:759:| `Absorber` | Implement | Important practical fallback |
phase1/runs/task-011-attempt-001/codex.stdout.txt:762:| `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
phase1/runs/task-011-attempt-001/codex.stdout.txt:841:- Absorber kernels
phase1/runs/task-011-attempt-001/codex.stdout.txt:1177:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:2156:        "StablePML",
phase1/runs/task-011-attempt-001/codex.stdout.txt:2160:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-011-attempt-001/codex.stdout.txt:14614: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:15416: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:16221: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:17094: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:17970: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:18888: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:19930: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:20953: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:21874: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:22875: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:23879: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:24934: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:25992: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:27065: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:28225: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:29382: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:30563: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:31741: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:32922: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:34117: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:35315: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:36510: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:37709: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:38906: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:40142: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:41339: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:42538: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:43776: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:44974: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:46174: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:47401: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:48664: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:49895: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:51123: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:52423: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:54684: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-011-attempt-001/codex.stdout.txt:55914: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-016-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:378:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-016-attempt-001/codex.stdout.txt:379:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-016-attempt-001/codex.stdout.txt:384:Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.
phase1/runs/task-016-attempt-001/codex.stdout.txt:472:phase1/runs/task-016-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:497:phase1/runs/task-016-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:631:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:632:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:656:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:657:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:658:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:659:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:660:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:661:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/codex.stdout.txt:662:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families :Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2220:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:809:phase1/runs/task-013-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2221:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:833:phase1/runs/task-011-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2222:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:834:phase1/runs/task-014-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2223:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:838:phase1/runs/task-010-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2224:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:839:phase1/runs/task-010-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2225:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:858:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2226:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:876:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2227:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:877:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2228:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:883:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2229:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:897:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2230:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:905:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2231:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:906:phase1/runs/task-011-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2232:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:924:phase1/runs/task-012-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2233:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:932:phase1/runs/task-012-attempt-001/codex.stdout.txt:1607:phase1/runs/task-012-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2234:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:933:phase1/runs/task-012-attempt-001/codex.stdout.txt:1629:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2235:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:934:phase1/runs/task-012-attempt-001/codex.stdout.txt:1639:phase1/runs/task-012-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2236:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:935:phase1/runs/task-012-attempt-001/codex.stdout.txt:1646:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2237:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:936:phase1/runs/task-012-attempt-001/codex.stdout.txt:1654:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2238:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:937:phase1/runs/task-012-attempt-001/codex.stdout.txt:1659:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2239:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:938:phase1/runs/task-012-attempt-001/codex.stdout.txt:1664:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2240:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:939:phase1/runs/task-012-attempt-001/codex.stdout.txt:1812:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2241:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:940:phase1/runs/task-012-attempt-001/codex.stdout.txt:1830:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2242:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:941:phase1/runs/task-012-attempt-001/codex.stdout.txt:1903:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2243:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:942:phase1/runs/task-012-attempt-001/codex.stdout.txt:2005:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2244:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:943:phase1/runs/task-012-attempt-001/codex.stdout.txt:2010:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2245:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:944:phase1/runs/task-012-attempt-001/codex.stdout.txt:2015:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2246:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:945:phase1/runs/task-012-attempt-001/codex.stdout.txt:2034:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2247:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:946:phase1/runs/task-012-attempt-001/codex.stdout.txt:2039:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2248:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:947:phase1/runs/task-012-attempt-001/codex.stdout.txt:2066:phase1/runs/task-010-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2249:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:948:phase1/runs/task-012-attempt-001/codex.stdout.txt:2108:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2250:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:949:phase1/runs/task-012-attempt-001/codex.stdout.txt:2113:phase1/runs/task-010-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2251:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:950:phase1/runs/task-012-attempt-001/codex.stdout.txt:2118:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2252:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:951:phase1/runs/task-012-attempt-001/codex.stdout.txt:2123:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2253:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:952:phase1/runs/task-012-attempt-001/codex.stdout.txt:2128:phase1/runs/task-011-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2254:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:953:phase1/runs/task-012-attempt-001/codex.stdout.txt:2133:phase1/runs/task-011-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2255:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:954:phase1/runs/task-012-attempt-001/codex.stdout.txt:2453:phase1/runs/task-008-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2256:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:955:phase1/runs/task-012-attempt-001/codex.stdout.txt:2463:phase1/runs/task-008-attempt-001/codex.stdout.txt:1523:phase1/runs/task-008-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2257:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:956:phase1/runs/task-012-attempt-001/codex.stdout.txt:2465:phase1/runs/task-008-attempt-001/codex.stdout.txt:1549:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2258:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:957:phase1/runs/task-012-attempt-001/codex.stdout.txt:2467:phase1/runs/task-008-attempt-001/codex.stdout.txt:1560:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2259:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:958:phase1/runs/task-012-attempt-001/codex.stdout.txt:2470:phase1/runs/task-008-attempt-001/codex.stdout.txt:1573:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2260:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:959:phase1/runs/task-012-attempt-001/codex.stdout.txt:2472:phase1/runs/task-008-attempt-001/codex.stdout.txt:1576:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2261:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:960:phase1/runs/task-012-attempt-001/codex.stdout.txt:2474:phase1/runs/task-008-attempt-001/codex.stdout.txt:1603:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2262:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:961:phase1/runs/task-012-attempt-001/codex.stdout.txt:2476:phase1/runs/task-008-attempt-001/codex.stdout.txt:1613:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2263:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:962:phase1/runs/task-012-attempt-001/codex.stdout.txt:2478:phase1/runs/task-008-attempt-001/codex.stdout.txt:1631:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2264:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:963:phase1/runs/task-012-attempt-001/codex.stdout.txt:2480:phase1/runs/task-008-attempt-001/codex.stdout.txt:1633:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2265:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:964:phase1/runs/task-012-attempt-001/codex.stdout.txt:2482:phase1/runs/task-008-attempt-001/codex.stdout.txt:1647:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2266:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:965:phase1/runs/task-012-attempt-001/codex.stdout.txt:2484:phase1/runs/task-008-attempt-001/codex.stdout.txt:1662:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2267:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:966:phase1/runs/task-012-attempt-001/codex.stdout.txt:2486:phase1/runs/task-008-attempt-001/codex.stdout.txt:1664:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2268:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:967:phase1/runs/task-012-attempt-001/codex.stdout.txt:2488:phase1/runs/task-008-attempt-001/codex.stdout.txt:1667:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2269:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:968:phase1/runs/task-012-attempt-001/codex.stdout.txt:2490:phase1/runs/task-008-attempt-001/codex.stdout.txt:1669:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2270:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:969:phase1/runs/task-012-attempt-001/codex.stdout.txt:2492:phase1/runs/task-008-attempt-001/codex.stdout.txt:1671:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2271:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:970:phase1/runs/task-012-attempt-001/codex.stdout.txt:2494:phase1/runs/task-008-attempt-001/codex.stdout.txt:1683:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2272:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:971:phase1/runs/task-012-attempt-001/codex.stdout.txt:2496:phase1/runs/task-008-attempt-001/codex.stdout.txt:1708:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2273:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:972:phase1/runs/task-012-attempt-001/codex.stdout.txt:2498:phase1/runs/task-008-attempt-001/codex.stdout.txt:1711:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2274:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:973:phase1/runs/task-012-attempt-001/codex.stdout.txt:2500:phase1/runs/task-008-attempt-001/codex.stdout.txt:1826:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2275:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:974:phase1/runs/task-012-attempt-001/codex.stdout.txt:2504:phase1/runs/task-008-attempt-001/codex.stdout.txt:3389:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2276:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:975:phase1/runs/task-012-attempt-001/codex.stdout.txt:2506:phase1/runs/task-008-attempt-001/codex.stdout.txt:3402:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2277:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:976:phase1/runs/task-012-attempt-001/codex.stdout.txt:2520:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2278:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:977:phase1/runs/task-012-attempt-001/codex.stdout.txt:2538:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2279:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:978:phase1/runs/task-012-attempt-001/codex.stdout.txt:2543:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2280:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:979:phase1/runs/task-012-attempt-001/codex.stdout.txt:2827:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2281:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:980:phase1/runs/task-012-attempt-001/codex.stdout.txt:2835:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2282:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:981:phase1/runs/task-012-attempt-001/codex.stdout.txt:2845:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2283:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:982:phase1/runs/task-012-attempt-001/codex.stdout.txt:2853:phase1/runs/task-009-attempt-001/codex.stdout.txt:422:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2284:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:983:phase1/runs/task-012-attempt-001/codex.stdout.txt:2854:phase1/runs/task-009-attempt-001/codex.stdout.txt:464:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2285:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:984:phase1/runs/task-012-attempt-001/codex.stdout.txt:2856:phase1/runs/task-009-attempt-001/codex.stdout.txt:506:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2286:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:985:phase1/runs/task-012-attempt-001/codex.stdout.txt:2857:phase1/runs/task-009-attempt-001/codex.stdout.txt:520:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2287:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:986:phase1/runs/task-012-attempt-001/codex.stdout.txt:2859:phase1/runs/task-009-attempt-001/codex.stdout.txt:572:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2288:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:987:phase1/runs/task-012-attempt-001/codex.stdout.txt:2860:phase1/runs/task-009-attempt-001/codex.stdout.txt:613:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2289:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:988:phase1/runs/task-012-attempt-001/codex.stdout.txt:2861:phase1/runs/task-009-attempt-001/codex.stdout.txt:616:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2290:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:989:phase1/runs/task-012-attempt-001/codex.stdout.txt:2862:phase1/runs/task-009-attempt-001/codex.stdout.txt:879:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2291:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:990:phase1/runs/task-012-attempt-001/codex.stdout.txt:2863:phase1/runs/task-009-attempt-001/codex.stdout.txt:881:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2292:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:991:phase1/runs/task-012-attempt-001/codex.stdout.txt:2864:phase1/runs/task-009-attempt-001/codex.stdout.txt:899:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2293:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:992:phase1/runs/task-012-attempt-001/codex.stdout.txt:2865:phase1/runs/task-009-attempt-001/codex.stdout.txt:902:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2294:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:993:phase1/runs/task-012-attempt-001/codex.stdout.txt:2866:phase1/runs/task-009-attempt-001/codex.stdout.txt:908:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2295:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:994:phase1/runs/task-012-attempt-001/codex.stdout.txt:2867:phase1/runs/task-009-attempt-001/codex.stdout.txt:912:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2296:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:995:phase1/runs/task-012-attempt-001/codex.stdout.txt:2869:phase1/runs/task-009-attempt-001/codex.stdout.txt:916:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2297:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:996:phase1/runs/task-012-attempt-001/codex.stdout.txt:2870:phase1/runs/task-009-attempt-001/codex.stdout.txt:927:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2298:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:997:phase1/runs/task-012-attempt-001/codex.stdout.txt:2871:phase1/runs/task-009-attempt-001/codex.stdout.txt:1059:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2299:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:998:phase1/runs/task-012-attempt-001/codex.stdout.txt:2872:phase1/runs/task-009-attempt-001/codex.stdout.txt:1157:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2300:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:999:phase1/runs/task-012-attempt-001/codex.stdout.txt:2873:phase1/runs/task-009-attempt-001/codex.stdout.txt:1359:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2301:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1000:phase1/runs/task-012-attempt-001/codex.stdout.txt:2877:phase1/runs/task-009-attempt-001/codex.stdout.txt:2556:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2302:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1001:phase1/runs/task-012-attempt-001/codex.stdout.txt:2878:phase1/runs/task-009-attempt-001/codex.stdout.txt:2568:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2303:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1002:phase1/runs/task-012-attempt-001/codex.stdout.txt:2879:phase1/runs/task-009-attempt-001/codex.stdout.txt:2938:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2304:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1003:phase1/runs/task-012-attempt-001/codex.stdout.txt:2880:phase1/runs/task-009-attempt-001/codex.stdout.txt:2987:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2305:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1019:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2306:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1027:phase1/runs/task-009-attempt-001/codex.stdout.txt:422:phase1/runs/task-009-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2307:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1028:phase1/runs/task-009-attempt-001/codex.stdout.txt:464:phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2308:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1029:phase1/runs/task-009-attempt-001/codex.stdout.txt:506:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2309:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1030:phase1/runs/task-009-attempt-001/codex.stdout.txt:520:phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2310:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1031:phase1/runs/task-009-attempt-001/codex.stdout.txt:572:phase1/runs/task-004-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2311:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1032:phase1/runs/task-009-attempt-001/codex.stdout.txt:613:phase1/runs/task-004-attempt-001/codex.stdout.txt:1480:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2312:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1033:phase1/runs/task-009-attempt-001/codex.stdout.txt:616:phase1/runs/task-004-attempt-001/codex.stdout.txt:1636:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2313:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1034:phase1/runs/task-009-attempt-001/codex.stdout.txt:879:phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2314:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1035:phase1/runs/task-009-attempt-001/codex.stdout.txt:881:phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2315:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1036:phase1/runs/task-009-attempt-001/codex.stdout.txt:899:phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2316:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1037:phase1/runs/task-009-attempt-001/codex.stdout.txt:902:phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2317:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1038:phase1/runs/task-009-attempt-001/codex.stdout.txt:908:phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2318:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1039:phase1/runs/task-009-attempt-001/codex.stdout.txt:912:phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2319:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1040:phase1/runs/task-009-attempt-001/codex.stdout.txt:916:phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2320:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1041:phase1/runs/task-009-attempt-001/codex.stdout.txt:927:phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2321:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1042:phase1/runs/task-009-attempt-001/codex.stdout.txt:1059:phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2322:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1043:phase1/runs/task-009-attempt-001/codex.stdout.txt:1157:phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2323:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1044:phase1/runs/task-009-attempt-001/codex.stdout.txt:1359:phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2324:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1045:phase1/runs/task-009-attempt-001/codex.stdout.txt:2556:phase1/runs/task-007-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2325:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1046:phase1/runs/task-009-attempt-001/codex.stdout.txt:2568:phase1/runs/task-007-attempt-001/codex.stdout.txt:398:phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2326:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1047:phase1/runs/task-009-attempt-001/codex.stdout.txt:2938:phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2327:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1048:phase1/runs/task-009-attempt-001/codex.stdout.txt:2987:phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2328:/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt:1049:phase1/runs/task-015-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-019-attempt-001/codex.stdout.txt:2769:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:5657:) -> Callable[[type[AbsorberSpec], int], int]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:5664:    def _warn_num_layers(cls: type[AbsorberSpec], val: int) -> int:
phase1/runs/task-019-attempt-001/codex.stdout.txt:5898:../tidy3d/tidy3d/components/boundary.py:701:class AbsorberSpec(BoundaryEdge):
phase1/runs/task-019-attempt-001/codex.stdout.txt:5918:../tidy3d/tidy3d/components/boundary.py:1052:            self.plus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:5919:../tidy3d/tidy3d/components/boundary.py:1053:        ) + isinstance(self.minus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary))
phase1/runs/task-019-attempt-001/codex.stdout.txt:6998:            self.plus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:6999:        ) + isinstance(self.minus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary))
phase1/runs/task-019-attempt-001/codex.stdout.txt:7265:        parameters: PMLParams = DefaultStablePMLParameters,
phase1/runs/task-019-attempt-001/codex.stdout.txt:7280:        plus = StablePML(num_layers=num_layers, parameters=parameters)
phase1/runs/task-019-attempt-001/codex.stdout.txt:7281:        minus = StablePML(num_layers=num_layers, parameters=parameters)
phase1/runs/task-019-attempt-001/codex.stdout.txt:7288:        parameters: PMLParams = DefaultAbsorberParameters,
phase1/runs/task-019-attempt-001/codex.stdout.txt:7303:        plus = Absorber(num_layers=num_layers, parameters=parameters)
phase1/runs/task-019-attempt-001/codex.stdout.txt:7304:        minus = Absorber(num_layers=num_layers, parameters=parameters)
phase1/runs/task-019-attempt-001/codex.stdout.txt:7552:        "StablePML",
phase1/runs/task-019-attempt-001/codex.stdout.txt:7556:    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
phase1/runs/task-019-attempt-001/codex.stdout.txt:9093: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:9443: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:9796: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:10146: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:10538: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:10927: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:11376: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:11822: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:12328: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:12831: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:13337: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:13476:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:13479:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:13498:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:13540:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:13567:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:14019: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:14158:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:14161:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:14180:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:14222:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:14249:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:14703: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:14842:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:14845:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:14864:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:14906:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:14933:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:15500: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:15639:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:15642:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:15661:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:15703:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:15730:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:16185: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:16268:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:16299:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:16390:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:16393:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:16412:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:16454:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:16481:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:16933: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:17016:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:17047:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:17138:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:17141:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:17160:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:17202:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:17229:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:17719:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:17722:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:17798:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:17822:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:17856: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:17939:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:17970:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:18061:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:18064:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:18083:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:18125:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:18152:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:18639:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:18642:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:18718:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:18742:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:18776: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:18859:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:18890:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:18981:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:18984:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:19003:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:19045:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:19072:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:19562:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:19565:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:19641:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:19665:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:19699: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:19782:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:19813:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:19904:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:19907:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:19926:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:19968:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:19995:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:20032:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:20116:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:20603:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:20606:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:20682:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:20706:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:20740: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:20823:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:20854:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:20945:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:20948:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:20967:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:21009:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:21036:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:21073:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:21157:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:21647:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:21650:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:21726:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:21750:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:21784: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:21867:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:21898:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:21989:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:21992:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:22011:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:22053:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:22080:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:22117:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:22201:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:22236:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:22265:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:22734:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:22737:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:22813:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:22837:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:22871: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:22954:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:22985:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:23076:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:23079:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:23098:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:23140:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:23167:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:23204:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:23288:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:23323:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:23352:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:23779:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:23793:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:23868:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:23871:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:23947:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:23971:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:24005: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:24088:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:24119:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:24210:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:24213:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:24232:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:24274:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:24301:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:24338:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:24422:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:24457:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:24486:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:24910:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:24924:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:24999:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:25002:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:25078:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:25102:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:25136: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:25219:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:25250:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:25341:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:25344:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:25363:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:25405:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:25432:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:25469:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:25553:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:25588:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:25617:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:26044:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:26058:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:26133:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:26136:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:26212:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:26236:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:26270: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:26353:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:26384:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:26475:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:26478:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:26497:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:26539:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:26566:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:26636:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:26720:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:26755:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:26784:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:27208:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:27222:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:27297:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:27300:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:27376:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:27400:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:27434: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:27517:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:27548:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:27639:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:27642:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:27661:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:27703:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:27730:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:27800:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:27884:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:27919:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:27948:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:28375:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:28389:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:28464:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:28467:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:28543:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:28567:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:28601: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:28684:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:28715:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:28806:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:28809:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:28828:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:28870:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:28897:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:28967:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:29051:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:29105:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:29134:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:29558:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:29572:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:29647:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:29650:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:29726:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:29750:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:29784: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:29867:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:29898:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:29989:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:29992:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:30011:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:30053:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:30080:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:30150:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:30234:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:30288:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:30317:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:30744:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:30758:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:30833:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:30836:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:30912:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:30936:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:30970: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:31053:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:31084:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:31175:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:31178:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:31197:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:31239:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:31266:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:31336:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:31420:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:31474:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:31503:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:31927:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:31941:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:32016:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:32019:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:32095:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:32119:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:32153: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:32236:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:32267:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:32358:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:32361:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:32380:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:32422:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:32449:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:32519:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:32603:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:32657:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:32686:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:33113:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:33127:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:33202:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:33205:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:33281:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:33305:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:33339: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:33422:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:33453:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:33544:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:33547:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:33566:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:33608:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:33635:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:33705:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:33789:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:33843:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:33872:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:33891:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:33989:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:34410:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:34424:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:34499:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:34502:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:34578:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:34602:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:34636: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:34719:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:34750:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:34841:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:34844:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:34863:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:34905:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:34932:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:35002:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:35086:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:35140:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:35169:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:35188:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:35286:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:35710:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:35724:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:35799:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:35802:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:35878:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:35902:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:35936: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:36044:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:36075:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:36166:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:36169:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:36188:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:36230:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:36257:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:36327:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:36411:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:36465:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:36494:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:36513:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:36611:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:37032:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:37046:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:37121:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:37124:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:37200:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:37224:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:37258: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:37366:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:37397:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:37488:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:37491:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:37510:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:37552:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:37579:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:37649:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:37733:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:37787:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:37816:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:37835:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:37933:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:37963:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:37977:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:38399:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:38413:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:38488:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:38491:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:38567:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:38591:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:38625: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:38733:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:38764:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:38855:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:38858:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:38877:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:38919:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:38946:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:39016:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:39100:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:39154:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:39183:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:39202:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:39300:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:39327:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:39341:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:39763:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:39777:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:39852:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:39855:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:39931:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:39955:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:39989: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:40097:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:40128:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:40219:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:40222:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:40241:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:40283:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:40310:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:40380:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:40464:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:40518:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:40547:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:40566:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:40664:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:40694:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:40708:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:41143:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:41157:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:41232:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:41235:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:41311:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:41335:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:41369: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:41477:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:41508:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:41599:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:41602:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:41621:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:41663:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:41690:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:41760:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:41844:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:41898:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:41927:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:41946:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:42044:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:42071:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:42085:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:42520:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:42534:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:42609:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:42612:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:42688:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:42712:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:42746: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:42854:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:42885:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:42976:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:42979:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:42998:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:43040:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:43067:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:43137:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:43221:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:43275:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:43304:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:43323:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:43421:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:43469:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:43483:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:43918:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:43932:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:44007:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:44010:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:44086:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:44110:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:44144: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:44252:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:44283:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:44374:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:44377:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:44396:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:44438:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:44465:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:44535:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:44619:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:44673:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:44702:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:44721:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:44819:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:44864:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:44878:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:45313:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:45327:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:45402:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:45405:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:45481:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:45505:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:45539: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:45647:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:45678:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:45769:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:45772:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:45791:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:45833:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:45860:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:45930:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:46014:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:46068:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:46097:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:46116:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:46214:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:46262:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:46276:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:46723:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:46737:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:46812:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:46815:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:46891:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:46915:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:46949: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:47057:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:47088:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:47179:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:47182:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:47201:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:47243:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:47270:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:47340:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:47424:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:47478:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:47507:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:47526:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:47624:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:47669:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:47683:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:48130:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:48144:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:48219:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:48222:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:48298:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:48322:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:48356: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:48464:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:48495:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:48586:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:48589:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:48608:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:48650:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:48677:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:48747:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:48831:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:48885:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:48914:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:48933:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:49031:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:49108:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:49122:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:49569:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:49583:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:49658:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:49661:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:49737:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:49761:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:49795: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:49903:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:49934:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:50025:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:50028:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:50047:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:50089:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:50116:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:50186:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:50270:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:50324:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:50353:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:50372:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:50470:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:50518:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:50532:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:50979:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:50993:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:51068:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:51071:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:51147:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:51171:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:51205: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:51332:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:51363:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:51454:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:51457:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:51476:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:51518:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:51545:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:51615:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:51699:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:51753:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:51782:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:51801:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:51899:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:51944:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:51958:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:52405:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:52419:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:52494:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:52497:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:52573:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:52597:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:52631: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:52758:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:52789:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:52880:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:52883:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:52902:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:52944:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:52971:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:53041:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:53125:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:53179:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:53208:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:53227:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:53325:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:53373:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:53387:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:53843:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:53857:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:53932:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:53935:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:54011:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:54035:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:54069: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:54196:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:54227:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:54318:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:54321:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:54340:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:54382:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:54409:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:54479:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:54563:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:54617:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:54646:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:54665:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:54763:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:54808:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:54822:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:55278:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:55292:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:55367:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:55370:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:55446:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:55470:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:55504: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:55631:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:55662:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:55753:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:55756:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:55775:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:55817:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:55844:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:55914:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:55998:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:56052:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:56081:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:56100:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:56198:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:56214:    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:56277:    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:56720:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:56734:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:57190:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:57204:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:57279:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:57282:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:57358:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:57382:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:57416: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:57543:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:57574:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:57665:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:57668:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:57687:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:57729:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:57756:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:57826:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:57910:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:57964:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:57993:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:58012:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:58110:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:58159:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:58173:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:58629:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:58643:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:58718:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:58721:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:58797:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:58821:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:58855: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:58982:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:59013:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:59104:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:59107:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:59126:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:59168:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:59195:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:59265:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:59349:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:59403:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:59432:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:59451:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:59549:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:59638:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:59652:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:60108:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:60122:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:60197:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:60200:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:60276:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:60300:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:60334: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:60461:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:60492:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:60583:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:60586:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:60605:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:60647:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:60674:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:60744:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:60828:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:60882:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:60911:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:60930:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:61028:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:61120:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:61134:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:61590:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:61604:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:61679:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:61682:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:61758:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:61782:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:61816: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:61943:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:61974:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:62065:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:62068:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:62087:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:62129:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:62156:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:62226:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:62310:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:62364:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:62393:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:62412:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:62510:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:62857:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:62871:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:63327:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:63341:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:63416:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:63419:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:63495:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:63519:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:63553: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:63680:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:63711:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:63802:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:63805:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:63824:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:63866:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:63893:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:63963:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:64047:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:64101:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:64130:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:64149:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:64247:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:64292:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:64306:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:64762:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:64776:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:64851:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:64854:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:64930:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:64954:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:64988: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:65115:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:65146:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:65237:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:65240:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:65259:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:65301:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:65328:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:65398:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:65482:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:65536:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:65565:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:65584:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:65682:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:65730:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:65744:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:66200:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:66214:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:66289:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:66292:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:66368:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:66392:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:66426: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:66553:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:66584:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:66675:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:66678:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:66697:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:66739:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:66766:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:66836:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:66920:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:66974:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:67003:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:67022:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:67119:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:67164:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:67178:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:67634:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:67648:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:67723:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:67726:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:67802:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:67826:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:67860: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:67987:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:68018:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:68109:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:68112:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:68131:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:68173:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:68200:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:68270:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:68354:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:68408:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:68437:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:68456:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:68553:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:68605:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:68619:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:69075:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:69089:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:69164:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:69167:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:69243:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:69267:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:69301: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:69428:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:69459:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:69550:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:69553:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:69572:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:69614:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:69641:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:69711:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:69795:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:69849:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:69878:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:69897:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:69994:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:70095:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:70109:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:70565:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:70579:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:70654:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:70657:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:70733:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:70757:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:70791: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:70918:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:70949:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:71040:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:71043:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:71062:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:71104:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:71131:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:71201:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:71285:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:71339:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:71368:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:71387:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:71484:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:71529:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:71543:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:71999:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:72013:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:72088:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:72091:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:72167:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:72191:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:72225: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:72352:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:72383:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:72474:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:72477:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:72496:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:72538:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:72565:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:72635:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:72719:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:72773:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:72802:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:72821:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:72918:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:72965:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:72979:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:73435:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:73449:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:73524:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:73527:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:73603:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:73627:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:73661: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:73788:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:73819:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:73910:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:73913:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:73932:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:73974:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:74001:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:74071:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:74155:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:74209:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:74238:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:74257:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:74354:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:74457:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:74471:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:74927:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:74941:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:75016:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:75019:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:75095:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:75119:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:75153: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:75280:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:75311:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:75402:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:75405:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:75424:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:75466:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:75493:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:75563:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:75647:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:75701:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:75730:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:75749:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:75846:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:75891:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:75905:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:76361:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:76375:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:76450:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:76453:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:76529:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:76553:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:76587: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:76714:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:76745:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:76836:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:76839:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:76858:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:76900:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:76927:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:76997:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:77081:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:77135:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:77164:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:77183:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:77280:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:77327:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:77341:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:77797:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:77811:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:77886:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:77889:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:77965:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:77989:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:78023: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:78150:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:78181:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:78272:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:78275:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:78294:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:78336:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:78363:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:78433:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:78517:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:78571:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:78600:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:78619:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:78716:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:78794:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:78808:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:79264:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:79278:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:79353:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:79356:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:79432:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:79456:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:79490: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:79617:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:79648:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:79739:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:79742:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:79761:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:79803:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:79830:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:79900:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:79984:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:80038:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:80067:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:80086:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:80183:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:80307:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:80321:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:80777:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:80791:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:80866:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:80869:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:80945:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:80969:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:81003: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:81130:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:81161:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:81252:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:81255:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:81274:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:81316:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:81343:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:81413:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:81497:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:81551:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:81580:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:81599:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:81696:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:81746:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:81760:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:82216:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:82230:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:82305:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:82308:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:82384:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:82408:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:82442: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:82569:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:82600:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:82691:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:82694:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:82713:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:82755:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:82782:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:82852:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:82936:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:82990:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:83019:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:83038:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:83135:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:83183:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:83197:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:83673:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:83687:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:83762:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:83765:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:83841:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:83865:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:83899: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:84026:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:84057:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:84148:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:84151:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:84170:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:84212:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:84239:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:84309:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:84393:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:84447:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:84476:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:84495:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:84592:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:84637:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:84651:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:85127:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:85141:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:85216:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:85219:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:85295:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:85319:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:85353: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:85480:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:85511:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:85602:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:85605:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:85624:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:85666:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:85693:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:85763:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:85847:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:85901:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:85930:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:85949:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:86046:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:86094:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:86108:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:86166:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:86184:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:86612:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:86626:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:86701:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:86704:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:86780:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:86804:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:86838: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:86965:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:86996:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:87087:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:87090:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:87109:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:87151:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:87178:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:87248:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:87332:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:87386:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:87415:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:87434:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:87531:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:87576:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:87590:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:87648:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:87666:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:88094:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:88108:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:88183:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:88186:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:88262:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:88286:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:88320: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:88447:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:88478:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:88569:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:88572:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:88591:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:88633:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:88660:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:88730:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:88814:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:88868:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:88897:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:88916:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:89013:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:89064:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:89078:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:89136:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:89154:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:89582:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:89596:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:89671:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:89674:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:89750:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:89774:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:89808: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:89935:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:89966:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:90057:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:90060:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:90079:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:90121:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:90148:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:90218:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:90302:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:90356:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:90385:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:90404:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:90501:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:90549:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:90563:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:90618:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:90628:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:90646:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:91074:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91088:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91163:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91166:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91242:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91266:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91300: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:91427:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91458:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91549:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91552:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91571:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91613:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91640:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91710:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91794:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91848:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91877:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91896:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:91993:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92038:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92052:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:92107:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:92117:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92135:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92563:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92577:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92652:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92655:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:92731:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92755:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:92789: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:92916:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92947:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93038:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:93041:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93060:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93102:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93129:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93199:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:93283:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93337:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:93366:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93385:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:93482:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93582:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93596:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:93651:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:93661:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93679:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:94107:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94121:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94196:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94199:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94275:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94299:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94333: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:94460:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94491:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94582:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94585:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94604:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94646:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94673:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94743:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94827:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94881:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94910:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94929:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:95026:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95074:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95088:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:95143:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:95153:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95171:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95599:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95613:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95688:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95691:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:95771:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95795:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:95829: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:95956:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95987:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96078:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:96081:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96100:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96142:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96169:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96239:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:96323:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96377:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:96406:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96425:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:96522:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96567:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96581:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:96636:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:96646:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96664:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:97092:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97106:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97181:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97184:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97264:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97288:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97322: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:97449:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97480:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97571:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97574:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97593:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97635:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97662:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97732:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97816:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97870:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97899:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97918:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:98015:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98063:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98077:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:98132:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:98142:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98160:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98588:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98602:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98677:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98680:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:98760:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98784:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:98818: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:98949:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98980:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99071:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:99074:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99093:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99135:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99162:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99232:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:99316:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99370:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:99399:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99418:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:99515:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99560:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99574:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:99629:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:99639:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99657:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:100085:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100099:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100174:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100177:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100257:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100281:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100315: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:100446:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100477:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100568:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100571:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100590:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100632:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100659:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100729:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100813:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100867:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100896:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100915:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:101012:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101060:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101074:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:101129:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:101139:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101157:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101585:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101599:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101674:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101677:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:101757:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101781:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:101815: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:101946:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101977:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102068:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:102071:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102130:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102200:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102227:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102295:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:102379:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102433:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:102462:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102481:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:102578:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102623:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102637:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:102692:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:102702:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102720:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:103148:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103162:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103237:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103240:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103320:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103344:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103378: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:103509:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103540:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103631:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103634:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103693:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103763:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103790:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103858:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:103942:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103996:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:104025:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104044:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:104141:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104189:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104203:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:104258:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:104268:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104286:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104714:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104728:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104803:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:104806:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104886:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104910:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:104944: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:105075:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105106:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105197:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:105200:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105259:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105329:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105356:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105424:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:105514:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105568:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:105597:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105616:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:105713:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105758:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105772:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:105827:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:105837:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105855:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:106283:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106297:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106372:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106375:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106455:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106479:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:106513: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:106644:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106675:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106766:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106769:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106828:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106898:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106925:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:106993:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:107083:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107137:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:107166:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107185:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:107282:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107330:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107344:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:107399:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:107409:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107427:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107855:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107869:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107944:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:107947:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108027:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108051:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108085: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:108216:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108247:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108338:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108341:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108400:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108470:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108497:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108565:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108655:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108709:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108738:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108757:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:108857:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108902:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108916:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:108971:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:108981:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108999:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:109427:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109441:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109516:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109519:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109599:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109623:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:109657: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:109788:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109819:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109910:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109913:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109972:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:110042:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110069:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110137:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:110227:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110281:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:110310:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110329:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:110429:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110491:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110505:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:110560:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:110570:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110588:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:111016:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111030:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111105:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111108:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111188:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111212:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111246: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:111377:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111408:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111499:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111502:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111561:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111631:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111658:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111726:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111816:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111870:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111899:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111918:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:112018:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112090:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112104:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:112159:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:112169:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112187:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112615:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112629:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112704:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112707:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:112787:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112811:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:112845: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:112976:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113007:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113098:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:113101:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113160:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113230:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113257:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113325:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:113415:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113469:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:113498:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113517:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:113617:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113714:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113728:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:113783:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:113793:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113811:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:114239:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114253:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114328:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114331:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114411:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114435:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:114469: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:114600:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114631:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114722:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114725:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114784:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114854:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114881:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:114949:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:115039:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115093:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:115122:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115141:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:115241:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115245:{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.","key_findings":["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.","Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.","`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.","Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.","Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed boundary IR classes and lowering helpers."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary scope documentation for the implemented Phase 1 subset."},{"path":"/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:115288:+  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115302:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:115357:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:115367:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115385:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115813:+    CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115827:+    "CompiledBoundarySpec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115902:+class CompiledBoundarySpec(AutoFDTDModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:115905:+    type: str = "CompiledBoundarySpec"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115985:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116009:+    return CompiledBoundarySpec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116043: _ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
phase1/runs/task-019-attempt-001/codex.stdout.txt:116174:+    BoundarySpecIR,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116205:+    "BoundarySpecIR",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116296:+class BoundarySpecIR(IRModel):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116299:+    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116358:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116428:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116455:+    return BoundarySpecIR(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116523:+from autofdtd.compiler.boundaries import BoundaryMode, CompiledBoundaryAxis, CompiledBoundarySpec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116613:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116667:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116696:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116715:+from autofdtd.ir import BoundarySpecIR, simulation_to_ir
phase1/runs/task-019-attempt-001/codex.stdout.txt:116815:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116820:{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.","key_findings":["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.","Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.","`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.","Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.","Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed boundary IR classes and lowering helpers."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary scope documentation for the implemented Phase 1 subset."},{"path":"/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.","next_action":"none"}

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary models for the foundational Phase 1 boundary surface."""

from __future__ import annotations

import cmath
import warnings
from collections.abc import Mapping
from typing import Literal

from pydantic import Field, field_validator, model_validator

from autofdtd.core.models import TaggedModel


def _normalize_optional_name(value: str | None) -> str | None:
    if value is None:
        return None
    stripped = value.strip()
    if not stripped:
        raise ValueError("names must not be empty or whitespace-only")
    return stripped


class BoundaryEdge(TaggedModel):
    """Base model for one simulation-domain boundary edge."""

    name: str | None = None

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_optional_name(value)


class Periodic(BoundaryEdge):
    """Periodic boundary condition."""

    type: Literal["Periodic"] = "Periodic"


class BlochBoundary(BoundaryEdge):
    """Phase-aware periodic boundary condition."""

    type: Literal["BlochBoundary"] = "BlochBoundary"
    bloch_vec: float

    @property
    def bloch_phase(self) -> complex:
        """Return the forward phase factor for a positive-domain wrap."""

        return cmath.exp(1j * 2.0 * cmath.pi * self.bloch_vec)


class PECBoundary(BoundaryEdge):
    """Perfect electric conductor boundary condition."""

    type: Literal["PECBoundary"] = "PECBoundary"


class PMCBoundary(BoundaryEdge):
    """Perfect magnetic conductor boundary condition."""

    type: Literal["PMCBoundary"] = "PMCBoundary"


class PMLParams(TaggedModel):
    """Polynomial profile parameters for the Phase 1 baseline PML."""

    type: Literal["PMLParams"] = "PMLParams"
    sigma_order: int = 3
    sigma_min: float = 0.0
    sigma_max: float = 1.5
    kappa_order: int = 3
    kappa_min: float = 1.0
    kappa_max: float = 3.0
    alpha_order: int = 1
    alpha_min: float = 0.0
    alpha_max: float = 0.0

    @field_validator("sigma_order", "kappa_order", "alpha_order")
    @classmethod
    def _validate_order(cls, value: int) -> int:
        if value < 0:
            raise ValueError("PML profile orders must be non-negative")
        return int(value)

    @field_validator(
        "sigma_min",
        "sigma_max",
        "kappa_min",
        "kappa_max",
        "alpha_min",
        "alpha_max",
    )
    @classmethod
    def _validate_non_negative(cls, value: float) -> float:
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("PML profile coefficients must be non-negative")
        return normalized

    @model_validator(mode="after")
    def _validate_bounds(self) -> PMLParams:
        if self.sigma_max < self.sigma_min:
            raise ValueError("PML sigma_max must be greater than or equal to sigma_min")
        if self.kappa_min < 1.0:
            raise ValueError("PML kappa_min must be at least 1.0")
        if self.kappa_max < self.kappa_min:
            raise ValueError("PML kappa_max must be greater than or equal to kappa_min")
        if self.alpha_max < self.alpha_min:
            raise ValueError("PML alpha_max must be greater than or equal to alpha_min")
        return self


DEFAULT_PML_PARAMS = PMLParams()


class PML(BoundaryEdge):
    """Complex-frequency-shift-inspired perfectly matched layer edge."""

    type: Literal["PML"] = "PML"
    num_layers: int = 12
    parameters: PMLParams = Field(default_factory=PMLParams)
    extrude_structures: bool = True

    @field_validator("num_layers")
    @classmethod
    def _validate_num_layers(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 1:
            raise ValueError("PML num_layers must be at least 1")
        if normalized < 6:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "PML boundaries with fewer than 6 layers are prone to reflection and should "
                "only be used for small smoke tests.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
        return normalized


ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML


def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
    """Normalize a supported Phase 1 non-absorbing boundary edge."""

    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
    boundary_type = str(value.get("type"))
    if boundary_type == "Periodic":
        return Periodic(**value)
    if boundary_type == "BlochBoundary":
        return BlochBoundary(**value)
    if boundary_type == "PECBoundary":
        return PECBoundary(**value)
    if boundary_type == "PMCBoundary":
        return PMCBoundary(**value)
    if boundary_type == "PML":
        return PML(**value)
    raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")


class Boundary(TaggedModel):
    """Boundary pair across one simulation axis."""

    type: Literal["Boundary"] = "Boundary"
    plus: object = Field(default_factory=Periodic)
    minus: object = Field(default_factory=Periodic)

    @field_validator("plus", "minus")
    @classmethod
    def _validate_edge(cls, value: object) -> object:
        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
                return boundary_edge_model_from_value(value)
        return value

    @model_validator(mode="after")
    def _validate_edge_composition(self) -> Boundary:
        plus = self.plus
        minus = self.minus

        num_bloch = isinstance(plus, BlochBoundary) + isinstance(minus, BlochBoundary)
        if num_bloch == 1:
            raise ValueError("Bloch boundaries must be applied on both sides of the same axis")
        if isinstance(plus, BlochBoundary) and isinstance(minus, BlochBoundary):
            if plus.bloch_vec != minus.bloch_vec:
                raise ValueError(
                    "Bloch boundaries on opposite sides of one axis must use the same bloch_vec"
                )
            return self

        changed = False

        if isinstance(minus, (PECBoundary, PMCBoundary)) and isinstance(plus, Periodic):
            plus = minus
            changed = True
        elif isinstance(plus, (PECBoundary, PMCBoundary)) and isinstance(minus, Periodic):
            minus = plus
            changed = True

        if changed:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
                "boundary so runtime halo handling stays reflection-only on that axis.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
            object.__setattr__(self, "plus", plus)
            object.__setattr__(self, "minus", minus)

        return self

    def __getitem__(self, field_name: str) -> object:
        if field_name == "plus":
            return self.plus
        if field_name == "minus":
            return self.minus
        if field_name == "type":
            return self.type
        raise KeyError(field_name)

    @classmethod
    def periodic(cls) -> Boundary:
        return cls(plus=Periodic(), minus=Periodic())

    @classmethod
    def bloch(cls, bloch_vec: float) -> Boundary:
        return cls(
            plus=BlochBoundary(bloch_vec=bloch_vec),
            minus=BlochBoundary(bloch_vec=bloch_vec),
        )

    @classmethod
    def pec(cls) -> Boundary:
        return cls(plus=PECBoundary(), minus=PECBoundary())

    @classmethod
    def pmc(cls) -> Boundary:
        return cls(plus=PMCBoundary(), minus=PMCBoundary())

    @classmethod
    def pml(
        cls,
        *,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> Boundary:
        pml = PML(

exec
/bin/bash -lc "sed -n '1,320p' src/autofdtd/compiler/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary compilation helpers for the foundational Phase 1 surface."""

from __future__ import annotations

import math
from enum import StrEnum
from typing import Literal

from autofdtd.boundaries import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    DEFAULT_PML_PARAMS,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)
from autofdtd.core.models import AutoFDTDModel


class BoundaryMode(StrEnum):
    """Runtime boundary mode consumed by staged halo handling."""

    PERIODIC = "periodic"
    BLOCH = "bloch"
    PEC = "pec"
    PMC = "pmc"
    PML = "pml"


class CompiledPMLCoefficients(AutoFDTDModel):
    """Prepared per-layer damping and memory coefficients for one PML face."""

    type: str = "CompiledPMLCoefficients"
    num_layers: int
    sigma: tuple[float, ...]
    kappa: tuple[float, ...]
    alpha: tuple[float, ...]
    attenuation: tuple[float, ...]
    memory_decay: tuple[float, ...]
    memory_drive: tuple[float, ...]
    stretch: tuple[float, ...]
    dt: float
    grid_spacing: float


class CompiledBoundaryEdge(AutoFDTDModel):
    """Runtime-ready metadata for one boundary face."""

    type: str = "CompiledBoundaryEdge"
    axis: Literal["x", "y", "z"]
    side: Literal["minus", "plus"]
    mode: BoundaryMode
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]
    source_side: Literal["minus", "plus"]
    phase_factor: complex = 1.0 + 0.0j
    pml: CompiledPMLCoefficients | None = None


class CompiledBoundaryAxis(AutoFDTDModel):
    """Runtime-ready metadata for one axis boundary pair."""

    type: str = "CompiledBoundaryAxis"
    axis: Literal["x", "y", "z"]
    minus: CompiledBoundaryEdge
    plus: CompiledBoundaryEdge
    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
    pml_faces: tuple[Literal["minus", "plus"], ...] = ()


class CompiledSymmetryAxis(AutoFDTDModel):
    """Reflection-sign metadata for one reduced symmetry axis."""

    type: str = "CompiledSymmetryAxis"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]


class CompiledBoundarySpec(AutoFDTDModel):
    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""

    type: str = "CompiledBoundarySpec"
    x: CompiledBoundaryAxis
    y: CompiledBoundaryAxis
    z: CompiledBoundaryAxis
    periodic_axes: tuple[str, ...] = ()
    bloch_axes: tuple[str, ...] = ()
    reflective_axes: tuple[str, ...] = ()
    pml_axes: tuple[str, ...] = ()
    pml_faces: tuple[str, ...] = ()
    requires_complex_fields: bool = False
    symmetry: tuple[int, int, int] = (0, 0, 0)
    active_symmetry_axes: tuple[str, ...] = ()
    symmetry_axes: tuple[CompiledSymmetryAxis, ...] = ()
    halo_depth: int = 1
    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
    pml_dt: float = 1.0
    pml_grid_spacing: tuple[float, float, float] = (1.0, 1.0, 1.0)

    def axes(self) -> tuple[CompiledBoundaryAxis, CompiledBoundaryAxis, CompiledBoundaryAxis]:
        return (self.x, self.y, self.z)


def _mode_for_edge(value: object) -> BoundaryMode:
    edge = boundary_edge_model_from_value(value)
    if isinstance(edge, Periodic):
        return BoundaryMode.PERIODIC
    if isinstance(edge, BlochBoundary):
        return BoundaryMode.BLOCH
    if isinstance(edge, PECBoundary):
        return BoundaryMode.PEC
    if isinstance(edge, PMCBoundary):
        return BoundaryMode.PMC
    if isinstance(edge, PML):
        return BoundaryMode.PML
    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")


def _reflection_signs(
    mode: BoundaryMode, axis: int
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    normal = [1, 1, 1]
    tangential = [-1, -1, -1]
    normal[axis] = -1
    tangential[axis] = 1
    if mode is BoundaryMode.PEC:
        electric = tuple(tangential)
        magnetic = tuple(normal)
        return electric, magnetic
    if mode is BoundaryMode.PMC:
        electric = tuple(normal)
        magnetic = tuple(tangential)
        return electric, magnetic
    return (1, 1, 1), (1, 1, 1)


def _phase_factor_for_edge(edge: object, *, side: Literal["minus", "plus"]) -> complex:
    normalized = boundary_edge_model_from_value(edge)
    if not isinstance(normalized, BlochBoundary):
        return 1.0 + 0.0j
    phase = normalized.bloch_phase
    return phase.conjugate() if side == "minus" else phase


def _symmetry_signs(
    symmetry: int,
    axis: int,
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    if symmetry not in (-1, 1):
        raise ValueError("symmetry metadata only exists for active axes")
    spatial = [1, 1, 1]
    pseudo = [-1, -1, -1]
    spatial[axis] = -1
    pseudo[axis] = 1
    electric = tuple(sign * symmetry for sign in spatial)
    magnetic = tuple(sign * symmetry for sign in pseudo)
    return electric, magnetic


def _resolve_pml_params(edge: PML) -> PMLParams:
    params = edge.parameters
    if isinstance(params, PMLParams):
        return params
    return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)


def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
    return minimum + (maximum - minimum) * (fraction**order)


def _compile_pml_coefficients(
    edge: PML,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledPMLCoefficients:
    if dt <= 0.0:
        raise ValueError("PML coefficient preparation requires dt > 0")
    if grid_spacing <= 0.0:
        raise ValueError("PML coefficient preparation requires a positive grid spacing")
    params = _resolve_pml_params(edge)
    sigma: list[float] = []
    kappa: list[float] = []
    alpha: list[float] = []
    attenuation: list[float] = []
    memory_decay: list[float] = []
    memory_drive: list[float] = []
    stretch: list[float] = []
    layer_count = edge.num_layers
    for layer_index in range(layer_count):
        fraction = float(layer_index + 1) / float(layer_count)
        sigma_value = _polynomial_profile(
            params.sigma_min, params.sigma_max, params.sigma_order, fraction
        )
        kappa_value = _polynomial_profile(
            params.kappa_min, params.kappa_max, params.kappa_order, fraction
        )
        alpha_value = _polynomial_profile(
            params.alpha_min, params.alpha_max, params.alpha_order, fraction
        )
        damping_rate = max(0.0, sigma_value / kappa_value + alpha_value)
        attenuation_value = math.exp(-damping_rate * dt / grid_spacing)
        memory_decay_value = math.exp(-max(0.0, alpha_value) * dt)
        drive_value = (1.0 - attenuation_value) / max(kappa_value, 1.0)
        stretch_value = 1.0 / kappa_value
        sigma.append(sigma_value)
        kappa.append(kappa_value)
        alpha.append(alpha_value)
        attenuation.append(attenuation_value)
        memory_decay.append(memory_decay_value)
        memory_drive.append(drive_value)
        stretch.append(stretch_value)
    return CompiledPMLCoefficients(
        num_layers=edge.num_layers,
        sigma=tuple(sigma),
        kappa=tuple(kappa),
        alpha=tuple(alpha),
        attenuation=tuple(attenuation),
        memory_decay=tuple(memory_decay),
        memory_drive=tuple(memory_drive),
        stretch=tuple(stretch),
        dt=dt,
        grid_spacing=grid_spacing,
    )


def _compile_edge(
    axis_name: Literal["x", "y", "z"],
    side: Literal["minus", "plus"],
    edge: object,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledBoundaryEdge:
    axis_index = "xyz".index(axis_name)
    mode = _mode_for_edge(edge)
    electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
    pml = None
    normalized = boundary_edge_model_from_value(edge)
    if isinstance(normalized, PML):
        pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
    return CompiledBoundaryEdge(
        axis=axis_name,
        side=side,
        mode=mode,
        electric_signs=electric_signs,
        magnetic_signs=magnetic_signs,
        source_side="plus" if side == "minus" else "minus",
        phase_factor=_phase_factor_for_edge(edge, side=side),
        pml=pml,
    )


def _compile_axis(
    axis_name: Literal["x", "y", "z"],
    boundary: Boundary,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledBoundaryAxis:
    minus = _compile_edge(axis_name, "minus", boundary.minus, dt=dt, grid_spacing=grid_spacing)
    plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
    edge_modes = {minus.mode, plus.mode}
    if edge_modes == {BoundaryMode.PERIODIC}:
        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
    elif edge_modes == {BoundaryMode.BLOCH}:
        exchange_kind = "bloch"
    elif edge_modes == {BoundaryMode.PML}:
        exchange_kind = "pml"
    elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
        exchange_kind = "reflective"
    else:
        exchange_kind = "mixed"
    pml_faces = tuple(
        side
        for side, compiled in (("minus", minus), ("plus", plus))
        if compiled.mode is BoundaryMode.PML
    )
    return CompiledBoundaryAxis(
        axis=axis_name,
        minus=minus,
        plus=plus,
        exchange_kind=exchange_kind,
        pml_faces=pml_faces,
    )


def _compile_symmetry_axes(
    symmetry: tuple[int, int, int],
) -> tuple[CompiledSymmetryAxis, ...]:
    compiled: list[CompiledSymmetryAxis] = []
    for axis_name, axis_value in zip("xyz", symmetry, strict=True):
        if axis_value == 0:
            continue
        electric_signs, magnetic_signs = _symmetry_signs(axis_value, "xyz".index(axis_name))
        compiled.append(
            CompiledSymmetryAxis(
                axis=axis_name,
                symmetry=axis_value,
                electric_signs=electric_signs,
                magnetic_signs=magnetic_signs,
            )
        )
    return tuple(compiled)


def compile_boundary_spec(
    value: object | None,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
    dt: float = 1.0,
    grid_spacing: float | tuple[float, float, float] = 1.0,

exec
/bin/bash -lc "sed -n '1,340p' src/autofdtd/runtime/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Runtime-facing boundary orchestration for the foundational Phase 1 surface."""

from __future__ import annotations

from typing import Literal

import numpy as np

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryEdge,
    CompiledBoundarySpec,
    CompiledSymmetryAxis,
    compile_boundary_spec,
)
from autofdtd.kernels.boundaries import (
    PMLBoundaryState,
    PMLFaceState,
    apply_boundary_stages,
    boundary_edge_transform,
    symmetry_transform,
)


def allocate_pml_boundary_state(
    compiled_boundary_spec: CompiledBoundarySpec,
    field_shape: tuple[int, int, int, int],
    *,
    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
) -> PMLBoundaryState:
    """Allocate persistent PML memory arrays for all active faces."""

    if len(field_shape) != 4 or field_shape[-1] != 3:
        raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
    electric: dict[str, PMLFaceState] = {}
    magnetic: dict[str, PMLFaceState] = {}

    for axis_boundary in compiled_boundary_spec.axes():
        axis_index = "xyz".index(axis_boundary.axis)
        for side in ("minus", "plus"):
            edge = getattr(axis_boundary, side)
            if edge.mode is not BoundaryMode.PML or edge.pml is None:
                continue
            memory_shape = list(field_shape)
            memory_shape[axis_index] = edge.pml.num_layers
            face_state = PMLFaceState(
                axis=axis_boundary.axis,
                side=side,
                memory_shape=tuple(int(value) for value in memory_shape),
                memory=np.zeros(memory_shape, dtype=dtype),
            )
            face_key = f"{axis_boundary.axis}.{side}"
            electric[face_key] = face_state
            magnetic[face_key] = PMLFaceState(
                axis=axis_boundary.axis,
                side=side,
                memory_shape=face_state.memory_shape,
                memory=np.zeros(memory_shape, dtype=dtype),
            )
    return PMLBoundaryState(electric=electric, magnetic=magnetic)


class BoundaryRuntime:
    """Small runtime wrapper around compiled Phase 1 boundary metadata."""

    def __init__(
        self,
        boundary_spec: object | None = None,
        *,
        symmetry: tuple[int, int, int] = (0, 0, 0),
        dt: float = 1.0,
        grid_spacing: float | tuple[float, float, float] = 1.0,
        field_shape: tuple[int, int, int, int] | None = None,
        field_dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
    ) -> None:
        self.compiled = compile_boundary_spec(
            boundary_spec,
            symmetry=symmetry,
            dt=dt,
            grid_spacing=grid_spacing,
        )
        self.pml_state = (
            allocate_pml_boundary_state(self.compiled, field_shape, dtype=field_dtype)
            if field_shape is not None and self.compiled.pml_faces
            else None
        )

    def allocate_state(
        self,
        field_shape: tuple[int, int, int, int],
        *,
        dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
    ) -> PMLBoundaryState | None:
        """Allocate or refresh PML state for a given field layout."""

        if not self.compiled.pml_faces:
            self.pml_state = None
            return None
        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
        return self.pml_state

    def apply(
        self,
        electric_field: np.ndarray,
        magnetic_field: np.ndarray,
    ) -> tuple[np.ndarray, np.ndarray]:
        """Apply ghost-cell and PML stage updates for both field families."""

        if self.compiled.pml_faces and self.pml_state is None:
            self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
        return apply_boundary_stages(
            electric_field,
            magnetic_field,
            self.compiled,
            self.pml_state,
        )

    def transform_boundary_values(
        self,
        values: np.ndarray,
        edge: CompiledBoundaryEdge,
        *,
        field_family: Literal["electric", "magnetic"],
    ) -> np.ndarray:
        """Transform halo values imported from a neighboring chunk or wrapped face."""

        return boundary_edge_transform(values, edge, field_family=field_family)

    def transform_symmetry_values(
        self,
        values: np.ndarray,
        symmetry_axis: CompiledSymmetryAxis,
        *,
        field_family: Literal["electric", "magnetic"],
    ) -> np.ndarray:
        """Transform vector values imported through a mirror-symmetry reduction."""

        return symmetry_transform(values, symmetry_axis, field_family=field_family)


def build_boundary_runtime(
    boundary_spec: object | None = None,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
    dt: float = 1.0,
    grid_spacing: float | tuple[float, float, float] = 1.0,
    field_shape: tuple[int, int, int, int] | None = None,
    field_dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
) -> BoundaryRuntime:
    """Build the default Phase 1 boundary runtime wrapper."""

    return BoundaryRuntime(
        boundary_spec,
        symmetry=symmetry,
        dt=dt,
        grid_spacing=grid_spacing,
        field_shape=field_shape,
        field_dtype=field_dtype,
    )


def apply_compiled_boundaries(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    compiled_boundary_spec: CompiledBoundarySpec,
    *,
    state: PMLBoundaryState | None = None,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply boundary ghosts and PML stages using a precompiled specification."""

    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)

exec
/bin/bash -lc "sed -n '260,420p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
exec
/bin/bash -lc "sed -n '320,520p' src/autofdtd/compiler/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
    grid_spacing: float | tuple[float, float, float] = 1.0,
) -> CompiledBoundarySpec:
    """Compile the supported Phase 1 boundary subset into runtime metadata."""

    if value is None:
        boundary_spec = BoundarySpec()
    else:
        try:
            boundary_spec = boundary_spec_model_from_value(value)
        except Exception as exc:  # noqa: BLE001
            raise ValueError(
                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
            ) from exc

    if isinstance(grid_spacing, tuple):
        if len(grid_spacing) != 3 or any(step <= 0.0 for step in grid_spacing):
            raise ValueError("grid_spacing must contain three positive axis spacings")
        axis_spacing = grid_spacing
    else:
        if grid_spacing <= 0.0:
            raise ValueError("grid_spacing must be positive")
        axis_spacing = (float(grid_spacing), float(grid_spacing), float(grid_spacing))

    compiled_axes = {
        axis_name: _compile_axis(
            axis_name,
            axis_boundary,
            dt=dt,
            grid_spacing=axis_spacing["xyz".index(axis_name)],
        )
        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
    }
    periodic_axes = tuple(
        axis.axis for axis in compiled_axes.values() if axis.exchange_kind == "periodic"
    )
    bloch_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.exchange_kind == "bloch")
    reflective_axes = tuple(
        axis.axis
        for axis in compiled_axes.values()
        if axis.exchange_kind in {"reflective", "mixed"}
    )
    pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
    pml_faces = tuple(
        f"{axis.axis}.{side}"
        for axis in compiled_axes.values()
        for side in axis.pml_faces
    )
    symmetry_axes = _compile_symmetry_axes(symmetry)
    stage_order = ["electric_boundary", "magnetic_boundary"]
    if pml_faces:
        stage_order.extend(("electric_pml", "magnetic_pml"))
    return CompiledBoundarySpec(
        x=compiled_axes["x"],
        y=compiled_axes["y"],
        z=compiled_axes["z"],
        periodic_axes=periodic_axes,
        bloch_axes=bloch_axes,
        reflective_axes=reflective_axes,
        pml_axes=pml_axes,
        pml_faces=pml_faces,
        requires_complex_fields=bool(bloch_axes),
        symmetry=symmetry,
        active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
        symmetry_axes=symmetry_axes,
        stage_order=tuple(stage_order),
        pml_dt=dt,
        pml_grid_spacing=axis_spacing,
    )


def normalize_axis_boundary(value: object) -> Boundary:
    """Normalize a single axis boundary using the supported subset."""

    return boundary_model_from_value(value)

 succeeded in 0ms:
        pml = PML(
            num_layers=num_layers,
            parameters=DEFAULT_PML_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=pml, minus=pml)


def boundary_model_from_value(value: object) -> Boundary:
    """Normalize a supported axis boundary pair."""

    if isinstance(value, Boundary):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
    if "plus" not in value and "minus" not in value:
        edge = boundary_edge_model_from_value(value)
        return Boundary(plus=edge, minus=edge)
    return Boundary(**value)


class BoundarySpec(TaggedModel):
    """Boundary conditions across the x, y, and z simulation axes."""

    type: Literal["BoundarySpec"] = "BoundarySpec"
    x: Boundary = Field(default_factory=Boundary.periodic)
    y: Boundary = Field(default_factory=Boundary.periodic)
    z: Boundary = Field(default_factory=Boundary.periodic)

    @field_validator("x", "y", "z", mode="before")
    @classmethod
    def _coerce_axis_boundary(cls, value: object) -> object:
        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {
                "Boundary",
                "Periodic",
                "BlochBoundary",
                "PECBoundary",
                "PMCBoundary",
                "PML",
            }:
                return boundary_model_from_value(value)
        return value

    def __getitem__(self, field_name: str) -> Boundary:
        if field_name == "x":
            return self.x
        if field_name == "y":
            return self.y
        if field_name == "z":
            return self.z
        raise KeyError(field_name)

    def as_tuple(self) -> tuple[Boundary, Boundary, Boundary]:
        return (self.x, self.y, self.z)

    @classmethod
    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
        return cls(
            x=Boundary(plus=boundary, minus=boundary),
            y=Boundary(plus=boundary, minus=boundary),
            z=Boundary(plus=boundary, minus=boundary),
        )

    @classmethod
    def pec(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pec() if x else Boundary.periodic(),
            y=Boundary.pec() if y else Boundary.periodic(),
            z=Boundary.pec() if z else Boundary.periodic(),
        )

    @classmethod
    def pmc(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pmc() if x else Boundary.periodic(),
            y=Boundary.pmc() if y else Boundary.periodic(),
            z=Boundary.pmc() if z else Boundary.periodic(),
        )

    @classmethod
    def periodic(
        cls, *, x: bool = True, y: bool = True, z: bool = True
    ) -> BoundarySpec:
        return cls(
            x=Boundary.periodic() if x else Boundary.periodic(),
            y=Boundary.periodic() if y else Boundary.periodic(),
            z=Boundary.periodic() if z else Boundary.periodic(),
        )

    @classmethod
    def pml(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if y
                else Boundary.periodic()
            ),
            z=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if z
                else Boundary.periodic()
            ),
        )


def boundary_spec_model_from_value(value: object) -> BoundarySpec:
    """Normalize a supported BoundarySpec container."""

    if isinstance(value, BoundarySpec):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
    return BoundarySpec(**value)

exec
/bin/bash -lc "sed -n '1,380p' src/autofdtd/kernels/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary halo and PML update helpers for the foundational Phase 1 surface."""

from __future__ import annotations

from typing import Any, Literal

import numpy as np

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryAxis,
    CompiledBoundaryEdge,
    CompiledBoundarySpec,
    CompiledPMLCoefficients,
    CompiledSymmetryAxis,
)
from autofdtd.core.models import AutoFDTDModel

try:
    import warp as wp
except ModuleNotFoundError:  # pragma: no cover - optional dependency
    wp = None

WARP_AVAILABLE = wp is not None


class PMLFaceState(AutoFDTDModel):
    """Persistent per-face memory for the staged PML update."""

    type: str = "PMLFaceState"
    axis: Literal["x", "y", "z"]
    side: Literal["minus", "plus"]
    memory_shape: tuple[int, ...]
    memory: np.ndarray

    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}


class PMLBoundaryState(AutoFDTDModel):
    """Persistent state bundle for all active PML faces."""

    type: str = "PMLBoundaryState"
    electric: dict[str, PMLFaceState]
    magnetic: dict[str, PMLFaceState]

    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}


def _ghost_and_source_slices(
    axis: int, side: str, *, ndim: int
) -> tuple[tuple[object, ...], tuple[object, ...]]:
    ghost = [slice(None)] * ndim
    source = [slice(None)] * ndim
    if side == "minus":
        ghost[axis] = 0
        source[axis] = -2
    else:
        ghost[axis] = -1
        source[axis] = 1
    return tuple(ghost), tuple(source)


def _interior_reflection_slices(
    axis: int, side: str, *, ndim: int
) -> tuple[tuple[object, ...], tuple[object, ...]]:
    ghost = [slice(None)] * ndim
    source = [slice(None)] * ndim
    if side == "minus":
        ghost[axis] = 0
        source[axis] = 1
    else:
        ghost[axis] = -1
        source[axis] = -2
    return tuple(ghost), tuple(source)


def _apply_edge(
    field: np.ndarray,
    axis_index: int,
    side: str,
    mode: BoundaryMode,
    signs: tuple[int, int, int],
    phase_factor: complex,
) -> None:
    if field.ndim != 4 or field.shape[-1] != 3:
        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
    if min(field.shape[:3]) < 3:
        raise ValueError(
            "boundary halo updates require one ghost cell and one interior cell per side"
        )
    if mode is BoundaryMode.PERIODIC:
        ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
        field[ghost] = field[source]
        return
    if mode is BoundaryMode.BLOCH:
        ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
        field[ghost] = field[source] * field.dtype.type(phase_factor)
        return
    if mode is BoundaryMode.PML:
        return
    ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
    field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)


def _pml_region_slices(
    axis_index: int,
    side: Literal["minus", "plus"],
    num_layers: int,
    *,
    ndim: int,
) -> tuple[object, ...]:
    region = [slice(None)] * ndim
    if side == "minus":
        region[axis_index] = slice(1, 1 + num_layers)
    else:
        region[axis_index] = slice(-1 - num_layers, -1)
    return tuple(region)


def _validate_pml_capacity(field: np.ndarray, axis_index: int, num_layers: int) -> None:
    interior = field.shape[axis_index] - 2
    if num_layers > interior:
        raise ValueError(
            f"PML with {num_layers} layers does not fit along axis index {axis_index}; "
            f"only {interior} interior cells are available"
        )


def _apply_pml_region(
    field: np.ndarray,
    face_state: PMLFaceState,
    coefficients: CompiledPMLCoefficients,
    *,
    axis_index: int,
    side: Literal["minus", "plus"],
) -> None:
    region = _pml_region_slices(axis_index, side, coefficients.num_layers, ndim=field.ndim)
    region_values = field[region]
    line_shape = [1, 1, 1, 1]
    line_shape[axis_index] = coefficients.num_layers
    attenuation = np.asarray(coefficients.attenuation, dtype=field.real.dtype).reshape(line_shape)
    memory_decay = np.asarray(coefficients.memory_decay, dtype=field.real.dtype).reshape(line_shape)
    memory_drive = np.asarray(coefficients.memory_drive, dtype=field.real.dtype).reshape(line_shape)
    stretch = np.asarray(coefficients.stretch, dtype=field.real.dtype).reshape(line_shape)
    face_state.memory[...] = face_state.memory * memory_decay + region_values * memory_drive
    field[region] = region_values * attenuation * stretch


def boundary_edge_transform(
    values: np.ndarray,
    edge: CompiledBoundaryEdge,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply the runtime transform associated with one boundary edge."""

    if edge.mode is BoundaryMode.PERIODIC:
        return np.array(values, copy=True)
    if edge.mode is BoundaryMode.BLOCH:
        return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
    if edge.mode is BoundaryMode.PML:
        return np.array(values, copy=True)
    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)


def symmetry_transform(
    values: np.ndarray,
    symmetry_axis: CompiledSymmetryAxis,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply mirror-symmetry signs to vector values imported from a reflected chunk."""

    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    return np.array(values, copy=True) * np.asarray(getattr(symmetry_axis, signs_name), dtype=values.dtype)


def apply_axis_boundary_ghosts(
    field: np.ndarray,
    axis_boundary: CompiledBoundaryAxis,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply one axis boundary pair to a vector field with one ghost cell per face."""

    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    updated = np.array(field, copy=True)
    axis_index = "xyz".index(axis_boundary.axis)
    _apply_edge(
        updated,
        axis_index,
        "minus",
        axis_boundary.minus.mode,
        getattr(axis_boundary.minus, signs_name),
        axis_boundary.minus.phase_factor,
    )
    _apply_edge(
        updated,
        axis_index,
        "plus",
        axis_boundary.plus.mode,
        getattr(axis_boundary.plus, signs_name),
        axis_boundary.plus.phase_factor,
    )
    return updated


def apply_boundary_ghosts(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""

    electric = np.array(electric_field, copy=True)
    magnetic = np.array(magnetic_field, copy=True)
    for axis_boundary in boundary_spec.axes():
        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
    return electric, magnetic


def apply_pml_layers(
    field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
    state: PMLBoundaryState,
    *,
    field_family: Literal["electric", "magnetic"],
) -> np.ndarray:
    """Apply the staged PML damping update to one vector field family."""

    updated = np.array(field, copy=True)
    face_states = state.electric if field_family == "electric" else state.magnetic
    for axis_boundary in boundary_spec.axes():
        axis_index = "xyz".index(axis_boundary.axis)
        for side in ("minus", "plus"):
            edge = getattr(axis_boundary, side)
            if edge.mode is not BoundaryMode.PML or edge.pml is None:
                continue
            face_key = f"{axis_boundary.axis}.{side}"
            face_state = face_states[face_key]
            _validate_pml_capacity(updated, axis_index, edge.pml.num_layers)
            _apply_pml_region(
                updated,
                face_state,
                edge.pml,
                axis_index=axis_index,
                side=side,
            )
    return updated


def apply_boundary_stages(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
    state: PMLBoundaryState | None = None,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply all compiled boundary stages in declared order."""

    electric = np.array(electric_field, copy=True)
    magnetic = np.array(magnetic_field, copy=True)
    for stage in boundary_spec.stage_order:
        if stage == "electric_boundary":
            electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
        elif stage == "magnetic_boundary":
            magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
        elif stage == "electric_pml":
            if state is None:
                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
            electric = apply_pml_layers(electric, boundary_spec, state, field_family="electric")
        elif stage == "magnetic_pml":
            if state is None:
                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
            magnetic = apply_pml_layers(magnetic, boundary_spec, state, field_family="magnetic")
    return electric, magnetic


def boundary_kernel_metadata() -> dict[str, Any]:
    """Expose the current boundary backend and staged update choices."""

    return {
        "backend": "warp" if WARP_AVAILABLE else "numpy",
        "warp_available": WARP_AVAILABLE,
        "module_contents_stable": True,
        "stages": (
            "electric_boundary",
            "magnetic_boundary",
            "electric_pml",
            "magnetic_pml",
        ),
        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
        "supports_complex_fields": True,
        "supports_symmetry_transforms": True,
        "supports_pml_layers": True,
        "halo_depth": 1,
    }

exec
/bin/bash -lc "sed -n '1,320p' src/autofdtd/ir/models.py | sed -n '1,320p'" in /workspace/autofdtd
 succeeded in 0ms:
"""Versioned execution IR models and packaging helpers."""

from __future__ import annotations

import json
from collections.abc import Mapping
from hashlib import sha256
from pathlib import Path
from typing import Any, Literal

from pydantic import BaseModel, Field, field_validator

from autofdtd.boundaries import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PML,
    PMLParams,
    PECBoundary,
    PMCBoundary,
    Periodic,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)
from autofdtd.core.containers import Scene, Simulation, Structure, StructurePriorityMode
from autofdtd.core.models import TaggedModel, json_ready
from autofdtd.geometry import (
    Box,
    ClipOperation,
    Cylinder,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    PolySlab,
    Sphere,
    Transformed,
    geometry_model_from_value,
)
from autofdtd.grid import (
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    PolarizedAveraging,
    ResolvedGrid,
    ResolvedGridAxis,
    Staircasing,
    UniformGrid,
    grid_model_from_value,
    subpixel_model_from_value,
)
from autofdtd.materials import (
    AnisotropicMedium,
    Debye,
    Drude,
    Lorentz,
    Medium,
    PECMedium,
    PMCMedium,
    PoleResidue,
    Sellmeier,
    medium_model_from_value,
)
from autofdtd.version import __version__

IR_SCHEMA_VERSION = "phase1.v1"
EXECUTION_PACKAGE_FORMAT = "autofdtd.execution-package.v1"


class IRModel(TaggedModel):
    """Frozen tagged base for inspectable execution-IR payloads."""

    schema_version: str = Field(default=IR_SCHEMA_VERSION)

    @field_validator("schema_version")
    @classmethod
    def _validate_schema_version(cls, value: str) -> str:
        if value != IR_SCHEMA_VERSION:
            raise ValueError(f"schema_version must be {IR_SCHEMA_VERSION!r}")
        return value


class ComponentFamily(str):
    """Execution-IR family namespace for tagged components."""

    GEOMETRY = "geometry"
    MEDIUM = "medium"
    SOURCE = "source"
    MONITOR = "monitor"
    BOUNDARY = "boundary"
    GRID = "grid"
    SUBPIXEL = "subpixel"


class ComponentIR(IRModel):
    """Generic tagged envelope for feature-family payloads."""

    type: Literal["ComponentIR"] = "ComponentIR"
    family: str
    component_type: str
    name: str | None = None
    payload: dict[str, Any] = Field(default_factory=dict)

    @field_validator("family", "component_type")
    @classmethod
    def _validate_non_empty(cls, value: str) -> str:
        stripped = value.strip()
        if not stripped:
            raise ValueError("component family and type must be non-empty")
        return stripped

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        if value is None:
            return None
        stripped = value.strip()
        if not stripped:
            raise ValueError("component names must not be empty or whitespace-only")
        return stripped


class GeometryTransformIR(IRModel):
    """Scene-compilation transform metadata for primitive geometries."""

    type: Literal["GeometryTransformIR"] = "GeometryTransformIR"
    origin: tuple[float, float, float]
    axes: tuple[tuple[float, float, float], tuple[float, float, float], tuple[float, float, float]]


class BoxIR(IRModel):
    """Typed execution IR for an axis-aligned box primitive."""

    type: Literal["BoxIR"] = "BoxIR"
    component_type: Literal["Box"] = "Box"
    center: tuple[float, float, float]
    size: tuple[float, float, float]
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]
    transform: GeometryTransformIR


class SphereIR(IRModel):
    """Typed execution IR for a sphere primitive."""

    type: Literal["SphereIR"] = "SphereIR"
    component_type: Literal["Sphere"] = "Sphere"
    center: tuple[float, float, float]
    radius: float
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]
    transform: GeometryTransformIR


class CylinderIR(IRModel):
    """Typed execution IR for an axis-aligned cylinder primitive."""

    type: Literal["CylinderIR"] = "CylinderIR"
    component_type: Literal["Cylinder"] = "Cylinder"
    center: tuple[float, float, float]
    radius: float
    length: float
    axis: int
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]
    transform: GeometryTransformIR


class PolySlabIR(IRModel):
    """Typed execution IR for a constant-cross-section polygon extrusion."""

    type: Literal["PolySlabIR"] = "PolySlabIR"
    component_type: Literal["PolySlab"] = "PolySlab"
    vertices: tuple[tuple[float, float], ...]
    slab_bounds: tuple[float, float]
    axis: int
    sidewall_angle: float
    dilation: float
    reference_plane: str
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]
    transform: GeometryTransformIR


class GeometryGroupIR(IRModel):
    """Typed execution IR for a grouped geometry collection."""

    type: Literal["GeometryGroupIR"] = "GeometryGroupIR"
    component_type: Literal["GeometryGroup"] = "GeometryGroup"
    geometries: tuple[GeometryIR, ...]
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]


class TransformedIR(IRModel):
    """Typed execution IR for a transformed geometry wrapper."""

    type: Literal["TransformedIR"] = "TransformedIR"
    component_type: Literal["Transformed"] = "Transformed"
    geometry: GeometryIR
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]
    transform: GeometryTransformIR


class ClipOperationIR(IRModel):
    """Typed execution IR for Phase 1 clip-operation wrappers."""

    type: Literal["ClipOperationIR"] = "ClipOperationIR"
    component_type: Literal["ClipOperation"] = "ClipOperation"
    operation: Literal["union", "intersection", "difference"]
    geometry_a: GeometryIR
    geometry_b: GeometryIR
    bounds_min: tuple[float, float, float]
    bounds_max: tuple[float, float, float]


GeometryIR = (
    BoxIR
    | SphereIR
    | CylinderIR
    | PolySlabIR
    | GeometryGroupIR
    | TransformedIR
    | ClipOperationIR
    | ComponentIR
)


class ResolvedGridAxisIR(IRModel):
    """Resolved axis-grid metadata included in the execution IR."""

    type: Literal["ResolvedGridAxisIR"] = "ResolvedGridAxisIR"
    axis: Literal["x", "y", "z"]
    boundaries: tuple[float, ...]
    cell_sizes: tuple[float, ...]
    num_cells: int


class ResolvedGridIR(IRModel):
    """Resolved 3D grid metadata included in the execution IR."""

    type: Literal["ResolvedGridIR"] = "ResolvedGridIR"
    center: tuple[float, float, float]
    size: tuple[float, float, float]
    x: ResolvedGridAxisIR
    y: ResolvedGridAxisIR
    z: ResolvedGridAxisIR
    shape: tuple[int, int, int]
    total_cells: int
    min_step: float
    max_step: float


class UniformGridIR(IRModel):
    """Typed execution IR for a uniform axis-grid specification."""

    type: Literal["UniformGridIR"] = "UniformGridIR"
    component_type: Literal["UniformGrid"] = "UniformGrid"
    dl: float


class CustomGridBoundariesIR(IRModel):
    """Typed execution IR for explicit axis-grid boundaries."""

    type: Literal["CustomGridBoundariesIR"] = "CustomGridBoundariesIR"
    component_type: Literal["CustomGridBoundaries"] = "CustomGridBoundaries"
    coords: tuple[float, ...]


class CustomGridIR(IRModel):
    """Typed execution IR for explicit axis-grid cell sizes."""

    type: Literal["CustomGridIR"] = "CustomGridIR"
    component_type: Literal["CustomGrid"] = "CustomGrid"
    dl: tuple[float, ...]
    custom_offset: float | None = None


class AutoGridIR(IRModel):
    """Typed execution IR for a simplified AutoGrid axis specification."""

    type: Literal["AutoGridIR"] = "AutoGridIR"
    component_type: Literal["AutoGrid"] = "AutoGrid"
    min_steps_per_wvl: float
    min_steps_per_sim_size: float
    max_scale: float
    dl_min: float | None = None


class GridRefinementIR(IRModel):
    """Typed execution IR for local AutoGrid refinement metadata."""

    type: Literal["GridRefinementIR"] = "GridRefinementIR"
    component_type: Literal["GridRefinement"] = "GridRefinement"
    refinement_factor: float | None = None
    dl: float | None = None
    num_cells: int


class LayerRefinementSpecIR(IRModel):
    """Typed execution IR for the Phase 1 layer-refinement subset."""

    type: Literal["LayerRefinementSpecIR"] = "LayerRefinementSpecIR"
    component_type: Literal["LayerRefinementSpec"] = "LayerRefinementSpec"
    axis: int
    center: tuple[float, float, float]
    size: tuple[float, float, float]
    min_steps_along_axis: float | None = None
    bounds_refinement: GridRefinementIR | None = None
    bounds_snapping: Literal["bounds", "lower", "upper", "center"] | None = None


GridAxisIR = UniformGridIR | CustomGridBoundariesIR | CustomGridIR | AutoGridIR | ComponentIR


class GridSpecIR(IRModel):

exec
/bin/bash -lc "sed -n '1,340p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
from __future__ import annotations

import numpy as np
import pytest

from autofdtd.api import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    Simulation,
)
from autofdtd.compiler import BoundaryMode, compile_boundary_spec
from autofdtd.core.validation import AutoFDTDValidationWarning
from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
from autofdtd.kernels import (
    apply_boundary_ghosts,
    apply_boundary_stages,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime


def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
    with pytest.warns(
        AutoFDTDValidationWarning,
        match="periodic boundary opposite a PEC/PMC boundary",
    ):
        boundary = Boundary(minus=PECBoundary(), plus=Periodic())

    assert boundary.minus.type == "PECBoundary"
    assert boundary.plus.type == "PECBoundary"


def test_simulation_normalizes_supported_boundary_subset() -> None:
    simulation = Simulation(
        size=(1.0, 1.0, 1.0),
        run_time=1.0,
        boundary_spec={
            "type": "BoundarySpec",
            "x": {"type": "Periodic"},
            "y": {
                "type": "Boundary",
                "minus": {"type": "PECBoundary"},
                "plus": {"type": "Periodic"},
            },
            "z": {"type": "PMCBoundary"},
        },
    )

    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"


def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
        symmetry=(1, 0, -1),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.25),
    )

    assert compiled.x.exchange_kind == "bloch"
    assert compiled.x.minus.mode is BoundaryMode.BLOCH
    assert compiled.x.plus.phase_factor == pytest.approx(1j)
    assert compiled.x.minus.phase_factor == pytest.approx(-1j)
    assert compiled.y.minus.mode is BoundaryMode.PEC
    assert compiled.z.plus.mode is BoundaryMode.PML
    assert compiled.bloch_axes == ("x",)
    assert compiled.reflective_axes == ("y",)
    assert compiled.pml_axes == ("z",)
    assert compiled.pml_faces == ("z.minus", "z.plus")
    assert compiled.z.plus.pml is not None
    assert compiled.z.plus.pml.num_layers == 4
    assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
    assert compiled.stage_order == (
        "electric_boundary",
        "magnetic_boundary",
        "electric_pml",
        "magnetic_pml",
    )
    assert compiled.requires_complex_fields is True
    assert compiled.active_symmetry_axes == ("x", "z")
    assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
    assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)

    with pytest.raises(
        ValueError,
        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
    ):
        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})


def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    )
    electric = np.zeros((4, 4, 4, 3), dtype=np.complex128)
    magnetic = np.zeros((4, 4, 4, 3), dtype=np.complex128)

    electric[1, 1, 1] = (1.0, 2.0, 3.0)
    electric[-2, 1, 1] = (4.0, 5.0, 6.0)
    electric[2, 1, 2] = (7.0, 8.0, 9.0)
    electric[1, -2, 2] = (11.0, 12.0, 13.0)
    electric[2, 2, 1] = (14.0, 15.0, 16.0)
    electric[1, 2, -2] = (17.0, 18.0, 19.0)

    magnetic[1, 1, 1] = (21.0, 22.0, 23.0)
    magnetic[-2, 1, 1] = (24.0, 25.0, 26.0)
    magnetic[2, 1, 2] = (27.0, 28.0, 29.0)
    magnetic[2, -2, 2] = (31.0, 32.0, 33.0)
    magnetic[2, 2, 1] = (34.0, 35.0, 36.0)
    magnetic[2, 2, -2] = (37.0, 38.0, 39.0)

    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)

    assert tuple(updated_electric[0, 1, 1]) == pytest.approx((-4.0j, -5.0j, -6.0j))
    assert tuple(updated_electric[-1, 1, 1]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(updated_magnetic[0, 1, 1]) == pytest.approx((-24.0j, -25.0j, -26.0j))
    assert tuple(updated_electric[2, 0, 2]) == pytest.approx((-7.0, 8.0, -9.0))
    assert tuple(updated_magnetic[2, 0, 2]) == pytest.approx((27.0, -28.0, 29.0))
    assert tuple(updated_electric[2, 2, 0]) == pytest.approx((14.0, 15.0, -16.0))
    assert tuple(updated_magnetic[2, 2, -1]) == pytest.approx((-37.0, -38.0, 39.0))


def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
    boundary_spec = BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    runtime = build_boundary_runtime(boundary_spec, symmetry=(1, 0, -1))
    metadata = boundary_kernel_metadata()
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=boundary_spec,
            symmetry=(1, 0, -1),
        )
    )

    assert runtime.compiled.bloch_axes == ("x",)
    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
    assert simulation_ir.boundary_spec.requires_complex_fields is True
    assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")


def test_pml_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.pml(
                    num_layers=8,
                    parameters=PMLParams(
                        sigma_order=2,
                        sigma_max=2.0,
                        kappa_max=4.0,
                        alpha_max=0.1,
                    ),
                )
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
    assert simulation_ir.boundary_spec.x.minus.num_layers == 8
    assert simulation_ir.boundary_spec.x.minus.parameters.kappa_max == pytest.approx(4.0)
    assert simulation_ir.boundary_spec.pml_axes == ("x",)
    assert simulation_ir.boundary_spec.pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.reflective_axes == ("x",)


def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
    runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
    vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)

    boundary_values = runtime.transform_boundary_values(
        vector,
        runtime.compiled.x.plus,
        field_family="electric",
    )
    mirrored = runtime.transform_symmetry_values(
        vector,
        runtime.compiled.symmetry_axes[0],
        field_family="magnetic",
    )

    assert tuple(boundary_values[0]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(mirrored[0]) == pytest.approx((1.0, -2.0, -3.0))
    assert tuple(
        boundary_edge_transform(vector, runtime.compiled.x.minus, field_family="electric")[0]
    ) == pytest.approx((-1.0j, -2.0j, -3.0j))
    assert tuple(
        symmetry_transform(
            vector,
            runtime.compiled.symmetry_axes[0],
            field_family="electric",
        )[0]
    ) == pytest.approx((-1.0, 2.0, 3.0))


def test_pml_state_allocation_tracks_active_faces() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.pml(num_layers=3),
            y=Boundary.periodic(),
            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
        )
    )
    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)

    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
    assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
    assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
    assert np.all(state.magnetic["x.plus"].memory == 0.0)


def test_pml_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 4, 4, 3), dtype=np.float64)
    electric[1:5, 1:3, 1:3, 0] = 1.0
    magnetic[-5:-1, 1:3, 1:3, 1] = 1.0

    before_left = float(np.sum(electric[1:5, ...] ** 2))
    before_center = float(np.sum(electric[5:7, ...] ** 2))
    for _ in range(12):
        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
    after_left = float(np.sum(electric[1:5, ...] ** 2))
    after_right_h = float(np.sum(magnetic[-5:-1, ...] ** 2))

    assert after_left < before_left * 0.2
    assert after_right_h < 0.2 * 16.0
    assert before_center == pytest.approx(0.0)
    assert float(np.sum(electric[5:7, ...] ** 2)) == pytest.approx(0.0)
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0


def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
    boundary = Boundary.bloch(0.125)
    assert isinstance(boundary.plus, BlochBoundary)
    assert boundary.plus.bloch_phase == pytest.approx(
        np.exp(1j * 2.0 * np.pi * 0.125),
    )

    with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
        Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())

exec
/bin/bash -lc "sed -n '1,260p' ../tidy3d/tidy3d/components/boundary.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Defines electromagnetic boundary conditions"""

from __future__ import annotations

from abc import ABC
from typing import TYPE_CHECKING, Any, Optional, Union

import numpy as np
from pydantic import (
    Field,
    NonNegativeFloat,
    NonNegativeInt,
    PositiveFloat,
    field_validator,
    model_validator,
)

from tidy3d.components.validators import _assert_min_freq, assert_plane
from tidy3d.components.viz import ARROW_ALPHA, ARROW_COLOR_ABSORBER, plot_params_absorber
from tidy3d.constants import C_0, CONDUCTIVITY, EPSILON_0, HERTZ, MU_0, PML_SIGMA
from tidy3d.exceptions import DataError, SetupError, ValidationError
from tidy3d.log import log

from .base import Tidy3dBaseModel, cached_property
from .geometry.base import Box
from .medium import Medium
from .mode_spec import ModeSpec
from .source.field import TFSF, GaussianBeam, ModeSource, PlaneWave
from .types import TYPE_TAG_STR, Direction, FreqBound
from .types.mode_spec import ModeSpecType

if TYPE_CHECKING:
    from typing import Callable

    from numpy.typing import NDArray

    from tidy3d.compat import Self
    from tidy3d.components.viz import PlotParams

    from .monitor import ModeMonitor, ModeSolverMonitor
    from .types import Ax, Axis, Complex

MIN_NUM_PML_LAYERS = 6
MIN_NUM_STABLE_PML_LAYERS = 6
MIN_NUM_ABSORBER_LAYERS = 6

# Margin (in grid cells) defining the extrusion clipping region. The clipping bounding box is inset
# by this margin on both the lower and upper bounds of each axis.
CLIPPING_MARGIN = 2


def warn_num_layers_factory(
    min_num_layers: int, descr: str
) -> Callable[[type[AbsorberSpec], int], int]:
    """Several similar classes defined have a ``num_layers`` data member, and they generate
    similar warning messages when ``num_layers`` is too small.  This function creates a pydantic
    validator which can be shared with all of these classes to create these warning messages."""

    @field_validator("num_layers")
    @classmethod
    def _warn_num_layers(cls: type[AbsorberSpec], val: int) -> int:
        if val < min_num_layers:
            cls_name = cls.__name__
            log.warning(
                f"A {descr} ({cls_name}) boundary was created with {val} layers. "
                f"{cls_name}s with less than {min_num_layers} layers are highly subject to "
                "unwanted numerical artifacts. We strongly recommend "
                "increasing the number of layers. For more details refer to: "
                f"'https://docs.flexcompute.com/projects/tidy3d/en/latest/api/_autosummary/tidy3d.{cls_name}.html'.",
                log_once=True,
            )
        return val

    return _warn_num_layers


DEFAULT_MODE_SPEC_MODE_ABC = ModeSpec()
DEFAULT_BROADBAND_MODE_ABC_FITTER_TOLERANCE = 1e-6
DEFAULT_BROADBAND_MODE_ABC_NUM_FREQS = 15
DEFAULT_BROADBAND_MODE_ABC_NUM_POLES = 5
MAX_BROADBAND_MODE_ABC_NUM_POLES = 10
MAX_BROADBAND_MODE_ABC_NUM_FREQS = 101


class BoundaryEdge(ABC, Tidy3dBaseModel):
    """Electromagnetic boundary condition at a domain edge."""

    name: Optional[str] = Field(
        None,
        title="Name",
        description="Optional unique name for boundary.",
    )


# PBC keyword
class Periodic(BoundaryEdge):
    """Periodic boundary condition class."""

    @property
    def bloch_vec(self) -> int:
        """Periodic boundaries are effectively Bloch boundaries with ``bloch_vec == 0``.
        In practice, periodic boundaries do not force the use of complex fields, while Bloch
        boundaries do, even with ``bloch_vec == 0``. Thus, it is more efficient to use periodic.
        """
        return 0


# PEC keyword
class PECBoundary(BoundaryEdge):
    """Perfect electric conductor boundary condition class."""


# PMC keyword
class PMCBoundary(BoundaryEdge):
    """Perfect magnetic conductor boundary condition class."""


class AbstractABCBoundary(BoundaryEdge, ABC):
    """One-way wave equation absorbing boundary conditions abstract base class."""


class ABCBoundary(AbstractABCBoundary):
    """One-way wave equation absorbing boundary conditions.
    See, for example, John B. Schneider, Understanding the Finite-Difference Time-Domain Method, Chapter 6.
    """

    permittivity: Optional[float] = Field(
        None,
        title="Effective Permittivity",
        description="Effective permittivity for determining propagation constant. "
        "If ``None``, this value will be automatically inferred from the medium at "
        "the domain boundary and the central frequency of the source.",
        ge=1.0,
    )

    conductivity: Optional[NonNegativeFloat] = Field(
        None,
        title="Effective Conductivity",
        description="Effective conductivity for determining propagation constant. "
        "If ``None``, this value will be automatically inferred from the medium at "
        "the domain boundary and the central frequency of the source.",
        json_schema_extra={"units": CONDUCTIVITY},
    )

    @model_validator(mode="after")
    def _conductivity_only_with_float_permittivity(self) -> Self:
        """Validate that conductivity can be provided only with float permittivity."""
        if self.conductivity is not None and self.permittivity is None:
            raise ValidationError(
                "Field 'conductivity' in 'ABCBoundary' can only be provided "
                "simultaneously with 'permittivity'."
            )
        return self


class BroadbandModeABCFitterParam(Tidy3dBaseModel):
    """Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.

    Notes
    -----
    The number of poles and frequency sampling points are constrained to be within the range [1, 10] and [1, 101] respectively.

    Example
    -------
    >>> fitter_param = BroadbandModeABCFitterParam(max_num_poles=5, tolerance_rms=1e-4, frequency_sampling_points=10)
    """

    max_num_poles: int = Field(
        DEFAULT_BROADBAND_MODE_ABC_NUM_POLES,
        title="Maximal Number Of Poles",
        description="Maximal number of poles in complex-conjugate pole residue model for "
        "fitting the mode propagation index.",
        gt=0,
        le=MAX_BROADBAND_MODE_ABC_NUM_POLES,
    )

    tolerance_rms: NonNegativeFloat = Field(
        DEFAULT_BROADBAND_MODE_ABC_FITTER_TOLERANCE,
        title="Fitting Tolerance",
        description="Tolerance in fitting the mode propagation index.",
    )

    frequency_sampling_points: int = Field(
        DEFAULT_BROADBAND_MODE_ABC_NUM_FREQS,
        title="Number Of Frequencies",
        description="Number of sampling frequencies used in fitting the mode propagation index.",
        gt=0,
        le=MAX_BROADBAND_MODE_ABC_NUM_FREQS,
    )


DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS = BroadbandModeABCFitterParam()


class BroadbandModeABCSpec(Tidy3dBaseModel):
    """Specifies the broadband mode absorption boundary conditions. The mode propagation index is approximated by a sum of pole-residue pairs.

    Example
    -------
    >>> broadband_mode_abc_spec = BroadbandModeABCSpec(frequency_range=(100e12, 120e12), fit_param=BroadbandModeABCFitterParam())
    """

    frequency_range: FreqBound = Field(
        title="Frequency Range",
        description="Frequency range for the broadband mode absorption boundary conditions.",
        json_schema_extra={"units": (HERTZ, HERTZ)},
    )

    fit_param: BroadbandModeABCFitterParam = Field(
        DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS,
        title="Fitting Parameters For Broadband Mode Absorption Boundary Conditions",
        description="Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.",
    )

    @field_validator("frequency_range", mode="after")
    @classmethod
    def validate_frequency_range(cls, val: FreqBound) -> FreqBound:
        """Validate that max frequency is greater than min frequency."""
        _assert_min_freq(val[0], "min frequency")
        if val[1] <= val[0]:
            raise ValidationError("max frequency must be greater than min frequency.")
        return val

    @classmethod
    def from_wavelength_range(
        cls,
        wavelength_range: FreqBound,
        fit_param: BroadbandModeABCFitterParam = DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS,
    ) -> BroadbandModeABCSpec:
        """Instantiate from a wavelength range.

        Parameters
        ----------
        wavelength_range : FreqBound
            Wavelength range for the broadband mode absorption boundary conditions.
        fit_param : BroadbandModeABCFitterParam = DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS
            Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.

        Returns
        -------
        :class:`BroadbandModeABCSpec`
            Broadband mode absorption boundary conditions.
        """
        # check that min wavelength > 0
        if wavelength_range[0] <= 0:
            raise SetupError("min wavelength must be greater than 0.")
        # check that max wavelength > min wavelength
        if wavelength_range[1] <= wavelength_range[0]:
            raise SetupError("max wavelength must be greater than min wavelength.")

        return cls(
            frequency_range=(C_0 / wavelength_range[1], C_0 / wavelength_range[0]),
            fit_param=fit_param,
        )

    @property
    def _frequency_grid(self) -> NDArray:
        """Frequency grid for the broadband mode absorption boundary conditions.
        Propagation constant is sampled at these frequencies and fitted using pole-residue pair model.
        """

exec
/bin/bash -lc 'rg -n "class StablePML|class Absorber|StablePML|Absorber" ../tidy3d/tidy3d/components/boundary.py ../tidy3d/schemas/Simulation.json -S' in /workspace/autofdtd
 succeeded in 0ms:
../tidy3d/tidy3d/components/boundary.py:54:) -> Callable[[type[AbsorberSpec], int], int]:
../tidy3d/tidy3d/components/boundary.py:61:    def _warn_num_layers(cls: type[AbsorberSpec], val: int) -> int:
../tidy3d/tidy3d/components/boundary.py:385:class InternalAbsorber(Box):
../tidy3d/tidy3d/components/boundary.py:398:        title="Absorber Shift",
../tidy3d/tidy3d/components/boundary.py:591:# """ Absorber parameters """
../tidy3d/tidy3d/components/boundary.py:594:class AbsorberParams(Tidy3dBaseModel):
../tidy3d/tidy3d/components/boundary.py:595:    """Specifies parameters common to Absorbers and PMLs.
../tidy3d/tidy3d/components/boundary.py:599:    >>> params = AbsorberParams(sigma_order=3, sigma_min=0.0, sigma_max=1.5)
../tidy3d/tidy3d/components/boundary.py:626:class PMLParams(AbsorberParams):
../tidy3d/tidy3d/components/boundary.py:673:DefaultAbsorberParameters = AbsorberParams(sigma_order=3, sigma_min=0.0, sigma_max=6.4)
../tidy3d/tidy3d/components/boundary.py:685:DefaultStablePMLParameters = PMLParams(
../tidy3d/tidy3d/components/boundary.py:698:""" Absorber specifications """
../tidy3d/tidy3d/components/boundary.py:701:class AbsorberSpec(BoundaryEdge):
../tidy3d/tidy3d/components/boundary.py:709:    parameters: AbsorberParams = Field(
../tidy3d/tidy3d/components/boundary.py:710:        title="Absorber Parameters",
../tidy3d/tidy3d/components/boundary.py:726:class PML(AbsorberSpec):
../tidy3d/tidy3d/components/boundary.py:784:        if dispersion is necessary, switching from the :class:`PML` to :class:`Absorber` can effectively address the
../tidy3d/tidy3d/components/boundary.py:822:    :class:`StablePML`:
../tidy3d/tidy3d/components/boundary.py:825:    :class:`Absorber`:
../tidy3d/tidy3d/components/boundary.py:866:class StablePML(AbsorberSpec):
../tidy3d/tidy3d/components/boundary.py:872:    >>> pml = StablePML(num_layers=40)
../tidy3d/tidy3d/components/boundary.py:880:    :class:`Absorber`:
../tidy3d/tidy3d/components/boundary.py:898:        DefaultStablePMLParameters,
../tidy3d/tidy3d/components/boundary.py:919:class Absorber(AbsorberSpec):
../tidy3d/tidy3d/components/boundary.py:938:        light reaches the :class:`PEC` boundary at the end of the :class:`Absorber`, travels back through it,
../tidy3d/tidy3d/components/boundary.py:940:        the maximum conductivity :class:`AbsorberParams` can help. In both cases, changing the order of the scaling
../tidy3d/tidy3d/components/boundary.py:941:        of the conductivity (:attr:`tidy3d.AbsorberParams.sigma_order`) can also have an effect, but this is a more
../tidy3d/tidy3d/components/boundary.py:946:    >>> pml = Absorber(num_layers=40)
../tidy3d/tidy3d/components/boundary.py:965:    parameters: AbsorberParams = Field(
../tidy3d/tidy3d/components/boundary.py:966:        DefaultAbsorberParameters,
../tidy3d/tidy3d/components/boundary.py:967:        title="Absorber Parameters",
../tidy3d/tidy3d/components/boundary.py:977:PMLTypes = Union[PML, StablePML, Absorber, None]
../tidy3d/tidy3d/components/boundary.py:989:    StablePML,
../tidy3d/tidy3d/components/boundary.py:990:    Absorber,
../tidy3d/tidy3d/components/boundary.py:1052:            self.plus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary)
../tidy3d/tidy3d/components/boundary.py:1053:        ) + isinstance(self.minus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary))
../tidy3d/tidy3d/components/boundary.py:1319:        parameters: PMLParams = DefaultStablePMLParameters,
../tidy3d/tidy3d/components/boundary.py:1334:        plus = StablePML(num_layers=num_layers, parameters=parameters)
../tidy3d/tidy3d/components/boundary.py:1335:        minus = StablePML(num_layers=num_layers, parameters=parameters)
../tidy3d/tidy3d/components/boundary.py:1342:        parameters: PMLParams = DefaultAbsorberParameters,
../tidy3d/tidy3d/components/boundary.py:1357:        plus = Absorber(num_layers=num_layers, parameters=parameters)
../tidy3d/tidy3d/components/boundary.py:1358:        minus = Absorber(num_layers=num_layers, parameters=parameters)
../tidy3d/schemas/Simulation.json:54:    "Absorber": {
../tidy3d/schemas/Simulation.json:83:          "$ref": "#/$defs/AbsorberParams",
../tidy3d/schemas/Simulation.json:89:            "type": "AbsorberParams"
../tidy3d/schemas/Simulation.json:93:          "const": "Absorber",
../tidy3d/schemas/Simulation.json:94:          "default": "Absorber",
../tidy3d/schemas/Simulation.json:100:    "AbsorberParams": {
../tidy3d/schemas/Simulation.json:124:          "const": "AbsorberParams",
../tidy3d/schemas/Simulation.json:125:          "default": "AbsorberParams",
../tidy3d/schemas/Simulation.json:1620:              "$ref": "#/$defs/Absorber"
../tidy3d/schemas/Simulation.json:1641:              "$ref": "#/$defs/StablePML"
../tidy3d/schemas/Simulation.json:1671:              "$ref": "#/$defs/Absorber"
../tidy3d/schemas/Simulation.json:1692:              "$ref": "#/$defs/StablePML"
../tidy3d/schemas/Simulation.json:9172:    "InternalAbsorber": {
../tidy3d/schemas/Simulation.json:9234:          "const": "InternalAbsorber",
../tidy3d/schemas/Simulation.json:9235:          "default": "InternalAbsorber",
../tidy3d/schemas/Simulation.json:15806:    "StablePML": {
../tidy3d/schemas/Simulation.json:15851:          "const": "StablePML",
../tidy3d/schemas/Simulation.json:15852:          "default": "StablePML",
../tidy3d/schemas/Simulation.json:17627:        "$ref": "#/$defs/InternalAbsorber"

exec
/bin/bash -lc "sed -n '1,240p' docs/boundaries.md" in /workspace/autofdtd
exec
/bin/bash -lc "sed -n '70,120p' phase1/kernel-formula-sources.md" in /workspace/autofdtd
 succeeded in 0ms:
# Boundary Scope

Phase 1 now has an explicit non-absorbing boundary surface built around
`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.

## Public API

- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
- `Boundary` pairs `minus` and `plus` edges along one axis.
- `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
  behavior on all sides in the currently implemented subset.
- If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
  coerced to the conductor edge with a warning so later runtime handling stays
  reflection-based instead of mixing halo semantics on one axis.

## Lowering And Runtime

- `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
  surface into a runtime plan with explicit per-face mode tags, sign transforms,
  halo depth, and stage ordering.
- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
  vector fields with one ghost cell on each side.
- Periodic boundaries copy ghost cells from the opposite interior face.
- PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
  field parity is explicit instead of being hidden in a monolithic timestep kernel.

## Current Limits

- Runtime compilation currently supports only the non-absorbing subset:
  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
  outside the runtime compiler path for this task and fail explicitly there.
- The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
  cell per face.

 succeeded in 0ms:
  - `../libctl/utils/geom.c`
  - `papers/meep_paper.pdf`
- Expected outputs:
  - staircasing vs averaging policy behavior
  - effective coefficient generation rules
  - clear boundary between implemented and deferred subpixel methods

## Boundary Layers

- Primary purpose:
  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
- First references:
  - `../tidy3d/tidy3d/components/boundary.py`
  - `../meep/src/boundaries.cpp`
  - `papers/meep_paper.pdf`
- Expected outputs:
  - per-boundary family coefficient/state definitions
  - update-kernel formulas or runtime transforms
  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`

## Periodic, Bloch, And Symmetry Transforms

- Primary purpose:
  Clarify what is a kernel update versus what is a runtime/topology transform.
- First references:
  - `../tidy3d/tidy3d/components/boundary.py`
  - `../meep/doc/docs/Chunks_and_Symmetry.md`
  - `../meep/src/boundaries.cpp`
  - `papers/meep_paper.pdf`
- Expected outputs:
  - logical-vs-stored topology rules
  - phase-transform rules for Bloch boundaries
  - ownership/halo implications for symmetry-reduced layouts

## Source Injection

- Primary purpose:
  Determine the interpolation/restriction and injection formulas for current sources, field sources, plane waves, beams, mode sources, and TFSF.
- First references:
  - `../tidy3d/tidy3d/components/source/current.py`
  - `../tidy3d/tidy3d/components/source/field.py`
  - `../meep/src/loop_in_chunks.cpp`
  - `papers/meep_paper.pdf`
- Expected outputs:
  - injection metadata model
  - source-specific injection kernel/runtime rules
  - restrictions for unsupported source/geometry combinations

## Monitor Accumulation And Postprocessing

- Primary purpose:

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/examples/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Examples for the foundational Phase 1 boundary subset."""

from __future__ import annotations

import numpy as np

from autofdtd.api import Boundary, BoundarySpec
from autofdtd.compiler import compile_boundary_spec
from autofdtd.runtime import build_boundary_runtime


def boundary_runtime_snapshot() -> dict[str, object]:
    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""

    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.periodic(),
            y=Boundary.pec(),
            z=Boundary.pmc(),
        )
    )
    return compiled.to_payload()


def pml_absorption_snapshot() -> dict[str, object]:
    """Return a compact snapshot showing the staged PML attenuation path."""

    runtime = build_boundary_runtime(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        field_shape=(10, 4, 4, 3),
    )
    electric = np.zeros((10, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((10, 4, 4, 3), dtype=np.float64)
    electric[5:8, 1:3, 1:3, 0] = 1.0
    before = float(np.sum(np.abs(electric) ** 2))
    for _ in range(8):
        electric, magnetic = runtime.apply(electric, magnetic)
    after = float(np.sum(np.abs(electric) ** 2))
    return {
        "compiled_pml_faces": runtime.compiled.pml_faces,
        "stage_order": runtime.compiled.stage_order,
        "energy_before": before,
        "energy_after": after,
    }

exec
/bin/bash -lc "sed -n '420,620p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
    kappa_max: float
    alpha_order: int
    alpha_min: float
    alpha_max: float


class PMLIR(IRModel):
    """Typed execution IR for a baseline PML boundary edge."""

    type: Literal["PMLIR"] = "PMLIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PML"] = "PML"
    name: str | None = None
    num_layers: int
    parameters: PMLParamsIR
    extrude_structures: bool = True


class SymmetryAxisIR(IRModel):
    """Typed execution IR for one mirror-symmetry axis."""

    type: Literal["SymmetryAxisIR"] = "SymmetryAxisIR"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]


BoundaryEdgeIR = (
    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
)


class BoundaryIR(IRModel):
    """Typed execution IR for one axis boundary pair."""

    type: Literal["BoundaryIR"] = "BoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Boundary"] = "Boundary"
    plus: BoundaryEdgeIR
    minus: BoundaryEdgeIR


class BoundarySpecIR(IRModel):
    """Typed execution IR for the Phase 1 boundary container."""

    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["BoundarySpec"] = "BoundarySpec"
    x: BoundaryIR
    y: BoundaryIR
    z: BoundaryIR
    requires_complex_fields: bool = False
    periodic_axes: tuple[str, ...] = ()
    bloch_axes: tuple[str, ...] = ()
    reflective_axes: tuple[str, ...] = ()
    pml_axes: tuple[str, ...] = ()
    pml_faces: tuple[str, ...] = ()
    active_symmetry_axes: tuple[str, ...] = ()
    symmetry_axes: tuple[SymmetryAxisIR, ...] = ()


class MediumIR(IRModel):
    """Typed execution IR for a homogeneous isotropic dielectric or conductor."""

    type: Literal["MediumIR"] = "MediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Medium"] = "Medium"
    name: str | None = None
    permittivity: float
    conductivity: float
    permeability: float
    magnetic_conductivity: float


class PECMediumIR(IRModel):
    """Typed execution IR for a perfect electric conductor marker."""

    type: Literal["PECMediumIR"] = "PECMediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["PECMedium"] = "PECMedium"
    name: str | None = None


class PMCMediumIR(IRModel):
    """Typed execution IR for a perfect magnetic conductor marker."""

    type: Literal["PMCMediumIR"] = "PMCMediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["PMCMedium"] = "PMCMedium"
    name: str | None = None


class PoleResidueIR(IRModel):
    """Typed execution IR for a pole-residue dispersive medium."""

    type: Literal["PoleResidueIR"] = "PoleResidueIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["PoleResidue"] = "PoleResidue"
    name: str | None = None
    eps_inf: float
    poles: tuple[tuple[tuple[float, float], tuple[float, float]], ...]


class SellmeierIR(IRModel):
    """Typed execution IR for a Sellmeier optical-dispersion medium."""

    type: Literal["SellmeierIR"] = "SellmeierIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Sellmeier"] = "Sellmeier"
    name: str | None = None
    coeffs: tuple[tuple[float, float], ...]


class LorentzIR(IRModel):
    """Typed execution IR for a Lorentz dispersive medium."""

    type: Literal["LorentzIR"] = "LorentzIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Lorentz"] = "Lorentz"
    name: str | None = None
    eps_inf: float
    coeffs: tuple[tuple[float, float, float], ...]


class DrudeIR(IRModel):
    """Typed execution IR for a Drude dispersive medium."""

    type: Literal["DrudeIR"] = "DrudeIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Drude"] = "Drude"
    name: str | None = None
    eps_inf: float
    coeffs: tuple[tuple[float, float], ...]


class DebyeIR(IRModel):
    """Typed execution IR for a Debye dispersive medium."""

    type: Literal["DebyeIR"] = "DebyeIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Debye"] = "Debye"
    name: str | None = None
    eps_inf: float
    coeffs: tuple[tuple[float, float], ...]


class AnisotropicMediumIR(IRModel):
    """Typed execution IR for a diagonal anisotropic medium."""

    type: Literal["AnisotropicMediumIR"] = "AnisotropicMediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["AnisotropicMedium"] = "AnisotropicMedium"
    name: str | None = None
    xx: (
        MediumIR
        | PECMediumIR
        | PMCMediumIR
        | PoleResidueIR
        | SellmeierIR
        | LorentzIR
        | DrudeIR
        | DebyeIR
    )
    yy: (
        MediumIR
        | PECMediumIR
        | PMCMediumIR
        | PoleResidueIR
        | SellmeierIR
        | LorentzIR
        | DrudeIR
        | DebyeIR
    )
    zz: (
        MediumIR
        | PECMediumIR
        | PMCMediumIR
        | PoleResidueIR
        | SellmeierIR
        | LorentzIR
        | DrudeIR
        | DebyeIR
    )


MediumComponentIR = (
    MediumIR
    | PECMediumIR
    | PMCMediumIR
    | PoleResidueIR
    | SellmeierIR
    | LorentzIR
    | DrudeIR
    | DebyeIR
    | AnisotropicMediumIR
    | ComponentIR
)


class StructureIR(IRModel):

exec
/bin/bash -lc 'rg -n "class .*IR|def boundary_spec_to_ir|PMLIR|PeriodicIR|BlochBoundaryIR|BoundarySpecIR" src/autofdtd/ir/models.py -n -S' in /workspace/autofdtd
 succeeded in 0ms:
73:class IRModel(TaggedModel):
98:class ComponentIR(IRModel):
126:class GeometryTransformIR(IRModel):
134:class BoxIR(IRModel):
146:class SphereIR(IRModel):
158:class CylinderIR(IRModel):
172:class PolySlabIR(IRModel):
188:class GeometryGroupIR(IRModel):
198:class TransformedIR(IRModel):
209:class ClipOperationIR(IRModel):
233:class ResolvedGridAxisIR(IRModel):
243:class ResolvedGridIR(IRModel):
258:class UniformGridIR(IRModel):
266:class CustomGridBoundariesIR(IRModel):
274:class CustomGridIR(IRModel):
283:class AutoGridIR(IRModel):
294:class GridRefinementIR(IRModel):
304:class LayerRefinementSpecIR(IRModel):
320:class GridSpecIR(IRModel):
333:class StaircasingIR(IRModel):
343:class PolarizedAveragingIR(IRModel):
356:class SubpixelSpecIR(IRModel):
372:class PeriodicIR(IRModel):
375:    type: Literal["PeriodicIR"] = "PeriodicIR"
381:class BlochBoundaryIR(IRModel):
384:    type: Literal["BlochBoundaryIR"] = "BlochBoundaryIR"
393:class PECBoundaryIR(IRModel):
402:class PMCBoundaryIR(IRModel):
411:class PMLParamsIR(IRModel):
426:class PMLIR(IRModel):
429:    type: Literal["PMLIR"] = "PMLIR"
438:class SymmetryAxisIR(IRModel):
449:    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
453:class BoundaryIR(IRModel):
463:class BoundarySpecIR(IRModel):
466:    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
482:class MediumIR(IRModel):
495:class PECMediumIR(IRModel):
504:class PMCMediumIR(IRModel):
513:class PoleResidueIR(IRModel):
524:class SellmeierIR(IRModel):
534:class LorentzIR(IRModel):
545:class DrudeIR(IRModel):
556:class DebyeIR(IRModel):
567:class AnisotropicMediumIR(IRModel):
620:class StructureIR(IRModel):
635:class SceneIR(IRModel):
645:class SimulationIR(IRModel):
660:    boundary_spec: BoundarySpecIR | ComponentIR | None = None
665:class ArtifactIR(IRModel):
676:class ExecutionManifestIR(IRModel):
694:class ExecutionPackageIR(IRModel):
974:        return PeriodicIR(name=edge.name)
976:        return BlochBoundaryIR(
992:        return PMLIR(
1065:def boundary_spec_to_ir(
1069:) -> BoundarySpecIR | ComponentIR:
1120:    return BoundarySpecIR(

exec
/bin/bash -lc "sed -n '620,760p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
class StructureIR(IRModel):
    """Lowered structure shell for scene compilation and materialization."""

    type: Literal["StructureIR"] = "StructureIR"
    name: str | None = None
    original_type: str = "Structure"
    source_index: int = 0
    priority: int | None = None
    resolved_priority: int = 0
    precedence_rank: int = 0
    geometry: GeometryIR
    medium: MediumComponentIR
    background_medium: MediumComponentIR | None = None


class SceneIR(IRModel):
    """Ordered scene IR used by later compilation stages."""

    type: Literal["SceneIR"] = "SceneIR"
    original_type: str = "Scene"
    structure_priority_mode: str = StructurePriorityMode.EQUAL.value
    background_medium: MediumComponentIR
    structures: tuple[StructureIR, ...] = ()


class SimulationIR(IRModel):
    """Top-level execution IR for a Phase 1 simulation."""

    type: Literal["SimulationIR"] = "SimulationIR"
    original_type: str = "Simulation"
    autofdtd_version: str = __version__
    center: tuple[float, float, float]
    size: tuple[float, float, float]
    run_time: float
    courant: float
    symmetry: tuple[int, int, int]
    shutoff: float | None = None
    scene: SceneIR
    sources: tuple[ComponentIR, ...] = ()
    monitors: tuple[ComponentIR, ...] = ()
    boundary_spec: BoundarySpecIR | ComponentIR | None = None
    grid_spec: GridSpecIR | ComponentIR | None = None
    subpixel: SubpixelSpecIR | ComponentIR | None = None


class ArtifactIR(IRModel):
    """Manifest entry for an external or packaged execution artifact."""

    type: Literal["ArtifactIR"] = "ArtifactIR"
    logical_path: str
    role: str
    media_type: str
    sha256: str
    byte_count: int


class ExecutionManifestIR(IRModel):
    """Transport metadata for a packaged IR bundle."""

    type: Literal["ExecutionManifestIR"] = "ExecutionManifestIR"
    package_format: str = EXECUTION_PACKAGE_FORMAT
    autofdtd_version: str = __version__
    entrypoint: str = "simulation.json"
    artifacts: tuple[ArtifactIR, ...] = ()
    metadata: dict[str, Any] = Field(default_factory=dict)

    @field_validator("package_format")
    @classmethod
    def _validate_package_format(cls, value: str) -> str:
        if value != EXECUTION_PACKAGE_FORMAT:
            raise ValueError(f"package_format must be {EXECUTION_PACKAGE_FORMAT!r}")
        return value


class ExecutionPackageIR(IRModel):
    """Filesystem- and transport-ready bundle for local or remote execution."""

    type: Literal["ExecutionPackageIR"] = "ExecutionPackageIR"
    manifest: ExecutionManifestIR
    simulation: SimulationIR

    @classmethod
    def from_simulation(
        cls,
        simulation: Simulation,
        *,
        metadata: dict[str, Any] | None = None,
    ) -> ExecutionPackageIR:
        """Lower a public simulation shell into the versioned execution IR."""
        simulation_ir = simulation_to_ir(simulation)
        manifest = ExecutionManifestIR(metadata=metadata or {})
        return cls(manifest=manifest, simulation=simulation_ir)

    def write_bundle(self, root: str | Path) -> Path:
        """Write a directory bundle with manifest and simulation payloads."""
        root_path = Path(root)
        root_path.mkdir(parents=True, exist_ok=True)
        simulation_path = root_path / self.manifest.entrypoint
        simulation_path.write_text(self.simulation.to_json_text() + "\n", encoding="utf-8")
        artifact = ArtifactIR(
            logical_path=self.manifest.entrypoint,
            role="simulation",
            media_type="application/json",
            sha256=_sha256_file(simulation_path),
            byte_count=simulation_path.stat().st_size,
        )
        manifest = self.manifest.model_copy(update={"artifacts": (artifact,)})
        manifest_path = root_path / "manifest.json"
        manifest_path.write_text(manifest.to_json_text() + "\n", encoding="utf-8")
        return manifest_path


def _sha256_file(path: Path) -> str:
    digest = sha256()
    digest.update(path.read_bytes())
    return digest.hexdigest()


def _component_payload(value: object) -> dict[str, Any]:
    if isinstance(value, BaseModel):
        raw = value.model_dump(mode="json", exclude_none=True)
    elif isinstance(value, dict):
        raw = dict(value)
    else:
        raise TypeError(f"component payload must be mapping-like, got {type(value)!r}")
    return {str(key): json_ready(item) for key, item in raw.items()}


def component_to_ir(value: object, *, family: str) -> ComponentIR:
    """Normalize a tagged public component into a transport-safe IR envelope."""
    raw = _component_payload(value)
    component_type = str(raw.pop("type", value.__class__.__name__))
    name = raw.get("name")
    return ComponentIR(
        family=family,
        component_type=component_type,
        name=str(name) if isinstance(name, str) else None,
        payload=raw,
    )



exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/boundaries/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary-family models for the foundational Phase 1 surface."""

from autofdtd.boundaries.models import (
    Boundary,
    BoundaryEdge,
    BoundarySpec,
    BlochBoundary,
    DEFAULT_PML_PARAMS,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)

__all__ = [
    "Boundary",
    "BoundaryEdge",
    "BoundarySpec",
    "BlochBoundary",
    "DEFAULT_PML_PARAMS",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCBoundary",
    "Periodic",
    "boundary_edge_model_from_value",
    "boundary_model_from_value",
    "boundary_spec_model_from_value",
]

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/compiler/__init__.py && sed -n '1,220p' src/autofdtd/runtime/__init__.py && sed -n '1,220p' src/autofdtd/kernels/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Scene compilation, normalization, and lowering namespace."""

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryAxis,
    CompiledBoundaryEdge,
    CompiledPMLCoefficients,
    CompiledBoundarySpec,
    CompiledSymmetryAxis,
    compile_boundary_spec,
    normalize_axis_boundary,
)
from autofdtd.compiler.materials import (
    AnisotropicMaterialCoefficients,
    EPSILON_0,
    MU_0,
    ConstitutiveMode,
    IsotropicMaterialCoefficients,
    MaterialCoefficients,
    PoleResidueMaterialCoefficients,
    PoleResidueTermCoefficients,
    SceneMaterialSample,
    compile_debye_coefficients,
    compile_anisotropic_medium_coefficients,
    compile_drude_coefficients,
    compile_isotropic_medium_coefficients,
    compile_lorentz_coefficients,
    compile_medium_coefficients,
    compile_pole_residue_coefficients,
    compile_scene_medium_coefficients,
    compile_sellmeier_coefficients,
    pole_residue_terms_as_complex,
    sample_scene_mediums,
)

__all__ = [
    "BoundaryMode",
    "CompiledBoundaryAxis",
    "CompiledBoundaryEdge",
    "CompiledPMLCoefficients",
    "CompiledBoundarySpec",
    "CompiledSymmetryAxis",
    "ConstitutiveMode",
    "EPSILON_0",
    "MU_0",
    "AnisotropicMaterialCoefficients",
    "IsotropicMaterialCoefficients",
    "MaterialCoefficients",
    "PoleResidueMaterialCoefficients",
    "PoleResidueTermCoefficients",
    "SceneMaterialSample",
    "compile_anisotropic_medium_coefficients",
    "compile_boundary_spec",
    "compile_debye_coefficients",
    "compile_drude_coefficients",
    "compile_medium_coefficients",
    "compile_isotropic_medium_coefficients",
    "compile_lorentz_coefficients",
    "compile_pole_residue_coefficients",
    "compile_sellmeier_coefficients",
    "compile_scene_medium_coefficients",
    "pole_residue_terms_as_complex",
    "normalize_axis_boundary",
    "sample_scene_mediums",
]
"""Chunk runtime, scheduling, and execution-context namespace."""

from autofdtd.runtime.boundaries import (
    BoundaryRuntime,
    allocate_pml_boundary_state,
    apply_compiled_boundaries,
    build_boundary_runtime,
)

__all__ = [
    "BoundaryRuntime",
    "allocate_pml_boundary_state",
    "apply_compiled_boundaries",
    "build_boundary_runtime",
]
"""Warp kernel conventions and staged update namespace."""

from autofdtd.kernels.boundaries import (
    WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
    PMLBoundaryState,
    PMLFaceState,
    apply_axis_boundary_ghosts,
    apply_boundary_ghosts,
    apply_boundary_stages,
    apply_pml_layers,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.kernels.materials import (
    AnisotropicAuxiliaryState,
    WARP_AVAILABLE,
    allocate_anisotropic_state,
    anisotropic_electric_update,
    anisotropic_magnetic_update,
    PoleResidueAuxiliaryState,
    allocate_pole_residue_state,
    constitutive_kernel_metadata,
    electric_constitutive_update,
    magnetic_constitutive_update,
    pole_residue_electric_update,
    warp_backend_available,
)

__all__ = [
    "AnisotropicAuxiliaryState",
    "BOUNDARY_WARP_AVAILABLE",
    "PMLBoundaryState",
    "PMLFaceState",
    "PoleResidueAuxiliaryState",
    "WARP_AVAILABLE",
    "allocate_anisotropic_state",
    "allocate_pole_residue_state",
    "apply_axis_boundary_ghosts",
    "apply_boundary_ghosts",
    "apply_boundary_stages",
    "apply_pml_layers",
    "boundary_edge_transform",
    "anisotropic_electric_update",
    "anisotropic_magnetic_update",
    "boundary_kernel_metadata",
    "constitutive_kernel_metadata",
    "electric_constitutive_update",
    "magnetic_constitutive_update",
    "pole_residue_electric_update",
    "symmetry_transform",
    "warp_backend_available",
]

I’ve read the current boundary stack and reference surface. Next I’m extending the existing PML path rather than inventing a separate absorber pipeline, with distinct edge/IR types and compiled profile kinds for `StablePML` and `Absorber`.
  ✓ Map current boundary implementation and reference defaults for StablePML/Absorber
  → Extend public API, IR, and compiler metadata for StablePML and Absorber
  • Integrate kernel/runtime state handling for the new absorbing profiles
  • Add tests, examples, docs, and working-memory updates; run focused verification
exec
/bin/bash -lc "sed -n '590,980p' ../tidy3d/tidy3d/components/boundary.py" in /workspace/autofdtd
 succeeded in 0ms:

# """ Absorber parameters """


class AbsorberParams(Tidy3dBaseModel):
    """Specifies parameters common to Absorbers and PMLs.

    Example
    -------
    >>> params = AbsorberParams(sigma_order=3, sigma_min=0.0, sigma_max=1.5)
    """

    sigma_order: NonNegativeInt = Field(
        3,
        title="Sigma Order",
        description=(
            "Order of the polynomial describing the absorber profile "
            "(:math:`\\mathrm{dist}^{\\text{sigma\\_order}}`)."
        ),
    )

    sigma_min: NonNegativeFloat = Field(
        0.0,
        title="Sigma Minimum",
        description="Minimum value of the absorber conductivity.",
        json_schema_extra={"units": PML_SIGMA},
    )

    sigma_max: NonNegativeFloat = Field(
        1.5,
        title="Sigma Maximum",
        description="Maximum value of the absorber conductivity.",
        json_schema_extra={"units": PML_SIGMA},
    )


class PMLParams(AbsorberParams):
    """Specifies full set of parameters needed for complex, frequency-shifted PML.

    Example
    -------
    >>> params = PMLParams(sigma_order=3, sigma_min=0.0, sigma_max=1.5, kappa_min=0.0)
    """

    kappa_order: NonNegativeInt = Field(
        3,
        title="Kappa Order",
        description=(
            "Order of the polynomial describing the PML kappa profile "
            "(:math:`\\kappa\\sim\\mathrm{dist}^{\\text{kappa\\_order}}`)."
        ),
    )

    kappa_min: NonNegativeFloat = Field(0.0, title="Kappa Minimum")

    kappa_max: NonNegativeFloat = Field(1.5, title="Kappa Maximum")

    alpha_order: NonNegativeInt = Field(
        3,
        title="Alpha Order",
        description=(
            "Order of the polynomial describing the PML alpha profile "
            "(:math:`\\alpha\\sim\\mathrm{dist}^{\\text{alpha\\_order}}`)."
        ),
    )

    alpha_min: NonNegativeFloat = Field(
        0.0,
        title="Alpha Minimum",
        description="Minimum value of the PML alpha.",
        json_schema_extra={"units": PML_SIGMA},
    )

    alpha_max: NonNegativeFloat = Field(
        1.5,
        title="Alpha Maximum",
        description="Maximum value of the PML alpha.",
        json_schema_extra={"units": PML_SIGMA},
    )


""" Default absorber parameters """

DefaultAbsorberParameters = AbsorberParams(sigma_order=3, sigma_min=0.0, sigma_max=6.4)
DefaultPMLParameters = PMLParams(
    sigma_order=3,
    sigma_min=0.0,
    sigma_max=1.5,
    kappa_order=3,
    kappa_min=1.0,
    kappa_max=3.0,
    alpha_order=1,
    alpha_min=0.0,
    alpha_max=0.0,
)
DefaultStablePMLParameters = PMLParams(
    sigma_order=3,
    sigma_min=0.0,
    sigma_max=1.0,
    kappa_order=3,
    kappa_min=1.0,
    kappa_max=5.0,
    alpha_order=1,
    alpha_min=0.0,
    alpha_max=0.9,
)


""" Absorber specifications """


class AbsorberSpec(BoundaryEdge):
    """Specifies the generic absorber properties along a single dimension."""

    num_layers: float = Field(
        title="Number of Layers",
        description="Number of layers of standard PML.",
        ge=1,
    )
    parameters: AbsorberParams = Field(
        title="Absorber Parameters",
        description="Parameters to fine tune the absorber profile and properties.",
    )

    extrude_structures: bool = Field(
        False,
        title="Enable structure extrusion to PML",
        description="Automatically extrude structures into the absorbing region (e.g., PML or adiabatic absorber). "
        f"Any structure located within {CLIPPING_MARGIN} cells of a simulation boundary will be extended "
        "through the full thickness of the PML/absorber. "
        f"The extruded region is assigned the material properties of the structure {CLIPPING_MARGIN} cells "
        "from the simulation boundary. "
        "Extrusion is performed along the direction normal to the PML/absorber surface.",
    )


class PML(AbsorberSpec):
    """Specifies a standard PML along a single dimension.

    Notes
    ------

        **1D Model Illustration**

        Consider a transformed wave equation in the :math:`x` dimension below _`[1]`:

        .. math::

             \\left( \\left( \\frac{1}{s(x)} \\frac{\\delta}{\\delta x} \\right)^2 - \\frac{1}{c^2} \\frac{\\delta^2}{\\delta t^2} \\right) E = 0

        where the wave stretch factor :math:`s(x)` depends on the PML boundary position in the :math:`x` dimension.

        .. TODO what is x at 0?

        .. math::

             s(x) = \\left \\{
                        \\begin{array}{lr}
                            1, & \\text{for } x < 0 \\\\
                            1 - \\frac{\\sigma}{i \\omega \\epsilon_0}, & \\text{for } x > 0
                        \\end{array}
                    \\right \\}

        The wave equation can be solved and plotted accordingly as a function of the :math:`x` dimension.

        .. math::

             E(x) = \\left \\{
                        \\begin{array}{lr}
                            e^{i(kx - \\omega t)}, & \\text{for } x < 0 \\\\
                            e^{i(kx - \\omega t)} \\times e^{-\\frac{\\sigma x}{c \\epsilon_0}} & \\text{for } x > 0
                        \\end{array}
                    \\right \\}

        Hence, we see how this PML stretch factor induces frequency-independent exponential attentation and no
        reflection after the boundary at :math:`x=0`.

        .. image:: ../../_static/img/pml_boundary.png

        .. TODO make this image better

        **Usage Caveats**

        A perfectly matched layer (PML) is the most commonly used boundary condition in FDTD simulations to truncate
        a simulation domain and absorb outgoing radiation. However, many divergence issues are associated with the
        use of PML. One of the most common causes of a diverged simulation is structures inserted into PML at an angle.

        .. TODO links to absorber boundaries

        .. image:: ../../notebooks/img/diverged-fdtd-simulation.png

        Incorporating a dispersive material into the PML can also cause simulation divergence in certain scenarios.
        If your simulation lacks any structures inserted into the PML at an angle, but includes dispersive material
        in PML, it is advisable to substitute a nondispersive material for the dispersive material. Alternatively,
        if dispersion is necessary, switching from the :class:`PML` to :class:`Absorber` can effectively address the
        issue.

        The PML can effectively absorb outgoing radiation with minimum reflection as if the radiation just propagates
        into the free space. However, it’s important to keep in mind that the PML only absorbs propagating fields. For
        evanescent fields, the PML can act as an amplification medium and cause a simulation to diverge. In Tidy3D,
        a warning will appear if the distance between a structure is smaller than half of a wavelength to prevent
        evanescent fields from leaking into PML. In most cases, the evanescent field will naturally die off within
        half a wavelength, but in some instances, a larger distance may be required.

        .. image:: ../../notebooks/img/diverged-fdtd-simulation1.png


        **References**

        .. [1]  W.C. Chew and W.H. Weedon, Microwave and Optical Tech. Lett., 7 (13), 599,1994; S. Johnson, arXiv 2108.05348, 2021
        .. [2]  Antonios Giannopoulos, IEEE Transactions on Antennas and Propagation, 56(9), 2995, 2008

    Note
    ----

        **Practical Advice**

        For best results, structures that intersect with the PML or simulation edges should extend all the way
        through using ``td.inf``::

            Structure(geometry=Box(size=(td.inf, width, height)), medium=core)

        Structures that terminate inside PML can cause evanescent fields at the interface to be
        amplified by the absorber, potentially leading to simulation divergence.

    Example
    -------
    >>> pml = PML(num_layers=10)

    See Also
    --------

    :class:`StablePML`:
         This PML deals handles possibly divergent simulations better, but at the expense of more layers.

    :class:`Absorber`:
         Specifies an adiabatic absorber along a single dimension.

    **Notebooks:**
        * `How to troubleshoot a diverged FDTD simulation <../../notebooks/DivergedFDTDSimulation.html>`_

    **Lectures:**
        * `Using FDTD to Compute a Transmission Spectrum <https://www.flexcompute.com/fdtd101/Lecture-2-Using-FDTD-to-Compute-a-Transmission-Spectrum/>`__
        * `Introduction to perfectly matched layer (PML) tutorial <https://www.flexcompute.com/fdtd101/Lecture-6-Introduction-to-perfectly-matched-layer/>`__

    """

    num_layers: int = Field(
        12,
        title="Number of Layers",
        description="Number of layers of standard PML.",
        ge=1,
    )

    parameters: PMLParams = Field(
        DefaultPMLParameters,
        title="PML Parameters",
        description="Parameters of the complex frequency-shifted absorption poles.",
    )

    _warn_num_layers = warn_num_layers_factory(
        min_num_layers=MIN_NUM_PML_LAYERS, descr="perfectly-matched layer"
    )

    extrude_structures: bool = Field(
        True,
        title="Enable structure extrusion to PML",
        description="Automatically extrude structures into the absorbing region (e.g., PML or adiabatic absorber). "
        f"Any structure located within {CLIPPING_MARGIN} cells of a simulation boundary will be extended "
        "through the full thickness of the PML/absorber. "
        f"The extruded region is assigned the material properties of the structure {CLIPPING_MARGIN} cells "
        "from the simulation boundary. "
        "Extrusion is performed along the direction normal to the PML/absorber surface.",
    )


class StablePML(AbsorberSpec):
    """Specifies a 'stable' PML along a single dimension.
    This PML deals handles possibly divergent simulations better, but at the expense of more layers.

    Example
    -------
    >>> pml = StablePML(num_layers=40)

    See Also
    --------

    :class:`PML`:
         A standard PML along a single dimension.

    :class:`Absorber`:
         Specifies an adiabatic absorber along a single dimension.

    **Notebooks:**
        * `How to troubleshoot a diverged FDTD simulation <../../notebooks/DivergedFDTDSimulation.html>`_

    **Lectures:**
        * `Introduction to perfectly matched layer (PML) tutorial <https://www.flexcompute.com/fdtd101/Lecture-6-Introduction-to-perfectly-matched-layer/>`__
    """

    num_layers: int = Field(
        40,
        title="Number of Layers",
        description="Number of layers of 'stable' PML.",
        ge=1,
    )

    parameters: PMLParams = Field(
        DefaultStablePMLParameters,
        title="Stable PML Parameters",
        description="'Stable' parameters of the complex frequency-shifted absorption poles.",
    )

    _warn_num_layers = warn_num_layers_factory(
        min_num_layers=MIN_NUM_STABLE_PML_LAYERS, descr="stable perfectly-matched layer"
    )

    extrude_structures: bool = Field(
        True,
        title="Enable structure extrusion to PML",
        description="Automatically extrude structures into the absorbing region (e.g., PML or adiabatic absorber). "
        f"Any structure located within {CLIPPING_MARGIN} cells of a simulation boundary will be extended "
        "through the full thickness of the PML/absorber. "
        f"The extruded region is assigned the material properties of the structure {CLIPPING_MARGIN} cells "
        "from the simulation boundary. "
        "Extrusion is performed along the direction normal to the PML/absorber surface.",
    )


class Absorber(AbsorberSpec):
    """Specifies an adiabatic absorber along a single dimension.

    Notes
    -----

        This absorber is well-suited for dispersive materials intersecting with absorbing edges of the simulation at the
        expense of more layers.

        **Usage Caveats**

        Using absorber boundary is often a good remedy to resolve divergence issues related to :class:`PML`. The
        adiabatic absorber is a multilayer system with gradually increasing conductivity. The absorber usually has a
        larger undesired reflection compared to :class:`PML`. In practice, this small difference rarely matters,
        but is important to understand for simulations that require high accuracy.

        There are two possible sources for the reflection from absorbers. The first, and more common one, is that the
        ramping up of the conductivity is not sufficiently slow, which can be remedied by increasing the number of
        absorber layers (40 by default). The second one is that the absorption is not high enough, such that the
        light reaches the :class:`PEC` boundary at the end of the :class:`Absorber`, travels back through it,
        and is still not fully attenuated before re-entering the simulation region. If this is the case, increasing
        the maximum conductivity :class:`AbsorberParams` can help. In both cases, changing the order of the scaling
        of the conductivity (:attr:`tidy3d.AbsorberParams.sigma_order`) can also have an effect, but this is a more
        advanced setting that we typically do not recommend modifying.

    Example
    -------
    >>> pml = Absorber(num_layers=40)

    See Also
    --------

    :class:`PML`:
         A standard PML along a single dimension.

    **Notebooks:**
        * `How to troubleshoot a diverged FDTD simulation <../../notebooks/DivergedFDTDSimulation.html>`_
    """

    num_layers: int = Field(
        40,
        title="Number of Layers",
        description="Number of layers of absorber to add to + and - boundaries.",
        ge=1,
    )

    parameters: AbsorberParams = Field(
        DefaultAbsorberParameters,
        title="Absorber Parameters",
        description="Adiabatic absorber parameters.",
    )

    _warn_num_layers = warn_num_layers_factory(
        min_num_layers=MIN_NUM_ABSORBER_LAYERS, descr="absorber"
    )


# pml types allowed in simulation init
PMLTypes = Union[PML, StablePML, Absorber, None]


# """ boundary specification classes """

exec
/bin/bash -lc "python - <<'PY'
import json
from pathlib import Path
p=Path('../tidy3d/schemas/Simulation.json')
obj=json.loads(p.read_text())
for key in ['AbsorberParams','Absorber','StablePML','PML']:
    print('\\n###', key)
    print(json.dumps(obj['"'$defs'"'][key], indent=2)[:5000])
PY" in /workspace/autofdtd
 succeeded in 0ms:

### AbsorberParams
{
  "additionalProperties": false,
  "properties": {
    "attrs": {
      "additionalProperties": true,
      "default": {},
      "type": "object"
    },
    "sigma_max": {
      "default": 1.5,
      "minimum": 0,
      "type": "number"
    },
    "sigma_min": {
      "default": 0.0,
      "minimum": 0,
      "type": "number"
    },
    "sigma_order": {
      "default": 3,
      "minimum": 0,
      "type": "integer"
    },
    "type": {
      "const": "AbsorberParams",
      "default": "AbsorberParams",
      "type": "string"
    }
  },
  "type": "object"
}

### Absorber
{
  "additionalProperties": false,
  "properties": {
    "attrs": {
      "additionalProperties": true,
      "default": {},
      "type": "object"
    },
    "extrude_structures": {
      "default": false,
      "type": "boolean"
    },
    "name": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "string"
        }
      ],
      "default": null
    },
    "num_layers": {
      "default": 40,
      "minimum": 1,
      "type": "integer"
    },
    "parameters": {
      "$ref": "#/$defs/AbsorberParams",
      "default": {
        "attrs": {},
        "sigma_max": 6.4,
        "sigma_min": 0.0,
        "sigma_order": 3,
        "type": "AbsorberParams"
      }
    },
    "type": {
      "const": "Absorber",
      "default": "Absorber",
      "type": "string"
    }
  },
  "type": "object"
}

### StablePML
{
  "additionalProperties": false,
  "properties": {
    "attrs": {
      "additionalProperties": true,
      "default": {},
      "type": "object"
    },
    "extrude_structures": {
      "default": true,
      "type": "boolean"
    },
    "name": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "string"
        }
      ],
      "default": null
    },
    "num_layers": {
      "default": 40,
      "minimum": 1,
      "type": "integer"
    },
    "parameters": {
      "$ref": "#/$defs/PMLParams",
      "default": {
        "alpha_max": 0.9,
        "alpha_min": 0.0,
        "alpha_order": 1,
        "attrs": {},
        "kappa_max": 5.0,
        "kappa_min": 1.0,
        "kappa_order": 3,
        "sigma_max": 1.0,
        "sigma_min": 0.0,
        "sigma_order": 3,
        "type": "PMLParams"
      }
    },
    "type": {
      "const": "StablePML",
      "default": "StablePML",
      "type": "string"
    }
  },
  "type": "object"
}

### PML
{
  "additionalProperties": false,
  "properties": {
    "attrs": {
      "additionalProperties": true,
      "default": {},
      "type": "object"
    },
    "extrude_structures": {
      "default": true,
      "type": "boolean"
    },
    "name": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "string"
        }
      ],
      "default": null
    },
    "num_layers": {
      "default": 12,
      "minimum": 1,
      "type": "integer"
    },
    "parameters": {
      "$ref": "#/$defs/PMLParams",
      "default": {
        "alpha_max": 0.0,
        "alpha_min": 0.0,
        "alpha_order": 1,
        "attrs": {},
        "kappa_max": 3.0,
        "kappa_min": 1.0,
        "kappa_order": 3,
        "sigma_max": 1.5,
        "sigma_min": 0.0,
        "sigma_order": 3,
        "type": "PMLParams"
      }
    },
    "type": {
      "const": "PML",
      "default": "PML",
      "type": "string"
    }
  },
  "type": "object"
}

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/core/validation.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Checklist-aware normalization and validation helpers for Phase 1 models."""

from __future__ import annotations

import math
import warnings
from collections.abc import Iterable, Mapping, Sequence
from typing import SupportsFloat, cast

from pydantic import BaseModel

from autofdtd.boundaries import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMCBoundary,
    Periodic,
    boundary_spec_model_from_value,
)
from autofdtd.geometry import (
    ClipOperation,
    GeometryArray,
    GeometryGroup,
    PolySlab,
    Transformed,
    geometry_model_from_value,
)
from autofdtd.geometry.polyslab import _normalize_vertices
from autofdtd.geometry.primitives import Box, Cylinder, Sphere
from autofdtd.grid import (
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    SubpixelSpec,
    UniformGrid,
    grid_model_from_value,
    subpixel_model_from_value,
)
from autofdtd.materials import (
    AnisotropicMedium,
    Debye,
    Drude,
    Lorentz,
    LossyMetalMedium,
    Medium,
    PECMedium,
    PMCMedium,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    Sellmeier,
    medium_model_from_value,
)
from autofdtd.planning import FeatureStatus, feature_entry

Vec3 = tuple[float, float, float]
_IMPLEMENTED_BOUNDS_GEOMETRY = frozenset(
    {
        "Box",
        "Sphere",
        "Cylinder",
        "PolySlab",
        "GeometryGroup",
        "Transformed",
        "ClipOperation",
        "GeometryArray",
    }
)
_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
_PARTIAL_GEOMETRY_TYPES = frozenset({"ClipOperation"})
_PHASE1_BOUNDARY_TYPES = frozenset(
    {
        "BoundarySpec",
        "Boundary",
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
        "PMLParams",
    }
)
_PHASE1_GRID_TYPES = frozenset(
    {
        "GridSpec",
        "UniformGrid",
        "CustomGrid",
        "CustomGridBoundaries",
        "AutoGrid",
        "GridRefinement",
        "LayerRefinementSpec",
    }
)
_PHASE1_SUBPIXEL_TYPES = frozenset({"SubpixelSpec", "Staircasing", "PolarizedAveraging"})
_IMPLEMENTED_MEDIUM_TYPES = frozenset(
    {
        "Medium",
        "PECMedium",
        "PMCMedium",
        "LossyMetalMedium",
        "PoleResidue",
        "Sellmeier",
        "Lorentz",
        "Drude",
        "Debye",
        "AnisotropicMedium",
        "PerturbationMedium",
        "PerturbationPoleResidue",
    }
)
_PLANNED_GRID_TYPES = frozenset(
    {
        "QuasiUniformGrid",
    }
)


class AutoFDTDValidationWarning(UserWarning):
    """Warning category for non-fatal normalization and coercion behavior."""


class UnsupportedFeatureError(ValueError):
    """Raised when a feature is explicitly deferred or rejected in Phase 1."""


def normalize_name(name: str | None) -> str | None:
    """Normalize optional names by trimming whitespace and rejecting empties."""

    if name is None:
        return None
    stripped = name.strip()
    if not stripped:
        raise ValueError("names must not be empty or whitespace-only")
    return stripped


def normalize_vec3(value: Sequence[object], *, field_name: str) -> Vec3:
    """Coerce a 3-vector to a finite float tuple."""

    if len(value) != 3:
        raise ValueError(f"{field_name} must contain exactly three components")
    normalized = (
        float(cast(SupportsFloat, value[0])),
        float(cast(SupportsFloat, value[1])),
        float(cast(SupportsFloat, value[2])),
    )
    if any(not math.isfinite(component) for component in normalized):
        raise ValueError(f"{field_name} components must be finite")
    return normalized


def component_type_name(component: object) -> str | None:
    """Extract the tagged type field from a component-like payload."""

    if isinstance(component, BaseModel) and hasattr(component, "type"):
        value = component.type
        return str(value) if value is not None else None
    if isinstance(component, Mapping):
        value = component.get("type")
        return str(value) if value is not None else None
    return None


def normalize_component(component: object, *, context: str) -> object:
    """Normalize a tagged component payload and reject unsupported feature tags."""

    normalized = _normalize_name_field(component)
    _validate_supported_types(normalized, context=context)
    if context == "geometry":
        return _normalize_geometry(normalized)
    if context == "grid_spec":
        return _normalize_grid(normalized)
    if context == "subpixel":
        return _normalize_subpixel(normalized)
    if context == "boundary_spec":
        return _normalize_boundary_spec(normalized)
    if context in {"medium", "background_medium", "scene.medium"}:
        return _normalize_medium(normalized)
    return normalized


def validate_simulation_bounds(
    *,
    center: Vec3,
    size: Vec3,
    structures: Sequence[object],
) -> None:
    """Validate known structure bounds against the simulation domain."""

    sim_min = tuple(origin - extent / 2.0 for origin, extent in zip(center, size, strict=True))
    sim_max = tuple(origin + extent / 2.0 for origin, extent in zip(center, size, strict=True))

    for structure in structures:
        geometry = getattr(structure, "geometry", None)
        bounds = geometry_bounds(geometry)
        if bounds is None:
            continue
        geom_min, geom_max = bounds
        structure_name = getattr(structure, "name", None) or "<unnamed>"
        if any(
            geom_max[axis] < sim_min[axis] or geom_min[axis] > sim_max[axis] for axis in range(3)
        ):
            raise ValueError(
                f"structure {structure_name!r} lies fully outside the simulation bounds"
            )
        if any(low < allowed for low, allowed in zip(geom_min, sim_min, strict=True)) or any(
            high > allowed for high, allowed in zip(geom_max, sim_max, strict=True)
        ):
            warnings.warn(
                f"structure {structure_name!r} extends beyond the simulation bounds and will "
                "require clipping during scene compilation",
                AutoFDTDValidationWarning,
                stacklevel=3,
            )


def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
    """Coerce absorbing boundaries to periodic boundaries on zero-sized axes."""

    if isinstance(boundary_spec, BoundarySpec):
        updated = {}
        changed = False
        for axis_index, axis_name in enumerate("xyz"):
            axis_boundary = boundary_spec[axis_name]
            if size[axis_index] != 0.0:
                updated[axis_name] = axis_boundary
                continue
            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
            updated[axis_name] = coerced
            changed = changed or coerced is not axis_boundary
        return boundary_spec.copy_update(**updated) if changed else boundary_spec

    if not isinstance(boundary_spec, Mapping):
        return boundary_spec

    updated = dict(boundary_spec)
    changed = False

    for axis_index, axis_name in enumerate("xyz"):
        if size[axis_index] != 0.0:
            continue
        axis_boundary = updated.get(axis_name)
        if axis_boundary is None:
            continue
        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
        if coerced is not axis_boundary:
            updated[axis_name] = coerced
            changed = True

    return updated if changed else boundary_spec


def geometry_bounds(geometry: object) -> tuple[Vec3, Vec3] | None:
    """Return axis-aligned bounds for the known primitive geometries."""


exec
/bin/bash -lc 'rg -n "StablePML|Absorber|PML" src/autofdtd/core/validation.py src/autofdtd/api -S' in /workspace/autofdtd
 succeeded in 0ms:
src/autofdtd/core/validation.py:17:    PML,
src/autofdtd/core/validation.py:74:_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
src/autofdtd/core/validation.py:84:        "PML",
src/autofdtd/core/validation.py:85:        "PMLParams",
src/autofdtd/core/validation.py:362:    if context == "boundary_spec" and feature_name == "PMLParams":
src/autofdtd/core/validation.py:363:        return feature_entry("PML")
src/autofdtd/core/validation.py:608:    if isinstance(boundary, PML):
src/autofdtd/api/simulation.py:16:    PML,
src/autofdtd/api/simulation.py:17:    PMLParams,
src/autofdtd/api/simulation.py:78:    "PML",
src/autofdtd/api/simulation.py:79:    "PMLParams",
src/autofdtd/api/simulation.py:101:    "PML",
src/autofdtd/api/simulation.py:102:    "PMLParams",
src/autofdtd/api/__init__.py:34:    PML,
src/autofdtd/api/__init__.py:35:    PMLParams,
src/autofdtd/api/__init__.py:93:    "PML",
src/autofdtd/api/__init__.py:94:    "PMLParams",

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/api/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API namespace for Tidy3D-inspired object models."""

from autofdtd.api.simulation import (
    AbstractSubpixelPolicy,
    AnisotropicMedium,
    AutoFDTDModel,
    AutoFDTDValidationWarning,
    AutoGrid,
    Box,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    ClipOperation,
    CustomAnisotropicMedium,
    CustomMedium,
    CustomGrid,
    CustomGridBoundaries,
    Cylinder,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PECBoundary,
    PML,
    PMLParams,
    PMCMedium,
    PMCBoundary,
    PerturbationMedium,
    PerturbationPoleResidue,
    PolarizedAveraging,
    PoleResidue,
    PolySlab,
    PrimitiveGeometry,
    Periodic,
    ResolvedGrid,
    ResolvedGridAxis,
    Scene,
    Sellmeier,
    Simulation,
    Sphere,
    Staircasing,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
    SubpixelSpec,
    TaggedModel,
    Transformed,
    UniformGrid,
    UnsupportedFeatureError,
)

__all__ = [
    "AutoFDTDModel",
    "AutoFDTDValidationWarning",
    "AbstractSubpixelPolicy",
    "AnisotropicMedium",
    "AutoGrid",
    "Box",
    "BlochBoundary",
    "Boundary",
    "BoundarySpec",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",
    "Sellmeier",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "ResolvedGrid",
    "ResolvedGridAxis",
    "Scene",
    "Simulation",
    "Sphere",
    "Structure",
    "StructurePrecedence",
    "StructurePriorityMode",
    "Staircasing",
    "SubpixelSpec",
    "TaggedModel",
    "Transformed",
    "UniformGrid",
    "UnsupportedFeatureError",
]

exec
/bin/bash -lc "sed -n '1,140p' src/autofdtd/api/simulation.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API aliases for the foundational Phase 1 container models."""

from autofdtd.core.containers import (
    Scene,
    Simulation,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
)
from autofdtd.core.models import AutoFDTDModel, TaggedModel
from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
from autofdtd.boundaries import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PML,
    PMLParams,
    PECBoundary,
    PMCBoundary,
    Periodic,
)
from autofdtd.geometry import (
    Box,
    ClipOperation,
    Cylinder,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    PolySlab,
    PrimitiveGeometry,
    Sphere,
    Transformed,
)
from autofdtd.grid import (
    AbstractSubpixelPolicy,
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    PolarizedAveraging,
    ResolvedGrid,
    ResolvedGridAxis,
    Staircasing,
    SubpixelSpec,
    UniformGrid,
)
from autofdtd.materials import (
    AnisotropicMedium,
    CustomAnisotropicMedium,
    CustomMedium,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PMCMedium,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    Sellmeier,
)

__all__ = [
    "AutoFDTDValidationWarning",
    "AutoFDTDModel",
    "AbstractSubpixelPolicy",
    "AutoGrid",
    "AnisotropicMedium",
    "BlochBoundary",
    "Box",
    "Boundary",
    "BoundarySpec",
    "PML",
    "PMLParams",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",
    "Sellmeier",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "ResolvedGrid",
    "ResolvedGridAxis",
    "Scene",
    "Simulation",
    "Sphere",
    "Structure",
    "StructurePrecedence",
    "StructurePriorityMode",
    "Staircasing",
    "SubpixelSpec",
    "TaggedModel",
    "Transformed",
    "UniformGrid",
    "UnsupportedFeatureError",
]

exec
/bin/bash -lc "sed -n '940,1135p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
    if isinstance(medium, Sellmeier):
        return SellmeierIR(name=medium.name, coeffs=medium.coeffs)
    if isinstance(medium, Lorentz):
        return LorentzIR(name=medium.name, eps_inf=medium.eps_inf, coeffs=medium.coeffs)
    if isinstance(medium, Drude):
        return DrudeIR(name=medium.name, eps_inf=medium.eps_inf, coeffs=medium.coeffs)
    if isinstance(medium, Debye):
        return DebyeIR(name=medium.name, eps_inf=medium.eps_inf, coeffs=medium.coeffs)
    if isinstance(medium, AnisotropicMedium):
        return AnisotropicMediumIR(
            name=medium.name,
            xx=medium_to_ir(medium.xx),
            yy=medium_to_ir(medium.yy),
            zz=medium_to_ir(medium.zz),
        )
    return component_to_ir(value, family=ComponentFamily.MEDIUM)


def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
    """Lower a supported boundary edge into typed execution IR."""

    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
        edge = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
    }:
        edge = boundary_edge_model_from_value(value)

    if isinstance(edge, Periodic):
        return PeriodicIR(name=edge.name)
    if isinstance(edge, BlochBoundary):
        return BlochBoundaryIR(
            name=edge.name,
            bloch_vec=edge.bloch_vec,
            phase_real=float(edge.bloch_phase.real),
            phase_imag=float(edge.bloch_phase.imag),
        )
    if isinstance(edge, PECBoundary):
        return PECBoundaryIR(name=edge.name)
    if isinstance(edge, PMCBoundary):
        return PMCBoundaryIR(name=edge.name)
    if isinstance(edge, PML):
        params = (
            edge.parameters
            if isinstance(edge.parameters, PMLParams)
            else PMLParams.model_validate(edge.parameters)
        )
        return PMLIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=PMLParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
                kappa_order=params.kappa_order,
                kappa_min=params.kappa_min,
                kappa_max=params.kappa_max,
                alpha_order=params.alpha_order,
                alpha_min=params.alpha_min,
                alpha_max=params.alpha_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    return component_to_ir(value, family=ComponentFamily.BOUNDARY)


def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
    """Lower a supported axis boundary pair into typed execution IR."""

    boundary: Boundary | None = None
    if isinstance(value, Boundary):
        boundary = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Boundary",
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
    }:
        boundary = boundary_model_from_value(value)
    if boundary is None:
        return component_to_ir(value, family=ComponentFamily.BOUNDARY)
    return BoundaryIR(
        plus=boundary_edge_to_ir(boundary.plus),
        minus=boundary_edge_to_ir(boundary.minus),
    )


def _reflection_signs_for_axis(
    symmetry: int,
    axis_name: Literal["x", "y", "z"],
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    axis_index = "xyz".index(axis_name)
    spatial_reflection = [1, 1, 1]
    pseudo_reflection = [-1, -1, -1]
    spatial_reflection[axis_index] = -1
    pseudo_reflection[axis_index] = 1
    electric = tuple(sign * symmetry for sign in spatial_reflection)
    magnetic = tuple(sign * symmetry for sign in pseudo_reflection)
    return electric, magnetic


def _symmetry_axes_to_ir(symmetry: tuple[int, int, int]) -> tuple[SymmetryAxisIR, ...]:
    axes: list[SymmetryAxisIR] = []
    for axis_name, axis_value in zip("xyz", symmetry, strict=True):
        if axis_value == 0:
            continue
        electric_signs, magnetic_signs = _reflection_signs_for_axis(axis_value, axis_name)
        axes.append(
            SymmetryAxisIR(
                axis=axis_name,
                symmetry=axis_value,
                electric_signs=electric_signs,
                magnetic_signs=magnetic_signs,
            )
        )
    return tuple(axes)


def boundary_spec_to_ir(
    value: object,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
) -> BoundarySpecIR | ComponentIR:
    """Lower a supported BoundarySpec container into typed execution IR."""

    boundary_spec: BoundarySpec | None = None
    if isinstance(value, BoundarySpec):
        boundary_spec = value
    elif isinstance(value, Mapping) and str(value.get("type")) == "BoundarySpec":
        try:
            boundary_spec = boundary_spec_model_from_value(value)
        except Exception:  # noqa: BLE001
            boundary_spec = None
    if boundary_spec is None:
        return component_to_ir(value, family=ComponentFamily.BOUNDARY)

    periodic_axes: list[str] = []
    bloch_axes: list[str] = []
    reflective_axes: list[str] = []
    pml_axes: list[str] = []
    pml_faces: list[str] = []
    requires_complex_fields = False
    axes = {}
    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
        lowered = boundary_to_ir(axis_boundary)
        if not isinstance(lowered, BoundaryIR):
            return component_to_ir(value, family=ComponentFamily.BOUNDARY)
        axes[axis_name] = lowered
        edge_types = {lowered.minus.component_type, lowered.plus.component_type}
        if edge_types == {"Periodic"}:
            periodic_axes.append(axis_name)
        elif edge_types == {"BlochBoundary"}:
            bloch_axes.append(axis_name)
            requires_complex_fields = True
        elif edge_types == {"PML"}:
            pml_axes.append(axis_name)
            if lowered.minus.component_type == "PML":
                pml_faces.append(f"{axis_name}.minus")
            if lowered.plus.component_type == "PML":
                pml_faces.append(f"{axis_name}.plus")
            reflective_axes.append(axis_name)
        else:
            if lowered.minus.component_type == "PML":
                pml_faces.append(f"{axis_name}.minus")
            if lowered.plus.component_type == "PML":
                pml_faces.append(f"{axis_name}.plus")
            if (
                lowered.minus.component_type == "PML"
                or lowered.plus.component_type == "PML"
            ) and axis_name not in pml_axes:
                pml_axes.append(axis_name)
            reflective_axes.append(axis_name)
    symmetry_axes = _symmetry_axes_to_ir(symmetry)
    return BoundarySpecIR(
        x=axes["x"],
        y=axes["y"],
        z=axes["z"],
        requires_complex_fields=requires_complex_fields,
        periodic_axes=tuple(periodic_axes),
        bloch_axes=tuple(bloch_axes),
        reflective_axes=tuple(reflective_axes),
        pml_axes=tuple(pml_axes),
        pml_faces=tuple(pml_faces),
        active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
        symmetry_axes=symmetry_axes,
    )


def _resolved_grid_axis_to_ir(value: ResolvedGridAxis) -> ResolvedGridAxisIR:

exec
/bin/bash -lc "sed -n '560,660p' src/autofdtd/core/validation.py" in /workspace/autofdtd
 succeeded in 0ms:
    if not math.isfinite(normalized) or normalized <= 0.0:
        raise ValueError(f"{field_name} must be a positive finite value")
    return normalized


def _non_negative_float(value: object, *, field_name: str) -> float:
    normalized = float(cast(SupportsFloat, value))
    if not math.isfinite(normalized) or normalized < 0.0:
        raise ValueError(f"{field_name} must be a non-negative finite value")
    return normalized


def _phase1_zero_only(value: object, *, field_name: str) -> float:
    normalized = float(cast(SupportsFloat, value))
    if not math.isfinite(normalized):
        raise ValueError(f"{field_name} must be finite")
    if abs(normalized) > 1e-12:
        raise ValueError(
            f"{field_name}={normalized} is not supported in Phase 1; "
            "only constant-cross-section PolySlab extrusions are implemented"
        )
    return 0.0


def _normalize_slab_bounds(value: Sequence[object]) -> tuple[float, float]:
    if len(value) != 2:
        raise ValueError("geometry.slab_bounds must contain exactly two coordinates")
    lower = float(cast(SupportsFloat, value[0]))
    upper = float(cast(SupportsFloat, value[1]))
    if not math.isfinite(lower) or not math.isfinite(upper):
        raise ValueError("geometry.slab_bounds must be finite")
    if upper <= lower:
        raise ValueError("geometry.slab_bounds must be strictly increasing")
    return (lower, upper)


def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
    if isinstance(boundary, Boundary):
        updated = {}
        changed = False
        for side in ("minus", "plus"):
            edge = boundary[side]
            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
            updated[side] = coerced
            changed = changed or coerced is not edge
        return boundary.copy_update(**updated) if changed else boundary
    if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
        return boundary
    if isinstance(boundary, PML):
        warnings.warn(
            f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
            AutoFDTDValidationWarning,
            stacklevel=3,
        )
        return Periodic()
    if not isinstance(boundary, Mapping):
        return boundary

    boundary_type = component_type_name(boundary)
    if boundary_type == "BlochBoundary":
        raise ValueError(f"zero-sized axis {axis_name!r} cannot use BlochBoundary")
    if boundary_type in _ABSORBING_BOUNDARY_TYPES:
        warnings.warn(
            f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
            AutoFDTDValidationWarning,
            stacklevel=3,
        )
        return {"type": "Periodic"}
    if boundary_type != "Boundary":
        return boundary

    updated = dict(boundary)
    changed = False
    for side in ("minus", "plus"):
        edge = updated.get(side)
        if isinstance(edge, Mapping) and component_type_name(edge) in _ABSORBING_BOUNDARY_TYPES:
            warnings.warn(
                f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing "
                f"{axis_name}.{side} to Periodic",
                AutoFDTDValidationWarning,
                stacklevel=3,
            )
            updated[side] = {"type": "Periodic"}
            changed = True
    return updated if changed else boundary

exec
/bin/bash -lc 'cat phase1/output.schema.json' in /workspace/autofdtd
 succeeded in 0ms:
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "task_id",
    "status",
    "summary",
    "key_findings",
    "artifacts",
    "error_summary",
    "follow_up_notes",
    "next_action"
  ],
  "properties": {
    "task_id": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "completed",
        "blocked",
        "failed",
        "needs_retry"
      ]
    },
    "summary": {
      "type": "string"
    },
    "key_findings": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "path",
          "description"
        ],
        "properties": {
          "path": {
            "type": "string"
          },
          "description": {
            "type": "string"
          }
        }
      }
    },
    "error_summary": {
      "type": "string"
    },
    "follow_up_notes": {
      "type": "string"
    },
    "next_action": {
      "type": "string",
      "enum": [
        "none",
        "retry",
        "human_review"
      ]
    }
  }
}

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/boundaries/models.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/boundaries/__init__.py
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/api/simulation.py
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/api/__init__.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/core/validation.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/boundaries.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/kernels/boundaries.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..e60323d40503ed1d7993dd3e225560ed33e74989
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,8 +158,10 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
+    if edge.mode is BoundaryMode.ABSORBER:
+        return np.array(values, copy=True) * np.asarray(edge.electric_signs, dtype=values.dtype)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
 
@@ -236,7 +238,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +296,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..e60323d40503ed1d7993dd3e225560ed33e74989
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,8 +158,10 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
+    if edge.mode is BoundaryMode.ABSORBER:
+        return np.array(values, copy=True) * np.asarray(edge.electric_signs, dtype=values.dtype)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
 
@@ -236,7 +238,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +296,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/kernels/boundaries.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/models.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

exec
/bin/bash -lc "sed -n '1,240p' src/autofdtd/ir/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Tagged execution-IR namespace."""

from autofdtd.ir.models import (
    EXECUTION_PACKAGE_FORMAT,
    IR_SCHEMA_VERSION,
    AnisotropicMediumIR,
    ArtifactIR,
    AutoGridIR,
    BlochBoundaryIR,
    BoxIR,
    BoundaryIR,
    BoundarySpecIR,
    ClipOperationIR,
    ComponentFamily,
    ComponentIR,
    CustomGridBoundariesIR,
    CustomGridIR,
    CylinderIR,
    DebyeIR,
    DrudeIR,
    ExecutionManifestIR,
    ExecutionPackageIR,
    GeometryGroupIR,
    GeometryIR,
    GeometryTransformIR,
    GridRefinementIR,
    GridSpecIR,
    LayerRefinementSpecIR,
    LorentzIR,
    MediumComponentIR,
    MediumIR,
    PECBoundaryIR,
    PECMediumIR,
    PMLIR,
    PMLParamsIR,
    PMCBoundaryIR,
    PMCMediumIR,
    PeriodicIR,
    PolarizedAveragingIR,
    PoleResidueIR,
    PolySlabIR,
    ResolvedGridAxisIR,
    ResolvedGridIR,
    SceneIR,
    SellmeierIR,
    SimulationIR,
    SphereIR,
    StaircasingIR,
    StructureIR,
    SubpixelPolicyIR,
    SubpixelSpecIR,
    SymmetryAxisIR,
    TransformedIR,
    UniformGridIR,
    boundary_edge_to_ir,
    boundary_spec_to_ir,
    boundary_to_ir,
    component_to_ir,
    execution_package_json_schema,
    geometry_to_ir,
    geometry_transform_to_ir,
    grid_spec_to_ir,
    medium_to_ir,
    scene_to_ir,
    simulation_to_ir,
    structure_to_ir,
    write_execution_package_schema,
)

__all__ = [
    "EXECUTION_PACKAGE_FORMAT",
    "IR_SCHEMA_VERSION",
    "AnisotropicMediumIR",
    "AutoGridIR",
    "ArtifactIR",
    "BlochBoundaryIR",
    "BoxIR",
    "BoundaryIR",
    "BoundarySpecIR",
    "ClipOperationIR",
    "ComponentFamily",
    "ComponentIR",
    "CylinderIR",
    "DebyeIR",
    "DrudeIR",
    "ExecutionManifestIR",
    "ExecutionPackageIR",
    "GridSpecIR",
    "GeometryGroupIR",
    "GeometryIR",
    "GeometryTransformIR",
    "GridRefinementIR",
    "CustomGridBoundariesIR",
    "CustomGridIR",
    "PolySlabIR",
    "PolarizedAveragingIR",
    "LayerRefinementSpecIR",
    "LorentzIR",
    "MediumComponentIR",
    "MediumIR",
    "PECBoundaryIR",
    "PECMediumIR",
    "PMLIR",
    "PMLParamsIR",
    "PMCBoundaryIR",
    "PMCMediumIR",
    "PeriodicIR",
    "PoleResidueIR",
    "ResolvedGridAxisIR",
    "ResolvedGridIR",
    "SceneIR",
    "SellmeierIR",
    "SimulationIR",
    "SphereIR",
    "StructureIR",
    "StaircasingIR",
    "SubpixelPolicyIR",
    "SubpixelSpecIR",
    "SymmetryAxisIR",
    "TransformedIR",
    "boundary_edge_to_ir",
    "boundary_spec_to_ir",
    "boundary_to_ir",
    "component_to_ir",
    "execution_package_json_schema",
    "geometry_to_ir",
    "geometry_transform_to_ir",
    "grid_spec_to_ir",
    "medium_to_ir",
    "scene_to_ir",
    "simulation_to_ir",
    "structure_to_ir",
    "UniformGridIR",
    "write_execution_package_schema",
]

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/__init__.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

2026-04-06T19:48:51.169760Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /workspace/autofdtd/tests/test_boundaries.py:
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
    boundary = Boundary.bloch(0.125)
    assert isinstance(boundary.plus, BlochBoundary)
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

exec
/bin/bash -lc "sed -n '1,320p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
from __future__ import annotations

import numpy as np
import pytest

from autofdtd.api import (
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    Simulation,
)
from autofdtd.compiler import BoundaryMode, compile_boundary_spec
from autofdtd.core.validation import AutoFDTDValidationWarning
from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
from autofdtd.kernels import (
    apply_boundary_ghosts,
    apply_boundary_stages,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime


def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
    with pytest.warns(
        AutoFDTDValidationWarning,
        match="periodic boundary opposite a PEC/PMC boundary",
    ):
        boundary = Boundary(minus=PECBoundary(), plus=Periodic())

    assert boundary.minus.type == "PECBoundary"
    assert boundary.plus.type == "PECBoundary"


def test_simulation_normalizes_supported_boundary_subset() -> None:
    simulation = Simulation(
        size=(1.0, 1.0, 1.0),
        run_time=1.0,
        boundary_spec={
            "type": "BoundarySpec",
            "x": {"type": "Periodic"},
            "y": {
                "type": "Boundary",
                "minus": {"type": "PECBoundary"},
                "plus": {"type": "Periodic"},
            },
            "z": {"type": "PMCBoundary"},
        },
    )

    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"


def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
        symmetry=(1, 0, -1),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.25),
    )

    assert compiled.x.exchange_kind == "bloch"
    assert compiled.x.minus.mode is BoundaryMode.BLOCH
    assert compiled.x.plus.phase_factor == pytest.approx(1j)
    assert compiled.x.minus.phase_factor == pytest.approx(-1j)
    assert compiled.y.minus.mode is BoundaryMode.PEC
    assert compiled.z.plus.mode is BoundaryMode.PML
    assert compiled.bloch_axes == ("x",)
    assert compiled.reflective_axes == ("y",)
    assert compiled.pml_axes == ("z",)
    assert compiled.pml_faces == ("z.minus", "z.plus")
    assert compiled.z.plus.pml is not None
    assert compiled.z.plus.pml.num_layers == 4
    assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
    assert compiled.stage_order == (
        "electric_boundary",
        "magnetic_boundary",
        "electric_pml",
        "magnetic_pml",
    )
    assert compiled.requires_complex_fields is True
    assert compiled.active_symmetry_axes == ("x", "z")
    assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
    assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)

    with pytest.raises(
        ValueError,
        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
    ):
        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})


def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    )
    electric = np.zeros((4, 4, 4, 3), dtype=np.complex128)
    magnetic = np.zeros((4, 4, 4, 3), dtype=np.complex128)

    electric[1, 1, 1] = (1.0, 2.0, 3.0)
    electric[-2, 1, 1] = (4.0, 5.0, 6.0)
    electric[2, 1, 2] = (7.0, 8.0, 9.0)
    electric[1, -2, 2] = (11.0, 12.0, 13.0)
    electric[2, 2, 1] = (14.0, 15.0, 16.0)
    electric[1, 2, -2] = (17.0, 18.0, 19.0)

    magnetic[1, 1, 1] = (21.0, 22.0, 23.0)
    magnetic[-2, 1, 1] = (24.0, 25.0, 26.0)
    magnetic[2, 1, 2] = (27.0, 28.0, 29.0)
    magnetic[2, -2, 2] = (31.0, 32.0, 33.0)
    magnetic[2, 2, 1] = (34.0, 35.0, 36.0)
    magnetic[2, 2, -2] = (37.0, 38.0, 39.0)

    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)

    assert tuple(updated_electric[0, 1, 1]) == pytest.approx((-4.0j, -5.0j, -6.0j))
    assert tuple(updated_electric[-1, 1, 1]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(updated_magnetic[0, 1, 1]) == pytest.approx((-24.0j, -25.0j, -26.0j))
    assert tuple(updated_electric[2, 0, 2]) == pytest.approx((-7.0, 8.0, -9.0))
    assert tuple(updated_magnetic[2, 0, 2]) == pytest.approx((27.0, -28.0, 29.0))
    assert tuple(updated_electric[2, 2, 0]) == pytest.approx((14.0, 15.0, -16.0))
    assert tuple(updated_magnetic[2, 2, -1]) == pytest.approx((-37.0, -38.0, 39.0))


def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
    boundary_spec = BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    runtime = build_boundary_runtime(boundary_spec, symmetry=(1, 0, -1))
    metadata = boundary_kernel_metadata()
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=boundary_spec,
            symmetry=(1, 0, -1),
        )
    )

    assert runtime.compiled.bloch_axes == ("x",)
    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
    assert simulation_ir.boundary_spec.requires_complex_fields is True
    assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")


def test_pml_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.pml(
                    num_layers=8,
                    parameters=PMLParams(
                        sigma_order=2,
                        sigma_max=2.0,
                        kappa_max=4.0,
                        alpha_max=0.1,
                    ),
                )
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
    assert simulation_ir.boundary_spec.x.minus.num_layers == 8
    assert simulation_ir.boundary_spec.x.minus.parameters.kappa_max == pytest.approx(4.0)
    assert simulation_ir.boundary_spec.pml_axes == ("x",)
    assert simulation_ir.boundary_spec.pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.reflective_axes == ("x",)


def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
    runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
    vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)

    boundary_values = runtime.transform_boundary_values(
        vector,
        runtime.compiled.x.plus,
        field_family="electric",
    )
    mirrored = runtime.transform_symmetry_values(
        vector,
        runtime.compiled.symmetry_axes[0],
        field_family="magnetic",
    )

    assert tuple(boundary_values[0]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(mirrored[0]) == pytest.approx((1.0, -2.0, -3.0))
    assert tuple(
        boundary_edge_transform(vector, runtime.compiled.x.minus, field_family="electric")[0]
    ) == pytest.approx((-1.0j, -2.0j, -3.0j))
    assert tuple(
        symmetry_transform(
            vector,
            runtime.compiled.symmetry_axes[0],
            field_family="electric",
        )[0]
    ) == pytest.approx((-1.0, 2.0, 3.0))


def test_pml_state_allocation_tracks_active_faces() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.pml(num_layers=3),
            y=Boundary.periodic(),
            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
        )
    )
    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)

    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
    assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
    assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
    assert np.all(state.magnetic["x.plus"].memory == 0.0)


def test_pml_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 4, 4, 3), dtype=np.float64)
    electric[1:5, 1:3, 1:3, 0] = 1.0
    magnetic[-5:-1, 1:3, 1:3, 1] = 1.0

    before_left = float(np.sum(electric[1:5, ...] ** 2))
    before_center = float(np.sum(electric[5:7, ...] ** 2))
    for _ in range(12):
        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
    after_left = float(np.sum(electric[1:5, ...] ** 2))
    after_right_h = float(np.sum(magnetic[-5:-1, ...] ** 2))

    assert after_left < before_left * 0.2
    assert after_right_h < 0.2 * 16.0
    assert before_center == pytest.approx(0.0)
    assert float(np.sum(electric[5:7, ...] ** 2)) == pytest.approx(0.0)
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0


def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
    boundary = Boundary.bloch(0.125)
    assert isinstance(boundary.plus, BlochBoundary)
    assert boundary.plus.bloch_phase == pytest.approx(
        np.exp(1j * 2.0 * np.pi * 0.125),
    )

    with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
        Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/examples/boundaries.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/docs/boundaries.md
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..29fdbe73ac0a54a8e6299c2004bd5310bb11dcdf
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,11 +256,53 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
         dt=dt,
         grid_spacing=grid_spacing,
     )
 
 
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
 def _compile_edge(
     axis_name: Literal["x", "y", "z"],
     side: Literal["minus", "plus"],
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -365,6 +438,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +475,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/runtime/boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

exec
/bin/bash -lc "rg -n \"def copy_update\" -n src/autofdtd/core/models.py && sed -n '1,220p' src/autofdtd/core/models.py" in /workspace/autofdtd
 succeeded in 0ms:
38:    def copy_update(self, **updates: Any) -> Self:
"""Shared frozen model, tagging, and serialization helpers."""

from __future__ import annotations

from pathlib import Path
from typing import Any, Self

from pydantic import BaseModel, ConfigDict, Field, field_validator


def json_ready(value: Any) -> Any:
    """Normalize arbitrary model payloads into JSON-safe Python values."""
    if value is None or isinstance(value, (str, int, float, bool)):
        return value
    if isinstance(value, Path):
        return value.as_posix()
    if isinstance(value, BaseModel):
        return json_ready(value.model_dump(mode="json", exclude_none=True))
    if isinstance(value, dict):
        return {str(key): json_ready(item) for key, item in value.items()}
    if isinstance(value, (list, tuple)):
        return [json_ready(item) for item in value]
    raise TypeError(f"unsupported JSON payload value: {type(value)!r}")


class AutoFDTDModel(BaseModel):
    """Frozen base model with copy/update and stable serialization helpers."""

    model_config = ConfigDict(extra="forbid", frozen=True)

    def model_tag(self) -> str:
        """Return the explicit type tag when present, else the model class name."""
        tagged_type = getattr(self, "type", None)
        if isinstance(tagged_type, str) and tagged_type.strip():
            return tagged_type.strip()
        return self.__class__.__name__

    def copy_update(self, **updates: Any) -> Self:
        """Return a frozen copy with a shallow field update."""
        return self.model_copy(update=updates)

    def to_payload(self) -> dict[str, Any]:
        """Return a JSON-ready payload with stable field names."""
        return self.model_dump(mode="json", exclude_none=True)

    def to_json_text(self, *, indent: int = 2) -> str:
        """Return a stable JSON string for snapshots and debugging."""
        return self.model_dump_json(indent=indent, exclude_none=True)

    def write_json(self, path: str | Path, *, indent: int = 2) -> None:
        """Write the model payload to disk."""
        Path(path).write_text(self.to_json_text(indent=indent) + "\n", encoding="utf-8")


class TaggedModel(AutoFDTDModel):
    """Shared base for immutable tagged public and IR-facing models."""

    type: str = Field(default="")

    @field_validator("type")
    @classmethod
    def _validate_type(cls, value: str) -> str:
        stripped = value.strip()
        if not stripped:
            raise ValueError("type tags must be non-empty")
        return stripped

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

 exited 1 in 463ms:
..F...F..............................................                    [100%]
=================================== FAILURES ===================================
____ test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants ____

    def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
        compiled = compile_boundary_spec(
            BoundarySpec(
                x=Boundary.bloch(0.25),
                y=Boundary.stable_pml(num_layers=8),
                z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
            ),
            symmetry=(1, 0, -1),
            dt=0.5,
            grid_spacing=(0.25, 0.5, 0.25),
        )
    
        assert compiled.x.exchange_kind == "bloch"
        assert compiled.x.minus.mode is BoundaryMode.BLOCH
        assert compiled.x.plus.phase_factor == pytest.approx(1j)
        assert compiled.x.minus.phase_factor == pytest.approx(-1j)
        assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
        assert compiled.z.minus.mode is BoundaryMode.ABSORBER
        assert compiled.z.plus.mode is BoundaryMode.PML
        assert compiled.bloch_axes == ("x",)
>       assert compiled.reflective_axes == ("y", "z")
E       AssertionError: assert () == ('y', 'z')
E         
E         Right contains 2 more items, first extra item: 'y'
E         Use -v to get more diff

tests/test_boundaries.py:88: AssertionError
______ test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed _______

    def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
        simulation_ir = simulation_to_ir(
>           Simulation(
                size=(1.0, 1.0, 1.0),
                run_time=1.0,
                boundary_spec=BoundarySpec(
                    x=Boundary.stable_pml(
                        num_layers=10,
                        parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
                    ),
                    z=Boundary.absorber(
                        num_layers=12,
                        parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
                    ),
                ),
            )
        )
E       pydantic_core._pydantic_core.ValidationError: 1 validation error for Simulation
E       boundary_spec
E         Value error, boundary_spec uses unknown feature type 'AbsorberParams'; add it to the Phase 1 feature matrix before accepting it in the public API [type=value_error, input_value=BoundarySpec(type='Bounda...rude_structures=False))), input_type=BoundarySpec]
E           For further information visit https://errors.pydantic.dev/2.12/v/value_error

tests/test_boundaries.py:204: ValidationError
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
FAILED tests/test_boundaries.py::test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed
2 failed, 51 passed, 7 warnings in 0.31s

exec
/bin/bash -lc "sed -n '330,390p' src/autofdtd/core/validation.py" in /workspace/autofdtd
 succeeded in 0ms:
        if name != component.name:
            return component.model_copy(update={"name": name})
        return component
    if isinstance(component, Mapping) and "name" in component:
        normalized_name = normalize_name(component.get("name"))
        if normalized_name != component.get("name"):
            updated = dict(component)
            updated["name"] = normalized_name
            return updated
    return component


def _validate_supported_types(component: object, *, context: str) -> None:
    for feature_name in _iter_type_tags(component):
        if context == "geometry" and feature_name in {"ClipOperation", "GeometryTransform"}:
            continue
        entry = _feature_entry_for_type(feature_name, context=context)
        if entry is None:
            raise UnsupportedFeatureError(
                f"{context} uses unknown feature type {feature_name!r}; add it to the Phase 1 "
                "feature matrix before accepting it in the public API"
            )
        if entry.status is FeatureStatus.IMPLEMENT:
            continue
        status_text = {
            FeatureStatus.DEFER: "deferred",
            FeatureStatus.REJECT_CLEARLY: "rejected clearly",
        }[entry.status]
        raise UnsupportedFeatureError(
            f"{context} uses {status_text} feature {feature_name!r}. {entry.notes}"
        )


def _feature_entry_for_type(feature_name: str, *, context: str):
    entry = feature_entry(feature_name)
    if entry is not None:
        return entry
    if context == "boundary_spec" and feature_name == "PMLParams":
        return feature_entry("PML")
    if context in {"medium", "background_medium", "scene.medium"}:
        if feature_name.startswith("Custom"):
            return feature_entry("CustomMedia")
    return None


def _iter_type_tags(value: object) -> Iterable[str]:
    if isinstance(value, BaseModel):
        yield from _iter_type_tags(value.model_dump(mode="python", exclude_none=True))
        return
    if isinstance(value, Mapping):
        tagged_type = value.get("type")
        if tagged_type is not None:
            yield str(tagged_type)
        for nested in value.values():
            yield from _iter_type_tags(nested)
        return
    if isinstance(value, Sequence) and not isinstance(value, (str, bytes, bytearray)):
        for item in value:
            yield from _iter_type_tags(item)



diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..f3ba34005d20f27f8c8d70133b85727d87cf0e73
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -605,7 +610,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/core/validation.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..1154ab337857faaf11f73f6205e2108442b4a34d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

 succeeded in 406ms:
.....................................................                    [100%]
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
53 passed, 7 warnings in 0.26s

API, IR, compiler, and runtime support for `StablePML` and `Absorber` are in place. I’m finishing verification and writing the handoff notes and result artifact.
  ✓ Map current boundary implementation and reference defaults for StablePML/Absorber
  ✓ Extend public API, IR, and compiler metadata for StablePML and Absorber
  ✓ Integrate kernel/runtime state handling for the new absorbing profiles
  → Add tests, examples, docs, and working-memory updates; run focused verification
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

exec
/bin/bash -lc 'PYTHONPATH=src python -m compileall src/autofdtd' in /workspace/autofdtd
 succeeded in 0ms:
Listing 'src/autofdtd'...
Listing 'src/autofdtd/api'...
Listing 'src/autofdtd/boundaries'...
Listing 'src/autofdtd/compiler'...
Listing 'src/autofdtd/core'...
Listing 'src/autofdtd/diagnostics'...
Listing 'src/autofdtd/examples'...
Compiling 'src/autofdtd/examples/boundaries.py'...
Listing 'src/autofdtd/geometry'...
Listing 'src/autofdtd/grid'...
Listing 'src/autofdtd/ir'...
Listing 'src/autofdtd/kernels'...
Listing 'src/autofdtd/materials'...
Listing 'src/autofdtd/modes'...
Listing 'src/autofdtd/monitors'...
Listing 'src/autofdtd/runtime'...
Listing 'src/autofdtd/sources'...

exec
/bin/bash -lc 'git status --short' in /workspace/autofdtd
 succeeded in 0ms:
 M README.md
 M docs/architecture.md
 M docs/index.md
 M mkdocs.yml
 M phase1/feature-checklist.md
 M phase1/logs/console.log
 M phase1/logs/events.jsonl
 M phase1/loop.spec.md
 M phase1/working-memory.md
 M src/autofdtd/__init__.py
 M src/autofdtd/api/__init__.py
 M src/autofdtd/api/simulation.py
 M src/autofdtd/boundaries/__init__.py
 M src/autofdtd/compiler/__init__.py
 M src/autofdtd/compiler/materials.py
 M src/autofdtd/core/validation.py
 M src/autofdtd/examples/__init__.py
 M src/autofdtd/examples/materials.py
 M src/autofdtd/ir/__init__.py
 M src/autofdtd/ir/models.py
 M src/autofdtd/kernels/__init__.py
 M src/autofdtd/kernels/materials.py
 M src/autofdtd/materials/__init__.py
 M src/autofdtd/planning.py
 M src/autofdtd/runtime/__init__.py
 M tests/test_containers.py
 M tests/test_ir.py
 M tests/test_materials.py
?? docs/boundaries.md
?? phase1/runs/task-017-attempt-019/
?? phase1/runs/task-018-attempt-001/
?? phase1/runs/task-019-attempt-001/
?? phase1/runs/task-020-attempt-001/
?? phase1/runs/task-021-attempt-001/
?? phase1/runs/task-022-attempt-001/
?? src/autofdtd/boundaries/models.py
?? src/autofdtd/compiler/boundaries.py
?? src/autofdtd/examples/boundaries.py
?? src/autofdtd/kernels/boundaries.py
?? src/autofdtd/materials/advanced.py
?? src/autofdtd/materials/anisotropic.py
?? src/autofdtd/runtime/boundaries.py
?? tests/test_boundaries.py

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

 succeeded in 354ms:
......................................................                   [100%]
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
54 passed, 7 warnings in 0.22s

exec
/bin/bash -lc 'ls -la phase1/runs/task-022-attempt-001' in /workspace/autofdtd
 succeeded in 0ms:
total 4132
drwxrwxr-x  2 root root      60 Apr  6 19:43 .
drwxrwxr-x 42 root root    4096 Apr  6 19:43 ..
-rw-rw-r--  1 root root 2770506 Apr  6 19:51 codex.stdout.txt
-rw-rw-r--  1 root root   32561 Apr  6 19:43 prompt.txt

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/phase1/working-memory.md
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

apply patch
patch: completed
/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/runs/task-022-attempt-001/result.json b/phase1/runs/task-022-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..d92f2910e949f3710621dc1a4c4e06d978e79aaa
--- /dev/null
+++ b/phase1/runs/task-022-attempt-001/result.json
@@ -0,0 +1,49 @@
+{
+  "task_id": "task-022",
+  "status": "completed",
+  "summary": "Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.",
+  "key_findings": [
+    "`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.",
+    "Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.",
+    "Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.",
+    "The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.",
+    "Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Extended staged boundary kernels to handle StablePML and Absorber runtime modes."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Updated runtime state allocation so all absorbing families share the staged boundary execution path."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."
+    },
+    {
+      "path": "/workspace/autofdtd/phase1/working-memory.md",
+      "description": "Recorded the StablePML/Absorber implementation decisions and extension seams for later ABC work."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/runs/task-022-attempt-001/result.json b/phase1/runs/task-022-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..d92f2910e949f3710621dc1a4c4e06d978e79aaa
--- /dev/null
+++ b/phase1/runs/task-022-attempt-001/result.json
@@ -0,0 +1,49 @@
+{
+  "task_id": "task-022",
+  "status": "completed",
+  "summary": "Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.",
+  "key_findings": [
+    "`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.",
+    "Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.",
+    "Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.",
+    "The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.",
+    "Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Extended staged boundary kernels to handle StablePML and Absorber runtime modes."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Updated runtime state allocation so all absorbing families share the staged boundary execution path."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."
+    },
+    {
+      "path": "/workspace/autofdtd/phase1/working-memory.md",
+      "description": "Recorded the StablePML/Absorber implementation decisions and extension seams for later ABC work."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

  ✓ Map current boundary implementation and reference defaults for StablePML/Absorber
  ✓ Extend public API, IR, and compiler metadata for StablePML and Absorber
  ✓ Integrate kernel/runtime state handling for the new absorbing profiles
  ✓ Add tests, examples, docs, and working-memory updates; run focused verification
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/runs/task-022-attempt-001/result.json b/phase1/runs/task-022-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..d92f2910e949f3710621dc1a4c4e06d978e79aaa
--- /dev/null
+++ b/phase1/runs/task-022-attempt-001/result.json
@@ -0,0 +1,49 @@
+{
+  "task_id": "task-022",
+  "status": "completed",
+  "summary": "Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.",
+  "key_findings": [
+    "`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.",
+    "Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.",
+    "Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.",
+    "The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.",
+    "Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Extended staged boundary kernels to handle StablePML and Absorber runtime modes."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Updated runtime state allocation so all absorbing families share the staged boundary execution path."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."
+    },
+    {
+      "path": "/workspace/autofdtd/phase1/working-memory.md",
+      "description": "Recorded the StablePML/Absorber implementation decisions and extension seams for later ABC work."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

codex
{"task_id":"task-022","status":"completed","summary":"Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.","key_findings":["`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.","Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.","Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.","The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.","Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Extended staged boundary kernels to handle StablePML and Absorber runtime modes."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Updated runtime state allocation so all absorbing families share the staged boundary execution path."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."},{"path":"/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json","description":"Structured task result for this loop iteration."}],"error_summary":"","follow_up_notes":"The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.","next_action":"none"}
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 70ff9c0b0ce36fcafedd208cfd5a16ad29a5e0c0..43156dc89a37e90f03b4dacff0da3489f32e2a25
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -1,36 +1,53 @@
 # Boundary Scope
 
-Phase 1 now has an explicit non-absorbing boundary surface built around
-`Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
+Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
+absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
+`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
+  and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
+- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
+  default preset.
+- `Absorber` uses a simpler conductivity-only profile and defaults
+  `extrude_structures=False`, matching the local Tidy3D schema surface.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
+  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
-  halo depth, and stage ordering.
+  halo depth, stage ordering, and per-face absorbing profile metadata.
 - `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
   vector fields with one ghost cell on each side.
 - Periodic boundaries copy ghost cells from the opposite interior face.
+- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `PML` and `StablePML` share the staged damping-layer backend and differ through
+  compiled coefficient presets.
+- `Absorber` uses the same staged region-update seam but with conductivity-only
+  attenuation coefficients and a reflective outer wall.
 
 ## Current Limits
 
-- Runtime compilation currently supports only the non-absorbing subset:
-  `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`.
-- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
-  outside the runtime compiler path for this task and fail explicitly there.
+- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
+  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+  `BoundarySpec`.
+- `StablePML` is currently implemented as a preset-driven extension of the baseline
+  staged PML backend rather than a separate split-field formulation.
+- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
+  reflective outer wall, not a full material-aware adiabatic absorber.
+- ABC-family boundaries remain outside the runtime compiler path and should fail
+  explicitly until `task-023`.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/runs/task-022-attempt-001/result.json b/phase1/runs/task-022-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..d92f2910e949f3710621dc1a4c4e06d978e79aaa
--- /dev/null
+++ b/phase1/runs/task-022-attempt-001/result.json
@@ -0,0 +1,49 @@
+{
+  "task_id": "task-022",
+  "status": "completed",
+  "summary": "Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.",
+  "key_findings": [
+    "`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.",
+    "Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.",
+    "Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.",
+    "The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.",
+    "Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Extended staged boundary kernels to handle StablePML and Absorber runtime modes."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Updated runtime state allocation so all absorbing families share the staged boundary execution path."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."
+    },
+    {
+      "path": "/workspace/autofdtd/phase1/working-memory.md",
+      "description": "Recorded the StablePML/Absorber implementation decisions and extension seams for later ABC work."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 19f8b9900968807e6bc2ca6b4ed7caac269ae391..871953459ad0ece1a2d2c81c837beea61abd32f6
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -57,6 +57,9 @@
 - `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
 - `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
 - `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
+- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
+- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
+- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
 
 ## Decisions
 
@@ -89,6 +92,8 @@
 - Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
 - Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
+- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
+- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
 
 ## Task Handoffs
 
@@ -105,3 +110,4 @@
 - Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
+- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 4f77783cc86ee37d01ad81c9d0ec8f4b95d232a9..cd0962c0c5f7e4e32bf77a6efc82731b4de56b42
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,8 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    Absorber,
+    AbsorberParams,
     AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
@@ -52,6 +54,7 @@
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -63,6 +66,8 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "Absorber",
+    "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
@@ -110,6 +115,7 @@
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index ac5e1c426b81c81ef16aece32cc374527a8f7934..7fe38b2b812610b6659ec31871901c9e24df6926
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,6 +10,8 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -18,6 +20,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
 )
 from autofdtd.geometry import (
     Box,
@@ -71,6 +74,8 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "Absorber",
+    "AbsorberParams",
     "BlochBoundary",
     "Box",
     "Boundary",
@@ -120,6 +125,7 @@
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",
     "TaggedModel",
     "Transformed",
     "UniformGrid",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index dc18da927df364802fe128c2944ba9a24be46ea5..4458d00c00e8c84d254b5e3e5f63cb5637fb220e
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,32 +1,42 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    Absorber,
+    AbsorberParams,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
     BlochBoundary,
+    DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
+    DEFAULT_STABLE_PML_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
 )
 
 __all__ = [
+    "Absorber",
+    "AbsorberParams",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
     "BlochBoundary",
+    "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
+    "DEFAULT_STABLE_PML_PARAMS",
     "PECBoundary",
     "PML",
     "PMLParams",
     "PMCBoundary",
     "Periodic",
+    "StablePML",
     "boundary_edge_model_from_value",
     "boundary_model_from_value",
     "boundary_spec_model_from_value",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 853af70a426326147fb69616bbefc620788653a0..830631745400653084a082cd301c393b1acd54ca
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -112,7 +112,49 @@
         return self
 
 
+class AbsorberParams(TaggedModel):
+    """Polynomial conductivity profile parameters for adiabatic absorbers."""
+
+    type: Literal["AbsorberParams"] = "AbsorberParams"
+    sigma_order: int = 3
+    sigma_min: float = 0.0
+    sigma_max: float = 6.4
+
+    @field_validator("sigma_order")
+    @classmethod
+    def _validate_order(cls, value: int) -> int:
+        if value < 0:
+            raise ValueError("Absorber profile orders must be non-negative")
+        return int(value)
+
+    @field_validator("sigma_min", "sigma_max")
+    @classmethod
+    def _validate_non_negative(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Absorber profile coefficients must be non-negative")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_bounds(self) -> AbsorberParams:
+        if self.sigma_max < self.sigma_min:
+            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
+        return self
+
+
 DEFAULT_PML_PARAMS = PMLParams()
+DEFAULT_STABLE_PML_PARAMS = PMLParams(
+    sigma_order=3,
+    sigma_min=0.0,
+    sigma_max=1.0,
+    kappa_order=3,
+    kappa_min=1.0,
+    kappa_max=5.0,
+    alpha_order=1,
+    alpha_min=0.0,
+    alpha_max=0.9,
+)
+DEFAULT_ABSORBER_PARAMS = AbsorberParams()
 
 
 class PML(BoundaryEdge):
@@ -141,13 +183,67 @@
         return normalized
 
 
-ImplementedBoundaryEdge = Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML
+class StablePML(BoundaryEdge):
+    """More conservative complex-frequency-shift PML preset."""
+
+    type: Literal["StablePML"] = "StablePML"
+    num_layers: int = 40
+    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
+    extrude_structures: bool = True
+
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("StablePML num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
+
+            warnings.warn(
+                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+class Absorber(BoundaryEdge):
+    """Adiabatic absorbing layer backed by a reflective outer wall."""
+
+    type: Literal["Absorber"] = "Absorber"
+    num_layers: int = 40
+    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
+    extrude_structures: bool = False
 
+    @field_validator("num_layers")
+    @classmethod
+    def _validate_num_layers(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Absorber num_layers must be at least 1")
+        if normalized < 6:
+            from autofdtd.core.validation import AutoFDTDValidationWarning
 
+            warnings.warn(
+                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
+                "should only be used for small smoke tests.",
+                AutoFDTDValidationWarning,
+                stacklevel=2,
+            )
+        return normalized
+
+
+ImplementedBoundaryEdge = (
+    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+)
+
+
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -162,6 +258,10 @@
         return PMCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
+    if boundary_type == "StablePML":
+        return StablePML(**value)
+    if boundary_type == "Absorber":
+        return Absorber(**value)
     raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
 
 
@@ -175,11 +275,19 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
-            if boundary_type in {"Periodic", "BlochBoundary", "PECBoundary", "PMCBoundary", "PML"}:
+            if boundary_type in {
+                "Periodic",
+                "BlochBoundary",
+                "PECBoundary",
+                "PMCBoundary",
+                "PML",
+                "StablePML",
+                "Absorber",
+            }:
                 return boundary_edge_model_from_value(value)
         return value
 
@@ -264,6 +372,36 @@
         )
         return cls(plus=pml, minus=pml)
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> Boundary:
+        stable = StablePML(
+            num_layers=num_layers,
+            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=stable, minus=stable)
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> Boundary:
+        absorber = Absorber(
+            num_layers=num_layers,
+            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
+            extrude_structures=extrude_structures,
+        )
+        return cls(plus=absorber, minus=absorber)
+
 
 def boundary_model_from_value(value: object) -> Boundary:
     """Normalize a supported axis boundary pair."""
@@ -289,7 +427,10 @@
     @field_validator("x", "y", "z", mode="before")
     @classmethod
     def _coerce_axis_boundary(cls, value: object) -> object:
-        if isinstance(value, (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+        if isinstance(
+            value,
+            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -300,6 +441,8 @@
                 "PECBoundary",
                 "PMCBoundary",
                 "PML",
+                "StablePML",
+                "Absorber",
             }:
                 return boundary_model_from_value(value)
         return value
@@ -391,6 +534,88 @@
             ),
         )
 
+    @classmethod
+    def stable_pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.stable_pml(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def absorber(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 40,
+        parameters: AbsorberParams | None = None,
+        extrude_structures: bool = False,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.absorber(
+                    num_layers=num_layers,
+                    parameters=parameters,
+                    extrude_structures=extrude_structures,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
 
 def boundary_spec_model_from_value(value: object) -> BoundarySpec:
     """Normalize a supported BoundarySpec container."""
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index ccd283d09ea3c7cd0430b65d03f050ef3d335893..9e9bb479da89610437f887bec99075ee7793c6be
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,15 +7,19 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
+    DEFAULT_ABSORBER_PARAMS,
     PECBoundary,
     PML,
     PMLParams,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -31,12 +35,15 @@
     PEC = "pec"
     PMC = "pmc"
     PML = "pml"
+    STABLE_PML = "stable_pml"
+    ABSORBER = "absorber"
 
 
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
     type: str = "CompiledPMLCoefficients"
+    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
     num_layers: int
     sigma: tuple[float, ...]
     kappa: tuple[float, ...]
@@ -45,6 +52,8 @@
     memory_decay: tuple[float, ...]
     memory_drive: tuple[float, ...]
     stretch: tuple[float, ...]
+    uses_memory: bool = True
+    terminal_reflection: Literal["none", "pec"] = "none"
     dt: float
     grid_spacing: float
 
@@ -96,6 +105,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     requires_complex_fields: bool = False
     symmetry: tuple[int, int, int] = (0, 0, 0)
     active_symmetry_axes: tuple[str, ...] = ()
@@ -121,6 +134,10 @@
         return BoundaryMode.PMC
     if isinstance(edge, PML):
         return BoundaryMode.PML
+    if isinstance(edge, StablePML):
+        return BoundaryMode.STABLE_PML
+    if isinstance(edge, Absorber):
+        return BoundaryMode.ABSORBER
     raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
 
 
@@ -139,6 +156,10 @@
         electric = tuple(normal)
         magnetic = tuple(tangential)
         return electric, magnetic
+    if mode is BoundaryMode.ABSORBER:
+        electric = tuple(tangential)
+        magnetic = tuple(normal)
+        return electric, magnetic
     return (1, 1, 1), (1, 1, 1)
 
 
@@ -165,19 +186,26 @@
     return electric, magnetic
 
 
-def _resolve_pml_params(edge: PML) -> PMLParams:
+def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
     params = edge.parameters
     if isinstance(params, PMLParams):
         return params
     return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)
 
 
+def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
+    params = edge.parameters
+    if isinstance(params, AbsorberParams):
+        return params
+    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)
+
+
 def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
     return minimum + (maximum - minimum) * (fraction**order)
 
 
 def _compile_pml_coefficients(
-    edge: PML,
+    edge: PML | StablePML,
     *,
     dt: float,
     grid_spacing: float,
@@ -219,6 +247,7 @@
         memory_drive.append(drive_value)
         stretch.append(stretch_value)
     return CompiledPMLCoefficients(
+        boundary_type=edge.type,
         num_layers=edge.num_layers,
         sigma=tuple(sigma),
         kappa=tuple(kappa),
@@ -227,6 +256,48 @@
         memory_decay=tuple(memory_decay),
         memory_drive=tuple(memory_drive),
         stretch=tuple(stretch),
+        uses_memory=True,
+        terminal_reflection="none",
+        dt=dt,
+        grid_spacing=grid_spacing,
+    )
+
+
+def _compile_absorber_coefficients(
+    edge: Absorber,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledPMLCoefficients:
+    if dt <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
+    params = _resolve_absorber_params(edge)
+    sigma: list[float] = []
+    attenuation: list[float] = []
+    zero = [0.0] * edge.num_layers
+    ones = [1.0] * edge.num_layers
+    for layer_index in range(edge.num_layers):
+        fraction = float(layer_index + 1) / float(edge.num_layers)
+        sigma_value = _polynomial_profile(
+            params.sigma_min, params.sigma_max, params.sigma_order, fraction
+        )
+        damping_rate = max(0.0, sigma_value)
+        sigma.append(sigma_value)
+        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
+    return CompiledPMLCoefficients(
+        boundary_type=edge.type,
+        num_layers=edge.num_layers,
+        sigma=tuple(sigma),
+        kappa=tuple(ones),
+        alpha=tuple(zero),
+        attenuation=tuple(attenuation),
+        memory_decay=tuple(zero),
+        memory_drive=tuple(zero),
+        stretch=tuple(ones),
+        uses_memory=False,
+        terminal_reflection="pec",
         dt=dt,
         grid_spacing=grid_spacing,
     )
@@ -245,8 +316,10 @@
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
     pml = None
     normalized = boundary_edge_model_from_value(edge)
-    if isinstance(normalized, PML):
+    if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
+    elif isinstance(normalized, Absorber):
+        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     return CompiledBoundaryEdge(
         axis=axis_name,
         side=side,
@@ -273,7 +346,7 @@
         exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
-    elif edge_modes == {BoundaryMode.PML}:
+    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
@@ -282,7 +355,7 @@
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
-        if compiled.mode is BoundaryMode.PML
+        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
     )
     return CompiledBoundaryAxis(
         axis=axis_name,
@@ -329,7 +402,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
+                "Absorber entries"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -357,7 +431,7 @@
     reflective_axes = tuple(
         axis.axis
         for axis in compiled_axes.values()
-        if axis.exchange_kind in {"reflective", "mixed"}
+        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
@@ -365,6 +439,30 @@
         for axis in compiled_axes.values()
         for side in axis.pml_faces
     )
+    stable_pml_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(
+            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
+        )
+    )
+    stable_pml_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
+    )
+    absorber_axes = tuple(
+        axis.axis
+        for axis in compiled_axes.values()
+        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
+    )
+    absorber_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in ("minus", "plus")
+        if getattr(axis, side).mode is BoundaryMode.ABSORBER
+    )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
     if pml_faces:
@@ -378,6 +476,10 @@
         reflective_axes=reflective_axes,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
+        stable_pml_axes=stable_pml_axes,
+        stable_pml_faces=stable_pml_faces,
+        absorber_axes=absorber_axes,
+        absorber_faces=absorber_faces,
         requires_complex_fields=bool(bloch_axes),
         symmetry=symmetry,
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index b7b04c21ef77a0136671bb5060374600d73b41d6..5cea4be944906b2f933855c94a911358d531a5db
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,6 +10,7 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    Absorber,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -17,6 +18,7 @@
     PML,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_spec_model_from_value,
 )
 from autofdtd.geometry import (
@@ -83,6 +85,9 @@
         "PMCBoundary",
         "PML",
         "PMLParams",
+        "StablePML",
+        "Absorber",
+        "AbsorberParams",
     }
 )
 _PHASE1_GRID_TYPES = frozenset(
@@ -361,6 +366,8 @@
         return entry
     if context == "boundary_spec" and feature_name == "PMLParams":
         return feature_entry("PML")
+    if context == "boundary_spec" and feature_name == "AbsorberParams":
+        return feature_entry("Absorber")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
@@ -605,7 +612,7 @@
         return boundary.copy_update(**updated) if changed else boundary
     if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
         return boundary
-    if isinstance(boundary, PML):
+    if isinstance(boundary, (PML, StablePML, Absorber)):
         warnings.warn(
             f"zero-sized axis {axis_name!r} cannot use absorbing boundaries; coercing to Periodic",
             AutoFDTDValidationWarning,
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index f529874e910d33d78c493bcfe8ac2310e9b7cd48..59eefd36da3c16986bd78390d1b7453eafd3069c
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -1,10 +1,10 @@
-"""Examples for the foundational Phase 1 boundary subset."""
+"""Examples for the Phase 1 boundary subset."""
 
 from __future__ import annotations
 
 import numpy as np
 
-from autofdtd.api import Boundary, BoundarySpec
+from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
 from autofdtd.runtime import build_boundary_runtime
 
@@ -43,3 +43,27 @@
         "energy_before": before,
         "energy_after": after,
     }
+
+
+def absorbing_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for StablePML and Absorber compilation."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=8),
+            y=Boundary.absorber(
+                num_layers=10,
+                parameters=AbsorberParams(sigma_max=7.2),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    return {
+        "stage_order": compiled.stage_order,
+        "pml_faces": compiled.pml_faces,
+        "stable_pml_faces": compiled.stable_pml_faces,
+        "absorber_faces": compiled.absorber_faces,
+        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index c55beca5d4ebe03eee14d891b3b19bf23d0401f0..758954ee35864eb2b2af282f04b2e10ac7a5641a
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,8 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    AbsorberIR,
+    AbsorberParamsIR,
     AnisotropicMediumIR,
     ArtifactIR,
     AutoGridIR,
@@ -46,6 +48,7 @@
     SimulationIR,
     SphereIR,
     StaircasingIR,
+    StablePMLIR,
     StructureIR,
     SubpixelPolicyIR,
     SubpixelSpecIR,
@@ -70,6 +73,8 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "AbsorberIR",
+    "AbsorberParamsIR",
     "AnisotropicMediumIR",
     "AutoGridIR",
     "ArtifactIR",
@@ -112,6 +117,7 @@
     "SellmeierIR",
     "SimulationIR",
     "SphereIR",
+    "StablePMLIR",
     "StructureIR",
     "StaircasingIR",
     "SubpixelPolicyIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 17aa0b2328d3411c375fc3808978d9c3bde23a15..3b3ce73e6477ff20d19bed1ed91654b787145c5b
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,6 +11,8 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -19,6 +21,7 @@
     PECBoundary,
     PMCBoundary,
     Periodic,
+    StablePML,
     boundary_edge_model_from_value,
     boundary_model_from_value,
     boundary_spec_model_from_value,
@@ -435,6 +438,39 @@
     extrude_structures: bool = True
 
 
+class StablePMLIR(IRModel):
+    """Typed execution IR for a stable-PML boundary edge."""
+
+    type: Literal["StablePMLIR"] = "StablePMLIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["StablePML"] = "StablePML"
+    name: str | None = None
+    num_layers: int
+    parameters: PMLParamsIR
+    extrude_structures: bool = True
+
+
+class AbsorberParamsIR(IRModel):
+    """Typed execution IR for adiabatic absorber parameters."""
+
+    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
+    sigma_order: int
+    sigma_min: float
+    sigma_max: float
+
+
+class AbsorberIR(IRModel):
+    """Typed execution IR for an adiabatic absorber edge."""
+
+    type: Literal["AbsorberIR"] = "AbsorberIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["Absorber"] = "Absorber"
+    name: str | None = None
+    num_layers: int
+    parameters: AbsorberParamsIR
+    extrude_structures: bool = False
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -446,7 +482,14 @@
 
 
 BoundaryEdgeIR = (
-    PeriodicIR | BlochBoundaryIR | PECBoundaryIR | PMCBoundaryIR | PMLIR | ComponentIR
+    PeriodicIR
+    | BlochBoundaryIR
+    | PECBoundaryIR
+    | PMCBoundaryIR
+    | PMLIR
+    | StablePMLIR
+    | AbsorberIR
+    | ComponentIR
 )
 
 
@@ -475,6 +518,10 @@
     reflective_axes: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
+    stable_pml_axes: tuple[str, ...] = ()
+    stable_pml_faces: tuple[str, ...] = ()
+    absorber_axes: tuple[str, ...] = ()
+    absorber_faces: tuple[str, ...] = ()
     active_symmetry_axes: tuple[str, ...] = ()
     symmetry_axes: tuple[SymmetryAxisIR, ...] = ()
 
@@ -958,8 +1005,8 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML)):
+    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
+    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
@@ -967,6 +1014,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1005,6 +1054,44 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, StablePML):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, PMLParams)
+            else PMLParams.model_validate(edge.parameters)
+        )
+        return StablePMLIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=PMLParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+                kappa_order=params.kappa_order,
+                kappa_min=params.kappa_min,
+                kappa_max=params.kappa_max,
+                alpha_order=params.alpha_order,
+                alpha_min=params.alpha_min,
+                alpha_max=params.alpha_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
+    if isinstance(edge, Absorber):
+        params = (
+            edge.parameters
+            if isinstance(edge.parameters, AbsorberParams)
+            else AbsorberParams.model_validate(edge.parameters)
+        )
+        return AbsorberIR(
+            name=edge.name,
+            num_layers=edge.num_layers,
+            parameters=AbsorberParamsIR(
+                sigma_order=params.sigma_order,
+                sigma_min=params.sigma_min,
+                sigma_max=params.sigma_max,
+            ),
+            extrude_structures=edge.extrude_structures,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1021,6 +1108,8 @@
         "PECBoundary",
         "PMCBoundary",
         "PML",
+        "StablePML",
+        "Absorber",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1085,6 +1174,10 @@
     reflective_axes: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
+    stable_pml_axes: list[str] = []
+    stable_pml_faces: list[str] = []
+    absorber_axes: list[str] = []
+    absorber_faces: list[str] = []
     requires_complex_fields = False
     axes = {}
     for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
@@ -1098,24 +1191,36 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
-        elif edge_types == {"PML"}:
+        elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             reflective_axes.append(axis_name)
         else:
-            if lowered.minus.component_type == "PML":
+            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.minus")
-            if lowered.plus.component_type == "PML":
+            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                 pml_faces.append(f"{axis_name}.plus")
             if (
-                lowered.minus.component_type == "PML"
-                or lowered.plus.component_type == "PML"
+                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
+                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
+            stable_pml_axes.append(axis_name)
+        if lowered.minus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "StablePML":
+            stable_pml_faces.append(f"{axis_name}.plus")
+        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
+            absorber_axes.append(axis_name)
+        if lowered.minus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "Absorber":
+            absorber_faces.append(f"{axis_name}.plus")
     symmetry_axes = _symmetry_axes_to_ir(symmetry)
     return BoundarySpecIR(
         x=axes["x"],
@@ -1127,6 +1232,10 @@
         reflective_axes=tuple(reflective_axes),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
+        stable_pml_axes=tuple(stable_pml_axes),
+        stable_pml_faces=tuple(stable_pml_faces),
+        absorber_axes=tuple(absorber_axes),
+        absorber_faces=tuple(absorber_faces),
         active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
         symmetry_axes=symmetry_axes,
     )
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index f85e5f6d5e71a583cb0eb3c76dba39f3b2e35a37..873a5b5e5ac5868b6774e3d7a3308db4654340b8
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -96,7 +96,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode is BoundaryMode.PML:
+    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -158,7 +158,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode is BoundaryMode.PML:
+    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -236,7 +236,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             face_key = f"{axis_boundary.axis}.{side}"
             face_state = face_states[face_key]
@@ -290,9 +294,11 @@
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml"),
+        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
         "supports_pml_layers": True,
+        "supports_stable_pml": True,
+        "supports_absorber": True,
         "halo_depth": 1,
     }
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index 9cd6d97a59de03ef235ef8f518fec3789ee05b83..d6785ffa7f955b202680d7788c752d028b3e5df1
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -28,7 +28,7 @@
     *,
     dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
 ) -> PMLBoundaryState:
-    """Allocate persistent PML memory arrays for all active faces."""
+    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
 
     if len(field_shape) != 4 or field_shape[-1] != 3:
         raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
@@ -39,7 +39,11 @@
         axis_index = "xyz".index(axis_boundary.axis)
         for side in ("minus", "plus"):
             edge = getattr(axis_boundary, side)
-            if edge.mode is not BoundaryMode.PML or edge.pml is None:
+            if edge.mode not in {
+                BoundaryMode.PML,
+                BoundaryMode.STABLE_PML,
+                BoundaryMode.ABSORBER,
+            } or edge.pml is None:
                 continue
             memory_shape = list(field_shape)
             memory_shape[axis_index] = edge.pml.num_layers
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index da49c77fa38fc4077c6dd9d5615a5d012b74fc26..37e81ba2d38cc53394d49541ba667e34745c9271
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,6 +4,8 @@
 import pytest
 
 from autofdtd.api import (
+    Absorber,
+    AbsorberParams,
     BlochBoundary,
     Boundary,
     BoundarySpec,
@@ -13,10 +15,11 @@
     PMCBoundary,
     Periodic,
     Simulation,
+    StablePML,
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import BoundarySpecIR, PMLIR, simulation_to_ir
+from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -62,9 +65,13 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
-def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
+def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
-        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pml(num_layers=4)),
+        BoundarySpec(
+            x=Boundary.bloch(0.25),
+            y=Boundary.stable_pml(num_layers=8),
+            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
+        ),
         symmetry=(1, 0, -1),
         dt=0.5,
         grid_spacing=(0.25, 0.5, 0.25),
@@ -74,14 +81,22 @@
     assert compiled.x.minus.mode is BoundaryMode.BLOCH
     assert compiled.x.plus.phase_factor == pytest.approx(1j)
     assert compiled.x.minus.phase_factor == pytest.approx(-1j)
-    assert compiled.y.minus.mode is BoundaryMode.PEC
+    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
+    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
     assert compiled.z.plus.mode is BoundaryMode.PML
     assert compiled.bloch_axes == ("x",)
-    assert compiled.reflective_axes == ("y",)
-    assert compiled.pml_axes == ("z",)
-    assert compiled.pml_faces == ("z.minus", "z.plus")
+    assert compiled.reflective_axes == ("y", "z")
+    assert compiled.pml_axes == ("y", "z")
+    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
+    assert compiled.stable_pml_axes == ("y",)
+    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
+    assert compiled.absorber_axes == ("z",)
+    assert compiled.absorber_faces == ("z.minus",)
     assert compiled.z.plus.pml is not None
     assert compiled.z.plus.pml.num_layers == 4
+    assert compiled.z.minus.pml.boundary_type == "Absorber"
+    assert compiled.z.minus.pml.terminal_reflection == "pec"
+    assert compiled.y.plus.pml.boundary_type == "StablePML"
     assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
     assert compiled.stage_order == (
         "electric_boundary",
@@ -94,12 +109,6 @@
     assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
-    with pytest.raises(
-        ValueError,
-        match="supports only BoundarySpec with Periodic, BlochBoundary, PECBoundary, PMCBoundary, and PML",
-    ):
-        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "StablePML"}})
-
 
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
@@ -147,7 +156,15 @@
     )
 
     assert runtime.compiled.bloch_axes == ("x",)
-    assert metadata["supported_modes"] == ("periodic", "bloch", "pec", "pmc", "pml")
+    assert metadata["supported_modes"] == (
+        "periodic",
+        "bloch",
+        "pec",
+        "pmc",
+        "pml",
+        "stable_pml",
+        "absorber",
+    )
     assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
     assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
     assert simulation_ir.boundary_spec.requires_complex_fields is True
@@ -182,6 +199,35 @@
     assert simulation_ir.boundary_spec.reflective_axes == ("x",)
 
 
+def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(
+                x=Boundary.stable_pml(
+                    num_layers=10,
+                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
+                ),
+                z=Boundary.absorber(
+                    num_layers=12,
+                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
+                ),
+            ),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
+    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
+    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
+    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
+    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
+    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
+    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
+    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
+
+
 def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
     runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
     vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)
@@ -215,14 +261,15 @@
     compiled = compile_boundary_spec(
         BoundarySpec(
             x=Boundary.pml(num_layers=3),
-            y=Boundary.periodic(),
-            z=Boundary(minus=PML(num_layers=2), plus=Periodic()),
+            y=Boundary.absorber(num_layers=5),
+            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
         )
     )
     state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
 
-    assert set(state.electric) == {"x.minus", "x.plus", "z.minus"}
+    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
     assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
+    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
     assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
@@ -253,6 +300,52 @@
     assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
 
 
+def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(
+            x=Boundary.stable_pml(num_layers=6),
+            y=Boundary.absorber(
+                num_layers=6,
+                parameters=AbsorberParams(sigma_max=8.0),
+            ),
+            z=Boundary.periodic(),
+        ),
+        dt=0.5,
+        grid_spacing=(0.25, 0.25, 0.5),
+    )
+    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
+    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
+    electric[1:7, 1:7, 1:3, 0] = 1.0
+    magnetic[1:7, 1:7, 1:3, 1] = 1.0
+
+    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+    for _ in range(10):
+        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
+    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
+    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
+
+    assert after_x < before_x * 0.3
+    assert after_y < before_y * 0.3
+    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
+    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)
+
+
+def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
+    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
+    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
+    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)
+
+    electric[1, 1, 1] = (1.0, 2.0, 3.0)
+    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)
+
+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
+
+    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
+    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))
+
+
 def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
     boundary = Boundary.bloch(0.125)
     assert isinstance(boundary.plus, BlochBoundary)
@@ -262,3 +355,6 @@
 
     with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
         Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())
+
+    assert isinstance(Boundary.stable_pml().minus, StablePML)
+    assert isinstance(Boundary.absorber().plus, Absorber)

tokens used
124,276
{"task_id":"task-022","status":"completed","summary":"Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.","key_findings":["`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.","Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.","Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.","The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.","Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Extended staged boundary kernels to handle StablePML and Absorber runtime modes."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Updated runtime state allocation so all absorbing families share the staged boundary execution path."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."},{"path":"/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json","description":"Structured task result for this loop iteration."}],"error_summary":"","follow_up_notes":"The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.","next_action":"none"}
