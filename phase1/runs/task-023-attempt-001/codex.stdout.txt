OpenAI Codex v0.118.0 (research preview)
--------
workdir: /workspace/autofdtd
model: gpt-5.4
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: none
reasoning summaries: none
session id: 019d645a-3d9c-73d2-96ff-a099c61d126c
--------
user
# Phase 1 Maxwell Solver Loop

## Mission

Drive Phase 1 of autofdtd from an almost-empty repository to a credible, pip-installable, GPU-first FDTD package whose feature plan is anchored explicitly to the local Tidy3D EM API surface. The loop must produce a Tidy3D-inspired public API, a tagged execution IR, a scene-compilation pipeline, a chunk-based multi-node-ready runtime, Warp kernel conventions, concrete implementations for the highest-priority Tidy3D feature families, validation examples, and documentation without copying protected code or docs from any reference project.

## Operator Policies

- Failure policy: `retry`
- Permission mode: `danger-full-access`
- End-of-list behavior: `stop cleanly`
- Codex working directory: `..` (repo root relative to `phase1/`)

## Global Execution Rules

- Read relevant files before editing.
- Keep progress notes concise and factual.
- Treat `loop.spec.md` as the queue source of truth.
- Report output that matches `output.schema.json`.
- Use `completed` only when the task's success criteria were actually satisfied.
- Use `needs_retry` when meaningful work was done but the success criteria are not yet fully met.
- Use `blocked` only when an external dependency or missing input prevents further progress.
- Use `failed` when the attempt is unusable or the task direction was not successfully carried out.
- Prefer producing concrete artifacts and findings over generic summaries.
- Keep `working-memory.md` concise and deduplicated.
- Do not repeat reference material in `working-memory.md`; point to local files when possible.
- Prefer recording file paths, decisions, and distilled facts instead of copying long passages.
- Do substantial reference-backed work before implementation and keep updating working-memory.md with distilled decisions that later tasks should inherit.
- Treat Tidy3D compatibility as a typed tagged object-model and lowering problem, not as a loose dict compatibility exercise.
- Treat the local Tidy3D EM feature surface as the gold standard for feature planning and error-policy decisions in Phase 1.
- Keep the mathematical stage graph separate from execution scheduling so bulk-synchronous stepping, overlap, and future systolic variants can coexist.
- Separate scene modeling, scene compilation, runtime stepping, and IO or packaging into distinct subsystems.
- Use NVIDIA Warp for GPU kernel implementation work and design kernels around stable module contents, persistent arrays, capture-safe stepping, and intentional instrumentation.
- Do not build a monolithic everything-kernel. Prefer staged update kernels and capability-driven specialization by region or chunk.
- Every substantive implementation task should include or update tests, examples, benchmarks, or instrumentation that exercise the new behavior.
- When a Tidy3D feature is not yet supported, implement or document explicit validation or error behavior rather than leaving silent gaps.
- Record initialization time, JIT time, warm steady-state timestep metrics, and cells-updated-per-second style metrics whenever solver execution paths become runnable.
- Use local reference repos and papers aggressively for research, but do not copy protected code or documentation into this repository.
- Assume the current machine has at least two NVIDIA GPUs available and use that for meaningful smoke tests where feasible.

## Output Contract

- `summary` should be a concise outcome statement.
- `key_findings` should capture the most important concrete discoveries or decisions.
- `artifacts` should list meaningful outputs with both `path` and `description`.
- `error_summary` should be filled for `blocked` or `failed` runs and empty otherwise.
- `next_action` should be `retry`, `human_review`, or `none`.
## Reference Material

### Reference 1

Repository baseline at bundle generation time: the `autofdtd` repo has a minimal README, phase directories, and an effectively empty src tree. Phase 1 therefore starts from architecture, package skeleton, and subsystem contracts rather than refining an existing solver. The prep-generated feature ledger lives at `phase1/feature-checklist.md` and should be treated as input context for the loop, not as a loop deliverable.

### Reference 2

Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.

### Reference 3

Tidy3D compatibility guidance for Phase 1: keep the public and IR models tagged, immutable, schema-driven, and validation-heavy; preserve named monitor access patterns and ordered structure semantics; and front-load an explicit feature matrix that distinguishes must-implement features, defer buckets, and reject-with-clear-error buckets rather than leaving gaps ambiguous.

### Reference 4

Meep architectural findings: the stable decomposition unit is the chunk rather than the rank; a rank may own multiple chunks; chunk ownership, symmetry reduction, and periodic or Bloch remapping behave like logical address transforms over a full-domain view; communication plans are stage-specific and component-family-specific; geometry and material initialization are separate from stepping; and near-to-far is a DFT-backed monitor family rather than a lightweight add-on. The Meep paper sharpens this further: the solver is designed to preserve an illusion of continuous space and time on top of chunked discrete execution, uses pervasive interpolation and restriction for sources, monitors, and field queries, relies on anisotropic subpixel smoothing to remove first-order interface error, and favors bulk region operations over pointwise APIs. High-value references: `../meep/doc/docs/Parallel_Meep.md`, `../meep/doc/docs/Chunks_and_Symmetry.md`, `../meep/src/structure.cpp`, `../meep/src/boundaries.cpp`, `../meep/src/fields.cpp`, `../meep/src/loop_in_chunks.cpp`, `../meep/src/near2far.cpp`, and `papers/meep_paper.pdf`.

### Reference 5

Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.

### Reference 6

Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.

### Reference 7

Geometry and mode-solver findings: GeometryPrimitives.jl is centered on analytic shape queries such as level, nearest surface point, normals, bounds, and translation rather than a full scene graph. Orthogonal prisms and convex polygons are natural early scope; subpixel support exists mainly through local plane-cut overlap ideas rather than a full arbitrary-shape averaging engine; KD-tree and periodic replication patterns matter early; and libctl provides richer object-model and overlap-integral precedents for material-carrying ordered geometry lists and periodic duplication. VectorModesolver.jl is best treated as a formulation and interface reference rather than a polished dependency surface. It expects a sampled tensor-valued permittivity callback over a rectilinear cross-section, assembles a sparse operator, solves for magnetic fields, and reconstructs electric fields.

### Reference 8

Working-memory seed facts that later tasks should preserve: chunk is the architectural unit while rank or device is placement; stored topology and logical topology diverge because of symmetry and periodic or Bloch remapping; scene compilation must remain distinct from stepping; continuous problem specifications must lower through explicit interpolation and restriction into discrete execution artifacts; geometry lowering and materialization must precede serious kernels; subpixel smoothing can induce effective anisotropy even for physically isotropic materials; stage-specific halos and monitor metadata should be explicit; Tidy3D-style compatibility should be represented as a tagged immutable IR with normalization rules and unsupported-feature policy; runtime metrics should include Gcells/s plus separate end-to-end and steady-state timing; and near-to-far should be built on top of DFT monitor infrastructure rather than as an isolated feature.


## Runtime Context

- The runner injects the current task's title, success criteria, and notes from `loop.spec.md` on every iteration. Do not duplicate the full task catalog here.
- Use `phase1/loop.spec.md` as the authoritative task board.
- Use `phase1/feature-checklist.md` as the compact feature ledger for what Phase 1 should implement, defer, or reject clearly.
- Use `phase1/kernel-formula-sources.md` as the compact source map for kernel formulas, update equations, and implementation references.
- Keep the key architectural dependencies in mind while executing any task:
  - mode solver work should precede `ModeSource` and mode-monitor workflows
  - second-order subpixel smoothing depends on the anisotropic material representation and update path already working
  - logging and convergence behavior are first-class runtime deliverables, not incidental byproducts
  - kernel/runtime support must be explicit for materials, boundaries, sources, monitors, and postprocessing rather than implied by API support

## Working Memory

# Phase 1 Maxwell Solver Loop Working Memory

Use this file for cumulative findings, reusable facts, decisions, and cross-task notes.

Keep it concise. Do not repeat information already captured in `loop.prompt.md` or other local files.
Prefer referencing local file paths and distilled takeaways instead of copying long excerpts.

Suggested sections:

- Facts
- Decisions
- Open Questions
- Reusable References
- Task Handoffs

## Facts

- `task-001` starts from an effectively empty repo with no `src/` package or tooling.
- `phase1/feature-checklist.md` is now mirrored in code via `src/autofdtd/planning.py` so later tasks can reuse a local feature/status source instead of reparsing markdown.
- `src/autofdtd/core/containers.py` now holds the first frozen tagged container models for `Structure`, `Scene`, and `Simulation`; `src/autofdtd/api/simulation.py` re-exports them as the public API surface.
- `src/autofdtd/ir/models.py` now defines the first versioned execution IR, with `ComponentIR`, `StructureIR`, `SceneIR`, `SimulationIR`, and `ExecutionPackageIR` as the transport contract.
- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
- `src/autofdtd/core/models.py` now provides the shared frozen model contract with `model_tag()`, `copy_update()`, `json_ready()`, and stable JSON serialization helpers reused by both public containers and IR models.
- `src/autofdtd/geometry/primitives.py` now provides typed `Box`, `Sphere`, and `Cylinder` public models plus `GeometryTransform`, bounds, point-containment, and translation helpers.
- `src/autofdtd/ir/models.py` now lowers primitive geometries into typed `BoxIR`, `SphereIR`, and `CylinderIR` payloads with explicit bounds and scene-compilation transform metadata.
- `src/autofdtd/geometry/polyslab.py` now provides a typed `PolySlab` model for simple-polygon extrusions with bounds, containment, translation, and simple-polygon validation; `src/autofdtd/ir/models.py` lowers it into `PolySlabIR`.
- `src/autofdtd/geometry/composite.py` now provides `GeometryGroup`, `Transformed`, `GeometryArray`, and a narrow `ClipOperation` subset (`union`, `intersection`, `difference`) for Phase 1 scene construction.
- `GeometryTransform` now supports explicit rigid-transform composition plus translation, rotation, reflection, and conservative transformed-bounds evaluation for wrapper lowering and scene queries.
- `src/autofdtd/ir/models.py` now carries typed wrapper IR via `GeometryGroupIR`, `TransformedIR`, and `ClipOperationIR`.
- `GeometryArray` uses Tidy3D-style `geometry + offsets + transforms` public semantics, but Phase 1 keeps transforms rigid and linear-only and lowers arrays into explicit grouped instances for IR and overlap evaluation.
- `Scene.structure_precedence()` and `Scene.resolve_structure_stack_at_point()` now expose concrete overlap order with `source_index`, `resolved_priority`, and `precedence_rank`, and `StructureIR` carries the same metadata for later compiler stages.
- `src/autofdtd/grid/specs.py` now provides `AutoGrid`, `GridRefinement`, and `LayerRefinementSpec` in addition to the manual grid models; the current AutoGrid path is wavelength-driven, axis-local, and only applies axis-aligned layer refinement metadata.
- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
- `src/autofdtd/grid/subpixel.py` now provides the first explicit subpixel policy layer with `SubpixelSpec`, `Staircasing`, and `PolarizedAveraging`, plus Tidy3D-style `bool` coercion for `Simulation.subpixel`.
- `src/autofdtd/ir/models.py` now lowers subpixel settings into `SubpixelSpecIR` with explicit averaging-vs-staircasing targets and an anisotropic-materialization flag for later scene compilation.
- `src/autofdtd/materials/isotropic.py` now provides typed `Medium`, `PECMedium`, and `PMCMedium` public models with validation and normalization helpers; `src/autofdtd/ir/models.py` lowers them into typed `MediumIR`, `PECMediumIR`, and `PMCMediumIR`.
- `src/autofdtd/compiler/materials.py` now compiles isotropic constitutive coefficients, exposes scene-point medium sampling, and keeps explicit clamp-zero modes for `PECMedium` and `PMCMedium`.
- `src/autofdtd/kernels/materials.py` now provides the first staged constitutive-update helpers with a NumPy path and optional Warp-availability reporting.
- `src/autofdtd/materials/dispersive.py` now provides a Phase 1 `PoleResidue` public model with JSON-safe complex-pair storage, causal validation (`Re(a) <= 0`), `eps_model()`, and limited `Medium` conversion helpers.
- `src/autofdtd/materials/dispersive.py` now also provides a passive homogeneous `Sellmeier` model with `(B_i, C_i)` coefficient storage, direct `eps_model()` evaluation, single-pole `from_dispersion()` fitting, and explicit lowering to an equivalent `PoleResidue` representation.
- `src/autofdtd/materials/dispersive.py` now also provides passive homogeneous `Lorentz`, damped `Drude`, and passive homogeneous `Debye` models with family-specific analytical coefficients and explicit lowering to equivalent `PoleResidue` payloads.
- `src/autofdtd/ir/models.py` now lowers supported dispersive media into typed `PoleResidueIR` payloads instead of a generic medium envelope.
- `src/autofdtd/ir/models.py` now lowers `Sellmeier` into a typed `SellmeierIR` while keeping runtime compilation on the shared pole-residue auxiliary-state contract.
- `src/autofdtd/ir/models.py` now lowers `Lorentz`, `Drude`, and `Debye` into typed IR families while runtime compilation still targets the shared pole-residue auxiliary-state contract.
- `src/autofdtd/compiler/materials.py` now compiles `PoleResidue` media into explicit epsilon-infinity electric coefficients plus per-pole exact-hold recurrence terms for complex auxiliary polarization state.
- `src/autofdtd/compiler/materials.py` now compiles `Sellmeier` by first reducing it to an equivalent `PoleResidue`, then re-tagging the compiled coefficients as `medium_type="Sellmeier"` so scene diagnostics preserve the public family name.
- `src/autofdtd/compiler/materials.py` now compiles `Lorentz`, `Drude`, and `Debye` by first reducing them to equivalent `PoleResidue` terms, re-tagging the compiled coefficients with the public family name, and failing explicitly when converted poles are too stiff for the current exact-hold timestep path.
- `src/autofdtd/kernels/materials.py` now allocates explicit PoleResidue auxiliary state and applies a staged `pole_residue_electric_update()` that subtracts a compiled polarization-current estimate from the baseline electric update.
- `src/autofdtd/materials/anisotropic.py` now provides `AnisotropicMedium` as the implemented diagonal tensor family plus explicit `FullyAnisotropicMedium` and `Medium2D` defer-policy surfaces; `Medium2D` is parseable and convertible to volumetric `AnisotropicMedium`, but direct simulation use stays rejected in Phase 1.
- `src/autofdtd/compiler/materials.py` now compiles `AnisotropicMedium` into `AnisotropicMaterialCoefficients` with one scalar constitutive branch per principal axis, allowing nondispersive, dispersive, and clamp-mode components to coexist without introducing a dense-tensor runtime yet.
- `src/autofdtd/kernels/materials.py` now provides `allocate_anisotropic_state()`, `anisotropic_electric_update()`, and `anisotropic_magnetic_update()` for diagonal tensor updates with per-axis dispersive auxiliary state.
- `src/autofdtd/materials/advanced.py` now maps `LossyMetalMedium`, `PerturbationMedium`, `PerturbationPoleResidue`, `CustomMedium`, and `CustomAnisotropicMedium` into explicit Phase 1 policy surfaces, and `src/autofdtd/core/validation.py` buckets unknown `Custom*` medium tags into the clear custom-media reject path instead of treating them as unmapped types.
- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile the supported subset into explicit halo metadata and apply periodic-copy or conductor-reflection ghost-cell updates for `(nx, ny, nz, 3)` vector fields.
- `src/autofdtd/boundaries/models.py` now implements `BlochBoundary` plus `Boundary.bloch(...)`; runtime compilation lowers Bloch axes into explicit conjugate phase factors on `minus` faces and forward phase factors on `plus` faces, which forces complex field storage even when `bloch_vec == 0`.
- `src/autofdtd/compiler/boundaries.py` and `src/autofdtd/ir/models.py` now attach symmetry-axis metadata as explicit electric and magnetic sign transforms derived from mirror parity, so chunk planning and future reduced-domain exchange can reuse the same compiled transform contract as conductor reflections.
- `src/autofdtd/boundaries/models.py` now implements baseline `PML` and `PMLParams`; `src/autofdtd/ir/models.py` lowers them into typed `PMLIR` and `PMLParamsIR`, and `BoundarySpecIR` now records explicit `pml_axes` and `pml_faces`.
- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
- `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
- `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
- `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.

## Decisions

- Reserve subsystem namespaces now: `api`, `core`, `geometry`, `materials`, `boundaries`, `sources`, `monitors`, `grid`, `modes`, `ir`, `compiler`, `runtime`, `kernels`, `diagnostics`, `examples`.
- Use `hatchling` with a `src/` layout and ship a small CLI (`autofdtd-feature-matrix`) so the scaffold has a concrete install/runtime smoke target before solver code exists.
- Keep this task focused on packaging, docs, and planning metadata; full tagged models remain for `task-002` and `task-003`.
- Model ordered overlap explicitly through `Scene.structures_in_resolution_order()`, sorting by resolved priority and then insertion order so later structures still win ties during sequential materialization.
- Keep the initial container layer serialization-light: `to_payload()`, `to_json_text()`, and `write_json()` are enough for snapshots and handoff, while `task-005` can generalize the shared model base.
- Use a generic family-tagged component envelope in the IR so Phase 1 can package realistic simulations before every feature family has concrete typed IR classes; later tasks can specialize payloads without changing bundle shape.
- Bundle packaging is directory-based: `manifest.json` points at a hashed `simulation.json` entrypoint, avoiding self-referential manifest hashing while staying suitable for remote upload or local caching.
- Core validation is checklist-driven: implemented feature tags pass, deferred or rejected tags fail early with explicit errors, and known primitive geometry payloads validate only when enough fields are present so placeholder tagged dicts can survive until their concrete family tasks land.
- Shared model behavior should live in `autofdtd.core.models`; public API and IR classes should subclass it rather than duplicating serialization or copy helpers in subsystem-local base classes.
- Primitive geometry stays axis-aligned in Phase 1 public models; scene-compilation transforms are still explicit so later `Transformed` and grouped-geometry tasks can lower onto the same contract instead of rewriting primitive handling.
- Structure precedence is now inspectable with `Scene.resolve_structure_at_point()`, which mirrors materialization order by applying primitive containment checks in resolved-priority order.
- Keep `GeometryArray` as a public wrapper only; repeated placements lower into ordinary grouped and transformed instances so runtime/compiler code can treat arrays as pre-expanded ordered geometry.
- Phase 1 `PolySlab` scope is intentionally limited to constant-cross-section extrusions of simple polygons on principal axes; non-zero `dilation` or `sidewall_angle`, self-intersections, and curved or boolean polygon features must fail explicitly until later geometry tasks land.
- Keep the feature ledger conservative for clipping: `ClipOperation` remains a deferred family in `planning.py`, but geometry validation explicitly allows the implemented subset and rejects unsupported boolean ops with clear errors.
- Phase 1 transform wrappers are rigid only; free-form affine matrices and exact boolean meshing remain out of scope even though the public model now supports grouped and transformed composition.
- Keep the top-level simulation grid surface strict: `grid_spec` must be a `GridSpec`, `AutoGrid` requires `GridSpec.wavelength`, and `QuasiUniformGrid` remains an explicit deferred error.
- Phase 1 AutoGrid is deliberately narrow: derive a vacuum baseline from wavelength and domain size, discretize piecewise with a simple `max_scale` grading pass, and limit refinement inputs to `LayerRefinementSpec` body and bound hints rather than full geometry-driven meshing.
- Keep Phase 1 subpixel support narrow and explicit: dielectric interfaces may use `PolarizedAveraging` or `Staircasing`, while metal, PEC, PMC, and lossy-metal interfaces stay staircasing-only until anisotropic media and conformal policies land.
- Treat PEC and PMC as explicit constitutive clamp modes in coefficient compilation and runtime helpers instead of pretending they are extremely lossy ordinary media.
- Phase 1 `PoleResidue` keeps complex numbers JSON-safe as `(real, imag)` tuples in the public API and IR so serialization stays stable without inventing ad hoc string encoding.
- Phase 1 PoleResidue runtime support is electric-only and uses one complex polarization accumulator per pole; magnetic dispersion remains out of scope until later material families require it.
- Phase 1 `Sellmeier` support is intentionally passive-only and homogeneous-only: `B_i >= 0`, `C_i >= 0`, zero-`C_i` terms fold into `eps_inf`, and all timestep execution reuses the existing electric-only `PoleResidueMaterialCoefficients` path instead of creating a new auxiliary-state layout.
- Phase 1 `Lorentz` is passive-only (`delta_eps >= 0`, `damping >= 0`, `frequency^2 != damping^2`), `Drude` is limited to damped poles (`damping > 0`) so it can lower cleanly onto the current pole-residue path, and `Debye` is passive-only (`delta_eps >= 0`, `tau > 0`).
- The current exact-hold pole-residue recurrence path can overflow for sufficiently stiff poles relative to `dt`; material compilation now raises an explicit error instead of emitting unusable coefficients, which is especially relevant for very small-`tau` Debye fits.
- Phase 1 anisotropy stops at diagonal tensor media: `AnisotropicMedium` is implemented and IR-lowered, `FullyAnisotropicMedium` remains an explicit deferred error for runtime compilation, and `Medium2D` remains a deferred direct-simulation feature with offline conversion helpers only.
- Advanced material policy is explicit rather than silent: `LossyMetalMedium` and perturbation media are parseable defer buckets with manual fallback helpers, while sampled `Custom*` media are parsed or bucketed only to produce clear reject errors.
- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
- Conductor boundaries are represented as reflection-sign transforms rather than special-casing them inside the future timestep loop: PEC uses odd tangential electric and odd normal magnetic parity, while PMC uses the opposite parity assignment.
- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
- Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
- Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.

## Task Handoffs

- `task-003` should add versioned IR models under `autofdtd.ir` without coupling them to the public API module layout; it can treat `Simulation.to_payload()` as an interim handoff format, not the final IR contract.
- Later geometry, material, source, and monitor tasks can plug concrete tagged models into the existing container fields without changing the ordered-container semantics.
- `task-004` and `task-005` can reuse `ComponentIR.payload` plus the shared IR schema version constants instead of inventing another transport shape.
- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
- Later compiler work can rely on wrapper IR bounds as conservative planning envelopes; exact voxelization or boolean rasterization still belongs in scene compilation rather than the public geometry layer.
- Later compiler work can consume `StructureIR.source_index` plus `StructureIR.precedence_rank` directly when building ordered region materialization plans or conflict diagnostics.
- Later grid/compiler tasks should treat `LayerRefinementSpecIR` as planning metadata only; corner finding, snapping-point localization, and structure-derived meshing still need a separate scene-aware meshing pass.
- Material and compiler tasks should consume `SubpixelSpecIR.averaging_targets` and `requires_anisotropic_materialization` directly instead of re-deriving which interface families need tensor-valued effective media.
- Later runtime and chunk-planning tasks can treat `IsotropicMaterialCoefficients` plus `ConstitutiveMode` as the stable baseline constitutive contract before dispersive and anisotropic state is added.
- Later Lorentz, Drude, Debye, and Sellmeier tasks should lower onto the `PoleResidueMaterialCoefficients` auxiliary-state contract whenever the family can be reduced to pole-residue terms instead of creating parallel runtime state layouts.
- Later subpixel materialization work can target `AnisotropicMaterialCoefficients` plus the diagonal anisotropic kernel path instead of inventing a separate tensor-material runtime for effective-media smoothing.
- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.

## Current Task

Task ID: task-023
Title: Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.

warning: Codex could not find system bubblewrap on PATH. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
  → Inspect task board and current boundary implementation to identify ABC-related gaps and existing defer policy surfaces
  • Implement Phase 1 ABC/ModeABC public models, IR/compiler/runtime policy, and validation/defer behavior
  • Add or update tests, docs/examples, and working memory; run targeted verification
exec
/bin/bash -lc "sed -n '1,220p' phase1/loop.spec.md" in /workspace/autofdtd
 succeeded in 0ms:
# Phase 1 Maxwell Solver Loop

Drive Phase 1 of autofdtd from an almost-empty repository to a credible, pip-installable, GPU-first FDTD package whose feature plan is anchored explicitly to the local Tidy3D EM API surface. The loop must produce a Tidy3D-inspired public API, a tagged execution IR, a scene-compilation pipeline, a chunk-based multi-node-ready runtime, Warp kernel conventions, concrete implementations for the highest-priority Tidy3D feature families, validation examples, and documentation without copying protected code or docs from any reference project.

Statuses: `QUEUED`, `RUNNING`, `COMPLETED`, `FAILED`, `BLOCKED`

## [COMPLETED] task-001 - Create the package, tooling, and docs scaffold around the planned feature surface
Success: Create a pip-installable src-layout package with module namespaces, development tooling, test entrypoints, and docs scaffolding that are aligned with the feature checklist and subsystem boundaries.
Attempts: 1
Last Run: task-001-attempt-001
Last Duration: 335.1s
Completed At: 2026-04-06T02:09:22Z
Notes: The Tidy3D feature checklist is already part of prep and should be consumed here rather than regenerated.

## [COMPLETED] task-002 - Implement the tagged core Simulation, Scene, and Structure data model
Success: Provide the foundational public and IR-facing container models for Simulation, Scene, and Structure, including ordered structure semantics, names, priority behavior, and serialization scaffolding.
Attempts: 1
Last Run: task-002-attempt-001
Last Duration: 318.3s
Completed At: 2026-04-06T02:14:41Z
Notes: This is the container backbone that many later feature tasks depend on.

## [COMPLETED] task-003 - Define the tagged execution IR and serialization contract
Success: Land an inspectable, versioned IR schema and serialization plan that can represent realistic Phase 1 simulations independently of the public API and is suitable for local or remote execution packaging.
Attempts: 1
Last Run: task-003-attempt-001
Last Duration: 249.2s
Completed At: 2026-04-06T02:18:50Z
Notes: The IR should be specific enough to support later feature tasks without redesign.

## [COMPLETED] task-004 - Implement core normalization and validation behavior
Success: Provide validation and normalization passes for names, bounds, basic shape constraints, unsupported-feature errors, and selected warn-and-coerce behaviors that align with the feature checklist.
Attempts: 1
Last Run: task-004-attempt-001
Last Duration: 499.7s
Completed At: 2026-04-06T02:27:10Z
Notes: This task should make later feature work safer instead of treating validation as cleanup at the end.

## [COMPLETED] task-005 - Implement shared model-base and serialization helpers
Success: Provide the shared base-model, tagging, copy-or-update, and serialization helpers needed by the public API and IR layers so later feature tasks build on a common contract.
Attempts: 1
Last Run: task-005-attempt-001
Last Duration: 164.9s
Completed At: 2026-04-06T02:29:54Z
Notes: This keeps tagging and serialization from being reimplemented ad hoc in every feature family.

## [COMPLETED] task-006 - Implement primitive geometry support for Box, Sphere, and Cylinder
Success: Provide public and IR representations plus tests for Box, Sphere, and Cylinder geometries, including bounds, transforms needed for scene compilation, and precedence interaction inside structures.
Attempts: 1
Last Run: task-006-attempt-001
Last Duration: 406.1s
Completed At: 2026-04-06T02:36:41Z
Notes: Start with the simplest Tidy3D geometry primitives first.

## [COMPLETED] task-007 - Implement PolySlab and polygon-driven geometry support
Success: Support PolySlab-style polygon extrusion geometry with explicit scope and limitation documentation, plus tests that cover representative planar photonics cases.
Attempts: 1
Last Run: task-007-attempt-001
Last Duration: 460.5s
Completed At: 2026-04-06T02:44:21Z
Notes: This is one of the most important geometry families for Tidy3D-style compatibility.

## [COMPLETED] task-008 - Implement geometry grouping, transforms, and clip-style wrappers
Success: Provide GeometryGroup-style composition plus the minimum transform and clip operation support needed for credible Phase 1 scene construction.
Attempts: 1
Last Run: task-008-attempt-001
Last Duration: 531.5s
Completed At: 2026-04-06T02:53:13Z
Notes: This task should turn individual primitives into useful scene-building blocks.

## [COMPLETED] task-009 - Implement geometry arrays, precedence, and repeated placement semantics
Success: Support repeated or array-style geometry placement and explicit precedence semantics for ordered structures with tests that verify overlap behavior.
Attempts: 1
Last Run: task-009-attempt-001
Last Duration: 378.3s
Completed At: 2026-04-06T02:59:31Z
Notes: This is where structure ordering becomes concrete.

## [COMPLETED] task-010 - Implement grid controls for UniformGrid, CustomGrid, and GridSpec core paths
Success: Provide the core grid specification surface needed for Phase 1 simulations, including uniform grids, custom boundaries or custom grids, and GridSpec-style composition.
Attempts: 1
Last Run: task-010-attempt-001
Last Duration: 678.8s
Completed At: 2026-04-06T03:10:50Z
Notes: Grid support should be concrete enough to drive later discretization and mode-solver work.

## [COMPLETED] task-011 - Implement AutoGrid and refinement-spec planning surfaces
Success: Support an initial AutoGrid-style and refinement-spec surface with clear limitations and lowering behavior, even if the first meshing policy is simpler than Tidy3D's full behavior.
Attempts: 1
Last Run: task-011-attempt-001
Last Duration: 414.5s
Completed At: 2026-04-06T03:17:44Z
Notes: This task can begin with planning or simplified refinement behavior, but it must not be hand-wavy.

## [COMPLETED] task-012 - Implement SubpixelSpec and core averaging policy models
Success: Provide SubpixelSpec-style controls and the core averaging or staircasing policy representations needed to drive geometry materialization behavior.
Attempts: 1
Last Run: task-012-attempt-001
Last Duration: 629.8s
Completed At: 2026-04-06T03:28:14Z
Notes: This task is about policy modeling before the heavier compiler behavior lands.

## [COMPLETED] task-013 - Implement isotropic Medium, PECMedium, and PMCMedium with coefficient compilation and constitutive kernels
Success: Support the foundational isotropic material family, including plain Medium and perfect-conductor medium variants, with tests for serialization, validation, structure application, coefficient preparation, and the corresponding constitutive-update kernel paths.
Attempts: 1
Last Run: task-013-attempt-001
Last Duration: 589.0s
Completed At: 2026-04-06T03:38:03Z
Notes: These are the baseline material types many later tasks assume exist.

## [COMPLETED] task-014 - Implement PoleResidue medium support with coefficient prep and update kernels
Success: Support PoleResidue-style dispersive media with explicit API and IR models, coefficient preparation, auxiliary-state handling, update-kernel support, and representative tests.
Attempts: 1
Last Run: task-014-attempt-001
Last Duration: 522.2s
Completed At: 2026-04-06T03:46:45Z
Notes: Keep PoleResidue separate because it is the most general dispersive family and often subsumes the others conceptually.

## [COMPLETED] task-015 - Implement Sellmeier medium support with coefficient prep and update kernels
Success: Support Sellmeier media or a clearly documented supported subset, including API and IR models, coefficient preparation, update-kernel support, and representative validation tests.
Attempts: 1
Last Run: task-015-attempt-001
Last Duration: 299.6s
Completed At: 2026-04-06T03:51:45Z
Notes: This stays separate so optical-material support is not hidden inside a large dispersive bucket.

## [COMPLETED] task-016 - Implement Lorentz, Drude, and Debye medium support with update kernels
Success: Support Lorentz, Drude, and Debye medium families or a clearly delimited subset, including API and IR models, coefficient preparation, update-kernel support, and tests that show each family exercises the intended constitutive path.
Attempts: 1
Last Run: task-016-attempt-001
Last Duration: 485.0s
Completed At: 2026-04-06T03:59:50Z
Notes: These families are close enough to share a task without becoming ambiguous if the tests name each family explicitly.

## [COMPLETED] task-017 - Implement anisotropic medium families, Medium2D policy, and anisotropic kernel support
Success: Support the initial anisotropic medium surface, including a clear policy for AnisotropicMedium, FullyAnisotropicMedium, and Medium2D-related behavior, with explicit tests, limitations, coefficient preparation, and anisotropic kernel support.
Attempts: 19
Last Run: task-017-attempt-019
Last Duration: 502.8s
Completed At: 2026-04-06T19:06:42Z
Notes: This task should respect the fact that subpixel smoothing can create effective anisotropy and must land before second-order subpixel materialization is treated as implemented.

## [COMPLETED] task-018 - Implement perturbation, lossy-metal, and advanced-material defer buckets
Success: Provide an explicit Phase 1 handling policy for PerturbationMedium, LossyMetalMedium, custom media, and other advanced material families so the API surface is mapped rather than ignored.
Attempts: 1
Last Run: task-018-attempt-001
Last Duration: 399.5s
Completed At: 2026-04-06T19:13:22Z
Notes: This is intentionally a policy-heavy task if full implementation is not yet realistic.

## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
Attempts: 1
Last Run: task-019-attempt-001
Last Duration: 708.3s
Completed At: 2026-04-06T19:25:10Z
Notes: These are the first boundary primitives the compiler and runtime should rely on.

## [COMPLETED] task-020 - Implement BlochBoundary, symmetry-aware metadata, and phase-aware runtime transforms
Success: Provide BlochBoundary support and the related symmetry-aware metadata and runtime transform support needed for chunk planning and execution lowering.
Attempts: 1
Last Run: task-020-attempt-001
Last Duration: 460.9s
Completed At: 2026-04-06T19:32:51Z
Notes: This task is important because periodic-phase behavior leaks into runtime metadata and not just public API models.

## [COMPLETED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
Attempts: 1
Last Run: task-021-attempt-001
Last Duration: 632.6s
Completed At: 2026-04-06T19:43:24Z
Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.

## [COMPLETED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
Attempts: 1
Last Run: task-022-attempt-001
Last Duration: 524.5s
Completed At: 2026-04-06T19:52:08Z
Notes: StablePML and Absorber are separate enough from baseline PML to justify their own task.

## [RUNNING] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
Attempts: 1
Last Run: task-023-attempt-001
Last Duration: -
Completed At: -
Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.

## [QUEUED] task-024 - Implement Simulation runtime controls for timestep count, shutoff, and convergence policy
Success: Support the core Simulation runtime-control surface needed for practical runs, including run-time or step-count control, shutoff or early-stop criteria, and clearly defined convergence-related behavior.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: This task makes convergence and stop behavior explicit instead of leaving it buried inside the simulation loop.

## [QUEUED] task-025 - Implement detailed simulation logging, progress reporting, and convergence evidence capture
Success: Execution paths emit structured progress logs, stop-reason records, convergence or shutoff evidence, and enough runtime detail to diagnose practical simulation behavior during end-to-end runs.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: Keep this distinct from benchmark metrics so debugging and validation remain first-class.

## [QUEUED] task-026 - Implement GaussianPulse and ContinuousWave source-time profiles
Success: Support GaussianPulse and ContinuousWave source-time profiles with explicit parameter validation, serialization, IR lowering, and representative waveform tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -
Notes: These are the two most common source-time profiles and should be available early.

## [QUEUED] task-027 - Implement BroadbandPulse and CustomSourceTime profiles
Success: Support BroadbandPulse and CustomSourceTime or a clearly delimited subset, including API and IR models, validation behavior, and representative tests.
Attempts: 0
Last Run: -
Last Duration: -
Completed At: -

exec
/bin/bash -lc "rg -n \"ABC|ModeABC|boundary\" src tests phase1 -g '"'!**/.venv/**'"'" in /workspace/autofdtd
 succeeded in 1ms:
phase1/working-memory.md:22:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/working-memory.md:33:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/working-memory.md:53:- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/working-memory.md:54:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/working-memory.md:59:- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
phase1/working-memory.md:92:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/working-memory.md:94:- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
phase1/working-memory.md:103:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/working-memory.md:111:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/working-memory.md:112:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/working-memory.md:113:- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
phase1/loop.prompt.md:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/loop.prompt.md:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/loop.prompt.md:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/loop.spec.md:151:## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/loop.spec.md:152:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/loop.spec.md:157:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/loop.spec.md:167:## [COMPLETED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/loop.spec.md:168:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/loop.spec.md:173:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/loop.spec.md:175:## [COMPLETED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/loop.spec.md:176:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/loop.spec.md:183:## [RUNNING] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/loop.spec.md:184:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/loop.spec.md:189:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/loop.spec.md:280:Success: Support the TFSF source family and any required source-frame metadata, boundary interaction logic, and runtime injection support, or provide explicit Phase 1 limitation handling.
phase1/loop.spec.md:397:Notes: This task isolates the core timestepping spine before material, boundary, and source complications are layered on.
phase1/loop.spec.md:407:## [QUEUED] task-051 - Implement boundary and absorbing-layer kernel stages
phase1/loop.spec.md:413:Notes: Keep boundary runtime support separate from source and material kernel work.
tests/test_containers.py:324:        boundary_spec={"type": "BoundarySpec"},
tests/test_containers.py:482:def test_zero_dim_absorbing_boundary_is_coerced_to_periodic_with_warning() -> None:
tests/test_containers.py:490:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
tests/test_containers.py:493:    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
tests/test_containers.py:494:    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
tests/test_boundaries.py:20:from autofdtd.compiler import BoundaryMode, compile_boundary_spec
tests/test_boundaries.py:24:    apply_boundary_ghosts,
tests/test_boundaries.py:25:    apply_boundary_stages,
tests/test_boundaries.py:26:    boundary_edge_transform,
tests/test_boundaries.py:27:    boundary_kernel_metadata,
tests/test_boundaries.py:30:from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
tests/test_boundaries.py:33:def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
tests/test_boundaries.py:36:        match="periodic boundary opposite a PEC/PMC boundary",
tests/test_boundaries.py:38:        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
tests/test_boundaries.py:40:    assert boundary.minus.type == "PECBoundary"
tests/test_boundaries.py:41:    assert boundary.plus.type == "PECBoundary"
tests/test_boundaries.py:44:def test_simulation_normalizes_supported_boundary_subset() -> None:
tests/test_boundaries.py:48:        boundary_spec={
tests/test_boundaries.py:60:    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
tests/test_boundaries.py:61:    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
tests/test_boundaries.py:62:    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
tests/test_boundaries.py:63:    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
tests/test_boundaries.py:64:    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
tests/test_boundaries.py:65:    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
tests/test_boundaries.py:68:def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
tests/test_boundaries.py:69:    compiled = compile_boundary_spec(
tests/test_boundaries.py:97:    assert compiled.z.minus.pml.boundary_type == "Absorber"
tests/test_boundaries.py:99:    assert compiled.y.plus.pml.boundary_type == "StablePML"
tests/test_boundaries.py:102:        "electric_boundary",
tests/test_boundaries.py:103:        "magnetic_boundary",
tests/test_boundaries.py:113:def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
tests/test_boundaries.py:114:    compiled = compile_boundary_spec(
tests/test_boundaries.py:134:    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
tests/test_boundaries.py:145:def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
tests/test_boundaries.py:146:    boundary_spec = BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
tests/test_boundaries.py:147:    runtime = build_boundary_runtime(boundary_spec, symmetry=(1, 0, -1))
tests/test_boundaries.py:148:    metadata = boundary_kernel_metadata()
tests/test_boundaries.py:153:            boundary_spec=boundary_spec,
tests/test_boundaries.py:168:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_boundaries.py:169:    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
tests/test_boundaries.py:170:    assert simulation_ir.boundary_spec.requires_complex_fields is True
tests/test_boundaries.py:171:    assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
tests/test_boundaries.py:179:            boundary_spec=BoundarySpec(
tests/test_boundaries.py:193:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_boundaries.py:194:    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
tests/test_boundaries.py:195:    assert simulation_ir.boundary_spec.x.minus.num_layers == 8
tests/test_boundaries.py:196:    assert simulation_ir.boundary_spec.x.minus.parameters.kappa_max == pytest.approx(4.0)
tests/test_boundaries.py:197:    assert simulation_ir.boundary_spec.pml_axes == ("x",)
tests/test_boundaries.py:198:    assert simulation_ir.boundary_spec.pml_faces == ("x.minus", "x.plus")
tests/test_boundaries.py:199:    assert simulation_ir.boundary_spec.reflective_axes == ("x",)
tests/test_boundaries.py:207:            boundary_spec=BoundarySpec(
tests/test_boundaries.py:220:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_boundaries.py:221:    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
tests/test_boundaries.py:222:    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
tests/test_boundaries.py:223:    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
tests/test_boundaries.py:224:    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
tests/test_boundaries.py:225:    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
tests/test_boundaries.py:226:    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
tests/test_boundaries.py:227:    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
tests/test_boundaries.py:228:    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)
tests/test_boundaries.py:231:def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
tests/test_boundaries.py:232:    runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
tests/test_boundaries.py:235:    boundary_values = runtime.transform_boundary_values(
tests/test_boundaries.py:246:    assert tuple(boundary_values[0]) == pytest.approx((1.0j, 2.0j, 3.0j))
tests/test_boundaries.py:249:        boundary_edge_transform(vector, runtime.compiled.x.minus, field_family="electric")[0]
tests/test_boundaries.py:261:    compiled = compile_boundary_spec(
tests/test_boundaries.py:268:    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)
tests/test_boundaries.py:277:def test_pml_boundary_stages_absorb_boundary_energy() -> None:
tests/test_boundaries.py:278:    compiled = compile_boundary_spec(
tests/test_boundaries.py:283:    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
tests/test_boundaries.py:292:        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
tests/test_boundaries.py:303:def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
tests/test_boundaries.py:304:    compiled = compile_boundary_spec(
tests/test_boundaries.py:316:    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
tests/test_boundaries.py:325:        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
tests/test_boundaries.py:336:    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
tests/test_boundaries.py:343:    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
tests/test_boundaries.py:349:def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
tests/test_boundaries.py:350:    boundary = Boundary.bloch(0.125)
tests/test_boundaries.py:351:    assert isinstance(boundary.plus, BlochBoundary)
tests/test_boundaries.py:352:    assert boundary.plus.bloch_phase == pytest.approx(
tests/test_ir.py:138:        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
tests/test_ir.py:185:    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
tests/test_ir.py:186:    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
phase1/runs/task-023-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-023-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-023-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-023-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-023-attempt-001/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-023-attempt-001/codex.stdout.txt:161:- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-023-attempt-001/codex.stdout.txt:162:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-023-attempt-001/codex.stdout.txt:167:- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
phase1/runs/task-023-attempt-001/codex.stdout.txt:200:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/runs/task-023-attempt-001/codex.stdout.txt:202:- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
phase1/runs/task-023-attempt-001/codex.stdout.txt:211:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-023-attempt-001/codex.stdout.txt:219:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/runs/task-023-attempt-001/codex.stdout.txt:220:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/runs/task-023-attempt-001/codex.stdout.txt:221:- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
phase1/runs/task-023-attempt-001/codex.stdout.txt:226:Title: Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-023-attempt-001/codex.stdout.txt:227:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-023-attempt-001/codex.stdout.txt:228:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-023-attempt-001/codex.stdout.txt:231:  → Inspect task board and current boundary implementation to identify ABC-related gaps and existing defer policy surfaces
phase1/runs/task-023-attempt-001/codex.stdout.txt:232:  • Implement Phase 1 ABC/ModeABC public models, IR/compiler/runtime policy, and validation/defer behavior
phase1/runs/task-006-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-006-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-006-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-006-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/prompt.txt:138:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/logs/events.jsonl:5:{"artifacts": [{"description": "Frozen tagged container models and precedence/serialization helpers for `Structure`, `Scene`, and `Simulation`.", "path": "/workspace/autofdtd/src/autofdtd/core/containers.py"}, {"description": "Public API re-exports for the foundational container models.", "path": "/workspace/autofdtd/src/autofdtd/api/simulation.py"}, {"description": "API namespace exports exposing the new container backbone.", "path": "/workspace/autofdtd/src/autofdtd/api/__init__.py"}, {"description": "Core namespace exports exposing the tagged container layer.", "path": "/workspace/autofdtd/src/autofdtd/core/__init__.py"}, {"description": "Container-model tests for names, precedence ordering, conductor priority defaults, and JSON serialization.", "path": "/workspace/autofdtd/tests/test_containers.py"}, {"description": "Architecture notes updated to describe the current container backbone and separation from future IR work.", "path": "/workspace/autofdtd/docs/architecture.md"}, {"description": "Condensed handoff notes capturing the container-model decisions for later tasks.", "path": "/workspace/autofdtd/phase1/working-memory.md"}, {"description": "Task board updated to mark `task-002` completed.", "path": "/workspace/autofdtd/phase1/loop.spec.md"}, {"description": "Recorded task output matching the Phase 1 result schema.", "path": "/workspace/autofdtd/phase1/runs/task-002-attempt-001/result.json"}], "attempt": 1, "duration_s": 318.3, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "`task-003` should now define the separate execution IR contract and treat the current container payload helpers as an interim handoff format rather than the final IR schema.", "key_findings": ["`src/autofdtd/core/containers.py` now provides frozen pydantic container models with explicit `type` tags, JSON-ready payload helpers, and minimal disk serialization entrypoints.", "`Scene` preserves ordered structure semantics and exposes `structures_in_resolution_order()` so later scene compilation can apply structures by resolved priority and insertion order without re-deriving precedence rules.", "`StructurePriorityMode.CONDUCTOR` is mapped early using medium type tags so implicit priority behavior can distinguish dielectric, lossy-metal, and PEC structures before concrete material classes land.", "`Simulation` now carries the Phase 1 shell fields from the feature checklist, including domain size, run time, sources, monitors, boundary and grid references, symmetry, shutoff, courant, and subpixel references.", "Focused tests now cover name normalization, duplicate-name rejection, ordered overlap behavior, conductor-priority defaults, and tagged JSON serialization, and the repo passes `pytest`, `ruff`, and `mypy`."], "message": "Implemented the first tagged immutable container backbone for `Simulation`, `Scene`, and `Structure`, including ordered structure precedence helpers, name validation, and JSON-ready serialization scaffolding for later IR work.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-002-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-002-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-002-attempt-001"}, "result_status": "completed", "run_id": "task-002-attempt-001", "status": "COMPLETED", "task_id": "task-002", "task_title": "Implement the tagged core Simulation, Scene, and Structure data model", "tasks_remaining": 57, "ts": "2026-04-06T02:14:41Z"}
phase1/logs/events.jsonl:9:{"artifacts": [{"description": "Shared Phase 1 validation and normalization helpers for tagged component payloads and simulation-bound checks.", "path": "src/autofdtd/core/validation.py"}, {"description": "Integrated container-level validators for geometry, media, boundaries, normalize-index, and deferred-feature policies.", "path": "src/autofdtd/core/containers.py"}, {"description": "Added feature lookup helpers and extra tracked Phase 1 feature tags used by validation.", "path": "src/autofdtd/planning.py"}, {"description": "Validation coverage for normalization, shape constraints, unsupported features, domain bounds, and zero-dimension boundary coercion.", "path": "tests/test_containers.py"}, {"description": "Architecture notes describing the new shared validation layer and its current responsibilities.", "path": "docs/architecture.md"}, {"description": "Distilled task handoff notes for later feature-family tasks to reuse the core validation path.", "path": "phase1/working-memory.md"}, {"description": "Structured task result artifact for this loop iteration.", "path": "phase1/runs/task-004-attempt-001/result.json"}], "attempt": 1, "duration_s": 499.7, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Verification passed with `ruff check`, `pytest -q`, and `mypy src`. Geometry, material, boundary, source, monitor, and grid tasks should extend `autofdtd.core.validation` rather than duplicating checklist-driven policy checks in each family.", "key_findings": ["Added `autofdtd.core.validation` as the shared normalization layer for tagged component payloads, covering name trimming, vector coercion, primitive geometry checks for `Box`/`Sphere`/`Cylinder`, structure-bound checks against the simulation domain, and zero-size-axis boundary coercion to `Periodic`.", "`Simulation` now exposes explicit Phase 1 control-policy fields for `normalize_index`, `relax_courant`, `low_freq_smoothing`, and `lumped_elements`, with implemented validation for normalize-index and clear fail-fast errors for deferred or rejected core features.", "Unsupported feature handling is now tied directly to the local feature matrix through lookup helpers in `autofdtd.planning`, so deferred and reject-clearly feature tags fail with explicit messages instead of being accepted silently or rejected as unknown extras.", "Coverage now includes normalization, primitive shape errors, out-of-domain warnings and errors, zero-dimension absorbing-boundary coercion, normalize-index validation, and planning lookups, with docs and working memory updated to point later tasks at the shared validator path."], "message": "Implemented checklist-aware core normalization and validation for the Phase 1 container layer, including primitive-shape sanity checks, simulation-bound validation, explicit unsupported-feature errors, normalize-index handling, zero-dimension boundary coercion, and supporting tests/docs.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-004-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-004-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-004-attempt-001"}, "result_status": "completed", "run_id": "task-004-attempt-001", "status": "COMPLETED", "task_id": "task-004", "task_title": "Implement core normalization and validation behavior", "tasks_remaining": 55, "ts": "2026-04-06T02:27:10Z"}
phase1/logs/events.jsonl:21:{"artifacts": [{"description": "Phase 1 manual grid models, resolution helpers, and domain-to-boundary lowering.", "path": "/workspace/autofdtd/src/autofdtd/grid/specs.py"}, {"description": "`Simulation.resolved_grid()` integration and grid-spec validation during simulation construction.", "path": "/workspace/autofdtd/src/autofdtd/core/containers.py"}, {"description": "Checklist-aware normalization for `GridSpec` and explicit rejection of not-yet-implemented grid families.", "path": "/workspace/autofdtd/src/autofdtd/core/validation.py"}, {"description": "Typed grid execution IR and lowering path with resolved grid metadata.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Focused grid behavior tests covering snapping, explicit boundaries, and resolved-grid shape metrics.", "path": "/workspace/autofdtd/tests/test_grid.py"}, {"description": "Grid-scope documentation for the new Phase 1 manual discretization surface.", "path": "/workspace/autofdtd/docs/grid.md"}], "attempt": 1, "duration_s": 678.8, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "The next grid task can add `AutoGrid`, refinement metadata, and related planning fields onto the existing `GridSpec`/`ResolvedGrid` seam without changing simulation or IR contracts.", "key_findings": ["`src/autofdtd/grid/specs.py` now provides concrete Phase 1 grid models plus `ResolvedGrid`/`ResolvedGridAxis` so simulations can resolve axis boundaries before compiler/runtime work lands.", "`Simulation.resolved_grid()` and `core.validation` now enforce a strict top-level `GridSpec` contract, validate manual grid compatibility against simulation bounds, and reject planned-but-not-yet-implemented grid families like `AutoGrid` explicitly.", "`src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR (`GridSpecIR`, per-axis IR, and resolved grid metadata), giving later discretization and mode-solver tasks an inspectable execution-side mesh contract.", "Public exports, examples, and docs were updated so the grid surface is part of the Tidy3D-inspired API rather than an opaque dict payload.", "Verification passed with `python -m ruff check src tests` and `python -m pytest` (47 tests passed)."], "message": "Implemented the Phase 1 manual grid surface with typed `UniformGrid`, `CustomGrid`, `CustomGridBoundaries`, and `GridSpec` models, simulation-time grid resolution, and typed grid IR lowering with resolved boundary metadata.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-010-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-010-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-010-attempt-001"}, "result_status": "completed", "run_id": "task-010-attempt-001", "status": "COMPLETED", "task_id": "task-010", "task_title": "Implement grid controls for UniformGrid, CustomGrid, and GridSpec core paths", "tasks_remaining": 49, "ts": "2026-04-06T03:10:50Z"}
phase1/logs/events.jsonl:33:{"artifacts": [{"description": "Added Lorentz, Drude, and Debye public models plus analytical-to-PoleResidue lowering helpers.", "path": "/workspace/autofdtd/src/autofdtd/materials/dispersive.py"}, {"description": "Extended Phase 1 medium dispatch and exports to include Lorentz, Drude, and Debye.", "path": "/workspace/autofdtd/src/autofdtd/materials/__init__.py"}, {"description": "Added Lorentz, Drude, and Debye coefficient compilation on the shared PoleResidue runtime contract plus explicit stiff-pole rejection.", "path": "/workspace/autofdtd/src/autofdtd/compiler/materials.py"}, {"description": "Added typed LorentzIR, DrudeIR, and DebyeIR lowering and medium dispatch integration.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Updated constitutive metadata so the shared dispersive kernel path reports Lorentz, Drude, and Debye support.", "path": "/workspace/autofdtd/src/autofdtd/kernels/materials.py"}, {"description": "Added compiled analytical-dispersion examples covering Lorentz, Drude, and Debye lowering and coefficient generation.", "path": "/workspace/autofdtd/src/autofdtd/examples/materials.py"}, {"description": "Documented the added analytical dispersive families and the explicit stiff-pole compile rejection policy.", "path": "/workspace/autofdtd/docs/materials.md"}, {"description": "Added named Lorentz, Drude, and Debye validation, IR, compilation, and shared-kernel tests.", "path": "/workspace/autofdtd/tests/test_materials.py"}, {"description": "Recorded the new analytical-dispersion decisions and the stiff-pole subset boundary for later tasks.", "path": "/workspace/autofdtd/phase1/working-memory.md"}, {"description": "Recorded the structured task result for this run.", "path": "/workspace/autofdtd/phase1/runs/task-016-attempt-001/result.json"}], "attempt": 1, "duration_s": 485.0, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Verification passed with `ruff check src tests`, `pytest -q tests/test_materials.py`, and `python -m compileall src`. A repo-wide `mypy src` run still reports pre-existing unrelated typing issues outside this task\u2019s scope.", "key_findings": ["`autofdtd.materials` now exposes `Lorentz`, `Drude`, and `Debye` models with analytical `eps_model()` evaluation, family-specific validation, and lowering to equivalent `PoleResidue` payloads.", "`autofdtd.ir` now emits typed `LorentzIR`, `DrudeIR`, and `DebyeIR` payloads, and the compiler preserves the originating family tag while compiling onto the existing `PoleResidueMaterialCoefficients` auxiliary-state contract.", "Compilation now fails explicitly when converted dispersive poles are too stiff for the current exact-hold timestep path, which clearly delimits the currently supported subset instead of emitting unusable coefficients.", "Examples, docs, tests, and working memory were updated so the new families are exercised and the subset boundary is recorded for later material/runtime tasks."], "message": "Implemented passive homogeneous Lorentz, damped Drude, and passive homogeneous Debye medium support across the public API, typed IR, coefficient compilation, and the shared PoleResidue-family update path, with explicit tests for each family.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-016-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-016-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-016-attempt-001"}, "result_status": "completed", "run_id": "task-016-attempt-001", "status": "COMPLETED", "task_id": "task-016", "task_title": "Implement Lorentz, Drude, and Debye medium support with update kernels", "tasks_remaining": 43, "ts": "2026-04-06T03:59:50Z"}
phase1/logs/events.jsonl:74:{"attempt": 1, "duration_s": null, "event": "task_selected", "message": "Task selected and marked RUNNING.", "paths": {"run_dir": "/workspace/autofdtd/phase1/runs/task-019-attempt-001"}, "run_id": "task-019-attempt-001", "status": "RUNNING", "task_id": "task-019", "task_title": "Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers", "tasks_remaining": 40, "ts": "2026-04-06T19:13:22Z"}
phase1/logs/events.jsonl:75:{"artifacts": [{"description": "Typed non-absorbing boundary public models and normalization helpers.", "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py"}, {"description": "Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling.", "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py"}, {"description": "Concrete ghost-cell update helpers and boundary kernel metadata.", "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py"}, {"description": "Runtime wrapper that applies compiled boundary ghost updates.", "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py"}, {"description": "Typed boundary IR classes and lowering helpers.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors.", "path": "/workspace/autofdtd/tests/test_boundaries.py"}, {"description": "Boundary scope documentation for the implemented Phase 1 subset.", "path": "/workspace/autofdtd/docs/boundaries.md"}, {"description": "Structured task result matching the Phase 1 output schema.", "path": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json"}], "attempt": 1, "duration_s": 708.3, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.", "key_findings": ["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.", "Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.", "`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.", "Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.", "Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."], "message": "Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-019-attempt-001"}, "result_status": "completed", "run_id": "task-019-attempt-001", "status": "COMPLETED", "task_id": "task-019", "task_title": "Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers", "tasks_remaining": 40, "ts": "2026-04-06T19:25:10Z"}
phase1/logs/events.jsonl:77:{"artifacts": [{"description": "Added `BlochBoundary`, public constructors, and boundary-pair validation rules.", "path": "src/autofdtd/boundaries/models.py"}, {"description": "Extended compiled boundary metadata with Bloch phase factors and symmetry-axis transforms.", "path": "src/autofdtd/compiler/boundaries.py"}, {"description": "Added typed Bloch and symmetry IR payloads plus boundary-spec lowering with complex-field requirements.", "path": "src/autofdtd/ir/models.py"}, {"description": "Implemented phase-aware Bloch ghost updates and reusable boundary/symmetry transform helpers.", "path": "src/autofdtd/kernels/boundaries.py"}, {"description": "Extended runtime boundary wrapper to compile symmetry metadata and expose transform helpers.", "path": "src/autofdtd/runtime/boundaries.py"}, {"description": "Added Bloch boundary, symmetry metadata, phase-transform, and runtime/IR coverage.", "path": "tests/test_boundaries.py"}, {"description": "Recorded Bloch runtime behavior and symmetry-metadata decisions for later boundary tasks.", "path": "phase1/working-memory.md"}], "attempt": 1, "duration_s": 460.9, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Absorbing-boundary tasks can now extend the existing `CompiledBoundarySpec` contract without redesigning periodic/Bloch/symmetry lowering. No multi-GPU smoke was attempted because this task only touched boundary metadata and local ghost-transform behavior.", "key_findings": ["`BlochBoundary` is now a first-class public boundary edge with `Boundary.bloch(...)`, strict both-sides validation, and explicit complex phase metadata derived from `bloch_vec`.", "Boundary IR now distinguishes periodic, Bloch, and reflective axes, marks `requires_complex_fields`, and carries per-axis symmetry sign metadata for electric and magnetic field families.", "Compiled boundary metadata now includes Bloch exchange kinds, per-face phase factors, active symmetry axes, and reusable symmetry transforms for future chunk-planning and reduced-domain exchange paths.", "Boundary ghost updates now apply conjugate phase wrapping on minus Bloch faces and forward phase wrapping on plus faces, while separate helper transforms expose boundary-edge and symmetry remaps directly to runtime callers.", "Focused validation passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py` and `python -m compileall src/autofdtd`."], "message": "Implemented BlochBoundary support plus symmetry-aware boundary metadata and phase-aware runtime transforms across the public API, IR, compiler, kernels, runtime wrapper, and tests.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-020-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-020-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-020-attempt-001"}, "result_status": "completed", "run_id": "task-020-attempt-001", "status": "COMPLETED", "task_id": "task-020", "task_title": "Implement BlochBoundary, symmetry-aware metadata, and phase-aware runtime transforms", "tasks_remaining": 39, "ts": "2026-04-06T19:32:51Z"}
phase1/logs/events.jsonl:78:{"attempt": 1, "duration_s": null, "event": "task_selected", "message": "Task selected and marked RUNNING.", "paths": {"run_dir": "/workspace/autofdtd/phase1/runs/task-021-attempt-001"}, "run_id": "task-021-attempt-001", "status": "RUNNING", "task_id": "task-021", "task_title": "Implement PML boundary support with coefficient prep and kernel stages", "tasks_remaining": 38, "ts": "2026-04-06T19:32:51Z"}
phase1/logs/events.jsonl:79:{"artifacts": [{"description": "Baseline PML public models and boundary constructors.", "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py"}, {"description": "Typed PML IR models and boundary-spec PML metadata lowering.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Compiled PML coefficient preparation and boundary-stage metadata.", "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py"}, {"description": "Boundary kernel stages including PML layer damping with persistent face memory.", "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py"}, {"description": "Runtime state allocation and staged boundary application wrapper.", "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py"}, {"description": "Representative PML validation and absorption tests.", "path": "/workspace/autofdtd/tests/test_boundaries.py"}, {"description": "Boundary runtime snapshots including a compact PML absorption example.", "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py"}, {"description": "Updated Phase 1 handoff notes for baseline PML support and extension seams.", "path": "/workspace/autofdtd/phase1/working-memory.md"}], "attempt": 1, "duration_s": 632.6, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "Current PML support is intentionally narrow: it uses a staged damping-layer backend with persistent face memory rather than a full split-field CPML. `StablePML` and `Absorber` should extend the compiled-face/state contract in task-022.", "key_findings": ["Added Tidy3D-shaped baseline `PML` and `PMLParams` models plus `Boundary.pml(...)` / `BoundarySpec.pml(...)` constructors and zero-dimension coercion behavior.", "Extended boundary IR with typed `PMLIR` and `PMLParamsIR`; `BoundarySpecIR` now records `pml_axes` and `pml_faces` so scene lowering preserves absorbing-face metadata explicitly.", "Boundary compilation now prepares per-face polynomial PML coefficient profiles (`sigma`, `kappa`, `alpha`, attenuation, memory terms) and advertises explicit `electric_pml` / `magnetic_pml` stage ordering.", "Runtime now allocates persistent per-face PML memory via `allocate_pml_boundary_state()` and applies PML stages through `apply_boundary_stages()` / `BoundaryRuntime.apply()`.", "Representative tests now cover typed PML lowering, coefficient prep, face-state allocation, and staged absorption behavior; full suite passes with `PYTHONPATH=src python -m pytest -q` (110 passed)."], "message": "Implemented baseline PML boundary support across the public API, typed IR, boundary compilation, runtime state allocation, staged kernel updates, examples, and absorption-focused tests.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-021-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-021-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-021-attempt-001"}, "result_status": "completed", "run_id": "task-021-attempt-001", "status": "COMPLETED", "task_id": "task-021", "task_title": "Implement PML boundary support with coefficient prep and kernel stages", "tasks_remaining": 38, "ts": "2026-04-06T19:43:24Z"}
phase1/logs/events.jsonl:80:{"attempt": 1, "duration_s": null, "event": "task_selected", "message": "Task selected and marked RUNNING.", "paths": {"run_dir": "/workspace/autofdtd/phase1/runs/task-022-attempt-001"}, "run_id": "task-022-attempt-001", "status": "RUNNING", "task_id": "task-022", "task_title": "Implement StablePML and Absorber boundary support with coefficient prep and kernel stages", "tasks_remaining": 37, "ts": "2026-04-06T19:43:24Z"}
phase1/logs/events.jsonl:81:{"artifacts": [{"description": "Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families.", "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py"}, {"description": "Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata.", "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py"}, {"description": "Extended staged boundary kernels to handle StablePML and Absorber runtime modes.", "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py"}, {"description": "Updated runtime state allocation so all absorbing families share the staged boundary execution path.", "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py"}, {"description": "Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`.", "path": "/workspace/autofdtd/src/autofdtd/ir/models.py"}, {"description": "Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption.", "path": "/workspace/autofdtd/tests/test_boundaries.py"}, {"description": "Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits.", "path": "/workspace/autofdtd/docs/boundaries.md"}, {"description": "Structured task result for this loop iteration.", "path": "/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json"}], "attempt": 1, "duration_s": 524.5, "error_summary": "", "event": "codex_finished", "exit_code": 0, "follow_up_notes": "The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.", "key_findings": ["`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.", "Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.", "Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.", "The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.", "Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."], "message": "Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.", "next_action": "none", "paths": {"codex_stdout": "/workspace/autofdtd/phase1/runs/task-022-attempt-001/codex.stdout.txt", "result_json": "/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json", "run_dir": "/workspace/autofdtd/phase1/runs/task-022-attempt-001"}, "result_status": "completed", "run_id": "task-022-attempt-001", "status": "COMPLETED", "task_id": "task-022", "task_title": "Implement StablePML and Absorber boundary support with coefficient prep and kernel stages", "tasks_remaining": 37, "ts": "2026-04-06T19:52:08Z"}
phase1/logs/events.jsonl:82:{"attempt": 1, "duration_s": null, "event": "task_selected", "message": "Task selected and marked RUNNING.", "paths": {"run_dir": "/workspace/autofdtd/phase1/runs/task-023-attempt-001"}, "run_id": "task-023-attempt-001", "status": "RUNNING", "task_id": "task-023", "task_title": "Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy", "tasks_remaining": 36, "ts": "2026-04-06T19:52:08Z"}
phase1/runs/task-023-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-023-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-023-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-023-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-023-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-023-attempt-001/prompt.txt:149:- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-023-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-023-attempt-001/prompt.txt:155:- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
phase1/runs/task-023-attempt-001/prompt.txt:188:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/runs/task-023-attempt-001/prompt.txt:190:- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
phase1/runs/task-023-attempt-001/prompt.txt:199:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-023-attempt-001/prompt.txt:207:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/runs/task-023-attempt-001/prompt.txt:208:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/runs/task-023-attempt-001/prompt.txt:209:- Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
phase1/runs/task-023-attempt-001/prompt.txt:214:Title: Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-023-attempt-001/prompt.txt:215:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-023-attempt-001/prompt.txt:216:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
src/autofdtd/geometry/polyslab.py:113:    """Return whether a 2D point lies inside or on the boundary of a simple polygon."""
phase1/runs/task-017-attempt-007/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-007/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-007/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-007/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-007/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-007/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/runtime/boundaries.py:1:"""Runtime-facing boundary orchestration for the foundational Phase 1 surface."""
src/autofdtd/runtime/boundaries.py:14:    compile_boundary_spec,
src/autofdtd/runtime/boundaries.py:19:    apply_boundary_stages,
src/autofdtd/runtime/boundaries.py:20:    boundary_edge_transform,
src/autofdtd/runtime/boundaries.py:25:def allocate_pml_boundary_state(
src/autofdtd/runtime/boundaries.py:26:    compiled_boundary_spec: CompiledBoundarySpec,
src/autofdtd/runtime/boundaries.py:31:    """Allocate persistent absorbing-boundary memory arrays for all active faces."""
src/autofdtd/runtime/boundaries.py:38:    for axis_boundary in compiled_boundary_spec.axes():
src/autofdtd/runtime/boundaries.py:39:        axis_index = "xyz".index(axis_boundary.axis)
src/autofdtd/runtime/boundaries.py:41:            edge = getattr(axis_boundary, side)
src/autofdtd/runtime/boundaries.py:51:                axis=axis_boundary.axis,
src/autofdtd/runtime/boundaries.py:56:            face_key = f"{axis_boundary.axis}.{side}"
src/autofdtd/runtime/boundaries.py:59:                axis=axis_boundary.axis,
src/autofdtd/runtime/boundaries.py:68:    """Small runtime wrapper around compiled Phase 1 boundary metadata."""
src/autofdtd/runtime/boundaries.py:72:        boundary_spec: object | None = None,
src/autofdtd/runtime/boundaries.py:80:        self.compiled = compile_boundary_spec(
src/autofdtd/runtime/boundaries.py:81:            boundary_spec,
src/autofdtd/runtime/boundaries.py:87:            allocate_pml_boundary_state(self.compiled, field_shape, dtype=field_dtype)
src/autofdtd/runtime/boundaries.py:103:        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
src/autofdtd/runtime/boundaries.py:115:        return apply_boundary_stages(
src/autofdtd/runtime/boundaries.py:122:    def transform_boundary_values(
src/autofdtd/runtime/boundaries.py:131:        return boundary_edge_transform(values, edge, field_family=field_family)
src/autofdtd/runtime/boundaries.py:145:def build_boundary_runtime(
src/autofdtd/runtime/boundaries.py:146:    boundary_spec: object | None = None,
src/autofdtd/runtime/boundaries.py:154:    """Build the default Phase 1 boundary runtime wrapper."""
src/autofdtd/runtime/boundaries.py:157:        boundary_spec,
src/autofdtd/runtime/boundaries.py:169:    compiled_boundary_spec: CompiledBoundarySpec,
src/autofdtd/runtime/boundaries.py:173:    """Apply boundary ghosts and PML stages using a precompiled specification."""
src/autofdtd/runtime/boundaries.py:175:    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
phase1/runs/task-017-attempt-016/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-016/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-016/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-016/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-016/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-016/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-022-attempt-001/result.json:1:{"task_id":"task-022","status":"completed","summary":"Implemented `StablePML` and `Absorber` boundary support across the public API, typed IR, boundary compiler, staged runtime path, examples, docs, and representative absorption tests.","key_findings":["`StablePML` and `Absorber` are now first-class public boundary edges with Tidy3D-aligned defaults, explicit `Boundary` / `BoundarySpec` constructors, and zero-dimension coercion behavior shared with baseline `PML`.","Boundary IR now distinguishes `PML`, `StablePML`, and `Absorber` through typed payloads (`PMLIR`, `StablePMLIR`, `AbsorberIR`) plus family-specific axis and face metadata on `BoundarySpecIR`.","Runtime compilation keeps the existing staged damping-layer seam: `StablePML` lowers onto the baseline PML coefficient path with a separate profile tag, while `Absorber` lowers into conductivity-only attenuation coefficients with a reflective outer-wall policy.","The absorbing-boundary runtime and state allocator now handle all three absorbing families through the shared staged kernel path, while kernel metadata advertises explicit `stable_pml` and `absorber` mode support.","Focused verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_imports.py tests/test_containers.py` and `PYTHONPATH=src python -m compileall src/autofdtd`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added `AbsorberParams`, `StablePML`, `Absorber`, and public constructors for the new absorbing boundary families."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Extended absorbing-boundary compilation to prepare StablePML and Absorber coefficient profiles and per-family face metadata."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Extended staged boundary kernels to handle StablePML and Absorber runtime modes."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Updated runtime state allocation so all absorbing families share the staged boundary execution path."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed IR lowering for `StablePML`, `Absorber`, and `AbsorberParams`."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added representative tests for StablePML and Absorber compilation, IR lowering, state allocation, reflective ghost behavior, and staged absorption."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Updated boundary-scope documentation to cover the implemented absorbing-boundary subset and current limits."},{"path":"/workspace/autofdtd/phase1/runs/task-022-attempt-001/result.json","description":"Structured task result for this loop iteration."}],"error_summary":"","follow_up_notes":"The current `StablePML` implementation is a preset-driven extension of the staged PML backend, and `Absorber` is a conductivity-ramp subset with a reflective outer wall. `task-023` should build ABC-family behavior on the existing absorbing-face metadata without redesigning the shared staged boundary seam.","next_action":"none"}
src/autofdtd/runtime/__init__.py:5:    allocate_pml_boundary_state,
src/autofdtd/runtime/__init__.py:7:    build_boundary_runtime,
src/autofdtd/runtime/__init__.py:12:    "allocate_pml_boundary_state",
src/autofdtd/runtime/__init__.py:14:    "build_boundary_runtime",
phase1/runs/task-017-attempt-016/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-016/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-016/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-016/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-016/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-016/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-007/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-007/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-007/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-007/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-007/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-007/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/planning.py:202:    FeatureEntry("boundaries", "Boundary", FeatureStatus.IMPLEMENT, "Per-axis boundary pair."),
src/autofdtd/planning.py:203:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
src/autofdtd/planning.py:204:    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
src/autofdtd/planning.py:205:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
src/autofdtd/planning.py:206:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
src/autofdtd/planning.py:211:        "Phase-aware periodic boundary.",
src/autofdtd/planning.py:221:    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
src/autofdtd/planning.py:222:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-017-attempt-015/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-015/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-015/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-015/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-015/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-015/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-018/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-018/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-018/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-018/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-018/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-018/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-018/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-018/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-018/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-018/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-018/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-018/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-015/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-015/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-015/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-015/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-015/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-015/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/examples/boundaries.py:1:"""Examples for the Phase 1 boundary subset."""
src/autofdtd/examples/boundaries.py:8:from autofdtd.compiler import compile_boundary_spec
src/autofdtd/examples/boundaries.py:9:from autofdtd.runtime import build_boundary_runtime
src/autofdtd/examples/boundaries.py:12:def boundary_runtime_snapshot() -> dict[str, object]:
src/autofdtd/examples/boundaries.py:13:    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
src/autofdtd/examples/boundaries.py:15:    compiled = compile_boundary_spec(
src/autofdtd/examples/boundaries.py:28:    runtime = build_boundary_runtime(
src/autofdtd/examples/boundaries.py:48:def absorbing_boundary_snapshot() -> dict[str, object]:
src/autofdtd/examples/boundaries.py:51:    compiled = compile_boundary_spec(
src/autofdtd/examples/boundaries.py:68:        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
phase1/runs/task-017-attempt-006/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-006/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-006/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-006/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-006/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-006/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-017/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-017/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-017/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-017/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-017/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-017/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-014/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-014/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-014/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-014/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-014/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-014/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-006/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-006/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-006/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-006/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-006/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-006/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-017/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-017/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-017/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-017/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-017/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-017/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-014/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-014/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-014/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-014/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-014/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-014/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/examples/__init__.py:3:from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
src/autofdtd/examples/__init__.py:17:    "boundary_runtime_snapshot",
phase1/runs/task-017-attempt-013/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-013/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-013/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-013/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-013/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-013/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-005/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-005/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-005/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-005/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-005/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-005/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-013/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-013/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-013/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-013/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-013/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-013/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-005/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-005/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-005/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-005/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-005/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-005/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-012/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-012/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-012/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-012/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-012/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-012/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-012/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-012/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-012/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-012/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-012/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-012/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-004/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-004/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-004/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-004/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-004/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-004/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-011/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-011/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-011/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-011/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-011/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-011/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-011/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-011/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-011/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-011/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-011/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-011/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/core/validation.py:22:    boundary_spec_model_from_value,
src/autofdtd/core/validation.py:185:    if context == "boundary_spec":
src/autofdtd/core/validation.py:186:        return _normalize_boundary_spec(normalized)
src/autofdtd/core/validation.py:227:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
src/autofdtd/core/validation.py:230:    if isinstance(boundary_spec, BoundarySpec):
src/autofdtd/core/validation.py:234:            axis_boundary = boundary_spec[axis_name]
src/autofdtd/core/validation.py:236:                updated[axis_name] = axis_boundary
src/autofdtd/core/validation.py:238:            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
src/autofdtd/core/validation.py:240:            changed = changed or coerced is not axis_boundary
src/autofdtd/core/validation.py:241:        return boundary_spec.copy_update(**updated) if changed else boundary_spec
src/autofdtd/core/validation.py:243:    if not isinstance(boundary_spec, Mapping):
src/autofdtd/core/validation.py:244:        return boundary_spec
src/autofdtd/core/validation.py:246:    updated = dict(boundary_spec)
src/autofdtd/core/validation.py:252:        axis_boundary = updated.get(axis_name)
src/autofdtd/core/validation.py:253:        if axis_boundary is None:
src/autofdtd/core/validation.py:255:        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
src/autofdtd/core/validation.py:256:        if coerced is not axis_boundary:
src/autofdtd/core/validation.py:260:    return updated if changed else boundary_spec
src/autofdtd/core/validation.py:367:    if context == "boundary_spec" and feature_name == "PMLParams":
src/autofdtd/core/validation.py:369:    if context == "boundary_spec" and feature_name == "AbsorberParams":
src/autofdtd/core/validation.py:504:def _normalize_boundary_spec(component: object) -> object:
src/autofdtd/core/validation.py:509:    boundary_type = component_type_name(component)
src/autofdtd/core/validation.py:510:    if boundary_type != "BoundarySpec":
src/autofdtd/core/validation.py:511:        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
src/autofdtd/core/validation.py:512:    boundary_tags = set(_iter_type_tags(component))
src/autofdtd/core/validation.py:513:    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
src/autofdtd/core/validation.py:514:        return boundary_spec_model_from_value(component).model_dump(
src/autofdtd/core/validation.py:603:def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
src/autofdtd/core/validation.py:604:    if isinstance(boundary, Boundary):
src/autofdtd/core/validation.py:608:            edge = boundary[side]
src/autofdtd/core/validation.py:609:            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
src/autofdtd/core/validation.py:612:        return boundary.copy_update(**updated) if changed else boundary
src/autofdtd/core/validation.py:613:    if isinstance(boundary, (Periodic, BlochBoundary, PECBoundary, PMCBoundary)):
src/autofdtd/core/validation.py:614:        return boundary
src/autofdtd/core/validation.py:615:    if isinstance(boundary, (PML, StablePML, Absorber)):
src/autofdtd/core/validation.py:622:    if not isinstance(boundary, Mapping):
src/autofdtd/core/validation.py:623:        return boundary
src/autofdtd/core/validation.py:625:    boundary_type = component_type_name(boundary)
src/autofdtd/core/validation.py:626:    if boundary_type == "BlochBoundary":
src/autofdtd/core/validation.py:628:    if boundary_type in _ABSORBING_BOUNDARY_TYPES:
src/autofdtd/core/validation.py:635:    if boundary_type != "Boundary":
src/autofdtd/core/validation.py:636:        return boundary
src/autofdtd/core/validation.py:638:    updated = dict(boundary)
src/autofdtd/core/validation.py:651:    return updated if changed else boundary
phase1/runs/task-006-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-006-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-006-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-006-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:150:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-006-attempt-001/codex.stdout.txt:316:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-006-attempt-001/codex.stdout.txt:317:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-006-attempt-001/codex.stdout.txt:322:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-006-attempt-001/codex.stdout.txt:332:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-006-attempt-001/codex.stdout.txt:333:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-006-attempt-001/codex.stdout.txt:338:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-006-attempt-001/codex.stdout.txt:340:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-006-attempt-001/codex.stdout.txt:341:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-006-attempt-001/codex.stdout.txt:348:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-006-attempt-001/codex.stdout.txt:349:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-006-attempt-001/codex.stdout.txt:354:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-006-attempt-001/codex.stdout.txt:403:| `Simulation` shell (`center`, `size`, `run_time`, `medium`, `structures`, `sources`, `monitors`, `boundary_spec`, `grid_spec`, `symmetry`, `shutoff`, `courant`, `subpixel`) | Implement | Core API and IR entrypoint |
phase1/runs/task-006-attempt-001/codex.stdout.txt:475:| `ABCBoundary` | Defer | Keep mapped |
phase1/runs/task-006-attempt-001/codex.stdout.txt:476:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/runs/task-006-attempt-001/codex.stdout.txt:554:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/runs/task-006-attempt-001/codex.stdout.txt:795:    boundary_spec: object | None = None
phase1/runs/task-006-attempt-001/codex.stdout.txt:861:    @field_validator("boundary_spec")
phase1/runs/task-006-attempt-001/codex.stdout.txt:863:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
phase1/runs/task-006-attempt-001/codex.stdout.txt:866:        return normalize_component(value, context="boundary_spec")
phase1/runs/task-006-attempt-001/codex.stdout.txt:983:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-006-attempt-001/codex.stdout.txt:986:    if not isinstance(boundary_spec, Mapping):
phase1/runs/task-006-attempt-001/codex.stdout.txt:987:        return boundary_spec
phase1/runs/task-006-attempt-001/codex.stdout.txt:989:    updated = dict(boundary_spec)
phase1/runs/task-006-attempt-001/codex.stdout.txt:995:        axis_boundary = updated.get(axis_name)
phase1/runs/task-006-attempt-001/codex.stdout.txt:996:        if axis_boundary is None:
phase1/runs/task-006-attempt-001/codex.stdout.txt:998:        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-006-attempt-001/codex.stdout.txt:999:        if coerced is not axis_boundary:
phase1/runs/task-006-attempt-001/codex.stdout.txt:1003:    return updated if changed else boundary_spec
phase1/runs/task-006-attempt-001/codex.stdout.txt:1177:    BOUNDARY = "boundary"
phase1/runs/task-006-attempt-001/codex.stdout.txt:1248:    boundary_spec: ComponentIR | None = None
phase1/runs/task-006-attempt-001/codex.stdout.txt:1515:        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-006-attempt-001/codex.stdout.txt:1585:def test_zero_dim_absorbing_boundary_is_coerced_to_periodic_with_warning() -> None:
phase1/runs/task-006-attempt-001/codex.stdout.txt:1593:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:1596:    assert simulation.boundary_spec["x"]["type"] == "Periodic"
phase1/runs/task-006-attempt-001/codex.stdout.txt:1689:        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:1785:def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-006-attempt-001/codex.stdout.txt:1786:    if not isinstance(boundary, Mapping):
phase1/runs/task-006-attempt-001/codex.stdout.txt:1787:        return boundary
phase1/runs/task-006-attempt-001/codex.stdout.txt:1789:    boundary_type = component_type_name(boundary)
phase1/runs/task-006-attempt-001/codex.stdout.txt:1790:    if boundary_type == "BlochBoundary":
phase1/runs/task-006-attempt-001/codex.stdout.txt:1792:    if boundary_type in _ABSORBING_BOUNDARY_TYPES:
phase1/runs/task-006-attempt-001/codex.stdout.txt:1799:    if boundary_type != "Boundary":
phase1/runs/task-006-attempt-001/codex.stdout.txt:1800:        return boundary
phase1/runs/task-006-attempt-001/codex.stdout.txt:1802:    updated = dict(boundary)
phase1/runs/task-006-attempt-001/codex.stdout.txt:1815:    return updated if changed else boundary
phase1/runs/task-006-attempt-001/codex.stdout.txt:1895:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:1915:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2280:            If True, removes extremely small features from each polygon's boundary.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2670:phase1/loop.spec.md:152:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2680:phase1/feature-checklist.md:165:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/runs/task-006-attempt-001/codex.stdout.txt:2681:phase1/generator.md:34:* The implementation should support complex materials, boundary layers, subpixel smoothing, variable grids in all dimensions, composable geometries, and important postprocessing routines such as DFT fields and near-to-far transforms.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2764:  - clear boundary between implemented and deferred subpixel methods
phase1/runs/task-006-attempt-001/codex.stdout.txt:2769:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-006-attempt-001/codex.stdout.txt:2771:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/runs/task-006-attempt-001/codex.stdout.txt:2775:  - per-boundary family coefficient/state definitions
phase1/runs/task-006-attempt-001/codex.stdout.txt:2777:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/runs/task-006-attempt-001/codex.stdout.txt:2784:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/runs/task-006-attempt-001/codex.stdout.txt:22544:    42	    BOUNDARY = "boundary"
phase1/runs/task-006-attempt-001/codex.stdout.txt:26197:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:26251:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:26300:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:26349:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-006-attempt-001/codex.stdout.txt:35306: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:35325: - Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-006-attempt-001/codex.stdout.txt:36218: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:36237: - Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-006-attempt-001/codex.stdout.txt:37133: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:37152: - Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-006-attempt-001/codex.stdout.txt:38047: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-006-attempt-001/codex.stdout.txt:38066: - Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-004/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-004/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-004/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-004/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-004/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-004/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/kernels/boundaries.py:86:        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
src/autofdtd/kernels/boundaries.py:89:            "boundary halo updates require one ghost cell and one interior cell per side"
src/autofdtd/kernels/boundaries.py:149:def boundary_edge_transform(
src/autofdtd/kernels/boundaries.py:155:    """Apply the runtime transform associated with one boundary edge."""
src/autofdtd/kernels/boundaries.py:179:def apply_axis_boundary_ghosts(
src/autofdtd/kernels/boundaries.py:181:    axis_boundary: CompiledBoundaryAxis,
src/autofdtd/kernels/boundaries.py:185:    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
src/autofdtd/kernels/boundaries.py:189:    axis_index = "xyz".index(axis_boundary.axis)
src/autofdtd/kernels/boundaries.py:194:        axis_boundary.minus.mode,
src/autofdtd/kernels/boundaries.py:195:        getattr(axis_boundary.minus, signs_name),
src/autofdtd/kernels/boundaries.py:196:        axis_boundary.minus.phase_factor,
src/autofdtd/kernels/boundaries.py:202:        axis_boundary.plus.mode,
src/autofdtd/kernels/boundaries.py:203:        getattr(axis_boundary.plus, signs_name),
src/autofdtd/kernels/boundaries.py:204:        axis_boundary.plus.phase_factor,
src/autofdtd/kernels/boundaries.py:209:def apply_boundary_ghosts(
src/autofdtd/kernels/boundaries.py:212:    boundary_spec: CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:214:    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
src/autofdtd/kernels/boundaries.py:218:    for axis_boundary in boundary_spec.axes():
src/autofdtd/kernels/boundaries.py:219:        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
src/autofdtd/kernels/boundaries.py:220:        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
src/autofdtd/kernels/boundaries.py:226:    boundary_spec: CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:235:    for axis_boundary in boundary_spec.axes():
src/autofdtd/kernels/boundaries.py:236:        axis_index = "xyz".index(axis_boundary.axis)
src/autofdtd/kernels/boundaries.py:238:            edge = getattr(axis_boundary, side)
src/autofdtd/kernels/boundaries.py:245:            face_key = f"{axis_boundary.axis}.{side}"
src/autofdtd/kernels/boundaries.py:258:def apply_boundary_stages(
src/autofdtd/kernels/boundaries.py:261:    boundary_spec: CompiledBoundarySpec,
src/autofdtd/kernels/boundaries.py:264:    """Apply all compiled boundary stages in declared order."""
src/autofdtd/kernels/boundaries.py:268:    for stage in boundary_spec.stage_order:
src/autofdtd/kernels/boundaries.py:269:        if stage == "electric_boundary":
src/autofdtd/kernels/boundaries.py:270:            electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
src/autofdtd/kernels/boundaries.py:271:        elif stage == "magnetic_boundary":
src/autofdtd/kernels/boundaries.py:272:            magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
src/autofdtd/kernels/boundaries.py:275:                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
src/autofdtd/kernels/boundaries.py:276:            electric = apply_pml_layers(electric, boundary_spec, state, field_family="electric")
src/autofdtd/kernels/boundaries.py:279:                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
src/autofdtd/kernels/boundaries.py:280:            magnetic = apply_pml_layers(magnetic, boundary_spec, state, field_family="magnetic")
src/autofdtd/kernels/boundaries.py:284:def boundary_kernel_metadata() -> dict[str, Any]:
src/autofdtd/kernels/boundaries.py:285:    """Expose the current boundary backend and staged update choices."""
src/autofdtd/kernels/boundaries.py:292:            "electric_boundary",
src/autofdtd/kernels/boundaries.py:293:            "magnetic_boundary",
phase1/runs/task-017-attempt-010/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-010/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-010/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-010/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-010/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-010/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/core/containers.py:213:    boundary_spec: object | None = None
src/autofdtd/core/containers.py:283:    @field_validator("boundary_spec")
src/autofdtd/core/containers.py:285:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
src/autofdtd/core/containers.py:288:        return normalize_component(value, context="boundary_spec")
src/autofdtd/core/containers.py:340:        if self.boundary_spec is not None:
src/autofdtd/core/containers.py:343:                "boundary_spec",
src/autofdtd/core/containers.py:344:                coerce_zero_dim_boundaries(self.boundary_spec, size=self.size),
phase1/runs/task-017-attempt-010/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-010/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-010/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-010/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-010/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-010/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-005-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-005-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-005-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-005-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/prompt.txt:136:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/kernels/__init__.py:7:    apply_axis_boundary_ghosts,
src/autofdtd/kernels/__init__.py:8:    apply_boundary_ghosts,
src/autofdtd/kernels/__init__.py:9:    apply_boundary_stages,
src/autofdtd/kernels/__init__.py:11:    boundary_edge_transform,
src/autofdtd/kernels/__init__.py:12:    boundary_kernel_metadata,
src/autofdtd/kernels/__init__.py:39:    "apply_axis_boundary_ghosts",
src/autofdtd/kernels/__init__.py:40:    "apply_boundary_ghosts",
src/autofdtd/kernels/__init__.py:41:    "apply_boundary_stages",
src/autofdtd/kernels/__init__.py:43:    "boundary_edge_transform",
src/autofdtd/kernels/__init__.py:46:    "boundary_kernel_metadata",
phase1/runs/task-017-attempt-003/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-003/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-003/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-003/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-003/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-003/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-009/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-009/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-009/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-009/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-009/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-009/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-009/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-009/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-009/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-009/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-009/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-009/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-003/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-003/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-003/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-003/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-003/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-003/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/compiler/boundaries.py:23:    boundary_edge_model_from_value,
src/autofdtd/compiler/boundaries.py:24:    boundary_model_from_value,
src/autofdtd/compiler/boundaries.py:25:    boundary_spec_model_from_value,
src/autofdtd/compiler/boundaries.py:31:    """Runtime boundary mode consumed by staged halo handling."""
src/autofdtd/compiler/boundaries.py:46:    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
src/autofdtd/compiler/boundaries.py:62:    """Runtime-ready metadata for one boundary face."""
src/autofdtd/compiler/boundaries.py:76:    """Runtime-ready metadata for one axis boundary pair."""
src/autofdtd/compiler/boundaries.py:97:    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
src/autofdtd/compiler/boundaries.py:117:    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
src/autofdtd/compiler/boundaries.py:126:    edge = boundary_edge_model_from_value(value)
src/autofdtd/compiler/boundaries.py:141:    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
src/autofdtd/compiler/boundaries.py:167:    normalized = boundary_edge_model_from_value(edge)
src/autofdtd/compiler/boundaries.py:250:        boundary_type=edge.type,
src/autofdtd/compiler/boundaries.py:290:        boundary_type=edge.type,
src/autofdtd/compiler/boundaries.py:318:    normalized = boundary_edge_model_from_value(edge)
src/autofdtd/compiler/boundaries.py:337:    boundary: Boundary,
src/autofdtd/compiler/boundaries.py:342:    minus = _compile_edge(axis_name, "minus", boundary.minus, dt=dt, grid_spacing=grid_spacing)
src/autofdtd/compiler/boundaries.py:343:    plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
src/autofdtd/compiler/boundaries.py:388:def compile_boundary_spec(
src/autofdtd/compiler/boundaries.py:395:    """Compile the supported Phase 1 boundary subset into runtime metadata."""
src/autofdtd/compiler/boundaries.py:398:        boundary_spec = BoundarySpec()
src/autofdtd/compiler/boundaries.py:401:            boundary_spec = boundary_spec_model_from_value(value)
src/autofdtd/compiler/boundaries.py:404:                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
src/autofdtd/compiler/boundaries.py:421:            axis_boundary,
src/autofdtd/compiler/boundaries.py:425:        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
src/autofdtd/compiler/boundaries.py:467:    stage_order = ["electric_boundary", "magnetic_boundary"]
src/autofdtd/compiler/boundaries.py:493:def normalize_axis_boundary(value: object) -> Boundary:
src/autofdtd/compiler/boundaries.py:494:    """Normalize a single axis boundary using the supported subset."""
src/autofdtd/compiler/boundaries.py:496:    return boundary_model_from_value(value)
phase1/runs/task-017-attempt-008/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-008/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-008/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-008/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-008/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-008/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-008/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-008/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-008/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-008/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-008/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-008/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/compiler/__init__.py:10:    compile_boundary_spec,
src/autofdtd/compiler/__init__.py:11:    normalize_axis_boundary,
src/autofdtd/compiler/__init__.py:53:    "compile_boundary_spec",
src/autofdtd/compiler/__init__.py:63:    "normalize_axis_boundary",
src/autofdtd/ir/models.py:25:    boundary_edge_model_from_value,
src/autofdtd/ir/models.py:26:    boundary_model_from_value,
src/autofdtd/ir/models.py:27:    boundary_spec_model_from_value,
src/autofdtd/ir/models.py:96:    BOUNDARY = "boundary"
src/autofdtd/ir/models.py:376:    """Typed execution IR for a periodic boundary edge."""
src/autofdtd/ir/models.py:379:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:385:    """Typed execution IR for a Bloch boundary edge."""
src/autofdtd/ir/models.py:388:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:397:    """Typed execution IR for a PEC boundary edge."""
src/autofdtd/ir/models.py:400:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:406:    """Typed execution IR for a PMC boundary edge."""
src/autofdtd/ir/models.py:409:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:430:    """Typed execution IR for a baseline PML boundary edge."""
src/autofdtd/ir/models.py:433:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:442:    """Typed execution IR for a stable-PML boundary edge."""
src/autofdtd/ir/models.py:445:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:466:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:497:    """Typed execution IR for one axis boundary pair."""
src/autofdtd/ir/models.py:500:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:507:    """Typed execution IR for the Phase 1 boundary container."""
src/autofdtd/ir/models.py:510:    family: Literal["boundary"] = "boundary"
src/autofdtd/ir/models.py:707:    boundary_spec: BoundarySpecIR | ComponentIR | None = None
src/autofdtd/ir/models.py:1005:def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
src/autofdtd/ir/models.py:1006:    """Lower a supported boundary edge into typed execution IR."""
src/autofdtd/ir/models.py:1020:        edge = boundary_edge_model_from_value(value)
src/autofdtd/ir/models.py:1098:def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
src/autofdtd/ir/models.py:1099:    """Lower a supported axis boundary pair into typed execution IR."""
src/autofdtd/ir/models.py:1101:    boundary: Boundary | None = None
src/autofdtd/ir/models.py:1103:        boundary = value
src/autofdtd/ir/models.py:1114:        boundary = boundary_model_from_value(value)
src/autofdtd/ir/models.py:1115:    if boundary is None:
src/autofdtd/ir/models.py:1118:        plus=boundary_edge_to_ir(boundary.plus),
src/autofdtd/ir/models.py:1119:        minus=boundary_edge_to_ir(boundary.minus),
src/autofdtd/ir/models.py:1154:def boundary_spec_to_ir(
src/autofdtd/ir/models.py:1161:    boundary_spec: BoundarySpec | None = None
src/autofdtd/ir/models.py:1163:        boundary_spec = value
src/autofdtd/ir/models.py:1166:            boundary_spec = boundary_spec_model_from_value(value)
src/autofdtd/ir/models.py:1168:            boundary_spec = None
src/autofdtd/ir/models.py:1169:    if boundary_spec is None:
src/autofdtd/ir/models.py:1183:    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
src/autofdtd/ir/models.py:1184:        lowered = boundary_to_ir(axis_boundary)
src/autofdtd/ir/models.py:1432:        boundary_spec=(
src/autofdtd/ir/models.py:1433:            boundary_spec_to_ir(simulation.boundary_spec, symmetry=simulation.symmetry)
src/autofdtd/ir/models.py:1434:            if simulation.boundary_spec is not None
phase1/runs/task-005-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-005-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-005-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-005-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:148:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-005-attempt-001/codex.stdout.txt:184:phase1/loop.spec.md:167:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-005-attempt-001/codex.stdout.txt:185:phase1/loop.spec.md:168:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-005-attempt-001/codex.stdout.txt:186:phase1/loop.spec.md:175:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-005-attempt-001/codex.stdout.txt:187:phase1/loop.spec.md:176:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-005-attempt-001/codex.stdout.txt:192:phase1/loop.spec.md:407:## [QUEUED] task-051 - Implement boundary and absorbing-layer kernel stages
phase1/runs/task-005-attempt-001/codex.stdout.txt:468:    boundary_spec: object | None = None
phase1/runs/task-005-attempt-001/codex.stdout.txt:534:    @field_validator("boundary_spec")
phase1/runs/task-005-attempt-001/codex.stdout.txt:536:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
phase1/runs/task-005-attempt-001/codex.stdout.txt:539:        return normalize_component(value, context="boundary_spec")
phase1/runs/task-005-attempt-001/codex.stdout.txt:604:    BOUNDARY = "boundary"
phase1/runs/task-005-attempt-001/codex.stdout.txt:675:    boundary_spec: ComponentIR | None = None
phase1/runs/task-005-attempt-001/codex.stdout.txt:843:        boundary_spec=(
phase1/runs/task-005-attempt-001/codex.stdout.txt:844:            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-005-attempt-001/codex.stdout.txt:845:            if simulation.boundary_spec is not None
phase1/runs/task-005-attempt-001/codex.stdout.txt:1008:        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-005-attempt-001/codex.stdout.txt:1077:def test_zero_dim_absorbing_boundary_is_coerced_to_periodic_with_warning() -> None:
phase1/runs/task-005-attempt-001/codex.stdout.txt:1085:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-005-attempt-001/codex.stdout.txt:1088:    assert simulation.boundary_spec["x"]["type"] == "Periodic"
phase1/runs/task-005-attempt-001/codex.stdout.txt:1184:        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-005-attempt-001/codex.stdout.txt:1401:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:1419:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-005-attempt-001/codex.stdout.txt:1513: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:1918: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:2470:    boundary_spec: object | None = None
phase1/runs/task-005-attempt-001/codex.stdout.txt:2536:    @field_validator("boundary_spec")
phase1/runs/task-005-attempt-001/codex.stdout.txt:2538:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
phase1/runs/task-005-attempt-001/codex.stdout.txt:2541:        return normalize_component(value, context="boundary_spec")
phase1/runs/task-005-attempt-001/codex.stdout.txt:2677:    BOUNDARY = "boundary"
phase1/runs/task-005-attempt-001/codex.stdout.txt:2748:    boundary_spec: ComponentIR | None = None
phase1/runs/task-005-attempt-001/codex.stdout.txt:2923: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:3381:        if self.boundary_spec is not None:
phase1/runs/task-005-attempt-001/codex.stdout.txt:3384:                "boundary_spec",
phase1/runs/task-005-attempt-001/codex.stdout.txt:3385:                coerce_zero_dim_boundaries(self.boundary_spec, size=self.size),
phase1/runs/task-005-attempt-001/codex.stdout.txt:3412: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:3822: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:4217: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:4616: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:5072: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:5470: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:5893: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:6322: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:6767: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:7265: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-005-attempt-001/codex.stdout.txt:7690: - `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-019/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-019/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-019/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-019/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-019/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-019/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-002/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-002/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-002/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-002/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-002/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-002/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/ir/__init__.py:58:    boundary_edge_to_ir,
src/autofdtd/ir/__init__.py:59:    boundary_spec_to_ir,
src/autofdtd/ir/__init__.py:60:    boundary_to_ir,
src/autofdtd/ir/__init__.py:127:    "boundary_edge_to_ir",
src/autofdtd/ir/__init__.py:128:    "boundary_spec_to_ir",
src/autofdtd/ir/__init__.py:129:    "boundary_to_ir",
phase1/runs/task-018-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-018-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-018-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-018-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-018-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-018-attempt-001/prompt.txt:182:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-004-attempt-001/result.json:1:{"task_id":"task-004","status":"completed","summary":"Implemented checklist-aware core normalization and validation for the Phase 1 container layer, including primitive-shape sanity checks, simulation-bound validation, explicit unsupported-feature errors, normalize-index handling, zero-dimension boundary coercion, and supporting tests/docs.","key_findings":["Added `autofdtd.core.validation` as the shared normalization layer for tagged component payloads, covering name trimming, vector coercion, primitive geometry checks for `Box`/`Sphere`/`Cylinder`, structure-bound checks against the simulation domain, and zero-size-axis boundary coercion to `Periodic`.","`Simulation` now exposes explicit Phase 1 control-policy fields for `normalize_index`, `relax_courant`, `low_freq_smoothing`, and `lumped_elements`, with implemented validation for normalize-index and clear fail-fast errors for deferred or rejected core features.","Unsupported feature handling is now tied directly to the local feature matrix through lookup helpers in `autofdtd.planning`, so deferred and reject-clearly feature tags fail with explicit messages instead of being accepted silently or rejected as unknown extras.","Coverage now includes normalization, primitive shape errors, out-of-domain warnings and errors, zero-dimension absorbing-boundary coercion, normalize-index validation, and planning lookups, with docs and working memory updated to point later tasks at the shared validator path."],"artifacts":[{"path":"src/autofdtd/core/validation.py","description":"Shared Phase 1 validation and normalization helpers for tagged component payloads and simulation-bound checks."},{"path":"src/autofdtd/core/containers.py","description":"Integrated container-level validators for geometry, media, boundaries, normalize-index, and deferred-feature policies."},{"path":"src/autofdtd/planning.py","description":"Added feature lookup helpers and extra tracked Phase 1 feature tags used by validation."},{"path":"tests/test_containers.py","description":"Validation coverage for normalization, shape constraints, unsupported features, domain bounds, and zero-dimension boundary coercion."},{"path":"docs/architecture.md","description":"Architecture notes describing the new shared validation layer and its current responsibilities."},{"path":"phase1/working-memory.md","description":"Distilled task handoff notes for later feature-family tasks to reuse the core validation path."},{"path":"phase1/runs/task-004-attempt-001/result.json","description":"Structured task result artifact for this loop iteration."}],"error_summary":"","follow_up_notes":"Verification passed with `ruff check`, `pytest -q`, and `mypy src`. Geometry, material, boundary, source, monitor, and grid tasks should extend `autofdtd.core.validation` rather than duplicating checklist-driven policy checks in each family.","next_action":"none"}
phase1/runs/task-017-attempt-002/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-002/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-002/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-002/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-002/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-002/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
src/autofdtd/boundaries/models.py:1:"""Boundary models for the foundational Phase 1 boundary surface."""
src/autofdtd/boundaries/models.py:25:    """Base model for one simulation-domain boundary edge."""
src/autofdtd/boundaries/models.py:36:    """Periodic boundary condition."""
src/autofdtd/boundaries/models.py:42:    """Phase-aware periodic boundary condition."""
src/autofdtd/boundaries/models.py:55:    """Perfect electric conductor boundary condition."""
src/autofdtd/boundaries/models.py:61:    """Perfect magnetic conductor boundary condition."""
src/autofdtd/boundaries/models.py:243:def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
src/autofdtd/boundaries/models.py:244:    """Normalize a supported Phase 1 non-absorbing boundary edge."""
src/autofdtd/boundaries/models.py:249:        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
src/autofdtd/boundaries/models.py:250:    boundary_type = str(value.get("type"))
src/autofdtd/boundaries/models.py:251:    if boundary_type == "Periodic":
src/autofdtd/boundaries/models.py:253:    if boundary_type == "BlochBoundary":
src/autofdtd/boundaries/models.py:255:    if boundary_type == "PECBoundary":
src/autofdtd/boundaries/models.py:257:    if boundary_type == "PMCBoundary":
src/autofdtd/boundaries/models.py:259:    if boundary_type == "PML":
src/autofdtd/boundaries/models.py:261:    if boundary_type == "StablePML":
src/autofdtd/boundaries/models.py:263:    if boundary_type == "Absorber":
src/autofdtd/boundaries/models.py:265:    raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")
src/autofdtd/boundaries/models.py:281:            boundary_type = str(value.get("type"))
src/autofdtd/boundaries/models.py:282:            if boundary_type in {
src/autofdtd/boundaries/models.py:291:                return boundary_edge_model_from_value(value)
src/autofdtd/boundaries/models.py:322:                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
src/autofdtd/boundaries/models.py:323:                "boundary so runtime halo handling stays reflection-only on that axis.",
src/autofdtd/boundaries/models.py:406:def boundary_model_from_value(value: object) -> Boundary:
src/autofdtd/boundaries/models.py:407:    """Normalize a supported axis boundary pair."""
src/autofdtd/boundaries/models.py:412:        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
src/autofdtd/boundaries/models.py:414:        edge = boundary_edge_model_from_value(value)
src/autofdtd/boundaries/models.py:429:    def _coerce_axis_boundary(cls, value: object) -> object:
src/autofdtd/boundaries/models.py:436:            boundary_type = str(value.get("type"))
src/autofdtd/boundaries/models.py:437:            if boundary_type in {
src/autofdtd/boundaries/models.py:447:                return boundary_model_from_value(value)
src/autofdtd/boundaries/models.py:463:    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
src/autofdtd/boundaries/models.py:465:            x=Boundary(plus=boundary, minus=boundary),
src/autofdtd/boundaries/models.py:466:            y=Boundary(plus=boundary, minus=boundary),
src/autofdtd/boundaries/models.py:467:            z=Boundary(plus=boundary, minus=boundary),
src/autofdtd/boundaries/models.py:620:def boundary_spec_model_from_value(value: object) -> BoundarySpec:
src/autofdtd/boundaries/models.py:626:        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-004-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-004-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-004-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-020-attempt-001/result.json:1:{"task_id":"task-020","status":"completed","summary":"Implemented BlochBoundary support plus symmetry-aware boundary metadata and phase-aware runtime transforms across the public API, IR, compiler, kernels, runtime wrapper, and tests.","key_findings":["`BlochBoundary` is now a first-class public boundary edge with `Boundary.bloch(...)`, strict both-sides validation, and explicit complex phase metadata derived from `bloch_vec`.","Boundary IR now distinguishes periodic, Bloch, and reflective axes, marks `requires_complex_fields`, and carries per-axis symmetry sign metadata for electric and magnetic field families.","Compiled boundary metadata now includes Bloch exchange kinds, per-face phase factors, active symmetry axes, and reusable symmetry transforms for future chunk-planning and reduced-domain exchange paths.","Boundary ghost updates now apply conjugate phase wrapping on minus Bloch faces and forward phase wrapping on plus faces, while separate helper transforms expose boundary-edge and symmetry remaps directly to runtime callers.","Focused validation passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py` and `python -m compileall src/autofdtd`."],"artifacts":[{"path":"src/autofdtd/boundaries/models.py","description":"Added `BlochBoundary`, public constructors, and boundary-pair validation rules."},{"path":"src/autofdtd/compiler/boundaries.py","description":"Extended compiled boundary metadata with Bloch phase factors and symmetry-axis transforms."},{"path":"src/autofdtd/ir/models.py","description":"Added typed Bloch and symmetry IR payloads plus boundary-spec lowering with complex-field requirements."},{"path":"src/autofdtd/kernels/boundaries.py","description":"Implemented phase-aware Bloch ghost updates and reusable boundary/symmetry transform helpers."},{"path":"src/autofdtd/runtime/boundaries.py","description":"Extended runtime boundary wrapper to compile symmetry metadata and expose transform helpers."},{"path":"tests/test_boundaries.py","description":"Added Bloch boundary, symmetry metadata, phase-transform, and runtime/IR coverage."},{"path":"phase1/working-memory.md","description":"Recorded Bloch runtime behavior and symmetry-metadata decisions for later boundary tasks."}],"error_summary":"","follow_up_notes":"Absorbing-boundary tasks can now extend the existing `CompiledBoundarySpec` contract without redesigning periodic/Bloch/symmetry lowering. No multi-GPU smoke was attempted because this task only touched boundary metadata and local ghost-transform behavior.","next_action":"none"}
src/autofdtd/boundaries/__init__.py:19:    boundary_edge_model_from_value,
src/autofdtd/boundaries/__init__.py:20:    boundary_model_from_value,
src/autofdtd/boundaries/__init__.py:21:    boundary_spec_model_from_value,
src/autofdtd/boundaries/__init__.py:40:    "boundary_edge_model_from_value",
src/autofdtd/boundaries/__init__.py:41:    "boundary_model_from_value",
src/autofdtd/boundaries/__init__.py:42:    "boundary_spec_model_from_value",
src/autofdtd/grid/specs.py:99:        normalized = tuple(_finite_float(coord, field_name="grid boundary") for coord in value)
src/autofdtd/grid/specs.py:198:    """Explicit one-dimensional boundary coordinates."""
src/autofdtd/grid/specs.py:498:                boundary_dl = spec.bounds_refinement.effective_dl(vacuum_dl=base_dl)
src/autofdtd/grid/specs.py:499:                half_width = boundary_dl * spec.bounds_refinement.num_cells / 2.0
src/autofdtd/grid/specs.py:511:                            boundary_dl,
phase1/generator.md:34:* The implementation should support complex materials, boundary layers, subpixel smoothing, variable grids in all dimensions, composable geometries, and important postprocessing routines such as DFT fields and near-to-far transforms.
phase1/generator.md:47:We expect combinatorial growth in kernel variants as features are added, especially for different materials, boundary conditions, and other localized behaviors. The architecture should explicitly plan for that complexity instead of avoiding it.
phase1/generator.md:88:* complex materials and boundary layers,
phase1/generator.md:98:The loop should also identify, as early as possible, which kernel formulas, update rules, boundary-condition details, material-model details, geometry-discretization rules, and postprocessing formulas must be researched and settled before implementation can proceed safely.
phase1/generator.md:149:* include early research tasks that extract and document the canonical kernel update equations, boundary-condition formulas, material-model formulas, geometry discretization rules, and IR requirements needed for implementation,
phase1/generator.md:153:* include a small set of canonical validation examples, such as vacuum propagation, a dielectric slab or waveguide, a PML boundary case, a symmetry-reduced case, and a near-to-far example,
phase1/feature-checklist.md:14:| `Simulation` shell (`center`, `size`, `run_time`, `medium`, `structures`, `sources`, `monitors`, `boundary_spec`, `grid_spec`, `symmetry`, `shutoff`, `courant`, `subpixel`) | Implement | Core API and IR entrypoint |
phase1/feature-checklist.md:86:| `ABCBoundary` | Defer | Keep mapped |
phase1/feature-checklist.md:87:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/feature-checklist.md:165:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/kernel-formula-sources.md:75:  - clear boundary between implemented and deferred subpixel methods
phase1/kernel-formula-sources.md:80:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/kernel-formula-sources.md:82:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/kernel-formula-sources.md:86:  - per-boundary family coefficient/state definitions
phase1/kernel-formula-sources.md:88:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/kernel-formula-sources.md:95:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/runs/task-017-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-001/prompt.txt:178:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-002-attempt-001/result.json:1:{"task_id":"task-002","status":"completed","summary":"Implemented the first tagged immutable container backbone for `Simulation`, `Scene`, and `Structure`, including ordered structure precedence helpers, name validation, and JSON-ready serialization scaffolding for later IR work.","key_findings":["`src/autofdtd/core/containers.py` now provides frozen pydantic container models with explicit `type` tags, JSON-ready payload helpers, and minimal disk serialization entrypoints.","`Scene` preserves ordered structure semantics and exposes `structures_in_resolution_order()` so later scene compilation can apply structures by resolved priority and insertion order without re-deriving precedence rules.","`StructurePriorityMode.CONDUCTOR` is mapped early using medium type tags so implicit priority behavior can distinguish dielectric, lossy-metal, and PEC structures before concrete material classes land.","`Simulation` now carries the Phase 1 shell fields from the feature checklist, including domain size, run time, sources, monitors, boundary and grid references, symmetry, shutoff, courant, and subpixel references.","Focused tests now cover name normalization, duplicate-name rejection, ordered overlap behavior, conductor-priority defaults, and tagged JSON serialization, and the repo passes `pytest`, `ruff`, and `mypy`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/core/containers.py","description":"Frozen tagged container models and precedence/serialization helpers for `Structure`, `Scene`, and `Simulation`."},{"path":"/workspace/autofdtd/src/autofdtd/api/simulation.py","description":"Public API re-exports for the foundational container models."},{"path":"/workspace/autofdtd/src/autofdtd/api/__init__.py","description":"API namespace exports exposing the new container backbone."},{"path":"/workspace/autofdtd/src/autofdtd/core/__init__.py","description":"Core namespace exports exposing the tagged container layer."},{"path":"/workspace/autofdtd/tests/test_containers.py","description":"Container-model tests for names, precedence ordering, conductor priority defaults, and JSON serialization."},{"path":"/workspace/autofdtd/docs/architecture.md","description":"Architecture notes updated to describe the current container backbone and separation from future IR work."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Condensed handoff notes capturing the container-model decisions for later tasks."},{"path":"/workspace/autofdtd/phase1/loop.spec.md","description":"Task board updated to mark `task-002` completed."},{"path":"/workspace/autofdtd/phase1/runs/task-002-attempt-001/result.json","description":"Recorded task output matching the Phase 1 result schema."}],"error_summary":"","follow_up_notes":"`task-003` should now define the separate execution IR contract and treat the current container payload helpers as an interim handoff format rather than the final IR schema.","next_action":"none"}
phase1/runs/task-002-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-002-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-002-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-001/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-001/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-003-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-003-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-003-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-016-attempt-001/result.json:1:{"task_id":"task-016","status":"completed","summary":"Implemented passive homogeneous Lorentz, damped Drude, and passive homogeneous Debye medium support across the public API, typed IR, coefficient compilation, and the shared PoleResidue-family update path, with explicit tests for each family.","key_findings":["`autofdtd.materials` now exposes `Lorentz`, `Drude`, and `Debye` models with analytical `eps_model()` evaluation, family-specific validation, and lowering to equivalent `PoleResidue` payloads.","`autofdtd.ir` now emits typed `LorentzIR`, `DrudeIR`, and `DebyeIR` payloads, and the compiler preserves the originating family tag while compiling onto the existing `PoleResidueMaterialCoefficients` auxiliary-state contract.","Compilation now fails explicitly when converted dispersive poles are too stiff for the current exact-hold timestep path, which clearly delimits the currently supported subset instead of emitting unusable coefficients.","Examples, docs, tests, and working memory were updated so the new families are exercised and the subset boundary is recorded for later material/runtime tasks."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/materials/dispersive.py","description":"Added Lorentz, Drude, and Debye public models plus analytical-to-PoleResidue lowering helpers."},{"path":"/workspace/autofdtd/src/autofdtd/materials/__init__.py","description":"Extended Phase 1 medium dispatch and exports to include Lorentz, Drude, and Debye."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/materials.py","description":"Added Lorentz, Drude, and Debye coefficient compilation on the shared PoleResidue runtime contract plus explicit stiff-pole rejection."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed LorentzIR, DrudeIR, and DebyeIR lowering and medium dispatch integration."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/materials.py","description":"Updated constitutive metadata so the shared dispersive kernel path reports Lorentz, Drude, and Debye support."},{"path":"/workspace/autofdtd/src/autofdtd/examples/materials.py","description":"Added compiled analytical-dispersion examples covering Lorentz, Drude, and Debye lowering and coefficient generation."},{"path":"/workspace/autofdtd/docs/materials.md","description":"Documented the added analytical dispersive families and the explicit stiff-pole compile rejection policy."},{"path":"/workspace/autofdtd/tests/test_materials.py","description":"Added named Lorentz, Drude, and Debye validation, IR, compilation, and shared-kernel tests."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the new analytical-dispersion decisions and the stiff-pole subset boundary for later tasks."},{"path":"/workspace/autofdtd/phase1/runs/task-016-attempt-001/result.json","description":"Recorded the structured task result for this run."}],"error_summary":"","follow_up_notes":"Verification passed with `ruff check src tests`, `pytest -q tests/test_materials.py`, and `python -m compileall src`. A repo-wide `mypy src` run still reports pre-existing unrelated typing issues outside this task’s scope.","next_action":"none"}
phase1/runs/task-012-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-012-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-012-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-012-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-012-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-012-attempt-001/prompt.txt:156:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-010-attempt-001/result.json:1:{"task_id":"task-010","status":"completed","summary":"Implemented the Phase 1 manual grid surface with typed `UniformGrid`, `CustomGrid`, `CustomGridBoundaries`, and `GridSpec` models, simulation-time grid resolution, and typed grid IR lowering with resolved boundary metadata.","key_findings":["`src/autofdtd/grid/specs.py` now provides concrete Phase 1 grid models plus `ResolvedGrid`/`ResolvedGridAxis` so simulations can resolve axis boundaries before compiler/runtime work lands.","`Simulation.resolved_grid()` and `core.validation` now enforce a strict top-level `GridSpec` contract, validate manual grid compatibility against simulation bounds, and reject planned-but-not-yet-implemented grid families like `AutoGrid` explicitly.","`src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR (`GridSpecIR`, per-axis IR, and resolved grid metadata), giving later discretization and mode-solver tasks an inspectable execution-side mesh contract.","Public exports, examples, and docs were updated so the grid surface is part of the Tidy3D-inspired API rather than an opaque dict payload.","Verification passed with `python -m ruff check src tests` and `python -m pytest` (47 tests passed)."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/grid/specs.py","description":"Phase 1 manual grid models, resolution helpers, and domain-to-boundary lowering."},{"path":"/workspace/autofdtd/src/autofdtd/core/containers.py","description":"`Simulation.resolved_grid()` integration and grid-spec validation during simulation construction."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Checklist-aware normalization for `GridSpec` and explicit rejection of not-yet-implemented grid families."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed grid execution IR and lowering path with resolved grid metadata."},{"path":"/workspace/autofdtd/tests/test_grid.py","description":"Focused grid behavior tests covering snapping, explicit boundaries, and resolved-grid shape metrics."},{"path":"/workspace/autofdtd/docs/grid.md","description":"Grid-scope documentation for the new Phase 1 manual discretization surface."}],"error_summary":"","follow_up_notes":"The next grid task can add `AutoGrid`, refinement metadata, and related planning fields onto the existing `GridSpec`/`ResolvedGrid` seam without changing simulation or IR contracts.","next_action":"none"}
phase1/runs/task-016-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-016-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-016-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-016-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-016-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-016-attempt-001/prompt.txt:173:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-018-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-018-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-018-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-018-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-018-attempt-001/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-018-attempt-001/codex.stdout.txt:194:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-018-attempt-001/codex.stdout.txt:364:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-018-attempt-001/codex.stdout.txt:365:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-018-attempt-001/codex.stdout.txt:370:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-018-attempt-001/codex.stdout.txt:380:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-018-attempt-001/codex.stdout.txt:381:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-018-attempt-001/codex.stdout.txt:386:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-018-attempt-001/codex.stdout.txt:388:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-018-attempt-001/codex.stdout.txt:389:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-018-attempt-001/codex.stdout.txt:396:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-018-attempt-001/codex.stdout.txt:397:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-018-attempt-001/codex.stdout.txt:402:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-018-attempt-001/codex.stdout.txt:1420:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-018-attempt-001/codex.stdout.txt:1423:    if not isinstance(boundary_spec, Mapping):
phase1/runs/task-018-attempt-001/codex.stdout.txt:1424:        return boundary_spec
phase1/runs/task-018-attempt-001/codex.stdout.txt:1426:    updated = dict(boundary_spec)
phase1/runs/task-018-attempt-001/codex.stdout.txt:1432:        axis_boundary = updated.get(axis_name)
phase1/runs/task-018-attempt-001/codex.stdout.txt:1433:        if axis_boundary is None:
phase1/runs/task-018-attempt-001/codex.stdout.txt:1435:        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-018-attempt-001/codex.stdout.txt:1436:        if coerced is not axis_boundary:
phase1/runs/task-018-attempt-001/codex.stdout.txt:1440:    return updated if changed else boundary_spec
phase1/runs/task-018-attempt-001/codex.stdout.txt:1853:    FeatureEntry("boundaries", "Boundary", FeatureStatus.IMPLEMENT, "Per-axis boundary pair."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1854:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1855:    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1856:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1857:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1862:        "Phase-aware periodic boundary.",
phase1/runs/task-018-attempt-001/codex.stdout.txt:1872:    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:1873:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-018-attempt-001/codex.stdout.txt:2177:    boundary_spec: object | None = None
phase1/runs/task-018-attempt-001/codex.stdout.txt:2266:    BOUNDARY = "boundary"
phase1/runs/task-018-attempt-001/codex.stdout.txt:3104:| `ABCBoundary` | Defer | Keep mapped |
phase1/runs/task-018-attempt-001/codex.stdout.txt:3105:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/runs/task-018-attempt-001/codex.stdout.txt:3225:def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-018-attempt-001/codex.stdout.txt:3226:    if not isinstance(boundary, Mapping):
phase1/runs/task-018-attempt-001/codex.stdout.txt:3227:        return boundary
phase1/runs/task-018-attempt-001/codex.stdout.txt:3229:    boundary_type = component_type_name(boundary)
phase1/runs/task-018-attempt-001/codex.stdout.txt:3230:    if boundary_type == "BlochBoundary":
phase1/runs/task-018-attempt-001/codex.stdout.txt:3232:    if boundary_type in _ABSORBING_BOUNDARY_TYPES:
phase1/runs/task-018-attempt-001/codex.stdout.txt:3239:    if boundary_type != "Boundary":
phase1/runs/task-018-attempt-001/codex.stdout.txt:3240:        return boundary
phase1/runs/task-018-attempt-001/codex.stdout.txt:3242:    updated = dict(boundary)
phase1/runs/task-018-attempt-001/codex.stdout.txt:3781:../tidy3d/tidy3d/components/medium.py:912:class AbstractCustomMedium(AbstractMedium, ABC):
phase1/runs/task-018-attempt-001/codex.stdout.txt:3817:../tidy3d/tidy3d/components/medium.py:2755:class CustomDispersiveMedium(AbstractCustomMedium, DispersiveMedium, ABC):
phase1/runs/task-018-attempt-001/codex.stdout.txt:3859:../tidy3d/tidy3d/components/medium.py:6862:class AbstractPerturbationMedium(ABC, Tidy3dBaseModel):
phase1/runs/task-018-attempt-001/codex.stdout.txt:3980:    """Lossy metal that can be modeled with a surface impedance boundary condition (SIBC).
phase1/runs/task-018-attempt-001/codex.stdout.txt:4173:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-018-attempt-001/codex.stdout.txt:4184:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-018-attempt-001/codex.stdout.txt:4237:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-010-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-010-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-010-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-010-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-010-attempt-001/prompt.txt:152:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-019/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-017-attempt-019/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-017-attempt-019/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-017-attempt-019/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-017-attempt-019/codex.stdout.txt:141:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-017-attempt-019/codex.stdout.txt:190:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-017-attempt-019/codex.stdout.txt:242:phase1/loop.spec.md:397:Notes: This task isolates the core timestepping spine before material, boundary, and source complications are layered on.
phase1/runs/task-017-attempt-019/codex.stdout.txt:246:phase1/loop.spec.md:413:Notes: Keep boundary runtime support separate from source and material kernel work.
phase1/runs/task-017-attempt-019/codex.stdout.txt:1377:    boundary_spec: ComponentIR | None = None
phase1/runs/task-017-attempt-019/codex.stdout.txt:1886:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-017-attempt-019/codex.stdout.txt:1889:    if not isinstance(boundary_spec, Mapping):
phase1/runs/task-017-attempt-019/codex.stdout.txt:1890:        return boundary_spec
phase1/runs/task-017-attempt-019/codex.stdout.txt:1892:    updated = dict(boundary_spec)
phase1/runs/task-017-attempt-019/codex.stdout.txt:1898:        axis_boundary = updated.get(axis_name)
phase1/runs/task-017-attempt-019/codex.stdout.txt:1899:        if axis_boundary is None:
phase1/runs/task-017-attempt-019/codex.stdout.txt:1901:        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-017-attempt-019/codex.stdout.txt:1902:        if coerced is not axis_boundary:
phase1/runs/task-017-attempt-019/codex.stdout.txt:1906:    return updated if changed else boundary_spec
phase1/runs/task-017-attempt-019/codex.stdout.txt:3492:../tidy3d/tests/test_components/autograd/numerical/test_flexible_dielectric_pec_boundary.py:55:    box_2d_dielectric_material_left = td.Medium2D.from_medium(
phase1/runs/task-017-attempt-019/codex.stdout.txt:3493:../tidy3d/tests/test_components/autograd/numerical/test_flexible_dielectric_pec_boundary.py:58:    box_2d_dielectric_material_center = td.Medium2D.from_medium(
phase1/runs/task-017-attempt-019/codex.stdout.txt:5396:    BOUNDARY = "boundary"
phase1/runs/task-017-attempt-019/codex.stdout.txt:36401:            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-017-attempt-019/codex.stdout.txt:36402:            if simulation.boundary_spec is not None
phase1/runs/task-021-attempt-001/result.json:1:{"task_id":"task-021","status":"completed","summary":"Implemented baseline PML boundary support across the public API, typed IR, boundary compilation, runtime state allocation, staged kernel updates, examples, and absorption-focused tests.","key_findings":["Added Tidy3D-shaped baseline `PML` and `PMLParams` models plus `Boundary.pml(...)` / `BoundarySpec.pml(...)` constructors and zero-dimension coercion behavior.","Extended boundary IR with typed `PMLIR` and `PMLParamsIR`; `BoundarySpecIR` now records `pml_axes` and `pml_faces` so scene lowering preserves absorbing-face metadata explicitly.","Boundary compilation now prepares per-face polynomial PML coefficient profiles (`sigma`, `kappa`, `alpha`, attenuation, memory terms) and advertises explicit `electric_pml` / `magnetic_pml` stage ordering.","Runtime now allocates persistent per-face PML memory via `allocate_pml_boundary_state()` and applies PML stages through `apply_boundary_stages()` / `BoundaryRuntime.apply()`.","Representative tests now cover typed PML lowering, coefficient prep, face-state allocation, and staged absorption behavior; full suite passes with `PYTHONPATH=src python -m pytest -q` (110 passed)."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Baseline PML public models and boundary constructors."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed PML IR models and boundary-spec PML metadata lowering."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Compiled PML coefficient preparation and boundary-stage metadata."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Boundary kernel stages including PML layer damping with persistent face memory."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime state allocation and staged boundary application wrapper."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Representative PML validation and absorption tests."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Boundary runtime snapshots including a compact PML absorption example."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Updated Phase 1 handoff notes for baseline PML support and extension seams."}],"error_summary":"","follow_up_notes":"Current PML support is intentionally narrow: it uses a staged damping-layer backend with persistent face memory rather than a full split-field CPML. `StablePML` and `Absorber` should extend the compiled-face/state contract in task-022.","next_action":"none"}
phase1/runs/task-014-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-014-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-014-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-014-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-014-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-014-attempt-001/prompt.txt:163:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-021-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-021-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-021-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-021-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-021-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-021-attempt-001/prompt.txt:149:- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-021-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-021-attempt-001/prompt.txt:183:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, and `PMCBoundary`; absorbing and ABC-family entries remain outside the runtime compiler until the later boundary tasks land.
phase1/runs/task-021-attempt-001/prompt.txt:191:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-021-attempt-001/prompt.txt:199:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch and symmetry metadata now already live on that runtime contract.
phase1/runs/task-021-attempt-001/prompt.txt:204:Title: Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-021-attempt-001/prompt.txt:205:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-021-attempt-001/prompt.txt:206:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-022-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-022-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-022-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-022-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-022-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads, layer-refinement metadata, and resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state instead of reparsing public models.
phase1/runs/task-022-attempt-001/prompt.txt:149:- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-022-attempt-001/prompt.txt:150:- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-022-attempt-001/prompt.txt:155:- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now prepare polynomial PML coefficients (`sigma`, `kappa`, `alpha`, attenuation and memory terms), allocate per-face persistent boundary-layer state, and run explicit `electric_pml` / `magnetic_pml` stages after halo handling.
phase1/runs/task-022-attempt-001/prompt.txt:185:- Phase 1 boundary runtime support now covers `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, and baseline `PML`; `StablePML`, `Absorber`, and ABC-family entries remain outside the runtime compiler until later boundary tasks land.
phase1/runs/task-022-attempt-001/prompt.txt:187:- Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
phase1/runs/task-022-attempt-001/prompt.txt:194:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-022-attempt-001/prompt.txt:202:- Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
phase1/runs/task-022-attempt-001/prompt.txt:203:- `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
phase1/runs/task-022-attempt-001/prompt.txt:208:Title: Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-022-attempt-001/prompt.txt:209:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-003-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-003-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-003-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-003-attempt-001/codex.stdout.txt:371:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-003-attempt-001/codex.stdout.txt:372:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-003-attempt-001/codex.stdout.txt:377:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-003-attempt-001/codex.stdout.txt:387:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-003-attempt-001/codex.stdout.txt:388:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-003-attempt-001/codex.stdout.txt:393:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-003-attempt-001/codex.stdout.txt:395:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-003-attempt-001/codex.stdout.txt:396:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-003-attempt-001/codex.stdout.txt:403:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-003-attempt-001/codex.stdout.txt:404:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-003-attempt-001/codex.stdout.txt:409:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-003-attempt-001/codex.stdout.txt:647:    boundary_spec: object | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:806:        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-003-attempt-001/codex.stdout.txt:904:| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-003-attempt-001/codex.stdout.txt:918:- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-003-attempt-001/codex.stdout.txt:930:  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-003-attempt-001/codex.stdout.txt:1198:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:1379:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:1450:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:1611:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:1612:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:1613:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:1689:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:1769:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:1950:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:2021:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:2182:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:2183:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:2184:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:2260:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:2342:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:2523:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:2594:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:2755:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:2756:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:2757:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:2833:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:3263:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:3444:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:3515:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:3676:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:3677:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:3678:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:3754:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:3837:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:4015:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:4086:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:4247:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:4248:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:4249:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:4325:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:4405:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:4583:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:4654:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:4815:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:4816:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:4817:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:4893:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:4975:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:5153:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:5224:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:5385:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:5386:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:5387:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:5463:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:5577:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:5755:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:5826:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:5987:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:5988:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:5989:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:6065:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:6153:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:6338:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:6409:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:6577:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:6578:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:6579:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:6655:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:6735:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:6920:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:6991:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:7159:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:7160:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:7161:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:7237:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:7326:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:7511:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:7582:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:7750:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:7751:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:7752:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:7828:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:8015:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:8200:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:8271:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:8439:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:8440:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:8441:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:8517:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-003-attempt-001/codex.stdout.txt:8599:+  boundary, grid, and subpixel payloads.
phase1/runs/task-003-attempt-001/codex.stdout.txt:8784:+    BOUNDARY = "boundary"
phase1/runs/task-003-attempt-001/codex.stdout.txt:8855:+    boundary_spec: ComponentIR | None = None
phase1/runs/task-003-attempt-001/codex.stdout.txt:9023:+        boundary_spec=(
phase1/runs/task-003-attempt-001/codex.stdout.txt:9024:+            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-003-attempt-001/codex.stdout.txt:9025:+            if simulation.boundary_spec is not None
phase1/runs/task-003-attempt-001/codex.stdout.txt:9101:+        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-001-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-001-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-001-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-010-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-010-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-010-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-010-attempt-001/codex.stdout.txt:130:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-010-attempt-001/codex.stdout.txt:164:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-010-attempt-001/codex.stdout.txt:332:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-010-attempt-001/codex.stdout.txt:333:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-010-attempt-001/codex.stdout.txt:338:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-010-attempt-001/codex.stdout.txt:348:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-010-attempt-001/codex.stdout.txt:349:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-010-attempt-001/codex.stdout.txt:354:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-010-attempt-001/codex.stdout.txt:356:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-010-attempt-001/codex.stdout.txt:357:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-010-attempt-001/codex.stdout.txt:364:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-010-attempt-001/codex.stdout.txt:365:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-010-attempt-001/codex.stdout.txt:370:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-010-attempt-001/codex.stdout.txt:614:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-010-attempt-001/codex.stdout.txt:617:    if not isinstance(boundary_spec, Mapping):
phase1/runs/task-010-attempt-001/codex.stdout.txt:618:        return boundary_spec
phase1/runs/task-010-attempt-001/codex.stdout.txt:620:    updated = dict(boundary_spec)
phase1/runs/task-010-attempt-001/codex.stdout.txt:626:        axis_boundary = updated.get(axis_name)
phase1/runs/task-010-attempt-001/codex.stdout.txt:627:        if axis_boundary is None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:629:        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-010-attempt-001/codex.stdout.txt:630:        if coerced is not axis_boundary:
phase1/runs/task-010-attempt-001/codex.stdout.txt:634:    return updated if changed else boundary_spec
phase1/runs/task-010-attempt-001/codex.stdout.txt:859:    BOUNDARY = "boundary"
phase1/runs/task-010-attempt-001/codex.stdout.txt:1039:    boundary_spec: ComponentIR | None = None
phase1/runs/task-010-attempt-001/codex.stdout.txt:1292:        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-010-attempt-001/codex.stdout.txt:1551:    FeatureEntry("boundaries", "Boundary", FeatureStatus.IMPLEMENT, "Per-axis boundary pair."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1552:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1553:    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1554:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1555:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1560:        "Phase-aware periodic boundary.",
phase1/runs/task-010-attempt-001/codex.stdout.txt:1570:    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1571:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-010-attempt-001/codex.stdout.txt:1891:    boundary_spec: object | None = None
phase1/runs/task-010-attempt-001/codex.stdout.txt:1957:    @field_validator("boundary_spec")
phase1/runs/task-010-attempt-001/codex.stdout.txt:1959:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:1962:        return normalize_component(value, context="boundary_spec")
phase1/runs/task-010-attempt-001/codex.stdout.txt:2186:/bin/bash -lc 'rg -n "grid_spec|boundary_spec|subpixel|UniformGrid|GridSpec" src tests' in /workspace/autofdtd
phase1/runs/task-010-attempt-001/codex.stdout.txt:2188:tests/test_ir.py:115:        boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-010-attempt-001/codex.stdout.txt:2191:tests/test_containers.py:321:        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-010-attempt-001/codex.stdout.txt:2194:tests/test_containers.py:487:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-010-attempt-001/codex.stdout.txt:2195:tests/test_containers.py:490:    assert simulation.boundary_spec["x"]["type"] == "Periodic"
phase1/runs/task-010-attempt-001/codex.stdout.txt:2203:src/autofdtd/ir/models.py:233:    boundary_spec: ComponentIR | None = None
phase1/runs/task-010-attempt-001/codex.stdout.txt:2206:src/autofdtd/ir/models.py:517:        boundary_spec=(
phase1/runs/task-010-attempt-001/codex.stdout.txt:2207:src/autofdtd/ir/models.py:518:            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-010-attempt-001/codex.stdout.txt:2208:src/autofdtd/ir/models.py:519:            if simulation.boundary_spec is not None
phase1/runs/task-010-attempt-001/codex.stdout.txt:2215:src/autofdtd/core/validation.py:132:def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-010-attempt-001/codex.stdout.txt:2216:src/autofdtd/core/validation.py:135:    if not isinstance(boundary_spec, Mapping):
phase1/runs/task-010-attempt-001/codex.stdout.txt:2217:src/autofdtd/core/validation.py:136:        return boundary_spec
phase1/runs/task-010-attempt-001/codex.stdout.txt:2218:src/autofdtd/core/validation.py:138:    updated = dict(boundary_spec)
phase1/runs/task-010-attempt-001/codex.stdout.txt:2219:src/autofdtd/core/validation.py:152:    return updated if changed else boundary_spec
phase1/runs/task-010-attempt-001/codex.stdout.txt:2221:src/autofdtd/core/containers.py:212:    boundary_spec: object | None = None
phase1/runs/task-010-attempt-001/codex.stdout.txt:2224:src/autofdtd/core/containers.py:278:    @field_validator("boundary_spec")
phase1/runs/task-010-attempt-001/codex.stdout.txt:2225:src/autofdtd/core/containers.py:280:    def _validate_boundary_spec(cls, value: object | None) -> object | None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:2226:src/autofdtd/core/containers.py:283:        return normalize_component(value, context="boundary_spec")
phase1/runs/task-010-attempt-001/codex.stdout.txt:2233:src/autofdtd/core/containers.py:335:        if self.boundary_spec is not None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:2234:src/autofdtd/core/containers.py:338:                "boundary_spec",
phase1/runs/task-010-attempt-001/codex.stdout.txt:2235:src/autofdtd/core/containers.py:339:                coerce_zero_dim_boundaries(self.boundary_spec, size=self.size),
phase1/runs/task-010-attempt-001/codex.stdout.txt:2261:        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-010-attempt-001/codex.stdout.txt:2419:def test_zero_dim_absorbing_boundary_is_coerced_to_periodic_with_warning() -> None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:2427:            boundary_spec={"type": "BoundarySpec", "x": {"type": "PML"}},
phase1/runs/task-010-attempt-001/codex.stdout.txt:2430:    assert simulation.boundary_spec["x"]["type"] == "Periodic"
phase1/runs/task-010-attempt-001/codex.stdout.txt:2500:        if self.boundary_spec is not None:
phase1/runs/task-010-attempt-001/codex.stdout.txt:2503:                "boundary_spec",
phase1/runs/task-010-attempt-001/codex.stdout.txt:2504:                coerce_zero_dim_boundaries(self.boundary_spec, size=self.size),
phase1/runs/task-010-attempt-001/codex.stdout.txt:2525:| `Simulation` shell (`center`, `size`, `run_time`, `medium`, `structures`, `sources`, `monitors`, `boundary_spec`, `grid_spec`, `symmetry`, `shutoff`, `courant`, `subpixel`) | Implement | Core API and IR entrypoint |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2597:| `ABCBoundary` | Defer | Keep mapped |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2598:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/runs/task-010-attempt-001/codex.stdout.txt:2676:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/runs/task-010-attempt-001/codex.stdout.txt:2693:from abc import ABC, abstractmethod
phase1/runs/task-010-attempt-001/codex.stdout.txt:2868:class GridSpec1d(Tidy3dBaseModel, ABC):
phase1/runs/task-010-attempt-001/codex.stdout.txt:2896:            Apply periodic boundary condition or not.
phase1/runs/task-010-attempt-001/codex.stdout.txt:2915:        # Determine if one should apply periodic boundary condition.
phase1/runs/task-010-attempt-001/codex.stdout.txt:2953:180:class GridSpec1d(Tidy3dBaseModel, ABC):
phase1/runs/task-010-attempt-001/codex.stdout.txt:3061:    """Custom 1D grid supplied as a list of grid cell boundary coordinates.
phase1/runs/task-010-attempt-001/codex.stdout.txt:3070:        description="An array of grid boundary coordinates.",
phase1/runs/task-010-attempt-001/codex.stdout.txt: WARNING: stopped searching binary file after match (found "\0" byte around offset 160926)
phase1/runs/task-008-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-008-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-008-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-008-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-008-attempt-001/prompt.txt:144:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-009-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-009-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-009-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-009-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-009-attempt-001/prompt.txt:149:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-002-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-002-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-002-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-002-attempt-001/codex.stdout.txt:306:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-002-attempt-001/codex.stdout.txt:307:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-002-attempt-001/codex.stdout.txt:312:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-002-attempt-001/codex.stdout.txt:322:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-002-attempt-001/codex.stdout.txt:323:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-002-attempt-001/codex.stdout.txt:328:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-002-attempt-001/codex.stdout.txt:330:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-002-attempt-001/codex.stdout.txt:331:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-002-attempt-001/codex.stdout.txt:338:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-002-attempt-001/codex.stdout.txt:339:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-002-attempt-001/codex.stdout.txt:344:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-002-attempt-001/codex.stdout.txt:755:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:756:    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:757:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:758:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:763:        "Phase-aware periodic boundary.",
phase1/runs/task-002-attempt-001/codex.stdout.txt:773:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-002-attempt-001/codex.stdout.txt:939:| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-002-attempt-001/codex.stdout.txt:953:- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:972:| `Simulation` shell (`center`, `size`, `run_time`, `medium`, `structures`, `sources`, `monitors`, `boundary_spec`, `grid_spec`, `symmetry`, `shutoff`, `courant`, `subpixel`) | Implement | Core API and IR entrypoint |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1044:| `ABCBoundary` | Defer | Keep mapped |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1045:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/runs/task-002-attempt-001/codex.stdout.txt:1123:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/runs/task-002-attempt-001/codex.stdout.txt:1142:from abc import ABC, abstractmethod
phase1/runs/task-002-attempt-001/codex.stdout.txt:1175:from .boundary import (
phase1/runs/task-002-attempt-001/codex.stdout.txt:1178:    ABCBoundary,
phase1/runs/task-002-attempt-001/codex.stdout.txt:1185:    ModeABCBoundary,
phase1/runs/task-002-attempt-001/codex.stdout.txt:1294:    from .boundary import BoundaryEdgeType
phase1/runs/task-002-attempt-001/codex.stdout.txt:1368:        val = self.boundary_spec
phase1/runs/task-002-attempt-001/codex.stdout.txt:1374:        for dim, (boundary, symmetry_dim, size_dim) in enumerate(zip(boundaries, symmetry, size)):
phase1/runs/task-002-attempt-001/codex.stdout.txt:1628:from abc import ABC, abstractmethod
phase1/runs/task-002-attempt-001/codex.stdout.txt:1659:class AbstractSimulation(Box, ABC):
phase1/runs/task-002-attempt-001/codex.stdout.txt:1715:    boundary_spec: Literal[None] = Field(
phase1/runs/task-002-attempt-001/codex.stdout.txt:1718:        description="Specification of boundary conditions.",
phase1/runs/task-002-attempt-001/codex.stdout.txt:1827:        boundary_spec = self.boundary_spec
phase1/runs/task-002-attempt-001/codex.stdout.txt:1830:                boundary_spec.to_list
phase1/runs/task-002-attempt-001/codex.stdout.txt:1831:                if boundary_spec is not None and hasattr(boundary_spec, "to_list")
phase1/runs/task-002-attempt-001/codex.stdout.txt:2229:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-002-attempt-001/codex.stdout.txt:2241:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-002-attempt-001/codex.stdout.txt:2245:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-002-attempt-001/codex.stdout.txt:2692:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:2924:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:3170:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:3413:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:3717:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:4016:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:4323:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:4627:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:4929:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:5083:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:5351:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:5505:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:5800:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:5954:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:6015: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:6027:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:6316:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:6470:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:6516: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:6528:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:6817:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:6971:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:7022: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:7034:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:7323:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:7477:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:7570: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:7582:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:7871:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:8025:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:8078: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:8090:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:8380:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:8534:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:8580: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:8592:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:8882:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:9036:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:9082: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:9094:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:9384:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:9538:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:9587: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:9599:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:9890:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:10044:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:10090: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:10102:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:10393:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:10547:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:10611: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:10623:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:10914:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:11068:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:11119: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:11131:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:11422:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:11576:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:11637: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:11649:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:11940:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:12094:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:12328: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:12340:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:12631:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:12785:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:12834: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:12846:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:13156:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:13310:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:13356: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:13368:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:13678:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:13832:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:13858:{\"task_id\":\"task-002\",\"status\":\"completed\",\"summary\":\"Implemented the first tagged immutable container backbone for "'`Simulation`, `Scene`, and `Structure`, including ordered structure precedence helpers, name validation, and JSON-ready serialization scaffolding for later IR work.","key_findings":["`src/autofdtd/core/containers.py` now provides frozen pydantic container models with explicit `type` tags, JSON-ready payload helpers, and minimal disk serialization entrypoints.","`Scene` preserves ordered structure semantics and exposes `structures_in_resolution_order()` so later scene compilation can apply structures by resolved priority and insertion order without re-deriving precedence rules.","`StructurePriorityMode.CONDUCTOR` is mapped early using medium type tags so implicit priority behavior can distinguish dielectric, lossy-metal, and PEC structures before concrete material classes land.","`Simulation` now carries the Phase 1 shell fields from the feature checklist, including domain size, run time, sources, monitors, boundary and grid references, symmetry, shutoff, courant, and subpixel references.","Focused tests now cover name normalization, duplicate-name rejection, ordered overlap behavior, conductor-priority defaults, and tagged JSON serialization, and the repo passes `pytest`, `ruff`, and `mypy`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/core/containers.py","description":"Frozen tagged container models and precedence/serialization helpers for `Structure`, `Scene`, and `Simulation`."},{"path":"/workspace/autofdtd/src/autofdtd/api/simulation.py","description":"Public API re-exports for the foundational container models."},{"path":"/workspace/autofdtd/src/autofdtd/api/__init__.py","description":"API namespace exports exposing the new container backbone."},{"path":"/workspace/autofdtd/src/autofdtd/core/__init__.py","description":"Core namespace exports exposing the tagged container layer."},{"path":"/workspace/autofdtd/tests/test_containers.py","description":"Container-model tests for names, precedence ordering, conductor priority defaults, and JSON serialization."},{"path":"/workspace/autofdtd/docs/architecture.md","description":"Architecture notes updated to describe the current container backbone and separation from future IR work."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Condensed handoff notes capturing the container-model decisions for later tasks."},{"path":"/workspace/autofdtd/phase1/loop.spec.md","description":"Task board updated to mark `task-002` completed."}],"error_summary":"","follow_up_notes":"`task-003` should now define the separate execution IR contract and treat the current container payload helpers as an interim handoff format rather than the final IR schema.","next_action":"none"}
phase1/runs/task-002-attempt-001/codex.stdout.txt:13883: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:13895:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:14205:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:14359:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:14422: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:14434:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:14744:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:14898:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:14959: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:14971:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:15281:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:15435:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:15558:    95	        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:15756:   174	    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:15827:    14	| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-002-attempt-001/codex.stdout.txt:15841:    28	- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:15853:    40	  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:15880: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:15892:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:16202:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:16356:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:16405: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:16417:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:16719:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:16873:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:16919: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:16931:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:17233:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:17387:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:17435: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:17447:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:17749:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:17903:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:17955: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:17967:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:18269:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:18423:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:18448:{"task_id":"task-002","status":"completed","summary":"Implemented the first tagged immutable container backbone for `Simulation`, `Scene`, and `Structure`, including ordered structure precedence helpers, name validation, and JSON-ready serialization scaffolding for later IR work.","key_findings":["`src/autofdtd/core/containers.py` now provides frozen pydantic container models with explicit `type` tags, JSON-ready payload helpers, and minimal disk serialization entrypoints.","`Scene` preserves ordered structure semantics and exposes `structures_in_resolution_order()` so later scene compilation can apply structures by resolved priority and insertion order without re-deriving precedence rules.","`StructurePriorityMode.CONDUCTOR` is mapped early using medium type tags so implicit priority behavior can distinguish dielectric, lossy-metal, and PEC structures before concrete material classes land.","`Simulation` now carries the Phase 1 shell fields from the feature checklist, including domain size, run time, sources, monitors, boundary and grid references, symmetry, shutoff, courant, and subpixel references.","Focused tests now cover name normalization, duplicate-name rejection, ordered overlap behavior, conductor-priority defaults, and tagged JSON serialization, and the repo passes `pytest`, `ruff`, and `mypy`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/core/containers.py","description":"Frozen tagged container models and precedence/serialization helpers for `Structure`, `Scene`, and `Simulation`."},{"path":"/workspace/autofdtd/src/autofdtd/api/simulation.py","description":"Public API re-exports for the foundational container models."},{"path":"/workspace/autofdtd/src/autofdtd/api/__init__.py","description":"API namespace exports exposing the new container backbone."},{"path":"/workspace/autofdtd/src/autofdtd/core/__init__.py","description":"Core namespace exports exposing the tagged container layer."},{"path":"/workspace/autofdtd/tests/test_containers.py","description":"Container-model tests for names, precedence ordering, conductor priority defaults, and JSON serialization."},{"path":"/workspace/autofdtd/docs/architecture.md","description":"Architecture notes updated to describe the current container backbone and separation from future IR work."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Condensed handoff notes capturing the container-model decisions for later tasks."},{"path":"/workspace/autofdtd/phase1/loop.spec.md","description":"Task board updated to mark `task-002` completed."},{"path":"/workspace/autofdtd/phase1/runs/task-002-attempt-001/result.json","description":"Recorded task output matching the Phase 1 result schema."}],"error_summary":"","follow_up_notes":"`task-003` should now define the separate execution IR contract and treat the current container payload helpers as an interim handoff format rather than the final IR schema.","next_action":"none"}
phase1/runs/task-002-attempt-001/codex.stdout.txt:18471: - Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-002-attempt-001/codex.stdout.txt:18483:+  monitors, boundary and grid references, symmetry, and version tagging.
phase1/runs/task-002-attempt-001/codex.stdout.txt:18785:+    boundary_spec: object | None = None
phase1/runs/task-002-attempt-001/codex.stdout.txt:18939:+        boundary_spec={"type": "BoundarySpec"},
phase1/runs/task-002-attempt-001/codex.stdout.txt:18965:{"task_id":"task-002","status":"completed","summary":"Implemented the first tagged immutable container backbone for `Simulation`, `Scene`, and `Structure`, including ordered structure precedence helpers, name validation, and JSON-ready serialization scaffolding for later IR work.","key_findings":["`src/autofdtd/core/containers.py` now provides frozen pydantic container models with explicit `type` tags, JSON-ready payload helpers, and minimal disk serialization entrypoints.","`Scene` preserves ordered structure semantics and exposes `structures_in_resolution_order()` so later scene compilation can apply structures by resolved priority and insertion order without re-deriving precedence rules.","`StructurePriorityMode.CONDUCTOR` is mapped early using medium type tags so implicit priority behavior can distinguish dielectric, lossy-metal, and PEC structures before concrete material classes land.","`Simulation` now carries the Phase 1 shell fields from the feature checklist, including domain size, run time, sources, monitors, boundary and grid references, symmetry, shutoff, courant, and subpixel references.","Focused tests now cover name normalization, duplicate-name rejection, ordered overlap behavior, conductor-priority defaults, and tagged JSON serialization, and the repo passes `pytest`, `ruff`, and `mypy`."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/core/containers.py","description":"Frozen tagged container models and precedence/serialization helpers for `Structure`, `Scene`, and `Simulation`."},{"path":"/workspace/autofdtd/src/autofdtd/api/simulation.py","description":"Public API re-exports for the foundational container models."},{"path":"/workspace/autofdtd/src/autofdtd/api/__init__.py","description":"API namespace exports exposing the new container backbone."},{"path":"/workspace/autofdtd/src/autofdtd/core/__init__.py","description":"Core namespace exports exposing the tagged container layer."},{"path":"/workspace/autofdtd/tests/test_containers.py","description":"Container-model tests for names, precedence ordering, conductor priority defaults, and JSON serialization."},{"path":"/workspace/autofdtd/docs/architecture.md","description":"Architecture notes updated to describe the current container backbone and separation from future IR work."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Condensed handoff notes capturing the container-model decisions for later tasks."},{"path":"/workspace/autofdtd/phase1/loop.spec.md","description":"Task board updated to mark `task-002` completed."},{"path":"/workspace/autofdtd/phase1/runs/task-002-attempt-001/result.json","description":"Recorded task output matching the Phase 1 result schema."}],"error_summary":"","follow_up_notes":"`task-003` should now define the separate execution IR contract and treat the current container payload helpers as an interim handoff format rather than the final IR schema.","next_action":"none"}
phase1/runs/task-001-attempt-001/codex.stdout.txt:68:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-001-attempt-001/codex.stdout.txt:80:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-001-attempt-001/codex.stdout.txt:84:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-001-attempt-001/codex.stdout.txt:153:| `Simulation` shell (`center`, `size`, `run_time`, `medium`, `structures`, `sources`, `monitors`, `boundary_spec`, `grid_spec`, `symmetry`, `shutoff`, `courant`, `subpixel`) | Implement | Core API and IR entrypoint |
phase1/runs/task-001-attempt-001/codex.stdout.txt:225:| `ABCBoundary` | Defer | Keep mapped |
phase1/runs/task-001-attempt-001/codex.stdout.txt:226:| `ModeABCBoundary` | Defer | Keep mapped |
phase1/runs/task-001-attempt-001/codex.stdout.txt:304:- Periodic / Bloch / symmetry boundary handling kernels or runtime transforms
phase1/runs/task-001-attempt-001/codex.stdout.txt:465:## [QUEUED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-001-attempt-001/codex.stdout.txt:466:Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-001-attempt-001/codex.stdout.txt:471:Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-001-attempt-001/codex.stdout.txt:481:## [QUEUED] task-021 - Implement PML boundary support with coefficient prep and kernel stages
phase1/runs/task-001-attempt-001/codex.stdout.txt:482:Success: Support PML boundaries with explicit API and IR models, coefficient preparation, boundary-layer state allocation, kernel-stage integration, and representative absorption tests.
phase1/runs/task-001-attempt-001/codex.stdout.txt:487:Notes: PML is important enough to deserve its own task instead of sharing a generic absorbing-boundary bucket.
phase1/runs/task-001-attempt-001/codex.stdout.txt:489:## [QUEUED] task-022 - Implement StablePML and Absorber boundary support with coefficient prep and kernel stages
phase1/runs/task-001-attempt-001/codex.stdout.txt:490:Success: Support StablePML and Absorber boundaries or a clearly delimited subset, including API and IR models, coefficient preparation, kernel-stage integration, and representative tests for practical absorbing-boundary behavior.
phase1/runs/task-001-attempt-001/codex.stdout.txt:497:## [QUEUED] task-023 - Implement ABCBoundary, ModeABCBoundary, and advanced boundary defer policy
phase1/runs/task-001-attempt-001/codex.stdout.txt:498:Success: Provide an explicit Phase 1 treatment for ABCBoundary, ModeABCBoundary, and other specialized boundary variants, including concrete implementation where practical and clear reject-or-defer behavior otherwise.
phase1/runs/task-001-attempt-001/codex.stdout.txt:503:Notes: This task prevents specialized Tidy3D boundary features from disappearing into ambiguity.
phase1/runs/task-001-attempt-001/codex.stdout.txt:735:  - clear boundary between implemented and deferred subpixel methods
phase1/runs/task-001-attempt-001/codex.stdout.txt:740:  Settle runtime formulas and kernel structure for `PML`, `Absorber`, and related boundary families.
phase1/runs/task-001-attempt-001/codex.stdout.txt:742:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/runs/task-001-attempt-001/codex.stdout.txt:746:  - per-boundary family coefficient/state definitions
phase1/runs/task-001-attempt-001/codex.stdout.txt:748:  - supported/deferred list for `StablePML`, `ABCBoundary`, `ModeABCBoundary`
phase1/runs/task-001-attempt-001/codex.stdout.txt:755:  - `../tidy3d/tidy3d/components/boundary.py`
phase1/runs/task-001-attempt-001/codex.stdout.txt:832:from abc import ABC, abstractmethod
phase1/runs/task-001-attempt-001/codex.stdout.txt:865:from .boundary import (
phase1/runs/task-001-attempt-001/codex.stdout.txt:868:    ABCBoundary,
phase1/runs/task-001-attempt-001/codex.stdout.txt:875:    ModeABCBoundary,
phase1/runs/task-001-attempt-001/codex.stdout.txt:984:    from .boundary import BoundaryEdgeType
phase1/runs/task-001-attempt-001/codex.stdout.txt:1058:        val = self.boundary_spec
phase1/runs/task-001-attempt-001/codex.stdout.txt:1064:        for dim, (boundary, symmetry_dim, size_dim) in enumerate(zip(boundaries, symmetry, size)):
phase1/runs/task-001-attempt-001/codex.stdout.txt:1521:            If True, removes extremely small features from each polygon's boundary.
phase1/runs/task-001-attempt-001/codex.stdout.txt:1640:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:1654:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:2125:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2126:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2127:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2128:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2129:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2133:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2366:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:2380:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:2851:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2852:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2853:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2854:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2855:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:2859:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3155:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3156:    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3157:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3158:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3159:    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3163:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3306:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:3320:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:3791:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3792:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3793:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3794:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3795:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:3799:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4034:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:4048:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:4519:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4520:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4521:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4522:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4523:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4527:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:4760:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:4774:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:5245:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5246:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5247:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5248:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5249:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5253:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5486:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:5500:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:5971:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5972:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5973:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5974:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5975:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:5979:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6366:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:6380:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:6851:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6852:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6853:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6854:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6855:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:6859:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7096:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:7110:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:7581:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7582:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7583:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7584:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7585:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7589:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7926:72 |     FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7927:73 |     FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7928:74 |     FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7937:74 |     FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:7942:78 |     FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8290:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:8304:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:8775:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8776:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8777:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8778:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8779:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:8783:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9096:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:9110:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:9581:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9582:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9583:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9584:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9585:+    FeatureEntry("boundaries", "BlochBoundary", FeatureStatus.IMPLEMENT, "Phase-aware periodic boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9589:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:9828:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:9842:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:10356:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10357:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10358:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10359:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10364:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:10369:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:10695:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:10709:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:11223:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11224:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11225:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11226:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11231:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:11236:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11521:128 |     FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:11692:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:11706:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:12220:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12221:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12222:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12223:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12228:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:12233:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:12568:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:12582:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:13096:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13097:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13098:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13099:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13104:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:13114:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:13475:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:13489:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:14003:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14004:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14005:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14006:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14011:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:14021:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14331:  - ModeABCBoundary [Defer]
phase1/runs/task-001-attempt-001/codex.stdout.txt:14416:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:14430:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:14944:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14945:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14946:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14947:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:14952:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:14962:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15337:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:15351:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:15865:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15866:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15867:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15868:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:15873:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:15883:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16246:+| `autofdtd.boundaries` | Boundary containers and boundary-family models |
phase1/runs/task-001-attempt-001/codex.stdout.txt:16260:+- Preserve room for chunk-local interior stages, boundary stages, and future overlap-aware scheduling.
phase1/runs/task-001-attempt-001/codex.stdout.txt:16774:+    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16775:+    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16776:+    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16777:+    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
phase1/runs/task-001-attempt-001/codex.stdout.txt:16782:+        "Phase-aware periodic boundary.",
phase1/runs/task-001-attempt-001/codex.stdout.txt:16792:+    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
phase1/runs/task-007-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-007-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-007-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-007-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-007-attempt-001/prompt.txt:142:- Later geometry, source, monitor, boundary, and grid tasks should route new object models through `autofdtd.core.validation.normalize_component()` or extend its equivalents instead of scattering policy checks across each family.
phase1/runs/task-011-attempt-001/prompt.txt:56:Tidy3D EM feature surface identified from the local codebase and schemas: Simulation plus Scene and Structure containers; geometry families including Box, Sphere, Cylinder, PolySlab, GeometryGroup, Transformed, ClipOperation, GeometryArray, and TriangleMesh-related paths; material families including Medium, PECMedium, PMCMedium, custom and dispersive isotropic media, PoleResidue, Sellmeier, Lorentz, Drude, Debye, LossyMetalMedium, AnisotropicMedium, FullyAnisotropicMedium, CustomAnisotropicMedium, PerturbationMedium, and Medium2D; boundary families including Periodic, PECBoundary, PMCBoundary, BlochBoundary, PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary, Boundary, and BoundarySpec; source families including GaussianPulse, ContinuousWave, CustomSourceTime, BroadbandPulse, UniformCurrentSource, PointDipole, CustomCurrentSource, CustomFieldSource, ModeSource, PlaneWave, GaussianBeam, AstigmaticGaussianBeam, and TFSF; monitor families including FieldMonitor, FieldTimeMonitor, AuxFieldTimeMonitor, MediumMonitor, PermittivityMonitor, FluxMonitor, FluxTimeMonitor, ModeMonitor, ModeSolverMonitor, GaussianOverlapMonitor, AstigmaticGaussianOverlapMonitor, FieldProjectionAngleMonitor, DirectivityMonitor, FieldProjectionCartesianMonitor, FieldProjectionKSpaceMonitor, DiffractionMonitor, SurfaceFieldMonitor, and SurfaceFieldTimeMonitor; and grid or subpixel controls including UniformGrid, CustomGridBoundaries, CustomGrid, AutoGrid, QuasiUniformGrid, GridRefinement, LayerRefinementSpec, GridSpec, ModeSpec, and SubpixelSpec with staircasing and averaging variants. High-value references: `../tidy3d/tidy3d/components/simulation.py`, `../tidy3d/tidy3d/components/structure.py`, `../tidy3d/tidy3d/components/geometry/base.py`, `../tidy3d/tidy3d/components/geometry/primitives.py`, `../tidy3d/tidy3d/components/geometry/polyslab.py`, `../tidy3d/tidy3d/components/source/current.py`, `../tidy3d/tidy3d/components/source/field.py`, `../tidy3d/tidy3d/components/source/time.py`, `../tidy3d/tidy3d/components/monitor.py`, `../tidy3d/tidy3d/components/medium.py`, `../tidy3d/tidy3d/components/boundary.py`, `../tidy3d/tidy3d/components/grid/grid_spec.py`, `../tidy3d/tidy3d/components/mode_spec.py`, `../tidy3d/tidy3d/components/subpixel_spec.py`, and `../tidy3d/schemas/Simulation.json`.
phase1/runs/task-011-attempt-001/prompt.txt:68:Warp backend findings: Warp kernels are Python-authored and JIT-compiled per module; all kernels in a module compile together and should be stabilized early to avoid reload churn. Arrays, vectors, matrices, and structs are strong fits for field buffers and metadata. CUDA launches are asynchronous; streams and events are first-class; graph capture and replay are realistic optimization targets for timestep loops; tile APIs are promising for later shared-memory optimizations but should not block a first Phase 1 backend; and built-in profiling via ScopedTimer, NVTX, and CUDA activity summaries is good enough for Phase 1 instrumentation. Benchmarking guidance from `papers/gpu_benchmarking.pdf` adds that FDTD should be tracked primarily in cells updated per second or Gcells/s, usually as a bandwidth-bound workload, and that benchmark metadata must preserve grid shape, total cells, timestep count, precision, boundary or material class, single-versus-multi-GPU context, and separate end-to-end versus steady-state timing.
phase1/runs/task-011-attempt-001/prompt.txt:72:Scheduling findings from `papers/systolic_fdtd.pdf`: a naive GPU FDTD is constrained mostly by memory traffic and synchronization, so the important architectural seam is the split between chunk-local interior work, lower-dimensional boundary exchange, and a pluggable execution policy that may later delay or overlap neighbor consumption. Phase 1 should remain bulk-synchronous by default, but chunk metadata and runtime orchestration should already preserve per-face, per-stage halo semantics, interior-versus-boundary planning, and scheduler hooks so future systolic or overlapped execution can be added without redesign.
phase1/runs/task-011-attempt-001/prompt.txt:118:- `src/autofdtd/core/validation.py` now centralizes Phase 1 normalization rules, unsupported-feature checks, primitive geometry sanity checks, simulation-bound checks, and zero-dimension boundary coercion.
phase1/runs/task-011-attempt-001/prompt.txt:129:- `src/autofdtd/ir/models.py` now lowers `GridSpec` into typed grid IR with per-axis spec payloads plus resolved boundary and shape metadata, so later compiler work can consume inspectable discretization state ins019-attempt-001/codex.stdout.txt:90944:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:90945:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:90976:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:90977:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:90982:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:90984:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:90999:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91003:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:91004:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91005:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91021:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91023:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91024:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91025:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91055:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91061:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91075:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91076:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91096:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91104:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91126:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91127:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91128:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91134:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91142:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91154:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91164:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91173:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91180:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91187:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91206:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91208:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91209:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91242:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91243:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91246:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:91249:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91252:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:91257:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91258:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91275:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91276:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91278:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91293:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91310:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:91311:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91316: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91319:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91323:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:91325:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:91327:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91329:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:91330:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91332:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91333:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91339:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91344:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91345:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:91346:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91347:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:91348:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91349:+        return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91359: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91360:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91364:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:91365:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91368:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:91369:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91370:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:91371:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91372:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:91381:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:91389:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91399:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91404:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91407:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91408:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91410:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91447:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91448:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91449:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91478:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91479:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91480:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91498:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91499:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91500:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91510:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91513:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91519:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91522:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91528:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91531:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91540:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91543:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91550:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91553:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91570:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91571:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91579:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91580:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91586:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91597:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91598:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91600:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91602:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:91604:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91605:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91608:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91609:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91613:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91616:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91618:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:91621:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91623:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91624:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91630:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:91631:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91655:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91656:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91657:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91659:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:91672:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91673:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91674:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91688:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91689:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91692:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91752:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91754:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91763:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91765:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91769:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91773:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91778:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91779:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91785:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91786:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91791:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91794:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91796:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91800:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:91801:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91802:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:91806:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91807:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91813:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:91828:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91834:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91842:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91848:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91849:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:91853:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91855:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91856:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91865:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91868:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91869:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91871:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91877:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:91879:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:91881:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91894:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:91897:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:91898:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:91901:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91904:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:91906:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:91908:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91909:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91912:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91916:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:91928:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91929:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91930:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91931:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91932:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91933:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:91936:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91937:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91948:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:91951:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91952:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:91972:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91983:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:91984:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:91985:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91986:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:91988:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91993:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:91994:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92004:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92006:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:92008:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:92022:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:92027:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92039:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92042:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:92052:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:92063: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:92065:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:92089:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:92090:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:92091: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92098: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92107:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:92116:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92117:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92126:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92135:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:92304:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92305:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92306:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92317:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92318:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92319:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92349:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92360:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92366:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92372:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92380:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92381:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92386:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92387:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:92388:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:92390:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:92392:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:92394:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92410:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:92411:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92412:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92433:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:92434:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92465:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92466:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92471:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92473:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92488:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92492:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:92493:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92494:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92510:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92512:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:92513:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:92514:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:92544:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92550:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92564:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92565:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92585:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92593:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92615:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92616:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92617:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92623:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92631:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92643:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92653:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92662:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92669:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92676:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92695:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92697:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92698:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92731:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92732:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92735:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:92738:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92741:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:92746:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92747:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92764:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92765:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92767:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92782:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92799:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:92800:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92805: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92808:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92812:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:92814:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92816:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92818:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92819:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:92821:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92822:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:92828:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92833:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92834:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:92835:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:92836:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:92837:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92838:+        return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92848: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92849:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92853:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:92854:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:92857:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92858:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92859:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92860:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:92861:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:92870:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:92878:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92888:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92893:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:92896:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:92897:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:92899:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:92936:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92937:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92938:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92967:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92968:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92969:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:92987:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92988:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92989:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:92999:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93002:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93008:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93011:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93017:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93020:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93029:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93032:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93039:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93042:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93059:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93060:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93068:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93069:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93075:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93086:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93087:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93089:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93091:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:93093:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93094:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93097:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:93098:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:93102:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93105:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93107:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:93110:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93112:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93113:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93119:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:93120:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93144:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93145:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93146:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93148:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93161:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93162:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93163:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93177:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93178:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93181:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93241:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93243:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93252:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93254:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93258:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93262:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93267:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93268:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:93274:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93275:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:93280:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93283:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93285:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93289:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:93290:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93291:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93295:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93296:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93302:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:93317:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93323:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93331:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93337:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:93338:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:93342:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93344:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93345:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93354:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93357:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93358:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93360:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93366:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93368:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93370:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93383:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:93386:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:93387:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:93390:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93393:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93395:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:93397:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93398:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93401:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93405:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:93417:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93418:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93419:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93420:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93421:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93422:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:93425:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93426:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93437:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:93440:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93441:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:93461:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93472:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93473:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:93474:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93475:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:93477:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93482:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93483:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93514:492 101         return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93522:1121 107             boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:93526:53 103         raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93548:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93550:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:93552:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:93566:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:93571:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93583:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:93586:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:93596:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:93607: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:93609:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:93633:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:93634:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:93635: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93642: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93651:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:93660:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93661:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93670:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93679:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:93848:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93849:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93850:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:93861:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93862:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93863:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:93893:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93904:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93910:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93916:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93924:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93925:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:93930:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93931:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:93932:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:93934:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:93936:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:93938:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:93954:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:93955:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:93956:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:93977:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:93978:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94009:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94010:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94015:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94017:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94032:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94036:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:94037:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94038:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94054:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94056:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94057:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94058:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94088:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94094:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94108:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94109:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94129:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94137:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94159:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94160:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94161:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94167:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94175:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94187:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94197:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94206:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94213:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94220:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94239:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94241:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94242:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94275:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94276:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94279:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:94282:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94285:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:94290:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94291:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94308:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94309:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94311:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94326:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94343:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:94344:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94349: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94352:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94356:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:94358:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:94360:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94362:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:94363:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94365:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94366:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94372:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94377:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94378:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:94379:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94380:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:94381:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94382:+        return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94392: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94393:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94397:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:94398:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94401:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:94402:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94403:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:94404:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94405:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:94414:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:94422:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94432:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94437:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94440:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94441:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94443:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94480:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94481:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94482:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94511:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94512:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94513:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94531:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94532:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94533:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94543:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94546:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94552:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94555:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94561:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94564:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94573:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94576:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94583:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94586:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94603:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94604:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94612:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94613:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94619:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94630:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94631:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94633:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94635:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:94637:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94638:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94641:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94642:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94646:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94649:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94651:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:94654:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94656:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94657:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94663:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:94664:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94688:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94689:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94690:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94692:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:94705:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94706:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94707:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94721:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94722:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94725:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94785:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94787:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94796:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94798:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94802:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94806:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94811:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94812:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94818:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94819:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94824:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94827:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94829:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94833:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:94834:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94835:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:94839:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94840:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94846:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:94861:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94867:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94875:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94881:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94882:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:94886:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94888:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94889:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94898:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94901:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94902:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94904:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94910:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:94912:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:94914:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:94927:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:94930:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:94931:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:94934:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94937:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:94939:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:94941:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94942:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94945:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94949:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:94961:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94962:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94963:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94964:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94965:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94966:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:94969:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94970:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:94981:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:94984:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:94985:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:95005:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95016:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95017:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:95018:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95019:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:95021:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95026:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95027:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95040:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95042:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:95044:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:95058:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:95063:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95075:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95078:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:95088:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:95099: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:95101:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:95125:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:95126:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:95127: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95134: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95143:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:95152:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95153:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95162:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95171:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:95340:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95341:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95342:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95353:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95354:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95355:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95385:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95396:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95402:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95408:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95416:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95417:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95422:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95423:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:95424:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:95426:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:95428:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:95430:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95446:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:95447:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95448:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95469:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:95470:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95501:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95502:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95507:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95509:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95524:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95528:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:95529:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95530:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95546:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95548:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:95549:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:95550:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:95580:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95586:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95600:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95601:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95621:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95629:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95651:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95652:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95653:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95659:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95667:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95679:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95689:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95698:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95705:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95712:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95733:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95735:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95736:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95771:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95772:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95775:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:95778:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95781:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:95786:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95787:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95804:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95805:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95807:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95822:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95839:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:95840:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95845: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95848:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95852:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:95854:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95856:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95858:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95859:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:95861:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95862:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:95868:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95873:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95874:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:95875:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:95876:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:95877:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95878:+        return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95888: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95889:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95893:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:95894:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:95897:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95898:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95899:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95900:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:95901:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:95910:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:95918:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:95928:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95933:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:95936:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:95937:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:95939:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:95976:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95977:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:95978:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96007:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96008:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96009:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96027:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96028:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96029:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96039:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96042:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96048:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96051:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96057:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96060:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96069:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96072:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96079:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96082:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96099:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96100:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96108:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96109:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96115:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96126:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96127:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96129:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96131:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:96133:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96134:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96137:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:96138:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:96142:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96145:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96147:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:96150:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96152:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96153:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96159:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:96160:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96184:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96185:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96186:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96188:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:96201:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96202:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96203:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96217:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96218:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96221:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96281:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96283:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96292:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96294:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96298:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96302:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96307:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96308:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:96314:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96315:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:96320:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96323:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96325:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96329:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:96330:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96331:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96335:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96336:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96342:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:96357:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96363:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96371:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96377:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:96378:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:96382:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96384:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96385:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96394:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96397:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96398:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96400:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96406:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96408:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96410:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96423:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:96426:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:96427:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:96430:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96433:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96435:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:96437:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96438:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96441:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96445:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:96457:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96458:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96459:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96460:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96461:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96462:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:96465:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96466:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96477:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:96480:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96481:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:96501:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96512:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96513:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:96514:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96515:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:96517:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96522:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96523:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96533:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96535:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:96537:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:96551:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:96556:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96568:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:96571:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:96581:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:96592: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:96594:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:96618:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:96619:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:96620: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96627: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96636:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:96645:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96646:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96655:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96664:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:96833:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96834:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96835:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:96846:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96847:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96848:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96878:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96889:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96895:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96901:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96909:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96910:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:96915:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96916:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:96917:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:96919:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:96921:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:96923:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:96939:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:96940:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96941:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:96962:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:96963:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:96994:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:96995:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97000:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97002:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97017:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97021:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:97022:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97023:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97039:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97041:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97042:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97043:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97073:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97079:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97093:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97094:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97114:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97122:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97144:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97145:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97146:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97152:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97160:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97172:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97182:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97191:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97198:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97205:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97226:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97228:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97229:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97264:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97265:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97268:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:97271:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97274:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:97279:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97280:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97297:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97298:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97300:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97315:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97332:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:97333:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97338: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97341:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97345:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:97347:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:97349:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97351:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:97352:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97354:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97355:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97361:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97366:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97367:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:97368:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97369:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:97370:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97371:+        return boundary_spec_model_from_value(component).model_dump(mode="python", exclude_none=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97381: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97382:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97386:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:97387:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97390:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:97391:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97392:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:97393:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97394:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:97403:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:97411:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97421:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97426:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97429:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97430:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97432:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97469:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97470:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97471:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97500:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97501:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97502:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97520:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97521:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97522:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97532:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97535:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97541:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97544:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97550:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97553:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97562:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97565:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97572:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97575:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97592:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97593:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97601:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97602:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97608:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97619:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97620:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97622:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97624:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:97626:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97627:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97630:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97631:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97635:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97638:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97640:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:97643:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97645:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97646:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97652:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:97653:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97677:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97678:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97679:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97681:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:97694:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97695:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97696:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97710:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97711:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97714:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97774:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97776:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97785:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97787:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97791:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97795:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97800:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97801:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97807:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97808:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97813:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97816:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97818:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97822:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:97823:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97824:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:97828:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97829:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97835:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:97850:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97856:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97864:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97870:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97871:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:97875:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97877:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97878:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97887:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97890:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97891:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97893:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97899:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:97901:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:97903:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:97916:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:97919:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:97920:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:97923:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97926:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:97928:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:97930:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97931:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97934:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97938:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:97950:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97951:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97952:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97953:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97954:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97955:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:97958:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97959:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97970:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:97973:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:97974:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:97994:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98005:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98006:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:98007:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98008:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:98010:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98015:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98016:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98029:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98031:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:98033:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:98047:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:98052:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98064:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98067:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:98077:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:98088: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:98090:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:98114:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:98115:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:98116: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98123: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98132:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:98141:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98142:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98151:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98160:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:98329:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98330:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98331:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98342:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98343:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98344:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98374:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98385:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98391:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98397:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98405:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98406:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98411:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98412:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:98413:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:98415:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:98417:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:98419:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98435:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:98436:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98437:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98458:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:98459:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98490:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98491:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98496:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98498:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98513:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98517:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:98518:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98519:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98535:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98537:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:98538:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:98539:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:98569:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98575:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98589:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98590:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98610:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98618:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98640:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98641:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98642:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98648:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98656:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98668:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98678:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98687:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98694:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98701:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98722:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98724:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98725:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98760:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98761:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98764:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:98767:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98770:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:98775:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98776:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98793:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98794:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98796:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98811:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98830:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:98831:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98836: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98839:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98843:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:98845:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98847:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98849:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98850:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:98852:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98853:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:98859:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98864:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98865:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:98866:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:98867:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:98868:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98869:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:98881: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98882:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98886:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:98887:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:98890:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98891:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98892:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98893:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:98894:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:98903:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:98911:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:98921:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98926:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:98929:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:98930:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:98932:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:98969:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98970:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:98971:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99000:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99001:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99002:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99020:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99021:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99022:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99032:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99035:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99041:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99044:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99050:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99053:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99062:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99065:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99072:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99075:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99092:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99093:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99101:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99102:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99108:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99119:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99120:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99122:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99124:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:99126:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99127:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99130:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:99131:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:99135:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99138:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99140:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:99143:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99145:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99146:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99152:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:99153:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99177:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99178:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99179:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99181:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:99194:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99195:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99196:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99210:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99211:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99214:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99274:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99276:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99285:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99287:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99291:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99295:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99300:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99301:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:99307:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99308:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:99313:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99316:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99318:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99322:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:99323:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99324:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99328:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99329:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99335:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:99350:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99356:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99364:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99370:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:99371:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:99375:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99377:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99378:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99387:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99390:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99391:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99393:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99399:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99401:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99403:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99416:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:99419:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:99420:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:99423:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99426:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99428:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:99430:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99431:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99434:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99438:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:99450:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99451:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99452:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99453:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99454:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99455:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:99458:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99459:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99470:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:99473:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99474:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:99494:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99505:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99506:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:99507:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99508:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:99510:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99515:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99516:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99526:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99528:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:99530:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:99544:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:99549:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99561:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:99564:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:99574:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:99585: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:99587:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:99611:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:99612:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:99613: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99620: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99629:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:99638:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99639:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99648:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99657:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:99826:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99827:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99828:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:99839:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99840:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99841:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99871:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99882:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99888:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99894:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99902:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99903:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99908:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99909:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:99910:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:99912:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:99914:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:99916:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99932:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:99933:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99934:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:99955:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:99956:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:99987:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:99988:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:99993:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:99995:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100010:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100014:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:100015:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100016:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100032:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100034:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100035:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100036:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100066:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100072:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100086:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100087:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100107:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100115:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100137:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100138:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100139:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100145:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100153:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100165:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100175:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100184:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100191:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100198:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100219:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100221:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100222:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100257:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100258:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100261:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:100264:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100267:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:100272:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100273:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100290:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100291:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100293:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100308:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100327:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:100328:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100333: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100336:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100340:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:100342:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:100344:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100346:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:100347:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100349:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100350:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100356:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100361:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100362:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:100363:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100364:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:100365:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100366:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100378: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100379:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100383:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:100384:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100387:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:100388:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100389:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:100390:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100391:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:100400:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:100408:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100418:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100423:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100426:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100427:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100429:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100466:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100467:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100468:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100497:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100498:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100499:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100517:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100518:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100519:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100529:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100532:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100538:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100541:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100547:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100550:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100559:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100562:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100569:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100572:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100589:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100590:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100598:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100599:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100605:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100616:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100617:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100619:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100621:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:100623:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100624:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100627:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100628:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100632:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100635:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100637:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:100640:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100642:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100643:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100649:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:100650:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100674:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100675:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100676:-            if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100678:+            boundary_spec_to_ir(simulation.boundary_spec) if simulation.boundary_spec is not None else None
phase1/runs/task-019-attempt-001/codex.stdout.txt:100691:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100692:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100693:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100707:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100708:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100711:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100771:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100773:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100782:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100784:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100788:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100792:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100797:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100798:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100804:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100805:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100810:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100813:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100815:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100819:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:100820:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100821:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:100825:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100826:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100832:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:100847:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100853:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100861:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100867:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100868:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:100872:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100874:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100875:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100884:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100887:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100888:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100890:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100896:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:100898:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:100900:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:100913:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:100916:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:100917:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:100920:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100923:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:100925:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:100927:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100928:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100931:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100935:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:100947:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100948:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100949:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100950:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100951:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100952:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:100955:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100956:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100967:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:100970:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:100971:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:100991:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101002:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101003:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:101004:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101005:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:101007:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101012:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101013:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101026:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101028:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:101030:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:101044:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:101049:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101061:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101064:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:101074:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:101085: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:101087:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:101111:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:101112:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:101113: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101120: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101129:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:101138:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101139:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101148:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101157:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:101326:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101327:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101328:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101339:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101340:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101341:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101371:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101382:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101388:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101394:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101402:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101403:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101408:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101409:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:101410:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:101412:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:101414:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:101416:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101432:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:101433:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101434:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101455:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:101456:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101487:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101488:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101493:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101495:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101510:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101514:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:101515:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101516:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101532:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101534:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:101535:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:101536:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:101566:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101572:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101586:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101587:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101607:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101615:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101637:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101638:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101639:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101645:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101653:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101665:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101675:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101684:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101691:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101698:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101719:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101721:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101722:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101757:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101758:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101761:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:101764:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101767:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:101772:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101773:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101790:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101791:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101793:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101808:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101827:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:101828:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101833: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101836:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101840:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:101842:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101844:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101846:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101847:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:101849:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101850:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:101856:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101861:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101862:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:101863:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:101864:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:101865:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101866:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:101878: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101879:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101883:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:101884:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:101887:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101888:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101889:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101890:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:101891:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:101900:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:101908:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101918:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101923:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:101926:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:101927:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:101929:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:101966:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101967:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101968:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:101997:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101998:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:101999:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102017:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102018:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102019:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102029:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102032:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102038:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102041:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102047:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102050:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102059:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102062:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102069:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102072:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102129:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102130:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102157:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102158:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102168:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102179:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102180:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102182:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102184:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:102191:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102192:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102195:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:102196:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:102200:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102203:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102205:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:102208:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102210:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102211:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102217:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:102218:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102242:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102243:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102244:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102245:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:102257:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102258:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102259:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102273:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102274:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102277:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102337:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102339:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102348:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102350:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102354:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102358:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102363:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102364:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:102370:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102371:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:102376:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102379:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102381:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102385:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:102386:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102387:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102391:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102392:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102398:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:102413:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102419:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102427:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102433:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:102434:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:102438:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102440:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102441:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102450:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102453:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102454:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102456:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102462:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102464:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102466:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102479:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:102482:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:102483:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:102486:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102489:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102491:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:102493:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102494:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102497:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102501:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:102513:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102514:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102515:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102516:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102517:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102518:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:102521:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102522:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102533:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:102536:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102537:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:102557:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102568:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102569:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:102570:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102571:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:102573:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102578:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:102579:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102589:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102591:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:102593:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:102607:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:102612:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102624:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:102627:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:102637:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:102648: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:102650:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:102674:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:102675:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:102676: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102683: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102692:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:102701:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102702:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102711:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102720:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:102889:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102890:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102891:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:102902:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102903:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102904:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:102934:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102945:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102951:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102957:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102965:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102966:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:102971:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102972:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:102973:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:102975:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:102977:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:102979:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:102995:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:102996:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:102997:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103018:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:103019:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103050:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103051:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103056:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103058:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103073:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103077:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:103078:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103079:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103095:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103097:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103098:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103099:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103129:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103135:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103149:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103150:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103170:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103178:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103200:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103201:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103202:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103208:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103216:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103228:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103238:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103247:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103254:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103261:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103282:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103284:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103285:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103320:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103321:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103324:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:103327:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103330:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:103335:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103336:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103353:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103354:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103356:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103371:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103390:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:103391:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103396: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103399:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103403:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:103405:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:103407:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103409:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:103410:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:103412:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103413:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:103419:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103424:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103425:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:103426:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103427:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:103428:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103429:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103441: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103442:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103446:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:103447:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103450:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:103451:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103452:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:103453:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103454:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:103463:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:103471:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103481:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103486:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:103489:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103490:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103492:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103529:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103530:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103531:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103560:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103561:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103562:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103580:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103581:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103582:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103592:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103595:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103601:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103604:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103610:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103613:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103622:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103625:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103632:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103635:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:103692:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103693:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103720:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103721:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103731:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103742:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103743:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103745:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103747:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:103754:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103755:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103758:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103759:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103763:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103766:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103768:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:103771:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103773:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103774:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103780:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:103781:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103805:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103806:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103807:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103808:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:103820:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103821:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103822:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103836:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103837:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103840:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103900:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103902:+        raise ValueError("boundary halo updates require one ghost cell and one interior cell per side")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103911:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103913:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103917:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103921:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:103926:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103927:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103933:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103934:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103939:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:103942:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103944:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103948:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:103949:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103950:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:103954:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:103955:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103961:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:103976:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:103982:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:103990:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:103996:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:103997:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:104001:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104003:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104004:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104013:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104016:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104017:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104019:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104025:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104027:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104029:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104042:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:104045:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:104046:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:104049:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104052:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104054:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:104056:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104057:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104060:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104064:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:104076:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104077:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104078:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104079:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104080:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104081:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:104084:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104085:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:104096:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:104099:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104100:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:104120:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104131:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104132:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:104133:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104134:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:104136:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104141:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104142:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104155:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104157:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:104159:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:104173:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:104178:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104190:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:104193:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:104203:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:104214: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:104216:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:104240:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:104241:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:104242: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104249: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104258:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:104267:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104268:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104277:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104286:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:104455:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104456:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104457:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104468:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104469:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104470:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104500:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104511:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104517:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104523:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104531:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104532:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104537:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104538:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:104539:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:104541:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:104543:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:104545:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104561:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:104562:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104563:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104584:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:104585:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104616:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104617:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104622:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104624:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104639:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104643:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:104644:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104645:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104661:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104663:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:104664:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:104665:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:104695:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104701:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104715:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104716:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104736:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104744:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:104766:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104767:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104768:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104774:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104782:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104794:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104804:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104813:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104820:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104827:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104848:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104850:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104851:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104886:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104887:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104890:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:104893:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104896:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:104901:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104902:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104919:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104920:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:104922:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104937:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:104956:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:104957:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104962: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104965:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:104969:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:104971:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:104973:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104975:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:104976:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:104978:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:104979:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:104985:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104990:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:104991:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:104992:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:104993:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:104994:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:104995:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105007: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105008:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:105012:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:105013:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105016:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:105017:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:105018:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:105019:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:105020:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:105029:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:105037:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105047:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105052:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:105055:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105056:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105058:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105095:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105096:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105097:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105126:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105127:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105128:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105146:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105147:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105148:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105158:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105161:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105167:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105170:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105176:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105179:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105188:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105191:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105198:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105201:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105258:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105259:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105286:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105287:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105297:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105308:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105309:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105311:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105313:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:105320:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105321:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105324:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:105325:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:105329:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105332:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105334:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:105337:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105339:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105340:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105346:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:105347:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105371:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105372:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105373:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105374:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:105386:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105387:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105388:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105402:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105403:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105406:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105470:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:105473:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105483:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105485:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105489:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105493:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105498:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105499:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:105505:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105506:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:105511:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105514:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105516:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105520:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:105521:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:105522:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:105526:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105527:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105533:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:105548:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105554:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105562:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105568:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:105569:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:105573:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105575:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105576:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105585:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105588:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105589:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105591:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105597:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105599:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:105601:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105614:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:105617:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:105618:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:105621:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105624:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:105626:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:105628:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105629:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105632:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105636:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:105648:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105649:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105650:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105651:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105652:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105653:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:105656:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105657:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105668:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:105671:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105672:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:105692:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105703:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:105704:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:105705:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105706:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:105708:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105713:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:105714:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:105724:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:105726:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:105728:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:105742:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:105747:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105759:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:105762:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:105772:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:105783: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:105785:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:105809:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:105810:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:105811: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105818: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105827:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:105836:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105837:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105846:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:105855:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:106024:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106025:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106026:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106037:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106038:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106039:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106069:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106080:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106086:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106092:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106100:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106101:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106106:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106107:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:106108:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:106110:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:106112:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:106114:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106130:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:106131:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106132:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106153:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:106154:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106185:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106186:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106191:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106193:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106208:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106212:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:106213:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106214:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106230:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106232:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:106233:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:106234:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:106264:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106270:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106284:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106285:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106305:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106313:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106335:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106336:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106337:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106343:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106351:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106363:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106373:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106382:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106389:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106396:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106417:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106419:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106420:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106455:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106456:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106459:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:106462:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106465:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:106470:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106471:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106488:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106489:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106491:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106506:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106525:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:106526:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106531: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106534:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106538:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:106540:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:106542:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106544:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:106545:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:106547:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106548:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:106554:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106559:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106560:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:106561:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:106562:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:106563:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106564:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:106576: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106577:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106581:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:106582:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106585:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:106586:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106587:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:106588:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106589:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:106598:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:106606:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106616:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106621:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:106624:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106625:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106627:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:106664:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106665:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106666:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106695:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106696:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106697:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106715:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106716:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106717:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106727:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106730:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106736:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106739:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106745:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106748:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106757:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106760:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106767:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106770:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:106827:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106828:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106855:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106856:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106866:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106877:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106878:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:106880:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106882:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:106889:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106890:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106893:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:106894:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:106898:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106901:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106903:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:106906:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106908:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106909:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:106915:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:106916:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106940:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:106941:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106942:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:106943:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:106955:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106956:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106957:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:106971:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106972:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:106975:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107039:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107042:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107052:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:107054:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107058:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107062:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107067:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107068:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107074:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107075:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107080:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:107083:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107085:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107089:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:107090:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107091:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107095:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107096:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107102:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107117:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107123:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107131:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107137:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:107138:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:107142:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107144:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107145:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107154:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107157:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107158:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107160:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107166:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107168:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107170:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107183:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:107186:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:107187:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:107190:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107193:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107195:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:107197:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107198:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107201:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107205:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:107217:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107218:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107219:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107220:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107221:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107222:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:107225:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107226:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:107237:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:107240:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107241:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:107261:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107272:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107273:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:107274:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107275:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:107277:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107282:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107283:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107296:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107298:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:107300:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:107314:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:107319:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107331:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:107334:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:107344:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:107355: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:107357:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:107381:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:107382:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:107383: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107390: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107399:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:107408:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107409:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107418:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107427:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:107596:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107597:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107598:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107609:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107610:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107611:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107641:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107652:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107658:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107664:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107672:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107673:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107678:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107679:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:107680:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:107682:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:107684:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:107686:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107702:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:107703:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107704:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107725:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:107726:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107757:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107758:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107763:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107765:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107780:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107784:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:107785:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107786:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107802:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107804:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107805:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107806:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:107836:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107842:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107856:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107857:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107877:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107885:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:107907:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107908:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107909:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:107915:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107923:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107935:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107945:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:107954:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107961:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107968:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:107989:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:107991:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:107992:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108027:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108028:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108031:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:108034:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108037:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:108042:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108043:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108060:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108061:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108063:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108078:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108097:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:108098:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108103: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108106:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108110:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:108112:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108114:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108116:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108117:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108119:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108120:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108126:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108131:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108132:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:108133:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:108134:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:108135:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108136:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108148: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108149:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108153:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:108154:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108157:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108158:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108159:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108160:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108161:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108170:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:108178:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108188:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108193:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108196:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108197:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108199:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108236:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108237:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108238:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108267:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108268:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108269:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108287:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108288:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108289:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108299:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108302:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108308:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108311:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108317:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108320:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108329:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108332:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108339:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108342:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108399:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108400:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108427:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108428:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108438:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108449:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108450:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108452:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108454:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:108461:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108462:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108465:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:108466:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:108470:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108473:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108475:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:108478:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108480:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108481:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108487:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:108488:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108512:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108513:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108514:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108515:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:108527:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108528:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108529:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108543:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108544:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108547:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108611:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:108614:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108624:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108626:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108630:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108634:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108639:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108640:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:108646:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108647:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:108652:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108655:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108657:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108661:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:108662:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:108663:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:108667:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108668:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108674:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:108689:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108695:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108703:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108709:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108710:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:108714:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108716:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108717:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108726:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108729:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108730:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108732:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108738:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108740:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:108742:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108755:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:108758:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:108759:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:108762:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108765:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:108767:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:108769:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108770:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108773:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108777:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:108789:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108790:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108791:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108792:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108793:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108794:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:108797:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108798:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108812:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:108815:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108816:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:108836:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108847:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:108848:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:108849:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108850:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:108852:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108857:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:108858:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:108868:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:108870:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:108872:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:108886:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:108891:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108903:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:108906:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:108916:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:108927: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:108929:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:108953:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:108954:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:108955: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108962: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108971:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:108980:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108981:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108990:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:108999:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:109168:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109169:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109170:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109181:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109182:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109183:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109213:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109224:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109230:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109236:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109244:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109245:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109250:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109251:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:109252:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:109254:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:109256:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:109258:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109274:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:109275:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109276:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109297:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:109298:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109329:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109330:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109335:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109337:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109352:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109356:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:109357:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109358:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109374:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109376:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:109377:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:109378:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:109408:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109414:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109428:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109429:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109449:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109457:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109479:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109480:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109481:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109487:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109495:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109507:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109517:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109526:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109533:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109540:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109561:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109563:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109564:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109599:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109600:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109603:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:109606:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109609:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:109614:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109615:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109632:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109633:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109635:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109650:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109669:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:109670:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109675: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109678:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109682:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:109684:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:109686:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109688:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:109689:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:109691:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109692:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:109698:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109703:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109704:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:109705:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:109706:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:109707:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109708:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:109720: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109721:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109725:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:109726:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:109729:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:109730:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109731:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:109732:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:109733:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:109742:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:109750:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109760:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109765:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:109768:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:109769:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109771:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:109808:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109809:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109810:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109839:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109840:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109841:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:109859:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109860:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109861:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:109871:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109874:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109880:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109883:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109889:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109892:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109901:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109904:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109911:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:109914:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:109971:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:109972:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:109999:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110000:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110010:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110021:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110022:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110024:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:110026:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:110033:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110034:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110037:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110038:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110042:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110045:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:110047:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:110050:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110052:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:110053:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110059:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:110060:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110084:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110085:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110086:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110087:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:110099:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110100:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110101:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110115:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110116:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110119:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110183:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110186:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110196:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110198:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110202:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110206:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110211:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110212:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110218:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110219:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110224:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110227:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110229:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110233:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:110234:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110235:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110239:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110240:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110246:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110261:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110267:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110275:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110281:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:110282:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:110286:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110288:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110289:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110298:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110301:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110302:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110304:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110310:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110312:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110314:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110327:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:110330:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:110331:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:110334:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110337:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110339:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:110341:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110342:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110345:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110349:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:110361:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110362:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110363:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110364:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110365:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110366:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:110369:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110370:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110384:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:110387:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110388:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:110408:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110419:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110420:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:110421:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110422:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:110424:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110429:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110430:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110457:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110459:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:110461:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:110475:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:110480:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110492:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:110495:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:110505:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:110516: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:110518:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:110542:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:110543:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:110544: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110551: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110560:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:110569:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110570:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110579:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110588:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:110757:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110758:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110759:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:110770:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110771:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110772:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110802:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110813:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110819:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110825:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110833:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110834:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110839:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110840:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:110841:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:110843:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:110845:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:110847:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110863:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:110864:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110865:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110886:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:110887:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:110918:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110919:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:110924:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:110926:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110941:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110945:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:110946:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110947:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:110963:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:110965:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110966:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110967:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:110997:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111003:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111017:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111018:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111038:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111046:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111068:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111069:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111070:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111076:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111084:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111096:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111106:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111115:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111122:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111129:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111150:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111152:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111153:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111188:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111189:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111192:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:111195:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111198:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:111203:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111204:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111221:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111222:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111224:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111239:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111258:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:111259:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111264: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111267:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111271:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:111273:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:111275:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111277:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:111278:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111280:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111281:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111287:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111292:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111293:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:111294:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111295:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:111296:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111297:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111309: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111310:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111314:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:111315:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111318:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:111319:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111320:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:111321:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111322:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:111331:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:111339:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111349:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111354:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111357:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111358:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111360:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111397:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111398:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111399:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111428:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111429:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111430:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111448:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111449:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111450:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111460:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111463:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111469:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111472:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111478:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111481:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111490:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111493:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111500:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111503:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111560:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111561:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111588:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111589:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111599:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111610:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111611:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111613:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111615:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:111622:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111623:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111626:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:111627:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:111631:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111634:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111636:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:111639:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111641:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111642:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111648:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:111649:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111673:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111674:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111675:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111676:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:111688:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111689:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111690:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111704:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111705:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111708:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111772:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111775:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111785:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111787:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111791:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111795:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111800:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111801:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:111807:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111808:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:111813:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111816:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111818:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111822:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:111823:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111824:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:111828:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111829:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111835:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:111850:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111856:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111864:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111870:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111871:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:111875:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111877:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111878:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111887:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111890:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111891:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111893:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111899:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:111901:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:111903:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:111916:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:111919:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:111920:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:111923:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111926:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:111928:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:111930:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111931:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111934:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111938:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:111950:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111951:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111952:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111953:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111954:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111955:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:111958:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111959:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111973:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:111976:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:111977:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:111997:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112008:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112009:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:112010:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112011:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:112013:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112018:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112019:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112056:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112058:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:112060:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:112074:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:112079:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112091:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112094:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:112104:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:112115: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:112117:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:112141:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:112142:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:112143: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112150: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112159:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:112168:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112169:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112178:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112187:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:112356:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112357:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112358:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112369:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112370:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112371:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112401:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112412:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112418:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112424:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112432:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112433:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112438:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112439:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:112440:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:112442:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:112444:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:112446:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112462:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:112463:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112464:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112485:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:112486:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112517:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112518:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112523:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112525:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112540:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112544:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:112545:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112546:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112562:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112564:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:112565:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:112566:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:112596:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112602:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112616:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112617:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112637:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112645:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112667:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112668:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112669:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112675:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112683:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112695:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112705:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112714:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112721:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112728:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112749:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112751:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112752:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112787:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112788:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112791:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:112794:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112797:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:112802:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112803:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112820:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112821:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112823:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112838:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112857:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:112858:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112863: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112866:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112870:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:112872:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112874:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112876:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112877:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:112879:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112880:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:112886:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112891:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112892:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:112893:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:112894:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:112895:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112896:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:112908: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112909:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112913:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:112914:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:112917:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112918:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112919:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112920:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:112921:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:112930:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:112938:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:112948:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112953:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:112956:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:112957:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:112959:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:112996:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112997:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:112998:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113027:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113028:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113029:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113047:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113048:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113049:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113059:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113062:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113068:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113071:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113077:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113080:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113089:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113092:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113099:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113102:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113159:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113160:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113187:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113188:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113198:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113209:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113210:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113212:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113214:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:113221:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113222:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113225:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:113226:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:113230:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113233:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113235:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:113238:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113240:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113241:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113247:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:113248:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113272:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113273:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113274:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113275:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:113287:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113288:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113289:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113303:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113304:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113307:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113371:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:113374:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113384:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113386:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113390:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113394:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113399:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113400:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:113406:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113407:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:113412:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113415:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113417:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113421:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:113422:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:113423:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:113427:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113428:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113434:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:113449:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113455:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113463:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113469:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:113470:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:113474:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113476:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113477:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113486:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113489:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113490:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113492:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113498:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113500:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:113502:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113515:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:113518:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:113519:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:113522:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113525:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113527:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:113529:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113530:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113533:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113537:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:113549:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113550:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113551:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113552:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113553:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113554:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:113557:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113558:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113572:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:113575:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113576:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:113596:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113607:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:113608:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:113609:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113610:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:113612:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113617:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:113618:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:113623:tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
phase1/runs/task-019-attempt-001/codex.stdout.txt:113624:  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113680:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113682:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:113684:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:113698:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:113703:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113715:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:113718:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:113728:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:113739: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:113741:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:113765:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:113766:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:113767: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113774: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113783:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:113792:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113793:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113802:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113811:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:113980:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113981:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113982:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:113993:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113994:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:113995:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114025:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114036:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114042:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114048:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114056:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114057:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114062:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114063:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:114064:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:114066:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:114068:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:114070:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114086:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:114087:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114088:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114109:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:114110:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114141:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114142:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114147:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114149:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114164:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114168:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:114169:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114170:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114186:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114188:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:114189:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:114190:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:114220:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114226:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114240:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114241:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114261:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114269:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114291:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114292:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114293:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114299:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114307:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114319:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114329:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114338:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114345:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114352:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114373:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114375:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114376:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114411:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114412:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114415:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:114418:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114421:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:114426:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114427:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114444:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114445:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114447:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114462:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114481:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:114482:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114487: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114490:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114494:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:114496:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:114498:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114500:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:114501:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:114503:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114504:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:114510:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114515:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114516:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:114517:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114518:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:114519:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114520:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:114532: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114533:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114537:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:114538:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114541:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:114542:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114543:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:114544:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114545:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:114554:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:114562:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114572:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114577:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:114580:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114581:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114583:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:114620:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114621:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114622:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114651:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114652:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114653:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114671:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114672:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114673:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114683:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114686:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114692:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114695:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114701:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114704:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114713:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114716:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114723:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114726:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:114783:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114784:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114811:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114812:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114822:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114833:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114834:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:114836:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114838:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:114845:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114846:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114849:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:114850:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:114854:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114857:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114859:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:114862:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114864:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114865:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:114871:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:114872:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114896:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:114897:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114898:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:114899:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:114911:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114912:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114913:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:114927:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114928:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114931:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:114995:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:114998:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115008:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:115010:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115014:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115018:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115023:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115024:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115030:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115031:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115036:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:115039:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115041:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115045:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:115046:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115047:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115051:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115052:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115058:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115073:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115079:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115087:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115093:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:115094:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:115098:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115100:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115101:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115110:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115113:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115114:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115116:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115122:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115124:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115126:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115139:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:115142:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:115143:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:115146:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115149:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115151:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:115153:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115154:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115157:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115161:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:115173:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115174:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115175:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115176:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115177:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115178:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:115181:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115182:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:115196:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:115199:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115200:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:115220:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115231:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115232:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:115233:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115234:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:115236:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115241:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115242:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115245:{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.","key_findings":["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.","Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.","`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.","Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.","Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed boundary IR classes and lowering helpers."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary scope documentation for the implemented Phase 1 subset."},{"path":"/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:115254:+The boundary namespace now has the first runtime-meaningful boundary subset. `Periodic`,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115256:+`autofdtd.ir` lowers them into typed boundary IR; `autofdtd.compiler.boundaries`
phase1/runs/task-019-attempt-001/codex.stdout.txt:115258:+periodic-copy or conductor-reflection ghost updates without folding boundary behavior
phase1/runs/task-019-attempt-001/codex.stdout.txt:115272:+Phase 1 now has an explicit non-absorbing boundary surface built around
phase1/runs/task-019-attempt-001/codex.stdout.txt:115277:+- `Periodic`, `PECBoundary`, and `PMCBoundary` are typed boundary-edge models.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115289:+- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:115292:+- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
phase1/runs/task-019-attempt-001/codex.stdout.txt:115302:+- `BlochBoundary`, `PML`, `StablePML`, `Absorber`, and ABC-family boundaries remain
phase1/runs/task-019-attempt-001/codex.stdout.txt:115313: - `Simulation.resolved_grid()` for inspectable axis-boundary planning
phase1/runs/task-019-attempt-001/codex.stdout.txt:115315:+- compiled non-absorbing boundary runtime metadata and ghost-update helpers
phase1/runs/task-019-attempt-001/codex.stdout.txt:115339:-## [RUNNING] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:115340:+## [COMPLETED] task-019 - Implement boundary support for Periodic, PECBoundary, PMCBoundary, and Boundary containers
phase1/runs/task-019-attempt-001/codex.stdout.txt:115341: Success: Support the foundational non-absorbing boundary surface, including Boundary and BoundarySpec composition, periodic behavior, conductor boundaries, and the runtime transform or kernel support needed by the timestep loop.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115348: Notes: These are the first boundary primitives the compiler and runtime should rely on.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115357:+{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, boundary compiler, ghost-update kernels, runtime wrapper, docs, and tests.","key_findings":["`autofdtd.boundaries` now exposes typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, including periodic-opposite-conductor coercion so one axis never mixes periodic and reflective runtime semantics.","`autofdtd.ir` now lowers the supported subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while unsupported boundary families still fall back to the generic boundary envelope.","`autofdtd.compiler.boundaries.compile_boundary_spec()` now produces explicit per-face halo metadata, parity-sign transforms, periodic-versus-reflective axis grouping, and stage ordering for later timestep integration.","`autofdtd.kernels.boundaries` and `autofdtd.runtime.boundaries` now provide concrete ghost-cell update helpers for `(nx, ny, nz, 3)` vector fields, covering periodic copy behavior plus PEC and PMC reflection parity.","Examples, docs, working memory, and focused tests now exercise the public models, IR lowering, runtime compilation, explicit unsupported-boundary errors, and ghost-update behavior."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Added the typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/core/validation.py","description":"Extended normalization and zero-dimension coercion to understand the supported boundary subset."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Added typed boundary IR classes and lowering helpers for the supported subset."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Added boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Added concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Added the runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/examples/boundaries.py","description":"Added a simple compiled-boundary example snapshot."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Documented the implemented subset, runtime lowering, and current limits."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Added focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/phase1/working-memory.md","description":"Recorded the boundary-runtime design decisions and handoff notes for later Bloch and absorber tasks."}],"error_summary":"","follow_up_notes":"Verification passed with `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py`, `PYTHONPATH=src pytest -q tests/test_imports.py tests/test_planning.py`, and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment, so lint verification could not be run here.","next_action":"none"}
phase1/runs/task-019-attempt-001/codex.stdout.txt:115366:+- `src/autofdtd/boundaries/models.py` now provides the first typed non-absorbing boundary surface with `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec`, including periodic-opposite-conductor coercion.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115367:+- `src/autofdtd/ir/models.py` now lowers the supported boundary subset into typed `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR` payloads while leaving unsupported boundary families in the generic boundary envelope.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115376:+- Phase 1 boundary runtime support is intentionally narrow: runtime compilation accepts only `BoundarySpec` composed from `Periodic`, `PECBoundary`, and `PMCBoundary`; PML, Bloch, and ABC-family entries stay parseable at the API layer but are not compiled on this task’s runtime path.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115385:+- Later Bloch and absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics.
phase1/runs/task-019-attempt-001/codex.stdout.txt:115554:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115555:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115556:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115567:+    "boundary_edge_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115568:+    "boundary_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115569:+    "boundary_spec_model_from_value",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115599:+    """Base model for one simulation-domain boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115610:+    """Periodic boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115616:+    """Perfect electric conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115622:+    """Perfect magnetic conductor boundary condition."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115630:+def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115631:+    """Normalize a supported Phase 1 non-absorbing boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115636:+        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115637:+    boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:115638:+    if boundary_type == "Periodic":
phase1/runs/task-019-attempt-001/codex.stdout.txt:115640:+    if boundary_type == "PECBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:115642:+    if boundary_type == "PMCBoundary":
phase1/runs/task-019-attempt-001/codex.stdout.txt:115644:+    raise TypeError(f"unsupported Phase 1 non-absorbing boundary edge type {boundary_type!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115660:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:115661:+            if boundary_type in {"Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115662:+                return boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115683:+                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
phase1/runs/task-019-attempt-001/codex.stdout.txt:115684:+                "boundary so runtime halo handling stays reflection-only on that axis.",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115715:+def boundary_model_from_value(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115716:+    """Normalize a supported axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115721:+        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115723:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115738:+    def _coerce_axis_boundary(cls, value: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115742:+            boundary_type = str(value.get("type"))
phase1/runs/task-019-attempt-001/codex.stdout.txt:115743:+            if boundary_type in {"Boundary", "Periodic", "PECBoundary", "PMCBoundary"}:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115744:+                return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115760:+    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115762:+            x=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115763:+            y=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115764:+            z=Boundary(plus=boundary, minus=boundary),
phase1/runs/task-019-attempt-001/codex.stdout.txt:115794:+def boundary_spec_model_from_value(value: object) -> BoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115800:+        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115814:+    compile_boundary_spec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115815:+    normalize_axis_boundary,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115835:+    "compile_boundary_spec",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115843:+    "normalize_axis_boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:115865:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115866:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115867:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:115873:+    """Runtime boundary mode consumed by staged halo handling."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115881:+    """Runtime-ready metadata for one boundary face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115893:+    """Runtime-ready metadata for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115903:+    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115912:+    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115919:+    edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115926:+    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
phase1/runs/task-019-attempt-001/codex.stdout.txt:115947:+def _compile_axis(axis_name: Literal["x", "y", "z"], boundary: Boundary) -> CompiledBoundaryAxis:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115949:+    minus_mode = _mode_for_edge(boundary.minus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115950:+    plus_mode = _mode_for_edge(boundary.plus)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115985:+def compile_boundary_spec(value: object | None) -> CompiledBoundarySpec:
phase1/runs/task-019-attempt-001/codex.stdout.txt:115986:+    """Compile the supported non-absorbing boundary subset into runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:115989:+        boundary_spec = BoundarySpec()
phase1/runs/task-019-attempt-001/codex.stdout.txt:115992:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:115995:+                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
phase1/runs/task-019-attempt-001/codex.stdout.txt:116000:+        axis_name: _compile_axis(axis_name, axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116001:+        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116018:+def normalize_axis_boundary(value: object) -> Boundary:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116019:+    """Normalize a single axis boundary using the supported subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116021:+    return boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116036:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116055:+    if context == "boundary_spec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:116056:+        return _normalize_boundary_spec(normalized)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116061: def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116064:+    if isinstance(boundary_spec, BoundarySpec):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116068:+            axis_boundary = boundary_spec[axis_name]
phase1/runs/task-019-attempt-001/codex.stdout.txt:116070:+                updated[axis_name] = axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:116072:+            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116074:+            changed = changed or coerced is not axis_boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:116075:+        return boundary_spec.copy_update(**updated) if changed else boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116077:     if not isinstance(boundary_spec, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116078:         return boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116084:+def _normalize_boundary_spec(component: object) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116089:+    boundary_type = component_type_name(component)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116090:+    if boundary_type != "BoundarySpec":
phase1/runs/task-019-attempt-001/codex.stdout.txt:116091:+        raise ValueError("simulation.boundary_spec must use a top-level 'BoundarySpec' container")
phase1/runs/task-019-attempt-001/codex.stdout.txt:116092:+    boundary_tags = set(_iter_type_tags(component))
phase1/runs/task-019-attempt-001/codex.stdout.txt:116093:+    if boundary_tags and boundary_tags <= _PHASE1_BOUNDARY_TYPES:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116094:+        return boundary_spec_model_from_value(component).model_dump(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116106: def _coerce_axis_boundary(boundary: object, *, axis_name: str) -> object:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116107:+    if isinstance(boundary, Boundary):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116111:+            edge = boundary[side]
phase1/runs/task-019-attempt-001/codex.stdout.txt:116112:+            coerced = _coerce_axis_boundary(edge, axis_name=axis_name)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116115:+        return boundary.copy_update(**updated) if changed else boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:116116:+    if isinstance(boundary, (Periodic, PECBoundary, PMCBoundary)):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116117:+        return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:116118:     if not isinstance(boundary, Mapping):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116119:         return boundary
phase1/runs/task-019-attempt-001/codex.stdout.txt:116128:+from autofdtd.examples.boundaries import boundary_runtime_snapshot
phase1/runs/task-019-attempt-001/codex.stdout.txt:116136:+    "boundary_runtime_snapshot",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116146:+"""Examples for the foundational non-absorbing boundary subset."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116151:+from autofdtd.compiler import compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116154:+def boundary_runtime_snapshot() -> dict[str, object]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116155:+    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116157:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116194:+    boundary_edge_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116195:+    boundary_spec_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116196:+    boundary_to_ir,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116225:+    "boundary_edge_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116226:+    "boundary_spec_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116227:+    "boundary_to_ir",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116245:+    boundary_edge_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116246:+    boundary_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116247:+    boundary_spec_model_from_value,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116257:+    """Typed execution IR for a periodic boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116260:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116266:+    """Typed execution IR for a PEC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116269:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116275:+    """Typed execution IR for a PMC boundary edge."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116278:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116287:+    """Typed execution IR for one axis boundary pair."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116290:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116297:+    """Typed execution IR for the Phase 1 boundary container."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116300:+    family: Literal["boundary"] = "boundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116357:-    boundary_spec: ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116358:+    boundary_spec: BoundarySpecIR | ComponentIR | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116385:+def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116386:+    """Lower a supported boundary edge into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116396:+        edge = boundary_edge_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116407:+def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116408:+    """Lower a supported axis boundary pair into typed execution IR."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116410:+    boundary: Boundary | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116412:+        boundary = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:116419:+        boundary = boundary_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116420:+    if boundary is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116423:+        plus=boundary_edge_to_ir(boundary.plus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:116424:+        minus=boundary_edge_to_ir(boundary.minus),
phase1/runs/task-019-attempt-001/codex.stdout.txt:116428:+def boundary_spec_to_ir(value: object) -> BoundarySpecIR | ComponentIR:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116431:+    boundary_spec: BoundarySpec | None = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116433:+        boundary_spec = value
phase1/runs/task-019-attempt-001/codex.stdout.txt:116436:+            boundary_spec = boundary_spec_model_from_value(value)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116438:+            boundary_spec = None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116439:+    if boundary_spec is None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116445:+    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
phase1/runs/task-019-attempt-001/codex.stdout.txt:116446:+        lowered = boundary_to_ir(axis_boundary)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116470:         boundary_spec=(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116471:-            component_to_ir(simulation.boundary_spec, family=ComponentFamily.BOUNDARY)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116472:+            boundary_spec_to_ir(simulation.boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116473:             if simulation.boundary_spec is not None
phase1/runs/task-019-attempt-001/codex.stdout.txt:116485:+    apply_axis_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116486:+    apply_boundary_ghosts,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116487:+    boundary_kernel_metadata,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116501:+    "apply_axis_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116502:+    "apply_boundary_ghosts",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116505:+    "boundary_kernel_metadata",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116569:+        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
phase1/runs/task-019-attempt-001/codex.stdout.txt:116572:+            "boundary halo updates require one ghost cell and one interior cell per side"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116582:+def apply_axis_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116584:+    axis_boundary: CompiledBoundaryAxis,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116588:+    """Apply one axis boundary pair to a vector field with one ghost cell per face."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116592:+    axis_index = "xyz".index(axis_boundary.axis)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116597:+        axis_boundary.minus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116598:+        getattr(axis_boundary.minus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:116604:+        axis_boundary.plus.mode,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116605:+        getattr(axis_boundary.plus, signs_name),
phase1/runs/task-019-attempt-001/codex.stdout.txt:116610:+def apply_boundary_ghosts(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116613:+    boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116615:+    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116619:+    for axis_boundary in boundary_spec.axes():
phase1/runs/task-019-attempt-001/codex.stdout.txt:116620:+        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
phase1/runs/task-019-attempt-001/codex.stdout.txt:116621:+        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
phase1/runs/task-019-attempt-001/codex.stdout.txt:116625:+def boundary_kernel_metadata() -> dict[str, Any]:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116626:+    """Expose the current boundary backend and staged update choices."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116632:+        "stages": ("electric_boundary", "magnetic_boundary"),
phase1/runs/task-019-attempt-001/codex.stdout.txt:116647:+    build_boundary_runtime,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116653:+    "build_boundary_runtime",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116661:+"""Runtime-facing boundary orchestration for the foundational non-absorbing Phase 1 surface."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116667:+from autofdtd.compiler.boundaries import CompiledBoundarySpec, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116668:+from autofdtd.kernels.boundaries import apply_boundary_ghosts
phase1/runs/task-019-attempt-001/codex.stdout.txt:116672:+    """Small runtime wrapper around compiled non-absorbing boundary metadata."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116674:+    def __init__(self, boundary_spec: object | None = None) -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116675:+        self.compiled = compile_boundary_spec(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116684:+        return apply_boundary_ghosts(electric_field, magnetic_field, self.compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116687:+def build_boundary_runtime(boundary_spec: object | None = None) -> BoundaryRuntime:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116688:+    """Build the default Phase 1 boundary runtime wrapper."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116690:+    return BoundaryRuntime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116696:+    compiled_boundary_spec: CompiledBoundarySpec,
phase1/runs/task-019-attempt-001/codex.stdout.txt:116698:+    """Apply boundary ghosts using a precompiled boundary specification."""
phase1/runs/task-019-attempt-001/codex.stdout.txt:116700:+    return apply_boundary_ghosts(electric_field, magnetic_field, compiled_boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116713:+from autofdtd.compiler import BoundaryMode, compile_boundary_spec
phase1/runs/task-019-attempt-001/codex.stdout.txt:116716:+from autofdtd.kernels import apply_boundary_ghosts, boundary_kernel_metadata
phase1/runs/task-019-attempt-001/codex.stdout.txt:116717:+from autofdtd.runtime import build_boundary_runtime
phase1/runs/task-019-attempt-001/codex.stdout.txt:116720:+def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116723:+        match="periodic boundary opposite a PEC/PMC boundary",
phase1/runs/task-019-attempt-001/codex.stdout.txt:116725:+        boundary = Boundary(minus=PECBoundary(), plus=Periodic())
phase1/runs/task-019-attempt-001/codex.stdout.txt:116727:+    assert boundary.minus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116728:+    assert boundary.plus.type == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116731:+def test_simulation_normalizes_supported_boundary_subset() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116735:+        boundary_spec={
phase1/runs/task-019-attempt-001/codex.stdout.txt:116747:+    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116748:+    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116749:+    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116750:+    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116751:+    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116752:+    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
phase1/runs/task-019-attempt-001/codex.stdout.txt:116755:+def test_compile_boundary_spec_builds_runtime_modes_and_rejects_unsupported_edges() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116756:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116770:+        compile_boundary_spec({"type": "BoundarySpec", "x": {"type": "PML"}})
phase1/runs/task-019-attempt-001/codex.stdout.txt:116773:+def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116774:+    compiled = compile_boundary_spec(
phase1/runs/task-019-attempt-001/codex.stdout.txt:116794:+    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116805:+def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
phase1/runs/task-019-attempt-001/codex.stdout.txt:116806:+    boundary_spec = BoundarySpec(x=Boundary.periodic(), y=Boundary.pec(), z=Boundary.pmc())
phase1/runs/task-019-attempt-001/codex.stdout.txt:116807:+    runtime = build_boundary_runtime(boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116808:+    metadata = boundary_kernel_metadata()
phase1/runs/task-019-attempt-001/codex.stdout.txt:116810:+        Simulation(size=(1.0, 1.0, 1.0), run_time=1.0, boundary_spec=boundary_spec)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116815:+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
phase1/runs/task-019-attempt-001/codex.stdout.txt:116816:+    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
phase1/runs/task-019-attempt-001/codex.stdout.txt:116820:{"task_id":"task-019","status":"completed","summary":"Implemented the foundational non-absorbing boundary surface across the public API, typed IR, compiler/runtime metadata, ghost-update kernels, tests, and docs.","key_findings":["The public API now has typed `Periodic`, `PECBoundary`, `PMCBoundary`, `Boundary`, and `BoundarySpec` models, with periodic-opposite-conductor coercion to keep each axis on one runtime mode.","Boundary lowering is now typed in IR via `PeriodicIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `BoundaryIR`, and `BoundarySpecIR`, while unsupported boundary families still fall back to the generic boundary envelope.","`compile_boundary_spec()` now produces explicit per-face halo metadata and reflection parity, and the runtime/kernel path can apply periodic copy plus PEC/PMC ghost-cell transforms on `(nx, ny, nz, 3)` vector fields.","Unsupported runtime compilation is explicit: PML, Bloch, and ABC-family boundaries are still parseable at the API layer but rejected by the Phase 1 non-absorbing boundary compiler.","Focused verification passed: `PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_containers.py tests/test_ir.py tests/test_imports.py tests/test_planning.py` and `PYTHONPATH=src python -m compileall src`. `ruff` was not installed in the environment."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Typed non-absorbing boundary public models and normalization helpers."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary runtime compilation metadata for periodic, PEC, and PMC halo handling."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Concrete ghost-cell update helpers and boundary kernel metadata."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime wrapper that applies compiled boundary ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"Typed boundary IR classes and lowering helpers."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests for boundary normalization, IR lowering, compiler/runtime behavior, and explicit unsupported-boundary errors."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary scope documentation for the implemented Phase 1 subset."},{"path":"/workspace/autofdtd/phase1/runs/task-019-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"The supported runtime subset is intentionally narrow for this task. `task-020` and later boundary tasks should extend the compiled boundary metadata and ghost-update stage instead of redesigning the public boundary containers.","next_action":"none"}

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary models for the foundational Phase 1 boundary surface."""

from __future__ import annotations

import cmath
import warnings
from collections.abc import Mapping
from typing import Literal

from pydantic import Field, field_validator, model_validator

from autofdtd.core.models import TaggedModel


def _normalize_optional_name(value: str | None) -> str | None:
    if value is None:
        return None
    stripped = value.strip()
    if not stripped:
        raise ValueError("names must not be empty or whitespace-only")
    return stripped


class BoundaryEdge(TaggedModel):
    """Base model for one simulation-domain boundary edge."""

    name: str | None = None

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_optional_name(value)


class Periodic(BoundaryEdge):
    """Periodic boundary condition."""

    type: Literal["Periodic"] = "Periodic"


class BlochBoundary(BoundaryEdge):
    """Phase-aware periodic boundary condition."""

    type: Literal["BlochBoundary"] = "BlochBoundary"
    bloch_vec: float

    @property
    def bloch_phase(self) -> complex:
        """Return the forward phase factor for a positive-domain wrap."""

        return cmath.exp(1j * 2.0 * cmath.pi * self.bloch_vec)


class PECBoundary(BoundaryEdge):
    """Perfect electric conductor boundary condition."""

    type: Literal["PECBoundary"] = "PECBoundary"


class PMCBoundary(BoundaryEdge):
    """Perfect magnetic conductor boundary condition."""

    type: Literal["PMCBoundary"] = "PMCBoundary"


class PMLParams(TaggedModel):
    """Polynomial profile parameters for the Phase 1 baseline PML."""

    type: Literal["PMLParams"] = "PMLParams"
    sigma_order: int = 3
    sigma_min: float = 0.0
    sigma_max: float = 1.5
    kappa_order: int = 3
    kappa_min: float = 1.0
    kappa_max: float = 3.0
    alpha_order: int = 1
    alpha_min: float = 0.0
    alpha_max: float = 0.0

    @field_validator("sigma_order", "kappa_order", "alpha_order")
    @classmethod
    def _validate_order(cls, value: int) -> int:
        if value < 0:
            raise ValueError("PML profile orders must be non-negative")
        return int(value)

    @field_validator(
        "sigma_min",
        "sigma_max",
        "kappa_min",
        "kappa_max",
        "alpha_min",
        "alpha_max",
    )
    @classmethod
    def _validate_non_negative(cls, value: float) -> float:
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("PML profile coefficients must be non-negative")
        return normalized

    @model_validator(mode="after")
    def _validate_bounds(self) -> PMLParams:
        if self.sigma_max < self.sigma_min:
            raise ValueError("PML sigma_max must be greater than or equal to sigma_min")
        if self.kappa_min < 1.0:
            raise ValueError("PML kappa_min must be at least 1.0")
        if self.kappa_max < self.kappa_min:
            raise ValueError("PML kappa_max must be greater than or equal to kappa_min")
        if self.alpha_max < self.alpha_min:
            raise ValueError("PML alpha_max must be greater than or equal to alpha_min")
        return self


class AbsorberParams(TaggedModel):
    """Polynomial conductivity profile parameters for adiabatic absorbers."""

    type: Literal["AbsorberParams"] = "AbsorberParams"
    sigma_order: int = 3
    sigma_min: float = 0.0
    sigma_max: float = 6.4

    @field_validator("sigma_order")
    @classmethod
    def _validate_order(cls, value: int) -> int:
        if value < 0:
            raise ValueError("Absorber profile orders must be non-negative")
        return int(value)

    @field_validator("sigma_min", "sigma_max")
    @classmethod
    def _validate_non_negative(cls, value: float) -> float:
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("Absorber profile coefficients must be non-negative")
        return normalized

    @model_validator(mode="after")
    def _validate_bounds(self) -> AbsorberParams:
        if self.sigma_max < self.sigma_min:
            raise ValueError("Absorber sigma_max must be greater than or equal to sigma_min")
        return self


DEFAULT_PML_PARAMS = PMLParams()
DEFAULT_STABLE_PML_PARAMS = PMLParams(
    sigma_order=3,
    sigma_min=0.0,
    sigma_max=1.0,
    kappa_order=3,
    kappa_min=1.0,
    kappa_max=5.0,
    alpha_order=1,
    alpha_min=0.0,
    alpha_max=0.9,
)
DEFAULT_ABSORBER_PARAMS = AbsorberParams()


class PML(BoundaryEdge):
    """Complex-frequency-shift-inspired perfectly matched layer edge."""

    type: Literal["PML"] = "PML"
    num_layers: int = 12
    parameters: PMLParams = Field(default_factory=PMLParams)
    extrude_structures: bool = True

    @field_validator("num_layers")
    @classmethod
    def _validate_num_layers(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 1:
            raise ValueError("PML num_layers must be at least 1")
        if normalized < 6:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "PML boundaries with fewer than 6 layers are prone to reflection and should "
                "only be used for small smoke tests.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
        return normalized


class StablePML(BoundaryEdge):
    """More conservative complex-frequency-shift PML preset."""

    type: Literal["StablePML"] = "StablePML"
    num_layers: int = 40
    parameters: PMLParams = Field(default_factory=lambda: DEFAULT_STABLE_PML_PARAMS.copy_update())
    extrude_structures: bool = True

    @field_validator("num_layers")
    @classmethod
    def _validate_num_layers(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 1:
            raise ValueError("StablePML num_layers must be at least 1")
        if normalized < 6:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "StablePML boundaries with fewer than 6 layers are prone to reflection and "
                "should only be used for small smoke tests.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
        return normalized


class Absorber(BoundaryEdge):
    """Adiabatic absorbing layer backed by a reflective outer wall."""

    type: Literal["Absorber"] = "Absorber"
    num_layers: int = 40
    parameters: AbsorberParams = Field(default_factory=AbsorberParams)
    extrude_structures: bool = False

    @field_validator("num_layers")
    @classmethod
    def _validate_num_layers(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 1:
            raise ValueError("Absorber num_layers must be at least 1")
        if normalized < 6:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
                "should only be used for small smoke tests.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
        return normalized


ImplementedBoundaryEdge = (
    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
)


def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
    """Normalize a supported Phase 1 non-absorbing boundary edge."""

    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
    boundary_type = str(value.get("type"))
    if boundary_type == "Periodic":
        return Periodic(**value)
    if boundary_type == "BlochBoundary":
        return BlochBoundary(**value)
    if boundary_type == "PECBoundary":
        return PECBoundary(**value)
    if boundary_type == "PMCBoundary":
        return PMCBoundary(**value)
    if boundary_type == "PML":
        return PML(**value)

exec
/bin/bash -lc "sed -n '1,340p' src/autofdtd/compiler/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary compilation helpers for the foundational Phase 1 surface."""

from __future__ import annotations

import math
from enum import StrEnum
from typing import Literal

from autofdtd.boundaries import (
    Absorber,
    AbsorberParams,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    DEFAULT_PML_PARAMS,
    DEFAULT_ABSORBER_PARAMS,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    StablePML,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)
from autofdtd.core.models import AutoFDTDModel


class BoundaryMode(StrEnum):
    """Runtime boundary mode consumed by staged halo handling."""

    PERIODIC = "periodic"
    BLOCH = "bloch"
    PEC = "pec"
    PMC = "pmc"
    PML = "pml"
    STABLE_PML = "stable_pml"
    ABSORBER = "absorber"


class CompiledPMLCoefficients(AutoFDTDModel):
    """Prepared per-layer damping and memory coefficients for one PML face."""

    type: str = "CompiledPMLCoefficients"
    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
    num_layers: int
    sigma: tuple[float, ...]
    kappa: tuple[float, ...]
    alpha: tuple[float, ...]
    attenuation: tuple[float, ...]
    memory_decay: tuple[float, ...]
    memory_drive: tuple[float, ...]
    stretch: tuple[float, ...]
    uses_memory: bool = True
    terminal_reflection: Literal["none", "pec"] = "none"
    dt: float
    grid_spacing: float


class CompiledBoundaryEdge(AutoFDTDModel):
    """Runtime-ready metadata for one boundary face."""

    type: str = "CompiledBoundaryEdge"
    axis: Literal["x", "y", "z"]
    side: Literal["minus", "plus"]
    mode: BoundaryMode
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]
    source_side: Literal["minus", "plus"]
    phase_factor: complex = 1.0 + 0.0j
    pml: CompiledPMLCoefficients | None = None


class CompiledBoundaryAxis(AutoFDTDModel):
    """Runtime-ready metadata for one axis boundary pair."""

    type: str = "CompiledBoundaryAxis"
    axis: Literal["x", "y", "z"]
    minus: CompiledBoundaryEdge
    plus: CompiledBoundaryEdge
    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
    pml_faces: tuple[Literal["minus", "plus"], ...] = ()


class CompiledSymmetryAxis(AutoFDTDModel):
    """Reflection-sign metadata for one reduced symmetry axis."""

    type: str = "CompiledSymmetryAxis"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]


class CompiledBoundarySpec(AutoFDTDModel):
    """Compiled boundary metadata consumed by the Phase 1 timestep loop."""

    type: str = "CompiledBoundarySpec"
    x: CompiledBoundaryAxis
    y: CompiledBoundaryAxis
    z: CompiledBoundaryAxis
    periodic_axes: tuple[str, ...] = ()
    bloch_axes: tuple[str, ...] = ()
    reflective_axes: tuple[str, ...] = ()
    pml_axes: tuple[str, ...] = ()
    pml_faces: tuple[str, ...] = ()
    stable_pml_axes: tuple[str, ...] = ()
    stable_pml_faces: tuple[str, ...] = ()
    absorber_axes: tuple[str, ...] = ()
    absorber_faces: tuple[str, ...] = ()
    requires_complex_fields: bool = False
    symmetry: tuple[int, int, int] = (0, 0, 0)
    active_symmetry_axes: tuple[str, ...] = ()
    symmetry_axes: tuple[CompiledSymmetryAxis, ...] = ()
    halo_depth: int = 1
    stage_order: tuple[str, ...] = ("electric_boundary", "magnetic_boundary")
    pml_dt: float = 1.0
    pml_grid_spacing: tuple[float, float, float] = (1.0, 1.0, 1.0)

    def axes(self) -> tuple[CompiledBoundaryAxis, CompiledBoundaryAxis, CompiledBoundaryAxis]:
        return (self.x, self.y, self.z)


def _mode_for_edge(value: object) -> BoundaryMode:
    edge = boundary_edge_model_from_value(value)
    if isinstance(edge, Periodic):
        return BoundaryMode.PERIODIC
    if isinstance(edge, BlochBoundary):
        return BoundaryMode.BLOCH
    if isinstance(edge, PECBoundary):
        return BoundaryMode.PEC
    if isinstance(edge, PMCBoundary):
        return BoundaryMode.PMC
    if isinstance(edge, PML):
        return BoundaryMode.PML
    if isinstance(edge, StablePML):
        return BoundaryMode.STABLE_PML
    if isinstance(edge, Absorber):
        return BoundaryMode.ABSORBER
    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")


def _reflection_signs(
    mode: BoundaryMode, axis: int
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    normal = [1, 1, 1]
    tangential = [-1, -1, -1]
    normal[axis] = -1
    tangential[axis] = 1
    if mode is BoundaryMode.PEC:
        electric = tuple(tangential)
        magnetic = tuple(normal)
        return electric, magnetic
    if mode is BoundaryMode.PMC:
        electric = tuple(normal)
        magnetic = tuple(tangential)
        return electric, magnetic
    if mode is BoundaryMode.ABSORBER:
        electric = tuple(tangential)
        magnetic = tuple(normal)
        return electric, magnetic
    return (1, 1, 1), (1, 1, 1)


def _phase_factor_for_edge(edge: object, *, side: Literal["minus", "plus"]) -> complex:
    normalized = boundary_edge_model_from_value(edge)
    if not isinstance(normalized, BlochBoundary):
        return 1.0 + 0.0j
    phase = normalized.bloch_phase
    return phase.conjugate() if side == "minus" else phase


def _symmetry_signs(
    symmetry: int,
    axis: int,
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    if symmetry not in (-1, 1):
        raise ValueError("symmetry metadata only exists for active axes")
    spatial = [1, 1, 1]
    pseudo = [-1, -1, -1]
    spatial[axis] = -1
    pseudo[axis] = 1
    electric = tuple(sign * symmetry for sign in spatial)
    magnetic = tuple(sign * symmetry for sign in pseudo)
    return electric, magnetic


def _resolve_pml_params(edge: PML | StablePML) -> PMLParams:
    params = edge.parameters
    if isinstance(params, PMLParams):
        return params
    return PMLParams.model_validate(params if params is not None else DEFAULT_PML_PARAMS)


def _resolve_absorber_params(edge: Absorber) -> AbsorberParams:
    params = edge.parameters
    if isinstance(params, AbsorberParams):
        return params
    return AbsorberParams.model_validate(params if params is not None else DEFAULT_ABSORBER_PARAMS)


def _polynomial_profile(minimum: float, maximum: float, order: int, fraction: float) -> float:
    return minimum + (maximum - minimum) * (fraction**order)


def _compile_pml_coefficients(
    edge: PML | StablePML,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledPMLCoefficients:
    if dt <= 0.0:
        raise ValueError("PML coefficient preparation requires dt > 0")
    if grid_spacing <= 0.0:
        raise ValueError("PML coefficient preparation requires a positive grid spacing")
    params = _resolve_pml_params(edge)
    sigma: list[float] = []
    kappa: list[float] = []
    alpha: list[float] = []
    attenuation: list[float] = []
    memory_decay: list[float] = []
    memory_drive: list[float] = []
    stretch: list[float] = []
    layer_count = edge.num_layers
    for layer_index in range(layer_count):
        fraction = float(layer_index + 1) / float(layer_count)
        sigma_value = _polynomial_profile(
            params.sigma_min, params.sigma_max, params.sigma_order, fraction
        )
        kappa_value = _polynomial_profile(
            params.kappa_min, params.kappa_max, params.kappa_order, fraction
        )
        alpha_value = _polynomial_profile(
            params.alpha_min, params.alpha_max, params.alpha_order, fraction
        )
        damping_rate = max(0.0, sigma_value / kappa_value + alpha_value)
        attenuation_value = math.exp(-damping_rate * dt / grid_spacing)
        memory_decay_value = math.exp(-max(0.0, alpha_value) * dt)
        drive_value = (1.0 - attenuation_value) / max(kappa_value, 1.0)
        stretch_value = 1.0 / kappa_value
        sigma.append(sigma_value)
        kappa.append(kappa_value)
        alpha.append(alpha_value)
        attenuation.append(attenuation_value)
        memory_decay.append(memory_decay_value)
        memory_drive.append(drive_value)
        stretch.append(stretch_value)
    return CompiledPMLCoefficients(
        boundary_type=edge.type,
        num_layers=edge.num_layers,
        sigma=tuple(sigma),
        kappa=tuple(kappa),
        alpha=tuple(alpha),
        attenuation=tuple(attenuation),
        memory_decay=tuple(memory_decay),
        memory_drive=tuple(memory_drive),
        stretch=tuple(stretch),
        uses_memory=True,
        terminal_reflection="none",
        dt=dt,
        grid_spacing=grid_spacing,
    )


def _compile_absorber_coefficients(
    edge: Absorber,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledPMLCoefficients:
    if dt <= 0.0:
        raise ValueError("Absorber coefficient preparation requires dt > 0")
    if grid_spacing <= 0.0:
        raise ValueError("Absorber coefficient preparation requires a positive grid spacing")
    params = _resolve_absorber_params(edge)
    sigma: list[float] = []
    attenuation: list[float] = []
    zero = [0.0] * edge.num_layers
    ones = [1.0] * edge.num_layers
    for layer_index in range(edge.num_layers):
        fraction = float(layer_index + 1) / float(edge.num_layers)
        sigma_value = _polynomial_profile(
            params.sigma_min, params.sigma_max, params.sigma_order, fraction
        )
        damping_rate = max(0.0, sigma_value)
        sigma.append(sigma_value)
        attenuation.append(math.exp(-damping_rate * dt / grid_spacing))
    return CompiledPMLCoefficients(
        boundary_type=edge.type,
        num_layers=edge.num_layers,
        sigma=tuple(sigma),
        kappa=tuple(ones),
        alpha=tuple(zero),
        attenuation=tuple(attenuation),
        memory_decay=tuple(zero),
        memory_drive=tuple(zero),
        stretch=tuple(ones),
        uses_memory=False,
        terminal_reflection="pec",
        dt=dt,
        grid_spacing=grid_spacing,
    )


def _compile_edge(
    axis_name: Literal["x", "y", "z"],
    side: Literal["minus", "plus"],
    edge: object,
    *,
    dt: float,
    grid_spacing: float,
) -> CompiledBoundaryEdge:
    axis_index = "xyz".index(axis_name)
    mode = _mode_for_edge(edge)
    electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
    pml = None
    normalized = boundary_edge_model_from_value(edge)
    if isinstance(normalized, (PML, StablePML)):
        pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
    elif isinstance(normalized, Absorber):
        pml = _compile_absorber_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
    return CompiledBoundaryEdge(
        axis=axis_name,
        side=side,
        mode=mode,
        electric_signs=electric_signs,
        magnetic_signs=magnetic_signs,
        source_side="plus" if side == "minus" else "minus",
        phase_factor=_phase_factor_for_edge(edge, side=side),
        pml=pml,
    )


def _compile_axis(
    axis_name: Literal["x", "y", "z"],
    boundary: Boundary,
    *,
    dt: float,
    grid_spacing: float,

exec
/bin/bash -lc "sed -n '1,300p' src/autofdtd/core/validation.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Checklist-aware normalization and validation helpers for Phase 1 models."""

from __future__ import annotations

import math
import warnings
from collections.abc import Iterable, Mapping, Sequence
from typing import SupportsFloat, cast

from pydantic import BaseModel

from autofdtd.boundaries import (
    Absorber,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMCBoundary,
    Periodic,
    StablePML,
    boundary_spec_model_from_value,
)
from autofdtd.geometry import (
    ClipOperation,
    GeometryArray,
    GeometryGroup,
    PolySlab,
    Transformed,
    geometry_model_from_value,
)
from autofdtd.geometry.polyslab import _normalize_vertices
from autofdtd.geometry.primitives import Box, Cylinder, Sphere
from autofdtd.grid import (
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    SubpixelSpec,
    UniformGrid,
    grid_model_from_value,
    subpixel_model_from_value,
)
from autofdtd.materials import (
    AnisotropicMedium,
    Debye,
    Drude,
    Lorentz,
    LossyMetalMedium,
    Medium,
    PECMedium,
    PMCMedium,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    Sellmeier,
    medium_model_from_value,
)
from autofdtd.planning import FeatureStatus, feature_entry

Vec3 = tuple[float, float, float]
_IMPLEMENTED_BOUNDS_GEOMETRY = frozenset(
    {
        "Box",
        "Sphere",
        "Cylinder",
        "PolySlab",
        "GeometryGroup",
        "Transformed",
        "ClipOperation",
        "GeometryArray",
    }
)
_ABSORBING_BOUNDARY_TYPES = frozenset({"PML", "StablePML", "Absorber"})
_PARTIAL_GEOMETRY_TYPES = frozenset({"ClipOperation"})
_PHASE1_BOUNDARY_TYPES = frozenset(
    {
        "BoundarySpec",
        "Boundary",
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
        "PMLParams",
        "StablePML",
        "Absorber",
        "AbsorberParams",
    }
)
_PHASE1_GRID_TYPES = frozenset(
    {
        "GridSpec",
        "UniformGrid",
        "CustomGrid",
        "CustomGridBoundaries",
        "AutoGrid",
        "GridRefinement",
        "LayerRefinementSpec",
    }
)
_PHASE1_SUBPIXEL_TYPES = frozenset({"SubpixelSpec", "Staircasing", "PolarizedAveraging"})
_IMPLEMENTED_MEDIUM_TYPES = frozenset(
    {
        "Medium",
        "PECMedium",
        "PMCMedium",
        "LossyMetalMedium",
        "PoleResidue",
        "Sellmeier",
        "Lorentz",
        "Drude",
        "Debye",
        "AnisotropicMedium",
        "PerturbationMedium",
        "PerturbationPoleResidue",
    }
)
_PLANNED_GRID_TYPES = frozenset(
    {
        "QuasiUniformGrid",
    }
)


class AutoFDTDValidationWarning(UserWarning):
    """Warning category for non-fatal normalization and coercion behavior."""


class UnsupportedFeatureError(ValueError):
    """Raised when a feature is explicitly deferred or rejected in Phase 1."""


def normalize_name(name: str | None) -> str | None:
    """Normalize optional names by trimming whitespace and rejecting empties."""

    if name is None:
        return None
    stripped = name.strip()
    if not stripped:
        raise ValueError("names must not be empty or whitespace-only")
    return stripped


def normalize_vec3(value: Sequence[object], *, field_name: str) -> Vec3:
    """Coerce a 3-vector to a finite float tuple."""

    if len(value) != 3:
        raise ValueError(f"{field_name} must contain exactly three components")
    normalized = (
        float(cast(SupportsFloat, value[0])),
        float(cast(SupportsFloat, value[1])),
        float(cast(SupportsFloat, value[2])),
    )
    if any(not math.isfinite(component) for component in normalized):
        raise ValueError(f"{field_name} components must be finite")
    return normalized


def component_type_name(component: object) -> str | None:
    """Extract the tagged type field from a component-like payload."""

    if isinstance(component, BaseModel) and hasattr(component, "type"):
        value = component.type
        return str(value) if value is not None else None
    if isinstance(component, Mapping):
        value = component.get("type")
        return str(value) if value is not None else None
    return None


def normalize_component(component: object, *, context: str) -> object:
    """Normalize a tagged component payload and reject unsupported feature tags."""

    normalized = _normalize_name_field(component)
    _validate_supported_types(normalized, context=context)
    if context == "geometry":
        return _normalize_geometry(normalized)
    if context == "grid_spec":
        return _normalize_grid(normalized)
    if context == "subpixel":
        return _normalize_subpixel(normalized)
    if context == "boundary_spec":
        return _normalize_boundary_spec(normalized)
    if context in {"medium", "background_medium", "scene.medium"}:
        return _normalize_medium(normalized)
    return normalized


def validate_simulation_bounds(
    *,
    center: Vec3,
    size: Vec3,
    structures: Sequence[object],
) -> None:
    """Validate known structure bounds against the simulation domain."""

    sim_min = tuple(origin - extent / 2.0 for origin, extent in zip(center, size, strict=True))
    sim_max = tuple(origin + extent / 2.0 for origin, extent in zip(center, size, strict=True))

    for structure in structures:
        geometry = getattr(structure, "geometry", None)
        bounds = geometry_bounds(geometry)
        if bounds is None:
            continue
        geom_min, geom_max = bounds
        structure_name = getattr(structure, "name", None) or "<unnamed>"
        if any(
            geom_max[axis] < sim_min[axis] or geom_min[axis] > sim_max[axis] for axis in range(3)
        ):
            raise ValueError(
                f"structure {structure_name!r} lies fully outside the simulation bounds"
            )
        if any(low < allowed for low, allowed in zip(geom_min, sim_min, strict=True)) or any(
            high > allowed for high, allowed in zip(geom_max, sim_max, strict=True)
        ):
            warnings.warn(
                f"structure {structure_name!r} extends beyond the simulation bounds and will "
                "require clipping during scene compilation",
                AutoFDTDValidationWarning,
                stacklevel=3,
            )


def coerce_zero_dim_boundaries(boundary_spec: object, *, size: Vec3) -> object:
    """Coerce absorbing boundaries to periodic boundaries on zero-sized axes."""

    if isinstance(boundary_spec, BoundarySpec):
        updated = {}
        changed = False
        for axis_index, axis_name in enumerate("xyz"):
            axis_boundary = boundary_spec[axis_name]
            if size[axis_index] != 0.0:
                updated[axis_name] = axis_boundary
                continue
            coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
            updated[axis_name] = coerced
            changed = changed or coerced is not axis_boundary
        return boundary_spec.copy_update(**updated) if changed else boundary_spec

    if not isinstance(boundary_spec, Mapping):
        return boundary_spec

    updated = dict(boundary_spec)
    changed = False

    for axis_index, axis_name in enumerate("xyz"):
        if size[axis_index] != 0.0:
            continue
        axis_boundary = updated.get(axis_name)
        if axis_boundary is None:
            continue
        coerced = _coerce_axis_boundary(axis_boundary, axis_name=axis_name)
        if coerced is not axis_boundary:
            updated[axis_name] = coerced
            changed = True

    return updated if changed else boundary_spec


def geometry_bounds(geometry: object) -> tuple[Vec3, Vec3] | None:
    """Return axis-aligned bounds for the known primitive geometries."""

    if isinstance(
        geometry,
        (
            Box,
            Sphere,
            Cylinder,
            PolySlab,
            GeometryGroup,
            Transformed,
            ClipOperation,
            GeometryArray,
        ),
    ):
        return geometry.bounds

    if not isinstance(geometry, Mapping):
        return None
    geometry_type = component_type_name(geometry)
    if geometry_type not in _IMPLEMENTED_BOUNDS_GEOMETRY:
        return None
    required_fields = {
        "Box": {"size"},
        "Sphere": {"radius"},
        "Cylinder": {"radius", "length", "axis"},
        "PolySlab": {"vertices", "slab_bounds"},
        "GeometryGroup": {"geometries"},
        "Transformed": {"geometry", "transform"},
        "ClipOperation": {"operation", "geometry_a", "geometry_b"},
        "GeometryArray": {"geometry"},
    }[geometry_type]
    if not required_fields <= geometry.keys():
        return None
    return geometry_model_from_value(geometry).bounds



exec
/bin/bash -lc "sed -n '1,320p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
from __future__ import annotations

import numpy as np
import pytest

from autofdtd.api import (
    Absorber,
    AbsorberParams,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    Simulation,
    StablePML,
)
from autofdtd.compiler import BoundaryMode, compile_boundary_spec
from autofdtd.core.validation import AutoFDTDValidationWarning
from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
from autofdtd.kernels import (
    apply_boundary_ghosts,
    apply_boundary_stages,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime


def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
    with pytest.warns(
        AutoFDTDValidationWarning,
        match="periodic boundary opposite a PEC/PMC boundary",
    ):
        boundary = Boundary(minus=PECBoundary(), plus=Periodic())

    assert boundary.minus.type == "PECBoundary"
    assert boundary.plus.type == "PECBoundary"


def test_simulation_normalizes_supported_boundary_subset() -> None:
    simulation = Simulation(
        size=(1.0, 1.0, 1.0),
        run_time=1.0,
        boundary_spec={
            "type": "BoundarySpec",
            "x": {"type": "Periodic"},
            "y": {
                "type": "Boundary",
                "minus": {"type": "PECBoundary"},
                "plus": {"type": "Periodic"},
            },
            "z": {"type": "PMCBoundary"},
        },
    )

    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"


def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.bloch(0.25),
            y=Boundary.stable_pml(num_layers=8),
            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
        ),
        symmetry=(1, 0, -1),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.25),
    )

    assert compiled.x.exchange_kind == "bloch"
    assert compiled.x.minus.mode is BoundaryMode.BLOCH
    assert compiled.x.plus.phase_factor == pytest.approx(1j)
    assert compiled.x.minus.phase_factor == pytest.approx(-1j)
    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
    assert compiled.z.plus.mode is BoundaryMode.PML
    assert compiled.bloch_axes == ("x",)
    assert compiled.reflective_axes == ("y", "z")
    assert compiled.pml_axes == ("y", "z")
    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
    assert compiled.stable_pml_axes == ("y",)
    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
    assert compiled.absorber_axes == ("z",)
    assert compiled.absorber_faces == ("z.minus",)
    assert compiled.z.plus.pml is not None
    assert compiled.z.plus.pml.num_layers == 4
    assert compiled.z.minus.pml.boundary_type == "Absorber"
    assert compiled.z.minus.pml.terminal_reflection == "pec"
    assert compiled.y.plus.pml.boundary_type == "StablePML"
    assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
    assert compiled.stage_order == (
        "electric_boundary",
        "magnetic_boundary",
        "electric_pml",
        "magnetic_pml",
    )
    assert compiled.requires_complex_fields is True
    assert compiled.active_symmetry_axes == ("x", "z")
    assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
    assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)


def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    )
    electric = np.zeros((4, 4, 4, 3), dtype=np.complex128)
    magnetic = np.zeros((4, 4, 4, 3), dtype=np.complex128)

    electric[1, 1, 1] = (1.0, 2.0, 3.0)
    electric[-2, 1, 1] = (4.0, 5.0, 6.0)
    electric[2, 1, 2] = (7.0, 8.0, 9.0)
    electric[1, -2, 2] = (11.0, 12.0, 13.0)
    electric[2, 2, 1] = (14.0, 15.0, 16.0)
    electric[1, 2, -2] = (17.0, 18.0, 19.0)

    magnetic[1, 1, 1] = (21.0, 22.0, 23.0)
    magnetic[-2, 1, 1] = (24.0, 25.0, 26.0)
    magnetic[2, 1, 2] = (27.0, 28.0, 29.0)
    magnetic[2, -2, 2] = (31.0, 32.0, 33.0)
    magnetic[2, 2, 1] = (34.0, 35.0, 36.0)
    magnetic[2, 2, -2] = (37.0, 38.0, 39.0)

    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)

    assert tuple(updated_electric[0, 1, 1]) == pytest.approx((-4.0j, -5.0j, -6.0j))
    assert tuple(updated_electric[-1, 1, 1]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(updated_magnetic[0, 1, 1]) == pytest.approx((-24.0j, -25.0j, -26.0j))
    assert tuple(updated_electric[2, 0, 2]) == pytest.approx((-7.0, 8.0, -9.0))
    assert tuple(updated_magnetic[2, 0, 2]) == pytest.approx((27.0, -28.0, 29.0))
    assert tuple(updated_electric[2, 2, 0]) == pytest.approx((14.0, 15.0, -16.0))
    assert tuple(updated_magnetic[2, 2, -1]) == pytest.approx((-37.0, -38.0, 39.0))


def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
    boundary_spec = BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    runtime = build_boundary_runtime(boundary_spec, symmetry=(1, 0, -1))
    metadata = boundary_kernel_metadata()
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=boundary_spec,
            symmetry=(1, 0, -1),
        )
    )

    assert runtime.compiled.bloch_axes == ("x",)
    assert metadata["supported_modes"] == (
        "periodic",
        "bloch",
        "pec",
        "pmc",
        "pml",
        "stable_pml",
        "absorber",
    )
    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
    assert simulation_ir.boundary_spec.requires_complex_fields is True
    assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")


def test_pml_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.pml(
                    num_layers=8,
                    parameters=PMLParams(
                        sigma_order=2,
                        sigma_max=2.0,
                        kappa_max=4.0,
                        alpha_max=0.1,
                    ),
                )
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
    assert simulation_ir.boundary_spec.x.minus.num_layers == 8
    assert simulation_ir.boundary_spec.x.minus.parameters.kappa_max == pytest.approx(4.0)
    assert simulation_ir.boundary_spec.pml_axes == ("x",)
    assert simulation_ir.boundary_spec.pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.reflective_axes == ("x",)


def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.stable_pml(
                    num_layers=10,
                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
                ),
                z=Boundary.absorber(
                    num_layers=12,
                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
                ),
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)


def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
    runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
    vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)

    boundary_values = runtime.transform_boundary_values(
        vector,
        runtime.compiled.x.plus,
        field_family="electric",
    )
    mirrored = runtime.transform_symmetry_values(
        vector,
        runtime.compiled.symmetry_axes[0],
        field_family="magnetic",
    )

    assert tuple(boundary_values[0]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(mirrored[0]) == pytest.approx((1.0, -2.0, -3.0))
    assert tuple(
        boundary_edge_transform(vector, runtime.compiled.x.minus, field_family="electric")[0]
    ) == pytest.approx((-1.0j, -2.0j, -3.0j))
    assert tuple(
        symmetry_transform(
            vector,
            runtime.compiled.symmetry_axes[0],
            field_family="electric",
        )[0]
    ) == pytest.approx((-1.0, 2.0, 3.0))


def test_pml_state_allocation_tracks_active_faces() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.pml(num_layers=3),
            y=Boundary.absorber(num_layers=5),
            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
        )
    )
    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)

    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
    assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
    assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
    assert np.all(state.magnetic["x.plus"].memory == 0.0)


def test_pml_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 4, 4, 3), dtype=np.float64)
    electric[1:5, 1:3, 1:3, 0] = 1.0
    magnetic[-5:-1, 1:3, 1:3, 1] = 1.0

    before_left = float(np.sum(electric[1:5, ...] ** 2))
    before_center = float(np.sum(electric[5:7, ...] ** 2))
    for _ in range(12):
        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
    after_left = float(np.sum(electric[1:5, ...] ** 2))
    after_right_h = float(np.sum(magnetic[-5:-1, ...] ** 2))

    assert after_left < before_left * 0.2
    assert after_right_h < 0.2 * 16.0
    assert before_center == pytest.approx(0.0)
    assert float(np.sum(electric[5:7, ...] ** 2)) == pytest.approx(0.0)
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0


def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.stable_pml(num_layers=6),
            y=Boundary.absorber(
                num_layers=6,
                parameters=AbsorberParams(sigma_max=8.0),
            ),
            z=Boundary.periodic(),
        ),
        dt=0.5,
        grid_spacing=(0.25, 0.25, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
    electric[1:7, 1:7, 1:3, 0] = 1.0
    magnetic[1:7, 1:7, 1:3, 1] = 1.0

exec
/bin/bash -lc "sed -n '260,520p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
        return PML(**value)
    if boundary_type == "StablePML":
        return StablePML(**value)
    if boundary_type == "Absorber":
        return Absorber(**value)
    raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")


class Boundary(TaggedModel):
    """Boundary pair across one simulation axis."""

    type: Literal["Boundary"] = "Boundary"
    plus: object = Field(default_factory=Periodic)
    minus: object = Field(default_factory=Periodic)

    @field_validator("plus", "minus")
    @classmethod
    def _validate_edge(cls, value: object) -> object:
        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {
                "Periodic",
                "BlochBoundary",
                "PECBoundary",
                "PMCBoundary",
                "PML",
                "StablePML",
                "Absorber",
            }:
                return boundary_edge_model_from_value(value)
        return value

    @model_validator(mode="after")
    def _validate_edge_composition(self) -> Boundary:
        plus = self.plus
        minus = self.minus

        num_bloch = isinstance(plus, BlochBoundary) + isinstance(minus, BlochBoundary)
        if num_bloch == 1:
            raise ValueError("Bloch boundaries must be applied on both sides of the same axis")
        if isinstance(plus, BlochBoundary) and isinstance(minus, BlochBoundary):
            if plus.bloch_vec != minus.bloch_vec:
                raise ValueError(
                    "Bloch boundaries on opposite sides of one axis must use the same bloch_vec"
                )
            return self

        changed = False

        if isinstance(minus, (PECBoundary, PMCBoundary)) and isinstance(plus, Periodic):
            plus = minus
            changed = True
        elif isinstance(plus, (PECBoundary, PMCBoundary)) and isinstance(minus, Periodic):
            minus = plus
            changed = True

        if changed:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
                "boundary so runtime halo handling stays reflection-only on that axis.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
            object.__setattr__(self, "plus", plus)
            object.__setattr__(self, "minus", minus)

        return self

    def __getitem__(self, field_name: str) -> object:
        if field_name == "plus":
            return self.plus
        if field_name == "minus":
            return self.minus
        if field_name == "type":
            return self.type
        raise KeyError(field_name)

    @classmethod
    def periodic(cls) -> Boundary:
        return cls(plus=Periodic(), minus=Periodic())

    @classmethod
    def bloch(cls, bloch_vec: float) -> Boundary:
        return cls(
            plus=BlochBoundary(bloch_vec=bloch_vec),
            minus=BlochBoundary(bloch_vec=bloch_vec),
        )

    @classmethod
    def pec(cls) -> Boundary:
        return cls(plus=PECBoundary(), minus=PECBoundary())

    @classmethod
    def pmc(cls) -> Boundary:
        return cls(plus=PMCBoundary(), minus=PMCBoundary())

    @classmethod
    def pml(
        cls,
        *,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> Boundary:
        pml = PML(
            num_layers=num_layers,
            parameters=DEFAULT_PML_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=pml, minus=pml)

    @classmethod
    def stable_pml(
        cls,
        *,
        num_layers: int = 40,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> Boundary:
        stable = StablePML(
            num_layers=num_layers,
            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=stable, minus=stable)

    @classmethod
    def absorber(
        cls,
        *,
        num_layers: int = 40,
        parameters: AbsorberParams | None = None,
        extrude_structures: bool = False,
    ) -> Boundary:
        absorber = Absorber(
            num_layers=num_layers,
            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=absorber, minus=absorber)


def boundary_model_from_value(value: object) -> Boundary:
    """Normalize a supported axis boundary pair."""

    if isinstance(value, Boundary):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
    if "plus" not in value and "minus" not in value:
        edge = boundary_edge_model_from_value(value)
        return Boundary(plus=edge, minus=edge)
    return Boundary(**value)


class BoundarySpec(TaggedModel):
    """Boundary conditions across the x, y, and z simulation axes."""

    type: Literal["BoundarySpec"] = "BoundarySpec"
    x: Boundary = Field(default_factory=Boundary.periodic)
    y: Boundary = Field(default_factory=Boundary.periodic)
    z: Boundary = Field(default_factory=Boundary.periodic)

    @field_validator("x", "y", "z", mode="before")
    @classmethod
    def _coerce_axis_boundary(cls, value: object) -> object:
        if isinstance(
            value,
            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
        ):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {
                "Boundary",
                "Periodic",
                "BlochBoundary",
                "PECBoundary",
                "PMCBoundary",
                "PML",
                "StablePML",
                "Absorber",
            }:
                return boundary_model_from_value(value)
        return value

    def __getitem__(self, field_name: str) -> Boundary:
        if field_name == "x":
            return self.x
        if field_name == "y":
            return self.y
        if field_name == "z":
            return self.z
        raise KeyError(field_name)

    def as_tuple(self) -> tuple[Boundary, Boundary, Boundary]:
        return (self.x, self.y, self.z)

    @classmethod
    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
        return cls(
            x=Boundary(plus=boundary, minus=boundary),
            y=Boundary(plus=boundary, minus=boundary),
            z=Boundary(plus=boundary, minus=boundary),
        )

    @classmethod
    def pec(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pec() if x else Boundary.periodic(),
            y=Boundary.pec() if y else Boundary.periodic(),
            z=Boundary.pec() if z else Boundary.periodic(),
        )

    @classmethod
    def pmc(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pmc() if x else Boundary.periodic(),
            y=Boundary.pmc() if y else Boundary.periodic(),
            z=Boundary.pmc() if z else Boundary.periodic(),
        )

    @classmethod
    def periodic(
        cls, *, x: bool = True, y: bool = True, z: bool = True
    ) -> BoundarySpec:
        return cls(
            x=Boundary.periodic() if x else Boundary.periodic(),
            y=Boundary.periodic() if y else Boundary.periodic(),
            z=Boundary.periodic() if z else Boundary.periodic(),
        )

    @classmethod
    def pml(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,

exec
/bin/bash -lc "sed -n '340,520p' src/autofdtd/compiler/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
    grid_spacing: float,
) -> CompiledBoundaryAxis:
    minus = _compile_edge(axis_name, "minus", boundary.minus, dt=dt, grid_spacing=grid_spacing)
    plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
    edge_modes = {minus.mode, plus.mode}
    if edge_modes == {BoundaryMode.PERIODIC}:
        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
    elif edge_modes == {BoundaryMode.BLOCH}:
        exchange_kind = "bloch"
    elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
        exchange_kind = "pml"
    elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
        exchange_kind = "reflective"
    else:
        exchange_kind = "mixed"
    pml_faces = tuple(
        side
        for side, compiled in (("minus", minus), ("plus", plus))
        if compiled.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}
    )
    return CompiledBoundaryAxis(
        axis=axis_name,
        minus=minus,
        plus=plus,
        exchange_kind=exchange_kind,
        pml_faces=pml_faces,
    )


def _compile_symmetry_axes(
    symmetry: tuple[int, int, int],
) -> tuple[CompiledSymmetryAxis, ...]:
    compiled: list[CompiledSymmetryAxis] = []
    for axis_name, axis_value in zip("xyz", symmetry, strict=True):
        if axis_value == 0:
            continue
        electric_signs, magnetic_signs = _symmetry_signs(axis_value, "xyz".index(axis_name))
        compiled.append(
            CompiledSymmetryAxis(
                axis=axis_name,
                symmetry=axis_value,
                electric_signs=electric_signs,
                magnetic_signs=magnetic_signs,
            )
        )
    return tuple(compiled)


def compile_boundary_spec(
    value: object | None,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
    dt: float = 1.0,
    grid_spacing: float | tuple[float, float, float] = 1.0,
) -> CompiledBoundarySpec:
    """Compile the supported Phase 1 boundary subset into runtime metadata."""

    if value is None:
        boundary_spec = BoundarySpec()
    else:
        try:
            boundary_spec = boundary_spec_model_from_value(value)
        except Exception as exc:  # noqa: BLE001
            raise ValueError(
                "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
                "Absorber entries"
            ) from exc

    if isinstance(grid_spacing, tuple):
        if len(grid_spacing) != 3 or any(step <= 0.0 for step in grid_spacing):
            raise ValueError("grid_spacing must contain three positive axis spacings")
        axis_spacing = grid_spacing
    else:
        if grid_spacing <= 0.0:
            raise ValueError("grid_spacing must be positive")
        axis_spacing = (float(grid_spacing), float(grid_spacing), float(grid_spacing))

    compiled_axes = {
        axis_name: _compile_axis(
            axis_name,
            axis_boundary,
            dt=dt,
            grid_spacing=axis_spacing["xyz".index(axis_name)],
        )
        for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True)
    }
    periodic_axes = tuple(
        axis.axis for axis in compiled_axes.values() if axis.exchange_kind == "periodic"
    )
    bloch_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.exchange_kind == "bloch")
    reflective_axes = tuple(
        axis.axis
        for axis in compiled_axes.values()
        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
    )
    pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
    pml_faces = tuple(
        f"{axis.axis}.{side}"
        for axis in compiled_axes.values()
        for side in axis.pml_faces
    )
    stable_pml_axes = tuple(
        axis.axis
        for axis in compiled_axes.values()
        if any(
            getattr(axis, side).mode is BoundaryMode.STABLE_PML for side in ("minus", "plus")
        )
    )
    stable_pml_faces = tuple(
        f"{axis.axis}.{side}"
        for axis in compiled_axes.values()
        for side in ("minus", "plus")
        if getattr(axis, side).mode is BoundaryMode.STABLE_PML
    )
    absorber_axes = tuple(
        axis.axis
        for axis in compiled_axes.values()
        if any(getattr(axis, side).mode is BoundaryMode.ABSORBER for side in ("minus", "plus"))
    )
    absorber_faces = tuple(
        f"{axis.axis}.{side}"
        for axis in compiled_axes.values()
        for side in ("minus", "plus")
        if getattr(axis, side).mode is BoundaryMode.ABSORBER
    )
    symmetry_axes = _compile_symmetry_axes(symmetry)
    stage_order = ["electric_boundary", "magnetic_boundary"]
    if pml_faces:
        stage_order.extend(("electric_pml", "magnetic_pml"))
    return CompiledBoundarySpec(
        x=compiled_axes["x"],
        y=compiled_axes["y"],
        z=compiled_axes["z"],
        periodic_axes=periodic_axes,
        bloch_axes=bloch_axes,
        reflective_axes=reflective_axes,
        pml_axes=pml_axes,
        pml_faces=pml_faces,
        stable_pml_axes=stable_pml_axes,
        stable_pml_faces=stable_pml_faces,
        absorber_axes=absorber_axes,
        absorber_faces=absorber_faces,
        requires_complex_fields=bool(bloch_axes),
        symmetry=symmetry,
        active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
        symmetry_axes=symmetry_axes,
        stage_order=tuple(stage_order),
        pml_dt=dt,
        pml_grid_spacing=axis_spacing,
    )


def normalize_axis_boundary(value: object) -> Boundary:
    """Normalize a single axis boundary using the supported subset."""

    return boundary_model_from_value(value)

exec
/bin/bash -lc "sed -n '1,220p' docs/boundaries.md" in /workspace/autofdtd
 succeeded in 0ms:
# Boundary Scope

Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.

## Public API

- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
  and `Absorber` are typed boundary-edge models.
- `Boundary` pairs `minus` and `plus` edges along one axis.
- `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
  behavior on all sides in the currently implemented subset.
- If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
  coerced to the conductor edge with a warning so later runtime handling stays
  reflection-based instead of mixing halo semantics on one axis.
- `StablePML` keeps the same parameter surface as `PML` but uses a more conservative
  default preset.
- `Absorber` uses a simpler conductivity-only profile and defaults
  `extrude_structures=False`, matching the local Tidy3D schema surface.

## Lowering And Runtime

- `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
- `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
  surface into a runtime plan with explicit per-face mode tags, sign transforms,
  halo depth, stage ordering, and per-face absorbing profile metadata.
- `autofdtd.kernels.boundaries.apply_boundary_ghosts()` applies the compiled plan to
  vector fields with one ghost cell on each side.
- Periodic boundaries copy ghost cells from the opposite interior face.
- Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
- PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
  field parity is explicit instead of being hidden in a monolithic timestep kernel.
- `PML` and `StablePML` share the staged damping-layer backend and differ through
  compiled coefficient presets.
- `Absorber` uses the same staged region-update seam but with conductivity-only
  attenuation coefficients and a reflective outer wall.

## Current Limits

- Runtime compilation currently supports `Periodic`, `BlochBoundary`,
  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
  `BoundarySpec`.
- `StablePML` is currently implemented as a preset-driven extension of the baseline
  staged PML backend rather than a separate split-field formulation.
- `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
  reflective outer wall, not a full material-aware adiabatic absorber.
- ABC-family boundaries remain outside the runtime compiler path and should fail
  explicitly until `task-023`.
- The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
  cell per face.

exec
/bin/bash -lc 'rg -n "ABCBoundary|ModeABCBoundary|unsupported boundary|defer" src/autofdtd/ir/models.py src/autofdtd/api src/autofdtd/boundaries src/autofdtd/compiler tests' in /workspace/autofdtd
 succeeded in 0ms:
tests/test_materials.py:121:    assert "deferred" in tensor.phase1_policy
tests/test_materials.py:159:    assert "deferred" in lossy.phase1_policy
tests/test_materials.py:514:    with pytest.raises(ValueError, match="runtime compilation is deferred"):
tests/test_materials.py:585:def test_deferred_and_rejected_advanced_media_are_mapped_by_validation_instead_of_unknown() -> None:
tests/test_materials.py:586:    with pytest.raises(Exception, match="deferred feature 'LossyMetalMedium'"):
tests/test_materials.py:593:    with pytest.raises(Exception, match="deferred feature 'PerturbationMedium'"):
tests/test_grid.py:141:def test_subpixel_spec_rejects_deferred_or_invalid_policy_selection() -> None:
tests/test_containers.py:470:def test_deferred_or_rejected_features_raise_clear_errors() -> None:
tests/test_containers.py:471:    with pytest.raises(ValueError, match="deferred feature 'TriangleMesh'"):
tests/test_containers.py:541:    with pytest.raises(ValueError, match="deferred feature 'QuasiUniformGrid'"):
src/autofdtd/compiler/materials.py:383:            "LossyMetalMedium parsing is available, but runtime compilation is deferred in Phase 1; "
src/autofdtd/compiler/materials.py:393:            "FullyAnisotropicMedium parsing is available, but runtime compilation is deferred in Phase 1"
src/autofdtd/compiler/materials.py:397:            "PerturbationMedium parsing is available, but runtime perturbation support is deferred "
src/autofdtd/compiler/materials.py:403:            "deferred in Phase 1; compile normalized.base_medium() explicitly if you want the unperturbed medium"
src/autofdtd/compiler/boundaries.py:141:    raise TypeError(f"unsupported boundary edge type {type(edge)!r}")

exec
/bin/bash -lc 'rg -n "class ABCBoundary|class ModeABCBoundary|ABCBoundary|ModeABCBoundary" ../tidy3d/tidy3d/components/boundary.py ../tidy3d/schemas/Simulation.json' in /workspace/autofdtd
 succeeded in 0ms:
../tidy3d/tidy3d/components/boundary.py:118:class AbstractABCBoundary(BoundaryEdge, ABC):
../tidy3d/tidy3d/components/boundary.py:122:class ABCBoundary(AbstractABCBoundary):
../tidy3d/tidy3d/components/boundary.py:150:                "Field 'conductivity' in 'ABCBoundary' can only be provided "
../tidy3d/tidy3d/components/boundary.py:268:class ModeABCBoundary(AbstractABCBoundary):
../tidy3d/tidy3d/components/boundary.py:305:                f"'ModeABCBoundary' target plane must be planar, given size={val.size}"
../tidy3d/tidy3d/components/boundary.py:326:        :class:`.ModeABCBoundary`
../tidy3d/tidy3d/components/boundary.py:334:        >>> abc_boundary = ModeABCBoundary.from_source(source=source)
../tidy3d/tidy3d/components/boundary.py:367:        :class:`.ModeABCBoundary`
../tidy3d/tidy3d/components/boundary.py:374:        >>> abc_boundary = ModeABCBoundary.from_monitor(monitor=mnt, mode_index=0)
../tidy3d/tidy3d/components/boundary.py:404:    boundary_spec: Union[ModeABCBoundary, ABCBoundary] = Field(
../tidy3d/tidy3d/components/boundary.py:416:        cls, val: Union[ModeABCBoundary, ABCBoundary]
../tidy3d/tidy3d/components/boundary.py:417:    ) -> Union[ModeABCBoundary, ABCBoundary]:
../tidy3d/tidy3d/components/boundary.py:418:        """Validate that permittivity is provided for ABCBoundary."""
../tidy3d/tidy3d/components/boundary.py:419:        if isinstance(val, ABCBoundary) and val.permittivity is None:
../tidy3d/tidy3d/components/boundary.py:421:                "Must provide 'permittivity' in 'ABCBoundary' when used in an internal absorber."
../tidy3d/tidy3d/components/boundary.py:992:    ABCBoundary,
../tidy3d/tidy3d/components/boundary.py:993:    ModeABCBoundary,
../tidy3d/tidy3d/components/boundary.py:1052:            self.plus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary)
../tidy3d/tidy3d/components/boundary.py:1053:        ) + isinstance(self.minus, (PML, StablePML, Absorber, ABCBoundary, ModeABCBoundary))
../tidy3d/tidy3d/components/boundary.py:1189:        plus = ABCBoundary(
../tidy3d/tidy3d/components/boundary.py:1193:        minus = ABCBoundary(
../tidy3d/tidy3d/components/boundary.py:1226:        plus = ModeABCBoundary(
../tidy3d/tidy3d/components/boundary.py:1232:        minus = ModeABCBoundary(
../tidy3d/tidy3d/components/boundary.py:1263:        plus = ModeABCBoundary.from_source(source=source, freq_spec=freq_spec)
../tidy3d/tidy3d/components/boundary.py:1264:        minus = ModeABCBoundary.from_source(source=source, freq_spec=freq_spec)
../tidy3d/tidy3d/components/boundary.py:1282:        plus = ModeABCBoundary.from_monitor(
../tidy3d/tidy3d/components/boundary.py:1287:        minus = ModeABCBoundary.from_monitor(
../tidy3d/schemas/Simulation.json:3:    "ABCBoundary": {
../tidy3d/schemas/Simulation.json:47:          "const": "ABCBoundary",
../tidy3d/schemas/Simulation.json:48:          "default": "ABCBoundary",
../tidy3d/schemas/Simulation.json:1617:              "$ref": "#/$defs/ABCBoundary"
../tidy3d/schemas/Simulation.json:1626:              "$ref": "#/$defs/ModeABCBoundary"
../tidy3d/schemas/Simulation.json:1668:              "$ref": "#/$defs/ABCBoundary"
../tidy3d/schemas/Simulation.json:1677:              "$ref": "#/$defs/ModeABCBoundary"
../tidy3d/schemas/Simulation.json:9183:              "ABCBoundary": "#/$defs/ABCBoundary",
../tidy3d/schemas/Simulation.json:9184:              "ModeABCBoundary": "#/$defs/ModeABCBoundary"
../tidy3d/schemas/Simulation.json:9190:              "$ref": "#/$defs/ABCBoundary"
../tidy3d/schemas/Simulation.json:9193:              "$ref": "#/$defs/ModeABCBoundary"
../tidy3d/schemas/Simulation.json:11889:    "ModeABCBoundary": {
../tidy3d/schemas/Simulation.json:11986:          "const": "ModeABCBoundary",
../tidy3d/schemas/Simulation.json:11987:          "default": "ModeABCBoundary",

exec
/bin/bash -lc 'rg -n "BoundaryMode|BoundarySpecIR|PeriodicIR|BlochBoundaryIR|PMLIR|StablePMLIR|AbsorberIR" src/autofdtd/ir/models.py' in /workspace/autofdtd
 succeeded in 0ms:
375:class PeriodicIR(IRModel):
378:    type: Literal["PeriodicIR"] = "PeriodicIR"
384:class BlochBoundaryIR(IRModel):
387:    type: Literal["BlochBoundaryIR"] = "BlochBoundaryIR"
429:class PMLIR(IRModel):
432:    type: Literal["PMLIR"] = "PMLIR"
441:class StablePMLIR(IRModel):
444:    type: Literal["StablePMLIR"] = "StablePMLIR"
462:class AbsorberIR(IRModel):
465:    type: Literal["AbsorberIR"] = "AbsorberIR"
485:    PeriodicIR
486:    | BlochBoundaryIR
489:    | PMLIR
490:    | StablePMLIR
491:    | AbsorberIR
506:class BoundarySpecIR(IRModel):
509:    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
707:    boundary_spec: BoundarySpecIR | ComponentIR | None = None
1023:        return PeriodicIR(name=edge.name)
1025:        return BlochBoundaryIR(
1041:        return PMLIR(
1063:        return StablePMLIR(
1085:        return AbsorberIR(
1158:) -> BoundarySpecIR | ComponentIR:
1225:    return BoundarySpecIR(

exec
/bin/bash -lc 'rg -n "ABCBoundary|ModeABCBoundary|Boundary" phase1/feature-checklist.md src/autofdtd/planning.py' in /workspace/autofdtd
 succeeded in 0ms:
src/autofdtd/planning.py:202:    FeatureEntry("boundaries", "Boundary", FeatureStatus.IMPLEMENT, "Per-axis boundary pair."),
src/autofdtd/planning.py:203:    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
src/autofdtd/planning.py:205:    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
src/autofdtd/planning.py:206:    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
src/autofdtd/planning.py:209:        "BlochBoundary",
src/autofdtd/planning.py:221:    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
src/autofdtd/planning.py:222:    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
src/autofdtd/planning.py:338:        "Boundary families",
phase1/feature-checklist.md:55:| `PECConformal` / `SurfaceImpedance` | Defer | Boundary/material refinement bucket |
phase1/feature-checklist.md:62:| `PECMedium` / `PMCMedium` | Implement | Boundary-aligned materials |
phase1/feature-checklist.md:79:| `Boundary` / `BoundarySpec` | Implement | Core surface |
phase1/feature-checklist.md:81:| `PECBoundary` / `PMCBoundary` | Implement | Core |
phase1/feature-checklist.md:82:| `BlochBoundary` | Implement | Important for periodic phase behavior |
phase1/feature-checklist.md:86:| `ABCBoundary` | Defer | Keep mapped |
phase1/feature-checklist.md:87:| `ModeABCBoundary` | Defer | Keep mapped |

exec
/bin/bash -lc "sed -n '100,360p' ../tidy3d/tidy3d/components/boundary.py" in /workspace/autofdtd
 succeeded in 0ms:
    def bloch_vec(self) -> int:
        """Periodic boundaries are effectively Bloch boundaries with ``bloch_vec == 0``.
        In practice, periodic boundaries do not force the use of complex fields, while Bloch
        boundaries do, even with ``bloch_vec == 0``. Thus, it is more efficient to use periodic.
        """
        return 0


# PEC keyword
class PECBoundary(BoundaryEdge):
    """Perfect electric conductor boundary condition class."""


# PMC keyword
class PMCBoundary(BoundaryEdge):
    """Perfect magnetic conductor boundary condition class."""


class AbstractABCBoundary(BoundaryEdge, ABC):
    """One-way wave equation absorbing boundary conditions abstract base class."""


class ABCBoundary(AbstractABCBoundary):
    """One-way wave equation absorbing boundary conditions.
    See, for example, John B. Schneider, Understanding the Finite-Difference Time-Domain Method, Chapter 6.
    """

    permittivity: Optional[float] = Field(
        None,
        title="Effective Permittivity",
        description="Effective permittivity for determining propagation constant. "
        "If ``None``, this value will be automatically inferred from the medium at "
        "the domain boundary and the central frequency of the source.",
        ge=1.0,
    )

    conductivity: Optional[NonNegativeFloat] = Field(
        None,
        title="Effective Conductivity",
        description="Effective conductivity for determining propagation constant. "
        "If ``None``, this value will be automatically inferred from the medium at "
        "the domain boundary and the central frequency of the source.",
        json_schema_extra={"units": CONDUCTIVITY},
    )

    @model_validator(mode="after")
    def _conductivity_only_with_float_permittivity(self) -> Self:
        """Validate that conductivity can be provided only with float permittivity."""
        if self.conductivity is not None and self.permittivity is None:
            raise ValidationError(
                "Field 'conductivity' in 'ABCBoundary' can only be provided "
                "simultaneously with 'permittivity'."
            )
        return self


class BroadbandModeABCFitterParam(Tidy3dBaseModel):
    """Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.

    Notes
    -----
    The number of poles and frequency sampling points are constrained to be within the range [1, 10] and [1, 101] respectively.

    Example
    -------
    >>> fitter_param = BroadbandModeABCFitterParam(max_num_poles=5, tolerance_rms=1e-4, frequency_sampling_points=10)
    """

    max_num_poles: int = Field(
        DEFAULT_BROADBAND_MODE_ABC_NUM_POLES,
        title="Maximal Number Of Poles",
        description="Maximal number of poles in complex-conjugate pole residue model for "
        "fitting the mode propagation index.",
        gt=0,
        le=MAX_BROADBAND_MODE_ABC_NUM_POLES,
    )

    tolerance_rms: NonNegativeFloat = Field(
        DEFAULT_BROADBAND_MODE_ABC_FITTER_TOLERANCE,
        title="Fitting Tolerance",
        description="Tolerance in fitting the mode propagation index.",
    )

    frequency_sampling_points: int = Field(
        DEFAULT_BROADBAND_MODE_ABC_NUM_FREQS,
        title="Number Of Frequencies",
        description="Number of sampling frequencies used in fitting the mode propagation index.",
        gt=0,
        le=MAX_BROADBAND_MODE_ABC_NUM_FREQS,
    )


DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS = BroadbandModeABCFitterParam()


class BroadbandModeABCSpec(Tidy3dBaseModel):
    """Specifies the broadband mode absorption boundary conditions. The mode propagation index is approximated by a sum of pole-residue pairs.

    Example
    -------
    >>> broadband_mode_abc_spec = BroadbandModeABCSpec(frequency_range=(100e12, 120e12), fit_param=BroadbandModeABCFitterParam())
    """

    frequency_range: FreqBound = Field(
        title="Frequency Range",
        description="Frequency range for the broadband mode absorption boundary conditions.",
        json_schema_extra={"units": (HERTZ, HERTZ)},
    )

    fit_param: BroadbandModeABCFitterParam = Field(
        DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS,
        title="Fitting Parameters For Broadband Mode Absorption Boundary Conditions",
        description="Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.",
    )

    @field_validator("frequency_range", mode="after")
    @classmethod
    def validate_frequency_range(cls, val: FreqBound) -> FreqBound:
        """Validate that max frequency is greater than min frequency."""
        _assert_min_freq(val[0], "min frequency")
        if val[1] <= val[0]:
            raise ValidationError("max frequency must be greater than min frequency.")
        return val

    @classmethod
    def from_wavelength_range(
        cls,
        wavelength_range: FreqBound,
        fit_param: BroadbandModeABCFitterParam = DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS,
    ) -> BroadbandModeABCSpec:
        """Instantiate from a wavelength range.

        Parameters
        ----------
        wavelength_range : FreqBound
            Wavelength range for the broadband mode absorption boundary conditions.
        fit_param : BroadbandModeABCFitterParam = DEFAULT_BROADBAND_MODE_ABC_FITTER_PARAMS
            Parameters for fitting the mode propagation index over the frequency range using pole-residue pair model.

        Returns
        -------
        :class:`BroadbandModeABCSpec`
            Broadband mode absorption boundary conditions.
        """
        # check that min wavelength > 0
        if wavelength_range[0] <= 0:
            raise SetupError("min wavelength must be greater than 0.")
        # check that max wavelength > min wavelength
        if wavelength_range[1] <= wavelength_range[0]:
            raise SetupError("max wavelength must be greater than min wavelength.")

        return cls(
            frequency_range=(C_0 / wavelength_range[1], C_0 / wavelength_range[0]),
            fit_param=fit_param,
        )

    @property
    def _frequency_grid(self) -> NDArray:
        """Frequency grid for the broadband mode absorption boundary conditions.
        Propagation constant is sampled at these frequencies and fitted using pole-residue pair model.
        """
        return np.linspace(
            self.frequency_range[0],
            self.frequency_range[1],
            self.fit_param.frequency_sampling_points,
        )


class ModeABCBoundary(AbstractABCBoundary):
    """One-way wave equation absorbing boundary conditions for absorbing a waveguide mode."""

    mode_spec: ModeSpecType = Field(
        DEFAULT_MODE_SPEC_MODE_ABC,
        title="Mode Specification",
        description="Parameters that determine the modes computed by the mode solver.",
        discriminator=TYPE_TAG_STR,
    )

    mode_index: NonNegativeInt = Field(
        0,
        title="Mode Index",
        description="Index into the collection of modes returned by mode solver. "
        "The absorbing boundary conditions are configured to absorb the specified mode. "
        "If larger than ``mode_spec.num_modes``, "
        "``num_modes`` in the solver will be set to ``mode_index + 1``.",
    )

    freq_spec: Optional[Union[PositiveFloat, BroadbandModeABCSpec]] = Field(
        None,
        title="Absorption Frequency Specification",
        description="Specifies the frequency at which field is absorbed. If ``None``, then the central frequency of the source is used. If ``BroadbandModeABCSpec``, then the field is absorbed over the specified frequency range.",
    )

    plane: Box = Field(
        ...,
        title="Plane",
        description="Cross-sectional plane in which the absorbed mode will be computed.",
    )

    @field_validator("plane")
    @classmethod
    def is_plane(cls, val: Box) -> Box:
        """Raise validation error if not planar."""
        if val.size.count(0.0) != 1:
            raise ValidationError(
                f"'ModeABCBoundary' target plane must be planar, given size={val.size}"
            )
        return val

    @classmethod
    def from_source(
        cls,
        source: ModeSource,
        freq_spec: Optional[Union[PositiveFloat, BroadbandModeABCSpec]] = None,
    ) -> Self:
        """Instantiate from a ``ModeSource``.

        Parameters
        ----------
        source : :class:`~tidy3d.ModeSource`
            Mode source.
        freq_spec : Optional[Union[PositiveFloat, BroadbandModeABCSpec]] = None
            Specifies the frequency at which field is absorbed. If ``None``, then the central frequency of the source is used. If ``BroadbandModeABCSpec``, then the field is absorbed over the specified frequency range.

        Returns
        -------
        :class:`.ModeABCBoundary`
            Boundary conditions for absorbing the desired mode.

        Example
        -------
        >>> from tidy3d import GaussianPulse, ModeSource, inf
        >>> pulse = GaussianPulse(freq0=200e12, fwidth=20e12)
        >>> source = ModeSource(size=(1, 1, 0), source_time=pulse, direction='+')
        >>> abc_boundary = ModeABCBoundary.from_source(source=source)
        """

        if freq_spec is None:
            freq_spec = source.source_time._freq0

        return cls(
            plane=source.bounding_box,
            mode_spec=source.mode_spec,
            mode_index=source.mode_index,
            freq_spec=freq_spec,
        )

    @classmethod
    def from_monitor(
        cls,
        monitor: Union[ModeMonitor, ModeSolverMonitor],
        mode_index: NonNegativeInt = 0,
        freq_spec: Optional[Union[PositiveFloat, BroadbandModeABCSpec]] = None,
    ) -> Self:
        """Instantiate from a ``ModeMonitor`` or ``ModeSolverMonitor``.

        Parameters
        ----------
        monitor : Union[:class:`ModeMonitor`, :class:`ModeSolverMonitor`]
            Mode monitor.
        mode_index : NonNegativeInt = 0

exec
/bin/bash -lc "sed -n '11880,12080p' ../tidy3d/schemas/Simulation.json" in /workspace/autofdtd
 succeeded in 0ms:
      "required": [
        "direction",
        "mode_spec",
        "size",
        "source_time",
        "terminal_label"
      ],
      "type": "object"
    },
    "ModeABCBoundary": {
      "additionalProperties": false,
      "properties": {
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "freq_spec": {
          "anyOf": [
            {
              "$ref": "#/$defs/BroadbandModeABCSpec"
            },
            {
              "exclusiveMinimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "mode_index": {
          "default": 0,
          "minimum": 0,
          "type": "integer"
        },
        "mode_spec": {
          "default": {
            "angle_phi": 0.0,
            "angle_rotation": false,
            "angle_theta": 0.0,
            "attrs": {},
            "bend_axis": null,
            "bend_radius": null,
            "filter_pol": null,
            "group_index_step": false,
            "interp_spec": null,
            "num_modes": 1,
            "num_pml": [
              0,
              0
            ],
            "precision": "double",
            "sort_spec": {
              "attrs": {},
              "bounding_box": null,
              "filter_key": null,
              "filter_order": "over",
              "filter_reference": 0.0,
              "keep_modes": "all",
              "sort_key": "n_eff",
              "sort_order": "descending",
              "sort_reference": null,
              "track_freq": "central",
              "type": "ModeSortSpec"
            },
            "target_neff": null,
            "track_freq": null,
            "type": "ModeSpec"
          },
          "discriminator": {
            "mapping": {
              "MicrowaveModeSpec": "#/$defs/MicrowaveModeSpec",
              "MicrowaveTerminalModeSpec": "#/$defs/MicrowaveTerminalModeSpec",
              "ModeSpec": "#/$defs/ModeSpec"
            },
            "propertyName": "type"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/MicrowaveModeSpec"
            },
            {
              "$ref": "#/$defs/MicrowaveTerminalModeSpec"
            },
            {
              "$ref": "#/$defs/ModeSpec"
            }
          ]
        },
        "name": {
          "anyOf": [
            {
              "type": "null"
            },
            {
              "type": "string"
            }
          ],
          "default": null
        },
        "plane": {
          "$ref": "#/$defs/Box"
        },
        "type": {
          "const": "ModeABCBoundary",
          "default": "ModeABCBoundary",
          "type": "string"
        }
      },
      "required": [
        "plane"
      ],
      "type": "object"
    },
    "ModeInterpSpec": {
      "additionalProperties": false,
      "properties": {
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "method": {
          "default": "linear",
          "enum": [
            "cubic",
            "linear",
            "poly"
          ],
          "type": "string"
        },
        "reduce_data": {
          "default": false,
          "type": "boolean"
        },
        "sampling_spec": {
          "discriminator": {
            "mapping": {
              "ChebSampling": "#/$defs/ChebSampling",
              "CustomSampling": "#/$defs/CustomSampling",
              "UniformSampling": "#/$defs/UniformSampling"
            },
            "propertyName": "type"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/ChebSampling"
            },
            {
              "$ref": "#/$defs/CustomSampling"
            },
            {
              "$ref": "#/$defs/UniformSampling"
            }
          ]
        },
        "type": {
          "const": "ModeInterpSpec",
          "default": "ModeInterpSpec",
          "type": "string"
        }
      },
      "required": [
        "sampling_spec"
      ],
      "type": "object"
    },
    "ModeMonitor": {
      "additionalProperties": false,
      "properties": {
        "apodization": {
          "$ref": "#/$defs/ApodizationSpec",
          "default": {
            "attrs": {},
            "end": null,
            "start": null,
            "type": "ApodizationSpec",
            "width": null
          }
        },
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "center": {
          "default": [
            0.0,
            0.0,
            0.0
          ],
          "maxItems": 3,
          "minItems": 3,
          "prefixItems": [
            {},
            {},
            {}
          ],
          "type": "array"

exec
/bin/bash -lc "sed -n '1,120p' ../tidy3d/schemas/Simulation.json" in /workspace/autofdtd
 succeeded in 0ms:
{
  "$defs": {
    "ABCBoundary": {
      "additionalProperties": false,
      "properties": {
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "conductivity": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "name": {
          "anyOf": [
            {
              "type": "null"
            },
            {
              "type": "string"
            }
          ],
          "default": null
        },
        "permittivity": {
          "anyOf": [
            {
              "minimum": 1.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "type": {
          "const": "ABCBoundary",
          "default": "ABCBoundary",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Absorber": {
      "additionalProperties": false,
      "properties": {
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "extrude_structures": {
          "default": false,
          "type": "boolean"
        },
        "name": {
          "anyOf": [
            {
              "type": "null"
            },
            {
              "type": "string"
            }
          ],
          "default": null
        },
        "num_layers": {
          "default": 40,
          "minimum": 1,
          "type": "integer"
        },
        "parameters": {
          "$ref": "#/$defs/AbsorberParams",
          "default": {
            "attrs": {},
            "sigma_max": 6.4,
            "sigma_min": 0.0,
            "sigma_order": 3,
            "type": "AbsorberParams"
          }
        },
        "type": {
          "const": "Absorber",
          "default": "Absorber",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AbsorberParams": {
      "additionalProperties": false,
      "properties": {
        "attrs": {
          "additionalProperties": true,
          "default": {},
          "type": "object"
        },
        "sigma_max": {
          "default": 1.5,
          "minimum": 0,
          "type": "number"
        },
        "sigma_min": {
          "default": 0.0,
          "minimum": 0,
          "type": "number"
        },
        "sigma_order": {
          "default": 3,
          "minimum": 0,

exec
/bin/bash -lc "sed -n '360,560p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
    """Typed execution IR for Phase 1 subpixel interface policy selection."""

    type: Literal["SubpixelSpecIR"] = "SubpixelSpecIR"
    component_type: Literal["SubpixelSpec"] = "SubpixelSpec"
    dielectric: SubpixelPolicyIR
    metal: SubpixelPolicyIR
    pec: SubpixelPolicyIR
    pmc: SubpixelPolicyIR
    lossy_metal: SubpixelPolicyIR
    averaging_targets: tuple[str, ...] = ()
    staircasing_targets: tuple[str, ...] = ()
    requires_anisotropic_materialization: bool
    courant_ratio: float


class PeriodicIR(IRModel):
    """Typed execution IR for a periodic boundary edge."""

    type: Literal["PeriodicIR"] = "PeriodicIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Periodic"] = "Periodic"
    name: str | None = None


class BlochBoundaryIR(IRModel):
    """Typed execution IR for a Bloch boundary edge."""

    type: Literal["BlochBoundaryIR"] = "BlochBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["BlochBoundary"] = "BlochBoundary"
    name: str | None = None
    bloch_vec: float
    phase_real: float
    phase_imag: float


class PECBoundaryIR(IRModel):
    """Typed execution IR for a PEC boundary edge."""

    type: Literal["PECBoundaryIR"] = "PECBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PECBoundary"] = "PECBoundary"
    name: str | None = None


class PMCBoundaryIR(IRModel):
    """Typed execution IR for a PMC boundary edge."""

    type: Literal["PMCBoundaryIR"] = "PMCBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PMCBoundary"] = "PMCBoundary"
    name: str | None = None


class PMLParamsIR(IRModel):
    """Typed execution IR for baseline PML profile parameters."""

    type: Literal["PMLParamsIR"] = "PMLParamsIR"
    sigma_order: int
    sigma_min: float
    sigma_max: float
    kappa_order: int
    kappa_min: float
    kappa_max: float
    alpha_order: int
    alpha_min: float
    alpha_max: float


class PMLIR(IRModel):
    """Typed execution IR for a baseline PML boundary edge."""

    type: Literal["PMLIR"] = "PMLIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PML"] = "PML"
    name: str | None = None
    num_layers: int
    parameters: PMLParamsIR
    extrude_structures: bool = True


class StablePMLIR(IRModel):
    """Typed execution IR for a stable-PML boundary edge."""

    type: Literal["StablePMLIR"] = "StablePMLIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["StablePML"] = "StablePML"
    name: str | None = None
    num_layers: int
    parameters: PMLParamsIR
    extrude_structures: bool = True


class AbsorberParamsIR(IRModel):
    """Typed execution IR for adiabatic absorber parameters."""

    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
    sigma_order: int
    sigma_min: float
    sigma_max: float


class AbsorberIR(IRModel):
    """Typed execution IR for an adiabatic absorber edge."""

    type: Literal["AbsorberIR"] = "AbsorberIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Absorber"] = "Absorber"
    name: str | None = None
    num_layers: int
    parameters: AbsorberParamsIR
    extrude_structures: bool = False


class SymmetryAxisIR(IRModel):
    """Typed execution IR for one mirror-symmetry axis."""

    type: Literal["SymmetryAxisIR"] = "SymmetryAxisIR"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]


BoundaryEdgeIR = (
    PeriodicIR
    | BlochBoundaryIR
    | PECBoundaryIR
    | PMCBoundaryIR
    | PMLIR
    | StablePMLIR
    | AbsorberIR
    | ComponentIR
)


class BoundaryIR(IRModel):
    """Typed execution IR for one axis boundary pair."""

    type: Literal["BoundaryIR"] = "BoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Boundary"] = "Boundary"
    plus: BoundaryEdgeIR
    minus: BoundaryEdgeIR


class BoundarySpecIR(IRModel):
    """Typed execution IR for the Phase 1 boundary container."""

    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["BoundarySpec"] = "BoundarySpec"
    x: BoundaryIR
    y: BoundaryIR
    z: BoundaryIR
    requires_complex_fields: bool = False
    periodic_axes: tuple[str, ...] = ()
    bloch_axes: tuple[str, ...] = ()
    reflective_axes: tuple[str, ...] = ()
    pml_axes: tuple[str, ...] = ()
    pml_faces: tuple[str, ...] = ()
    stable_pml_axes: tuple[str, ...] = ()
    stable_pml_faces: tuple[str, ...] = ()
    absorber_axes: tuple[str, ...] = ()
    absorber_faces: tuple[str, ...] = ()
    active_symmetry_axes: tuple[str, ...] = ()
    symmetry_axes: tuple[SymmetryAxisIR, ...] = ()


class MediumIR(IRModel):
    """Typed execution IR for a homogeneous isotropic dielectric or conductor."""

    type: Literal["MediumIR"] = "MediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Medium"] = "Medium"
    name: str | None = None
    permittivity: float
    conductivity: float
    permeability: float
    magnetic_conductivity: float


class PECMediumIR(IRModel):
    """Typed execution IR for a perfect electric conductor marker."""

    type: Literal["PECMediumIR"] = "PECMediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["PECMedium"] = "PECMedium"
    name: str | None = None


class PMCMediumIR(IRModel):
    """Typed execution IR for a perfect magnetic conductor marker."""

    type: Literal["PMCMediumIR"] = "PMCMediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["PMCMedium"] = "PMCMedium"
    name: str | None = None


class PoleResidueIR(IRModel):

exec
/bin/bash -lc "rg -n \"class ModeSpec|ModeSpec\\(\" src/autofdtd" in /workspace/autofdtd
 exited 1 in 0ms:
exec
/bin/bash -lc "sed -n '1000,1105p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
            zz=medium_to_ir(medium.zz),
        )
    return component_to_ir(value, family=ComponentFamily.MEDIUM)


def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
    """Lower a supported boundary edge into typed execution IR."""

    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
        edge = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
        "StablePML",
        "Absorber",
    }:
        edge = boundary_edge_model_from_value(value)

    if isinstance(edge, Periodic):
        return PeriodicIR(name=edge.name)
    if isinstance(edge, BlochBoundary):
        return BlochBoundaryIR(
            name=edge.name,
            bloch_vec=edge.bloch_vec,
            phase_real=float(edge.bloch_phase.real),
            phase_imag=float(edge.bloch_phase.imag),
        )
    if isinstance(edge, PECBoundary):
        return PECBoundaryIR(name=edge.name)
    if isinstance(edge, PMCBoundary):
        return PMCBoundaryIR(name=edge.name)
    if isinstance(edge, PML):
        params = (
            edge.parameters
            if isinstance(edge.parameters, PMLParams)
            else PMLParams.model_validate(edge.parameters)
        )
        return PMLIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=PMLParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
                kappa_order=params.kappa_order,
                kappa_min=params.kappa_min,
                kappa_max=params.kappa_max,
                alpha_order=params.alpha_order,
                alpha_min=params.alpha_min,
                alpha_max=params.alpha_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    if isinstance(edge, StablePML):
        params = (
            edge.parameters
            if isinstance(edge.parameters, PMLParams)
            else PMLParams.model_validate(edge.parameters)
        )
        return StablePMLIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=PMLParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
                kappa_order=params.kappa_order,
                kappa_min=params.kappa_min,
                kappa_max=params.kappa_max,
                alpha_order=params.alpha_order,
                alpha_min=params.alpha_min,
                alpha_max=params.alpha_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    if isinstance(edge, Absorber):
        params = (
            edge.parameters
            if isinstance(edge.parameters, AbsorberParams)
            else AbsorberParams.model_validate(edge.parameters)
        )
        return AbsorberIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=AbsorberParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    return component_to_ir(value, family=ComponentFamily.BOUNDARY)


def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
    """Lower a supported axis boundary pair into typed execution IR."""

    boundary: Boundary | None = None
    if isinstance(value, Boundary):
        boundary = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Boundary",

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/api/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API namespace for Tidy3D-inspired object models."""

from autofdtd.api.simulation import (
    AbstractSubpixelPolicy,
    Absorber,
    AbsorberParams,
    AnisotropicMedium,
    AutoFDTDModel,
    AutoFDTDValidationWarning,
    AutoGrid,
    Box,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    ClipOperation,
    CustomAnisotropicMedium,
    CustomMedium,
    CustomGrid,
    CustomGridBoundaries,
    Cylinder,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PECBoundary,
    PML,
    PMLParams,
    PMCMedium,
    PMCBoundary,
    PerturbationMedium,
    PerturbationPoleResidue,
    PolarizedAveraging,
    PoleResidue,
    PolySlab,
    PrimitiveGeometry,
    Periodic,
    ResolvedGrid,
    ResolvedGridAxis,
    Scene,
    Sellmeier,
    Simulation,
    Sphere,
    Staircasing,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
    StablePML,
    SubpixelSpec,
    TaggedModel,
    Transformed,
    UniformGrid,
    UnsupportedFeatureError,
)

__all__ = [
    "AutoFDTDModel",
    "AutoFDTDValidationWarning",
    "AbstractSubpixelPolicy",
    "Absorber",
    "AbsorberParams",
    "AnisotropicMedium",
    "AutoGrid",
    "Box",
    "BlochBoundary",
    "Boundary",
    "BoundarySpec",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",
    "Sellmeier",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "ResolvedGrid",
    "ResolvedGridAxis",
    "Scene",
    "Simulation",
    "Sphere",
    "Structure",
    "StructurePrecedence",
    "StructurePriorityMode",
    "StablePML",
    "Staircasing",
    "SubpixelSpec",
    "TaggedModel",
    "Transformed",
    "UniformGrid",
    "UnsupportedFeatureError",
]

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/boundaries/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary-family models for the foundational Phase 1 surface."""

from autofdtd.boundaries.models import (
    Absorber,
    AbsorberParams,
    Boundary,
    BoundaryEdge,
    BoundarySpec,
    BlochBoundary,
    DEFAULT_ABSORBER_PARAMS,
    DEFAULT_PML_PARAMS,
    DEFAULT_STABLE_PML_PARAMS,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    StablePML,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)

__all__ = [
    "Absorber",
    "AbsorberParams",
    "Boundary",
    "BoundaryEdge",
    "BoundarySpec",
    "BlochBoundary",
    "DEFAULT_ABSORBER_PARAMS",
    "DEFAULT_PML_PARAMS",
    "DEFAULT_STABLE_PML_PARAMS",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCBoundary",
    "Periodic",
    "StablePML",
    "boundary_edge_model_from_value",
    "boundary_model_from_value",
    "boundary_spec_model_from_value",
]

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/compiler/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Scene compilation, normalization, and lowering namespace."""

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryAxis,
    CompiledBoundaryEdge,
    CompiledPMLCoefficients,
    CompiledBoundarySpec,
    CompiledSymmetryAxis,
    compile_boundary_spec,
    normalize_axis_boundary,
)
from autofdtd.compiler.materials import (
    AnisotropicMaterialCoefficients,
    EPSILON_0,
    MU_0,
    ConstitutiveMode,
    IsotropicMaterialCoefficients,
    MaterialCoefficients,
    PoleResidueMaterialCoefficients,
    PoleResidueTermCoefficients,
    SceneMaterialSample,
    compile_debye_coefficients,
    compile_anisotropic_medium_coefficients,
    compile_drude_coefficients,
    compile_isotropic_medium_coefficients,
    compile_lorentz_coefficients,
    compile_medium_coefficients,
    compile_pole_residue_coefficients,
    compile_scene_medium_coefficients,
    compile_sellmeier_coefficients,
    pole_residue_terms_as_complex,
    sample_scene_mediums,
)

__all__ = [
    "BoundaryMode",
    "CompiledBoundaryAxis",
    "CompiledBoundaryEdge",
    "CompiledPMLCoefficients",
    "CompiledBoundarySpec",
    "CompiledSymmetryAxis",
    "ConstitutiveMode",
    "EPSILON_0",
    "MU_0",
    "AnisotropicMaterialCoefficients",
    "IsotropicMaterialCoefficients",
    "MaterialCoefficients",
    "PoleResidueMaterialCoefficients",
    "PoleResidueTermCoefficients",
    "SceneMaterialSample",
    "compile_anisotropic_medium_coefficients",
    "compile_boundary_spec",
    "compile_debye_coefficients",
    "compile_drude_coefficients",
    "compile_medium_coefficients",
    "compile_isotropic_medium_coefficients",
    "compile_lorentz_coefficients",
    "compile_pole_residue_coefficients",
    "compile_sellmeier_coefficients",
    "compile_scene_medium_coefficients",
    "pole_residue_terms_as_complex",
    "normalize_axis_boundary",
    "sample_scene_mediums",
]

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/kernels/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Warp kernel conventions and staged update namespace."""

from autofdtd.kernels.boundaries import (
    WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
    PMLBoundaryState,
    PMLFaceState,
    apply_axis_boundary_ghosts,
    apply_boundary_ghosts,
    apply_boundary_stages,
    apply_pml_layers,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.kernels.materials import (
    AnisotropicAuxiliaryState,
    WARP_AVAILABLE,
    allocate_anisotropic_state,
    anisotropic_electric_update,
    anisotropic_magnetic_update,
    PoleResidueAuxiliaryState,
    allocate_pole_residue_state,
    constitutive_kernel_metadata,
    electric_constitutive_update,
    magnetic_constitutive_update,
    pole_residue_electric_update,
    warp_backend_available,
)

__all__ = [
    "AnisotropicAuxiliaryState",
    "BOUNDARY_WARP_AVAILABLE",
    "PMLBoundaryState",
    "PMLFaceState",
    "PoleResidueAuxiliaryState",
    "WARP_AVAILABLE",
    "allocate_anisotropic_state",
    "allocate_pole_residue_state",
    "apply_axis_boundary_ghosts",
    "apply_boundary_ghosts",
    "apply_boundary_stages",
    "apply_pml_layers",
    "boundary_edge_transform",
    "anisotropic_electric_update",
    "anisotropic_magnetic_update",
    "boundary_kernel_metadata",
    "constitutive_kernel_metadata",
    "electric_constitutive_update",
    "magnetic_constitutive_update",
    "pole_residue_electric_update",
    "symmetry_transform",
    "warp_backend_available",
]

exec
/bin/bash -lc "sed -n '1,360p' src/autofdtd/kernels/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary halo and PML update helpers for the foundational Phase 1 surface."""

from __future__ import annotations

from typing import Any, Literal

import numpy as np

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryAxis,
    CompiledBoundaryEdge,
    CompiledBoundarySpec,
    CompiledPMLCoefficients,
    CompiledSymmetryAxis,
)
from autofdtd.core.models import AutoFDTDModel

try:
    import warp as wp
except ModuleNotFoundError:  # pragma: no cover - optional dependency
    wp = None

WARP_AVAILABLE = wp is not None


class PMLFaceState(AutoFDTDModel):
    """Persistent per-face memory for the staged PML update."""

    type: str = "PMLFaceState"
    axis: Literal["x", "y", "z"]
    side: Literal["minus", "plus"]
    memory_shape: tuple[int, ...]
    memory: np.ndarray

    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}


class PMLBoundaryState(AutoFDTDModel):
    """Persistent state bundle for all active PML faces."""

    type: str = "PMLBoundaryState"
    electric: dict[str, PMLFaceState]
    magnetic: dict[str, PMLFaceState]

    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}


def _ghost_and_source_slices(
    axis: int, side: str, *, ndim: int
) -> tuple[tuple[object, ...], tuple[object, ...]]:
    ghost = [slice(None)] * ndim
    source = [slice(None)] * ndim
    if side == "minus":
        ghost[axis] = 0
        source[axis] = -2
    else:
        ghost[axis] = -1
        source[axis] = 1
    return tuple(ghost), tuple(source)


def _interior_reflection_slices(
    axis: int, side: str, *, ndim: int
) -> tuple[tuple[object, ...], tuple[object, ...]]:
    ghost = [slice(None)] * ndim
    source = [slice(None)] * ndim
    if side == "minus":
        ghost[axis] = 0
        source[axis] = 1
    else:
        ghost[axis] = -1
        source[axis] = -2
    return tuple(ghost), tuple(source)


def _apply_edge(
    field: np.ndarray,
    axis_index: int,
    side: str,
    mode: BoundaryMode,
    signs: tuple[int, int, int],
    phase_factor: complex,
) -> None:
    if field.ndim != 4 or field.shape[-1] != 3:
        raise ValueError("boundary halo updates expect arrays shaped (nx, ny, nz, 3)")
    if min(field.shape[:3]) < 3:
        raise ValueError(
            "boundary halo updates require one ghost cell and one interior cell per side"
        )
    if mode is BoundaryMode.PERIODIC:
        ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
        field[ghost] = field[source]
        return
    if mode is BoundaryMode.BLOCH:
        ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
        field[ghost] = field[source] * field.dtype.type(phase_factor)
        return
    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
        return
    ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
    field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)


def _pml_region_slices(
    axis_index: int,
    side: Literal["minus", "plus"],
    num_layers: int,
    *,
    ndim: int,
) -> tuple[object, ...]:
    region = [slice(None)] * ndim
    if side == "minus":
        region[axis_index] = slice(1, 1 + num_layers)
    else:
        region[axis_index] = slice(-1 - num_layers, -1)
    return tuple(region)


def _validate_pml_capacity(field: np.ndarray, axis_index: int, num_layers: int) -> None:
    interior = field.shape[axis_index] - 2
    if num_layers > interior:
        raise ValueError(
            f"PML with {num_layers} layers does not fit along axis index {axis_index}; "
            f"only {interior} interior cells are available"
        )


def _apply_pml_region(
    field: np.ndarray,
    face_state: PMLFaceState,
    coefficients: CompiledPMLCoefficients,
    *,
    axis_index: int,
    side: Literal["minus", "plus"],
) -> None:
    region = _pml_region_slices(axis_index, side, coefficients.num_layers, ndim=field.ndim)
    region_values = field[region]
    line_shape = [1, 1, 1, 1]
    line_shape[axis_index] = coefficients.num_layers
    attenuation = np.asarray(coefficients.attenuation, dtype=field.real.dtype).reshape(line_shape)
    memory_decay = np.asarray(coefficients.memory_decay, dtype=field.real.dtype).reshape(line_shape)
    memory_drive = np.asarray(coefficients.memory_drive, dtype=field.real.dtype).reshape(line_shape)
    stretch = np.asarray(coefficients.stretch, dtype=field.real.dtype).reshape(line_shape)
    face_state.memory[...] = face_state.memory * memory_decay + region_values * memory_drive
    field[region] = region_values * attenuation * stretch


def boundary_edge_transform(
    values: np.ndarray,
    edge: CompiledBoundaryEdge,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply the runtime transform associated with one boundary edge."""

    if edge.mode is BoundaryMode.PERIODIC:
        return np.array(values, copy=True)
    if edge.mode is BoundaryMode.BLOCH:
        return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
        return np.array(values, copy=True)
    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)


def symmetry_transform(
    values: np.ndarray,
    symmetry_axis: CompiledSymmetryAxis,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply mirror-symmetry signs to vector values imported from a reflected chunk."""

    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    return np.array(values, copy=True) * np.asarray(getattr(symmetry_axis, signs_name), dtype=values.dtype)


def apply_axis_boundary_ghosts(
    field: np.ndarray,
    axis_boundary: CompiledBoundaryAxis,
    *,
    field_family: str,
) -> np.ndarray:
    """Apply one axis boundary pair to a vector field with one ghost cell per face."""

    signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
    updated = np.array(field, copy=True)
    axis_index = "xyz".index(axis_boundary.axis)
    _apply_edge(
        updated,
        axis_index,
        "minus",
        axis_boundary.minus.mode,
        getattr(axis_boundary.minus, signs_name),
        axis_boundary.minus.phase_factor,
    )
    _apply_edge(
        updated,
        axis_index,
        "plus",
        axis_boundary.plus.mode,
        getattr(axis_boundary.plus, signs_name),
        axis_boundary.plus.phase_factor,
    )
    return updated


def apply_boundary_ghosts(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply compiled boundary ghost updates to electric and magnetic vector fields."""

    electric = np.array(electric_field, copy=True)
    magnetic = np.array(magnetic_field, copy=True)
    for axis_boundary in boundary_spec.axes():
        electric = apply_axis_boundary_ghosts(electric, axis_boundary, field_family="electric")
        magnetic = apply_axis_boundary_ghosts(magnetic, axis_boundary, field_family="magnetic")
    return electric, magnetic


def apply_pml_layers(
    field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
    state: PMLBoundaryState,
    *,
    field_family: Literal["electric", "magnetic"],
) -> np.ndarray:
    """Apply the staged PML damping update to one vector field family."""

    updated = np.array(field, copy=True)
    face_states = state.electric if field_family == "electric" else state.magnetic
    for axis_boundary in boundary_spec.axes():
        axis_index = "xyz".index(axis_boundary.axis)
        for side in ("minus", "plus"):
            edge = getattr(axis_boundary, side)
            if edge.mode not in {
                BoundaryMode.PML,
                BoundaryMode.STABLE_PML,
                BoundaryMode.ABSORBER,
            } or edge.pml is None:
                continue
            face_key = f"{axis_boundary.axis}.{side}"
            face_state = face_states[face_key]
            _validate_pml_capacity(updated, axis_index, edge.pml.num_layers)
            _apply_pml_region(
                updated,
                face_state,
                edge.pml,
                axis_index=axis_index,
                side=side,
            )
    return updated


def apply_boundary_stages(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    boundary_spec: CompiledBoundarySpec,
    state: PMLBoundaryState | None = None,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply all compiled boundary stages in declared order."""

    electric = np.array(electric_field, copy=True)
    magnetic = np.array(magnetic_field, copy=True)
    for stage in boundary_spec.stage_order:
        if stage == "electric_boundary":
            electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
        elif stage == "magnetic_boundary":
            magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
        elif stage == "electric_pml":
            if state is None:
                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
            electric = apply_pml_layers(electric, boundary_spec, state, field_family="electric")
        elif stage == "magnetic_pml":
            if state is None:
                raise ValueError("PML boundary stages require allocated PMLBoundaryState")
            magnetic = apply_pml_layers(magnetic, boundary_spec, state, field_family="magnetic")
    return electric, magnetic


def boundary_kernel_metadata() -> dict[str, Any]:
    """Expose the current boundary backend and staged update choices."""

    return {
        "backend": "warp" if WARP_AVAILABLE else "numpy",
        "warp_available": WARP_AVAILABLE,
        "module_contents_stable": True,
        "stages": (
            "electric_boundary",
            "magnetic_boundary",
            "electric_pml",
            "magnetic_pml",
        ),
        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
        "supports_complex_fields": True,
        "supports_symmetry_transforms": True,
        "supports_pml_layers": True,
        "supports_stable_pml": True,
        "supports_absorber": True,
        "halo_depth": 1,
    }

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/runtime/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Runtime-facing boundary orchestration for the foundational Phase 1 surface."""

from __future__ import annotations

from typing import Literal

import numpy as np

from autofdtd.compiler.boundaries import (
    BoundaryMode,
    CompiledBoundaryEdge,
    CompiledBoundarySpec,
    CompiledSymmetryAxis,
    compile_boundary_spec,
)
from autofdtd.kernels.boundaries import (
    PMLBoundaryState,
    PMLFaceState,
    apply_boundary_stages,
    boundary_edge_transform,
    symmetry_transform,
)


def allocate_pml_boundary_state(
    compiled_boundary_spec: CompiledBoundarySpec,
    field_shape: tuple[int, int, int, int],
    *,
    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
) -> PMLBoundaryState:
    """Allocate persistent absorbing-boundary memory arrays for all active faces."""

    if len(field_shape) != 4 or field_shape[-1] != 3:
        raise ValueError("PML state allocation expects field shapes (nx, ny, nz, 3)")
    electric: dict[str, PMLFaceState] = {}
    magnetic: dict[str, PMLFaceState] = {}

    for axis_boundary in compiled_boundary_spec.axes():
        axis_index = "xyz".index(axis_boundary.axis)
        for side in ("minus", "plus"):
            edge = getattr(axis_boundary, side)
            if edge.mode not in {
                BoundaryMode.PML,
                BoundaryMode.STABLE_PML,
                BoundaryMode.ABSORBER,
            } or edge.pml is None:
                continue
            memory_shape = list(field_shape)
            memory_shape[axis_index] = edge.pml.num_layers
            face_state = PMLFaceState(
                axis=axis_boundary.axis,
                side=side,
                memory_shape=tuple(int(value) for value in memory_shape),
                memory=np.zeros(memory_shape, dtype=dtype),
            )
            face_key = f"{axis_boundary.axis}.{side}"
            electric[face_key] = face_state
            magnetic[face_key] = PMLFaceState(
                axis=axis_boundary.axis,
                side=side,
                memory_shape=face_state.memory_shape,
                memory=np.zeros(memory_shape, dtype=dtype),
            )
    return PMLBoundaryState(electric=electric, magnetic=magnetic)


class BoundaryRuntime:
    """Small runtime wrapper around compiled Phase 1 boundary metadata."""

    def __init__(
        self,
        boundary_spec: object | None = None,
        *,
        symmetry: tuple[int, int, int] = (0, 0, 0),
        dt: float = 1.0,
        grid_spacing: float | tuple[float, float, float] = 1.0,
        field_shape: tuple[int, int, int, int] | None = None,
        field_dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
    ) -> None:
        self.compiled = compile_boundary_spec(
            boundary_spec,
            symmetry=symmetry,
            dt=dt,
            grid_spacing=grid_spacing,
        )
        self.pml_state = (
            allocate_pml_boundary_state(self.compiled, field_shape, dtype=field_dtype)
            if field_shape is not None and self.compiled.pml_faces
            else None
        )

    def allocate_state(
        self,
        field_shape: tuple[int, int, int, int],
        *,
        dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
    ) -> PMLBoundaryState | None:
        """Allocate or refresh PML state for a given field layout."""

        if not self.compiled.pml_faces:
            self.pml_state = None
            return None
        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
        return self.pml_state

    def apply(
        self,
        electric_field: np.ndarray,
        magnetic_field: np.ndarray,
    ) -> tuple[np.ndarray, np.ndarray]:
        """Apply ghost-cell and PML stage updates for both field families."""

        if self.compiled.pml_faces and self.pml_state is None:
            self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
        return apply_boundary_stages(
            electric_field,
            magnetic_field,
            self.compiled,
            self.pml_state,
        )

    def transform_boundary_values(
        self,
        values: np.ndarray,
        edge: CompiledBoundaryEdge,
        *,
        field_family: Literal["electric", "magnetic"],
    ) -> np.ndarray:
        """Transform halo values imported from a neighboring chunk or wrapped face."""

        return boundary_edge_transform(values, edge, field_family=field_family)

    def transform_symmetry_values(
        self,
        values: np.ndarray,
        symmetry_axis: CompiledSymmetryAxis,
        *,
        field_family: Literal["electric", "magnetic"],
    ) -> np.ndarray:
        """Transform vector values imported through a mirror-symmetry reduction."""

        return symmetry_transform(values, symmetry_axis, field_family=field_family)


def build_boundary_runtime(
    boundary_spec: object | None = None,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
    dt: float = 1.0,
    grid_spacing: float | tuple[float, float, float] = 1.0,
    field_shape: tuple[int, int, int, int] | None = None,
    field_dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
) -> BoundaryRuntime:
    """Build the default Phase 1 boundary runtime wrapper."""

    return BoundaryRuntime(
        boundary_spec,
        symmetry=symmetry,
        dt=dt,
        grid_spacing=grid_spacing,
        field_shape=field_shape,
        field_dtype=field_dtype,
    )


def apply_compiled_boundaries(
    electric_field: np.ndarray,
    magnetic_field: np.ndarray,
    compiled_boundary_spec: CompiledBoundarySpec,
    *,
    state: PMLBoundaryState | None = None,
) -> tuple[np.ndarray, np.ndarray]:
    """Apply boundary ghosts and PML stages using a precompiled specification."""

    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)

exec
/bin/bash -lc 'rg -n "from autofdtd.boundaries|Absorber|StablePML|BoundarySpec" src/autofdtd/api/simulation.py' in /workspace/autofdtd
 succeeded in 0ms:
12:from autofdtd.boundaries import (
13:    Absorber,
14:    AbsorberParams,
17:    BoundarySpec,
23:    StablePML,
77:    "Absorber",
78:    "AbsorberParams",
82:    "BoundarySpec",
128:    "StablePML",

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/materials/advanced.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Explicit Phase 1 policy surfaces for deferred and rejected advanced media."""

from __future__ import annotations

from typing import Any, Literal

from pydantic import Field, field_validator, model_validator

from autofdtd.core.models import TaggedModel
from autofdtd.materials.dispersive import PoleResidue
from autofdtd.materials.isotropic import Medium, _normalize_name, _positive_finite


def _optional_positive_finite(value: float | None, *, field_name: str) -> float | None:
    if value is None:
        return None
    return _positive_finite(value, field_name=field_name)


class LossyMetalMedium(Medium):
    """Deferred Phase 1 surface for surface-impedance lossy-metal modeling."""

    type: Literal["LossyMetalMedium"] = "LossyMetalMedium"
    permittivity: Literal[1.0] = 1.0
    conductivity: float
    frequency_range: tuple[float, float]
    allow_gain: Literal[False] = False
    thickness: float | None = None
    roughness: float | None = None

    @field_validator("frequency_range", mode="before")
    @classmethod
    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
        if not isinstance(value, tuple | list) or len(value) != 2:
            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
        lower = _positive_finite(float(value[0]), field_name="frequency_range[0]")
        upper = _positive_finite(float(value[1]), field_name="frequency_range[1]")
        if upper <= lower:
            raise ValueError("frequency_range must be strictly increasing")
        return (lower, upper)

    @field_validator("thickness", "roughness")
    @classmethod
    def _validate_optional_positive(cls, value: float | None, info) -> float | None:
        return _optional_positive_finite(value, field_name=info.field_name)

    @property
    def phase1_policy(self) -> str:
        return (
            "LossyMetalMedium is parsed and serialized, but direct Phase 1 runtime support is "
            "deferred pending a dedicated surface-impedance or volumetric metal path."
        )

    def to_medium_approximation(self) -> Medium:
        """Return the homogeneous volumetric fallback medium used by Phase 1 guidance."""

        return Medium(
            name=self.name,
            permittivity=1.0,
            conductivity=self.conductivity,
            permeability=self.permeability,
            magnetic_conductivity=self.magnetic_conductivity,
        )


class PerturbationMedium(Medium):
    """Deferred Phase 1 surface for parameter-perturbed nondispersive media."""

    type: Literal["PerturbationMedium"] = "PerturbationMedium"
    permittivity_perturbation: dict[str, Any] | None = None
    conductivity_perturbation: dict[str, Any] | None = None
    perturbation_spec: dict[str, Any] | None = None
    frequency_range: tuple[float, float] | None = None
    subpixel: bool = True

    @field_validator("frequency_range", mode="before")
    @classmethod
    def _validate_frequency_range(
        cls, value: object | None
    ) -> tuple[float, float] | None:
        if value is None:
            return None
        if not isinstance(value, tuple | list) or len(value) != 2:
            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
        lower = _positive_finite(float(value[0]), field_name="frequency_range[0]")
        upper = _positive_finite(float(value[1]), field_name="frequency_range[1]")
        if upper <= lower:
            raise ValueError("frequency_range must be strictly increasing")
        return (lower, upper)

    @property
    def phase1_policy(self) -> str:
        return (
            "PerturbationMedium is parsed and serialized, but Phase 1 does not execute "
            "heat-, charge-, or bias-dependent material perturbations."
        )

    def base_medium(self) -> Medium:
        """Drop perturbation metadata and keep the unperturbed homogeneous medium."""

        return Medium(
            name=self.name,
            permittivity=self.permittivity,
            conductivity=self.conductivity,
            permeability=self.permeability,
            magnetic_conductivity=self.magnetic_conductivity,
        )


class PerturbationPoleResidue(PoleResidue):
    """Deferred Phase 1 surface for perturbed pole-residue media."""

    type: Literal["PerturbationPoleResidue"] = "PerturbationPoleResidue"
    eps_inf_perturbation: dict[str, Any] | None = None
    poles_perturbation: tuple[tuple[dict[str, Any] | None, dict[str, Any] | None], ...] | None = None
    perturbation_spec: dict[str, Any] | None = None
    subpixel: bool = True

    @property
    def phase1_policy(self) -> str:
        return (
            "PerturbationPoleResidue is parsed and serialized, but Phase 1 does not execute "
            "runtime perturbations on dispersive pole-residue coefficients."
        )

    def base_medium(self) -> PoleResidue:
        """Drop perturbation metadata and keep the unperturbed dispersive medium."""

        return PoleResidue(name=self.name, eps_inf=self.eps_inf, poles=self.poles)


class CustomMedium(TaggedModel):
    """Rejected Phase 1 surface for sampled spatially varying isotropic media."""

    type: Literal["CustomMedium"] = "CustomMedium"
    name: str | None = None
    permittivity: Any = None
    conductivity: Any = None
    frequency_range: tuple[float, float] | None = None
    derived_from: Any = None
    interp_method: Literal["nearest", "linear"] = "linear"
    subpixel: bool = True

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_name(value)

    @field_validator("frequency_range", mode="before")
    @classmethod
    def _validate_frequency_range(
        cls, value: object | None
    ) -> tuple[float, float] | None:
        if value is None:
            return None
        if not isinstance(value, tuple | list) or len(value) != 2:
            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
        lower = _positive_finite(float(value[0]), field_name="frequency_range[0]")
        upper = _positive_finite(float(value[1]), field_name="frequency_range[1]")
        if upper <= lower:
            raise ValueError("frequency_range must be strictly increasing")
        return (lower, upper)

    @property
    def phase1_policy(self) -> str:
        return (
            "CustomMedium is parsed for compatibility inspection only. Phase 1 rejects sampled "
            "or spatially varying custom media because runtime interpolation and storage are out of scope."
        )


class CustomAnisotropicMedium(TaggedModel):
    """Rejected Phase 1 surface for sampled spatially varying diagonal anisotropy."""

    type: Literal["CustomAnisotropicMedium"] = "CustomAnisotropicMedium"
    name: str | None = None
    xx: Any
    yy: Any
    zz: Any
    frequency_range: tuple[float, float] | None = None
    derived_from: Any = None
    interp_method: Literal["nearest", "linear"] | None = "linear"
    subpixel: bool | None = True

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_name(value)

    @field_validator("frequency_range", mode="before")
    @classmethod
    def _validate_frequency_range(
        cls, value: object | None
    ) -> tuple[float, float] | None:
        if value is None:
            return None
        if not isinstance(value, tuple | list) or len(value) != 2:
            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
        lower = _positive_finite(float(value[0]), field_name="frequency_range[0]")
        upper = _positive_finite(float(value[1]), field_name="frequency_range[1]")
        if upper <= lower:
            raise ValueError("frequency_range must be strictly increasing")
        return (lower, upper)

    @property
    def phase1_policy(self) -> str:
        return (
            "CustomAnisotropicMedium is parsed for compatibility inspection only. Phase 1 rejects "
            "sampled tensor media because per-voxel interpolation and tensor materialization are out of scope."
        )


class GenericCustomMedium(TaggedModel):
    """Rejected Phase 1 compatibility bucket for other `Custom*` material tags."""

    type: str
    name: str | None = None
    payload: dict[str, Any] = Field(default_factory=dict)

    @field_validator("type")
    @classmethod
    def _validate_type_name(cls, value: str) -> str:
        if not value.startswith("Custom"):
            raise ValueError("GenericCustomMedium only accepts Custom* type tags")
        return value

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_name(value)

    @model_validator(mode="before")
    @classmethod
    def _fold_extra_fields(cls, value: object) -> object:
        if not isinstance(value, dict):
            return value
        if "payload" in value:
            return value
        payload = {key: item for key, item in value.items() if key not in {"type", "name"}}
        return {"type": value.get("type"), "name": value.get("name"), "payload": payload}

    @property
    def phase1_policy(self) -> str:
        return (
            f"{self.type} is mapped to the Phase 1 custom-media reject bucket. "
            "Sampled custom material families are not executed in Phase 1."
        )


__all__ = [
    "CustomAnisotropicMedium",
    "CustomMedium",
    "GenericCustomMedium",
    "LossyMetalMedium",
    "PerturbationMedium",
    "PerturbationPoleResidue",
]

exec
/bin/bash -lc "sed -n '520,660p' tests/test_materials.py" in /workspace/autofdtd
 succeeded in 0ms:
        compile_medium_coefficients(
            LossyMetalMedium(conductivity=5.0e6, frequency_range=(8.0e9, 12.0e9)),
            dt=1e-12,
        )

    with pytest.raises(ValueError, match="PerturbationMedium parsing is available"):
        compile_medium_coefficients(
            PerturbationMedium(permittivity=2.5, permittivity_perturbation={"heat": {"coeff": 1.0e-4}}),
            dt=1e-12,
        )

    with pytest.raises(ValueError, match="custom-media reject bucket"):
        compile_medium_coefficients(
            CustomMedium(permittivity={"dataset": "eps"}),
            dt=1e-12,
        )


def test_typed_medium_ir_is_emitted_for_simulations() -> None:
    simulation = Simulation(
        size=(4.0, 4.0, 4.0),
        run_time=1.0,
        medium=PoleResidue(eps_inf=2.0, poles=(((-1.0e12, 0.0), (1.0e10, 0.0)),)),
        structure_priority_mode=StructurePriorityMode.CONDUCTOR,
        structures=(
            Structure(
                geometry=Box(center=(0.0, 0.0, 0.0), size=(1.0, 1.0, 1.0)),
                medium=PECMedium(),
                name="pec",
                background_medium=PMCMedium(),
            ),
        ),
    )
    simulation_ir = simulation_to_ir(simulation)

    assert isinstance(simulation_ir.scene.background_medium, PoleResidueIR)
    assert isinstance(simulation_ir.scene.structures[0].medium, PECMediumIR)
    assert isinstance(simulation_ir.scene.structures[0].background_medium, PMCMediumIR)


def test_anisotropic_medium_ir_is_emitted_for_simulations() -> None:
    simulation = Simulation(
        size=(4.0, 4.0, 4.0),
        run_time=1.0,
        medium=AnisotropicMedium(
            xx=Medium(permittivity=2.0),
            yy=Sellmeier(coeffs=((0.5, 3.0e-15),)),
            zz=PECMedium(),
        ),
    )

    simulation_ir = simulation_to_ir(simulation)

    assert isinstance(simulation_ir.scene.background_medium, AnisotropicMediumIR)
    assert simulation_ir.scene.background_medium.yy.component_type == "Sellmeier"


def test_medium2d_and_fully_anisotropic_are_rejected_by_simulation_validation() -> None:
    with pytest.raises(Exception, match="Medium2D"):
        Simulation(size=(4.0, 4.0, 4.0), run_time=1.0, medium=Medium2D(ss=Medium(), tt=Medium()))

    with pytest.raises(Exception, match="FullyAnisotropicMedium"):
        Simulation(size=(4.0, 4.0, 4.0), run_time=1.0, medium=FullyAnisotropicMedium())


def test_deferred_and_rejected_advanced_media_are_mapped_by_validation_instead_of_unknown() -> None:
    with pytest.raises(Exception, match="deferred feature 'LossyMetalMedium'"):
        Simulation(
            size=(4.0, 4.0, 4.0),
            run_time=1.0,
            medium=LossyMetalMedium(conductivity=5.0e6, frequency_range=(8.0e9, 12.0e9)),
        )

    with pytest.raises(Exception, match="deferred feature 'PerturbationMedium'"):
        Simulation(
            size=(4.0, 4.0, 4.0),
            run_time=1.0,
            medium=PerturbationMedium(permittivity=2.5, permittivity_perturbation={"heat": {"coeff": 1.0e-4}}),
        )

    with pytest.raises(Exception, match="rejected clearly feature 'CustomMedium'"):
        Simulation(
            size=(4.0, 4.0, 4.0),
            run_time=1.0,
            medium=CustomMedium(permittivity={"dataset": "eps"}),
        )

    with pytest.raises(Exception, match="rejected clearly feature 'CustomPoleResidue'"):
        Simulation(
            size=(4.0, 4.0, 4.0),
            run_time=1.0,
            medium={"type": "CustomPoleResidue", "eps_dataset": "eps"},
        )


def test_medium_model_from_value_parses_explicit_advanced_policy_surfaces() -> None:
    assert isinstance(
        medium_model_from_value(
            {"type": "LossyMetalMedium", "conductivity": 5.0e6, "frequency_range": (8.0e9, 12.0e9)}
        ),
        LossyMetalMedium,
    )
    assert isinstance(
        medium_model_from_value(
            {"type": "PerturbationPoleResidue", "eps_inf": 2.0, "poles": (((-1.0e12, 0.0), (1.0e10, 0.0)),)}
        ),
        PerturbationPoleResidue,
    )
    parsed = medium_model_from_value({"type": "CustomPoleResidue", "eps_dataset": "eps"})
    assert parsed.type == "CustomPoleResidue"


def test_sellmeier_ir_is_emitted_for_simulations() -> None:
    simulation = Simulation(
        size=(4.0, 4.0, 4.0),
        run_time=1.0,
        medium=Sellmeier(coeffs=((0.6961663, 4.67914825849e-15),)),
    )

    simulation_ir = simulation_to_ir(simulation)

    assert isinstance(simulation_ir.scene.background_medium, SellmeierIR)


@pytest.mark.parametrize(
    ("medium", "expected_type"),
    [
        (Lorentz(eps_inf=1.8, coeffs=((0.6, 220e12, 12e12),)), LorentzIR),
        (Drude(eps_inf=1.2, coeffs=((180e12, 8e12),)), DrudeIR),
        (Debye(eps_inf=2.1, coeffs=((1.4, 5.0e-12),)), DebyeIR),
    ],
)
def test_analytical_dispersion_ir_is_emitted_for_simulations(
    medium: Lorentz | Drude | Debye,
    expected_type,
) -> None:
    simulation = Simulation(size=(4.0, 4.0, 4.0), run_time=1.0, medium=medium)

    simulation_ir = simulation_to_ir(simulation)

    assert isinstance(simulation_ir.scene.background_medium, expected_type)

exec
/bin/bash -lc "rg -n \"def _validate_supported_types|feature_entry\\(\" src/autofdtd/core/validation.py src/autofdtd/planning.py" in /workspace/autofdtd
 succeeded in 0ms:
src/autofdtd/core/validation.py:342:def _validate_supported_types(component: object, *, context: str) -> None:
src/autofdtd/core/validation.py:364:    entry = feature_entry(feature_name)
src/autofdtd/core/validation.py:368:        return feature_entry("PML")
src/autofdtd/core/validation.py:370:        return feature_entry("Absorber")
src/autofdtd/core/validation.py:373:            return feature_entry("CustomMedia")
src/autofdtd/planning.py:400:def feature_entry(feature: str) -> FeatureEntry | None:
src/autofdtd/planning.py:409:    entry = feature_entry(feature)

exec
/bin/bash -lc "sed -n '320,420p' src/autofdtd/core/validation.py" in /workspace/autofdtd
 succeeded in 0ms:
        isinstance(geometry, Mapping)
        and component_type_name(geometry) in _IMPLEMENTED_BOUNDS_GEOMETRY
    ):
        return geometry_model_from_value(geometry).contains_point(normalized_point)
    raise TypeError(f"geometry containment is not available for {type(geometry)!r}")


def _normalize_name_field(component: object) -> object:
    if isinstance(component, BaseModel) and hasattr(component, "name"):
        name = normalize_name(component.name)
        if name != component.name:
            return component.model_copy(update={"name": name})
        return component
    if isinstance(component, Mapping) and "name" in component:
        normalized_name = normalize_name(component.get("name"))
        if normalized_name != component.get("name"):
            updated = dict(component)
            updated["name"] = normalized_name
            return updated
    return component


def _validate_supported_types(component: object, *, context: str) -> None:
    for feature_name in _iter_type_tags(component):
        if context == "geometry" and feature_name in {"ClipOperation", "GeometryTransform"}:
            continue
        entry = _feature_entry_for_type(feature_name, context=context)
        if entry is None:
            raise UnsupportedFeatureError(
                f"{context} uses unknown feature type {feature_name!r}; add it to the Phase 1 "
                "feature matrix before accepting it in the public API"
            )
        if entry.status is FeatureStatus.IMPLEMENT:
            continue
        status_text = {
            FeatureStatus.DEFER: "deferred",
            FeatureStatus.REJECT_CLEARLY: "rejected clearly",
        }[entry.status]
        raise UnsupportedFeatureError(
            f"{context} uses {status_text} feature {feature_name!r}. {entry.notes}"
        )


def _feature_entry_for_type(feature_name: str, *, context: str):
    entry = feature_entry(feature_name)
    if entry is not None:
        return entry
    if context == "boundary_spec" and feature_name == "PMLParams":
        return feature_entry("PML")
    if context == "boundary_spec" and feature_name == "AbsorberParams":
        return feature_entry("Absorber")
    if context in {"medium", "background_medium", "scene.medium"}:
        if feature_name.startswith("Custom"):
            return feature_entry("CustomMedia")
    return None


def _iter_type_tags(value: object) -> Iterable[str]:
    if isinstance(value, BaseModel):
        yield from _iter_type_tags(value.model_dump(mode="python", exclude_none=True))
        return
    if isinstance(value, Mapping):
        tagged_type = value.get("type")
        if tagged_type is not None:
            yield str(tagged_type)
        for nested in value.values():
            yield from _iter_type_tags(nested)
        return
    if isinstance(value, Sequence) and not isinstance(value, (str, bytes, bytearray)):
        for item in value:
            yield from _iter_type_tags(item)


def _normalize_geometry(component: object) -> object:
    if not isinstance(component, Mapping):
        return component

    geometry_type = component_type_name(component)
    if geometry_type not in _IMPLEMENTED_BOUNDS_GEOMETRY:
        return component

    updated = dict(component)
    if geometry_type in {"GeometryGroup", "Transformed", "ClipOperation", "GeometryArray"}:
        if (
            geometry_type in _PARTIAL_GEOMETRY_TYPES
            and not _supports_phase1_clip_operation(updated)
        ):
            operation = updated.get("operation")
            raise ValueError(
                f"geometry.operation={operation!r} is not supported in Phase 1; only "
                "'union', 'intersection', and 'difference' ClipOperation wrappers are implemented"
            )
        return geometry_model_from_value(updated).model_dump(mode="python", exclude_none=True)
    if "center" in updated:
        updated["center"] = normalize_vec3(updated["center"], field_name="geometry.center")
    if geometry_type == "Box":
        if "size" not in updated:
            return updated
        size = normalize_vec3(updated["size"], field_name="geometry.size")
        if any(component < 0.0 for component in size):
            raise ValueError("geometry.size components must be non-negative")

exec
/bin/bash -lc "sed -n '180,240p' src/autofdtd/planning.py" in /workspace/autofdtd
 succeeded in 0ms:
        FeatureStatus.REJECT_CLEARLY,
        "Sampled custom dispersive media are out of Phase 1 scope.",
    ),
    FeatureEntry(
        "materials",
        "CustomDrude",
        FeatureStatus.REJECT_CLEARLY,
        "Sampled custom dispersive media are out of Phase 1 scope.",
    ),
    FeatureEntry(
        "materials",
        "CustomDebye",
        FeatureStatus.REJECT_CLEARLY,
        "Sampled custom dispersive media are out of Phase 1 scope.",
    ),
    FeatureEntry(
        "materials",
        "CustomMedia",
        FeatureStatus.REJECT_CLEARLY,
        "Out of Phase 1 scope.",
    ),
    # Boundaries
    FeatureEntry("boundaries", "Boundary", FeatureStatus.IMPLEMENT, "Per-axis boundary pair."),
    FeatureEntry("boundaries", "BoundarySpec", FeatureStatus.IMPLEMENT, "Core boundary container."),
    FeatureEntry("boundaries", "Periodic", FeatureStatus.IMPLEMENT, "Core boundary."),
    FeatureEntry("boundaries", "PECBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
    FeatureEntry("boundaries", "PMCBoundary", FeatureStatus.IMPLEMENT, "Conductor boundary."),
    FeatureEntry(
        "boundaries",
        "BlochBoundary",
        FeatureStatus.IMPLEMENT,
        "Phase-aware periodic boundary.",
    ),
    FeatureEntry("boundaries", "PML", FeatureStatus.IMPLEMENT, "Must-have absorber."),
    FeatureEntry(
        "boundaries",
        "StablePML",
        FeatureStatus.IMPLEMENT,
        "Supported subset acceptable.",
    ),
    FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
    # Sources
    FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
    FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
    FeatureEntry(
        "sources",
        "BroadbandPulse",
        FeatureStatus.IMPLEMENT,
        "Supported subset acceptable.",
    ),
    FeatureEntry(
        "sources",
        "UniformCurrentSource",
        FeatureStatus.IMPLEMENT,
        "Core current source.",
    ),
    FeatureEntry("sources", "PointDipole", FeatureStatus.IMPLEMENT, "Core localized source."),
    FeatureEntry(
        "sources",

exec
/bin/bash -lc "sed -n '1,180p' src/autofdtd/api/simulation.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API aliases for the foundational Phase 1 container models."""

from autofdtd.core.containers import (
    Scene,
    Simulation,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
)
from autofdtd.core.models import AutoFDTDModel, TaggedModel
from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
from autofdtd.boundaries import (
    Absorber,
    AbsorberParams,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PML,
    PMLParams,
    PECBoundary,
    PMCBoundary,
    Periodic,
    StablePML,
)
from autofdtd.geometry import (
    Box,
    ClipOperation,
    Cylinder,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    PolySlab,
    PrimitiveGeometry,
    Sphere,
    Transformed,
)
from autofdtd.grid import (
    AbstractSubpixelPolicy,
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    PolarizedAveraging,
    ResolvedGrid,
    ResolvedGridAxis,
    Staircasing,
    SubpixelSpec,
    UniformGrid,
)
from autofdtd.materials import (
    AnisotropicMedium,
    CustomAnisotropicMedium,
    CustomMedium,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PMCMedium,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    Sellmeier,
)

__all__ = [
    "AutoFDTDValidationWarning",
    "AutoFDTDModel",
    "AbstractSubpixelPolicy",
    "AutoGrid",
    "AnisotropicMedium",
    "Absorber",
    "AbsorberParams",
    "BlochBoundary",
    "Box",
    "Boundary",
    "BoundarySpec",
    "PML",
    "PMLParams",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",
    "Sellmeier",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "ResolvedGrid",
    "ResolvedGridAxis",
    "Scene",
    "Simulation",
    "Sphere",
    "Structure",
    "StructurePrecedence",
    "StructurePriorityMode",
    "Staircasing",
    "SubpixelSpec",
    "StablePML",
    "TaggedModel",
    "Transformed",
    "UniformGrid",
    "UnsupportedFeatureError",
]

exec
/bin/bash -lc 'ls -1 phase1/runs | tail -n 5 && pwd' in /workspace/autofdtd
 succeeded in 0ms:
task-019-attempt-001
task-020-attempt-001
task-021-attempt-001
task-022-attempt-001
task-023-attempt-001
/workspace/autofdtd

exec
/bin/bash -lc "find phase1/runs/task-023-attempt-001 -maxdepth 1 -type f -printf '%f
'" in /workspace/autofdtd
 succeeded in 0ms:
prompt.txt
codex.stdout.txt

exec
/bin/bash -lc "sed -n '520,760p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if y
                else Boundary.periodic()
            ),
            z=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if z
                else Boundary.periodic()
            ),
        )

    @classmethod
    def stable_pml(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 40,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.stable_pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.stable_pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if y
                else Boundary.periodic()
            ),
            z=(
                Boundary.stable_pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if z
                else Boundary.periodic()
            ),
        )

    @classmethod
    def absorber(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 40,
        parameters: AbsorberParams | None = None,
        extrude_structures: bool = False,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.absorber(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.absorber(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if y
                else Boundary.periodic()
            ),
            z=(
                Boundary.absorber(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if z
                else Boundary.periodic()
            ),
        )


def boundary_spec_model_from_value(value: object) -> BoundarySpec:
    """Normalize a supported BoundarySpec container."""

    if isinstance(value, BoundarySpec):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary-spec mapping, got {type(value)!r}")
    return BoundarySpec(**value)

exec
/bin/bash -lc "sed -n '1,120p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Versioned execution IR models and packaging helpers."""

from __future__ import annotations

import json
from collections.abc import Mapping
from hashlib import sha256
from pathlib import Path
from typing import Any, Literal

from pydantic import BaseModel, Field, field_validator

from autofdtd.boundaries import (
    Absorber,
    AbsorberParams,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PML,
    PMLParams,
    PECBoundary,
    PMCBoundary,
    Periodic,
    StablePML,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)
from autofdtd.core.containers import Scene, Simulation, Structure, StructurePriorityMode
from autofdtd.core.models import TaggedModel, json_ready
from autofdtd.geometry import (
    Box,
    ClipOperation,
    Cylinder,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    PolySlab,
    Sphere,
    Transformed,
    geometry_model_from_value,
)
from autofdtd.grid import (
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    PolarizedAveraging,
    ResolvedGrid,
    ResolvedGridAxis,
    Staircasing,
    UniformGrid,
    grid_model_from_value,
    subpixel_model_from_value,
)
from autofdtd.materials import (
    AnisotropicMedium,
    Debye,
    Drude,
    Lorentz,
    Medium,
    PECMedium,
    PMCMedium,
    PoleResidue,
    Sellmeier,
    medium_model_from_value,
)
from autofdtd.version import __version__

IR_SCHEMA_VERSION = "phase1.v1"
EXECUTION_PACKAGE_FORMAT = "autofdtd.execution-package.v1"


class IRModel(TaggedModel):
    """Frozen tagged base for inspectable execution-IR payloads."""

    schema_version: str = Field(default=IR_SCHEMA_VERSION)

    @field_validator("schema_version")
    @classmethod
    def _validate_schema_version(cls, value: str) -> str:
        if value != IR_SCHEMA_VERSION:
            raise ValueError(f"schema_version must be {IR_SCHEMA_VERSION!r}")
        return value


class ComponentFamily(str):
    """Execution-IR family namespace for tagged components."""

    GEOMETRY = "geometry"
    MEDIUM = "medium"
    SOURCE = "source"
    MONITOR = "monitor"
    BOUNDARY = "boundary"
    GRID = "grid"
    SUBPIXEL = "subpixel"


class ComponentIR(IRModel):
    """Generic tagged envelope for feature-family payloads."""

    type: Literal["ComponentIR"] = "ComponentIR"
    family: str
    component_type: str
    name: str | None = None
    payload: dict[str, Any] = Field(default_factory=dict)

    @field_validator("family", "component_type")
    @classmethod
    def _validate_non_empty(cls, value: str) -> str:
        stripped = value.strip()
        if not stripped:
            raise ValueError("component family and type must be non-empty")
        return stripped

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/boundaries/models.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,

exec
/bin/bash -lc "sed -n '1,260p' src/autofdtd/boundaries/models.py && printf '\\n---CUT---\\n' && sed -n '360,620p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary models for the foundational Phase 1 boundary surface."""

from __future__ import annotations

import cmath
import warnings
from collections.abc import Mapping
from typing import Any, Literal

from pydantic import Field, field_validator, model_validator

from autofdtd.core.models import TaggedModel
from autofdtd.geometry.primitives import Box


def _normalize_optional_name(value: str | None) -> str | None:
    if value is None:
        return None
    stripped = value.strip()
    if not stripped:
        raise ValueError("names must not be empty or whitespace-only")
    return stripped


class BoundaryEdge(TaggedModel):
    """Base model for one simulation-domain boundary edge."""

    name: str | None = None

    @field_validator("name")
    @classmethod
    def _validate_name(cls, value: str | None) -> str | None:
        return _normalize_optional_name(value)


class Periodic(BoundaryEdge):
    """Periodic boundary condition."""

    type: Literal["Periodic"] = "Periodic"


class BlochBoundary(BoundaryEdge):
    """Phase-aware periodic boundary condition."""

    type: Literal["BlochBoundary"] = "BlochBoundary"
    bloch_vec: float

    @property
    def bloch_phase(self) -> complex:
        """Return the forward phase factor for a positive-domain wrap."""

        return cmath.exp(1j * 2.0 * cmath.pi * self.bloch_vec)


class PECBoundary(BoundaryEdge):
    """Perfect electric conductor boundary condition."""

    type: Literal["PECBoundary"] = "PECBoundary"


class PMCBoundary(BoundaryEdge):
    """Perfect magnetic conductor boundary condition."""

    type: Literal["PMCBoundary"] = "PMCBoundary"


class ABCBoundary(BoundaryEdge):
    """First-order absorbing boundary condition with explicit effective medium inputs."""

    type: Literal["ABCBoundary"] = "ABCBoundary"
    permittivity: float | None = None
    conductivity: float | None = None

    @field_validator("permittivity")
    @classmethod
    def _validate_permittivity(cls, value: float | None) -> float | None:
        if value is None:
            return None
        normalized = float(value)
        if normalized < 1.0:
            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
        return normalized

    @field_validator("conductivity")
    @classmethod
    def _validate_conductivity(cls, value: float | None) -> float | None:
        if value is None:
            return None
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
        return normalized

    @model_validator(mode="after")
    def _validate_parameter_pair(self) -> ABCBoundary:
        if self.conductivity is not None and self.permittivity is None:
            raise ValueError(
                "ABCBoundary conductivity can only be provided together with permittivity"
            )
        return self


class BroadbandModeABCFitterParam(TaggedModel):
    """Deferred fit-parameter surface for broadband mode-ABC configuration."""

    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
    max_num_poles: int = 4
    tolerance_rms: float = 1.0e-4
    frequency_sampling_points: int = 11

    @field_validator("max_num_poles", "frequency_sampling_points")
    @classmethod
    def _validate_positive_int(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 1:
            raise ValueError("Broadband mode-ABC fit counts must be positive")
        return normalized

    @field_validator("tolerance_rms")
    @classmethod
    def _validate_non_negative_float(cls, value: float) -> float:
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
        return normalized


class BroadbandModeABCSpec(TaggedModel):
    """Deferred broadband fitting surface for mode-based absorbing boundaries."""

    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
    frequency_range: tuple[float, float]
    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)

    @field_validator("frequency_range", mode="before")
    @classmethod
    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
        if not isinstance(value, tuple | list) or len(value) != 2:
            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
        lower = float(value[0])
        upper = float(value[1])
        if lower <= 0.0 or upper <= 0.0:
            raise ValueError("frequency_range values must be positive")
        if upper <= lower:
            raise ValueError("frequency_range must be strictly increasing")
        return (lower, upper)


class ModeABCBoundary(BoundaryEdge):
    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""

    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
    mode_spec: dict[str, Any] = Field(
        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
    )
    mode_index: int = 0
    freq_spec: float | BroadbandModeABCSpec | None = None
    plane: Box

    @field_validator("mode_index")
    @classmethod
    def _validate_mode_index(cls, value: int) -> int:
        normalized = int(value)
        if normalized < 0:
            raise ValueError("ModeABCBoundary mode_index must be non-negative")
        return normalized

    @field_validator("freq_spec")
    @classmethod
    def _validate_freq_spec(
        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
    ) -> float | BroadbandModeABCSpec | None:
        if value is None or isinstance(value, BroadbandModeABCSpec):
            return value
        if isinstance(value, Mapping):
            return BroadbandModeABCSpec(**value)
        normalized = float(value)
        if normalized <= 0.0:
            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
        return normalized

    @field_validator("plane")
    @classmethod
    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
        plane = value if isinstance(value, Box) else Box(**value)
        if plane.size.count(0.0) != 1:
            raise ValueError(
                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
            )
        return plane

    @property
    def phase1_policy(self) -> str:
        return (
            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
            "Phase 1 until the mode solver and mode-source surfaces exist."
        )


class PMLParams(TaggedModel):
    """Polynomial profile parameters for the Phase 1 baseline PML."""

    type: Literal["PMLParams"] = "PMLParams"
    sigma_order: int = 3
    sigma_min: float = 0.0
    sigma_max: float = 1.5
    kappa_order: int = 3
    kappa_min: float = 1.0
    kappa_max: float = 3.0
    alpha_order: int = 1
    alpha_min: float = 0.0
    alpha_max: float = 0.0

    @field_validator("sigma_order", "kappa_order", "alpha_order")
    @classmethod
    def _validate_order(cls, value: int) -> int:
        if value < 0:
            raise ValueError("PML profile orders must be non-negative")
        return int(value)

    @field_validator(
        "sigma_min",
        "sigma_max",
        "kappa_min",
        "kappa_max",
        "alpha_min",
        "alpha_max",
    )
    @classmethod
    def _validate_non_negative(cls, value: float) -> float:
        normalized = float(value)
        if normalized < 0.0:
            raise ValueError("PML profile coefficients must be non-negative")
        return normalized

    @model_validator(mode="after")
    def _validate_bounds(self) -> PMLParams:
        if self.sigma_max < self.sigma_min:
            raise ValueError("PML sigma_max must be greater than or equal to sigma_min")
        if self.kappa_min < 1.0:
            raise ValueError("PML kappa_min must be at least 1.0")
        if self.kappa_max < self.kappa_min:
            raise ValueError("PML kappa_max must be greater than or equal to kappa_min")
        if self.alpha_max < self.alpha_min:
            raise ValueError("PML alpha_max must be greater than or equal to alpha_min")
        return self


class AbsorberParams(TaggedModel):
    """Polynomial conductivity profile parameters for adiabatic absorbers."""

    type: Literal["AbsorberParams"] = "AbsorberParams"
    sigma_order: int = 3
    sigma_min: float = 0.0
    sigma_max: float = 6.4

    @field_validator("sigma_order")
    @classmethod
    def _validate_order(cls, value: int) -> int:
        if value < 0:

---CUT---
        if normalized < 6:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "Absorber boundaries with fewer than 6 layers are prone to reflection and "
                "should only be used for small smoke tests.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
        return normalized


ImplementedBoundaryEdge = (
    Periodic
    | BlochBoundary
    | PECBoundary
    | PMCBoundary
    | ABCBoundary
    | ModeABCBoundary
    | PML
    | StablePML
    | Absorber
)


def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
    """Normalize a supported Phase 1 non-absorbing boundary edge."""

    if isinstance(
        value,
        (
            Periodic,
            BlochBoundary,
            PECBoundary,
            PMCBoundary,
            ABCBoundary,
            ModeABCBoundary,
            PML,
            StablePML,
            Absorber,
        ),
    ):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
    boundary_type = str(value.get("type"))
    if boundary_type == "Periodic":
        return Periodic(**value)
    if boundary_type == "BlochBoundary":
        return BlochBoundary(**value)
    if boundary_type == "PECBoundary":
        return PECBoundary(**value)
    if boundary_type == "PMCBoundary":
        return PMCBoundary(**value)
    if boundary_type == "ABCBoundary":
        return ABCBoundary(**value)
    if boundary_type == "ModeABCBoundary":
        return ModeABCBoundary(**value)
    if boundary_type == "PML":
        return PML(**value)
    if boundary_type == "StablePML":
        return StablePML(**value)
    if boundary_type == "Absorber":
        return Absorber(**value)
    raise TypeError(f"unsupported Phase 1 boundary edge type {boundary_type!r}")


class Boundary(TaggedModel):
    """Boundary pair across one simulation axis."""

    type: Literal["Boundary"] = "Boundary"
    plus: object = Field(default_factory=Periodic)
    minus: object = Field(default_factory=Periodic)

    @field_validator("plus", "minus")
    @classmethod
    def _validate_edge(cls, value: object) -> object:
        if isinstance(
            value,
            (
                Periodic,
                BlochBoundary,
                PECBoundary,
                PMCBoundary,
                ABCBoundary,
                ModeABCBoundary,
                PML,
                StablePML,
                Absorber,
            ),
        ):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {
                "Periodic",
                "BlochBoundary",
                "PECBoundary",
                "PMCBoundary",
                "ABCBoundary",
                "ModeABCBoundary",
                "PML",
                "StablePML",
                "Absorber",
            }:
                return boundary_edge_model_from_value(value)
        return value

    @model_validator(mode="after")
    def _validate_edge_composition(self) -> Boundary:
        plus = self.plus
        minus = self.minus

        num_bloch = isinstance(plus, BlochBoundary) + isinstance(minus, BlochBoundary)
        if num_bloch == 1:
            raise ValueError("Bloch boundaries must be applied on both sides of the same axis")
        if isinstance(plus, BlochBoundary) and isinstance(minus, BlochBoundary):
            if plus.bloch_vec != minus.bloch_vec:
                raise ValueError(
                    "Bloch boundaries on opposite sides of one axis must use the same bloch_vec"
                )
            return self

        changed = False

        if isinstance(minus, (PECBoundary, PMCBoundary)) and isinstance(plus, Periodic):
            plus = minus
            changed = True
        elif isinstance(plus, (PECBoundary, PMCBoundary)) and isinstance(minus, Periodic):
            minus = plus
            changed = True

        if changed:
            from autofdtd.core.validation import AutoFDTDValidationWarning

            warnings.warn(
                "A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor "
                "boundary so runtime halo handling stays reflection-only on that axis.",
                AutoFDTDValidationWarning,
                stacklevel=2,
            )
            object.__setattr__(self, "plus", plus)
            object.__setattr__(self, "minus", minus)

        return self

    def __getitem__(self, field_name: str) -> object:
        if field_name == "plus":
            return self.plus
        if field_name == "minus":
            return self.minus
        if field_name == "type":
            return self.type
        raise KeyError(field_name)

    @classmethod
    def periodic(cls) -> Boundary:
        return cls(plus=Periodic(), minus=Periodic())

    @classmethod
    def bloch(cls, bloch_vec: float) -> Boundary:
        return cls(
            plus=BlochBoundary(bloch_vec=bloch_vec),
            minus=BlochBoundary(bloch_vec=bloch_vec),
        )

    @classmethod
    def pec(cls) -> Boundary:
        return cls(plus=PECBoundary(), minus=PECBoundary())

    @classmethod
    def pmc(cls) -> Boundary:
        return cls(plus=PMCBoundary(), minus=PMCBoundary())

    @classmethod
    def abc(
        cls,
        *,
        permittivity: float | None = None,
        conductivity: float | None = None,
    ) -> Boundary:
        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
        return cls(plus=abc, minus=abc)

    @classmethod
    def mode_abc(
        cls,
        *,
        plane: Box,
        mode_spec: dict[str, Any] | None = None,
        mode_index: int = 0,
        freq_spec: float | BroadbandModeABCSpec | None = None,
    ) -> Boundary:
        boundary = ModeABCBoundary(
            plane=plane,
            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
            mode_index=mode_index,
            freq_spec=freq_spec,
        )
        return cls(plus=boundary, minus=boundary)

    @classmethod
    def pml(
        cls,
        *,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> Boundary:
        pml = PML(
            num_layers=num_layers,
            parameters=DEFAULT_PML_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=pml, minus=pml)

    @classmethod
    def stable_pml(
        cls,
        *,
        num_layers: int = 40,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> Boundary:
        stable = StablePML(
            num_layers=num_layers,
            parameters=DEFAULT_STABLE_PML_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=stable, minus=stable)

    @classmethod
    def absorber(
        cls,
        *,
        num_layers: int = 40,
        parameters: AbsorberParams | None = None,
        extrude_structures: bool = False,
    ) -> Boundary:
        absorber = Absorber(
            num_layers=num_layers,
            parameters=DEFAULT_ABSORBER_PARAMS if parameters is None else parameters,
            extrude_structures=extrude_structures,
        )
        return cls(plus=absorber, minus=absorber)


def boundary_model_from_value(value: object) -> Boundary:
    """Normalize a supported axis boundary pair."""

    if isinstance(value, Boundary):
        return value
    if not isinstance(value, Mapping):
        raise TypeError(f"expected a boundary mapping, got {type(value)!r}")
    if "plus" not in value and "minus" not in value:
        edge = boundary_edge_model_from_value(value)
        return Boundary(plus=edge, minus=edge)
    return Boundary(**value)


class BoundarySpec(TaggedModel):

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,

exec
/bin/bash -lc "sed -n '620,780p' src/autofdtd/boundaries/models.py" in /workspace/autofdtd
 succeeded in 0ms:
class BoundarySpec(TaggedModel):
    """Boundary conditions across the x, y, and z simulation axes."""

    type: Literal["BoundarySpec"] = "BoundarySpec"
    x: Boundary = Field(default_factory=Boundary.periodic)
    y: Boundary = Field(default_factory=Boundary.periodic)
    z: Boundary = Field(default_factory=Boundary.periodic)

    @field_validator("x", "y", "z", mode="before")
    @classmethod
    def _coerce_axis_boundary(cls, value: object) -> object:
        if isinstance(
            value,
            (
                Boundary,
                Periodic,
                BlochBoundary,
                PECBoundary,
                PMCBoundary,
                ABCBoundary,
                ModeABCBoundary,
                PML,
                StablePML,
                Absorber,
            ),
        ):
            return value
        if isinstance(value, Mapping):
            boundary_type = str(value.get("type"))
            if boundary_type in {
                "Boundary",
                "Periodic",
                "BlochBoundary",
                "PECBoundary",
                "PMCBoundary",
                "ABCBoundary",
                "ModeABCBoundary",
                "PML",
                "StablePML",
                "Absorber",
            }:
                return boundary_model_from_value(value)
        return value

    def __getitem__(self, field_name: str) -> Boundary:
        if field_name == "x":
            return self.x
        if field_name == "y":
            return self.y
        if field_name == "z":
            return self.z
        raise KeyError(field_name)

    def as_tuple(self) -> tuple[Boundary, Boundary, Boundary]:
        return (self.x, self.y, self.z)

    @classmethod
    def all_sides(cls, boundary: ImplementedBoundaryEdge) -> BoundarySpec:
        return cls(
            x=Boundary(plus=boundary, minus=boundary),
            y=Boundary(plus=boundary, minus=boundary),
            z=Boundary(plus=boundary, minus=boundary),
        )

    @classmethod
    def pec(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pec() if x else Boundary.periodic(),
            y=Boundary.pec() if y else Boundary.periodic(),
            z=Boundary.pec() if z else Boundary.periodic(),
        )

    @classmethod
    def pmc(cls, *, x: bool = False, y: bool = False, z: bool = False) -> BoundarySpec:
        return cls(
            x=Boundary.pmc() if x else Boundary.periodic(),
            y=Boundary.pmc() if y else Boundary.periodic(),
            z=Boundary.pmc() if z else Boundary.periodic(),
        )

    @classmethod
    def periodic(
        cls, *, x: bool = True, y: bool = True, z: bool = True
    ) -> BoundarySpec:
        return cls(
            x=Boundary.periodic() if x else Boundary.periodic(),
            y=Boundary.periodic() if y else Boundary.periodic(),
            z=Boundary.periodic() if z else Boundary.periodic(),
        )

    @classmethod
    def pml(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> BoundarySpec:
        return cls(
            x=(Boundary.abc() if x else Boundary.periodic()),
            y=(Boundary.abc() if y else Boundary.periodic()),
            z=(Boundary.abc() if z else Boundary.periodic()),
        )

    @classmethod
    def abc(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        permittivity: float | None = None,
        conductivity: float | None = None,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
                if y
                else Boundary.periodic()
            ),
            z=(
                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
                if z
                else Boundary.periodic()
            ),
        )

    @classmethod
    def pml(
        cls,
        *,
        x: bool = False,
        y: bool = False,
        z: bool = False,
        num_layers: int = 12,
        parameters: PMLParams | None = None,
        extrude_structures: bool = True,
    ) -> BoundarySpec:
        return cls(
            x=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,
                )
                if x
                else Boundary.periodic()
            ),
            y=(
                Boundary.pml(
                    num_layers=num_layers,
                    parameters=parameters,
                    extrude_structures=extrude_structures,

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/boundaries.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/kernels/boundaries.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/runtime/boundaries.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/kernels/__init__.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

exec
/bin/bash -lc "sed -n '1,200p' src/autofdtd/runtime/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Chunk runtime, scheduling, and execution-context namespace."""

from autofdtd.runtime.boundaries import (
    BoundaryRuntime,
    allocate_pml_boundary_state,
    apply_compiled_boundaries,
    build_boundary_runtime,
)

__all__ = [
    "BoundaryRuntime",
    "allocate_pml_boundary_state",
    "apply_compiled_boundaries",
    "build_boundary_runtime",
]

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/runtime/__init__.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/__init__.py
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/boundaries/__init__.py
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/api/simulation.py
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/api/__init__.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/core/validation.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/planning.py
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/phase1/feature-checklist.md
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

2026-04-06T19:59:23.540392Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /workspace/autofdtd/src/autofdtd/ir/models.py:
    reflective_axes = tuple(
        axis.axis
        for axis in compiled.axes()
        if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
    )
    return BoundarySpecIR(
        x=boundary_to_ir(boundary_spec.x),
        y=boundary_to_ir(boundary_spec.y),
        z=boundary_to_ir(boundary_spec.z),
        requires_complex_fields=compiled.requires_complex_fields,
        periodic_axes=compiled.periodic_axes,
        bloch_axes=compiled.bloch_axes,
        reflective_axes=reflective_axes,
        pml_axes=compiled.pml_axes,
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

exec
/bin/bash -lc "sed -n '380,540p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
    component_type: Literal["Periodic"] = "Periodic"
    name: str | None = None


class BlochBoundaryIR(IRModel):
    """Typed execution IR for a Bloch boundary edge."""

    type: Literal["BlochBoundaryIR"] = "BlochBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["BlochBoundary"] = "BlochBoundary"
    name: str | None = None
    bloch_vec: float
    phase_real: float
    phase_imag: float


class PECBoundaryIR(IRModel):
    """Typed execution IR for a PEC boundary edge."""

    type: Literal["PECBoundaryIR"] = "PECBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PECBoundary"] = "PECBoundary"
    name: str | None = None


class PMCBoundaryIR(IRModel):
    """Typed execution IR for a PMC boundary edge."""

    type: Literal["PMCBoundaryIR"] = "PMCBoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PMCBoundary"] = "PMCBoundary"
    name: str | None = None


class PMLParamsIR(IRModel):
    """Typed execution IR for baseline PML profile parameters."""

    type: Literal["PMLParamsIR"] = "PMLParamsIR"
    sigma_order: int
    sigma_min: float
    sigma_max: float
    kappa_order: int
    kappa_min: float
    kappa_max: float
    alpha_order: int
    alpha_min: float
    alpha_max: float


class PMLIR(IRModel):
    """Typed execution IR for a baseline PML boundary edge."""

    type: Literal["PMLIR"] = "PMLIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["PML"] = "PML"
    name: str | None = None
    num_layers: int
    parameters: PMLParamsIR
    extrude_structures: bool = True


class StablePMLIR(IRModel):
    """Typed execution IR for a stable-PML boundary edge."""

    type: Literal["StablePMLIR"] = "StablePMLIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["StablePML"] = "StablePML"
    name: str | None = None
    num_layers: int
    parameters: PMLParamsIR
    extrude_structures: bool = True


class AbsorberParamsIR(IRModel):
    """Typed execution IR for adiabatic absorber parameters."""

    type: Literal["AbsorberParamsIR"] = "AbsorberParamsIR"
    sigma_order: int
    sigma_min: float
    sigma_max: float


class AbsorberIR(IRModel):
    """Typed execution IR for an adiabatic absorber edge."""

    type: Literal["AbsorberIR"] = "AbsorberIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Absorber"] = "Absorber"
    name: str | None = None
    num_layers: int
    parameters: AbsorberParamsIR
    extrude_structures: bool = False


class SymmetryAxisIR(IRModel):
    """Typed execution IR for one mirror-symmetry axis."""

    type: Literal["SymmetryAxisIR"] = "SymmetryAxisIR"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]


BoundaryEdgeIR = (
    PeriodicIR
    | BlochBoundaryIR
    | PECBoundaryIR
    | PMCBoundaryIR
    | PMLIR
    | StablePMLIR
    | AbsorberIR
    | ComponentIR
)


class BoundaryIR(IRModel):
    """Typed execution IR for one axis boundary pair."""

    type: Literal["BoundaryIR"] = "BoundaryIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["Boundary"] = "Boundary"
    plus: BoundaryEdgeIR
    minus: BoundaryEdgeIR


class BoundarySpecIR(IRModel):
    """Typed execution IR for the Phase 1 boundary container."""

    type: Literal["BoundarySpecIR"] = "BoundarySpecIR"
    family: Literal["boundary"] = "boundary"
    component_type: Literal["BoundarySpec"] = "BoundarySpec"
    x: BoundaryIR
    y: BoundaryIR
    z: BoundaryIR
    requires_complex_fields: bool = False
    periodic_axes: tuple[str, ...] = ()
    bloch_axes: tuple[str, ...] = ()
    reflective_axes: tuple[str, ...] = ()
    pml_axes: tuple[str, ...] = ()
    pml_faces: tuple[str, ...] = ()
    stable_pml_axes: tuple[str, ...] = ()
    stable_pml_faces: tuple[str, ...] = ()
    absorber_axes: tuple[str, ...] = ()
    absorber_faces: tuple[str, ...] = ()
    active_symmetry_axes: tuple[str, ...] = ()
    symmetry_axes: tuple[SymmetryAxisIR, ...] = ()


class MediumIR(IRModel):
    """Typed execution IR for a homogeneous isotropic dielectric or conductor."""

    type: Literal["MediumIR"] = "MediumIR"
    family: Literal["medium"] = "medium"
    component_type: Literal["Medium"] = "Medium"
    name: str | None = None
    permittivity: float
    conductivity: float
    permeability: float
    magnetic_conductivity: float


exec
/bin/bash -lc "sed -n '1000,1245p' src/autofdtd/ir/models.py" in /workspace/autofdtd
 succeeded in 0ms:
            zz=medium_to_ir(medium.zz),
        )
    return component_to_ir(value, family=ComponentFamily.MEDIUM)


def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
    """Lower a supported boundary edge into typed execution IR."""

    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
        edge = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
        "StablePML",
        "Absorber",
    }:
        edge = boundary_edge_model_from_value(value)

    if isinstance(edge, Periodic):
        return PeriodicIR(name=edge.name)
    if isinstance(edge, BlochBoundary):
        return BlochBoundaryIR(
            name=edge.name,
            bloch_vec=edge.bloch_vec,
            phase_real=float(edge.bloch_phase.real),
            phase_imag=float(edge.bloch_phase.imag),
        )
    if isinstance(edge, PECBoundary):
        return PECBoundaryIR(name=edge.name)
    if isinstance(edge, PMCBoundary):
        return PMCBoundaryIR(name=edge.name)
    if isinstance(edge, PML):
        params = (
            edge.parameters
            if isinstance(edge.parameters, PMLParams)
            else PMLParams.model_validate(edge.parameters)
        )
        return PMLIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=PMLParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
                kappa_order=params.kappa_order,
                kappa_min=params.kappa_min,
                kappa_max=params.kappa_max,
                alpha_order=params.alpha_order,
                alpha_min=params.alpha_min,
                alpha_max=params.alpha_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    if isinstance(edge, StablePML):
        params = (
            edge.parameters
            if isinstance(edge.parameters, PMLParams)
            else PMLParams.model_validate(edge.parameters)
        )
        return StablePMLIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=PMLParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
                kappa_order=params.kappa_order,
                kappa_min=params.kappa_min,
                kappa_max=params.kappa_max,
                alpha_order=params.alpha_order,
                alpha_min=params.alpha_min,
                alpha_max=params.alpha_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    if isinstance(edge, Absorber):
        params = (
            edge.parameters
            if isinstance(edge.parameters, AbsorberParams)
            else AbsorberParams.model_validate(edge.parameters)
        )
        return AbsorberIR(
            name=edge.name,
            num_layers=edge.num_layers,
            parameters=AbsorberParamsIR(
                sigma_order=params.sigma_order,
                sigma_min=params.sigma_min,
                sigma_max=params.sigma_max,
            ),
            extrude_structures=edge.extrude_structures,
        )
    return component_to_ir(value, family=ComponentFamily.BOUNDARY)


def boundary_to_ir(value: object) -> BoundaryIR | ComponentIR:
    """Lower a supported axis boundary pair into typed execution IR."""

    boundary: Boundary | None = None
    if isinstance(value, Boundary):
        boundary = value
    elif isinstance(value, Mapping) and str(value.get("type")) in {
        "Boundary",
        "Periodic",
        "BlochBoundary",
        "PECBoundary",
        "PMCBoundary",
        "PML",
        "StablePML",
        "Absorber",
    }:
        boundary = boundary_model_from_value(value)
    if boundary is None:
        return component_to_ir(value, family=ComponentFamily.BOUNDARY)
    return BoundaryIR(
        plus=boundary_edge_to_ir(boundary.plus),
        minus=boundary_edge_to_ir(boundary.minus),
    )


def _reflection_signs_for_axis(
    symmetry: int,
    axis_name: Literal["x", "y", "z"],
) -> tuple[tuple[int, int, int], tuple[int, int, int]]:
    axis_index = "xyz".index(axis_name)
    spatial_reflection = [1, 1, 1]
    pseudo_reflection = [-1, -1, -1]
    spatial_reflection[axis_index] = -1
    pseudo_reflection[axis_index] = 1
    electric = tuple(sign * symmetry for sign in spatial_reflection)
    magnetic = tuple(sign * symmetry for sign in pseudo_reflection)
    return electric, magnetic


def _symmetry_axes_to_ir(symmetry: tuple[int, int, int]) -> tuple[SymmetryAxisIR, ...]:
    axes: list[SymmetryAxisIR] = []
    for axis_name, axis_value in zip("xyz", symmetry, strict=True):
        if axis_value == 0:
            continue
        electric_signs, magnetic_signs = _reflection_signs_for_axis(axis_value, axis_name)
        axes.append(
            SymmetryAxisIR(
                axis=axis_name,
                symmetry=axis_value,
                electric_signs=electric_signs,
                magnetic_signs=magnetic_signs,
            )
        )
    return tuple(axes)


def boundary_spec_to_ir(
    value: object,
    *,
    symmetry: tuple[int, int, int] = (0, 0, 0),
) -> BoundarySpecIR | ComponentIR:
    """Lower a supported BoundarySpec container into typed execution IR."""

    boundary_spec: BoundarySpec | None = None
    if isinstance(value, BoundarySpec):
        boundary_spec = value
    elif isinstance(value, Mapping) and str(value.get("type")) == "BoundarySpec":
        try:
            boundary_spec = boundary_spec_model_from_value(value)
        except Exception:  # noqa: BLE001
            boundary_spec = None
    if boundary_spec is None:
        return component_to_ir(value, family=ComponentFamily.BOUNDARY)

    periodic_axes: list[str] = []
    bloch_axes: list[str] = []
    reflective_axes: list[str] = []
    pml_axes: list[str] = []
    pml_faces: list[str] = []
    stable_pml_axes: list[str] = []
    stable_pml_faces: list[str] = []
    absorber_axes: list[str] = []
    absorber_faces: list[str] = []
    requires_complex_fields = False
    axes = {}
    for axis_name, axis_boundary in zip("xyz", boundary_spec.as_tuple(), strict=True):
        lowered = boundary_to_ir(axis_boundary)
        if not isinstance(lowered, BoundaryIR):
            return component_to_ir(value, family=ComponentFamily.BOUNDARY)
        axes[axis_name] = lowered
        edge_types = {lowered.minus.component_type, lowered.plus.component_type}
        if edge_types == {"Periodic"}:
            periodic_axes.append(axis_name)
        elif edge_types == {"BlochBoundary"}:
            bloch_axes.append(axis_name)
            requires_complex_fields = True
        elif edge_types <= {"PML", "StablePML", "Absorber"}:
            pml_axes.append(axis_name)
            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                pml_faces.append(f"{axis_name}.minus")
            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                pml_faces.append(f"{axis_name}.plus")
            reflective_axes.append(axis_name)
        else:
            if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
                pml_faces.append(f"{axis_name}.minus")
            if lowered.plus.component_type in {"PML", "StablePML", "Absorber"}:
                pml_faces.append(f"{axis_name}.plus")
            if (
                lowered.minus.component_type in {"PML", "StablePML", "Absorber"}
                or lowered.plus.component_type in {"PML", "StablePML", "Absorber"}
            ) and axis_name not in pml_axes:
                pml_axes.append(axis_name)
            reflective_axes.append(axis_name)
        if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
            stable_pml_axes.append(axis_name)
        if lowered.minus.component_type == "StablePML":
            stable_pml_faces.append(f"{axis_name}.minus")
        if lowered.plus.component_type == "StablePML":
            stable_pml_faces.append(f"{axis_name}.plus")
        if lowered.minus.component_type == "Absorber" or lowered.plus.component_type == "Absorber":
            absorber_axes.append(axis_name)
        if lowered.minus.component_type == "Absorber":
            absorber_faces.append(f"{axis_name}.minus")
        if lowered.plus.component_type == "Absorber":
            absorber_faces.append(f"{axis_name}.plus")
    symmetry_axes = _symmetry_axes_to_ir(symmetry)
    return BoundarySpecIR(
        x=axes["x"],
        y=axes["y"],
        z=axes["z"],
        requires_complex_fields=requires_complex_fields,
        periodic_axes=tuple(periodic_axes),
        bloch_axes=tuple(bloch_axes),
        reflective_axes=tuple(reflective_axes),
        pml_axes=tuple(pml_axes),
        pml_faces=tuple(pml_faces),
        stable_pml_axes=tuple(stable_pml_axes),
        stable_pml_faces=tuple(stable_pml_faces),
        absorber_axes=tuple(absorber_axes),
        absorber_faces=tuple(absorber_faces),
        active_symmetry_axes=tuple(axis.axis for axis in symmetry_axes),
        symmetry_axes=symmetry_axes,
    )


def _resolved_grid_axis_to_ir(value: ResolvedGridAxis) -> ResolvedGridAxisIR:
    return ResolvedGridAxisIR(

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/models.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..ae00bdad4495ff8c297e9e60a85b86a2284a896f
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..ae00bdad4495ff8c297e9e60a85b86a2284a896f
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/models.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..4064cb7447bb5a640eb461feef416dd0c73d1ef3
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..4064cb7447bb5a640eb461feef416dd0c73d1ef3
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/models.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..16d6e7e0685ea72552b817da568b8a12f55f0a68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..16d6e7e0685ea72552b817da568b8a12f55f0a68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/models.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..47f886f4ff0d4378ee9fa6f6615b24b65cf2e79c
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -505,6 +719,51 @@
         extrude_structures: bool = True,
     ) -> BoundarySpec:
         return cls(
+            x=(Boundary.abc() if x else Boundary.periodic()),
+            y=(Boundary.abc() if y else Boundary.periodic()),
+            z=(Boundary.abc() if z else Boundary.periodic()),
+        )
+
+    @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def pml(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        num_layers: int = 12,
+        parameters: PMLParams | None = None,
+        extrude_structures: bool = True,
+    ) -> BoundarySpec:
+        return cls(
             x=(
                 Boundary.pml(
                     num_layers=num_layers,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/boundaries/models.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

2026-04-06T20:01:14.500611Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /workspace/autofdtd/tests/test_boundaries.py:
    assert runtime.compiled.active_symmetry_axes == ("x", "z")


def test_pml_public_api_and_ir_lowering_are_typed() -> None:
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

exec
/bin/bash -lc "sed -n '1,140p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
from __future__ import annotations

import numpy as np
import pytest

from autofdtd.api import (
    Absorber,
    AbsorberParams,
    BlochBoundary,
    Boundary,
    BoundarySpec,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    Simulation,
    StablePML,
)
from autofdtd.compiler import BoundaryMode, compile_boundary_spec
from autofdtd.core.validation import AutoFDTDValidationWarning
from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
from autofdtd.kernels import (
    apply_boundary_ghosts,
    apply_boundary_stages,
    boundary_edge_transform,
    boundary_kernel_metadata,
    symmetry_transform,
)
from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime


def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
    with pytest.warns(
        AutoFDTDValidationWarning,
        match="periodic boundary opposite a PEC/PMC boundary",
    ):
        boundary = Boundary(minus=PECBoundary(), plus=Periodic())

    assert boundary.minus.type == "PECBoundary"
    assert boundary.plus.type == "PECBoundary"


def test_simulation_normalizes_supported_boundary_subset() -> None:
    simulation = Simulation(
        size=(1.0, 1.0, 1.0),
        run_time=1.0,
        boundary_spec={
            "type": "BoundarySpec",
            "x": {"type": "Periodic"},
            "y": {
                "type": "Boundary",
                "minus": {"type": "PECBoundary"},
                "plus": {"type": "Periodic"},
            },
            "z": {"type": "PMCBoundary"},
        },
    )

    assert simulation.boundary_spec["x"]["minus"]["type"] == "Periodic"
    assert simulation.boundary_spec["x"]["plus"]["type"] == "Periodic"
    assert simulation.boundary_spec["y"]["minus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["y"]["plus"]["type"] == "PECBoundary"
    assert simulation.boundary_spec["z"]["minus"]["type"] == "PMCBoundary"
    assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"


def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.bloch(0.25),
            y=Boundary.stable_pml(num_layers=8),
            z=Boundary(minus=Absorber(num_layers=6), plus=PML(num_layers=4)),
        ),
        symmetry=(1, 0, -1),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.25),
    )

    assert compiled.x.exchange_kind == "bloch"
    assert compiled.x.minus.mode is BoundaryMode.BLOCH
    assert compiled.x.plus.phase_factor == pytest.approx(1j)
    assert compiled.x.minus.phase_factor == pytest.approx(-1j)
    assert compiled.y.minus.mode is BoundaryMode.STABLE_PML
    assert compiled.z.minus.mode is BoundaryMode.ABSORBER
    assert compiled.z.plus.mode is BoundaryMode.PML
    assert compiled.bloch_axes == ("x",)
    assert compiled.reflective_axes == ("y", "z")
    assert compiled.pml_axes == ("y", "z")
    assert compiled.pml_faces == ("y.minus", "y.plus", "z.minus", "z.plus")
    assert compiled.stable_pml_axes == ("y",)
    assert compiled.stable_pml_faces == ("y.minus", "y.plus")
    assert compiled.absorber_axes == ("z",)
    assert compiled.absorber_faces == ("z.minus",)
    assert compiled.z.plus.pml is not None
    assert compiled.z.plus.pml.num_layers == 4
    assert compiled.z.minus.pml.boundary_type == "Absorber"
    assert compiled.z.minus.pml.terminal_reflection == "pec"
    assert compiled.y.plus.pml.boundary_type == "StablePML"
    assert compiled.z.plus.pml.grid_spacing == pytest.approx(0.25)
    assert compiled.stage_order == (
        "electric_boundary",
        "magnetic_boundary",
        "electric_pml",
        "magnetic_pml",
    )
    assert compiled.requires_complex_fields is True
    assert compiled.active_symmetry_axes == ("x", "z")
    assert compiled.symmetry_axes[0].electric_signs == (-1, 1, 1)
    assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)


def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    )
    electric = np.zeros((4, 4, 4, 3), dtype=np.complex128)
    magnetic = np.zeros((4, 4, 4, 3), dtype=np.complex128)

    electric[1, 1, 1] = (1.0, 2.0, 3.0)
    electric[-2, 1, 1] = (4.0, 5.0, 6.0)
    electric[2, 1, 2] = (7.0, 8.0, 9.0)
    electric[1, -2, 2] = (11.0, 12.0, 13.0)
    electric[2, 2, 1] = (14.0, 15.0, 16.0)
    electric[1, 2, -2] = (17.0, 18.0, 19.0)

    magnetic[1, 1, 1] = (21.0, 22.0, 23.0)
    magnetic[-2, 1, 1] = (24.0, 25.0, 26.0)
    magnetic[2, 1, 2] = (27.0, 28.0, 29.0)
    magnetic[2, -2, 2] = (31.0, 32.0, 33.0)
    magnetic[2, 2, 1] = (34.0, 35.0, 36.0)
    magnetic[2, 2, -2] = (37.0, 38.0, 39.0)

    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)

    assert tuple(updated_electric[0, 1, 1]) == pytest.approx((-4.0j, -5.0j, -6.0j))
    assert tuple(updated_electric[-1, 1, 1]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(updated_magnetic[0, 1, 1]) == pytest.approx((-24.0j, -25.0j, -26.0j))
    assert tuple(updated_electric[2, 0, 2]) == pytest.approx((-7.0, 8.0, -9.0))
    assert tuple(updated_magnetic[2, 0, 2]) == pytest.approx((27.0, -28.0, 29.0))

exec
/bin/bash -lc "sed -n '140,260p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
    assert tuple(updated_magnetic[2, 0, 2]) == pytest.approx((27.0, -28.0, 29.0))
    assert tuple(updated_electric[2, 2, 0]) == pytest.approx((14.0, 15.0, -16.0))
    assert tuple(updated_magnetic[2, 2, -1]) == pytest.approx((-37.0, -38.0, 39.0))


def test_boundary_runtime_wrapper_and_ir_lowering_expose_typed_metadata() -> None:
    boundary_spec = BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
    runtime = build_boundary_runtime(boundary_spec, symmetry=(1, 0, -1))
    metadata = boundary_kernel_metadata()
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=boundary_spec,
            symmetry=(1, 0, -1),
        )
    )

    assert runtime.compiled.bloch_axes == ("x",)
    assert metadata["supported_modes"] == (
        "periodic",
        "bloch",
        "pec",
        "pmc",
        "pml",
        "stable_pml",
        "absorber",
    )
    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert simulation_ir.boundary_spec.reflective_axes == ("y", "z")
    assert simulation_ir.boundary_spec.requires_complex_fields is True
    assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")


def test_pml_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.pml(
                    num_layers=8,
                    parameters=PMLParams(
                        sigma_order=2,
                        sigma_max=2.0,
                        kappa_max=4.0,
                        alpha_max=0.1,
                    ),
                )
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, PMLIR)
    assert simulation_ir.boundary_spec.x.minus.num_layers == 8
    assert simulation_ir.boundary_spec.x.minus.parameters.kappa_max == pytest.approx(4.0)
    assert simulation_ir.boundary_spec.pml_axes == ("x",)
    assert simulation_ir.boundary_spec.pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.reflective_axes == ("x",)


def test_stable_pml_and_absorber_public_api_and_ir_lowering_are_typed() -> None:
    simulation_ir = simulation_to_ir(
        Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.stable_pml(
                    num_layers=10,
                    parameters=PMLParams(sigma_max=1.2, kappa_max=4.5, alpha_max=0.8),
                ),
                z=Boundary.absorber(
                    num_layers=12,
                    parameters=AbsorberParams(sigma_order=2, sigma_max=7.5),
                ),
            ),
        )
    )

    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
    assert isinstance(simulation_ir.boundary_spec.x.minus, StablePMLIR)
    assert isinstance(simulation_ir.boundary_spec.z.minus, AbsorberIR)
    assert simulation_ir.boundary_spec.stable_pml_axes == ("x",)
    assert simulation_ir.boundary_spec.stable_pml_faces == ("x.minus", "x.plus")
    assert simulation_ir.boundary_spec.absorber_axes == ("z",)
    assert simulation_ir.boundary_spec.absorber_faces == ("z.minus", "z.plus")
    assert simulation_ir.boundary_spec.x.minus.parameters.alpha_max == pytest.approx(0.8)
    assert simulation_ir.boundary_spec.z.minus.parameters.sigma_max == pytest.approx(7.5)


def test_boundary_runtime_exposes_phase_and_symmetry_transforms() -> None:
    runtime = build_boundary_runtime(BoundarySpec(x=Boundary.bloch(0.25)), symmetry=(1, 0, 0))
    vector = np.asarray([[1.0 + 0.0j, 2.0 + 0.0j, 3.0 + 0.0j]], dtype=np.complex128)

    boundary_values = runtime.transform_boundary_values(
        vector,
        runtime.compiled.x.plus,
        field_family="electric",
    )
    mirrored = runtime.transform_symmetry_values(
        vector,
        runtime.compiled.symmetry_axes[0],
        field_family="magnetic",
    )

    assert tuple(boundary_values[0]) == pytest.approx((1.0j, 2.0j, 3.0j))
    assert tuple(mirrored[0]) == pytest.approx((1.0, -2.0, -3.0))
    assert tuple(
        boundary_edge_transform(vector, runtime.compiled.x.minus, field_family="electric")[0]
    ) == pytest.approx((-1.0j, -2.0j, -3.0j))
    assert tuple(
        symmetry_transform(
            vector,
            runtime.compiled.symmetry_axes[0],
            field_family="electric",
        )[0]
    ) == pytest.approx((-1.0, 2.0, 3.0))


def test_pml_state_allocation_tracks_active_faces() -> None:

exec
/bin/bash -lc "sed -n '260,420p' tests/test_boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
def test_pml_state_allocation_tracks_active_faces() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.pml(num_layers=3),
            y=Boundary.absorber(num_layers=5),
            z=Boundary(minus=StablePML(num_layers=2), plus=Periodic()),
        )
    )
    state = allocate_pml_boundary_state(compiled, (12, 8, 6, 3), dtype=np.float64)

    assert set(state.electric) == {"x.minus", "x.plus", "y.minus", "y.plus", "z.minus"}
    assert state.electric["x.minus"].memory_shape == (3, 8, 6, 3)
    assert state.electric["y.minus"].memory_shape == (12, 5, 6, 3)
    assert state.electric["z.minus"].memory_shape == (12, 8, 2, 3)
    assert np.all(state.magnetic["x.plus"].memory == 0.0)


def test_pml_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        grid_spacing=(0.25, 0.5, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 4, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 4, 4, 3), dtype=np.float64)
    electric[1:5, 1:3, 1:3, 0] = 1.0
    magnetic[-5:-1, 1:3, 1:3, 1] = 1.0

    before_left = float(np.sum(electric[1:5, ...] ** 2))
    before_center = float(np.sum(electric[5:7, ...] ** 2))
    for _ in range(12):
        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
    after_left = float(np.sum(electric[1:5, ...] ** 2))
    after_right_h = float(np.sum(magnetic[-5:-1, ...] ** 2))

    assert after_left < before_left * 0.2
    assert after_right_h < 0.2 * 16.0
    assert before_center == pytest.approx(0.0)
    assert float(np.sum(electric[5:7, ...] ** 2)) == pytest.approx(0.0)
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0


def test_stable_pml_and_absorber_boundary_stages_absorb_boundary_energy() -> None:
    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.stable_pml(num_layers=6),
            y=Boundary.absorber(
                num_layers=6,
                parameters=AbsorberParams(sigma_max=8.0),
            ),
            z=Boundary.periodic(),
        ),
        dt=0.5,
        grid_spacing=(0.25, 0.25, 0.5),
    )
    state = allocate_pml_boundary_state(compiled, (12, 12, 4, 3), dtype=np.float64)
    electric = np.zeros((12, 12, 4, 3), dtype=np.float64)
    magnetic = np.zeros((12, 12, 4, 3), dtype=np.float64)
    electric[1:7, 1:7, 1:3, 0] = 1.0
    magnetic[1:7, 1:7, 1:3, 1] = 1.0

    before_x = float(np.sum(electric[1:7, :, :, :] ** 2))
    before_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))
    for _ in range(10):
        electric, magnetic = apply_boundary_stages(electric, magnetic, compiled, state)
    after_x = float(np.sum(electric[1:7, :, :, :] ** 2))
    after_y = float(np.sum(magnetic[:, 1:7, :, :] ** 2))

    assert after_x < before_x * 0.3
    assert after_y < before_y * 0.3
    assert np.linalg.norm(state.electric["x.minus"].memory) > 0.0
    assert np.linalg.norm(state.electric["y.minus"].memory) == pytest.approx(0.0)


def test_absorber_ghost_updates_use_reflective_outer_wall() -> None:
    compiled = compile_boundary_spec(BoundarySpec(y=Boundary.absorber(num_layers=4)))
    electric = np.zeros((4, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((4, 4, 4, 3), dtype=np.float64)

    electric[1, 1, 1] = (1.0, 2.0, 3.0)
    magnetic[1, 1, 1] = (4.0, 5.0, 6.0)

    updated_electric, updated_magnetic = apply_boundary_ghosts(electric, magnetic, compiled)

    assert tuple(updated_electric[1, 0, 1]) == pytest.approx((-1.0, 2.0, -3.0))
    assert tuple(updated_magnetic[1, 0, 1]) == pytest.approx((4.0, -5.0, 6.0))


def test_boundary_rejects_single_sided_bloch_and_normalizes_public_api() -> None:
    boundary = Boundary.bloch(0.125)
    assert isinstance(boundary.plus, BlochBoundary)
    assert boundary.plus.bloch_phase == pytest.approx(
        np.exp(1j * 2.0 * np.pi * 0.125),
    )

    with pytest.raises(ValueError, match="Bloch boundaries must be applied on both sides"):
        Boundary(minus=BlochBoundary(bloch_vec=0.1), plus=Periodic())

    assert isinstance(Boundary.stable_pml().minus, StablePML)
    assert isinstance(Boundary.absorber().plus, Absorber)

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..b7f52fd03452c1408b502f1805955487b8864c2c
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..b7f52fd03452c1408b502f1805955487b8864c2c
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..cf78299353ec08bcf55d4e2a4b67e381bcca9285
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..cf78299353ec08bcf55d4e2a4b67e381bcca9285
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..356ec9dc12fca47c27cc8270c032fd5438c15f47
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..356ec9dc12fca47c27cc8270c032fd5438c15f47
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..be73ef9f373637c77b63c76c71210c5fc7997d56
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..be73ef9f373637c77b63c76c71210c5fc7997d56
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/docs/boundaries.md
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/ir/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Tagged execution-IR namespace."""

from autofdtd.ir.models import (
    EXECUTION_PACKAGE_FORMAT,
    IR_SCHEMA_VERSION,
    AbsorberIR,
    AbsorberParamsIR,
    AnisotropicMediumIR,
    ArtifactIR,
    AutoGridIR,
    BlochBoundaryIR,
    BoxIR,
    BoundaryIR,
    BoundarySpecIR,
    ClipOperationIR,
    ComponentFamily,
    ComponentIR,
    CustomGridBoundariesIR,
    CustomGridIR,
    CylinderIR,
    DebyeIR,
    DrudeIR,
    ExecutionManifestIR,
    ExecutionPackageIR,
    GeometryGroupIR,
    GeometryIR,
    GeometryTransformIR,
    GridRefinementIR,
    GridSpecIR,
    LayerRefinementSpecIR,
    LorentzIR,
    MediumComponentIR,
    MediumIR,
    PECBoundaryIR,
    PECMediumIR,
    PMLIR,
    PMLParamsIR,
    PMCBoundaryIR,
    PMCMediumIR,
    PeriodicIR,
    PolarizedAveragingIR,
    PoleResidueIR,
    PolySlabIR,
    ResolvedGridAxisIR,
    ResolvedGridIR,
    SceneIR,
    SellmeierIR,
    SimulationIR,
    SphereIR,
    StaircasingIR,
    StablePMLIR,
    StructureIR,
    SubpixelPolicyIR,
    SubpixelSpecIR,
    SymmetryAxisIR,
    TransformedIR,
    UniformGridIR,
    boundary_edge_to_ir,
    boundary_spec_to_ir,
    boundary_to_ir,
    component_to_ir,
    execution_package_json_schema,
    geometry_to_ir,
    geometry_transform_to_ir,
    grid_spec_to_ir,
    medium_to_ir,
    scene_to_ir,
    simulation_to_ir,
    structure_to_ir,
    write_execution_package_schema,
)

__all__ = [
    "EXECUTION_PACKAGE_FORMAT",
    "IR_SCHEMA_VERSION",
    "AbsorberIR",
    "AbsorberParamsIR",
    "AnisotropicMediumIR",
    "AutoGridIR",
    "ArtifactIR",
    "BlochBoundaryIR",
    "BoxIR",
    "BoundaryIR",
    "BoundarySpecIR",
    "ClipOperationIR",
    "ComponentFamily",
    "ComponentIR",
    "CylinderIR",
    "DebyeIR",
    "DrudeIR",
    "ExecutionManifestIR",
    "ExecutionPackageIR",
    "GridSpecIR",
    "GeometryGroupIR",
    "GeometryIR",
    "GeometryTransformIR",
    "GridRefinementIR",
    "CustomGridBoundariesIR",
    "CustomGridIR",
    "PolySlabIR",
    "PolarizedAveragingIR",
    "LayerRefinementSpecIR",
    "LorentzIR",
    "MediumComponentIR",
    "MediumIR",
    "PECBoundaryIR",
    "PECMediumIR",
    "PMLIR",
    "PMLParamsIR",
    "PMCBoundaryIR",
    "PMCMediumIR",
    "PeriodicIR",
    "PoleResidueIR",
    "ResolvedGridAxisIR",
    "ResolvedGridIR",
    "SceneIR",
    "SellmeierIR",
    "SimulationIR",
    "SphereIR",
    "StablePMLIR",
    "StructureIR",
    "StaircasingIR",
    "SubpixelPolicyIR",
    "SubpixelSpecIR",
    "SymmetryAxisIR",
    "TransformedIR",
    "boundary_edge_to_ir",
    "boundary_spec_to_ir",
    "boundary_to_ir",
    "component_to_ir",
    "execution_package_json_schema",
    "geometry_to_ir",
    "geometry_transform_to_ir",
    "grid_spec_to_ir",
    "medium_to_ir",
    "scene_to_ir",
    "simulation_to_ir",
    "structure_to_ir",
    "UniformGridIR",
    "write_execution_package_schema",
]

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/ir/__init__.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

The ABC and ModeABC surfaces are wired through the public models, compiler, IR, and runtime seams. I’m running the boundary-focused test slice now to catch integration issues before updating working memory and result artifacts.
  ✓ Inspect task board and current boundary implementation to identify ABC-related gaps and existing defer policy surfaces
  → Implement Phase 1 ABC/ModeABC public models, IR/compiler/runtime policy, and validation/defer behavior
  • Add or update tests, docs/examples, and working memory; run targeted verification
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..dba11786d5dedaffdd7de2e4985b0d72239a076d
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,8 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +233,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +379,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +401,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +417,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +441,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +482,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +513,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +551,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +562,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

 exited 1 in 425ms:
..F...F.......F............................................              [100%]
=================================== FAILURES ===================================
________________ test_simulation_normalizes_abc_boundary_subset ________________

    def test_simulation_normalizes_abc_boundary_subset() -> None:
        simulation = Simulation(
            size=(1.0, 1.0, 1.0),
            run_time=1.0,
            boundary_spec=BoundarySpec(
                x=Boundary.abc(permittivity=2.25, conductivity=0.02),
                y=Boundary.periodic(),
                z=Boundary.pec(),
            ),
        )
    
>       assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       TypeError: 'ABCBoundary' object is not subscriptable

tests/test_boundaries.py:84: TypeError
_ test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation _

    def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
        plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
    
        with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
            Simulation(
                size=(1.0, 1.0, 1.0),
                run_time=1.0,
                boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
            )
    
        with pytest.raises(ValueError, match="runtime compilation is deferred"):
>           compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))

tests/test_boundaries.py:172: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
src/autofdtd/compiler/boundaries.py:499: in compile_boundary_spec
    axis_name: _compile_axis(
src/autofdtd/compiler/boundaries.py:416: in _compile_axis
    minus = _compile_edge(axis_name, "minus", boundary.minus, dt=dt, grid_spacing=grid_spacing)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
src/autofdtd/compiler/boundaries.py:380: in _compile_edge
    mode = _mode_for_edge(edge)
           ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

value = ModeABCBoundary(type='ModeABCBoundary', name=None, mode_spec={'type': 'ModeSpec', 'num_modes': 1}, mode_index=0, freq_spec=None, plane=Box(type='Box', center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)))

    def _mode_for_edge(value: object) -> BoundaryMode:
        edge = boundary_edge_model_from_value(value)
        if isinstance(edge, Periodic):
            return BoundaryMode.PERIODIC
        if isinstance(edge, BlochBoundary):
            return BoundaryMode.BLOCH
        if isinstance(edge, PECBoundary):
            return BoundaryMode.PEC
        if isinstance(edge, PMCBoundary):
            return BoundaryMode.PMC
        if isinstance(edge, ABCBoundary):
            return BoundaryMode.ABC
        if isinstance(edge, PML):
            return BoundaryMode.PML
        if isinstance(edge, StablePML):
            return BoundaryMode.STABLE_PML
        if isinstance(edge, Absorber):
            return BoundaryMode.ABSORBER
>       raise TypeError(f"unsupported boundary edge type {type(edge)!r}")
E       TypeError: unsupported boundary edge type <class 'autofdtd.boundaries.models.ModeABCBoundary'>

src/autofdtd/compiler/boundaries.py:170: TypeError
________ test_abc_state_allocation_and_stage_update_track_active_faces _________

    def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
        compiled = compile_boundary_spec(
            BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
        )
        state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
        electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
        electric[1, 2:4, 1:3, 0] = 1.0
    
        updated = apply_abc_layers(electric, compiled, state, field_family="electric")
    
        assert set(state.electric) == {"x.minus", "x.plus"}
        assert state.electric["x.minus"].value_shape == (6, 4, 3)
>       assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
E       AssertionError: assert np.float64(0.0) > 0.0
E        +  where np.float64(0.0) = <function max at 0x7003148cad70>(array([[0., 0.],\n       [0., 0.]]))
E        +    where <function max at 0x7003148cad70> = np.max
E        +    and   array([[0., 0.],\n       [0., 0.]]) = <ufunc 'absolute'>(array([[0., 0.],\n       [0., 0.]]))
E        +      where <ufunc 'absolute'> = np.abs

tests/test_boundaries.py:368: AssertionError
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/test_boundaries.py::test_simulation_normalizes_abc_boundary_subset
FAILED tests/test_boundaries.py::test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation
FAILED tests/test_boundaries.py::test_abc_state_allocation_and_stage_update_track_active_faces
3 failed, 56 passed, 7 warnings in 0.29s

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/compiler/boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..4a9e891ca3708b5d97e4057a2a884aa70e849ac1
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"]["type"] == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"]["permittivity"] == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"]["conductivity"] == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,23 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic())
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +454,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/tests/test_boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

 succeeded in 415ms:
...........................................................              [100%]
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
59 passed, 7 warnings in 0.26s

exec
/bin/bash -lc 'PYTHONPATH=src python -m compileall src' in /workspace/autofdtd
 succeeded in 0ms:
Listing 'src'...
Listing 'src/autofdtd'...
Listing 'src/autofdtd/api'...
Listing 'src/autofdtd/boundaries'...
Listing 'src/autofdtd/compiler'...
Listing 'src/autofdtd/core'...
Listing 'src/autofdtd/diagnostics'...
Listing 'src/autofdtd/examples'...
Listing 'src/autofdtd/geometry'...
Listing 'src/autofdtd/grid'...
Listing 'src/autofdtd/ir'...
Listing 'src/autofdtd/kernels'...
Listing 'src/autofdtd/materials'...
Listing 'src/autofdtd/modes'...
Listing 'src/autofdtd/monitors'...
Listing 'src/autofdtd/runtime'...
Listing 'src/autofdtd/sources'...

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "git status --short && git diff -- src/autofdtd/boundaries/models.py src/autofdtd/compiler/boundaries.py src/autofdtd/kernels/boundaries.py src/autofdtd/runtime/boundaries.py src/autofdtd/ir/models.py src/autofdtd/api/simulation.py src/autofdtd/api/__init__.py src/autofdtd/boundaries/__init__.py src/autofdtd/compiler/__init__.py src/autofdtd/kernels/__init__.py src/autofdtd/runtime/__init__.py src/autofdtd/planning.py src/autofdtd/core/validation.py tests/test_boundaries.py docs/boundaries.md phase1/feature-checklist.md | sed -n '1,260p'" in /workspace/autofdtd
 succeeded in 0ms:
 M README.md
 M docs/architecture.md
 M docs/index.md
 M mkdocs.yml
 M phase1/feature-checklist.md
 M phase1/logs/console.log
 M phase1/logs/events.jsonl
 M phase1/loop.spec.md
 M phase1/working-memory.md
 M src/autofdtd/__init__.py
 M src/autofdtd/api/__init__.py
 M src/autofdtd/api/simulation.py
 M src/autofdtd/boundaries/__init__.py
 M src/autofdtd/compiler/__init__.py
 M src/autofdtd/compiler/materials.py
 M src/autofdtd/core/validation.py
 M src/autofdtd/examples/__init__.py
 M src/autofdtd/examples/materials.py
 M src/autofdtd/ir/__init__.py
 M src/autofdtd/ir/models.py
 M src/autofdtd/kernels/__init__.py
 M src/autofdtd/kernels/materials.py
 M src/autofdtd/materials/__init__.py
 M src/autofdtd/planning.py
 M src/autofdtd/runtime/__init__.py
 M tests/test_containers.py
 M tests/test_ir.py
 M tests/test_materials.py
?? docs/boundaries.md
?? phase1/runs/task-017-attempt-019/
?? phase1/runs/task-018-attempt-001/
?? phase1/runs/task-019-attempt-001/
?? phase1/runs/task-020-attempt-001/
?? phase1/runs/task-021-attempt-001/
?? phase1/runs/task-022-attempt-001/
?? phase1/runs/task-023-attempt-001/
?? src/autofdtd/boundaries/models.py
?? src/autofdtd/compiler/boundaries.py
?? src/autofdtd/examples/boundaries.py
?? src/autofdtd/kernels/boundaries.py
?? src/autofdtd/materials/advanced.py
?? src/autofdtd/materials/anisotropic.py
?? src/autofdtd/runtime/boundaries.py
?? tests/test_boundaries.py
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index c9ed8bb..8502032 100644
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -68,9 +68,9 @@ Statuses:
 | `AnisotropicMedium` | Implement | Needed explicitly and by second-order subpixel smoothing |
 | `FullyAnisotropicMedium` | Defer | Keep mapped |
 | `Medium2D` | Defer | Validation + future support |
-| `LossyMetalMedium` | Defer | Explicit defer bucket |
-| `PerturbationMedium` | Defer | Explicit defer bucket |
-| `Custom*` media families | Reject Clearly | Out of Phase 1 |
+| `LossyMetalMedium` | Defer | Explicit defer bucket with manual volumetric fallback guidance |
+| `PerturbationMedium` / `PerturbationPoleResidue` | Defer | Parsed but not executed in Phase 1 |
+| `Custom*` media families | Reject Clearly | Parsed or bucketed for clear errors, but out of Phase 1 |
 
 ## Boundaries
 
@@ -83,8 +83,8 @@ Statuses:
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index 77951cd..61a8321 100644
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,16 +2,28 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
+    Absorber,
+    AbsorberParams,
+    AnisotropicMedium,
     AutoFDTDModel,
     AutoFDTDValidationWarning,
     AutoGrid,
     Box,
+    BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
+    Boundary,
+    BoundarySpec,
     ClipOperation,
+    CustomAnisotropicMedium,
+    CustomMedium,
     CustomGrid,
     CustomGridBoundaries,
     Cylinder,
     Debye,
     Drude,
+    FullyAnisotropicMedium,
     GeometryArray,
     GeometryGroup,
     GeometryTransform,
@@ -19,13 +31,23 @@ from autofdtd.api.simulation import (
     GridSpec,
     LayerRefinementSpec,
     Lorentz,
+    LossyMetalMedium,
     Medium,
+    Medium2D,
+    ModeABCBoundary,
     PECMedium,
+    PECBoundary,
+    PML,
+    PMLParams,
     PMCMedium,
+    PMCBoundary,
+    PerturbationMedium,
+    PerturbationPoleResidue,
     PolarizedAveraging,
     PoleResidue,
     PolySlab,
     PrimitiveGeometry,
+    Periodic,
     ResolvedGrid,
     ResolvedGridAxis,
     Scene,
@@ -36,6 +58,7 @@ from autofdtd.api.simulation import (
     Structure,
     StructurePrecedence,
     StructurePriorityMode,
+    StablePML,
     SubpixelSpec,
     TaggedModel,
     Transformed,
@@ -47,14 +70,26 @@ __all__ = [
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
+    "Absorber",
+    "AbsorberParams",
+    "AnisotropicMedium",
     "AutoGrid",
     "Box",
+    "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
+    "Boundary",
+    "BoundarySpec",
     "ClipOperation",
+    "CustomAnisotropicMedium",
+    "CustomMedium",
     "CustomGrid",
     "CustomGridBoundaries",
     "Cylinder",
     "Debye",
     "Drude",
+    "FullyAnisotropicMedium",
     "GeometryArray",
     "GeometryGroup",
     "GeometryTransform",
@@ -62,14 +97,24 @@ __all__ = [
     "GridSpec",
     "LayerRefinementSpec",
     "Lorentz",
+    "LossyMetalMedium",
     "Medium",
+    "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
+    "PECBoundary",
+    "PML",
+    "PMLParams",
     "PMCMedium",
+    "PMCBoundary",
+    "PerturbationMedium",
+    "PerturbationPoleResidue",
     "PoleResidue",
     "Sellmeier",
     "PolarizedAveraging",
     "PolySlab",
     "PrimitiveGeometry",
+    "Periodic",
     "ResolvedGrid",
     "ResolvedGridAxis",
     "Scene",
@@ -78,6 +123,7 @@ __all__ = [
     "Structure",
     "StructurePrecedence",
     "StructurePriorityMode",
+    "StablePML",
     "Staircasing",
     "SubpixelSpec",
     "TaggedModel",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index d7d3aab..9df25bb 100644
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -9,6 +9,23 @@ from autofdtd.core.containers import (
 )
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
+from autofdtd.boundaries import (
+    ABCBoundary,
+    Absorber,
+    AbsorberParams,
+    BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
+    Boundary,
+    BoundarySpec,
+    ModeABCBoundary,
+    PML,
+    PMLParams,
+    PECBoundary,
+    PMCBoundary,
+    Periodic,
+    StablePML,
+)
 from autofdtd.geometry import (
     Box,
     ClipOperation,
@@ -37,12 +54,20 @@ from autofdtd.grid import (
     UniformGrid,
 )
 from autofdtd.materials import (
+    AnisotropicMedium,
+    CustomAnisotropicMedium,
+    CustomMedium,
     Debye,
     Drude,
+    FullyAnisotropicMedium,
     Lorentz,
+    LossyMetalMedium,
     Medium,
+    Medium2D,
     PECMedium,
     PMCMedium,
+    PerturbationMedium,
+    PerturbationPoleResidue,
     PoleResidue,
     Sellmeier,
 )
@@ -52,13 +77,27 @@ __all__ = [
     "AutoFDTDModel",
     "AbstractSubpixelPolicy",
     "AutoGrid",
+    "AnisotropicMedium",
+    "ABCBoundary",
+    "Absorber",
+    "AbsorberParams",
+    "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
+    "Boundary",
+    "BoundarySpec",
+    "PML",
+    "PMLParams",
     "ClipOperation",
+    "CustomAnisotropicMedium",
+    "CustomMedium",
     "CustomGrid",
     "CustomGridBoundaries",
     "Cylinder",
     "Debye",
     "Drude",
+    "FullyAnisotropicMedium",
     "GeometryArray",
     "GeometryGroup",
     "GeometryTransform",
@@ -66,14 +105,24 @@ __all__ = [
     "GridSpec",
     "LayerRefinementSpec",
     "Lorentz",
+    "LossyMetalMedium",
     "Medium",
+    "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
+    "PECBoundary",
+    "PML",
+    "PMLParams",
     "PMCMedium",
+    "PMCBoundary",
+    "PerturbationMedium",
+    "PerturbationPoleResidue",
     "PoleResidue",
     "Sellmeier",
     "PolarizedAveraging",
     "PolySlab",
     "PrimitiveGeometry",
+    "Periodic",
     "ResolvedGrid",
     "ResolvedGridAxis",
     "Scene",
@@ -84,6 +133,7 @@ __all__ = [
     "StructurePriorityMode",
     "Staircasing",
     "SubpixelSpec",
+    "StablePML",

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "sed -n '1,120p' src/autofdtd/api/simulation.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API aliases for the foundational Phase 1 container models."""

from autofdtd.core.containers import (
    Scene,
    Simulation,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
)
from autofdtd.core.models import AutoFDTDModel, TaggedModel
from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
from autofdtd.boundaries import (
    ABCBoundary,
    Absorber,
    AbsorberParams,
    BlochBoundary,
    BroadbandModeABCFitterParam,
    BroadbandModeABCSpec,
    Boundary,
    BoundarySpec,
    ModeABCBoundary,
    PML,
    PMLParams,
    PECBoundary,
    PMCBoundary,
    Periodic,
    StablePML,
)
from autofdtd.geometry import (
    Box,
    ClipOperation,
    Cylinder,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    PolySlab,
    PrimitiveGeometry,
    Sphere,
    Transformed,
)
from autofdtd.grid import (
    AbstractSubpixelPolicy,
    AutoGrid,
    CustomGrid,
    CustomGridBoundaries,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    PolarizedAveraging,
    ResolvedGrid,
    ResolvedGridAxis,
    Staircasing,
    SubpixelSpec,
    UniformGrid,
)
from autofdtd.materials import (
    AnisotropicMedium,
    CustomAnisotropicMedium,
    CustomMedium,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PMCMedium,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    Sellmeier,
)

__all__ = [
    "AutoFDTDValidationWarning",
    "AutoFDTDModel",
    "AbstractSubpixelPolicy",
    "AutoGrid",
    "AnisotropicMedium",
    "ABCBoundary",
    "Absorber",
    "AbsorberParams",
    "BlochBoundary",
    "BroadbandModeABCFitterParam",
    "BroadbandModeABCSpec",
    "Box",
    "Boundary",
    "BoundarySpec",
    "PML",
    "PMLParams",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "ModeABCBoundary",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",

exec
/bin/bash -lc "sed -n '1,120p' src/autofdtd/api/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Public API namespace for Tidy3D-inspired object models."""

from autofdtd.api.simulation import (
    AbstractSubpixelPolicy,
    ABCBoundary,
    Absorber,
    AbsorberParams,
    AnisotropicMedium,
    AutoFDTDModel,
    AutoFDTDValidationWarning,
    AutoGrid,
    Box,
    BlochBoundary,
    BroadbandModeABCFitterParam,
    BroadbandModeABCSpec,
    Boundary,
    BoundarySpec,
    ClipOperation,
    CustomAnisotropicMedium,
    CustomMedium,
    CustomGrid,
    CustomGridBoundaries,
    Cylinder,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    GeometryArray,
    GeometryGroup,
    GeometryTransform,
    GridRefinement,
    GridSpec,
    LayerRefinementSpec,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    ModeABCBoundary,
    PECMedium,
    PECBoundary,
    PML,
    PMLParams,
    PMCMedium,
    PMCBoundary,
    PerturbationMedium,
    PerturbationPoleResidue,
    PolarizedAveraging,
    PoleResidue,
    PolySlab,
    PrimitiveGeometry,
    Periodic,
    ResolvedGrid,
    ResolvedGridAxis,
    Scene,
    Sellmeier,
    Simulation,
    Sphere,
    Staircasing,
    Structure,
    StructurePrecedence,
    StructurePriorityMode,
    StablePML,
    SubpixelSpec,
    TaggedModel,
    Transformed,
    UniformGrid,
    UnsupportedFeatureError,
)

__all__ = [
    "AutoFDTDModel",
    "AutoFDTDValidationWarning",
    "AbstractSubpixelPolicy",
    "ABCBoundary",
    "Absorber",
    "AbsorberParams",
    "AnisotropicMedium",
    "AutoGrid",
    "Box",
    "BlochBoundary",
    "BroadbandModeABCFitterParam",
    "BroadbandModeABCSpec",
    "Boundary",
    "BoundarySpec",
    "ClipOperation",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "CustomGrid",
    "CustomGridBoundaries",
    "Cylinder",
    "Debye",
    "Drude",
    "FullyAnisotropicMedium",
    "GeometryArray",
    "GeometryGroup",
    "GeometryTransform",
    "GridRefinement",
    "GridSpec",
    "LayerRefinementSpec",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "ModeABCBoundary",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "PoleResidue",
    "Sellmeier",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "ResolvedGrid",
    "ResolvedGridAxis",
    "Scene",

exec
/bin/bash -lc "sed -n '1,120p' src/autofdtd/compiler/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Boundary compilation helpers for the foundational Phase 1 surface."""

from __future__ import annotations

import math
from enum import StrEnum
from typing import Literal

from autofdtd.boundaries import (
    ABCBoundary,
    Absorber,
    AbsorberParams,
    BlochBoundary,
    BroadbandModeABCSpec,
    Boundary,
    BoundarySpec,
    DEFAULT_PML_PARAMS,
    DEFAULT_ABSORBER_PARAMS,
    ModeABCBoundary,
    PECBoundary,
    PML,
    PMLParams,
    PMCBoundary,
    Periodic,
    StablePML,
    boundary_edge_model_from_value,
    boundary_model_from_value,
    boundary_spec_model_from_value,
)
from autofdtd.core.models import AutoFDTDModel


class BoundaryMode(StrEnum):
    """Runtime boundary mode consumed by staged halo handling."""

    PERIODIC = "periodic"
    BLOCH = "bloch"
    PEC = "pec"
    PMC = "pmc"
    ABC = "abc"
    PML = "pml"
    STABLE_PML = "stable_pml"
    ABSORBER = "absorber"


C0 = 299_792_458.0
EPSILON_0 = 8.854_187_812_8e-12


class CompiledABCCoefficients(AutoFDTDModel):
    """Prepared first-order absorbing-boundary coefficients for one face."""

    type: str = "CompiledABCCoefficients"
    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
    reflection_coefficient: float
    attenuation: float
    effective_permittivity: float
    effective_conductivity: float
    wave_speed: float
    dt: float
    grid_spacing: float
    requires_material_inference: bool = False


class CompiledPMLCoefficients(AutoFDTDModel):
    """Prepared per-layer damping and memory coefficients for one PML face."""

    type: str = "CompiledPMLCoefficients"
    boundary_type: Literal["PML", "StablePML", "Absorber"] = "PML"
    num_layers: int
    sigma: tuple[float, ...]
    kappa: tuple[float, ...]
    alpha: tuple[float, ...]
    attenuation: tuple[float, ...]
    memory_decay: tuple[float, ...]
    memory_drive: tuple[float, ...]
    stretch: tuple[float, ...]
    uses_memory: bool = True
    terminal_reflection: Literal["none", "pec"] = "none"
    dt: float
    grid_spacing: float


class CompiledBoundaryEdge(AutoFDTDModel):
    """Runtime-ready metadata for one boundary face."""

    type: str = "CompiledBoundaryEdge"
    axis: Literal["x", "y", "z"]
    side: Literal["minus", "plus"]
    mode: BoundaryMode
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]
    source_side: Literal["minus", "plus"]
    phase_factor: complex = 1.0 + 0.0j
    abc: CompiledABCCoefficients | None = None
    pml: CompiledPMLCoefficients | None = None


class CompiledBoundaryAxis(AutoFDTDModel):
    """Runtime-ready metadata for one axis boundary pair."""

    type: str = "CompiledBoundaryAxis"
    axis: Literal["x", "y", "z"]
    minus: CompiledBoundaryEdge
    plus: CompiledBoundaryEdge
    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
    pml_faces: tuple[Literal["minus", "plus"], ...] = ()


class CompiledSymmetryAxis(AutoFDTDModel):
    """Reflection-sign metadata for one reduced symmetry axis."""

    type: str = "CompiledSymmetryAxis"
    axis: Literal["x", "y", "z"]
    symmetry: Literal[-1, 1]
    electric_signs: tuple[int, int, int]
    magnetic_signs: tuple[int, int, int]



diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "rg -n \"ABCBoundary|ModeABCBoundary|BroadbandModeABC\" src/autofdtd/__init__.py src/autofdtd/materials/__init__.py src/autofdtd/examples -g '"'!**/__pycache__/**'"'" in /workspace/autofdtd
 exited 1 in 0ms:
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""AutoFDTD package scaffold for Phase 1 Maxwell solver work."""

from autofdtd.api import (
    AnisotropicMedium,
    Box,
    Boundary,
    BoundarySpec,
    CustomAnisotropicMedium,
    CustomMedium,
    Cylinder,
    Debye,
    Drude,
    FullyAnisotropicMedium,
    GeometryTransform,
    Lorentz,
    LossyMetalMedium,
    Medium,
    Medium2D,
    PECMedium,
    PECBoundary,
    PML,
    PMLParams,
    PMCMedium,
    PMCBoundary,
    PolarizedAveraging,
    PerturbationMedium,
    PerturbationPoleResidue,
    PoleResidue,
    PolySlab,
    PrimitiveGeometry,
    Periodic,
    Scene,
    Sellmeier,
    Simulation,
    Sphere,
    Staircasing,
    Structure,
    StructurePriorityMode,
    SubpixelSpec,
)
from autofdtd.core import AutoFDTDModel, TaggedModel
from autofdtd.ir import (
    EXECUTION_PACKAGE_FORMAT,
    IR_SCHEMA_VERSION,
    ExecutionPackageIR,
    execution_package_json_schema,
    simulation_to_ir,
)
from autofdtd.planning import (
    FeatureEntry,
    FeatureStatus,
    NamespacePlan,
    feature_entry,
    feature_matrix,
    feature_status,
    planned_namespace_map,
)
from autofdtd.version import __version__

__all__ = [
    "FeatureEntry",
    "FeatureStatus",
    "NamespacePlan",
    "AutoFDTDModel",
    "AnisotropicMedium",
    "Box",
    "Boundary",
    "BoundarySpec",
    "CustomAnisotropicMedium",
    "CustomMedium",
    "Cylinder",
    "Debye",
    "Drude",
    "EXECUTION_PACKAGE_FORMAT",
    "ExecutionPackageIR",
    "FullyAnisotropicMedium",
    "GeometryTransform",
    "Lorentz",
    "LossyMetalMedium",
    "Medium",
    "Medium2D",
    "PECMedium",
    "PECBoundary",
    "PML",
    "PMLParams",
    "PMCMedium",
    "PMCBoundary",
    "PoleResidue",
    "PerturbationMedium",
    "PerturbationPoleResidue",
    "Sellmeier",
    "IR_SCHEMA_VERSION",
    "PolarizedAveraging",
    "PolySlab",
    "PrimitiveGeometry",
    "Periodic",
    "Scene",
    "Simulation",
    "Sphere",
    "Staircasing",
    "Structure",
    "StructurePriorityMode",
    "SubpixelSpec",
    "TaggedModel",
    "__version__",
    "feature_entry",
    "execution_package_json_schema",
    "feature_matrix",
    "feature_status",
    "planned_namespace_map",
    "simulation_to_ir",
]

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/__init__.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "sed -n '1,220p' src/autofdtd/examples/boundaries.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Examples for the Phase 1 boundary subset."""

from __future__ import annotations

import numpy as np

from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
from autofdtd.compiler import compile_boundary_spec
from autofdtd.runtime import build_boundary_runtime


def boundary_runtime_snapshot() -> dict[str, object]:
    """Return a JSON-ready snapshot of the compiled Phase 1 boundary runtime metadata."""

    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.periodic(),
            y=Boundary.pec(),
            z=Boundary.pmc(),
        )
    )
    return compiled.to_payload()


def pml_absorption_snapshot() -> dict[str, object]:
    """Return a compact snapshot showing the staged PML attenuation path."""

    runtime = build_boundary_runtime(
        BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
        dt=0.5,
        field_shape=(10, 4, 4, 3),
    )
    electric = np.zeros((10, 4, 4, 3), dtype=np.float64)
    magnetic = np.zeros((10, 4, 4, 3), dtype=np.float64)
    electric[5:8, 1:3, 1:3, 0] = 1.0
    before = float(np.sum(np.abs(electric) ** 2))
    for _ in range(8):
        electric, magnetic = runtime.apply(electric, magnetic)
    after = float(np.sum(np.abs(electric) ** 2))
    return {
        "compiled_pml_faces": runtime.compiled.pml_faces,
        "stage_order": runtime.compiled.stage_order,
        "energy_before": before,
        "energy_after": after,
    }


def absorbing_boundary_snapshot() -> dict[str, object]:
    """Return a compact snapshot for StablePML and Absorber compilation."""

    compiled = compile_boundary_spec(
        BoundarySpec(
            x=Boundary.stable_pml(num_layers=8),
            y=Boundary.absorber(
                num_layers=10,
                parameters=AbsorberParams(sigma_max=7.2),
            ),
            z=Boundary.periodic(),
        ),
        dt=0.5,
        grid_spacing=(0.25, 0.25, 0.5),
    )
    return {
        "stage_order": compiled.stage_order,
        "pml_faces": compiled.pml_faces,
        "stable_pml_faces": compiled.stable_pml_faces,
        "absorber_faces": compiled.absorber_faces,
        "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
    }

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/examples/boundaries.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc "sed -n '1,200p' src/autofdtd/examples/__init__.py" in /workspace/autofdtd
 succeeded in 0ms:
"""Validation example and benchmark namespace."""

from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
from autofdtd.examples.composite_geometry import rotated_ring_resonator
from autofdtd.examples.geometry_array import rotated_post_array_scene
from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
from autofdtd.examples.materials import (
    build_material_scene,
    compiled_anisotropic_example,
    compiled_lorentz_drude_debye_examples,
    compiled_material_example,
)
from autofdtd.examples.polyslab import directional_coupler_scene, strip_waveguide_simulation
from autofdtd.examples.subpixel import build_subpixel_demo

__all__ = [
    "boundary_runtime_snapshot",
    "pml_absorption_snapshot",
    "directional_coupler_scene",
    "autogrid_example",
    "build_subpixel_demo",
    "compiled_anisotropic_example",
    "compiled_lorentz_drude_debye_examples",
    "build_material_scene",
    "compiled_material_example",
    "grid_spec_examples",
    "resolved_grid_example",
    "rotated_post_array_scene",
    "rotated_ring_resonator",
    "strip_waveguide_simulation",
]

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/src/autofdtd/examples/__init__.py
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

exec
/bin/bash -lc 'PYTHONPATH=src pytest -q tests/test_boundaries.py tests/test_ir.py tests/test_containers.py && PYTHONPATH=src python -m compileall src' in /workspace/autofdtd
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

 succeeded in 413ms:
...........................................................              [100%]
=============================== warnings summary ===============================
tests/test_boundaries.py::test_simulation_normalizes_supported_boundary_subset
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: A periodic boundary opposite a PEC/PMC boundary is coerced to the conductor boundary so runtime halo handling stays reflection-only on that axis.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants
tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_pml_boundary_stages_absorb_boundary_energy
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: PML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
tests/test_boundaries.py::test_absorber_ghost_updates_use_reflective_outer_wall
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: Absorber boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

tests/test_boundaries.py::test_pml_state_allocation_tracks_active_faces
  /venv/main/lib/python3.12/site-packages/pydantic/main.py:250: AutoFDTDValidationWarning: StablePML boundaries with fewer than 6 layers are prone to reflection and should only be used for small smoke tests.
    validated_self = self.__pydantic_validator__.validate_python(data, self_instance=self)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
59 passed, 7 warnings in 0.26s
Listing 'src'...
Listing 'src/autofdtd'...
Listing 'src/autofdtd/api'...
Listing 'src/autofdtd/boundaries'...
Listing 'src/autofdtd/compiler'...
Listing 'src/autofdtd/core'...
Listing 'src/autofdtd/diagnostics'...
Listing 'src/autofdtd/examples'...
Compiling 'src/autofdtd/examples/__init__.py'...
Compiling 'src/autofdtd/examples/boundaries.py'...
Listing 'src/autofdtd/geometry'...
Listing 'src/autofdtd/grid'...
Listing 'src/autofdtd/ir'...
Listing 'src/autofdtd/kernels'...
Listing 'src/autofdtd/materials'...
Listing 'src/autofdtd/modes'...
Listing 'src/autofdtd/monitors'...
Listing 'src/autofdtd/runtime'...
Listing 'src/autofdtd/sources'...

apply patch
patch: completed
/workspace/autofdtd/phase1/working-memory.md
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

The implementation and targeted verification are complete. I’m writing the task artifacts and final structured result now.
  ✓ Inspect task board and current boundary implementation to identify ABC-related gaps and existing defer policy surfaces
  ✓ Implement Phase 1 ABC/ModeABC public models, IR/compiler/runtime policy, and validation/defer behavior
  → Add or update tests, docs/examples, and working memory; run targeted verification
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

apply patch
patch: completed
/workspace/autofdtd/phase1/runs/task-023-attempt-001/result.json
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/runs/task-023-attempt-001/result.json b/phase1/runs/task-023-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..4d01906afa776c92866a2ba41bef8382733d34ad
--- /dev/null
+++ b/phase1/runs/task-023-attempt-001/result.json
@@ -0,0 +1,48 @@
+{
+  "task_id": "task-023",
+  "status": "completed",
+  "summary": "Implemented a practical Phase 1 ABCBoundary subset with typed API/IR/compiler/runtime support, while making ModeABCBoundary and related mode-fit surfaces parseable but explicitly deferred.",
+  "key_findings": [
+    "ABCBoundary is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.",
+    "BoundarySpecIR and the compiled runtime metadata now expose abc_axes and abc_faces so later chunk planning and execution stages can distinguish first-order ABC faces from PML or conductor faces.",
+    "ModeABCBoundary, BroadbandModeABCSpec, and BroadbandModeABCFitterParam are now parseable compatibility surfaces, but Simulation validation and runtime compilation still fail clearly because mode-solver dependencies are not available in Phase 1.",
+    "Examples, docs, and focused tests now cover ABC normalization, IR lowering, runtime coefficient preparation, staged ABC updates, and the explicit deferred policy for mode-based ABC."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Public boundary models now include ABCBoundary, deferred ModeABCBoundary compatibility types, and convenience constructors on Boundary and BoundarySpec."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Boundary compilation now prepares first-order ABC coefficients, abc face metadata, and explicit deferred errors for mode-based ABC."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "IR lowering now emits typed ABCBoundaryIR payloads and BoundarySpecIR abc_axes/abc_faces metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Focused tests exercise ABC normalization, compilation, staged updates, IR lowering, and ModeABCBoundary defer behavior."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Boundary documentation now explains the implemented ABC subset and the remaining ModeABCBoundary defer policy."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py",
+      "description": "Boundary examples now include an abc_boundary_snapshot helper for inspection and smoke usage."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "Automatic boundary-medium inference for ABCBoundary and all mode-based ABC behavior should build on the new abc_faces/staged-ABC seam once scene-material sampling and mode-solver tasks land.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/runs/task-023-attempt-001/result.json b/phase1/runs/task-023-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..4d01906afa776c92866a2ba41bef8382733d34ad
--- /dev/null
+++ b/phase1/runs/task-023-attempt-001/result.json
@@ -0,0 +1,48 @@
+{
+  "task_id": "task-023",
+  "status": "completed",
+  "summary": "Implemented a practical Phase 1 ABCBoundary subset with typed API/IR/compiler/runtime support, while making ModeABCBoundary and related mode-fit surfaces parseable but explicitly deferred.",
+  "key_findings": [
+    "ABCBoundary is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.",
+    "BoundarySpecIR and the compiled runtime metadata now expose abc_axes and abc_faces so later chunk planning and execution stages can distinguish first-order ABC faces from PML or conductor faces.",
+    "ModeABCBoundary, BroadbandModeABCSpec, and BroadbandModeABCFitterParam are now parseable compatibility surfaces, but Simulation validation and runtime compilation still fail clearly because mode-solver dependencies are not available in Phase 1.",
+    "Examples, docs, and focused tests now cover ABC normalization, IR lowering, runtime coefficient preparation, staged ABC updates, and the explicit deferred policy for mode-based ABC."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Public boundary models now include ABCBoundary, deferred ModeABCBoundary compatibility types, and convenience constructors on Boundary and BoundarySpec."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Boundary compilation now prepares first-order ABC coefficients, abc face metadata, and explicit deferred errors for mode-based ABC."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "IR lowering now emits typed ABCBoundaryIR payloads and BoundarySpecIR abc_axes/abc_faces metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Focused tests exercise ABC normalization, compilation, staged updates, IR lowering, and ModeABCBoundary defer behavior."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Boundary documentation now explains the implemented ABC subset and the remaining ModeABCBoundary defer policy."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py",
+      "description": "Boundary examples now include an abc_boundary_snapshot helper for inspection and smoke usage."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "Automatic boundary-medium inference for ABCBoundary and all mode-based ABC behavior should build on the new abc_faces/staged-ABC seam once scene-material sampling and mode-solver tasks land.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

The ABC subset, deferred mode-ABC policy, docs/examples, working memory, and structured task artifact are all in place.
  ✓ Inspect task board and current boundary implementation to identify ABC-related gaps and existing defer policy surfaces
  ✓ Implement Phase 1 ABC/ModeABC public models, IR/compiler/runtime policy, and validation/defer behavior
  ✓ Add or update tests, docs/examples, and working memory; run targeted verification
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/runs/task-023-attempt-001/result.json b/phase1/runs/task-023-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..4d01906afa776c92866a2ba41bef8382733d34ad
--- /dev/null
+++ b/phase1/runs/task-023-attempt-001/result.json
@@ -0,0 +1,48 @@
+{
+  "task_id": "task-023",
+  "status": "completed",
+  "summary": "Implemented a practical Phase 1 ABCBoundary subset with typed API/IR/compiler/runtime support, while making ModeABCBoundary and related mode-fit surfaces parseable but explicitly deferred.",
+  "key_findings": [
+    "ABCBoundary is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.",
+    "BoundarySpecIR and the compiled runtime metadata now expose abc_axes and abc_faces so later chunk planning and execution stages can distinguish first-order ABC faces from PML or conductor faces.",
+    "ModeABCBoundary, BroadbandModeABCSpec, and BroadbandModeABCFitterParam are now parseable compatibility surfaces, but Simulation validation and runtime compilation still fail clearly because mode-solver dependencies are not available in Phase 1.",
+    "Examples, docs, and focused tests now cover ABC normalization, IR lowering, runtime coefficient preparation, staged ABC updates, and the explicit deferred policy for mode-based ABC."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Public boundary models now include ABCBoundary, deferred ModeABCBoundary compatibility types, and convenience constructors on Boundary and BoundarySpec."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Boundary compilation now prepares first-order ABC coefficients, abc face metadata, and explicit deferred errors for mode-based ABC."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "IR lowering now emits typed ABCBoundaryIR payloads and BoundarySpecIR abc_axes/abc_faces metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Focused tests exercise ABC normalization, compilation, staged updates, IR lowering, and ModeABCBoundary defer behavior."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Boundary documentation now explains the implemented ABC subset and the remaining ModeABCBoundary defer policy."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py",
+      "description": "Boundary examples now include an abc_boundary_snapshot helper for inspection and smoke usage."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "Automatic boundary-medium inference for ABCBoundary and all mode-based ABC behavior should build on the new abc_faces/staged-ABC seam once scene-material sampling and mode-solver tasks land.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

codex
{"task_id":"task-023","status":"completed","summary":"Implemented a practical Phase 1 `ABCBoundary` subset with typed API/IR/compiler/runtime support, while making `ModeABCBoundary` and related mode-fit surfaces parseable but explicitly deferred.","key_findings":["`ABCBoundary` is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.","Boundary runtime and IR metadata now expose `abc_axes` and `abc_faces`, keeping the mathematical boundary family distinct from scheduling and later chunk-planning concerns.","`ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` are now mapped compatibility surfaces, but Simulation validation and runtime compilation fail clearly because mode-solver dependencies are not available in Phase 1.","Docs, examples, tests, working memory, and the task result artifact were updated to capture the implemented ABC subset and the explicit defer policy for mode-based ABC."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Public boundary models now include `ABCBoundary`, deferred `ModeABCBoundary` compatibility types, and convenience constructors on `Boundary` and `BoundarySpec`."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary compilation now prepares first-order ABC coefficients, `abc_*` face metadata, and explicit deferred errors for mode-based ABC."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"IR lowering now emits typed `ABCBoundaryIR` payloads and `BoundarySpecIR` `abc_axes` / `abc_faces` metadata."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests cover ABC normalization, compilation, staged updates, IR lowering, and the explicit `ModeABCBoundary` defer behavior."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary documentation now explains the implemented ABC subset and the remaining `ModeABCBoundary` defer policy."},{"path":"/workspace/autofdtd/phase1/runs/task-023-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"Automatic boundary-medium inference for `ABCBoundary` and all mode-based ABC behavior should build on the new `abc_faces` staged-boundary seam once scene-material sampling and mode-solver tasks land.","next_action":"none"}
diff --git a/docs/boundaries.md b/docs/boundaries.md
index 43156dc89a37e90f03b4dacff0da3489f32e2a25..68127c259204946711703102d9d4978370d43438
--- a/docs/boundaries.md
+++ b/docs/boundaries.md
@@ -2,15 +2,19 @@
 
 Phase 1 now has an explicit boundary surface for non-absorbing, phase-aware, and
 absorbing edges built around `Periodic`, `BlochBoundary`, `PECBoundary`,
-`PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and `BoundarySpec`.
+`PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
+`BoundarySpec`.
 
 ## Public API
 
-- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`,
-  and `Absorber` are typed boundary-edge models.
+- `Periodic`, `BlochBoundary`, `PECBoundary`, `PMCBoundary`, `ABCBoundary`,
+  `PML`, `StablePML`, and `Absorber` are typed boundary-edge models.
 - `Boundary` pairs `minus` and `plus` edges along one axis.
 - `BoundarySpec` carries `x`, `y`, and `z` axis boundaries and defaults to periodic
   behavior on all sides in the currently implemented subset.
+- `ABCBoundary` is implemented as a practical first-order subset. Runtime
+  compilation currently requires explicit `permittivity`, and optional
+  `conductivity` is only accepted when `permittivity` is also supplied.
 - If a periodic edge is placed opposite a PEC or PMC edge, the periodic edge is
   coerced to the conductor edge with a warning so later runtime handling stays
   reflection-based instead of mixing halo semantics on one axis.
@@ -18,12 +22,16 @@
   default preset.
 - `Absorber` uses a simpler conductivity-only profile and defaults
   `extrude_structures=False`, matching the local Tidy3D schema surface.
+- `ModeABCBoundary`, `BroadbandModeABCSpec`, and
+  `BroadbandModeABCFitterParam` are parseable compatibility surfaces, but remain
+  deferred until the mode-solver tasks land.
 
 ## Lowering And Runtime
 
 - `autofdtd.ir` lowers the supported subset into typed `PeriodicIR`,
-  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `PMLIR`, `StablePMLIR`,
-  `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR` payloads.
+  `BlochBoundaryIR`, `PECBoundaryIR`, `PMCBoundaryIR`, `ABCBoundaryIR`,
+  `PMLIR`, `StablePMLIR`, `AbsorberIR`, `BoundaryIR`, and `BoundarySpecIR`
+  payloads.
 - `autofdtd.compiler.boundaries.compile_boundary_spec()` turns the public boundary
   surface into a runtime plan with explicit per-face mode tags, sign transforms,
   halo depth, stage ordering, and per-face absorbing profile metadata.
@@ -33,6 +41,9 @@
 - Bloch boundaries apply explicit complex phase factors on wrapped ghost values.
 - PEC and PMC boundaries use reflection-sign conventions so tangential-versus-normal
   field parity is explicit instead of being hidden in a monolithic timestep kernel.
+- `ABCBoundary` uses a stateful first-order ghost-cell recurrence with an
+  explicit effective-medium coefficient. It is intended as a practical subset
+  for simple termination cases, not as a replacement for PML.
 - `PML` and `StablePML` share the staged damping-layer backend and differ through
   compiled coefficient presets.
 - `Absorber` uses the same staged region-update seam but with conductivity-only
@@ -41,13 +52,16 @@
 ## Current Limits
 
 - Runtime compilation currently supports `Periodic`, `BlochBoundary`,
-  `PECBoundary`, `PMCBoundary`, `PML`, `StablePML`, `Absorber`, `Boundary`, and
-  `BoundarySpec`.
+  `PECBoundary`, `PMCBoundary`, `ABCBoundary`, `PML`, `StablePML`, `Absorber`,
+  `Boundary`, and `BoundarySpec`.
+- `ABCBoundary` without explicit `permittivity` still lowers through the API and
+  IR, but runtime compilation fails clearly because automatic medium inference is
+  not wired into Phase 1 scene compilation yet.
 - `StablePML` is currently implemented as a preset-driven extension of the baseline
   staged PML backend rather than a separate split-field formulation.
 - `Absorber` is currently implemented as a conductivity-ramp absorbing subset with a
   reflective outer wall, not a full material-aware adiabatic absorber.
-- ABC-family boundaries remain outside the runtime compiler path and should fail
-  explicitly until `task-023`.
+- `ModeABCBoundary` remains outside the runtime compiler path and fails
+  explicitly until the mode-solver tasks land.
 - The kernel helpers assume vector fields shaped `(nx, ny, nz, 3)` with one ghost
   cell per face.
diff --git a/phase1/feature-checklist.md b/phase1/feature-checklist.md
index ec133dc47a0ea6d9cb5497096d4bc1ea954b9b9b..8502032f22ece2201afcb27ecb2aec94001f53df
--- a/phase1/feature-checklist.md
+++ b/phase1/feature-checklist.md
@@ -83,8 +83,8 @@
 | `PML` | Implement | Must-have |
 | `StablePML` | Implement | May land as a supported subset first |
 | `Absorber` | Implement | Important practical fallback |
-| `ABCBoundary` | Defer | Keep mapped |
-| `ModeABCBoundary` | Defer | Keep mapped |
+| `ABCBoundary` | Implement | First-order subset with explicit effective-medium inputs |
+| `ModeABCBoundary` | Defer | Keep mapped until mode-solver work lands |
 | `InternalAbsorber` | Reject Clearly | Out of Phase 1 |
 
 ## Sources
diff --git a/phase1/runs/task-023-attempt-001/result.json b/phase1/runs/task-023-attempt-001/result.json
new file mode 100644
index 0000000000000000000000000000000000000000..4d01906afa776c92866a2ba41bef8382733d34ad
--- /dev/null
+++ b/phase1/runs/task-023-attempt-001/result.json
@@ -0,0 +1,48 @@
+{
+  "task_id": "task-023",
+  "status": "completed",
+  "summary": "Implemented a practical Phase 1 ABCBoundary subset with typed API/IR/compiler/runtime support, while making ModeABCBoundary and related mode-fit surfaces parseable but explicitly deferred.",
+  "key_findings": [
+    "ABCBoundary is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.",
+    "BoundarySpecIR and the compiled runtime metadata now expose abc_axes and abc_faces so later chunk planning and execution stages can distinguish first-order ABC faces from PML or conductor faces.",
+    "ModeABCBoundary, BroadbandModeABCSpec, and BroadbandModeABCFitterParam are now parseable compatibility surfaces, but Simulation validation and runtime compilation still fail clearly because mode-solver dependencies are not available in Phase 1.",
+    "Examples, docs, and focused tests now cover ABC normalization, IR lowering, runtime coefficient preparation, staged ABC updates, and the explicit deferred policy for mode-based ABC."
+  ],
+  "artifacts": [
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/boundaries/models.py",
+      "description": "Public boundary models now include ABCBoundary, deferred ModeABCBoundary compatibility types, and convenience constructors on Boundary and BoundarySpec."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/compiler/boundaries.py",
+      "description": "Boundary compilation now prepares first-order ABC coefficients, abc face metadata, and explicit deferred errors for mode-based ABC."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/kernels/boundaries.py",
+      "description": "Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/runtime/boundaries.py",
+      "description": "Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/ir/models.py",
+      "description": "IR lowering now emits typed ABCBoundaryIR payloads and BoundarySpecIR abc_axes/abc_faces metadata."
+    },
+    {
+      "path": "/workspace/autofdtd/tests/test_boundaries.py",
+      "description": "Focused tests exercise ABC normalization, compilation, staged updates, IR lowering, and ModeABCBoundary defer behavior."
+    },
+    {
+      "path": "/workspace/autofdtd/docs/boundaries.md",
+      "description": "Boundary documentation now explains the implemented ABC subset and the remaining ModeABCBoundary defer policy."
+    },
+    {
+      "path": "/workspace/autofdtd/src/autofdtd/examples/boundaries.py",
+      "description": "Boundary examples now include an abc_boundary_snapshot helper for inspection and smoke usage."
+    }
+  ],
+  "error_summary": "",
+  "follow_up_notes": "Automatic boundary-medium inference for ABCBoundary and all mode-based ABC behavior should build on the new abc_faces/staged-ABC seam once scene-material sampling and mode-solver tasks land.",
+  "next_action": "none"
+}
diff --git a/phase1/working-memory.md b/phase1/working-memory.md
index 871953459ad0ece1a2d2c81c837beea61abd32f6..338b0e13d1d927caa6c94d73ba24010cfc7fcdd8
--- a/phase1/working-memory.md
+++ b/phase1/working-memory.md
@@ -60,6 +60,9 @@
 - `src/autofdtd/boundaries/models.py` now also implements `AbsorberParams`, `StablePML`, and `Absorber`, with Tidy3D-aligned default presets, explicit constructors on `Boundary` / `BoundarySpec`, and zero-dimension coercion that treats all absorbing families consistently.
 - `src/autofdtd/compiler/boundaries.py` now compiles `StablePML` onto the existing staged PML coefficient path with a separate profile tag and compiles `Absorber` into conductivity-only attenuation coefficients plus a reflective outer-wall policy; `CompiledBoundarySpec` now records `stable_pml_*` and `absorber_*` face metadata in addition to the existing absorbing-face alias.
 - `src/autofdtd/ir/models.py` now lowers `StablePML` and `Absorber` into typed `StablePMLIR` and `AbsorberIR` payloads, with `AbsorberParamsIR` for conductivity-only profile metadata.
+- `src/autofdtd/boundaries/models.py` now adds `ABCBoundary` as an implemented first-order subset with explicit effective-medium inputs, plus parseable deferred `ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` compatibility surfaces.
+- `src/autofdtd/compiler/boundaries.py`, `src/autofdtd/kernels/boundaries.py`, and `src/autofdtd/runtime/boundaries.py` now compile `ABCBoundary` into explicit first-order recurrence coefficients plus persistent per-face ABC state, while `ModeABCBoundary` fails with a clear runtime-deferred error until mode-solver work lands.
+- `src/autofdtd/ir/models.py` now lowers `ABCBoundary` into typed `ABCBoundaryIR` and records `abc_axes` / `abc_faces` on `BoundarySpecIR`; `ModeABCBoundary` remains a deferred public-model surface rather than a validated simulation feature.
 
 ## Decisions
 
@@ -94,6 +97,7 @@
 - Keep Phase 1 PML deliberately narrow: use Tidy3D-shaped `num_layers + parameters + extrude_structures` public models, compile one profile per boundary face, and treat the first runnable backend as a staged damping layer with persistent face memory rather than a full split-field CPML implementation.
 - Keep `StablePML` as a preset-driven extension of the baseline staged PML backend in Phase 1 rather than a separate field-splitting formulation; runtime differentiation is through compiled profile metadata and defaults, not a distinct kernel family.
 - Keep `Absorber` practical and explicit: Phase 1 uses a conductivity-ramp absorbing subset with no auxiliary memory contribution and a PEC-like outer-wall reflection policy, trading fidelity for a clear stable fallback when baseline PML is unsuitable.
+- Keep `ABCBoundary` practical and narrow: Phase 1 implements only the explicit-parameter first-order subset, and automatic medium inference plus mode-based ABC stay deferred until scene-material sampling and mode-solver tasks are available.
 
 ## Task Handoffs
 
@@ -111,3 +115,4 @@
 - Later absorbing-boundary tasks should extend `CompiledBoundarySpec` and the boundary ghost-update stage rather than reworking the public `Boundary` or `BoundarySpec` container semantics; Bloch, symmetry, and baseline PML metadata now already live on that runtime contract.
 - `task-022` should either preserve the `PMLParams` coefficient-shape contract for `StablePML` / `Absorber` or add a clearly separate compiled-face payload; `allocate_pml_boundary_state()` and `apply_boundary_stages()` are the current extension seams.
 - Later ABC-boundary work should treat the current `pml_faces` metadata on `CompiledBoundarySpec` as the legacy absorbing-face alias and prefer the newer `stable_pml_faces` / `absorber_faces` tags when family-specific behavior matters.
+- Later mode-ABC work should extend the new `abc_faces` / staged-ABC seam rather than folding mode-aware behavior into the generic halo transform path; the current deferral is specifically about mode-solver dependency, not about the ABC stage layout itself.
diff --git a/src/autofdtd/__init__.py b/src/autofdtd/__init__.py
index 580ce88029cf533a99eb810ea84d6896b03f12ce..478641bc8fc6e36f8844d87d54ab41236fb72a5f
--- a/src/autofdtd/__init__.py
+++ b/src/autofdtd/__init__.py
@@ -1,8 +1,11 @@
 """AutoFDTD package scaffold for Phase 1 Maxwell solver work."""
 
 from autofdtd.api import (
+    ABCBoundary,
     AnisotropicMedium,
     Box,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     CustomAnisotropicMedium,
@@ -16,6 +19,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -62,8 +66,11 @@
     "FeatureStatus",
     "NamespacePlan",
     "AutoFDTDModel",
+    "ABCBoundary",
     "AnisotropicMedium",
     "Box",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "CustomAnisotropicMedium",
@@ -79,6 +86,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/__init__.py b/src/autofdtd/api/__init__.py
index cd0962c0c5f7e4e32bf77a6efc82731b4de56b42..61a83219d04f8e7a9749040132820553df09f6b6
--- a/src/autofdtd/api/__init__.py
+++ b/src/autofdtd/api/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.api.simulation import (
     AbstractSubpixelPolicy,
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     AnisotropicMedium,
@@ -10,6 +11,8 @@
     AutoGrid,
     Box,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     ClipOperation,
@@ -31,6 +34,7 @@
     LossyMetalMedium,
     Medium,
     Medium2D,
+    ModeABCBoundary,
     PECMedium,
     PECBoundary,
     PML,
@@ -66,12 +70,15 @@
     "AutoFDTDModel",
     "AutoFDTDValidationWarning",
     "AbstractSubpixelPolicy",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "AnisotropicMedium",
     "AutoGrid",
     "Box",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundarySpec",
     "ClipOperation",
@@ -93,6 +100,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/api/simulation.py b/src/autofdtd/api/simulation.py
index 7fe38b2b812610b6659ec31871901c9e24df6926..9df25bb61517112a2dac5768ed938ac8dc4a2752
--- a/src/autofdtd/api/simulation.py
+++ b/src/autofdtd/api/simulation.py
@@ -10,11 +10,15 @@
 from autofdtd.core.models import AutoFDTDModel, TaggedModel
 from autofdtd.core.validation import AutoFDTDValidationWarning, UnsupportedFeatureError
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -74,9 +78,12 @@
     "AbstractSubpixelPolicy",
     "AutoGrid",
     "AnisotropicMedium",
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
     "BlochBoundary",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Box",
     "Boundary",
     "BoundarySpec",
@@ -101,6 +108,7 @@
     "LossyMetalMedium",
     "Medium",
     "Medium2D",
+    "ModeABCBoundary",
     "PECMedium",
     "PECBoundary",
     "PML",
diff --git a/src/autofdtd/boundaries/__init__.py b/src/autofdtd/boundaries/__init__.py
index 4458d00c00e8c84d254b5e3e5f63cb5637fb220e..9c38bfd7efe248c4dabef93308c84a3790ba5d9b
--- a/src/autofdtd/boundaries/__init__.py
+++ b/src/autofdtd/boundaries/__init__.py
@@ -1,8 +1,11 @@
 """Boundary-family models for the foundational Phase 1 surface."""
 
 from autofdtd.boundaries.models import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundaryEdge,
     BoundarySpec,
@@ -10,6 +13,7 @@
     DEFAULT_ABSORBER_PARAMS,
     DEFAULT_PML_PARAMS,
     DEFAULT_STABLE_PML_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -22,8 +26,11 @@
 )
 
 __all__ = [
+    "ABCBoundary",
     "Absorber",
     "AbsorberParams",
+    "BroadbandModeABCFitterParam",
+    "BroadbandModeABCSpec",
     "Boundary",
     "BoundaryEdge",
     "BoundarySpec",
@@ -31,6 +38,7 @@
     "DEFAULT_ABSORBER_PARAMS",
     "DEFAULT_PML_PARAMS",
     "DEFAULT_STABLE_PML_PARAMS",
+    "ModeABCBoundary",
     "PECBoundary",
     "PML",
     "PMLParams",
diff --git a/src/autofdtd/boundaries/models.py b/src/autofdtd/boundaries/models.py
index 830631745400653084a082cd301c393b1acd54ca..3d32197b5ef739a1d0eff3dfd5f97c54d04f98cf
--- a/src/autofdtd/boundaries/models.py
+++ b/src/autofdtd/boundaries/models.py
@@ -5,11 +5,12 @@
 import cmath
 import warnings
 from collections.abc import Mapping
-from typing import Literal
+from typing import Any, Literal
 
 from pydantic import Field, field_validator, model_validator
 
 from autofdtd.core.models import TaggedModel
+from autofdtd.geometry.primitives import Box
 
 
 def _normalize_optional_name(value: str | None) -> str | None:
@@ -63,6 +64,139 @@
     type: Literal["PMCBoundary"] = "PMCBoundary"
 
 
+class ABCBoundary(BoundaryEdge):
+    """First-order absorbing boundary condition with explicit effective medium inputs."""
+
+    type: Literal["ABCBoundary"] = "ABCBoundary"
+    permittivity: float | None = None
+    conductivity: float | None = None
+
+    @field_validator("permittivity")
+    @classmethod
+    def _validate_permittivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 1.0:
+            raise ValueError("ABCBoundary permittivity must be at least 1.0 when provided")
+        return normalized
+
+    @field_validator("conductivity")
+    @classmethod
+    def _validate_conductivity(cls, value: float | None) -> float | None:
+        if value is None:
+            return None
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("ABCBoundary conductivity must be non-negative when provided")
+        return normalized
+
+    @model_validator(mode="after")
+    def _validate_parameter_pair(self) -> ABCBoundary:
+        if self.conductivity is not None and self.permittivity is None:
+            raise ValueError(
+                "ABCBoundary conductivity can only be provided together with permittivity"
+            )
+        return self
+
+
+class BroadbandModeABCFitterParam(TaggedModel):
+    """Deferred fit-parameter surface for broadband mode-ABC configuration."""
+
+    type: Literal["BroadbandModeABCFitterParam"] = "BroadbandModeABCFitterParam"
+    max_num_poles: int = 4
+    tolerance_rms: float = 1.0e-4
+    frequency_sampling_points: int = 11
+
+    @field_validator("max_num_poles", "frequency_sampling_points")
+    @classmethod
+    def _validate_positive_int(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 1:
+            raise ValueError("Broadband mode-ABC fit counts must be positive")
+        return normalized
+
+    @field_validator("tolerance_rms")
+    @classmethod
+    def _validate_non_negative_float(cls, value: float) -> float:
+        normalized = float(value)
+        if normalized < 0.0:
+            raise ValueError("Broadband mode-ABC fit tolerance must be non-negative")
+        return normalized
+
+
+class BroadbandModeABCSpec(TaggedModel):
+    """Deferred broadband fitting surface for mode-based absorbing boundaries."""
+
+    type: Literal["BroadbandModeABCSpec"] = "BroadbandModeABCSpec"
+    frequency_range: tuple[float, float]
+    fit_param: BroadbandModeABCFitterParam = Field(default_factory=BroadbandModeABCFitterParam)
+
+    @field_validator("frequency_range", mode="before")
+    @classmethod
+    def _validate_frequency_range(cls, value: object) -> tuple[float, float]:
+        if not isinstance(value, tuple | list) or len(value) != 2:
+            raise TypeError("frequency_range must be a 2-tuple of positive frequencies")
+        lower = float(value[0])
+        upper = float(value[1])
+        if lower <= 0.0 or upper <= 0.0:
+            raise ValueError("frequency_range values must be positive")
+        if upper <= lower:
+            raise ValueError("frequency_range must be strictly increasing")
+        return (lower, upper)
+
+
+class ModeABCBoundary(BoundaryEdge):
+    """Deferred mode-absorbing boundary surface that depends on the future mode solver."""
+
+    type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    mode_spec: dict[str, Any] = Field(
+        default_factory=lambda: {"type": "ModeSpec", "num_modes": 1}
+    )
+    mode_index: int = 0
+    freq_spec: float | BroadbandModeABCSpec | None = None
+    plane: Box
+
+    @field_validator("mode_index")
+    @classmethod
+    def _validate_mode_index(cls, value: int) -> int:
+        normalized = int(value)
+        if normalized < 0:
+            raise ValueError("ModeABCBoundary mode_index must be non-negative")
+        return normalized
+
+    @field_validator("freq_spec")
+    @classmethod
+    def _validate_freq_spec(
+        cls, value: float | BroadbandModeABCSpec | Mapping[str, Any] | None
+    ) -> float | BroadbandModeABCSpec | None:
+        if value is None or isinstance(value, BroadbandModeABCSpec):
+            return value
+        if isinstance(value, Mapping):
+            return BroadbandModeABCSpec(**value)
+        normalized = float(value)
+        if normalized <= 0.0:
+            raise ValueError("ModeABCBoundary freq_spec must be positive when scalar")
+        return normalized
+
+    @field_validator("plane")
+    @classmethod
+    def _validate_plane(cls, value: Box | Mapping[str, Any]) -> Box:
+        plane = value if isinstance(value, Box) else Box(**value)
+        if plane.size.count(0.0) != 1:
+            raise ValueError(
+                f"ModeABCBoundary plane must be planar with one zero extent, got size={plane.size}"
+            )
+        return plane
+
+    @property
+    def phase1_policy(self) -> str:
+        return (
+            "ModeABCBoundary is parsed and serialized, but runtime compilation is deferred in "
+            "Phase 1 until the mode solver and mode-source surfaces exist."
+        )
+
+
 class PMLParams(TaggedModel):
     """Polynomial profile parameters for the Phase 1 baseline PML."""
 
@@ -236,14 +370,35 @@
 
 
 ImplementedBoundaryEdge = (
-    Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber
+    Periodic
+    | BlochBoundary
+    | PECBoundary
+    | PMCBoundary
+    | ABCBoundary
+    | ModeABCBoundary
+    | PML
+    | StablePML
+    | Absorber
 )
 
 
 def boundary_edge_model_from_value(value: object) -> ImplementedBoundaryEdge:
     """Normalize a supported Phase 1 non-absorbing boundary edge."""
 
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            ModeABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+        ),
+    ):
         return value
     if not isinstance(value, Mapping):
         raise TypeError(f"expected a boundary-edge mapping, got {type(value)!r}")
@@ -256,6 +411,10 @@
         return PECBoundary(**value)
     if boundary_type == "PMCBoundary":
         return PMCBoundary(**value)
+    if boundary_type == "ABCBoundary":
+        return ABCBoundary(**value)
+    if boundary_type == "ModeABCBoundary":
+        return ModeABCBoundary(**value)
     if boundary_type == "PML":
         return PML(**value)
     if boundary_type == "StablePML":
@@ -275,7 +434,20 @@
     @field_validator("plus", "minus")
     @classmethod
     def _validate_edge(cls, value: object) -> object:
-        if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+        if isinstance(
+            value,
+            (
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
+        ):
             return value
         if isinstance(value, Mapping):
             boundary_type = str(value.get("type"))
@@ -284,6 +456,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -358,6 +532,33 @@
         return cls(plus=PMCBoundary(), minus=PMCBoundary())
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> Boundary:
+        abc = ABCBoundary(permittivity=permittivity, conductivity=conductivity)
+        return cls(plus=abc, minus=abc)
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> Boundary:
+        boundary = ModeABCBoundary(
+            plane=plane,
+            mode_spec={"type": "ModeSpec", "num_modes": 1} if mode_spec is None else mode_spec,
+            mode_index=mode_index,
+            freq_spec=freq_spec,
+        )
+        return cls(plus=boundary, minus=boundary)
+
+    @classmethod
     def pml(
         cls,
         *,
@@ -429,7 +630,18 @@
     def _coerce_axis_boundary(cls, value: object) -> object:
         if isinstance(
             value,
-            (Boundary, Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber),
+            (
+                Boundary,
+                Periodic,
+                BlochBoundary,
+                PECBoundary,
+                PMCBoundary,
+                ABCBoundary,
+                ModeABCBoundary,
+                PML,
+                StablePML,
+                Absorber,
+            ),
         ):
             return value
         if isinstance(value, Mapping):
@@ -440,6 +652,8 @@
                 "BlochBoundary",
                 "PECBoundary",
                 "PMCBoundary",
+                "ABCBoundary",
+                "ModeABCBoundary",
                 "PML",
                 "StablePML",
                 "Absorber",
@@ -494,6 +708,79 @@
         )
 
     @classmethod
+    def abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        permittivity: float | None = None,
+        conductivity: float | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.abc(permittivity=permittivity, conductivity=conductivity)
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
+    def mode_abc(
+        cls,
+        *,
+        x: bool = False,
+        y: bool = False,
+        z: bool = False,
+        plane: Box,
+        mode_spec: dict[str, Any] | None = None,
+        mode_index: int = 0,
+        freq_spec: float | BroadbandModeABCSpec | None = None,
+    ) -> BoundarySpec:
+        return cls(
+            x=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if x
+                else Boundary.periodic()
+            ),
+            y=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if y
+                else Boundary.periodic()
+            ),
+            z=(
+                Boundary.mode_abc(
+                    plane=plane,
+                    mode_spec=mode_spec,
+                    mode_index=mode_index,
+                    freq_spec=freq_spec,
+                )
+                if z
+                else Boundary.periodic()
+            ),
+        )
+
+    @classmethod
     def pml(
         cls,
         *,
diff --git a/src/autofdtd/compiler/__init__.py b/src/autofdtd/compiler/__init__.py
index c5df6aee568a2b1684cec252fe6431f256ab86d6..868917a476cd26804cd6166b8ff9bfb6e46fd4b0
--- a/src/autofdtd/compiler/__init__.py
+++ b/src/autofdtd/compiler/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledPMLCoefficients,
@@ -35,6 +36,7 @@
 
 __all__ = [
     "BoundaryMode",
+    "CompiledABCCoefficients",
     "CompiledBoundaryAxis",
     "CompiledBoundaryEdge",
     "CompiledPMLCoefficients",
diff --git a/src/autofdtd/compiler/boundaries.py b/src/autofdtd/compiler/boundaries.py
index 9e9bb479da89610437f887bec99075ee7793c6be..9e96ff93bd016554eec57ded4b87ced6d370fd2b
--- a/src/autofdtd/compiler/boundaries.py
+++ b/src/autofdtd/compiler/boundaries.py
@@ -7,13 +7,16 @@
 from typing import Literal
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
     DEFAULT_PML_PARAMS,
     DEFAULT_ABSORBER_PARAMS,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -34,11 +37,31 @@
     BLOCH = "bloch"
     PEC = "pec"
     PMC = "pmc"
+    ABC = "abc"
     PML = "pml"
     STABLE_PML = "stable_pml"
     ABSORBER = "absorber"
 
 
+C0 = 299_792_458.0
+EPSILON_0 = 8.854_187_812_8e-12
+
+
+class CompiledABCCoefficients(AutoFDTDModel):
+    """Prepared first-order absorbing-boundary coefficients for one face."""
+
+    type: str = "CompiledABCCoefficients"
+    boundary_type: Literal["ABCBoundary"] = "ABCBoundary"
+    reflection_coefficient: float
+    attenuation: float
+    effective_permittivity: float
+    effective_conductivity: float
+    wave_speed: float
+    dt: float
+    grid_spacing: float
+    requires_material_inference: bool = False
+
+
 class CompiledPMLCoefficients(AutoFDTDModel):
     """Prepared per-layer damping and memory coefficients for one PML face."""
 
@@ -69,6 +92,7 @@
     magnetic_signs: tuple[int, int, int]
     source_side: Literal["minus", "plus"]
     phase_factor: complex = 1.0 + 0.0j
+    abc: CompiledABCCoefficients | None = None
     pml: CompiledPMLCoefficients | None = None
 
 
@@ -79,7 +103,8 @@
     axis: Literal["x", "y", "z"]
     minus: CompiledBoundaryEdge
     plus: CompiledBoundaryEdge
-    exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"]
+    exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"]
+    abc_faces: tuple[Literal["minus", "plus"], ...] = ()
     pml_faces: tuple[Literal["minus", "plus"], ...] = ()
 
 
@@ -103,6 +128,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -132,6 +159,13 @@
         return BoundaryMode.PEC
     if isinstance(edge, PMCBoundary):
         return BoundaryMode.PMC
+    if isinstance(edge, ABCBoundary):
+        return BoundaryMode.ABC
+    if isinstance(edge, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
     if isinstance(edge, PML):
         return BoundaryMode.PML
     if isinstance(edge, StablePML):
@@ -204,6 +238,42 @@
     return minimum + (maximum - minimum) * (fraction**order)
 
 
+def _compile_abc_coefficients(
+    edge: ABCBoundary,
+    *,
+    dt: float,
+    grid_spacing: float,
+) -> CompiledABCCoefficients:
+    if dt <= 0.0:
+        raise ValueError("ABC coefficient preparation requires dt > 0")
+    if grid_spacing <= 0.0:
+        raise ValueError("ABC coefficient preparation requires a positive grid spacing")
+    if edge.permittivity is None:
+        raise ValueError(
+            "ABCBoundary runtime compilation currently requires an explicit permittivity; "
+            "automatic boundary-medium inference is deferred in Phase 1"
+        )
+    effective_permittivity = float(edge.permittivity)
+    effective_conductivity = float(edge.conductivity or 0.0)
+    wave_speed = C0 / math.sqrt(effective_permittivity)
+    courant = wave_speed * dt / grid_spacing
+    reflection_coefficient = (courant - 1.0) / (courant + 1.0)
+    reflection_coefficient = max(-0.999_999, min(0.999_999, reflection_coefficient))
+    attenuation = math.exp(
+        -effective_conductivity * dt / max(2.0 * EPSILON_0 * effective_permittivity, 1.0e-30)
+    )
+    return CompiledABCCoefficients(
+        reflection_coefficient=reflection_coefficient,
+        attenuation=attenuation,
+        effective_permittivity=effective_permittivity,
+        effective_conductivity=effective_conductivity,
+        wave_speed=wave_speed,
+        dt=dt,
+        grid_spacing=grid_spacing,
+        requires_material_inference=False,
+    )
+
+
 def _compile_pml_coefficients(
     edge: PML | StablePML,
     *,
@@ -314,8 +384,16 @@
     axis_index = "xyz".index(axis_name)
     mode = _mode_for_edge(edge)
     electric_signs, magnetic_signs = _reflection_signs(mode, axis_index)
+    abc = None
     pml = None
     normalized = boundary_edge_model_from_value(edge)
+    if isinstance(normalized, ModeABCBoundary):
+        raise ValueError(
+            "ModeABCBoundary parsing is available, but runtime compilation is deferred in "
+            "Phase 1 until mode-solver work lands"
+        )
+    if isinstance(normalized, ABCBoundary):
+        abc = _compile_abc_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     if isinstance(normalized, (PML, StablePML)):
         pml = _compile_pml_coefficients(normalized, dt=dt, grid_spacing=grid_spacing)
     elif isinstance(normalized, Absorber):
@@ -328,6 +406,7 @@
         magnetic_signs=magnetic_signs,
         source_side="plus" if side == "minus" else "minus",
         phase_factor=_phase_factor_for_edge(edge, side=side),
+        abc=abc,
         pml=pml,
     )
 
@@ -343,15 +422,20 @@
     plus = _compile_edge(axis_name, "plus", boundary.plus, dt=dt, grid_spacing=grid_spacing)
     edge_modes = {minus.mode, plus.mode}
     if edge_modes == {BoundaryMode.PERIODIC}:
-        exchange_kind: Literal["periodic", "bloch", "reflective", "pml", "mixed"] = "periodic"
+        exchange_kind: Literal["periodic", "bloch", "reflective", "abc", "pml", "mixed"] = "periodic"
     elif edge_modes == {BoundaryMode.BLOCH}:
         exchange_kind = "bloch"
+    elif edge_modes == {BoundaryMode.ABC}:
+        exchange_kind = "abc"
     elif edge_modes <= {BoundaryMode.PML, BoundaryMode.STABLE_PML, BoundaryMode.ABSORBER}:
         exchange_kind = "pml"
     elif edge_modes <= {BoundaryMode.PEC, BoundaryMode.PMC}:
         exchange_kind = "reflective"
     else:
         exchange_kind = "mixed"
+    abc_faces = tuple(
+        side for side, compiled in (("minus", minus), ("plus", plus)) if compiled.mode is BoundaryMode.ABC
+    )
     pml_faces = tuple(
         side
         for side, compiled in (("minus", minus), ("plus", plus))
@@ -362,6 +446,7 @@
         minus=minus,
         plus=plus,
         exchange_kind=exchange_kind,
+        abc_faces=abc_faces,
         pml_faces=pml_faces,
     )
 
@@ -402,8 +487,8 @@
         except Exception as exc:  # noqa: BLE001
             raise ValueError(
                 "Phase 1 runtime boundary compilation currently supports only BoundarySpec with "
-                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, and "
-                "Absorber entries"
+                "Periodic, BlochBoundary, PECBoundary, PMCBoundary, ABCBoundary, PML, "
+                "StablePML, and Absorber entries, while ModeABCBoundary remains deferred"
             ) from exc
 
     if isinstance(grid_spacing, tuple):
@@ -433,6 +518,12 @@
         for axis in compiled_axes.values()
         if axis.exchange_kind in {"reflective", "mixed"} or axis.pml_faces
     )
+    abc_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.abc_faces)
+    abc_faces = tuple(
+        f"{axis.axis}.{side}"
+        for axis in compiled_axes.values()
+        for side in axis.abc_faces
+    )
     pml_axes = tuple(axis.axis for axis in compiled_axes.values() if axis.pml_faces)
     pml_faces = tuple(
         f"{axis.axis}.{side}"
@@ -465,6 +556,8 @@
     )
     symmetry_axes = _compile_symmetry_axes(symmetry)
     stage_order = ["electric_boundary", "magnetic_boundary"]
+    if abc_faces:
+        stage_order.extend(("electric_abc", "magnetic_abc"))
     if pml_faces:
         stage_order.extend(("electric_pml", "magnetic_pml"))
     return CompiledBoundarySpec(
@@ -474,6 +567,8 @@
         periodic_axes=periodic_axes,
         bloch_axes=bloch_axes,
         reflective_axes=reflective_axes,
+        abc_axes=abc_axes,
+        abc_faces=abc_faces,
         pml_axes=pml_axes,
         pml_faces=pml_faces,
         stable_pml_axes=stable_pml_axes,
diff --git a/src/autofdtd/core/validation.py b/src/autofdtd/core/validation.py
index 5cea4be944906b2f933855c94a911358d531a5db..36883a94160e67026910706ee77b8b74d0a5a05b
--- a/src/autofdtd/core/validation.py
+++ b/src/autofdtd/core/validation.py
@@ -10,10 +10,14 @@
 from pydantic import BaseModel
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     BlochBoundary,
+    BroadbandModeABCFitterParam,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMCBoundary,
@@ -83,6 +87,10 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
+        "ModeABCBoundary",
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
         "PML",
         "PMLParams",
         "StablePML",
@@ -368,6 +376,11 @@
         return feature_entry("PML")
     if context == "boundary_spec" and feature_name == "AbsorberParams":
         return feature_entry("Absorber")
+    if context == "boundary_spec" and feature_name in {
+        "BroadbandModeABCSpec",
+        "BroadbandModeABCFitterParam",
+    }:
+        return feature_entry("ModeABCBoundary")
     if context in {"medium", "background_medium", "scene.medium"}:
         if feature_name.startswith("Custom"):
             return feature_entry("CustomMedia")
diff --git a/src/autofdtd/examples/__init__.py b/src/autofdtd/examples/__init__.py
index 60620bc09fcfcefc2bb2528fc8bd2f3ca901f54a..12d99670beaa9ffae0dccb5fd6eac50cf011afe6
--- a/src/autofdtd/examples/__init__.py
+++ b/src/autofdtd/examples/__init__.py
@@ -1,6 +1,10 @@
 """Validation example and benchmark namespace."""
 
-from autofdtd.examples.boundaries import boundary_runtime_snapshot, pml_absorption_snapshot
+from autofdtd.examples.boundaries import (
+    abc_boundary_snapshot,
+    boundary_runtime_snapshot,
+    pml_absorption_snapshot,
+)
 from autofdtd.examples.composite_geometry import rotated_ring_resonator
 from autofdtd.examples.geometry_array import rotated_post_array_scene
 from autofdtd.examples.grids import autogrid_example, grid_spec_examples, resolved_grid_example
@@ -14,6 +18,7 @@
 from autofdtd.examples.subpixel import build_subpixel_demo
 
 __all__ = [
+    "abc_boundary_snapshot",
     "boundary_runtime_snapshot",
     "pml_absorption_snapshot",
     "directional_coupler_scene",
diff --git a/src/autofdtd/examples/boundaries.py b/src/autofdtd/examples/boundaries.py
index 59eefd36da3c16986bd78390d1b7453eafd3069c..ca5c795f42b9768e1c46ab669a70cdd6861e80f2
--- a/src/autofdtd/examples/boundaries.py
+++ b/src/autofdtd/examples/boundaries.py
@@ -6,7 +6,7 @@
 
 from autofdtd.api import AbsorberParams, Boundary, BoundarySpec
 from autofdtd.compiler import compile_boundary_spec
-from autofdtd.runtime import build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, build_boundary_runtime
 
 
 def boundary_runtime_snapshot() -> dict[str, object]:
@@ -67,3 +67,30 @@
         "absorber_faces": compiled.absorber_faces,
         "y_minus_boundary_type": compiled.y.minus.pml.boundary_type if compiled.y.minus.pml else None,
     }
+
+
+def abc_boundary_snapshot() -> dict[str, object]:
+    """Return a compact snapshot for the implemented first-order ABC subset."""
+
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 4, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 4, 4, 3), dtype=np.float64)
+    electric[1, 1:3, 1:3, 0] = 1.0
+    runtime = build_boundary_runtime(
+        BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        dt=1.0e-12,
+        grid_spacing=(5.0e-7, 5.0e-7, 5.0e-7),
+        field_shape=electric.shape,
+    )
+    updated_electric, _ = runtime.apply(electric, np.zeros_like(electric))
+    return {
+        "abc_faces": compiled.abc_faces,
+        "stage_order": compiled.stage_order,
+        "reflection_coefficient": compiled.x.minus.abc.reflection_coefficient if compiled.x.minus.abc else None,
+        "state_faces": tuple(sorted(state.electric)),
+        "ghost_sample": float(updated_electric[0, 1, 1, 0]),
+    }
diff --git a/src/autofdtd/ir/__init__.py b/src/autofdtd/ir/__init__.py
index 758954ee35864eb2b2af282f04b2e10ac7a5641a..d030fcbcf2fb0c0bb4ada43e3f7e10f1a6859fff
--- a/src/autofdtd/ir/__init__.py
+++ b/src/autofdtd/ir/__init__.py
@@ -3,6 +3,7 @@
 from autofdtd.ir.models import (
     EXECUTION_PACKAGE_FORMAT,
     IR_SCHEMA_VERSION,
+    ABCBoundaryIR,
     AbsorberIR,
     AbsorberParamsIR,
     AnisotropicMediumIR,
@@ -31,6 +32,7 @@
     LorentzIR,
     MediumComponentIR,
     MediumIR,
+    ModeABCBoundaryIR,
     PECBoundaryIR,
     PECMediumIR,
     PMLIR,
@@ -73,6 +75,7 @@
 __all__ = [
     "EXECUTION_PACKAGE_FORMAT",
     "IR_SCHEMA_VERSION",
+    "ABCBoundaryIR",
     "AbsorberIR",
     "AbsorberParamsIR",
     "AnisotropicMediumIR",
@@ -103,6 +106,7 @@
     "LorentzIR",
     "MediumComponentIR",
     "MediumIR",
+    "ModeABCBoundaryIR",
     "PECBoundaryIR",
     "PECMediumIR",
     "PMLIR",
diff --git a/src/autofdtd/ir/models.py b/src/autofdtd/ir/models.py
index 3b3ce73e6477ff20d19bed1ed91654b787145c5b..7dbce3f204b39cc03a1f06665606499e1a10ad68
--- a/src/autofdtd/ir/models.py
+++ b/src/autofdtd/ir/models.py
@@ -11,11 +11,14 @@
 from pydantic import BaseModel, Field, field_validator
 
 from autofdtd.boundaries import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PML,
     PMLParams,
     PECBoundary,
@@ -411,6 +414,18 @@
     name: str | None = None
 
 
+class ABCBoundaryIR(IRModel):
+    """Typed execution IR for a first-order absorbing boundary edge."""
+
+    type: Literal["ABCBoundaryIR"] = "ABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ABCBoundary"] = "ABCBoundary"
+    name: str | None = None
+    permittivity: float | None = None
+    conductivity: float | None = None
+    requires_material_inference: bool = False
+
+
 class PMLParamsIR(IRModel):
     """Typed execution IR for baseline PML profile parameters."""
 
@@ -471,6 +486,20 @@
     extrude_structures: bool = False
 
 
+class ModeABCBoundaryIR(IRModel):
+    """Typed execution IR for the deferred mode-absorbing boundary surface."""
+
+    type: Literal["ModeABCBoundaryIR"] = "ModeABCBoundaryIR"
+    family: Literal["boundary"] = "boundary"
+    component_type: Literal["ModeABCBoundary"] = "ModeABCBoundary"
+    name: str | None = None
+    mode_spec: dict[str, Any]
+    mode_index: int
+    freq_spec: float | dict[str, Any] | None = None
+    plane: BoxIR
+    phase1_policy: str
+
+
 class SymmetryAxisIR(IRModel):
     """Typed execution IR for one mirror-symmetry axis."""
 
@@ -486,9 +515,11 @@
     | BlochBoundaryIR
     | PECBoundaryIR
     | PMCBoundaryIR
+    | ABCBoundaryIR
     | PMLIR
     | StablePMLIR
     | AbsorberIR
+    | ModeABCBoundaryIR
     | ComponentIR
 )
 
@@ -516,6 +547,8 @@
     periodic_axes: tuple[str, ...] = ()
     bloch_axes: tuple[str, ...] = ()
     reflective_axes: tuple[str, ...] = ()
+    abc_axes: tuple[str, ...] = ()
+    abc_faces: tuple[str, ...] = ()
     pml_axes: tuple[str, ...] = ()
     pml_faces: tuple[str, ...] = ()
     stable_pml_axes: tuple[str, ...] = ()
@@ -1005,17 +1038,43 @@
 def boundary_edge_to_ir(value: object) -> BoundaryEdgeIR:
     """Lower a supported boundary edge into typed execution IR."""
 
-    edge: Periodic | BlochBoundary | PECBoundary | PMCBoundary | PML | StablePML | Absorber | None = None
-    if isinstance(value, (Periodic, BlochBoundary, PECBoundary, PMCBoundary, PML, StablePML, Absorber)):
+    edge: (
+        Periodic
+        | BlochBoundary
+        | PECBoundary
+        | PMCBoundary
+        | ABCBoundary
+        | PML
+        | StablePML
+        | Absorber
+        | ModeABCBoundary
+        | None
+    ) = None
+    if isinstance(
+        value,
+        (
+            Periodic,
+            BlochBoundary,
+            PECBoundary,
+            PMCBoundary,
+            ABCBoundary,
+            PML,
+            StablePML,
+            Absorber,
+            ModeABCBoundary,
+        ),
+    ):
         edge = value
     elif isinstance(value, Mapping) and str(value.get("type")) in {
         "Periodic",
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         edge = boundary_edge_model_from_value(value)
 
@@ -1032,6 +1091,13 @@
         return PECBoundaryIR(name=edge.name)
     if isinstance(edge, PMCBoundary):
         return PMCBoundaryIR(name=edge.name)
+    if isinstance(edge, ABCBoundary):
+        return ABCBoundaryIR(
+            name=edge.name,
+            permittivity=edge.permittivity,
+            conductivity=edge.conductivity,
+            requires_material_inference=edge.permittivity is None,
+        )
     if isinstance(edge, PML):
         params = (
             edge.parameters
@@ -1092,6 +1158,20 @@
             ),
             extrude_structures=edge.extrude_structures,
         )
+    if isinstance(edge, ModeABCBoundary):
+        freq_payload: float | dict[str, Any] | None
+        if isinstance(edge.freq_spec, BroadbandModeABCSpec):
+            freq_payload = edge.freq_spec.model_dump(mode="python", exclude_none=True)
+        else:
+            freq_payload = edge.freq_spec
+        return ModeABCBoundaryIR(
+            name=edge.name,
+            mode_spec=dict(edge.mode_spec),
+            mode_index=edge.mode_index,
+            freq_spec=freq_payload,
+            plane=geometry_to_ir(edge.plane),
+            phase1_policy=edge.phase1_policy,
+        )
     return component_to_ir(value, family=ComponentFamily.BOUNDARY)
 
 
@@ -1107,9 +1187,11 @@
         "BlochBoundary",
         "PECBoundary",
         "PMCBoundary",
+        "ABCBoundary",
         "PML",
         "StablePML",
         "Absorber",
+        "ModeABCBoundary",
     }:
         boundary = boundary_model_from_value(value)
     if boundary is None:
@@ -1172,6 +1254,8 @@
     periodic_axes: list[str] = []
     bloch_axes: list[str] = []
     reflective_axes: list[str] = []
+    abc_axes: list[str] = []
+    abc_faces: list[str] = []
     pml_axes: list[str] = []
     pml_faces: list[str] = []
     stable_pml_axes: list[str] = []
@@ -1191,6 +1275,8 @@
         elif edge_types == {"BlochBoundary"}:
             bloch_axes.append(axis_name)
             requires_complex_fields = True
+        elif edge_types == {"ABCBoundary"}:
+            abc_axes.append(axis_name)
         elif edge_types <= {"PML", "StablePML", "Absorber"}:
             pml_axes.append(axis_name)
             if lowered.minus.component_type in {"PML", "StablePML", "Absorber"}:
@@ -1209,6 +1295,13 @@
             ) and axis_name not in pml_axes:
                 pml_axes.append(axis_name)
             reflective_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary" or lowered.plus.component_type == "ABCBoundary":
+            if axis_name not in abc_axes:
+                abc_axes.append(axis_name)
+        if lowered.minus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.minus")
+        if lowered.plus.component_type == "ABCBoundary":
+            abc_faces.append(f"{axis_name}.plus")
         if lowered.minus.component_type == "StablePML" or lowered.plus.component_type == "StablePML":
             stable_pml_axes.append(axis_name)
         if lowered.minus.component_type == "StablePML":
@@ -1230,6 +1323,8 @@
         periodic_axes=tuple(periodic_axes),
         bloch_axes=tuple(bloch_axes),
         reflective_axes=tuple(reflective_axes),
+        abc_axes=tuple(abc_axes),
+        abc_faces=tuple(abc_faces),
         pml_axes=tuple(pml_axes),
         pml_faces=tuple(pml_faces),
         stable_pml_axes=tuple(stable_pml_axes),
diff --git a/src/autofdtd/kernels/__init__.py b/src/autofdtd/kernels/__init__.py
index 61cf26138ce4f8d5c7099681e66751b74cc559d0..3df503baf30c8077c05180edafeb248b68fc1379
--- a/src/autofdtd/kernels/__init__.py
+++ b/src/autofdtd/kernels/__init__.py
@@ -1,9 +1,12 @@
 """Warp kernel conventions and staged update namespace."""
 
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     WARP_AVAILABLE as BOUNDARY_WARP_AVAILABLE,
     PMLBoundaryState,
     PMLFaceState,
+    apply_abc_layers,
     apply_axis_boundary_ghosts,
     apply_boundary_ghosts,
     apply_boundary_stages,
@@ -28,12 +31,15 @@
 )
 
 __all__ = [
+    "ABCBoundaryState",
+    "ABCFaceState",
     "AnisotropicAuxiliaryState",
     "BOUNDARY_WARP_AVAILABLE",
     "PMLBoundaryState",
     "PMLFaceState",
     "PoleResidueAuxiliaryState",
     "WARP_AVAILABLE",
+    "apply_abc_layers",
     "allocate_anisotropic_state",
     "allocate_pole_residue_state",
     "apply_axis_boundary_ghosts",
diff --git a/src/autofdtd/kernels/boundaries.py b/src/autofdtd/kernels/boundaries.py
index 873a5b5e5ac5868b6774e3d7a3308db4654340b8..18f59fc2724795dc008a63946dcd9d8d3bde190b
--- a/src/autofdtd/kernels/boundaries.py
+++ b/src/autofdtd/kernels/boundaries.py
@@ -8,6 +8,7 @@
 
 from autofdtd.compiler.boundaries import (
     BoundaryMode,
+    CompiledABCCoefficients,
     CompiledBoundaryAxis,
     CompiledBoundaryEdge,
     CompiledBoundarySpec,
@@ -46,6 +47,29 @@
     model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
 
 
+class ABCFaceState(AutoFDTDModel):
+    """Persistent per-face state for first-order ABC ghost updates."""
+
+    type: str = "ABCFaceState"
+    axis: Literal["x", "y", "z"]
+    side: Literal["minus", "plus"]
+    value_shape: tuple[int, ...]
+    previous_boundary: np.ndarray
+    previous_adjacent: np.ndarray
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
+class ABCBoundaryState(AutoFDTDModel):
+    """Persistent state bundle for all active ABC faces."""
+
+    type: str = "ABCBoundaryState"
+    electric: dict[str, ABCFaceState]
+    magnetic: dict[str, ABCFaceState]
+
+    model_config = AutoFDTDModel.model_config | {"arbitrary_types_allowed": True}
+
+
 def _ghost_and_source_slices(
     axis: int, side: str, *, ndim: int
 ) -> tuple[tuple[object, ...], tuple[object, ...]]:
@@ -96,7 +120,7 @@
         ghost, source = _ghost_and_source_slices(axis_index, side, ndim=field.ndim)
         field[ghost] = field[source] * field.dtype.type(phase_factor)
         return
-    if mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return
     ghost, source = _interior_reflection_slices(axis_index, side, ndim=field.ndim)
     field[ghost] = field[source] * np.asarray(signs, dtype=field.dtype)
@@ -146,6 +170,39 @@
     field[region] = region_values * attenuation * stretch
 
 
+def _abc_state_slices(
+    axis_index: int, side: Literal["minus", "plus"], *, ndim: int
+) -> tuple[tuple[object, ...], tuple[object, ...]]:
+    ghost = [slice(None)] * ndim
+    adjacent = [slice(None)] * ndim
+    if side == "minus":
+        ghost[axis_index] = 0
+        adjacent[axis_index] = 1
+    else:
+        ghost[axis_index] = -1
+        adjacent[axis_index] = -2
+    return tuple(ghost), tuple(adjacent)
+
+
+def _apply_abc_region(
+    field: np.ndarray,
+    face_state: ABCFaceState,
+    coefficients: CompiledABCCoefficients,
+    *,
+    axis_index: int,
+    side: Literal["minus", "plus"],
+) -> None:
+    ghost, adjacent = _abc_state_slices(axis_index, side, ndim=field.ndim)
+    current_adjacent = np.array(field[adjacent], copy=True)
+    next_boundary = (
+        face_state.previous_adjacent
+        + coefficients.reflection_coefficient * (current_adjacent - face_state.previous_boundary)
+    ) * coefficients.attenuation
+    field[ghost] = next_boundary
+    face_state.previous_boundary[...] = next_boundary
+    face_state.previous_adjacent[...] = current_adjacent
+
+
 def boundary_edge_transform(
     values: np.ndarray,
     edge: CompiledBoundaryEdge,
@@ -158,7 +215,7 @@
         return np.array(values, copy=True)
     if edge.mode is BoundaryMode.BLOCH:
         return np.array(values, copy=True) * values.dtype.type(edge.phase_factor)
-    if edge.mode in {BoundaryMode.PML, BoundaryMode.STABLE_PML}:
+    if edge.mode in {BoundaryMode.ABC, BoundaryMode.PML, BoundaryMode.STABLE_PML}:
         return np.array(values, copy=True)
     signs_name = "electric_signs" if field_family == "electric" else "magnetic_signs"
     return np.array(values, copy=True) * np.asarray(getattr(edge, signs_name), dtype=values.dtype)
@@ -255,11 +312,41 @@
     return updated
 
 
+def apply_abc_layers(
+    field: np.ndarray,
+    boundary_spec: CompiledBoundarySpec,
+    state: ABCBoundaryState,
+    *,
+    field_family: Literal["electric", "magnetic"],
+) -> np.ndarray:
+    """Apply the staged first-order ABC update to one vector field family."""
+
+    updated = np.array(field, copy=True)
+    face_states = state.electric if field_family == "electric" else state.magnetic
+    for axis_boundary in boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            _apply_abc_region(
+                updated,
+                face_states[face_key],
+                edge.abc,
+                axis_index=axis_index,
+                side=side,
+            )
+    return updated
+
+
 def apply_boundary_stages(
     electric_field: np.ndarray,
     magnetic_field: np.ndarray,
     boundary_spec: CompiledBoundarySpec,
     state: PMLBoundaryState | None = None,
+    *,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply all compiled boundary stages in declared order."""
 
@@ -270,6 +357,14 @@
             electric = apply_boundary_ghosts(electric, magnetic, boundary_spec)[0]
         elif stage == "magnetic_boundary":
             magnetic = apply_boundary_ghosts(electric, magnetic, boundary_spec)[1]
+        elif stage == "electric_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            electric = apply_abc_layers(electric, boundary_spec, abc_state, field_family="electric")
+        elif stage == "magnetic_abc":
+            if abc_state is None:
+                raise ValueError("ABC boundary stages require allocated ABCBoundaryState")
+            magnetic = apply_abc_layers(magnetic, boundary_spec, abc_state, field_family="magnetic")
         elif stage == "electric_pml":
             if state is None:
                 raise ValueError("PML boundary stages require allocated PMLBoundaryState")
@@ -291,12 +386,24 @@
         "stages": (
             "electric_boundary",
             "magnetic_boundary",
+            "electric_abc",
+            "magnetic_abc",
             "electric_pml",
             "magnetic_pml",
         ),
-        "supported_modes": ("periodic", "bloch", "pec", "pmc", "pml", "stable_pml", "absorber"),
+        "supported_modes": (
+            "periodic",
+            "bloch",
+            "pec",
+            "pmc",
+            "abc",
+            "pml",
+            "stable_pml",
+            "absorber",
+        ),
         "supports_complex_fields": True,
         "supports_symmetry_transforms": True,
+        "supports_abc": True,
         "supports_pml_layers": True,
         "supports_stable_pml": True,
         "supports_absorber": True,
diff --git a/src/autofdtd/planning.py b/src/autofdtd/planning.py
index baf6020373cc05b907642b1b61cb78998ad37bfa..a522cbcef1bf7df5c5cb17c005d7469626f407ab
--- a/src/autofdtd/planning.py
+++ b/src/autofdtd/planning.py
@@ -218,8 +218,18 @@
         "Supported subset acceptable.",
     ),
     FeatureEntry("boundaries", "Absorber", FeatureStatus.IMPLEMENT, "Practical fallback."),
-    FeatureEntry("boundaries", "ABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
-    FeatureEntry("boundaries", "ModeABCBoundary", FeatureStatus.DEFER, "Mapped for later."),
+    FeatureEntry(
+        "boundaries",
+        "ABCBoundary",
+        FeatureStatus.IMPLEMENT,
+        "Supported as an explicit-parameter first-order subset.",
+    ),
+    FeatureEntry(
+        "boundaries",
+        "ModeABCBoundary",
+        FeatureStatus.DEFER,
+        "Parsed surface remains deferred until mode-solver work lands.",
+    ),
     # Sources
     FeatureEntry("sources", "GaussianPulse", FeatureStatus.IMPLEMENT, "Core source-time profile."),
     FeatureEntry("sources", "ContinuousWave", FeatureStatus.IMPLEMENT, "Core source-time profile."),
diff --git a/src/autofdtd/runtime/__init__.py b/src/autofdtd/runtime/__init__.py
index 3bdf90710bc57a51a3e5878f2ca60108c480435c..489a50203fbffb1f23ac0eec1ed672176ad37960
--- a/src/autofdtd/runtime/__init__.py
+++ b/src/autofdtd/runtime/__init__.py
@@ -2,6 +2,7 @@
 
 from autofdtd.runtime.boundaries import (
     BoundaryRuntime,
+    allocate_abc_boundary_state,
     allocate_pml_boundary_state,
     apply_compiled_boundaries,
     build_boundary_runtime,
@@ -9,6 +10,7 @@
 
 __all__ = [
     "BoundaryRuntime",
+    "allocate_abc_boundary_state",
     "allocate_pml_boundary_state",
     "apply_compiled_boundaries",
     "build_boundary_runtime",
diff --git a/src/autofdtd/runtime/boundaries.py b/src/autofdtd/runtime/boundaries.py
index d6785ffa7f955b202680d7788c752d028b3e5df1..7962227ff86779268bb9a8601187abd3a24ece17
--- a/src/autofdtd/runtime/boundaries.py
+++ b/src/autofdtd/runtime/boundaries.py
@@ -14,6 +14,8 @@
     compile_boundary_spec,
 )
 from autofdtd.kernels.boundaries import (
+    ABCBoundaryState,
+    ABCFaceState,
     PMLBoundaryState,
     PMLFaceState,
     apply_boundary_stages,
@@ -22,6 +24,46 @@
 )
 
 
+def allocate_abc_boundary_state(
+    compiled_boundary_spec: CompiledBoundarySpec,
+    field_shape: tuple[int, int, int, int],
+    *,
+    dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
+) -> ABCBoundaryState:
+    """Allocate persistent first-order ABC state for all active faces."""
+
+    if len(field_shape) != 4 or field_shape[-1] != 3:
+        raise ValueError("ABC state allocation expects field shapes (nx, ny, nz, 3)")
+    electric: dict[str, ABCFaceState] = {}
+    magnetic: dict[str, ABCFaceState] = {}
+
+    for axis_boundary in compiled_boundary_spec.axes():
+        axis_index = "xyz".index(axis_boundary.axis)
+        value_shape = tuple(
+            size for index, size in enumerate(field_shape) if index != axis_index
+        )
+        for side in ("minus", "plus"):
+            edge = getattr(axis_boundary, side)
+            if edge.mode is not BoundaryMode.ABC or edge.abc is None:
+                continue
+            face_key = f"{axis_boundary.axis}.{side}"
+            electric[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+            magnetic[face_key] = ABCFaceState(
+                axis=axis_boundary.axis,
+                side=side,
+                value_shape=value_shape,
+                previous_boundary=np.zeros(value_shape, dtype=dtype),
+                previous_adjacent=np.zeros(value_shape, dtype=dtype),
+            )
+    return ABCBoundaryState(electric=electric, magnetic=magnetic)
+
+
 def allocate_pml_boundary_state(
     compiled_boundary_spec: CompiledBoundarySpec,
     field_shape: tuple[int, int, int, int],
@@ -88,20 +130,31 @@
             if field_shape is not None and self.compiled.pml_faces
             else None
         )
+        self.abc_state = (
+            allocate_abc_boundary_state(self.compiled, field_shape, dtype=field_dtype)
+            if field_shape is not None and self.compiled.abc_faces
+            else None
+        )
 
     def allocate_state(
         self,
         field_shape: tuple[int, int, int, int],
         *,
         dtype: np.dtype[np.floating] | np.dtype[np.complexfloating] = np.float64,
-    ) -> PMLBoundaryState | None:
-        """Allocate or refresh PML state for a given field layout."""
+    ) -> PMLBoundaryState | ABCBoundaryState | tuple[PMLBoundaryState | None, ABCBoundaryState | None] | None:
+        """Allocate or refresh persistent absorbing-boundary state for a field layout."""
 
-        if not self.compiled.pml_faces:
+        if self.compiled.pml_faces:
+            self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
             self.pml_state = None
-            return None
-        self.pml_state = allocate_pml_boundary_state(self.compiled, field_shape, dtype=dtype)
-        return self.pml_state
+        if self.compiled.abc_faces:
+            self.abc_state = allocate_abc_boundary_state(self.compiled, field_shape, dtype=dtype)
+        else:
+            self.abc_state = None
+        if self.pml_state is not None and self.abc_state is not None:
+            return self.pml_state, self.abc_state
+        return self.pml_state if self.pml_state is not None else self.abc_state
 
     def apply(
         self,
@@ -110,13 +163,16 @@
     ) -> tuple[np.ndarray, np.ndarray]:
         """Apply ghost-cell and PML stage updates for both field families."""
 
-        if self.compiled.pml_faces and self.pml_state is None:
+        if (self.compiled.pml_faces and self.pml_state is None) or (
+            self.compiled.abc_faces and self.abc_state is None
+        ):
             self.allocate_state(electric_field.shape, dtype=electric_field.dtype)
         return apply_boundary_stages(
             electric_field,
             magnetic_field,
             self.compiled,
             self.pml_state,
+            abc_state=self.abc_state,
         )
 
     def transform_boundary_values(
@@ -169,7 +225,14 @@
     compiled_boundary_spec: CompiledBoundarySpec,
     *,
     state: PMLBoundaryState | None = None,
+    abc_state: ABCBoundaryState | None = None,
 ) -> tuple[np.ndarray, np.ndarray]:
     """Apply boundary ghosts and PML stages using a precompiled specification."""
 
-    return apply_boundary_stages(electric_field, magnetic_field, compiled_boundary_spec, state)
+    return apply_boundary_stages(
+        electric_field,
+        magnetic_field,
+        compiled_boundary_spec,
+        state,
+        abc_state=abc_state,
+    )
diff --git a/tests/test_boundaries.py b/tests/test_boundaries.py
index 37e81ba2d38cc53394d49541ba667e34745c9271..3eb00a9e463d1ff85636cd8e8dead3dc688ed760
--- a/tests/test_boundaries.py
+++ b/tests/test_boundaries.py
@@ -4,11 +4,15 @@
 import pytest
 
 from autofdtd.api import (
+    ABCBoundary,
     Absorber,
     AbsorberParams,
     BlochBoundary,
+    Box,
+    BroadbandModeABCSpec,
     Boundary,
     BoundarySpec,
+    ModeABCBoundary,
     PECBoundary,
     PML,
     PMLParams,
@@ -19,15 +23,16 @@
 )
 from autofdtd.compiler import BoundaryMode, compile_boundary_spec
 from autofdtd.core.validation import AutoFDTDValidationWarning
-from autofdtd.ir import AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
+from autofdtd.ir import ABCBoundaryIR, AbsorberIR, BoundarySpecIR, PMLIR, StablePMLIR, simulation_to_ir
 from autofdtd.kernels import (
+    apply_abc_layers,
     apply_boundary_ghosts,
     apply_boundary_stages,
     boundary_edge_transform,
     boundary_kernel_metadata,
     symmetry_transform,
 )
-from autofdtd.runtime import allocate_pml_boundary_state, build_boundary_runtime
+from autofdtd.runtime import allocate_abc_boundary_state, allocate_pml_boundary_state, build_boundary_runtime
 
 
 def test_boundary_coerces_periodic_opposite_conductor_with_warning() -> None:
@@ -65,6 +70,22 @@
     assert simulation.boundary_spec["z"]["plus"]["type"] == "PMCBoundary"
 
 
+def test_simulation_normalizes_abc_boundary_subset() -> None:
+    simulation = Simulation(
+        size=(1.0, 1.0, 1.0),
+        run_time=1.0,
+        boundary_spec=BoundarySpec(
+            x=Boundary.abc(permittivity=2.25, conductivity=0.02),
+            y=Boundary.periodic(),
+            z=Boundary.pec(),
+        ),
+    )
+
+    assert simulation.boundary_spec["x"]["minus"].type == "ABCBoundary"
+    assert simulation.boundary_spec["x"]["minus"].permittivity == pytest.approx(2.25)
+    assert simulation.boundary_spec["x"]["plus"].conductivity == pytest.approx(0.02)
+
+
 def test_compile_boundary_spec_builds_runtime_modes_for_absorbing_variants() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(
@@ -110,6 +131,47 @@
     assert compiled.symmetry_axes[1].magnetic_signs == (1, 1, -1)
 
 
+def test_compile_boundary_spec_builds_first_order_abc_subset() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.01), y=Boundary.periodic()),
+        dt=0.25,
+        grid_spacing=(0.5, 0.5, 0.5),
+    )
+
+    assert compiled.x.exchange_kind == "abc"
+    assert compiled.x.minus.mode is BoundaryMode.ABC
+    assert compiled.abc_axes == ("x",)
+    assert compiled.abc_faces == ("x.minus", "x.plus")
+    assert compiled.x.minus.abc is not None
+    assert compiled.x.minus.abc.effective_permittivity == pytest.approx(4.0)
+    assert compiled.x.minus.abc.effective_conductivity == pytest.approx(0.01)
+    assert compiled.stage_order[:4] == (
+        "electric_boundary",
+        "magnetic_boundary",
+        "electric_abc",
+        "magnetic_abc",
+    )
+
+
+def test_compile_boundary_spec_requires_explicit_permittivity_for_abc_subset() -> None:
+    with pytest.raises(ValueError, match="requires an explicit permittivity"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.abc()))
+
+
+def test_mode_abc_boundary_remains_deferred_in_validation_and_runtime_compilation() -> None:
+    plane = Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0))
+
+    with pytest.raises(Exception, match="deferred feature 'ModeABCBoundary'"):
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.mode_abc(plane=plane)),
+        )
+
+    with pytest.raises(ValueError, match="runtime compilation is deferred"):
+        compile_boundary_spec(BoundarySpec(x=Boundary.mode_abc(plane=plane)))
+
+
 def test_boundary_kernels_apply_periodic_and_reflective_ghost_updates() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.bloch(0.25), y=Boundary.pec(), z=Boundary.pmc())
@@ -161,6 +223,7 @@
         "bloch",
         "pec",
         "pmc",
+        "abc",
         "pml",
         "stable_pml",
         "absorber",
@@ -171,6 +234,22 @@
     assert simulation_ir.boundary_spec.active_symmetry_axes == ("x", "z")
 
 
+def test_abc_public_api_and_ir_lowering_are_typed() -> None:
+    simulation_ir = simulation_to_ir(
+        Simulation(
+            size=(1.0, 1.0, 1.0),
+            run_time=1.0,
+            boundary_spec=BoundarySpec(x=Boundary.abc(permittivity=2.25, conductivity=0.01)),
+        )
+    )
+
+    assert isinstance(simulation_ir.boundary_spec, BoundarySpecIR)
+    assert isinstance(simulation_ir.boundary_spec.x.minus, ABCBoundaryIR)
+    assert simulation_ir.boundary_spec.x.minus.permittivity == pytest.approx(2.25)
+    assert simulation_ir.boundary_spec.abc_axes == ("x",)
+    assert simulation_ir.boundary_spec.abc_faces == ("x.minus", "x.plus")
+
+
 def test_pml_public_api_and_ir_lowering_are_typed() -> None:
     simulation_ir = simulation_to_ir(
         Simulation(
@@ -274,6 +353,24 @@
     assert np.all(state.magnetic["x.plus"].memory == 0.0)
 
 
+def test_abc_state_allocation_and_stage_update_track_active_faces() -> None:
+    compiled = compile_boundary_spec(
+        BoundarySpec(x=Boundary.abc(permittivity=4.0, conductivity=0.02), y=Boundary.periodic()),
+        dt=1.0e-12,
+    )
+    state = allocate_abc_boundary_state(compiled, (8, 6, 4, 3), dtype=np.float64)
+    electric = np.zeros((8, 6, 4, 3), dtype=np.float64)
+    electric[1, 2:4, 1:3, 0] = 1.0
+
+    updated = apply_abc_layers(electric, compiled, state, field_family="electric")
+
+    assert set(state.electric) == {"x.minus", "x.plus"}
+    assert state.electric["x.minus"].value_shape == (6, 4, 3)
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) > 0.0
+    assert np.max(np.abs(updated[0, 2:4, 1:3, 0])) < 1.0
+    assert np.max(np.abs(state.electric["x.minus"].previous_adjacent[..., 0])) == pytest.approx(1.0)
+
+
 def test_pml_boundary_stages_absorb_boundary_energy() -> None:
     compiled = compile_boundary_spec(
         BoundarySpec(x=Boundary.pml(num_layers=4), y=Boundary.periodic(), z=Boundary.periodic()),
@@ -358,3 +455,10 @@
 
     assert isinstance(Boundary.stable_pml().minus, StablePML)
     assert isinstance(Boundary.absorber().plus, Absorber)
+    assert isinstance(Boundary.abc(permittivity=2.0).minus, ABCBoundary)
+    mode_boundary = Boundary.mode_abc(
+        plane=Box(center=(0.0, 0.0, 0.0), size=(0.0, 1.0, 1.0)),
+        freq_spec=BroadbandModeABCSpec(frequency_range=(1.0e14, 1.2e14)),
+    )
+    assert isinstance(mode_boundary.plus, ModeABCBoundary)
+    assert "deferred" in mode_boundary.plus.phase1_policy

tokens used
346,821
{"task_id":"task-023","status":"completed","summary":"Implemented a practical Phase 1 `ABCBoundary` subset with typed API/IR/compiler/runtime support, while making `ModeABCBoundary` and related mode-fit surfaces parseable but explicitly deferred.","key_findings":["`ABCBoundary` is now part of the implemented public boundary surface and compiles into explicit first-order recurrence coefficients plus persistent per-face ABC state for staged ghost updates.","Boundary runtime and IR metadata now expose `abc_axes` and `abc_faces`, keeping the mathematical boundary family distinct from scheduling and later chunk-planning concerns.","`ModeABCBoundary`, `BroadbandModeABCSpec`, and `BroadbandModeABCFitterParam` are now mapped compatibility surfaces, but Simulation validation and runtime compilation fail clearly because mode-solver dependencies are not available in Phase 1.","Docs, examples, tests, working memory, and the task result artifact were updated to capture the implemented ABC subset and the explicit defer policy for mode-based ABC."],"artifacts":[{"path":"/workspace/autofdtd/src/autofdtd/boundaries/models.py","description":"Public boundary models now include `ABCBoundary`, deferred `ModeABCBoundary` compatibility types, and convenience constructors on `Boundary` and `BoundarySpec`."},{"path":"/workspace/autofdtd/src/autofdtd/compiler/boundaries.py","description":"Boundary compilation now prepares first-order ABC coefficients, `abc_*` face metadata, and explicit deferred errors for mode-based ABC."},{"path":"/workspace/autofdtd/src/autofdtd/kernels/boundaries.py","description":"Boundary kernels now provide persistent ABC state models and staged electric/magnetic ABC ghost updates."},{"path":"/workspace/autofdtd/src/autofdtd/runtime/boundaries.py","description":"Runtime orchestration now allocates ABC state alongside PML state and applies both staged boundary families."},{"path":"/workspace/autofdtd/src/autofdtd/ir/models.py","description":"IR lowering now emits typed `ABCBoundaryIR` payloads and `BoundarySpecIR` `abc_axes` / `abc_faces` metadata."},{"path":"/workspace/autofdtd/tests/test_boundaries.py","description":"Focused tests cover ABC normalization, compilation, staged updates, IR lowering, and the explicit `ModeABCBoundary` defer behavior."},{"path":"/workspace/autofdtd/docs/boundaries.md","description":"Boundary documentation now explains the implemented ABC subset and the remaining `ModeABCBoundary` defer policy."},{"path":"/workspace/autofdtd/phase1/runs/task-023-attempt-001/result.json","description":"Structured task result matching the Phase 1 output schema."}],"error_summary":"","follow_up_notes":"Automatic boundary-medium inference for `ABCBoundary` and all mode-based ABC behavior should build on the new `abc_faces` staged-boundary seam once scene-material sampling and mode-solver tasks land.","next_action":"none"}
